<?php
// auto-generated by sfPropelCrud
// date: 2008/05/08 17:12:09
$path_theme = sfConfig::get('theme_simad');
$base_path = sfConfig::get('base_simad');

use_helper('Object');
?>
<div class="row">
  <div class="col-md-12">
    <!-- Contenedor Pagina -->
    <div class="panel panel-gradient" data-collapsed="0">      
      <div class="panel-heading">
        <div class="panel-title">
          Publicar Acto Administrativo
        </div>
      </div>

      <!-- Contenedor Contenido Formulario-->
      <div class="panel-body">
        <?php 
            echo form_tag('acto_administrativo/updateCartelera', array('name'=>'formCart', 'id'=>'formCart', 'role' => 'form', 'class' => 'form-horizontal form-groups-bordered validate')); 
            echo object_input_hidden_tag($acto_administrativo, 'getActoadministrativoId');
        ?>
        
        <div class="form-group">
          <!-- Observaciones -->
          <label for="lbgetObservaciones" class="col-sm-2 control-label">Observaciones Publicaci&oacute;n:</label>
          <div class="col-sm-6">    
            <textarea name="obs_publicacion" id="obs_publicacion" class="form-control input-sm required" aria-required="true"></textarea>
          </div>
        </div>
        <div class="form-group">
          <!-- Botonera -->
          <div class="col-sm-offset-4 col-sm-5">
            <button type="button" id="btn_pubcart" class="btn btn-blue btn-icon">Publicar Documento</button>
            <?php echo button_to('Cancelar','acto_administrativo/show?actoadministrativo_id='.$acto_administrativo->getPrimaryKey(),array('class' => 'btn btn-red')); ?>
          </div>
        </div>
        <div class="clear"></div>
      </form>
    </div>
    </div>
  </div>
</div>  

<script type="text/javascript">
  jQuery(document).ready(function()
  {
      jQuery('#btn_pubcart').on( "click", function(event)
      {
        event.preventDefault();
        const form = jQuery('#formCart');

        javascript:jQuery.LoadingStructData();   
        jQuery.ajax(
        {
            type: 'POST',          
            url: '<?php echo url_for('acto_administrativo/updateCartelera');?>',         
            data: form.serialize(),
            success: function(response)
            {
              if(response.status == 200)
              {
                javascript:jQuery.CloseLoadingStructData();
                toastr.success(response.mensaje);
                window.location.href = response.url_back;
              }
              else if(response.status == 403)
              {
                javascript:jQuery.CloseLoadingStructData();
                toastr.error(response.mensaje);
              }
              else if(response.status == 402)
              {
                javascript:jQuery.CloseLoadingStructData();
                toastr.error(response.mensaje);
              }
              else if(response.status == 401)
              {
                javascript:jQuery.CloseLoadingStructData();
                toastr.error(response.mensaje);
              }   
              else if(response.status == 400)
              {
                javascript:jQuery.CloseLoadingStructData();
                toastr.error(response.mensaje);
              }
            },
            error: function(jqXHR, textStatus, errorThrown) 
            {
              if (jqXHR.status == 404) 
              {
                javascript:jQuery.CloseLoadingStructData();
              }
              else if(jqXHR.status == 400)
              {
                javascript:jQuery.CloseLoadingStructData();
              } 
              else 
              {
                javascript:jQuery.CloseLoadingStructData();  
              }
            }
        });
    });
  });
</script>




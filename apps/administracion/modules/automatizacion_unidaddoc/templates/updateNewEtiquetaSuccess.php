<?php
// auto-generated by sfPropelCrud
// date: 2008/04/10 16:18:33
$path_theme = sfConfig::get('theme_simad');
$base_path = sfConfig::get('base_simad');

use_helper('Object','jQuery');

/*$resp_ietiquetas = null;
foreach ($list_ietiquetas as $row_etiqueta) {
    $resp_ietiquetas[] = array('id' => strtoupper($row_etiqueta->getNombreInterno()),'text' => $row_etiqueta->getNombreInterno());
}*/
?>
<script type="text/javascript">
    var list_ietiquetas = <?php echo json_encode($list_ietiquetas); ?>;

    $select_etiqueta = parent.jQuery('#auto_exp_etiqueta');
    var selected_eqfields = $select_etiqueta.val() || [];
    $select_etiqueta.empty();

    jQuery.each(list_ietiquetas, function(i, item) {
        var option = new Option(item.text, item.id, false, selected_eqfields.indexOf(item.id.toString()) >= 0);
        $select_etiqueta.append(option);
    });

    $select_etiqueta.trigger('change.select2');
    //$select_etiqueta.val(null).trigger('change');
    parent.jQuery.CloseModalSIMAD();
</script>
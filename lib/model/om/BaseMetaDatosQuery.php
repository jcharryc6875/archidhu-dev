<?php


/**
 * Base class that represents a query for the 'META_DATOS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:57
 *
 * @method MetaDatosQuery orderByMetadatoId($order = Criteria::ASC) Order by the METADATO_ID column
 * @method MetaDatosQuery orderByTipodatoId($order = Criteria::ASC) Order by the TIPODATO_ID column
 * @method MetaDatosQuery orderByNombre($order = Criteria::ASC) Order by the NOMBRE column
 * @method MetaDatosQuery orderBySize($order = Criteria::ASC) Order by the SIZE column
 * @method MetaDatosQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method MetaDatosQuery orderByOrden($order = Criteria::ASC) Order by the ORDEN column
 * @method MetaDatosQuery orderByEsObligatorio($order = Criteria::ASC) Order by the ES_OBLIGATORIO column
 * @method MetaDatosQuery orderByRegex($order = Criteria::ASC) Order by the REGEX column
 * @method MetaDatosQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method MetaDatosQuery orderByFechaModificacion($order = Criteria::ASC) Order by the FECHA_MODIFICACION column
 * @method MetaDatosQuery orderByEsActivo($order = Criteria::ASC) Order by the ES_ACTIVO column
 * @method MetaDatosQuery orderByEsConsulta($order = Criteria::ASC) Order by the ES_CONSULTA column
 * @method MetaDatosQuery orderByObligatorioConsulta($order = Criteria::ASC) Order by the OBLIGATORIO_CONSULTA column
 *
 * @method MetaDatosQuery groupByMetadatoId() Group by the METADATO_ID column
 * @method MetaDatosQuery groupByTipodatoId() Group by the TIPODATO_ID column
 * @method MetaDatosQuery groupByNombre() Group by the NOMBRE column
 * @method MetaDatosQuery groupBySize() Group by the SIZE column
 * @method MetaDatosQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method MetaDatosQuery groupByOrden() Group by the ORDEN column
 * @method MetaDatosQuery groupByEsObligatorio() Group by the ES_OBLIGATORIO column
 * @method MetaDatosQuery groupByRegex() Group by the REGEX column
 * @method MetaDatosQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method MetaDatosQuery groupByFechaModificacion() Group by the FECHA_MODIFICACION column
 * @method MetaDatosQuery groupByEsActivo() Group by the ES_ACTIVO column
 * @method MetaDatosQuery groupByEsConsulta() Group by the ES_CONSULTA column
 * @method MetaDatosQuery groupByObligatorioConsulta() Group by the OBLIGATORIO_CONSULTA column
 *
 * @method MetaDatosQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method MetaDatosQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method MetaDatosQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method MetaDatosQuery leftJoinTipoDato($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoDato relation
 * @method MetaDatosQuery rightJoinTipoDato($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoDato relation
 * @method MetaDatosQuery innerJoinTipoDato($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoDato relation
 *
 * @method MetaDatosQuery leftJoinMetadatosExpediente($relationAlias = null) Adds a LEFT JOIN clause to the query using the MetadatosExpediente relation
 * @method MetaDatosQuery rightJoinMetadatosExpediente($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MetadatosExpediente relation
 * @method MetaDatosQuery innerJoinMetadatosExpediente($relationAlias = null) Adds a INNER JOIN clause to the query using the MetadatosExpediente relation
 *
 * @method MetaDatosQuery leftJoinMetadatosSubserie($relationAlias = null) Adds a LEFT JOIN clause to the query using the MetadatosSubserie relation
 * @method MetaDatosQuery rightJoinMetadatosSubserie($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MetadatosSubserie relation
 * @method MetaDatosQuery innerJoinMetadatosSubserie($relationAlias = null) Adds a INNER JOIN clause to the query using the MetadatosSubserie relation
 *
 * @method MetaDatosQuery leftJoinMetadatosTipodoc($relationAlias = null) Adds a LEFT JOIN clause to the query using the MetadatosTipodoc relation
 * @method MetaDatosQuery rightJoinMetadatosTipodoc($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MetadatosTipodoc relation
 * @method MetaDatosQuery innerJoinMetadatosTipodoc($relationAlias = null) Adds a INNER JOIN clause to the query using the MetadatosTipodoc relation
 *
 * @method MetaDatos findOne(PropelPDO $con = null) Return the first MetaDatos matching the query
 * @method MetaDatos findOneOrCreate(PropelPDO $con = null) Return the first MetaDatos matching the query, or a new MetaDatos object populated from the query conditions when no match is found
 *
 * @method MetaDatos findOneByTipodatoId(int $TIPODATO_ID) Return the first MetaDatos filtered by the TIPODATO_ID column
 * @method MetaDatos findOneByNombre(string $NOMBRE) Return the first MetaDatos filtered by the NOMBRE column
 * @method MetaDatos findOneBySize(int $SIZE) Return the first MetaDatos filtered by the SIZE column
 * @method MetaDatos findOneByDescripcion(string $DESCRIPCION) Return the first MetaDatos filtered by the DESCRIPCION column
 * @method MetaDatos findOneByOrden(int $ORDEN) Return the first MetaDatos filtered by the ORDEN column
 * @method MetaDatos findOneByEsObligatorio(int $ES_OBLIGATORIO) Return the first MetaDatos filtered by the ES_OBLIGATORIO column
 * @method MetaDatos findOneByRegex(string $REGEX) Return the first MetaDatos filtered by the REGEX column
 * @method MetaDatos findOneByFechaCreacion(string $FECHA_CREACION) Return the first MetaDatos filtered by the FECHA_CREACION column
 * @method MetaDatos findOneByFechaModificacion(string $FECHA_MODIFICACION) Return the first MetaDatos filtered by the FECHA_MODIFICACION column
 * @method MetaDatos findOneByEsActivo(int $ES_ACTIVO) Return the first MetaDatos filtered by the ES_ACTIVO column
 * @method MetaDatos findOneByEsConsulta(int $ES_CONSULTA) Return the first MetaDatos filtered by the ES_CONSULTA column
 * @method MetaDatos findOneByObligatorioConsulta(int $OBLIGATORIO_CONSULTA) Return the first MetaDatos filtered by the OBLIGATORIO_CONSULTA column
 *
 * @method array findByMetadatoId(int $METADATO_ID) Return MetaDatos objects filtered by the METADATO_ID column
 * @method array findByTipodatoId(int $TIPODATO_ID) Return MetaDatos objects filtered by the TIPODATO_ID column
 * @method array findByNombre(string $NOMBRE) Return MetaDatos objects filtered by the NOMBRE column
 * @method array findBySize(int $SIZE) Return MetaDatos objects filtered by the SIZE column
 * @method array findByDescripcion(string $DESCRIPCION) Return MetaDatos objects filtered by the DESCRIPCION column
 * @method array findByOrden(int $ORDEN) Return MetaDatos objects filtered by the ORDEN column
 * @method array findByEsObligatorio(int $ES_OBLIGATORIO) Return MetaDatos objects filtered by the ES_OBLIGATORIO column
 * @method array findByRegex(string $REGEX) Return MetaDatos objects filtered by the REGEX column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return MetaDatos objects filtered by the FECHA_CREACION column
 * @method array findByFechaModificacion(string $FECHA_MODIFICACION) Return MetaDatos objects filtered by the FECHA_MODIFICACION column
 * @method array findByEsActivo(int $ES_ACTIVO) Return MetaDatos objects filtered by the ES_ACTIVO column
 * @method array findByEsConsulta(int $ES_CONSULTA) Return MetaDatos objects filtered by the ES_CONSULTA column
 * @method array findByObligatorioConsulta(int $OBLIGATORIO_CONSULTA) Return MetaDatos objects filtered by the OBLIGATORIO_CONSULTA column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseMetaDatosQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseMetaDatosQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'MetaDatos';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new MetaDatosQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   MetaDatosQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return MetaDatosQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof MetaDatosQuery) {
            return $criteria;
        }
        $query = new MetaDatosQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   MetaDatos|MetaDatos[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = MetaDatosPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(MetaDatosPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 MetaDatos A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByMetadatoId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 MetaDatos A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [METADATO_ID], [TIPODATO_ID], [NOMBRE], [SIZE], [DESCRIPCION], [ORDEN], [ES_OBLIGATORIO], [REGEX], [FECHA_CREACION], [FECHA_MODIFICACION], [ES_ACTIVO], [ES_CONSULTA], [OBLIGATORIO_CONSULTA] FROM [META_DATOS] WHERE [METADATO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new MetaDatos();
            $obj->hydrate($row);
            MetaDatosPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return MetaDatos|MetaDatos[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|MetaDatos[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(MetaDatosPeer::METADATO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(MetaDatosPeer::METADATO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the METADATO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByMetadatoId(1234); // WHERE METADATO_ID = 1234
     * $query->filterByMetadatoId(array(12, 34)); // WHERE METADATO_ID IN (12, 34)
     * $query->filterByMetadatoId(array('min' => 12)); // WHERE METADATO_ID >= 12
     * $query->filterByMetadatoId(array('max' => 12)); // WHERE METADATO_ID <= 12
     * </code>
     *
     * @param     mixed $metadatoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByMetadatoId($metadatoId = null, $comparison = null)
    {
        if (is_array($metadatoId)) {
            $useMinMax = false;
            if (isset($metadatoId['min'])) {
                $this->addUsingAlias(MetaDatosPeer::METADATO_ID, $metadatoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($metadatoId['max'])) {
                $this->addUsingAlias(MetaDatosPeer::METADATO_ID, $metadatoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::METADATO_ID, $metadatoId, $comparison);
    }

    /**
     * Filter the query on the TIPODATO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipodatoId(1234); // WHERE TIPODATO_ID = 1234
     * $query->filterByTipodatoId(array(12, 34)); // WHERE TIPODATO_ID IN (12, 34)
     * $query->filterByTipodatoId(array('min' => 12)); // WHERE TIPODATO_ID >= 12
     * $query->filterByTipodatoId(array('max' => 12)); // WHERE TIPODATO_ID <= 12
     * </code>
     *
     * @see       filterByTipoDato()
     *
     * @param     mixed $tipodatoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByTipodatoId($tipodatoId = null, $comparison = null)
    {
        if (is_array($tipodatoId)) {
            $useMinMax = false;
            if (isset($tipodatoId['min'])) {
                $this->addUsingAlias(MetaDatosPeer::TIPODATO_ID, $tipodatoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipodatoId['max'])) {
                $this->addUsingAlias(MetaDatosPeer::TIPODATO_ID, $tipodatoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::TIPODATO_ID, $tipodatoId, $comparison);
    }

    /**
     * Filter the query on the NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE NOMBRE = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the SIZE column
     *
     * Example usage:
     * <code>
     * $query->filterBySize(1234); // WHERE SIZE = 1234
     * $query->filterBySize(array(12, 34)); // WHERE SIZE IN (12, 34)
     * $query->filterBySize(array('min' => 12)); // WHERE SIZE >= 12
     * $query->filterBySize(array('max' => 12)); // WHERE SIZE <= 12
     * </code>
     *
     * @param     mixed $size The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterBySize($size = null, $comparison = null)
    {
        if (is_array($size)) {
            $useMinMax = false;
            if (isset($size['min'])) {
                $this->addUsingAlias(MetaDatosPeer::SIZE, $size['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($size['max'])) {
                $this->addUsingAlias(MetaDatosPeer::SIZE, $size['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::SIZE, $size, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ORDEN column
     *
     * Example usage:
     * <code>
     * $query->filterByOrden(1234); // WHERE ORDEN = 1234
     * $query->filterByOrden(array(12, 34)); // WHERE ORDEN IN (12, 34)
     * $query->filterByOrden(array('min' => 12)); // WHERE ORDEN >= 12
     * $query->filterByOrden(array('max' => 12)); // WHERE ORDEN <= 12
     * </code>
     *
     * @param     mixed $orden The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByOrden($orden = null, $comparison = null)
    {
        if (is_array($orden)) {
            $useMinMax = false;
            if (isset($orden['min'])) {
                $this->addUsingAlias(MetaDatosPeer::ORDEN, $orden['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($orden['max'])) {
                $this->addUsingAlias(MetaDatosPeer::ORDEN, $orden['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::ORDEN, $orden, $comparison);
    }

    /**
     * Filter the query on the ES_OBLIGATORIO column
     *
     * Example usage:
     * <code>
     * $query->filterByEsObligatorio(1234); // WHERE ES_OBLIGATORIO = 1234
     * $query->filterByEsObligatorio(array(12, 34)); // WHERE ES_OBLIGATORIO IN (12, 34)
     * $query->filterByEsObligatorio(array('min' => 12)); // WHERE ES_OBLIGATORIO >= 12
     * $query->filterByEsObligatorio(array('max' => 12)); // WHERE ES_OBLIGATORIO <= 12
     * </code>
     *
     * @param     mixed $esObligatorio The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByEsObligatorio($esObligatorio = null, $comparison = null)
    {
        if (is_array($esObligatorio)) {
            $useMinMax = false;
            if (isset($esObligatorio['min'])) {
                $this->addUsingAlias(MetaDatosPeer::ES_OBLIGATORIO, $esObligatorio['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($esObligatorio['max'])) {
                $this->addUsingAlias(MetaDatosPeer::ES_OBLIGATORIO, $esObligatorio['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::ES_OBLIGATORIO, $esObligatorio, $comparison);
    }

    /**
     * Filter the query on the REGEX column
     *
     * Example usage:
     * <code>
     * $query->filterByRegex('fooValue');   // WHERE REGEX = 'fooValue'
     * $query->filterByRegex('%fooValue%'); // WHERE REGEX LIKE '%fooValue%'
     * </code>
     *
     * @param     string $regex The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByRegex($regex = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($regex)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $regex)) {
                $regex = str_replace('*', '%', $regex);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::REGEX, $regex, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(MetaDatosPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(MetaDatosPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_MODIFICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaModificacion('2011-03-14'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion('now'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion(array('max' => 'yesterday')); // WHERE FECHA_MODIFICACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaModificacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByFechaModificacion($fechaModificacion = null, $comparison = null)
    {
        if (is_array($fechaModificacion)) {
            $useMinMax = false;
            if (isset($fechaModificacion['min'])) {
                $this->addUsingAlias(MetaDatosPeer::FECHA_MODIFICACION, $fechaModificacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaModificacion['max'])) {
                $this->addUsingAlias(MetaDatosPeer::FECHA_MODIFICACION, $fechaModificacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::FECHA_MODIFICACION, $fechaModificacion, $comparison);
    }

    /**
     * Filter the query on the ES_ACTIVO column
     *
     * Example usage:
     * <code>
     * $query->filterByEsActivo(1234); // WHERE ES_ACTIVO = 1234
     * $query->filterByEsActivo(array(12, 34)); // WHERE ES_ACTIVO IN (12, 34)
     * $query->filterByEsActivo(array('min' => 12)); // WHERE ES_ACTIVO >= 12
     * $query->filterByEsActivo(array('max' => 12)); // WHERE ES_ACTIVO <= 12
     * </code>
     *
     * @param     mixed $esActivo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByEsActivo($esActivo = null, $comparison = null)
    {
        if (is_array($esActivo)) {
            $useMinMax = false;
            if (isset($esActivo['min'])) {
                $this->addUsingAlias(MetaDatosPeer::ES_ACTIVO, $esActivo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($esActivo['max'])) {
                $this->addUsingAlias(MetaDatosPeer::ES_ACTIVO, $esActivo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::ES_ACTIVO, $esActivo, $comparison);
    }

    /**
     * Filter the query on the ES_CONSULTA column
     *
     * Example usage:
     * <code>
     * $query->filterByEsConsulta(1234); // WHERE ES_CONSULTA = 1234
     * $query->filterByEsConsulta(array(12, 34)); // WHERE ES_CONSULTA IN (12, 34)
     * $query->filterByEsConsulta(array('min' => 12)); // WHERE ES_CONSULTA >= 12
     * $query->filterByEsConsulta(array('max' => 12)); // WHERE ES_CONSULTA <= 12
     * </code>
     *
     * @param     mixed $esConsulta The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByEsConsulta($esConsulta = null, $comparison = null)
    {
        if (is_array($esConsulta)) {
            $useMinMax = false;
            if (isset($esConsulta['min'])) {
                $this->addUsingAlias(MetaDatosPeer::ES_CONSULTA, $esConsulta['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($esConsulta['max'])) {
                $this->addUsingAlias(MetaDatosPeer::ES_CONSULTA, $esConsulta['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::ES_CONSULTA, $esConsulta, $comparison);
    }

    /**
     * Filter the query on the OBLIGATORIO_CONSULTA column
     *
     * Example usage:
     * <code>
     * $query->filterByObligatorioConsulta(1234); // WHERE OBLIGATORIO_CONSULTA = 1234
     * $query->filterByObligatorioConsulta(array(12, 34)); // WHERE OBLIGATORIO_CONSULTA IN (12, 34)
     * $query->filterByObligatorioConsulta(array('min' => 12)); // WHERE OBLIGATORIO_CONSULTA >= 12
     * $query->filterByObligatorioConsulta(array('max' => 12)); // WHERE OBLIGATORIO_CONSULTA <= 12
     * </code>
     *
     * @param     mixed $obligatorioConsulta The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function filterByObligatorioConsulta($obligatorioConsulta = null, $comparison = null)
    {
        if (is_array($obligatorioConsulta)) {
            $useMinMax = false;
            if (isset($obligatorioConsulta['min'])) {
                $this->addUsingAlias(MetaDatosPeer::OBLIGATORIO_CONSULTA, $obligatorioConsulta['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($obligatorioConsulta['max'])) {
                $this->addUsingAlias(MetaDatosPeer::OBLIGATORIO_CONSULTA, $obligatorioConsulta['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MetaDatosPeer::OBLIGATORIO_CONSULTA, $obligatorioConsulta, $comparison);
    }

    /**
     * Filter the query by a related TipoDato object
     *
     * @param   TipoDato|PropelObjectCollection $tipoDato The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 MetaDatosQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoDato($tipoDato, $comparison = null)
    {
        if ($tipoDato instanceof TipoDato) {
            return $this
                ->addUsingAlias(MetaDatosPeer::TIPODATO_ID, $tipoDato->getTipodatoId(), $comparison);
        } elseif ($tipoDato instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(MetaDatosPeer::TIPODATO_ID, $tipoDato->toKeyValue('PrimaryKey', 'TipodatoId'), $comparison);
        } else {
            throw new PropelException('filterByTipoDato() only accepts arguments of type TipoDato or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoDato relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function joinTipoDato($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoDato');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoDato');
        }

        return $this;
    }

    /**
     * Use the TipoDato relation TipoDato object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoDatoQuery A secondary query class using the current class as primary query
     */
    public function useTipoDatoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTipoDato($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoDato', 'TipoDatoQuery');
    }

    /**
     * Filter the query by a related MetadatosExpediente object
     *
     * @param   MetadatosExpediente|PropelObjectCollection $metadatosExpediente  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 MetaDatosQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByMetadatosExpediente($metadatosExpediente, $comparison = null)
    {
        if ($metadatosExpediente instanceof MetadatosExpediente) {
            return $this
                ->addUsingAlias(MetaDatosPeer::METADATO_ID, $metadatosExpediente->getMetadatoId(), $comparison);
        } elseif ($metadatosExpediente instanceof PropelObjectCollection) {
            return $this
                ->useMetadatosExpedienteQuery()
                ->filterByPrimaryKeys($metadatosExpediente->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMetadatosExpediente() only accepts arguments of type MetadatosExpediente or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MetadatosExpediente relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function joinMetadatosExpediente($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MetadatosExpediente');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MetadatosExpediente');
        }

        return $this;
    }

    /**
     * Use the MetadatosExpediente relation MetadatosExpediente object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MetadatosExpedienteQuery A secondary query class using the current class as primary query
     */
    public function useMetadatosExpedienteQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinMetadatosExpediente($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MetadatosExpediente', 'MetadatosExpedienteQuery');
    }

    /**
     * Filter the query by a related MetadatosSubserie object
     *
     * @param   MetadatosSubserie|PropelObjectCollection $metadatosSubserie  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 MetaDatosQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByMetadatosSubserie($metadatosSubserie, $comparison = null)
    {
        if ($metadatosSubserie instanceof MetadatosSubserie) {
            return $this
                ->addUsingAlias(MetaDatosPeer::METADATO_ID, $metadatosSubserie->getMetadatoId(), $comparison);
        } elseif ($metadatosSubserie instanceof PropelObjectCollection) {
            return $this
                ->useMetadatosSubserieQuery()
                ->filterByPrimaryKeys($metadatosSubserie->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMetadatosSubserie() only accepts arguments of type MetadatosSubserie or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MetadatosSubserie relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function joinMetadatosSubserie($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MetadatosSubserie');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MetadatosSubserie');
        }

        return $this;
    }

    /**
     * Use the MetadatosSubserie relation MetadatosSubserie object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MetadatosSubserieQuery A secondary query class using the current class as primary query
     */
    public function useMetadatosSubserieQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinMetadatosSubserie($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MetadatosSubserie', 'MetadatosSubserieQuery');
    }

    /**
     * Filter the query by a related MetadatosTipodoc object
     *
     * @param   MetadatosTipodoc|PropelObjectCollection $metadatosTipodoc  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 MetaDatosQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByMetadatosTipodoc($metadatosTipodoc, $comparison = null)
    {
        if ($metadatosTipodoc instanceof MetadatosTipodoc) {
            return $this
                ->addUsingAlias(MetaDatosPeer::METADATO_ID, $metadatosTipodoc->getMetadatoId(), $comparison);
        } elseif ($metadatosTipodoc instanceof PropelObjectCollection) {
            return $this
                ->useMetadatosTipodocQuery()
                ->filterByPrimaryKeys($metadatosTipodoc->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMetadatosTipodoc() only accepts arguments of type MetadatosTipodoc or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MetadatosTipodoc relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function joinMetadatosTipodoc($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MetadatosTipodoc');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MetadatosTipodoc');
        }

        return $this;
    }

    /**
     * Use the MetadatosTipodoc relation MetadatosTipodoc object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MetadatosTipodocQuery A secondary query class using the current class as primary query
     */
    public function useMetadatosTipodocQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinMetadatosTipodoc($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MetadatosTipodoc', 'MetadatosTipodocQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   MetaDatos $metaDatos Object to remove from the list of results
     *
     * @return MetaDatosQuery The current query, for fluid interface
     */
    public function prune($metaDatos = null)
    {
        if ($metaDatos) {
            $this->addUsingAlias(MetaDatosPeer::METADATO_ID, $metaDatos->getMetadatoId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

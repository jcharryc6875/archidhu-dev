<?php


/**
 * Base class that represents a row from the 'PLANTILLASCOM_SERVICIO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * lun 03 nov 2025 21:22:50
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BasePlantillascomServicio extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'PlantillascomServicioPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        PlantillascomServicioPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the plantillascomservicio_id field.
     * @var        int
     */
    protected $plantillascomservicio_id;

    /**
     * The value for the tiposervicio_id field.
     * @var        int
     */
    protected $tiposervicio_id;

    /**
     * The value for the prioridadsolicitudservicio_id field.
     * @var        int
     */
    protected $prioridadsolicitudservicio_id;

    /**
     * The value for the plantillascom_id field.
     * @var        int
     */
    protected $plantillascom_id;

    /**
     * The value for the fecha_creacion field.
     * @var        string
     */
    protected $fecha_creacion;

    /**
     * @var        TipoServicio
     */
    protected $aTipoServicio;

    /**
     * @var        PrioridadSolicitudServicio
     */
    protected $aPrioridadSolicitudServicio;

    /**
     * @var        PlantillasCom
     */
    protected $aPlantillasCom;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * Get the [plantillascomservicio_id] column value.
     * 
     * @return int
     */
    public function getPlantillascomservicioId()
    {

        return $this->plantillascomservicio_id;
    }

    /**
     * Get the [tiposervicio_id] column value.
     * 
     * @return int
     */
    public function getTiposervicioId()
    {

        return $this->tiposervicio_id;
    }

    /**
     * Get the [prioridadsolicitudservicio_id] column value.
     * 
     * @return int
     */
    public function getPrioridadsolicitudservicioId()
    {

        return $this->prioridadsolicitudservicio_id;
    }

    /**
     * Get the [plantillascom_id] column value.
     * 
     * @return int
     */
    public function getPlantillascomId()
    {

        return $this->plantillascom_id;
    }

    /**
     * Get the [fecha_creacion] column value.
     * 
     * @return string
     */
    public function getFechaCreacion()
    {

        return $this->fecha_creacion;
    }

    /**
     * Set the value of [plantillascomservicio_id] column.
     * 
     * @param  int $v new value
     * @return PlantillascomServicio The current object (for fluent API support)
     */
    public function setPlantillascomservicioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->plantillascomservicio_id !== $v) {
            $this->plantillascomservicio_id = $v;
            $this->modifiedColumns[] = PlantillascomServicioPeer::PLANTILLASCOMSERVICIO_ID;
        }


        return $this;
    } // setPlantillascomservicioId()

    /**
     * Set the value of [tiposervicio_id] column.
     * 
     * @param  int $v new value
     * @return PlantillascomServicio The current object (for fluent API support)
     */
    public function setTiposervicioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->tiposervicio_id !== $v) {
            $this->tiposervicio_id = $v;
            $this->modifiedColumns[] = PlantillascomServicioPeer::TIPOSERVICIO_ID;
        }

        if ($this->aTipoServicio !== null && $this->aTipoServicio->getTiposervicioId() !== $v) {
            $this->aTipoServicio = null;
        }


        return $this;
    } // setTiposervicioId()

    /**
     * Set the value of [prioridadsolicitudservicio_id] column.
     * 
     * @param  int $v new value
     * @return PlantillascomServicio The current object (for fluent API support)
     */
    public function setPrioridadsolicitudservicioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->prioridadsolicitudservicio_id !== $v) {
            $this->prioridadsolicitudservicio_id = $v;
            $this->modifiedColumns[] = PlantillascomServicioPeer::PRIORIDADSOLICITUDSERVICIO_ID;
        }

        if ($this->aPrioridadSolicitudServicio !== null && $this->aPrioridadSolicitudServicio->getPrioridadsolicitudservicioId() !== $v) {
            $this->aPrioridadSolicitudServicio = null;
        }


        return $this;
    } // setPrioridadsolicitudservicioId()

    /**
     * Set the value of [plantillascom_id] column.
     * 
     * @param  int $v new value
     * @return PlantillascomServicio The current object (for fluent API support)
     */
    public function setPlantillascomId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->plantillascom_id !== $v) {
            $this->plantillascom_id = $v;
            $this->modifiedColumns[] = PlantillascomServicioPeer::PLANTILLASCOM_ID;
        }

        if ($this->aPlantillasCom !== null && $this->aPlantillasCom->getPlantillascomId() !== $v) {
            $this->aPlantillasCom = null;
        }


        return $this;
    } // setPlantillascomId()

    /**
     * Set the value of [fecha_creacion] column.
     * 
     * @param  string $v new value
     * @return PlantillascomServicio The current object (for fluent API support)
     */
    public function setFechaCreacion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->fecha_creacion !== $v) {
            $this->fecha_creacion = $v;
            $this->modifiedColumns[] = PlantillascomServicioPeer::FECHA_CREACION;
        }


        return $this;
    } // setFechaCreacion()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->plantillascomservicio_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->tiposervicio_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->prioridadsolicitudservicio_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->plantillascom_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->fecha_creacion = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 5; // 5 = PlantillascomServicioPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating PlantillascomServicio object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aTipoServicio !== null && $this->tiposervicio_id !== $this->aTipoServicio->getTiposervicioId()) {
            $this->aTipoServicio = null;
        }
        if ($this->aPrioridadSolicitudServicio !== null && $this->prioridadsolicitudservicio_id !== $this->aPrioridadSolicitudServicio->getPrioridadsolicitudservicioId()) {
            $this->aPrioridadSolicitudServicio = null;
        }
        if ($this->aPlantillasCom !== null && $this->plantillascom_id !== $this->aPlantillasCom->getPlantillascomId()) {
            $this->aPlantillasCom = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(PlantillascomServicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = PlantillascomServicioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aTipoServicio = null;
            $this->aPrioridadSolicitudServicio = null;
            $this->aPlantillasCom = null;
        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(PlantillascomServicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = PlantillascomServicioQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasePlantillascomServicio:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasePlantillascomServicio:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(PlantillascomServicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasePlantillascomServicio:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasePlantillascomServicio:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                PlantillascomServicioPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aTipoServicio !== null) {
                if ($this->aTipoServicio->isModified() || $this->aTipoServicio->isNew()) {
                    $affectedRows += $this->aTipoServicio->save($con);
                }
                $this->setTipoServicio($this->aTipoServicio);
            }

            if ($this->aPrioridadSolicitudServicio !== null) {
                if ($this->aPrioridadSolicitudServicio->isModified() || $this->aPrioridadSolicitudServicio->isNew()) {
                    $affectedRows += $this->aPrioridadSolicitudServicio->save($con);
                }
                $this->setPrioridadSolicitudServicio($this->aPrioridadSolicitudServicio);
            }

            if ($this->aPlantillasCom !== null) {
                if ($this->aPlantillasCom->isModified() || $this->aPlantillasCom->isNew()) {
                    $affectedRows += $this->aPlantillasCom->save($con);
                }
                $this->setPlantillasCom($this->aPlantillasCom);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $criteria = $this->buildCriteria();
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aTipoServicio !== null) {
                if (!$this->aTipoServicio->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aTipoServicio->getValidationFailures());
                }
            }

            if ($this->aPrioridadSolicitudServicio !== null) {
                if (!$this->aPrioridadSolicitudServicio->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aPrioridadSolicitudServicio->getValidationFailures());
                }
            }

            if ($this->aPlantillasCom !== null) {
                if (!$this->aPlantillasCom->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aPlantillasCom->getValidationFailures());
                }
            }


            if (($retval = PlantillascomServicioPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }



            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = PlantillascomServicioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getPlantillascomservicioId();
                break;
            case 1:
                return $this->getTiposervicioId();
                break;
            case 2:
                return $this->getPrioridadsolicitudservicioId();
                break;
            case 3:
                return $this->getPlantillascomId();
                break;
            case 4:
                return $this->getFechaCreacion();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['PlantillascomServicio'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['PlantillascomServicio'][$this->getPrimaryKey()] = true;
        $keys = PlantillascomServicioPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getPlantillascomservicioId(),
            $keys[1] => $this->getTiposervicioId(),
            $keys[2] => $this->getPrioridadsolicitudservicioId(),
            $keys[3] => $this->getPlantillascomId(),
            $keys[4] => $this->getFechaCreacion(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aTipoServicio) {
                $result['TipoServicio'] = $this->aTipoServicio->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aPrioridadSolicitudServicio) {
                $result['PrioridadSolicitudServicio'] = $this->aPrioridadSolicitudServicio->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aPlantillasCom) {
                $result['PlantillasCom'] = $this->aPlantillasCom->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = PlantillascomServicioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setPlantillascomservicioId($value);
                break;
            case 1:
                $this->setTiposervicioId($value);
                break;
            case 2:
                $this->setPrioridadsolicitudservicioId($value);
                break;
            case 3:
                $this->setPlantillascomId($value);
                break;
            case 4:
                $this->setFechaCreacion($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = PlantillascomServicioPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setPlantillascomservicioId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setTiposervicioId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setPrioridadsolicitudservicioId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setPlantillascomId($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setFechaCreacion($arr[$keys[4]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(PlantillascomServicioPeer::DATABASE_NAME);

        if ($this->isColumnModified(PlantillascomServicioPeer::PLANTILLASCOMSERVICIO_ID)) $criteria->add(PlantillascomServicioPeer::PLANTILLASCOMSERVICIO_ID, $this->plantillascomservicio_id);
        if ($this->isColumnModified(PlantillascomServicioPeer::TIPOSERVICIO_ID)) $criteria->add(PlantillascomServicioPeer::TIPOSERVICIO_ID, $this->tiposervicio_id);
        if ($this->isColumnModified(PlantillascomServicioPeer::PRIORIDADSOLICITUDSERVICIO_ID)) $criteria->add(PlantillascomServicioPeer::PRIORIDADSOLICITUDSERVICIO_ID, $this->prioridadsolicitudservicio_id);
        if ($this->isColumnModified(PlantillascomServicioPeer::PLANTILLASCOM_ID)) $criteria->add(PlantillascomServicioPeer::PLANTILLASCOM_ID, $this->plantillascom_id);
        if ($this->isColumnModified(PlantillascomServicioPeer::FECHA_CREACION)) $criteria->add(PlantillascomServicioPeer::FECHA_CREACION, $this->fecha_creacion);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(PlantillascomServicioPeer::DATABASE_NAME);
        $criteria->add(PlantillascomServicioPeer::PLANTILLASCOMSERVICIO_ID, $this->plantillascomservicio_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getPlantillascomservicioId();
    }

    /**
     * Generic method to set the primary key (plantillascomservicio_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setPlantillascomservicioId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getPlantillascomservicioId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of PlantillascomServicio (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setTiposervicioId($this->getTiposervicioId());
        $copyObj->setPrioridadsolicitudservicioId($this->getPrioridadsolicitudservicioId());
        $copyObj->setPlantillascomId($this->getPlantillascomId());
        $copyObj->setFechaCreacion($this->getFechaCreacion());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setPlantillascomservicioId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return PlantillascomServicio Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return PlantillascomServicioPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new PlantillascomServicioPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a TipoServicio object.
     *
     * @param                  TipoServicio $v
     * @return PlantillascomServicio The current object (for fluent API support)
     * @throws PropelException
     */
    public function setTipoServicio(TipoServicio $v = null)
    {
        if ($v === null) {
            $this->setTiposervicioId(NULL);
        } else {
            $this->setTiposervicioId($v->getTiposervicioId());
        }

        $this->aTipoServicio = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the TipoServicio object, it will not be re-added.
        if ($v !== null) {
            $v->addPlantillascomServicio($this);
        }


        return $this;
    }


    /**
     * Get the associated TipoServicio object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return TipoServicio The associated TipoServicio object.
     * @throws PropelException
     */
    public function getTipoServicio(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aTipoServicio === null && ($this->tiposervicio_id !== null) && $doQuery) {
            $this->aTipoServicio = TipoServicioQuery::create()->findPk($this->tiposervicio_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aTipoServicio->addPlantillascomServicios($this);
             */
        }

        return $this->aTipoServicio;
    }

    /**
     * Declares an association between this object and a PrioridadSolicitudServicio object.
     *
     * @param                  PrioridadSolicitudServicio $v
     * @return PlantillascomServicio The current object (for fluent API support)
     * @throws PropelException
     */
    public function setPrioridadSolicitudServicio(PrioridadSolicitudServicio $v = null)
    {
        if ($v === null) {
            $this->setPrioridadsolicitudservicioId(NULL);
        } else {
            $this->setPrioridadsolicitudservicioId($v->getPrioridadsolicitudservicioId());
        }

        $this->aPrioridadSolicitudServicio = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the PrioridadSolicitudServicio object, it will not be re-added.
        if ($v !== null) {
            $v->addPlantillascomServicio($this);
        }


        return $this;
    }


    /**
     * Get the associated PrioridadSolicitudServicio object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return PrioridadSolicitudServicio The associated PrioridadSolicitudServicio object.
     * @throws PropelException
     */
    public function getPrioridadSolicitudServicio(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aPrioridadSolicitudServicio === null && ($this->prioridadsolicitudservicio_id !== null) && $doQuery) {
            $this->aPrioridadSolicitudServicio = PrioridadSolicitudServicioQuery::create()->findPk($this->prioridadsolicitudservicio_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aPrioridadSolicitudServicio->addPlantillascomServicios($this);
             */
        }

        return $this->aPrioridadSolicitudServicio;
    }

    /**
     * Declares an association between this object and a PlantillasCom object.
     *
     * @param                  PlantillasCom $v
     * @return PlantillascomServicio The current object (for fluent API support)
     * @throws PropelException
     */
    public function setPlantillasCom(PlantillasCom $v = null)
    {
        if ($v === null) {
            $this->setPlantillascomId(NULL);
        } else {
            $this->setPlantillascomId($v->getPlantillascomId());
        }

        $this->aPlantillasCom = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the PlantillasCom object, it will not be re-added.
        if ($v !== null) {
            $v->addPlantillascomServicio($this);
        }


        return $this;
    }


    /**
     * Get the associated PlantillasCom object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return PlantillasCom The associated PlantillasCom object.
     * @throws PropelException
     */
    public function getPlantillasCom(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aPlantillasCom === null && ($this->plantillascom_id !== null) && $doQuery) {
            $this->aPlantillasCom = PlantillasComQuery::create()->findPk($this->plantillascom_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aPlantillasCom->addPlantillascomServicios($this);
             */
        }

        return $this->aPlantillasCom;
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->plantillascomservicio_id = null;
        $this->tiposervicio_id = null;
        $this->prioridadsolicitudservicio_id = null;
        $this->plantillascom_id = null;
        $this->fecha_creacion = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->aTipoServicio instanceof Persistent) {
              $this->aTipoServicio->clearAllReferences($deep);
            }
            if ($this->aPrioridadSolicitudServicio instanceof Persistent) {
              $this->aPrioridadSolicitudServicio->clearAllReferences($deep);
            }
            if ($this->aPlantillasCom instanceof Persistent) {
              $this->aPlantillasCom->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        $this->aTipoServicio = null;
        $this->aPrioridadSolicitudServicio = null;
        $this->aPlantillasCom = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(PlantillascomServicioPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BasePlantillascomServicio:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

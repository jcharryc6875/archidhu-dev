<?php


/**
 * Base class that represents a query for the 'REDIRECCION' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:42
 *
 * @method RedireccionQuery orderByRedireccionId($order = Criteria::ASC) Order by the REDIRECCION_ID column
 * @method RedireccionQuery orderByEstadoredireccionId($order = Criteria::ASC) Order by the ESTADOREDIRECCION_ID column
 * @method RedireccionQuery orderByMotivo($order = Criteria::ASC) Order by the MOTIVO column
 * @method RedireccionQuery orderByFechaInicial($order = Criteria::ASC) Order by the FECHA_INICIAL column
 * @method RedireccionQuery orderByFechaFinal($order = Criteria::ASC) Order by the FECHA_FINAL column
 *
 * @method RedireccionQuery groupByRedireccionId() Group by the REDIRECCION_ID column
 * @method RedireccionQuery groupByEstadoredireccionId() Group by the ESTADOREDIRECCION_ID column
 * @method RedireccionQuery groupByMotivo() Group by the MOTIVO column
 * @method RedireccionQuery groupByFechaInicial() Group by the FECHA_INICIAL column
 * @method RedireccionQuery groupByFechaFinal() Group by the FECHA_FINAL column
 *
 * @method RedireccionQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method RedireccionQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method RedireccionQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method RedireccionQuery leftJoinEstadoRedireccion($relationAlias = null) Adds a LEFT JOIN clause to the query using the EstadoRedireccion relation
 * @method RedireccionQuery rightJoinEstadoRedireccion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EstadoRedireccion relation
 * @method RedireccionQuery innerJoinEstadoRedireccion($relationAlias = null) Adds a INNER JOIN clause to the query using the EstadoRedireccion relation
 *
 * @method RedireccionQuery leftJoinRedireccionUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the RedireccionUsuario relation
 * @method RedireccionQuery rightJoinRedireccionUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the RedireccionUsuario relation
 * @method RedireccionQuery innerJoinRedireccionUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the RedireccionUsuario relation
 *
 * @method Redireccion findOne(PropelPDO $con = null) Return the first Redireccion matching the query
 * @method Redireccion findOneOrCreate(PropelPDO $con = null) Return the first Redireccion matching the query, or a new Redireccion object populated from the query conditions when no match is found
 *
 * @method Redireccion findOneByEstadoredireccionId(int $ESTADOREDIRECCION_ID) Return the first Redireccion filtered by the ESTADOREDIRECCION_ID column
 * @method Redireccion findOneByMotivo(string $MOTIVO) Return the first Redireccion filtered by the MOTIVO column
 * @method Redireccion findOneByFechaInicial(string $FECHA_INICIAL) Return the first Redireccion filtered by the FECHA_INICIAL column
 * @method Redireccion findOneByFechaFinal(string $FECHA_FINAL) Return the first Redireccion filtered by the FECHA_FINAL column
 *
 * @method array findByRedireccionId(int $REDIRECCION_ID) Return Redireccion objects filtered by the REDIRECCION_ID column
 * @method array findByEstadoredireccionId(int $ESTADOREDIRECCION_ID) Return Redireccion objects filtered by the ESTADOREDIRECCION_ID column
 * @method array findByMotivo(string $MOTIVO) Return Redireccion objects filtered by the MOTIVO column
 * @method array findByFechaInicial(string $FECHA_INICIAL) Return Redireccion objects filtered by the FECHA_INICIAL column
 * @method array findByFechaFinal(string $FECHA_FINAL) Return Redireccion objects filtered by the FECHA_FINAL column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseRedireccionQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseRedireccionQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Redireccion';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new RedireccionQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   RedireccionQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return RedireccionQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof RedireccionQuery) {
            return $criteria;
        }
        $query = new RedireccionQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Redireccion|Redireccion[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = RedireccionPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(RedireccionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Redireccion A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByRedireccionId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Redireccion A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [REDIRECCION_ID], [ESTADOREDIRECCION_ID], [MOTIVO], [FECHA_INICIAL], [FECHA_FINAL] FROM [REDIRECCION] WHERE [REDIRECCION_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Redireccion();
            $obj->hydrate($row);
            RedireccionPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Redireccion|Redireccion[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Redireccion[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the REDIRECCION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRedireccionId(1234); // WHERE REDIRECCION_ID = 1234
     * $query->filterByRedireccionId(array(12, 34)); // WHERE REDIRECCION_ID IN (12, 34)
     * $query->filterByRedireccionId(array('min' => 12)); // WHERE REDIRECCION_ID >= 12
     * $query->filterByRedireccionId(array('max' => 12)); // WHERE REDIRECCION_ID <= 12
     * </code>
     *
     * @param     mixed $redireccionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByRedireccionId($redireccionId = null, $comparison = null)
    {
        if (is_array($redireccionId)) {
            $useMinMax = false;
            if (isset($redireccionId['min'])) {
                $this->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $redireccionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($redireccionId['max'])) {
                $this->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $redireccionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $redireccionId, $comparison);
    }

    /**
     * Filter the query on the ESTADOREDIRECCION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoredireccionId(1234); // WHERE ESTADOREDIRECCION_ID = 1234
     * $query->filterByEstadoredireccionId(array(12, 34)); // WHERE ESTADOREDIRECCION_ID IN (12, 34)
     * $query->filterByEstadoredireccionId(array('min' => 12)); // WHERE ESTADOREDIRECCION_ID >= 12
     * $query->filterByEstadoredireccionId(array('max' => 12)); // WHERE ESTADOREDIRECCION_ID <= 12
     * </code>
     *
     * @see       filterByEstadoRedireccion()
     *
     * @param     mixed $estadoredireccionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByEstadoredireccionId($estadoredireccionId = null, $comparison = null)
    {
        if (is_array($estadoredireccionId)) {
            $useMinMax = false;
            if (isset($estadoredireccionId['min'])) {
                $this->addUsingAlias(RedireccionPeer::ESTADOREDIRECCION_ID, $estadoredireccionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadoredireccionId['max'])) {
                $this->addUsingAlias(RedireccionPeer::ESTADOREDIRECCION_ID, $estadoredireccionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionPeer::ESTADOREDIRECCION_ID, $estadoredireccionId, $comparison);
    }

    /**
     * Filter the query on the MOTIVO column
     *
     * Example usage:
     * <code>
     * $query->filterByMotivo('fooValue');   // WHERE MOTIVO = 'fooValue'
     * $query->filterByMotivo('%fooValue%'); // WHERE MOTIVO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $motivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByMotivo($motivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($motivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $motivo)) {
                $motivo = str_replace('*', '%', $motivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RedireccionPeer::MOTIVO, $motivo, $comparison);
    }

    /**
     * Filter the query on the FECHA_INICIAL column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaInicial('2011-03-14'); // WHERE FECHA_INICIAL = '2011-03-14'
     * $query->filterByFechaInicial('now'); // WHERE FECHA_INICIAL = '2011-03-14'
     * $query->filterByFechaInicial(array('max' => 'yesterday')); // WHERE FECHA_INICIAL < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaInicial The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByFechaInicial($fechaInicial = null, $comparison = null)
    {
        if (is_array($fechaInicial)) {
            $useMinMax = false;
            if (isset($fechaInicial['min'])) {
                $this->addUsingAlias(RedireccionPeer::FECHA_INICIAL, $fechaInicial['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaInicial['max'])) {
                $this->addUsingAlias(RedireccionPeer::FECHA_INICIAL, $fechaInicial['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionPeer::FECHA_INICIAL, $fechaInicial, $comparison);
    }

    /**
     * Filter the query on the FECHA_FINAL column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaFinal('2011-03-14'); // WHERE FECHA_FINAL = '2011-03-14'
     * $query->filterByFechaFinal('now'); // WHERE FECHA_FINAL = '2011-03-14'
     * $query->filterByFechaFinal(array('max' => 'yesterday')); // WHERE FECHA_FINAL < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaFinal The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function filterByFechaFinal($fechaFinal = null, $comparison = null)
    {
        if (is_array($fechaFinal)) {
            $useMinMax = false;
            if (isset($fechaFinal['min'])) {
                $this->addUsingAlias(RedireccionPeer::FECHA_FINAL, $fechaFinal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaFinal['max'])) {
                $this->addUsingAlias(RedireccionPeer::FECHA_FINAL, $fechaFinal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionPeer::FECHA_FINAL, $fechaFinal, $comparison);
    }

    /**
     * Filter the query by a related EstadoRedireccion object
     *
     * @param   EstadoRedireccion|PropelObjectCollection $estadoRedireccion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RedireccionQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEstadoRedireccion($estadoRedireccion, $comparison = null)
    {
        if ($estadoRedireccion instanceof EstadoRedireccion) {
            return $this
                ->addUsingAlias(RedireccionPeer::ESTADOREDIRECCION_ID, $estadoRedireccion->getEstadoredireccionId(), $comparison);
        } elseif ($estadoRedireccion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RedireccionPeer::ESTADOREDIRECCION_ID, $estadoRedireccion->toKeyValue('PrimaryKey', 'EstadoredireccionId'), $comparison);
        } else {
            throw new PropelException('filterByEstadoRedireccion() only accepts arguments of type EstadoRedireccion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EstadoRedireccion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function joinEstadoRedireccion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EstadoRedireccion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EstadoRedireccion');
        }

        return $this;
    }

    /**
     * Use the EstadoRedireccion relation EstadoRedireccion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EstadoRedireccionQuery A secondary query class using the current class as primary query
     */
    public function useEstadoRedireccionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEstadoRedireccion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EstadoRedireccion', 'EstadoRedireccionQuery');
    }

    /**
     * Filter the query by a related RedireccionUsuario object
     *
     * @param   RedireccionUsuario|PropelObjectCollection $redireccionUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RedireccionQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRedireccionUsuario($redireccionUsuario, $comparison = null)
    {
        if ($redireccionUsuario instanceof RedireccionUsuario) {
            return $this
                ->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $redireccionUsuario->getRedireccionId(), $comparison);
        } elseif ($redireccionUsuario instanceof PropelObjectCollection) {
            return $this
                ->useRedireccionUsuarioQuery()
                ->filterByPrimaryKeys($redireccionUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRedireccionUsuario() only accepts arguments of type RedireccionUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the RedireccionUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function joinRedireccionUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('RedireccionUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'RedireccionUsuario');
        }

        return $this;
    }

    /**
     * Use the RedireccionUsuario relation RedireccionUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RedireccionUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useRedireccionUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinRedireccionUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'RedireccionUsuario', 'RedireccionUsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Redireccion $redireccion Object to remove from the list of results
     *
     * @return RedireccionQuery The current query, for fluid interface
     */
    public function prune($redireccion = null)
    {
        if ($redireccion) {
            $this->addUsingAlias(RedireccionPeer::REDIRECCION_ID, $redireccion->getRedireccionId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a query for the 'TIPO_CONTROL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:59
 *
 * @method TipoControlQuery orderByTipocontrolId($order = Criteria::ASC) Order by the TIPOCONTROL_ID column
 * @method TipoControlQuery orderByModuloId($order = Criteria::ASC) Order by the MODULO_ID column
 * @method TipoControlQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 *
 * @method TipoControlQuery groupByTipocontrolId() Group by the TIPOCONTROL_ID column
 * @method TipoControlQuery groupByModuloId() Group by the MODULO_ID column
 * @method TipoControlQuery groupByDescripcion() Group by the DESCRIPCION column
 *
 * @method TipoControlQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TipoControlQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TipoControlQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TipoControlQuery leftJoinModulo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Modulo relation
 * @method TipoControlQuery rightJoinModulo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Modulo relation
 * @method TipoControlQuery innerJoinModulo($relationAlias = null) Adds a INNER JOIN clause to the query using the Modulo relation
 *
 * @method TipoControlQuery leftJoinNovedades($relationAlias = null) Adds a LEFT JOIN clause to the query using the Novedades relation
 * @method TipoControlQuery rightJoinNovedades($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Novedades relation
 * @method TipoControlQuery innerJoinNovedades($relationAlias = null) Adds a INNER JOIN clause to the query using the Novedades relation
 *
 * @method TipoControl findOne(PropelPDO $con = null) Return the first TipoControl matching the query
 * @method TipoControl findOneOrCreate(PropelPDO $con = null) Return the first TipoControl matching the query, or a new TipoControl object populated from the query conditions when no match is found
 *
 * @method TipoControl findOneByModuloId(int $MODULO_ID) Return the first TipoControl filtered by the MODULO_ID column
 * @method TipoControl findOneByDescripcion(string $DESCRIPCION) Return the first TipoControl filtered by the DESCRIPCION column
 *
 * @method array findByTipocontrolId(int $TIPOCONTROL_ID) Return TipoControl objects filtered by the TIPOCONTROL_ID column
 * @method array findByModuloId(int $MODULO_ID) Return TipoControl objects filtered by the MODULO_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return TipoControl objects filtered by the DESCRIPCION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTipoControlQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTipoControlQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'TipoControl';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TipoControlQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   TipoControlQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TipoControlQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TipoControlQuery) {
            return $criteria;
        }
        $query = new TipoControlQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   TipoControl|TipoControl[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TipoControlPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TipoControlPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoControl A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByTipocontrolId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoControl A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [TIPOCONTROL_ID], [MODULO_ID], [DESCRIPCION] FROM [TIPO_CONTROL] WHERE [TIPOCONTROL_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new TipoControl();
            $obj->hydrate($row);
            TipoControlPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return TipoControl|TipoControl[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|TipoControl[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the TIPOCONTROL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipocontrolId(1234); // WHERE TIPOCONTROL_ID = 1234
     * $query->filterByTipocontrolId(array(12, 34)); // WHERE TIPOCONTROL_ID IN (12, 34)
     * $query->filterByTipocontrolId(array('min' => 12)); // WHERE TIPOCONTROL_ID >= 12
     * $query->filterByTipocontrolId(array('max' => 12)); // WHERE TIPOCONTROL_ID <= 12
     * </code>
     *
     * @param     mixed $tipocontrolId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function filterByTipocontrolId($tipocontrolId = null, $comparison = null)
    {
        if (is_array($tipocontrolId)) {
            $useMinMax = false;
            if (isset($tipocontrolId['min'])) {
                $this->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $tipocontrolId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipocontrolId['max'])) {
                $this->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $tipocontrolId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $tipocontrolId, $comparison);
    }

    /**
     * Filter the query on the MODULO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByModuloId(1234); // WHERE MODULO_ID = 1234
     * $query->filterByModuloId(array(12, 34)); // WHERE MODULO_ID IN (12, 34)
     * $query->filterByModuloId(array('min' => 12)); // WHERE MODULO_ID >= 12
     * $query->filterByModuloId(array('max' => 12)); // WHERE MODULO_ID <= 12
     * </code>
     *
     * @see       filterByModulo()
     *
     * @param     mixed $moduloId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function filterByModuloId($moduloId = null, $comparison = null)
    {
        if (is_array($moduloId)) {
            $useMinMax = false;
            if (isset($moduloId['min'])) {
                $this->addUsingAlias(TipoControlPeer::MODULO_ID, $moduloId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($moduloId['max'])) {
                $this->addUsingAlias(TipoControlPeer::MODULO_ID, $moduloId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoControlPeer::MODULO_ID, $moduloId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoControlPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query by a related Modulo object
     *
     * @param   Modulo|PropelObjectCollection $modulo The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoControlQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByModulo($modulo, $comparison = null)
    {
        if ($modulo instanceof Modulo) {
            return $this
                ->addUsingAlias(TipoControlPeer::MODULO_ID, $modulo->getModuloId(), $comparison);
        } elseif ($modulo instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TipoControlPeer::MODULO_ID, $modulo->toKeyValue('PrimaryKey', 'ModuloId'), $comparison);
        } else {
            throw new PropelException('filterByModulo() only accepts arguments of type Modulo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Modulo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function joinModulo($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Modulo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Modulo');
        }

        return $this;
    }

    /**
     * Use the Modulo relation Modulo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ModuloQuery A secondary query class using the current class as primary query
     */
    public function useModuloQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinModulo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Modulo', 'ModuloQuery');
    }

    /**
     * Filter the query by a related Novedades object
     *
     * @param   Novedades|PropelObjectCollection $novedades  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoControlQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByNovedades($novedades, $comparison = null)
    {
        if ($novedades instanceof Novedades) {
            return $this
                ->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $novedades->getTipocontrolId(), $comparison);
        } elseif ($novedades instanceof PropelObjectCollection) {
            return $this
                ->useNovedadesQuery()
                ->filterByPrimaryKeys($novedades->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByNovedades() only accepts arguments of type Novedades or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Novedades relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function joinNovedades($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Novedades');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Novedades');
        }

        return $this;
    }

    /**
     * Use the Novedades relation Novedades object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NovedadesQuery A secondary query class using the current class as primary query
     */
    public function useNovedadesQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinNovedades($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Novedades', 'NovedadesQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   TipoControl $tipoControl Object to remove from the list of results
     *
     * @return TipoControlQuery The current query, for fluid interface
     */
    public function prune($tipoControl = null)
    {
        if ($tipoControl) {
            $this->addUsingAlias(TipoControlPeer::TIPOCONTROL_ID, $tipoControl->getTipocontrolId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a query for the 'LIBRO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:12
 *
 * @method LibroQuery orderByLibroId($order = Criteria::ASC) Order by the LIBRO_ID column
 * @method LibroQuery orderByDocumentacionId($order = Criteria::ASC) Order by the DOCUMENTACION_ID column
 * @method LibroQuery orderByMencionEdicion($order = Criteria::ASC) Order by the MENCION_EDICION column
 * @method LibroQuery orderByVersion($order = Criteria::ASC) Order by the VERSION column
 * @method LibroQuery orderByPaginacion($order = Criteria::ASC) Order by the PAGINACION column
 * @method LibroQuery orderByIlustraciones($order = Criteria::ASC) Order by the ILUSTRACIONES column
 * @method LibroQuery orderByVolumen($order = Criteria::ASC) Order by the VOLUMEN column
 * @method LibroQuery orderByMaterialComplementario($order = Criteria::ASC) Order by the MATERIAL_COMPLEMENTARIO column
 * @method LibroQuery orderByIsbn($order = Criteria::ASC) Order by the ISBN column
 * @method LibroQuery orderByNotasContenido($order = Criteria::ASC) Order by the NOTAS_CONTENIDO column
 *
 * @method LibroQuery groupByLibroId() Group by the LIBRO_ID column
 * @method LibroQuery groupByDocumentacionId() Group by the DOCUMENTACION_ID column
 * @method LibroQuery groupByMencionEdicion() Group by the MENCION_EDICION column
 * @method LibroQuery groupByVersion() Group by the VERSION column
 * @method LibroQuery groupByPaginacion() Group by the PAGINACION column
 * @method LibroQuery groupByIlustraciones() Group by the ILUSTRACIONES column
 * @method LibroQuery groupByVolumen() Group by the VOLUMEN column
 * @method LibroQuery groupByMaterialComplementario() Group by the MATERIAL_COMPLEMENTARIO column
 * @method LibroQuery groupByIsbn() Group by the ISBN column
 * @method LibroQuery groupByNotasContenido() Group by the NOTAS_CONTENIDO column
 *
 * @method LibroQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method LibroQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method LibroQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method LibroQuery leftJoinDocumentacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Documentacion relation
 * @method LibroQuery rightJoinDocumentacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Documentacion relation
 * @method LibroQuery innerJoinDocumentacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Documentacion relation
 *
 * @method Libro findOne(PropelPDO $con = null) Return the first Libro matching the query
 * @method Libro findOneOrCreate(PropelPDO $con = null) Return the first Libro matching the query, or a new Libro object populated from the query conditions when no match is found
 *
 * @method Libro findOneByDocumentacionId(int $DOCUMENTACION_ID) Return the first Libro filtered by the DOCUMENTACION_ID column
 * @method Libro findOneByMencionEdicion(string $MENCION_EDICION) Return the first Libro filtered by the MENCION_EDICION column
 * @method Libro findOneByVersion(double $VERSION) Return the first Libro filtered by the VERSION column
 * @method Libro findOneByPaginacion(int $PAGINACION) Return the first Libro filtered by the PAGINACION column
 * @method Libro findOneByIlustraciones(boolean $ILUSTRACIONES) Return the first Libro filtered by the ILUSTRACIONES column
 * @method Libro findOneByVolumen(double $VOLUMEN) Return the first Libro filtered by the VOLUMEN column
 * @method Libro findOneByMaterialComplementario(string $MATERIAL_COMPLEMENTARIO) Return the first Libro filtered by the MATERIAL_COMPLEMENTARIO column
 * @method Libro findOneByIsbn(string $ISBN) Return the first Libro filtered by the ISBN column
 * @method Libro findOneByNotasContenido(string $NOTAS_CONTENIDO) Return the first Libro filtered by the NOTAS_CONTENIDO column
 *
 * @method array findByLibroId(int $LIBRO_ID) Return Libro objects filtered by the LIBRO_ID column
 * @method array findByDocumentacionId(int $DOCUMENTACION_ID) Return Libro objects filtered by the DOCUMENTACION_ID column
 * @method array findByMencionEdicion(string $MENCION_EDICION) Return Libro objects filtered by the MENCION_EDICION column
 * @method array findByVersion(double $VERSION) Return Libro objects filtered by the VERSION column
 * @method array findByPaginacion(int $PAGINACION) Return Libro objects filtered by the PAGINACION column
 * @method array findByIlustraciones(boolean $ILUSTRACIONES) Return Libro objects filtered by the ILUSTRACIONES column
 * @method array findByVolumen(double $VOLUMEN) Return Libro objects filtered by the VOLUMEN column
 * @method array findByMaterialComplementario(string $MATERIAL_COMPLEMENTARIO) Return Libro objects filtered by the MATERIAL_COMPLEMENTARIO column
 * @method array findByIsbn(string $ISBN) Return Libro objects filtered by the ISBN column
 * @method array findByNotasContenido(string $NOTAS_CONTENIDO) Return Libro objects filtered by the NOTAS_CONTENIDO column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseLibroQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseLibroQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Libro';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new LibroQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   LibroQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return LibroQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof LibroQuery) {
            return $criteria;
        }
        $query = new LibroQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Libro|Libro[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = LibroPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(LibroPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Libro A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByLibroId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Libro A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [LIBRO_ID], [DOCUMENTACION_ID], [MENCION_EDICION], [VERSION], [PAGINACION], [ILUSTRACIONES], [VOLUMEN], [MATERIAL_COMPLEMENTARIO], [ISBN], [NOTAS_CONTENIDO] FROM [LIBRO] WHERE [LIBRO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Libro();
            $obj->hydrate($row);
            LibroPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Libro|Libro[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Libro[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(LibroPeer::LIBRO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(LibroPeer::LIBRO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the LIBRO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByLibroId(1234); // WHERE LIBRO_ID = 1234
     * $query->filterByLibroId(array(12, 34)); // WHERE LIBRO_ID IN (12, 34)
     * $query->filterByLibroId(array('min' => 12)); // WHERE LIBRO_ID >= 12
     * $query->filterByLibroId(array('max' => 12)); // WHERE LIBRO_ID <= 12
     * </code>
     *
     * @param     mixed $libroId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByLibroId($libroId = null, $comparison = null)
    {
        if (is_array($libroId)) {
            $useMinMax = false;
            if (isset($libroId['min'])) {
                $this->addUsingAlias(LibroPeer::LIBRO_ID, $libroId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($libroId['max'])) {
                $this->addUsingAlias(LibroPeer::LIBRO_ID, $libroId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(LibroPeer::LIBRO_ID, $libroId, $comparison);
    }

    /**
     * Filter the query on the DOCUMENTACION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDocumentacionId(1234); // WHERE DOCUMENTACION_ID = 1234
     * $query->filterByDocumentacionId(array(12, 34)); // WHERE DOCUMENTACION_ID IN (12, 34)
     * $query->filterByDocumentacionId(array('min' => 12)); // WHERE DOCUMENTACION_ID >= 12
     * $query->filterByDocumentacionId(array('max' => 12)); // WHERE DOCUMENTACION_ID <= 12
     * </code>
     *
     * @see       filterByDocumentacion()
     *
     * @param     mixed $documentacionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByDocumentacionId($documentacionId = null, $comparison = null)
    {
        if (is_array($documentacionId)) {
            $useMinMax = false;
            if (isset($documentacionId['min'])) {
                $this->addUsingAlias(LibroPeer::DOCUMENTACION_ID, $documentacionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($documentacionId['max'])) {
                $this->addUsingAlias(LibroPeer::DOCUMENTACION_ID, $documentacionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(LibroPeer::DOCUMENTACION_ID, $documentacionId, $comparison);
    }

    /**
     * Filter the query on the MENCION_EDICION column
     *
     * Example usage:
     * <code>
     * $query->filterByMencionEdicion('fooValue');   // WHERE MENCION_EDICION = 'fooValue'
     * $query->filterByMencionEdicion('%fooValue%'); // WHERE MENCION_EDICION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $mencionEdicion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByMencionEdicion($mencionEdicion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($mencionEdicion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $mencionEdicion)) {
                $mencionEdicion = str_replace('*', '%', $mencionEdicion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(LibroPeer::MENCION_EDICION, $mencionEdicion, $comparison);
    }

    /**
     * Filter the query on the VERSION column
     *
     * Example usage:
     * <code>
     * $query->filterByVersion(1234); // WHERE VERSION = 1234
     * $query->filterByVersion(array(12, 34)); // WHERE VERSION IN (12, 34)
     * $query->filterByVersion(array('min' => 12)); // WHERE VERSION >= 12
     * $query->filterByVersion(array('max' => 12)); // WHERE VERSION <= 12
     * </code>
     *
     * @param     mixed $version The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByVersion($version = null, $comparison = null)
    {
        if (is_array($version)) {
            $useMinMax = false;
            if (isset($version['min'])) {
                $this->addUsingAlias(LibroPeer::VERSION, $version['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($version['max'])) {
                $this->addUsingAlias(LibroPeer::VERSION, $version['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(LibroPeer::VERSION, $version, $comparison);
    }

    /**
     * Filter the query on the PAGINACION column
     *
     * Example usage:
     * <code>
     * $query->filterByPaginacion(1234); // WHERE PAGINACION = 1234
     * $query->filterByPaginacion(array(12, 34)); // WHERE PAGINACION IN (12, 34)
     * $query->filterByPaginacion(array('min' => 12)); // WHERE PAGINACION >= 12
     * $query->filterByPaginacion(array('max' => 12)); // WHERE PAGINACION <= 12
     * </code>
     *
     * @param     mixed $paginacion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByPaginacion($paginacion = null, $comparison = null)
    {
        if (is_array($paginacion)) {
            $useMinMax = false;
            if (isset($paginacion['min'])) {
                $this->addUsingAlias(LibroPeer::PAGINACION, $paginacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($paginacion['max'])) {
                $this->addUsingAlias(LibroPeer::PAGINACION, $paginacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(LibroPeer::PAGINACION, $paginacion, $comparison);
    }

    /**
     * Filter the query on the ILUSTRACIONES column
     *
     * Example usage:
     * <code>
     * $query->filterByIlustraciones(true); // WHERE ILUSTRACIONES = true
     * $query->filterByIlustraciones('yes'); // WHERE ILUSTRACIONES = true
     * </code>
     *
     * @param     boolean|string $ilustraciones The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByIlustraciones($ilustraciones = null, $comparison = null)
    {
        if (is_string($ilustraciones)) {
            $ilustraciones = in_array(strtolower($ilustraciones), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(LibroPeer::ILUSTRACIONES, $ilustraciones, $comparison);
    }

    /**
     * Filter the query on the VOLUMEN column
     *
     * Example usage:
     * <code>
     * $query->filterByVolumen(1234); // WHERE VOLUMEN = 1234
     * $query->filterByVolumen(array(12, 34)); // WHERE VOLUMEN IN (12, 34)
     * $query->filterByVolumen(array('min' => 12)); // WHERE VOLUMEN >= 12
     * $query->filterByVolumen(array('max' => 12)); // WHERE VOLUMEN <= 12
     * </code>
     *
     * @param     mixed $volumen The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByVolumen($volumen = null, $comparison = null)
    {
        if (is_array($volumen)) {
            $useMinMax = false;
            if (isset($volumen['min'])) {
                $this->addUsingAlias(LibroPeer::VOLUMEN, $volumen['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($volumen['max'])) {
                $this->addUsingAlias(LibroPeer::VOLUMEN, $volumen['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(LibroPeer::VOLUMEN, $volumen, $comparison);
    }

    /**
     * Filter the query on the MATERIAL_COMPLEMENTARIO column
     *
     * Example usage:
     * <code>
     * $query->filterByMaterialComplementario('fooValue');   // WHERE MATERIAL_COMPLEMENTARIO = 'fooValue'
     * $query->filterByMaterialComplementario('%fooValue%'); // WHERE MATERIAL_COMPLEMENTARIO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $materialComplementario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByMaterialComplementario($materialComplementario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($materialComplementario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $materialComplementario)) {
                $materialComplementario = str_replace('*', '%', $materialComplementario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(LibroPeer::MATERIAL_COMPLEMENTARIO, $materialComplementario, $comparison);
    }

    /**
     * Filter the query on the ISBN column
     *
     * Example usage:
     * <code>
     * $query->filterByIsbn('fooValue');   // WHERE ISBN = 'fooValue'
     * $query->filterByIsbn('%fooValue%'); // WHERE ISBN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $isbn The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByIsbn($isbn = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($isbn)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $isbn)) {
                $isbn = str_replace('*', '%', $isbn);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(LibroPeer::ISBN, $isbn, $comparison);
    }

    /**
     * Filter the query on the NOTAS_CONTENIDO column
     *
     * Example usage:
     * <code>
     * $query->filterByNotasContenido('fooValue');   // WHERE NOTAS_CONTENIDO = 'fooValue'
     * $query->filterByNotasContenido('%fooValue%'); // WHERE NOTAS_CONTENIDO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $notasContenido The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function filterByNotasContenido($notasContenido = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($notasContenido)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $notasContenido)) {
                $notasContenido = str_replace('*', '%', $notasContenido);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(LibroPeer::NOTAS_CONTENIDO, $notasContenido, $comparison);
    }

    /**
     * Filter the query by a related Documentacion object
     *
     * @param   Documentacion|PropelObjectCollection $documentacion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 LibroQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDocumentacion($documentacion, $comparison = null)
    {
        if ($documentacion instanceof Documentacion) {
            return $this
                ->addUsingAlias(LibroPeer::DOCUMENTACION_ID, $documentacion->getDocumentacionId(), $comparison);
        } elseif ($documentacion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(LibroPeer::DOCUMENTACION_ID, $documentacion->toKeyValue('PrimaryKey', 'DocumentacionId'), $comparison);
        } else {
            throw new PropelException('filterByDocumentacion() only accepts arguments of type Documentacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Documentacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function joinDocumentacion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Documentacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Documentacion');
        }

        return $this;
    }

    /**
     * Use the Documentacion relation Documentacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DocumentacionQuery A secondary query class using the current class as primary query
     */
    public function useDocumentacionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinDocumentacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Documentacion', 'DocumentacionQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Libro $libro Object to remove from the list of results
     *
     * @return LibroQuery The current query, for fluid interface
     */
    public function prune($libro = null)
    {
        if ($libro) {
            $this->addUsingAlias(LibroPeer::LIBRO_ID, $libro->getLibroId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a query for the 'EMAIL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * dom 17 ago 2025 16:34:26
 *
 * @method EmailQuery orderByEmailId($order = Criteria::ASC) Order by the EMAIL_ID column
 * @method EmailQuery orderByEmailaccountId($order = Criteria::ASC) Order by the EMAILACCOUNT_ID column
 * @method EmailQuery orderByEmailestadoId($order = Criteria::ASC) Order by the EMAILESTADO_ID column
 * @method EmailQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method EmailQuery orderByModuloId($order = Criteria::ASC) Order by the MODULO_ID column
 * @method EmailQuery orderByMessageId($order = Criteria::ASC) Order by the MESSAGE_ID column
 * @method EmailQuery orderByEmailOrigen($order = Criteria::ASC) Order by the EMAIL_ORIGEN column
 * @method EmailQuery orderByEmailDestino($order = Criteria::ASC) Order by the EMAIL_DESTINO column
 * @method EmailQuery orderByNombeOrigen($order = Criteria::ASC) Order by the NOMBE_ORIGEN column
 * @method EmailQuery orderByNombreDestino($order = Criteria::ASC) Order by the NOMBRE_DESTINO column
 * @method EmailQuery orderByAsunto($order = Criteria::ASC) Order by the ASUNTO column
 * @method EmailQuery orderByContenido($order = Criteria::ASC) Order by the CONTENIDO column
 * @method EmailQuery orderByEmailCopia($order = Criteria::ASC) Order by the EMAIL_COPIA column
 * @method EmailQuery orderByEmailOriginal($order = Criteria::ASC) Order by the EMAIL_ORIGINAL column
 * @method EmailQuery orderByFechaRecibido($order = Criteria::ASC) Order by the FECHA_RECIBIDO column
 * @method EmailQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method EmailQuery orderByFechaModificacion($order = Criteria::ASC) Order by the FECHA_MODIFICACION column
 * @method EmailQuery orderByEstaAsignado($order = Criteria::ASC) Order by the ESTA_ASIGNADO column
 * @method EmailQuery orderByCreateAt($order = Criteria::ASC) Order by the CREATE_AT column
 * @method EmailQuery orderByCountAttachment($order = Criteria::ASC) Order by the COUNT_ATTACHMENT column
 * @method EmailQuery orderByBodyHtml($order = Criteria::ASC) Order by the BODY_HTML column
 * @method EmailQuery orderByBodyClean($order = Criteria::ASC) Order by the BODY_CLEAN column
 * @method EmailQuery orderByRawEml($order = Criteria::ASC) Order by the RAW_EML column
 * @method EmailQuery orderByConsecutivoId($order = Criteria::ASC) Order by the CONSECUTIVO_ID column
 * @method EmailQuery orderByRadicadoSgdea($order = Criteria::ASC) Order by the RADICADO_SGDEA column
 * @method EmailQuery orderByFechaAnulacion($order = Criteria::ASC) Order by the FECHA_ANULACION column
 * @method EmailQuery orderByNotaAnulacion($order = Criteria::ASC) Order by the NOTA_ANULACION column
 *
 * @method EmailQuery groupByEmailId() Group by the EMAIL_ID column
 * @method EmailQuery groupByEmailaccountId() Group by the EMAILACCOUNT_ID column
 * @method EmailQuery groupByEmailestadoId() Group by the EMAILESTADO_ID column
 * @method EmailQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method EmailQuery groupByModuloId() Group by the MODULO_ID column
 * @method EmailQuery groupByMessageId() Group by the MESSAGE_ID column
 * @method EmailQuery groupByEmailOrigen() Group by the EMAIL_ORIGEN column
 * @method EmailQuery groupByEmailDestino() Group by the EMAIL_DESTINO column
 * @method EmailQuery groupByNombeOrigen() Group by the NOMBE_ORIGEN column
 * @method EmailQuery groupByNombreDestino() Group by the NOMBRE_DESTINO column
 * @method EmailQuery groupByAsunto() Group by the ASUNTO column
 * @method EmailQuery groupByContenido() Group by the CONTENIDO column
 * @method EmailQuery groupByEmailCopia() Group by the EMAIL_COPIA column
 * @method EmailQuery groupByEmailOriginal() Group by the EMAIL_ORIGINAL column
 * @method EmailQuery groupByFechaRecibido() Group by the FECHA_RECIBIDO column
 * @method EmailQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method EmailQuery groupByFechaModificacion() Group by the FECHA_MODIFICACION column
 * @method EmailQuery groupByEstaAsignado() Group by the ESTA_ASIGNADO column
 * @method EmailQuery groupByCreateAt() Group by the CREATE_AT column
 * @method EmailQuery groupByCountAttachment() Group by the COUNT_ATTACHMENT column
 * @method EmailQuery groupByBodyHtml() Group by the BODY_HTML column
 * @method EmailQuery groupByBodyClean() Group by the BODY_CLEAN column
 * @method EmailQuery groupByRawEml() Group by the RAW_EML column
 * @method EmailQuery groupByConsecutivoId() Group by the CONSECUTIVO_ID column
 * @method EmailQuery groupByRadicadoSgdea() Group by the RADICADO_SGDEA column
 * @method EmailQuery groupByFechaAnulacion() Group by the FECHA_ANULACION column
 * @method EmailQuery groupByNotaAnulacion() Group by the NOTA_ANULACION column
 *
 * @method EmailQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method EmailQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method EmailQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method EmailQuery leftJoinEmailAccount($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailAccount relation
 * @method EmailQuery rightJoinEmailAccount($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailAccount relation
 * @method EmailQuery innerJoinEmailAccount($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailAccount relation
 *
 * @method EmailQuery leftJoinEmailEstado($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailEstado relation
 * @method EmailQuery rightJoinEmailEstado($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailEstado relation
 * @method EmailQuery innerJoinEmailEstado($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailEstado relation
 *
 * @method EmailQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method EmailQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method EmailQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method EmailQuery leftJoinModulo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Modulo relation
 * @method EmailQuery rightJoinModulo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Modulo relation
 * @method EmailQuery innerJoinModulo($relationAlias = null) Adds a INNER JOIN clause to the query using the Modulo relation
 *
 * @method EmailQuery leftJoinEmailAttachment($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailAttachment relation
 * @method EmailQuery rightJoinEmailAttachment($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailAttachment relation
 * @method EmailQuery innerJoinEmailAttachment($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailAttachment relation
 *
 * @method EmailQuery leftJoinTransferencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Transferencia relation
 * @method EmailQuery rightJoinTransferencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Transferencia relation
 * @method EmailQuery innerJoinTransferencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Transferencia relation
 *
 * @method EmailQuery leftJoinEmailUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailUsuario relation
 * @method EmailQuery rightJoinEmailUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailUsuario relation
 * @method EmailQuery innerJoinEmailUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailUsuario relation
 *
 * @method EmailQuery leftJoinEmailBitacora($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailBitacora relation
 * @method EmailQuery rightJoinEmailBitacora($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailBitacora relation
 * @method EmailQuery innerJoinEmailBitacora($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailBitacora relation
 *
 * @method Email findOne(PropelPDO $con = null) Return the first Email matching the query
 * @method Email findOneOrCreate(PropelPDO $con = null) Return the first Email matching the query, or a new Email object populated from the query conditions when no match is found
 *
 * @method Email findOneByEmailaccountId(int $EMAILACCOUNT_ID) Return the first Email filtered by the EMAILACCOUNT_ID column
 * @method Email findOneByEmailestadoId(int $EMAILESTADO_ID) Return the first Email filtered by the EMAILESTADO_ID column
 * @method Email findOneByUsuarioId(int $USUARIO_ID) Return the first Email filtered by the USUARIO_ID column
 * @method Email findOneByModuloId(int $MODULO_ID) Return the first Email filtered by the MODULO_ID column
 * @method Email findOneByMessageId(string $MESSAGE_ID) Return the first Email filtered by the MESSAGE_ID column
 * @method Email findOneByEmailOrigen(string $EMAIL_ORIGEN) Return the first Email filtered by the EMAIL_ORIGEN column
 * @method Email findOneByEmailDestino(string $EMAIL_DESTINO) Return the first Email filtered by the EMAIL_DESTINO column
 * @method Email findOneByNombeOrigen(string $NOMBE_ORIGEN) Return the first Email filtered by the NOMBE_ORIGEN column
 * @method Email findOneByNombreDestino(string $NOMBRE_DESTINO) Return the first Email filtered by the NOMBRE_DESTINO column
 * @method Email findOneByAsunto(string $ASUNTO) Return the first Email filtered by the ASUNTO column
 * @method Email findOneByContenido(string $CONTENIDO) Return the first Email filtered by the CONTENIDO column
 * @method Email findOneByEmailCopia(string $EMAIL_COPIA) Return the first Email filtered by the EMAIL_COPIA column
 * @method Email findOneByEmailOriginal(string $EMAIL_ORIGINAL) Return the first Email filtered by the EMAIL_ORIGINAL column
 * @method Email findOneByFechaRecibido(string $FECHA_RECIBIDO) Return the first Email filtered by the FECHA_RECIBIDO column
 * @method Email findOneByFechaCreacion(string $FECHA_CREACION) Return the first Email filtered by the FECHA_CREACION column
 * @method Email findOneByFechaModificacion(string $FECHA_MODIFICACION) Return the first Email filtered by the FECHA_MODIFICACION column
 * @method Email findOneByEstaAsignado(int $ESTA_ASIGNADO) Return the first Email filtered by the ESTA_ASIGNADO column
 * @method Email findOneByCreateAt(string $CREATE_AT) Return the first Email filtered by the CREATE_AT column
 * @method Email findOneByCountAttachment(int $COUNT_ATTACHMENT) Return the first Email filtered by the COUNT_ATTACHMENT column
 * @method Email findOneByBodyHtml(string $BODY_HTML) Return the first Email filtered by the BODY_HTML column
 * @method Email findOneByBodyClean(string $BODY_CLEAN) Return the first Email filtered by the BODY_CLEAN column
 * @method Email findOneByRawEml(string $RAW_EML) Return the first Email filtered by the RAW_EML column
 * @method Email findOneByConsecutivoId(int $CONSECUTIVO_ID) Return the first Email filtered by the CONSECUTIVO_ID column
 * @method Email findOneByRadicadoSgdea(string $RADICADO_SGDEA) Return the first Email filtered by the RADICADO_SGDEA column
 * @method Email findOneByFechaAnulacion(string $FECHA_ANULACION) Return the first Email filtered by the FECHA_ANULACION column
 * @method Email findOneByNotaAnulacion(string $NOTA_ANULACION) Return the first Email filtered by the NOTA_ANULACION column
 *
 * @method array findByEmailId(int $EMAIL_ID) Return Email objects filtered by the EMAIL_ID column
 * @method array findByEmailaccountId(int $EMAILACCOUNT_ID) Return Email objects filtered by the EMAILACCOUNT_ID column
 * @method array findByEmailestadoId(int $EMAILESTADO_ID) Return Email objects filtered by the EMAILESTADO_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return Email objects filtered by the USUARIO_ID column
 * @method array findByModuloId(int $MODULO_ID) Return Email objects filtered by the MODULO_ID column
 * @method array findByMessageId(string $MESSAGE_ID) Return Email objects filtered by the MESSAGE_ID column
 * @method array findByEmailOrigen(string $EMAIL_ORIGEN) Return Email objects filtered by the EMAIL_ORIGEN column
 * @method array findByEmailDestino(string $EMAIL_DESTINO) Return Email objects filtered by the EMAIL_DESTINO column
 * @method array findByNombeOrigen(string $NOMBE_ORIGEN) Return Email objects filtered by the NOMBE_ORIGEN column
 * @method array findByNombreDestino(string $NOMBRE_DESTINO) Return Email objects filtered by the NOMBRE_DESTINO column
 * @method array findByAsunto(string $ASUNTO) Return Email objects filtered by the ASUNTO column
 * @method array findByContenido(string $CONTENIDO) Return Email objects filtered by the CONTENIDO column
 * @method array findByEmailCopia(string $EMAIL_COPIA) Return Email objects filtered by the EMAIL_COPIA column
 * @method array findByEmailOriginal(string $EMAIL_ORIGINAL) Return Email objects filtered by the EMAIL_ORIGINAL column
 * @method array findByFechaRecibido(string $FECHA_RECIBIDO) Return Email objects filtered by the FECHA_RECIBIDO column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return Email objects filtered by the FECHA_CREACION column
 * @method array findByFechaModificacion(string $FECHA_MODIFICACION) Return Email objects filtered by the FECHA_MODIFICACION column
 * @method array findByEstaAsignado(int $ESTA_ASIGNADO) Return Email objects filtered by the ESTA_ASIGNADO column
 * @method array findByCreateAt(string $CREATE_AT) Return Email objects filtered by the CREATE_AT column
 * @method array findByCountAttachment(int $COUNT_ATTACHMENT) Return Email objects filtered by the COUNT_ATTACHMENT column
 * @method array findByBodyHtml(string $BODY_HTML) Return Email objects filtered by the BODY_HTML column
 * @method array findByBodyClean(string $BODY_CLEAN) Return Email objects filtered by the BODY_CLEAN column
 * @method array findByRawEml(string $RAW_EML) Return Email objects filtered by the RAW_EML column
 * @method array findByConsecutivoId(int $CONSECUTIVO_ID) Return Email objects filtered by the CONSECUTIVO_ID column
 * @method array findByRadicadoSgdea(string $RADICADO_SGDEA) Return Email objects filtered by the RADICADO_SGDEA column
 * @method array findByFechaAnulacion(string $FECHA_ANULACION) Return Email objects filtered by the FECHA_ANULACION column
 * @method array findByNotaAnulacion(string $NOTA_ANULACION) Return Email objects filtered by the NOTA_ANULACION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEmailQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseEmailQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Email';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new EmailQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   EmailQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return EmailQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof EmailQuery) {
            return $criteria;
        }
        $query = new EmailQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Email|Email[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = EmailPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(EmailPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Email A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByEmailId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Email A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [EMAIL_ID], [EMAILACCOUNT_ID], [EMAILESTADO_ID], [USUARIO_ID], [MODULO_ID], [MESSAGE_ID], [EMAIL_ORIGEN], [EMAIL_DESTINO], [NOMBE_ORIGEN], [NOMBRE_DESTINO], [ASUNTO], [CONTENIDO], [EMAIL_COPIA], [EMAIL_ORIGINAL], [FECHA_RECIBIDO], [FECHA_CREACION], [FECHA_MODIFICACION], [ESTA_ASIGNADO], [CREATE_AT], [COUNT_ATTACHMENT], [BODY_HTML], [BODY_CLEAN], [RAW_EML], [CONSECUTIVO_ID], [RADICADO_SGDEA], [FECHA_ANULACION], [NOTA_ANULACION] FROM [EMAIL] WHERE [EMAIL_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Email();
            $obj->hydrate($row);
            EmailPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Email|Email[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Email[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(EmailPeer::EMAIL_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(EmailPeer::EMAIL_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the EMAIL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailId(1234); // WHERE EMAIL_ID = 1234
     * $query->filterByEmailId(array(12, 34)); // WHERE EMAIL_ID IN (12, 34)
     * $query->filterByEmailId(array('min' => 12)); // WHERE EMAIL_ID >= 12
     * $query->filterByEmailId(array('max' => 12)); // WHERE EMAIL_ID <= 12
     * </code>
     *
     * @param     mixed $emailId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailId($emailId = null, $comparison = null)
    {
        if (is_array($emailId)) {
            $useMinMax = false;
            if (isset($emailId['min'])) {
                $this->addUsingAlias(EmailPeer::EMAIL_ID, $emailId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($emailId['max'])) {
                $this->addUsingAlias(EmailPeer::EMAIL_ID, $emailId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAIL_ID, $emailId, $comparison);
    }

    /**
     * Filter the query on the EMAILACCOUNT_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailaccountId(1234); // WHERE EMAILACCOUNT_ID = 1234
     * $query->filterByEmailaccountId(array(12, 34)); // WHERE EMAILACCOUNT_ID IN (12, 34)
     * $query->filterByEmailaccountId(array('min' => 12)); // WHERE EMAILACCOUNT_ID >= 12
     * $query->filterByEmailaccountId(array('max' => 12)); // WHERE EMAILACCOUNT_ID <= 12
     * </code>
     *
     * @see       filterByEmailAccount()
     *
     * @param     mixed $emailaccountId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailaccountId($emailaccountId = null, $comparison = null)
    {
        if (is_array($emailaccountId)) {
            $useMinMax = false;
            if (isset($emailaccountId['min'])) {
                $this->addUsingAlias(EmailPeer::EMAILACCOUNT_ID, $emailaccountId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($emailaccountId['max'])) {
                $this->addUsingAlias(EmailPeer::EMAILACCOUNT_ID, $emailaccountId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAILACCOUNT_ID, $emailaccountId, $comparison);
    }

    /**
     * Filter the query on the EMAILESTADO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailestadoId(1234); // WHERE EMAILESTADO_ID = 1234
     * $query->filterByEmailestadoId(array(12, 34)); // WHERE EMAILESTADO_ID IN (12, 34)
     * $query->filterByEmailestadoId(array('min' => 12)); // WHERE EMAILESTADO_ID >= 12
     * $query->filterByEmailestadoId(array('max' => 12)); // WHERE EMAILESTADO_ID <= 12
     * </code>
     *
     * @see       filterByEmailEstado()
     *
     * @param     mixed $emailestadoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailestadoId($emailestadoId = null, $comparison = null)
    {
        if (is_array($emailestadoId)) {
            $useMinMax = false;
            if (isset($emailestadoId['min'])) {
                $this->addUsingAlias(EmailPeer::EMAILESTADO_ID, $emailestadoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($emailestadoId['max'])) {
                $this->addUsingAlias(EmailPeer::EMAILESTADO_ID, $emailestadoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAILESTADO_ID, $emailestadoId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(EmailPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(EmailPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the MODULO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByModuloId(1234); // WHERE MODULO_ID = 1234
     * $query->filterByModuloId(array(12, 34)); // WHERE MODULO_ID IN (12, 34)
     * $query->filterByModuloId(array('min' => 12)); // WHERE MODULO_ID >= 12
     * $query->filterByModuloId(array('max' => 12)); // WHERE MODULO_ID <= 12
     * </code>
     *
     * @see       filterByModulo()
     *
     * @param     mixed $moduloId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByModuloId($moduloId = null, $comparison = null)
    {
        if (is_array($moduloId)) {
            $useMinMax = false;
            if (isset($moduloId['min'])) {
                $this->addUsingAlias(EmailPeer::MODULO_ID, $moduloId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($moduloId['max'])) {
                $this->addUsingAlias(EmailPeer::MODULO_ID, $moduloId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::MODULO_ID, $moduloId, $comparison);
    }

    /**
     * Filter the query on the MESSAGE_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByMessageId('fooValue');   // WHERE MESSAGE_ID = 'fooValue'
     * $query->filterByMessageId('%fooValue%'); // WHERE MESSAGE_ID LIKE '%fooValue%'
     * </code>
     *
     * @param     string $messageId The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByMessageId($messageId = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($messageId)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $messageId)) {
                $messageId = str_replace('*', '%', $messageId);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::MESSAGE_ID, $messageId, $comparison);
    }

    /**
     * Filter the query on the EMAIL_ORIGEN column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailOrigen('fooValue');   // WHERE EMAIL_ORIGEN = 'fooValue'
     * $query->filterByEmailOrigen('%fooValue%'); // WHERE EMAIL_ORIGEN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailOrigen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailOrigen($emailOrigen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailOrigen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailOrigen)) {
                $emailOrigen = str_replace('*', '%', $emailOrigen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAIL_ORIGEN, $emailOrigen, $comparison);
    }

    /**
     * Filter the query on the EMAIL_DESTINO column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailDestino('fooValue');   // WHERE EMAIL_DESTINO = 'fooValue'
     * $query->filterByEmailDestino('%fooValue%'); // WHERE EMAIL_DESTINO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailDestino The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailDestino($emailDestino = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailDestino)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailDestino)) {
                $emailDestino = str_replace('*', '%', $emailDestino);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAIL_DESTINO, $emailDestino, $comparison);
    }

    /**
     * Filter the query on the NOMBE_ORIGEN column
     *
     * Example usage:
     * <code>
     * $query->filterByNombeOrigen('fooValue');   // WHERE NOMBE_ORIGEN = 'fooValue'
     * $query->filterByNombeOrigen('%fooValue%'); // WHERE NOMBE_ORIGEN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombeOrigen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByNombeOrigen($nombeOrigen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombeOrigen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombeOrigen)) {
                $nombeOrigen = str_replace('*', '%', $nombeOrigen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::NOMBE_ORIGEN, $nombeOrigen, $comparison);
    }

    /**
     * Filter the query on the NOMBRE_DESTINO column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreDestino('fooValue');   // WHERE NOMBRE_DESTINO = 'fooValue'
     * $query->filterByNombreDestino('%fooValue%'); // WHERE NOMBRE_DESTINO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombreDestino The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByNombreDestino($nombreDestino = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombreDestino)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombreDestino)) {
                $nombreDestino = str_replace('*', '%', $nombreDestino);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::NOMBRE_DESTINO, $nombreDestino, $comparison);
    }

    /**
     * Filter the query on the ASUNTO column
     *
     * Example usage:
     * <code>
     * $query->filterByAsunto('fooValue');   // WHERE ASUNTO = 'fooValue'
     * $query->filterByAsunto('%fooValue%'); // WHERE ASUNTO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $asunto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByAsunto($asunto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($asunto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $asunto)) {
                $asunto = str_replace('*', '%', $asunto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::ASUNTO, $asunto, $comparison);
    }

    /**
     * Filter the query on the CONTENIDO column
     *
     * Example usage:
     * <code>
     * $query->filterByContenido('fooValue');   // WHERE CONTENIDO = 'fooValue'
     * $query->filterByContenido('%fooValue%'); // WHERE CONTENIDO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contenido The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByContenido($contenido = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contenido)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contenido)) {
                $contenido = str_replace('*', '%', $contenido);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::CONTENIDO, $contenido, $comparison);
    }

    /**
     * Filter the query on the EMAIL_COPIA column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailCopia('fooValue');   // WHERE EMAIL_COPIA = 'fooValue'
     * $query->filterByEmailCopia('%fooValue%'); // WHERE EMAIL_COPIA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailCopia The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailCopia($emailCopia = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailCopia)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailCopia)) {
                $emailCopia = str_replace('*', '%', $emailCopia);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAIL_COPIA, $emailCopia, $comparison);
    }

    /**
     * Filter the query on the EMAIL_ORIGINAL column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailOriginal('fooValue');   // WHERE EMAIL_ORIGINAL = 'fooValue'
     * $query->filterByEmailOriginal('%fooValue%'); // WHERE EMAIL_ORIGINAL LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailOriginal The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEmailOriginal($emailOriginal = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailOriginal)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailOriginal)) {
                $emailOriginal = str_replace('*', '%', $emailOriginal);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::EMAIL_ORIGINAL, $emailOriginal, $comparison);
    }

    /**
     * Filter the query on the FECHA_RECIBIDO column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaRecibido('2011-03-14'); // WHERE FECHA_RECIBIDO = '2011-03-14'
     * $query->filterByFechaRecibido('now'); // WHERE FECHA_RECIBIDO = '2011-03-14'
     * $query->filterByFechaRecibido(array('max' => 'yesterday')); // WHERE FECHA_RECIBIDO < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaRecibido The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByFechaRecibido($fechaRecibido = null, $comparison = null)
    {
        if (is_array($fechaRecibido)) {
            $useMinMax = false;
            if (isset($fechaRecibido['min'])) {
                $this->addUsingAlias(EmailPeer::FECHA_RECIBIDO, $fechaRecibido['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaRecibido['max'])) {
                $this->addUsingAlias(EmailPeer::FECHA_RECIBIDO, $fechaRecibido['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::FECHA_RECIBIDO, $fechaRecibido, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(EmailPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(EmailPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_MODIFICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaModificacion('2011-03-14'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion('now'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion(array('max' => 'yesterday')); // WHERE FECHA_MODIFICACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaModificacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByFechaModificacion($fechaModificacion = null, $comparison = null)
    {
        if (is_array($fechaModificacion)) {
            $useMinMax = false;
            if (isset($fechaModificacion['min'])) {
                $this->addUsingAlias(EmailPeer::FECHA_MODIFICACION, $fechaModificacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaModificacion['max'])) {
                $this->addUsingAlias(EmailPeer::FECHA_MODIFICACION, $fechaModificacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::FECHA_MODIFICACION, $fechaModificacion, $comparison);
    }

    /**
     * Filter the query on the ESTA_ASIGNADO column
     *
     * Example usage:
     * <code>
     * $query->filterByEstaAsignado(1234); // WHERE ESTA_ASIGNADO = 1234
     * $query->filterByEstaAsignado(array(12, 34)); // WHERE ESTA_ASIGNADO IN (12, 34)
     * $query->filterByEstaAsignado(array('min' => 12)); // WHERE ESTA_ASIGNADO >= 12
     * $query->filterByEstaAsignado(array('max' => 12)); // WHERE ESTA_ASIGNADO <= 12
     * </code>
     *
     * @param     mixed $estaAsignado The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByEstaAsignado($estaAsignado = null, $comparison = null)
    {
        if (is_array($estaAsignado)) {
            $useMinMax = false;
            if (isset($estaAsignado['min'])) {
                $this->addUsingAlias(EmailPeer::ESTA_ASIGNADO, $estaAsignado['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estaAsignado['max'])) {
                $this->addUsingAlias(EmailPeer::ESTA_ASIGNADO, $estaAsignado['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::ESTA_ASIGNADO, $estaAsignado, $comparison);
    }

    /**
     * Filter the query on the CREATE_AT column
     *
     * Example usage:
     * <code>
     * $query->filterByCreateAt('2011-03-14'); // WHERE CREATE_AT = '2011-03-14'
     * $query->filterByCreateAt('now'); // WHERE CREATE_AT = '2011-03-14'
     * $query->filterByCreateAt(array('max' => 'yesterday')); // WHERE CREATE_AT < '2011-03-13'
     * </code>
     *
     * @param     mixed $createAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByCreateAt($createAt = null, $comparison = null)
    {
        if (is_array($createAt)) {
            $useMinMax = false;
            if (isset($createAt['min'])) {
                $this->addUsingAlias(EmailPeer::CREATE_AT, $createAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createAt['max'])) {
                $this->addUsingAlias(EmailPeer::CREATE_AT, $createAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::CREATE_AT, $createAt, $comparison);
    }

    /**
     * Filter the query on the COUNT_ATTACHMENT column
     *
     * Example usage:
     * <code>
     * $query->filterByCountAttachment(1234); // WHERE COUNT_ATTACHMENT = 1234
     * $query->filterByCountAttachment(array(12, 34)); // WHERE COUNT_ATTACHMENT IN (12, 34)
     * $query->filterByCountAttachment(array('min' => 12)); // WHERE COUNT_ATTACHMENT >= 12
     * $query->filterByCountAttachment(array('max' => 12)); // WHERE COUNT_ATTACHMENT <= 12
     * </code>
     *
     * @param     mixed $countAttachment The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByCountAttachment($countAttachment = null, $comparison = null)
    {
        if (is_array($countAttachment)) {
            $useMinMax = false;
            if (isset($countAttachment['min'])) {
                $this->addUsingAlias(EmailPeer::COUNT_ATTACHMENT, $countAttachment['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($countAttachment['max'])) {
                $this->addUsingAlias(EmailPeer::COUNT_ATTACHMENT, $countAttachment['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::COUNT_ATTACHMENT, $countAttachment, $comparison);
    }

    /**
     * Filter the query on the BODY_HTML column
     *
     * Example usage:
     * <code>
     * $query->filterByBodyHtml('fooValue');   // WHERE BODY_HTML = 'fooValue'
     * $query->filterByBodyHtml('%fooValue%'); // WHERE BODY_HTML LIKE '%fooValue%'
     * </code>
     *
     * @param     string $bodyHtml The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByBodyHtml($bodyHtml = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($bodyHtml)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $bodyHtml)) {
                $bodyHtml = str_replace('*', '%', $bodyHtml);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::BODY_HTML, $bodyHtml, $comparison);
    }

    /**
     * Filter the query on the BODY_CLEAN column
     *
     * Example usage:
     * <code>
     * $query->filterByBodyClean('fooValue');   // WHERE BODY_CLEAN = 'fooValue'
     * $query->filterByBodyClean('%fooValue%'); // WHERE BODY_CLEAN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $bodyClean The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByBodyClean($bodyClean = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($bodyClean)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $bodyClean)) {
                $bodyClean = str_replace('*', '%', $bodyClean);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::BODY_CLEAN, $bodyClean, $comparison);
    }

    /**
     * Filter the query on the RAW_EML column
     *
     * @param     mixed $rawEml The value to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByRawEml($rawEml = null, $comparison = null)
    {

        return $this->addUsingAlias(EmailPeer::RAW_EML, $rawEml, $comparison);
    }

    /**
     * Filter the query on the CONSECUTIVO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByConsecutivoId(1234); // WHERE CONSECUTIVO_ID = 1234
     * $query->filterByConsecutivoId(array(12, 34)); // WHERE CONSECUTIVO_ID IN (12, 34)
     * $query->filterByConsecutivoId(array('min' => 12)); // WHERE CONSECUTIVO_ID >= 12
     * $query->filterByConsecutivoId(array('max' => 12)); // WHERE CONSECUTIVO_ID <= 12
     * </code>
     *
     * @param     mixed $consecutivoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByConsecutivoId($consecutivoId = null, $comparison = null)
    {
        if (is_array($consecutivoId)) {
            $useMinMax = false;
            if (isset($consecutivoId['min'])) {
                $this->addUsingAlias(EmailPeer::CONSECUTIVO_ID, $consecutivoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($consecutivoId['max'])) {
                $this->addUsingAlias(EmailPeer::CONSECUTIVO_ID, $consecutivoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::CONSECUTIVO_ID, $consecutivoId, $comparison);
    }

    /**
     * Filter the query on the RADICADO_SGDEA column
     *
     * Example usage:
     * <code>
     * $query->filterByRadicadoSgdea('fooValue');   // WHERE RADICADO_SGDEA = 'fooValue'
     * $query->filterByRadicadoSgdea('%fooValue%'); // WHERE RADICADO_SGDEA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $radicadoSgdea The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByRadicadoSgdea($radicadoSgdea = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($radicadoSgdea)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $radicadoSgdea)) {
                $radicadoSgdea = str_replace('*', '%', $radicadoSgdea);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::RADICADO_SGDEA, $radicadoSgdea, $comparison);
    }

    /**
     * Filter the query on the FECHA_ANULACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaAnulacion('2011-03-14'); // WHERE FECHA_ANULACION = '2011-03-14'
     * $query->filterByFechaAnulacion('now'); // WHERE FECHA_ANULACION = '2011-03-14'
     * $query->filterByFechaAnulacion(array('max' => 'yesterday')); // WHERE FECHA_ANULACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaAnulacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByFechaAnulacion($fechaAnulacion = null, $comparison = null)
    {
        if (is_array($fechaAnulacion)) {
            $useMinMax = false;
            if (isset($fechaAnulacion['min'])) {
                $this->addUsingAlias(EmailPeer::FECHA_ANULACION, $fechaAnulacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaAnulacion['max'])) {
                $this->addUsingAlias(EmailPeer::FECHA_ANULACION, $fechaAnulacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailPeer::FECHA_ANULACION, $fechaAnulacion, $comparison);
    }

    /**
     * Filter the query on the NOTA_ANULACION column
     *
     * Example usage:
     * <code>
     * $query->filterByNotaAnulacion('fooValue');   // WHERE NOTA_ANULACION = 'fooValue'
     * $query->filterByNotaAnulacion('%fooValue%'); // WHERE NOTA_ANULACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $notaAnulacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function filterByNotaAnulacion($notaAnulacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($notaAnulacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $notaAnulacion)) {
                $notaAnulacion = str_replace('*', '%', $notaAnulacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailPeer::NOTA_ANULACION, $notaAnulacion, $comparison);
    }

    /**
     * Filter the query by a related EmailAccount object
     *
     * @param   EmailAccount|PropelObjectCollection $emailAccount The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailAccount($emailAccount, $comparison = null)
    {
        if ($emailAccount instanceof EmailAccount) {
            return $this
                ->addUsingAlias(EmailPeer::EMAILACCOUNT_ID, $emailAccount->getEmailaccountId(), $comparison);
        } elseif ($emailAccount instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmailPeer::EMAILACCOUNT_ID, $emailAccount->toKeyValue('PrimaryKey', 'EmailaccountId'), $comparison);
        } else {
            throw new PropelException('filterByEmailAccount() only accepts arguments of type EmailAccount or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailAccount relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinEmailAccount($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailAccount');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailAccount');
        }

        return $this;
    }

    /**
     * Use the EmailAccount relation EmailAccount object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailAccountQuery A secondary query class using the current class as primary query
     */
    public function useEmailAccountQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailAccount($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailAccount', 'EmailAccountQuery');
    }

    /**
     * Filter the query by a related EmailEstado object
     *
     * @param   EmailEstado|PropelObjectCollection $emailEstado The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailEstado($emailEstado, $comparison = null)
    {
        if ($emailEstado instanceof EmailEstado) {
            return $this
                ->addUsingAlias(EmailPeer::EMAILESTADO_ID, $emailEstado->getEmailestadoId(), $comparison);
        } elseif ($emailEstado instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmailPeer::EMAILESTADO_ID, $emailEstado->toKeyValue('PrimaryKey', 'EmailestadoId'), $comparison);
        } else {
            throw new PropelException('filterByEmailEstado() only accepts arguments of type EmailEstado or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailEstado relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinEmailEstado($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailEstado');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailEstado');
        }

        return $this;
    }

    /**
     * Use the EmailEstado relation EmailEstado object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailEstadoQuery A secondary query class using the current class as primary query
     */
    public function useEmailEstadoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailEstado($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailEstado', 'EmailEstadoQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(EmailPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmailPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related Modulo object
     *
     * @param   Modulo|PropelObjectCollection $modulo The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByModulo($modulo, $comparison = null)
    {
        if ($modulo instanceof Modulo) {
            return $this
                ->addUsingAlias(EmailPeer::MODULO_ID, $modulo->getModuloId(), $comparison);
        } elseif ($modulo instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmailPeer::MODULO_ID, $modulo->toKeyValue('PrimaryKey', 'ModuloId'), $comparison);
        } else {
            throw new PropelException('filterByModulo() only accepts arguments of type Modulo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Modulo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinModulo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Modulo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Modulo');
        }

        return $this;
    }

    /**
     * Use the Modulo relation Modulo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ModuloQuery A secondary query class using the current class as primary query
     */
    public function useModuloQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinModulo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Modulo', 'ModuloQuery');
    }

    /**
     * Filter the query by a related EmailAttachment object
     *
     * @param   EmailAttachment|PropelObjectCollection $emailAttachment  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailAttachment($emailAttachment, $comparison = null)
    {
        if ($emailAttachment instanceof EmailAttachment) {
            return $this
                ->addUsingAlias(EmailPeer::EMAIL_ID, $emailAttachment->getEmailId(), $comparison);
        } elseif ($emailAttachment instanceof PropelObjectCollection) {
            return $this
                ->useEmailAttachmentQuery()
                ->filterByPrimaryKeys($emailAttachment->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEmailAttachment() only accepts arguments of type EmailAttachment or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailAttachment relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinEmailAttachment($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailAttachment');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailAttachment');
        }

        return $this;
    }

    /**
     * Use the EmailAttachment relation EmailAttachment object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailAttachmentQuery A secondary query class using the current class as primary query
     */
    public function useEmailAttachmentQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailAttachment($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailAttachment', 'EmailAttachmentQuery');
    }

    /**
     * Filter the query by a related Transferencia object
     *
     * @param   Transferencia|PropelObjectCollection $transferencia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTransferencia($transferencia, $comparison = null)
    {
        if ($transferencia instanceof Transferencia) {
            return $this
                ->addUsingAlias(EmailPeer::EMAIL_ID, $transferencia->getEmailId(), $comparison);
        } elseif ($transferencia instanceof PropelObjectCollection) {
            return $this
                ->useTransferenciaQuery()
                ->filterByPrimaryKeys($transferencia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTransferencia() only accepts arguments of type Transferencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Transferencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinTransferencia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Transferencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Transferencia');
        }

        return $this;
    }

    /**
     * Use the Transferencia relation Transferencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TransferenciaQuery A secondary query class using the current class as primary query
     */
    public function useTransferenciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTransferencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Transferencia', 'TransferenciaQuery');
    }

    /**
     * Filter the query by a related EmailUsuario object
     *
     * @param   EmailUsuario|PropelObjectCollection $emailUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailUsuario($emailUsuario, $comparison = null)
    {
        if ($emailUsuario instanceof EmailUsuario) {
            return $this
                ->addUsingAlias(EmailPeer::EMAIL_ID, $emailUsuario->getEmailId(), $comparison);
        } elseif ($emailUsuario instanceof PropelObjectCollection) {
            return $this
                ->useEmailUsuarioQuery()
                ->filterByPrimaryKeys($emailUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEmailUsuario() only accepts arguments of type EmailUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinEmailUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailUsuario');
        }

        return $this;
    }

    /**
     * Use the EmailUsuario relation EmailUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useEmailUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailUsuario', 'EmailUsuarioQuery');
    }

    /**
     * Filter the query by a related EmailBitacora object
     *
     * @param   EmailBitacora|PropelObjectCollection $emailBitacora  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailBitacora($emailBitacora, $comparison = null)
    {
        if ($emailBitacora instanceof EmailBitacora) {
            return $this
                ->addUsingAlias(EmailPeer::EMAIL_ID, $emailBitacora->getEmailId(), $comparison);
        } elseif ($emailBitacora instanceof PropelObjectCollection) {
            return $this
                ->useEmailBitacoraQuery()
                ->filterByPrimaryKeys($emailBitacora->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEmailBitacora() only accepts arguments of type EmailBitacora or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailBitacora relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function joinEmailBitacora($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailBitacora');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailBitacora');
        }

        return $this;
    }

    /**
     * Use the EmailBitacora relation EmailBitacora object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailBitacoraQuery A secondary query class using the current class as primary query
     */
    public function useEmailBitacoraQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailBitacora($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailBitacora', 'EmailBitacoraQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Email $email Object to remove from the list of results
     *
     * @return EmailQuery The current query, for fluid interface
     */
    public function prune($email = null)
    {
        if ($email) {
            $this->addUsingAlias(EmailPeer::EMAIL_ID, $email->getEmailId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

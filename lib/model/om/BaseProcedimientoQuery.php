<?php


/**
 * Base class that represents a query for the 'PROCEDIMIENTO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:53
 *
 * @method ProcedimientoQuery orderByProcedimientoId($order = Criteria::ASC) Order by the PROCEDIMIENTO_ID column
 * @method ProcedimientoQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method ProcedimientoQuery orderByTipoprocedimientoId($order = Criteria::ASC) Order by the TIPOPROCEDIMIENTO_ID column
 * @method ProcedimientoQuery orderByDependenciaId($order = Criteria::ASC) Order by the DEPENDENCIA_ID column
 * @method ProcedimientoQuery orderByCodigo($order = Criteria::ASC) Order by the CODIGO column
 * @method ProcedimientoQuery orderByNombre($order = Criteria::ASC) Order by the NOMBRE column
 * @method ProcedimientoQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method ProcedimientoQuery orderByExtension($order = Criteria::ASC) Order by the EXTENSION column
 * @method ProcedimientoQuery orderByRuta($order = Criteria::ASC) Order by the RUTA column
 * @method ProcedimientoQuery orderByVersion($order = Criteria::ASC) Order by the VERSION column
 * @method ProcedimientoQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method ProcedimientoQuery orderByCodigoVersion($order = Criteria::ASC) Order by the CODIGO_VERSION column
 * @method ProcedimientoQuery orderByEsUltimaVersion($order = Criteria::ASC) Order by the ES_ULTIMA_VERSION column
 * @method ProcedimientoQuery orderByEstadoRevision($order = Criteria::ASC) Order by the ESTADO_REVISION column
 * @method ProcedimientoQuery orderByRevisadoPor($order = Criteria::ASC) Order by the REVISADO_POR column
 * @method ProcedimientoQuery orderByObservacionRevision($order = Criteria::ASC) Order by the OBSERVACION_REVISION column
 * @method ProcedimientoQuery orderByFechaAprobacion($order = Criteria::ASC) Order by the FECHA_APROBACION column
 * @method ProcedimientoQuery orderByTextCambios($order = Criteria::ASC) Order by the TEXT_CAMBIOS column
 * @method ProcedimientoQuery orderByEstadoAprobacion($order = Criteria::ASC) Order by the ESTADO_APROBACION column
 *
 * @method ProcedimientoQuery groupByProcedimientoId() Group by the PROCEDIMIENTO_ID column
 * @method ProcedimientoQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method ProcedimientoQuery groupByTipoprocedimientoId() Group by the TIPOPROCEDIMIENTO_ID column
 * @method ProcedimientoQuery groupByDependenciaId() Group by the DEPENDENCIA_ID column
 * @method ProcedimientoQuery groupByCodigo() Group by the CODIGO column
 * @method ProcedimientoQuery groupByNombre() Group by the NOMBRE column
 * @method ProcedimientoQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method ProcedimientoQuery groupByExtension() Group by the EXTENSION column
 * @method ProcedimientoQuery groupByRuta() Group by the RUTA column
 * @method ProcedimientoQuery groupByVersion() Group by the VERSION column
 * @method ProcedimientoQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method ProcedimientoQuery groupByCodigoVersion() Group by the CODIGO_VERSION column
 * @method ProcedimientoQuery groupByEsUltimaVersion() Group by the ES_ULTIMA_VERSION column
 * @method ProcedimientoQuery groupByEstadoRevision() Group by the ESTADO_REVISION column
 * @method ProcedimientoQuery groupByRevisadoPor() Group by the REVISADO_POR column
 * @method ProcedimientoQuery groupByObservacionRevision() Group by the OBSERVACION_REVISION column
 * @method ProcedimientoQuery groupByFechaAprobacion() Group by the FECHA_APROBACION column
 * @method ProcedimientoQuery groupByTextCambios() Group by the TEXT_CAMBIOS column
 * @method ProcedimientoQuery groupByEstadoAprobacion() Group by the ESTADO_APROBACION column
 *
 * @method ProcedimientoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ProcedimientoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ProcedimientoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ProcedimientoQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method ProcedimientoQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method ProcedimientoQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method ProcedimientoQuery leftJoinTipoProcedimiento($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoProcedimiento relation
 * @method ProcedimientoQuery rightJoinTipoProcedimiento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoProcedimiento relation
 * @method ProcedimientoQuery innerJoinTipoProcedimiento($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoProcedimiento relation
 *
 * @method ProcedimientoQuery leftJoinDependencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Dependencia relation
 * @method ProcedimientoQuery rightJoinDependencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Dependencia relation
 * @method ProcedimientoQuery innerJoinDependencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Dependencia relation
 *
 * @method ProcedimientoQuery leftJoinProcedimientoAprob($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProcedimientoAprob relation
 * @method ProcedimientoQuery rightJoinProcedimientoAprob($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProcedimientoAprob relation
 * @method ProcedimientoQuery innerJoinProcedimientoAprob($relationAlias = null) Adds a INNER JOIN clause to the query using the ProcedimientoAprob relation
 *
 * @method Procedimiento findOne(PropelPDO $con = null) Return the first Procedimiento matching the query
 * @method Procedimiento findOneOrCreate(PropelPDO $con = null) Return the first Procedimiento matching the query, or a new Procedimiento object populated from the query conditions when no match is found
 *
 * @method Procedimiento findOneByUsuarioId(int $USUARIO_ID) Return the first Procedimiento filtered by the USUARIO_ID column
 * @method Procedimiento findOneByTipoprocedimientoId(int $TIPOPROCEDIMIENTO_ID) Return the first Procedimiento filtered by the TIPOPROCEDIMIENTO_ID column
 * @method Procedimiento findOneByDependenciaId(int $DEPENDENCIA_ID) Return the first Procedimiento filtered by the DEPENDENCIA_ID column
 * @method Procedimiento findOneByCodigo(string $CODIGO) Return the first Procedimiento filtered by the CODIGO column
 * @method Procedimiento findOneByNombre(string $NOMBRE) Return the first Procedimiento filtered by the NOMBRE column
 * @method Procedimiento findOneByDescripcion(string $DESCRIPCION) Return the first Procedimiento filtered by the DESCRIPCION column
 * @method Procedimiento findOneByExtension(string $EXTENSION) Return the first Procedimiento filtered by the EXTENSION column
 * @method Procedimiento findOneByRuta(string $RUTA) Return the first Procedimiento filtered by the RUTA column
 * @method Procedimiento findOneByVersion(double $VERSION) Return the first Procedimiento filtered by the VERSION column
 * @method Procedimiento findOneByFechaCreacion(string $FECHA_CREACION) Return the first Procedimiento filtered by the FECHA_CREACION column
 * @method Procedimiento findOneByCodigoVersion(int $CODIGO_VERSION) Return the first Procedimiento filtered by the CODIGO_VERSION column
 * @method Procedimiento findOneByEsUltimaVersion(boolean $ES_ULTIMA_VERSION) Return the first Procedimiento filtered by the ES_ULTIMA_VERSION column
 * @method Procedimiento findOneByEstadoRevision(string $ESTADO_REVISION) Return the first Procedimiento filtered by the ESTADO_REVISION column
 * @method Procedimiento findOneByRevisadoPor(string $REVISADO_POR) Return the first Procedimiento filtered by the REVISADO_POR column
 * @method Procedimiento findOneByObservacionRevision(string $OBSERVACION_REVISION) Return the first Procedimiento filtered by the OBSERVACION_REVISION column
 * @method Procedimiento findOneByFechaAprobacion(string $FECHA_APROBACION) Return the first Procedimiento filtered by the FECHA_APROBACION column
 * @method Procedimiento findOneByTextCambios(string $TEXT_CAMBIOS) Return the first Procedimiento filtered by the TEXT_CAMBIOS column
 * @method Procedimiento findOneByEstadoAprobacion(string $ESTADO_APROBACION) Return the first Procedimiento filtered by the ESTADO_APROBACION column
 *
 * @method array findByProcedimientoId(int $PROCEDIMIENTO_ID) Return Procedimiento objects filtered by the PROCEDIMIENTO_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return Procedimiento objects filtered by the USUARIO_ID column
 * @method array findByTipoprocedimientoId(int $TIPOPROCEDIMIENTO_ID) Return Procedimiento objects filtered by the TIPOPROCEDIMIENTO_ID column
 * @method array findByDependenciaId(int $DEPENDENCIA_ID) Return Procedimiento objects filtered by the DEPENDENCIA_ID column
 * @method array findByCodigo(string $CODIGO) Return Procedimiento objects filtered by the CODIGO column
 * @method array findByNombre(string $NOMBRE) Return Procedimiento objects filtered by the NOMBRE column
 * @method array findByDescripcion(string $DESCRIPCION) Return Procedimiento objects filtered by the DESCRIPCION column
 * @method array findByExtension(string $EXTENSION) Return Procedimiento objects filtered by the EXTENSION column
 * @method array findByRuta(string $RUTA) Return Procedimiento objects filtered by the RUTA column
 * @method array findByVersion(double $VERSION) Return Procedimiento objects filtered by the VERSION column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return Procedimiento objects filtered by the FECHA_CREACION column
 * @method array findByCodigoVersion(int $CODIGO_VERSION) Return Procedimiento objects filtered by the CODIGO_VERSION column
 * @method array findByEsUltimaVersion(boolean $ES_ULTIMA_VERSION) Return Procedimiento objects filtered by the ES_ULTIMA_VERSION column
 * @method array findByEstadoRevision(string $ESTADO_REVISION) Return Procedimiento objects filtered by the ESTADO_REVISION column
 * @method array findByRevisadoPor(string $REVISADO_POR) Return Procedimiento objects filtered by the REVISADO_POR column
 * @method array findByObservacionRevision(string $OBSERVACION_REVISION) Return Procedimiento objects filtered by the OBSERVACION_REVISION column
 * @method array findByFechaAprobacion(string $FECHA_APROBACION) Return Procedimiento objects filtered by the FECHA_APROBACION column
 * @method array findByTextCambios(string $TEXT_CAMBIOS) Return Procedimiento objects filtered by the TEXT_CAMBIOS column
 * @method array findByEstadoAprobacion(string $ESTADO_APROBACION) Return Procedimiento objects filtered by the ESTADO_APROBACION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseProcedimientoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseProcedimientoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Procedimiento';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ProcedimientoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   ProcedimientoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ProcedimientoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ProcedimientoQuery) {
            return $criteria;
        }
        $query = new ProcedimientoQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Procedimiento|Procedimiento[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ProcedimientoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ProcedimientoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Procedimiento A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByProcedimientoId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Procedimiento A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [PROCEDIMIENTO_ID], [USUARIO_ID], [TIPOPROCEDIMIENTO_ID], [DEPENDENCIA_ID], [CODIGO], [NOMBRE], [DESCRIPCION], [EXTENSION], [RUTA], [VERSION], [FECHA_CREACION], [CODIGO_VERSION], [ES_ULTIMA_VERSION], [ESTADO_REVISION], [REVISADO_POR], [OBSERVACION_REVISION], [FECHA_APROBACION], [TEXT_CAMBIOS], [ESTADO_APROBACION] FROM [PROCEDIMIENTO] WHERE [PROCEDIMIENTO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Procedimiento();
            $obj->hydrate($row);
            ProcedimientoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Procedimiento|Procedimiento[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Procedimiento[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the PROCEDIMIENTO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByProcedimientoId(1234); // WHERE PROCEDIMIENTO_ID = 1234
     * $query->filterByProcedimientoId(array(12, 34)); // WHERE PROCEDIMIENTO_ID IN (12, 34)
     * $query->filterByProcedimientoId(array('min' => 12)); // WHERE PROCEDIMIENTO_ID >= 12
     * $query->filterByProcedimientoId(array('max' => 12)); // WHERE PROCEDIMIENTO_ID <= 12
     * </code>
     *
     * @param     mixed $procedimientoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByProcedimientoId($procedimientoId = null, $comparison = null)
    {
        if (is_array($procedimientoId)) {
            $useMinMax = false;
            if (isset($procedimientoId['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $procedimientoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($procedimientoId['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $procedimientoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $procedimientoId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the TIPOPROCEDIMIENTO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoprocedimientoId(1234); // WHERE TIPOPROCEDIMIENTO_ID = 1234
     * $query->filterByTipoprocedimientoId(array(12, 34)); // WHERE TIPOPROCEDIMIENTO_ID IN (12, 34)
     * $query->filterByTipoprocedimientoId(array('min' => 12)); // WHERE TIPOPROCEDIMIENTO_ID >= 12
     * $query->filterByTipoprocedimientoId(array('max' => 12)); // WHERE TIPOPROCEDIMIENTO_ID <= 12
     * </code>
     *
     * @see       filterByTipoProcedimiento()
     *
     * @param     mixed $tipoprocedimientoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByTipoprocedimientoId($tipoprocedimientoId = null, $comparison = null)
    {
        if (is_array($tipoprocedimientoId)) {
            $useMinMax = false;
            if (isset($tipoprocedimientoId['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::TIPOPROCEDIMIENTO_ID, $tipoprocedimientoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipoprocedimientoId['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::TIPOPROCEDIMIENTO_ID, $tipoprocedimientoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::TIPOPROCEDIMIENTO_ID, $tipoprocedimientoId, $comparison);
    }

    /**
     * Filter the query on the DEPENDENCIA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDependenciaId(1234); // WHERE DEPENDENCIA_ID = 1234
     * $query->filterByDependenciaId(array(12, 34)); // WHERE DEPENDENCIA_ID IN (12, 34)
     * $query->filterByDependenciaId(array('min' => 12)); // WHERE DEPENDENCIA_ID >= 12
     * $query->filterByDependenciaId(array('max' => 12)); // WHERE DEPENDENCIA_ID <= 12
     * </code>
     *
     * @see       filterByDependencia()
     *
     * @param     mixed $dependenciaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByDependenciaId($dependenciaId = null, $comparison = null)
    {
        if (is_array($dependenciaId)) {
            $useMinMax = false;
            if (isset($dependenciaId['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::DEPENDENCIA_ID, $dependenciaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($dependenciaId['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::DEPENDENCIA_ID, $dependenciaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::DEPENDENCIA_ID, $dependenciaId, $comparison);
    }

    /**
     * Filter the query on the CODIGO column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE CODIGO = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE CODIGO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE NOMBRE = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the EXTENSION column
     *
     * Example usage:
     * <code>
     * $query->filterByExtension('fooValue');   // WHERE EXTENSION = 'fooValue'
     * $query->filterByExtension('%fooValue%'); // WHERE EXTENSION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $extension The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByExtension($extension = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($extension)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $extension)) {
                $extension = str_replace('*', '%', $extension);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::EXTENSION, $extension, $comparison);
    }

    /**
     * Filter the query on the RUTA column
     *
     * Example usage:
     * <code>
     * $query->filterByRuta('fooValue');   // WHERE RUTA = 'fooValue'
     * $query->filterByRuta('%fooValue%'); // WHERE RUTA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $ruta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByRuta($ruta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($ruta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $ruta)) {
                $ruta = str_replace('*', '%', $ruta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::RUTA, $ruta, $comparison);
    }

    /**
     * Filter the query on the VERSION column
     *
     * Example usage:
     * <code>
     * $query->filterByVersion(1234); // WHERE VERSION = 1234
     * $query->filterByVersion(array(12, 34)); // WHERE VERSION IN (12, 34)
     * $query->filterByVersion(array('min' => 12)); // WHERE VERSION >= 12
     * $query->filterByVersion(array('max' => 12)); // WHERE VERSION <= 12
     * </code>
     *
     * @param     mixed $version The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByVersion($version = null, $comparison = null)
    {
        if (is_array($version)) {
            $useMinMax = false;
            if (isset($version['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::VERSION, $version['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($version['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::VERSION, $version['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::VERSION, $version, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the CODIGO_VERSION column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoVersion(1234); // WHERE CODIGO_VERSION = 1234
     * $query->filterByCodigoVersion(array(12, 34)); // WHERE CODIGO_VERSION IN (12, 34)
     * $query->filterByCodigoVersion(array('min' => 12)); // WHERE CODIGO_VERSION >= 12
     * $query->filterByCodigoVersion(array('max' => 12)); // WHERE CODIGO_VERSION <= 12
     * </code>
     *
     * @param     mixed $codigoVersion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByCodigoVersion($codigoVersion = null, $comparison = null)
    {
        if (is_array($codigoVersion)) {
            $useMinMax = false;
            if (isset($codigoVersion['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::CODIGO_VERSION, $codigoVersion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($codigoVersion['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::CODIGO_VERSION, $codigoVersion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::CODIGO_VERSION, $codigoVersion, $comparison);
    }

    /**
     * Filter the query on the ES_ULTIMA_VERSION column
     *
     * Example usage:
     * <code>
     * $query->filterByEsUltimaVersion(true); // WHERE ES_ULTIMA_VERSION = true
     * $query->filterByEsUltimaVersion('yes'); // WHERE ES_ULTIMA_VERSION = true
     * </code>
     *
     * @param     boolean|string $esUltimaVersion The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByEsUltimaVersion($esUltimaVersion = null, $comparison = null)
    {
        if (is_string($esUltimaVersion)) {
            $esUltimaVersion = in_array(strtolower($esUltimaVersion), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ProcedimientoPeer::ES_ULTIMA_VERSION, $esUltimaVersion, $comparison);
    }

    /**
     * Filter the query on the ESTADO_REVISION column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoRevision('fooValue');   // WHERE ESTADO_REVISION = 'fooValue'
     * $query->filterByEstadoRevision('%fooValue%'); // WHERE ESTADO_REVISION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estadoRevision The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByEstadoRevision($estadoRevision = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estadoRevision)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estadoRevision)) {
                $estadoRevision = str_replace('*', '%', $estadoRevision);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::ESTADO_REVISION, $estadoRevision, $comparison);
    }

    /**
     * Filter the query on the REVISADO_POR column
     *
     * Example usage:
     * <code>
     * $query->filterByRevisadoPor('fooValue');   // WHERE REVISADO_POR = 'fooValue'
     * $query->filterByRevisadoPor('%fooValue%'); // WHERE REVISADO_POR LIKE '%fooValue%'
     * </code>
     *
     * @param     string $revisadoPor The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByRevisadoPor($revisadoPor = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($revisadoPor)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $revisadoPor)) {
                $revisadoPor = str_replace('*', '%', $revisadoPor);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::REVISADO_POR, $revisadoPor, $comparison);
    }

    /**
     * Filter the query on the OBSERVACION_REVISION column
     *
     * Example usage:
     * <code>
     * $query->filterByObservacionRevision('fooValue');   // WHERE OBSERVACION_REVISION = 'fooValue'
     * $query->filterByObservacionRevision('%fooValue%'); // WHERE OBSERVACION_REVISION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observacionRevision The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByObservacionRevision($observacionRevision = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observacionRevision)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observacionRevision)) {
                $observacionRevision = str_replace('*', '%', $observacionRevision);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::OBSERVACION_REVISION, $observacionRevision, $comparison);
    }

    /**
     * Filter the query on the FECHA_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaAprobacion('2011-03-14'); // WHERE FECHA_APROBACION = '2011-03-14'
     * $query->filterByFechaAprobacion('now'); // WHERE FECHA_APROBACION = '2011-03-14'
     * $query->filterByFechaAprobacion(array('max' => 'yesterday')); // WHERE FECHA_APROBACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaAprobacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByFechaAprobacion($fechaAprobacion = null, $comparison = null)
    {
        if (is_array($fechaAprobacion)) {
            $useMinMax = false;
            if (isset($fechaAprobacion['min'])) {
                $this->addUsingAlias(ProcedimientoPeer::FECHA_APROBACION, $fechaAprobacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaAprobacion['max'])) {
                $this->addUsingAlias(ProcedimientoPeer::FECHA_APROBACION, $fechaAprobacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::FECHA_APROBACION, $fechaAprobacion, $comparison);
    }

    /**
     * Filter the query on the TEXT_CAMBIOS column
     *
     * Example usage:
     * <code>
     * $query->filterByTextCambios('fooValue');   // WHERE TEXT_CAMBIOS = 'fooValue'
     * $query->filterByTextCambios('%fooValue%'); // WHERE TEXT_CAMBIOS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $textCambios The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByTextCambios($textCambios = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($textCambios)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $textCambios)) {
                $textCambios = str_replace('*', '%', $textCambios);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::TEXT_CAMBIOS, $textCambios, $comparison);
    }

    /**
     * Filter the query on the ESTADO_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoAprobacion('fooValue');   // WHERE ESTADO_APROBACION = 'fooValue'
     * $query->filterByEstadoAprobacion('%fooValue%'); // WHERE ESTADO_APROBACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estadoAprobacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function filterByEstadoAprobacion($estadoAprobacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estadoAprobacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estadoAprobacion)) {
                $estadoAprobacion = str_replace('*', '%', $estadoAprobacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoPeer::ESTADO_APROBACION, $estadoAprobacion, $comparison);
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcedimientoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(ProcedimientoPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ProcedimientoPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related TipoProcedimiento object
     *
     * @param   TipoProcedimiento|PropelObjectCollection $tipoProcedimiento The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcedimientoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoProcedimiento($tipoProcedimiento, $comparison = null)
    {
        if ($tipoProcedimiento instanceof TipoProcedimiento) {
            return $this
                ->addUsingAlias(ProcedimientoPeer::TIPOPROCEDIMIENTO_ID, $tipoProcedimiento->getTipoprocedimientoId(), $comparison);
        } elseif ($tipoProcedimiento instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ProcedimientoPeer::TIPOPROCEDIMIENTO_ID, $tipoProcedimiento->toKeyValue('PrimaryKey', 'TipoprocedimientoId'), $comparison);
        } else {
            throw new PropelException('filterByTipoProcedimiento() only accepts arguments of type TipoProcedimiento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoProcedimiento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function joinTipoProcedimiento($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoProcedimiento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoProcedimiento');
        }

        return $this;
    }

    /**
     * Use the TipoProcedimiento relation TipoProcedimiento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoProcedimientoQuery A secondary query class using the current class as primary query
     */
    public function useTipoProcedimientoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTipoProcedimiento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoProcedimiento', 'TipoProcedimientoQuery');
    }

    /**
     * Filter the query by a related Dependencia object
     *
     * @param   Dependencia|PropelObjectCollection $dependencia The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcedimientoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDependencia($dependencia, $comparison = null)
    {
        if ($dependencia instanceof Dependencia) {
            return $this
                ->addUsingAlias(ProcedimientoPeer::DEPENDENCIA_ID, $dependencia->getDependenciaId(), $comparison);
        } elseif ($dependencia instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ProcedimientoPeer::DEPENDENCIA_ID, $dependencia->toKeyValue('PrimaryKey', 'DependenciaId'), $comparison);
        } else {
            throw new PropelException('filterByDependencia() only accepts arguments of type Dependencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Dependencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function joinDependencia($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Dependencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Dependencia');
        }

        return $this;
    }

    /**
     * Use the Dependencia relation Dependencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DependenciaQuery A secondary query class using the current class as primary query
     */
    public function useDependenciaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinDependencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Dependencia', 'DependenciaQuery');
    }

    /**
     * Filter the query by a related ProcedimientoAprob object
     *
     * @param   ProcedimientoAprob|PropelObjectCollection $procedimientoAprob  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcedimientoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByProcedimientoAprob($procedimientoAprob, $comparison = null)
    {
        if ($procedimientoAprob instanceof ProcedimientoAprob) {
            return $this
                ->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $procedimientoAprob->getProcedimientoId(), $comparison);
        } elseif ($procedimientoAprob instanceof PropelObjectCollection) {
            return $this
                ->useProcedimientoAprobQuery()
                ->filterByPrimaryKeys($procedimientoAprob->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProcedimientoAprob() only accepts arguments of type ProcedimientoAprob or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProcedimientoAprob relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function joinProcedimientoAprob($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProcedimientoAprob');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProcedimientoAprob');
        }

        return $this;
    }

    /**
     * Use the ProcedimientoAprob relation ProcedimientoAprob object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProcedimientoAprobQuery A secondary query class using the current class as primary query
     */
    public function useProcedimientoAprobQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinProcedimientoAprob($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProcedimientoAprob', 'ProcedimientoAprobQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Procedimiento $procedimiento Object to remove from the list of results
     *
     * @return ProcedimientoQuery The current query, for fluid interface
     */
    public function prune($procedimiento = null)
    {
        if ($procedimiento) {
            $this->addUsingAlias(ProcedimientoPeer::PROCEDIMIENTO_ID, $procedimiento->getProcedimientoId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

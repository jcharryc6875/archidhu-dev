<?php


/**
 * Base class that represents a query for the 'FACT_HIST' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:53
 *
 * @method FactHistQuery orderByFacthistId($order = Criteria::ASC) Order by the FACTHIST_ID column
 * @method FactHistQuery orderByRegionalId($order = Criteria::ASC) Order by the REGIONAL_ID column
 * @method FactHistQuery orderByNumeroFact($order = Criteria::ASC) Order by the NUMERO_FACT column
 * @method FactHistQuery orderByFecha($order = Criteria::ASC) Order by the FECHA column
 * @method FactHistQuery orderByRadicado($order = Criteria::ASC) Order by the RADICADO column
 * @method FactHistQuery orderByEmpresaOrigen($order = Criteria::ASC) Order by the EMPRESA_ORIGEN column
 * @method FactHistQuery orderByCodigoBarras($order = Criteria::ASC) Order by the CODIGO_BARRAS column
 * @method FactHistQuery orderByValor($order = Criteria::ASC) Order by the VALOR column
 * @method FactHistQuery orderByAprobador($order = Criteria::ASC) Order by the APROBADOR column
 * @method FactHistQuery orderByUen($order = Criteria::ASC) Order by the UEN column
 * @method FactHistQuery orderByUo($order = Criteria::ASC) Order by the UO column
 * @method FactHistQuery orderByCuentaPuc($order = Criteria::ASC) Order by the CUENTA_PUC column
 * @method FactHistQuery orderByCausador($order = Criteria::ASC) Order by the CAUSADOR column
 * @method FactHistQuery orderByValorCausado($order = Criteria::ASC) Order by the VALOR_CAUSADO column
 * @method FactHistQuery orderByBatchCausacion($order = Criteria::ASC) Order by the BATCH_CAUSACION column
 * @method FactHistQuery orderByIdCausacion($order = Criteria::ASC) Order by the ID_CAUSACION column
 * @method FactHistQuery orderByFacturaCausacion($order = Criteria::ASC) Order by the FACTURA_CAUSACION column
 * @method FactHistQuery orderByLotePago($order = Criteria::ASC) Order by the LOTE_PAGO column
 * @method FactHistQuery orderByBatchTesoreria($order = Criteria::ASC) Order by the BATCH_TESORERIA column
 * @method FactHistQuery orderByPeriodo($order = Criteria::ASC) Order by the PERIODO column
 * @method FactHistQuery orderByNotaCartera($order = Criteria::ASC) Order by the NOTA_CARTERA column
 * @method FactHistQuery orderByBatchCartera($order = Criteria::ASC) Order by the BATCH_CARTERA column
 * @method FactHistQuery orderByPagador($order = Criteria::ASC) Order by the PAGADOR column
 * @method FactHistQuery orderByCodBarrasGestion($order = Criteria::ASC) Order by the COD_BARRAS_GESTION column
 * @method FactHistQuery orderByRuta($order = Criteria::ASC) Order by the RUTA column
 * @method FactHistQuery orderByEstado($order = Criteria::ASC) Order by the ESTADO column
 * @method FactHistQuery orderByEstadoDesc($order = Criteria::ASC) Order by the ESTADO_DESC column
 * @method FactHistQuery orderByDestino($order = Criteria::ASC) Order by the DESTINO column
 * @method FactHistQuery orderBySoportes($order = Criteria::ASC) Order by the SOPORTES column
 * @method FactHistQuery orderByObs($order = Criteria::ASC) Order by the OBS column
 * @method FactHistQuery orderByAsunto($order = Criteria::ASC) Order by the ASUNTO column
 * @method FactHistQuery orderByTemporal1($order = Criteria::ASC) Order by the TEMPORAL1 column
 * @method FactHistQuery orderByEmporal2($order = Criteria::ASC) Order by the EMPORAL2 column
 * @method FactHistQuery orderByTemporal3($order = Criteria::ASC) Order by the TEMPORAL3 column
 * @method FactHistQuery orderByEmporal4($order = Criteria::ASC) Order by the EMPORAL4 column
 * @method FactHistQuery orderByTemporal5($order = Criteria::ASC) Order by the TEMPORAL5 column
 * @method FactHistQuery orderByDependenciaNombre($order = Criteria::ASC) Order by the DEPENDENCIA_NOMBRE column
 *
 * @method FactHistQuery groupByFacthistId() Group by the FACTHIST_ID column
 * @method FactHistQuery groupByRegionalId() Group by the REGIONAL_ID column
 * @method FactHistQuery groupByNumeroFact() Group by the NUMERO_FACT column
 * @method FactHistQuery groupByFecha() Group by the FECHA column
 * @method FactHistQuery groupByRadicado() Group by the RADICADO column
 * @method FactHistQuery groupByEmpresaOrigen() Group by the EMPRESA_ORIGEN column
 * @method FactHistQuery groupByCodigoBarras() Group by the CODIGO_BARRAS column
 * @method FactHistQuery groupByValor() Group by the VALOR column
 * @method FactHistQuery groupByAprobador() Group by the APROBADOR column
 * @method FactHistQuery groupByUen() Group by the UEN column
 * @method FactHistQuery groupByUo() Group by the UO column
 * @method FactHistQuery groupByCuentaPuc() Group by the CUENTA_PUC column
 * @method FactHistQuery groupByCausador() Group by the CAUSADOR column
 * @method FactHistQuery groupByValorCausado() Group by the VALOR_CAUSADO column
 * @method FactHistQuery groupByBatchCausacion() Group by the BATCH_CAUSACION column
 * @method FactHistQuery groupByIdCausacion() Group by the ID_CAUSACION column
 * @method FactHistQuery groupByFacturaCausacion() Group by the FACTURA_CAUSACION column
 * @method FactHistQuery groupByLotePago() Group by the LOTE_PAGO column
 * @method FactHistQuery groupByBatchTesoreria() Group by the BATCH_TESORERIA column
 * @method FactHistQuery groupByPeriodo() Group by the PERIODO column
 * @method FactHistQuery groupByNotaCartera() Group by the NOTA_CARTERA column
 * @method FactHistQuery groupByBatchCartera() Group by the BATCH_CARTERA column
 * @method FactHistQuery groupByPagador() Group by the PAGADOR column
 * @method FactHistQuery groupByCodBarrasGestion() Group by the COD_BARRAS_GESTION column
 * @method FactHistQuery groupByRuta() Group by the RUTA column
 * @method FactHistQuery groupByEstado() Group by the ESTADO column
 * @method FactHistQuery groupByEstadoDesc() Group by the ESTADO_DESC column
 * @method FactHistQuery groupByDestino() Group by the DESTINO column
 * @method FactHistQuery groupBySoportes() Group by the SOPORTES column
 * @method FactHistQuery groupByObs() Group by the OBS column
 * @method FactHistQuery groupByAsunto() Group by the ASUNTO column
 * @method FactHistQuery groupByTemporal1() Group by the TEMPORAL1 column
 * @method FactHistQuery groupByEmporal2() Group by the EMPORAL2 column
 * @method FactHistQuery groupByTemporal3() Group by the TEMPORAL3 column
 * @method FactHistQuery groupByEmporal4() Group by the EMPORAL4 column
 * @method FactHistQuery groupByTemporal5() Group by the TEMPORAL5 column
 * @method FactHistQuery groupByDependenciaNombre() Group by the DEPENDENCIA_NOMBRE column
 *
 * @method FactHistQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method FactHistQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method FactHistQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method FactHistQuery leftJoinRegional($relationAlias = null) Adds a LEFT JOIN clause to the query using the Regional relation
 * @method FactHistQuery rightJoinRegional($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Regional relation
 * @method FactHistQuery innerJoinRegional($relationAlias = null) Adds a INNER JOIN clause to the query using the Regional relation
 *
 * @method FactHistQuery leftJoinFactHistVitacora($relationAlias = null) Adds a LEFT JOIN clause to the query using the FactHistVitacora relation
 * @method FactHistQuery rightJoinFactHistVitacora($relationAlias = null) Adds a RIGHT JOIN clause to the query using the FactHistVitacora relation
 * @method FactHistQuery innerJoinFactHistVitacora($relationAlias = null) Adds a INNER JOIN clause to the query using the FactHistVitacora relation
 *
 * @method FactHist findOne(PropelPDO $con = null) Return the first FactHist matching the query
 * @method FactHist findOneOrCreate(PropelPDO $con = null) Return the first FactHist matching the query, or a new FactHist object populated from the query conditions when no match is found
 *
 * @method FactHist findOneByRegionalId(int $REGIONAL_ID) Return the first FactHist filtered by the REGIONAL_ID column
 * @method FactHist findOneByNumeroFact(string $NUMERO_FACT) Return the first FactHist filtered by the NUMERO_FACT column
 * @method FactHist findOneByFecha(string $FECHA) Return the first FactHist filtered by the FECHA column
 * @method FactHist findOneByRadicado(string $RADICADO) Return the first FactHist filtered by the RADICADO column
 * @method FactHist findOneByEmpresaOrigen(string $EMPRESA_ORIGEN) Return the first FactHist filtered by the EMPRESA_ORIGEN column
 * @method FactHist findOneByCodigoBarras(string $CODIGO_BARRAS) Return the first FactHist filtered by the CODIGO_BARRAS column
 * @method FactHist findOneByValor(int $VALOR) Return the first FactHist filtered by the VALOR column
 * @method FactHist findOneByAprobador(string $APROBADOR) Return the first FactHist filtered by the APROBADOR column
 * @method FactHist findOneByUen(string $UEN) Return the first FactHist filtered by the UEN column
 * @method FactHist findOneByUo(string $UO) Return the first FactHist filtered by the UO column
 * @method FactHist findOneByCuentaPuc(string $CUENTA_PUC) Return the first FactHist filtered by the CUENTA_PUC column
 * @method FactHist findOneByCausador(string $CAUSADOR) Return the first FactHist filtered by the CAUSADOR column
 * @method FactHist findOneByValorCausado(string $VALOR_CAUSADO) Return the first FactHist filtered by the VALOR_CAUSADO column
 * @method FactHist findOneByBatchCausacion(string $BATCH_CAUSACION) Return the first FactHist filtered by the BATCH_CAUSACION column
 * @method FactHist findOneByIdCausacion(string $ID_CAUSACION) Return the first FactHist filtered by the ID_CAUSACION column
 * @method FactHist findOneByFacturaCausacion(string $FACTURA_CAUSACION) Return the first FactHist filtered by the FACTURA_CAUSACION column
 * @method FactHist findOneByLotePago(string $LOTE_PAGO) Return the first FactHist filtered by the LOTE_PAGO column
 * @method FactHist findOneByBatchTesoreria(string $BATCH_TESORERIA) Return the first FactHist filtered by the BATCH_TESORERIA column
 * @method FactHist findOneByPeriodo(int $PERIODO) Return the first FactHist filtered by the PERIODO column
 * @method FactHist findOneByNotaCartera(string $NOTA_CARTERA) Return the first FactHist filtered by the NOTA_CARTERA column
 * @method FactHist findOneByBatchCartera(string $BATCH_CARTERA) Return the first FactHist filtered by the BATCH_CARTERA column
 * @method FactHist findOneByPagador(string $PAGADOR) Return the first FactHist filtered by the PAGADOR column
 * @method FactHist findOneByCodBarrasGestion(string $COD_BARRAS_GESTION) Return the first FactHist filtered by the COD_BARRAS_GESTION column
 * @method FactHist findOneByRuta(string $RUTA) Return the first FactHist filtered by the RUTA column
 * @method FactHist findOneByEstado(string $ESTADO) Return the first FactHist filtered by the ESTADO column
 * @method FactHist findOneByEstadoDesc(string $ESTADO_DESC) Return the first FactHist filtered by the ESTADO_DESC column
 * @method FactHist findOneByDestino(string $DESTINO) Return the first FactHist filtered by the DESTINO column
 * @method FactHist findOneBySoportes(string $SOPORTES) Return the first FactHist filtered by the SOPORTES column
 * @method FactHist findOneByObs(string $OBS) Return the first FactHist filtered by the OBS column
 * @method FactHist findOneByAsunto(string $ASUNTO) Return the first FactHist filtered by the ASUNTO column
 * @method FactHist findOneByTemporal1(string $TEMPORAL1) Return the first FactHist filtered by the TEMPORAL1 column
 * @method FactHist findOneByEmporal2(string $EMPORAL2) Return the first FactHist filtered by the EMPORAL2 column
 * @method FactHist findOneByTemporal3(string $TEMPORAL3) Return the first FactHist filtered by the TEMPORAL3 column
 * @method FactHist findOneByEmporal4(string $EMPORAL4) Return the first FactHist filtered by the EMPORAL4 column
 * @method FactHist findOneByTemporal5(string $TEMPORAL5) Return the first FactHist filtered by the TEMPORAL5 column
 * @method FactHist findOneByDependenciaNombre(string $DEPENDENCIA_NOMBRE) Return the first FactHist filtered by the DEPENDENCIA_NOMBRE column
 *
 * @method array findByFacthistId(int $FACTHIST_ID) Return FactHist objects filtered by the FACTHIST_ID column
 * @method array findByRegionalId(int $REGIONAL_ID) Return FactHist objects filtered by the REGIONAL_ID column
 * @method array findByNumeroFact(string $NUMERO_FACT) Return FactHist objects filtered by the NUMERO_FACT column
 * @method array findByFecha(string $FECHA) Return FactHist objects filtered by the FECHA column
 * @method array findByRadicado(string $RADICADO) Return FactHist objects filtered by the RADICADO column
 * @method array findByEmpresaOrigen(string $EMPRESA_ORIGEN) Return FactHist objects filtered by the EMPRESA_ORIGEN column
 * @method array findByCodigoBarras(string $CODIGO_BARRAS) Return FactHist objects filtered by the CODIGO_BARRAS column
 * @method array findByValor(int $VALOR) Return FactHist objects filtered by the VALOR column
 * @method array findByAprobador(string $APROBADOR) Return FactHist objects filtered by the APROBADOR column
 * @method array findByUen(string $UEN) Return FactHist objects filtered by the UEN column
 * @method array findByUo(string $UO) Return FactHist objects filtered by the UO column
 * @method array findByCuentaPuc(string $CUENTA_PUC) Return FactHist objects filtered by the CUENTA_PUC column
 * @method array findByCausador(string $CAUSADOR) Return FactHist objects filtered by the CAUSADOR column
 * @method array findByValorCausado(string $VALOR_CAUSADO) Return FactHist objects filtered by the VALOR_CAUSADO column
 * @method array findByBatchCausacion(string $BATCH_CAUSACION) Return FactHist objects filtered by the BATCH_CAUSACION column
 * @method array findByIdCausacion(string $ID_CAUSACION) Return FactHist objects filtered by the ID_CAUSACION column
 * @method array findByFacturaCausacion(string $FACTURA_CAUSACION) Return FactHist objects filtered by the FACTURA_CAUSACION column
 * @method array findByLotePago(string $LOTE_PAGO) Return FactHist objects filtered by the LOTE_PAGO column
 * @method array findByBatchTesoreria(string $BATCH_TESORERIA) Return FactHist objects filtered by the BATCH_TESORERIA column
 * @method array findByPeriodo(int $PERIODO) Return FactHist objects filtered by the PERIODO column
 * @method array findByNotaCartera(string $NOTA_CARTERA) Return FactHist objects filtered by the NOTA_CARTERA column
 * @method array findByBatchCartera(string $BATCH_CARTERA) Return FactHist objects filtered by the BATCH_CARTERA column
 * @method array findByPagador(string $PAGADOR) Return FactHist objects filtered by the PAGADOR column
 * @method array findByCodBarrasGestion(string $COD_BARRAS_GESTION) Return FactHist objects filtered by the COD_BARRAS_GESTION column
 * @method array findByRuta(string $RUTA) Return FactHist objects filtered by the RUTA column
 * @method array findByEstado(string $ESTADO) Return FactHist objects filtered by the ESTADO column
 * @method array findByEstadoDesc(string $ESTADO_DESC) Return FactHist objects filtered by the ESTADO_DESC column
 * @method array findByDestino(string $DESTINO) Return FactHist objects filtered by the DESTINO column
 * @method array findBySoportes(string $SOPORTES) Return FactHist objects filtered by the SOPORTES column
 * @method array findByObs(string $OBS) Return FactHist objects filtered by the OBS column
 * @method array findByAsunto(string $ASUNTO) Return FactHist objects filtered by the ASUNTO column
 * @method array findByTemporal1(string $TEMPORAL1) Return FactHist objects filtered by the TEMPORAL1 column
 * @method array findByEmporal2(string $EMPORAL2) Return FactHist objects filtered by the EMPORAL2 column
 * @method array findByTemporal3(string $TEMPORAL3) Return FactHist objects filtered by the TEMPORAL3 column
 * @method array findByEmporal4(string $EMPORAL4) Return FactHist objects filtered by the EMPORAL4 column
 * @method array findByTemporal5(string $TEMPORAL5) Return FactHist objects filtered by the TEMPORAL5 column
 * @method array findByDependenciaNombre(string $DEPENDENCIA_NOMBRE) Return FactHist objects filtered by the DEPENDENCIA_NOMBRE column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseFactHistQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseFactHistQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'FactHist';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new FactHistQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   FactHistQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return FactHistQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof FactHistQuery) {
            return $criteria;
        }
        $query = new FactHistQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   FactHist|FactHist[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = FactHistPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(FactHistPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 FactHist A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByFacthistId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 FactHist A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [FACTHIST_ID], [REGIONAL_ID], [NUMERO_FACT], [FECHA], [RADICADO], [EMPRESA_ORIGEN], [CODIGO_BARRAS], [VALOR], [APROBADOR], [UEN], [UO], [CUENTA_PUC], [CAUSADOR], [VALOR_CAUSADO], [BATCH_CAUSACION], [ID_CAUSACION], [FACTURA_CAUSACION], [LOTE_PAGO], [BATCH_TESORERIA], [PERIODO], [NOTA_CARTERA], [BATCH_CARTERA], [PAGADOR], [COD_BARRAS_GESTION], [RUTA], [ESTADO], [ESTADO_DESC], [DESTINO], [SOPORTES], [OBS], [ASUNTO], [TEMPORAL1], [EMPORAL2], [TEMPORAL3], [EMPORAL4], [TEMPORAL5], [DEPENDENCIA_NOMBRE] FROM [FACT_HIST] WHERE [FACTHIST_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new FactHist();
            $obj->hydrate($row);
            FactHistPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return FactHist|FactHist[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|FactHist[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(FactHistPeer::FACTHIST_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(FactHistPeer::FACTHIST_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the FACTHIST_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByFacthistId(1234); // WHERE FACTHIST_ID = 1234
     * $query->filterByFacthistId(array(12, 34)); // WHERE FACTHIST_ID IN (12, 34)
     * $query->filterByFacthistId(array('min' => 12)); // WHERE FACTHIST_ID >= 12
     * $query->filterByFacthistId(array('max' => 12)); // WHERE FACTHIST_ID <= 12
     * </code>
     *
     * @param     mixed $facthistId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByFacthistId($facthistId = null, $comparison = null)
    {
        if (is_array($facthistId)) {
            $useMinMax = false;
            if (isset($facthistId['min'])) {
                $this->addUsingAlias(FactHistPeer::FACTHIST_ID, $facthistId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($facthistId['max'])) {
                $this->addUsingAlias(FactHistPeer::FACTHIST_ID, $facthistId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactHistPeer::FACTHIST_ID, $facthistId, $comparison);
    }

    /**
     * Filter the query on the REGIONAL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRegionalId(1234); // WHERE REGIONAL_ID = 1234
     * $query->filterByRegionalId(array(12, 34)); // WHERE REGIONAL_ID IN (12, 34)
     * $query->filterByRegionalId(array('min' => 12)); // WHERE REGIONAL_ID >= 12
     * $query->filterByRegionalId(array('max' => 12)); // WHERE REGIONAL_ID <= 12
     * </code>
     *
     * @see       filterByRegional()
     *
     * @param     mixed $regionalId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByRegionalId($regionalId = null, $comparison = null)
    {
        if (is_array($regionalId)) {
            $useMinMax = false;
            if (isset($regionalId['min'])) {
                $this->addUsingAlias(FactHistPeer::REGIONAL_ID, $regionalId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($regionalId['max'])) {
                $this->addUsingAlias(FactHistPeer::REGIONAL_ID, $regionalId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactHistPeer::REGIONAL_ID, $regionalId, $comparison);
    }

    /**
     * Filter the query on the NUMERO_FACT column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroFact('fooValue');   // WHERE NUMERO_FACT = 'fooValue'
     * $query->filterByNumeroFact('%fooValue%'); // WHERE NUMERO_FACT LIKE '%fooValue%'
     * </code>
     *
     * @param     string $numeroFact The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByNumeroFact($numeroFact = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($numeroFact)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $numeroFact)) {
                $numeroFact = str_replace('*', '%', $numeroFact);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::NUMERO_FACT, $numeroFact, $comparison);
    }

    /**
     * Filter the query on the FECHA column
     *
     * Example usage:
     * <code>
     * $query->filterByFecha('2011-03-14'); // WHERE FECHA = '2011-03-14'
     * $query->filterByFecha('now'); // WHERE FECHA = '2011-03-14'
     * $query->filterByFecha(array('max' => 'yesterday')); // WHERE FECHA < '2011-03-13'
     * </code>
     *
     * @param     mixed $fecha The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByFecha($fecha = null, $comparison = null)
    {
        if (is_array($fecha)) {
            $useMinMax = false;
            if (isset($fecha['min'])) {
                $this->addUsingAlias(FactHistPeer::FECHA, $fecha['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fecha['max'])) {
                $this->addUsingAlias(FactHistPeer::FECHA, $fecha['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactHistPeer::FECHA, $fecha, $comparison);
    }

    /**
     * Filter the query on the RADICADO column
     *
     * Example usage:
     * <code>
     * $query->filterByRadicado('fooValue');   // WHERE RADICADO = 'fooValue'
     * $query->filterByRadicado('%fooValue%'); // WHERE RADICADO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $radicado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByRadicado($radicado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($radicado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $radicado)) {
                $radicado = str_replace('*', '%', $radicado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::RADICADO, $radicado, $comparison);
    }

    /**
     * Filter the query on the EMPRESA_ORIGEN column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaOrigen('fooValue');   // WHERE EMPRESA_ORIGEN = 'fooValue'
     * $query->filterByEmpresaOrigen('%fooValue%'); // WHERE EMPRESA_ORIGEN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $empresaOrigen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByEmpresaOrigen($empresaOrigen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($empresaOrigen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $empresaOrigen)) {
                $empresaOrigen = str_replace('*', '%', $empresaOrigen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::EMPRESA_ORIGEN, $empresaOrigen, $comparison);
    }

    /**
     * Filter the query on the CODIGO_BARRAS column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoBarras('fooValue');   // WHERE CODIGO_BARRAS = 'fooValue'
     * $query->filterByCodigoBarras('%fooValue%'); // WHERE CODIGO_BARRAS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigoBarras The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByCodigoBarras($codigoBarras = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigoBarras)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigoBarras)) {
                $codigoBarras = str_replace('*', '%', $codigoBarras);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::CODIGO_BARRAS, $codigoBarras, $comparison);
    }

    /**
     * Filter the query on the VALOR column
     *
     * Example usage:
     * <code>
     * $query->filterByValor(1234); // WHERE VALOR = 1234
     * $query->filterByValor(array(12, 34)); // WHERE VALOR IN (12, 34)
     * $query->filterByValor(array('min' => 12)); // WHERE VALOR >= 12
     * $query->filterByValor(array('max' => 12)); // WHERE VALOR <= 12
     * </code>
     *
     * @param     mixed $valor The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByValor($valor = null, $comparison = null)
    {
        if (is_array($valor)) {
            $useMinMax = false;
            if (isset($valor['min'])) {
                $this->addUsingAlias(FactHistPeer::VALOR, $valor['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valor['max'])) {
                $this->addUsingAlias(FactHistPeer::VALOR, $valor['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactHistPeer::VALOR, $valor, $comparison);
    }

    /**
     * Filter the query on the APROBADOR column
     *
     * Example usage:
     * <code>
     * $query->filterByAprobador('fooValue');   // WHERE APROBADOR = 'fooValue'
     * $query->filterByAprobador('%fooValue%'); // WHERE APROBADOR LIKE '%fooValue%'
     * </code>
     *
     * @param     string $aprobador The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByAprobador($aprobador = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($aprobador)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $aprobador)) {
                $aprobador = str_replace('*', '%', $aprobador);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::APROBADOR, $aprobador, $comparison);
    }

    /**
     * Filter the query on the UEN column
     *
     * Example usage:
     * <code>
     * $query->filterByUen('fooValue');   // WHERE UEN = 'fooValue'
     * $query->filterByUen('%fooValue%'); // WHERE UEN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $uen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByUen($uen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($uen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $uen)) {
                $uen = str_replace('*', '%', $uen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::UEN, $uen, $comparison);
    }

    /**
     * Filter the query on the UO column
     *
     * Example usage:
     * <code>
     * $query->filterByUo('fooValue');   // WHERE UO = 'fooValue'
     * $query->filterByUo('%fooValue%'); // WHERE UO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $uo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByUo($uo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($uo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $uo)) {
                $uo = str_replace('*', '%', $uo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::UO, $uo, $comparison);
    }

    /**
     * Filter the query on the CUENTA_PUC column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaPuc('fooValue');   // WHERE CUENTA_PUC = 'fooValue'
     * $query->filterByCuentaPuc('%fooValue%'); // WHERE CUENTA_PUC LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaPuc The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByCuentaPuc($cuentaPuc = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaPuc)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaPuc)) {
                $cuentaPuc = str_replace('*', '%', $cuentaPuc);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::CUENTA_PUC, $cuentaPuc, $comparison);
    }

    /**
     * Filter the query on the CAUSADOR column
     *
     * Example usage:
     * <code>
     * $query->filterByCausador('fooValue');   // WHERE CAUSADOR = 'fooValue'
     * $query->filterByCausador('%fooValue%'); // WHERE CAUSADOR LIKE '%fooValue%'
     * </code>
     *
     * @param     string $causador The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByCausador($causador = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($causador)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $causador)) {
                $causador = str_replace('*', '%', $causador);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::CAUSADOR, $causador, $comparison);
    }

    /**
     * Filter the query on the VALOR_CAUSADO column
     *
     * Example usage:
     * <code>
     * $query->filterByValorCausado('fooValue');   // WHERE VALOR_CAUSADO = 'fooValue'
     * $query->filterByValorCausado('%fooValue%'); // WHERE VALOR_CAUSADO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $valorCausado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByValorCausado($valorCausado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($valorCausado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $valorCausado)) {
                $valorCausado = str_replace('*', '%', $valorCausado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::VALOR_CAUSADO, $valorCausado, $comparison);
    }

    /**
     * Filter the query on the BATCH_CAUSACION column
     *
     * Example usage:
     * <code>
     * $query->filterByBatchCausacion('fooValue');   // WHERE BATCH_CAUSACION = 'fooValue'
     * $query->filterByBatchCausacion('%fooValue%'); // WHERE BATCH_CAUSACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $batchCausacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByBatchCausacion($batchCausacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($batchCausacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $batchCausacion)) {
                $batchCausacion = str_replace('*', '%', $batchCausacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::BATCH_CAUSACION, $batchCausacion, $comparison);
    }

    /**
     * Filter the query on the ID_CAUSACION column
     *
     * Example usage:
     * <code>
     * $query->filterByIdCausacion('fooValue');   // WHERE ID_CAUSACION = 'fooValue'
     * $query->filterByIdCausacion('%fooValue%'); // WHERE ID_CAUSACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $idCausacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByIdCausacion($idCausacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($idCausacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $idCausacion)) {
                $idCausacion = str_replace('*', '%', $idCausacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::ID_CAUSACION, $idCausacion, $comparison);
    }

    /**
     * Filter the query on the FACTURA_CAUSACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFacturaCausacion('fooValue');   // WHERE FACTURA_CAUSACION = 'fooValue'
     * $query->filterByFacturaCausacion('%fooValue%'); // WHERE FACTURA_CAUSACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $facturaCausacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByFacturaCausacion($facturaCausacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($facturaCausacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $facturaCausacion)) {
                $facturaCausacion = str_replace('*', '%', $facturaCausacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::FACTURA_CAUSACION, $facturaCausacion, $comparison);
    }

    /**
     * Filter the query on the LOTE_PAGO column
     *
     * Example usage:
     * <code>
     * $query->filterByLotePago('fooValue');   // WHERE LOTE_PAGO = 'fooValue'
     * $query->filterByLotePago('%fooValue%'); // WHERE LOTE_PAGO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $lotePago The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByLotePago($lotePago = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($lotePago)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $lotePago)) {
                $lotePago = str_replace('*', '%', $lotePago);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::LOTE_PAGO, $lotePago, $comparison);
    }

    /**
     * Filter the query on the BATCH_TESORERIA column
     *
     * Example usage:
     * <code>
     * $query->filterByBatchTesoreria('fooValue');   // WHERE BATCH_TESORERIA = 'fooValue'
     * $query->filterByBatchTesoreria('%fooValue%'); // WHERE BATCH_TESORERIA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $batchTesoreria The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByBatchTesoreria($batchTesoreria = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($batchTesoreria)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $batchTesoreria)) {
                $batchTesoreria = str_replace('*', '%', $batchTesoreria);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::BATCH_TESORERIA, $batchTesoreria, $comparison);
    }

    /**
     * Filter the query on the PERIODO column
     *
     * Example usage:
     * <code>
     * $query->filterByPeriodo(1234); // WHERE PERIODO = 1234
     * $query->filterByPeriodo(array(12, 34)); // WHERE PERIODO IN (12, 34)
     * $query->filterByPeriodo(array('min' => 12)); // WHERE PERIODO >= 12
     * $query->filterByPeriodo(array('max' => 12)); // WHERE PERIODO <= 12
     * </code>
     *
     * @param     mixed $periodo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByPeriodo($periodo = null, $comparison = null)
    {
        if (is_array($periodo)) {
            $useMinMax = false;
            if (isset($periodo['min'])) {
                $this->addUsingAlias(FactHistPeer::PERIODO, $periodo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($periodo['max'])) {
                $this->addUsingAlias(FactHistPeer::PERIODO, $periodo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactHistPeer::PERIODO, $periodo, $comparison);
    }

    /**
     * Filter the query on the NOTA_CARTERA column
     *
     * Example usage:
     * <code>
     * $query->filterByNotaCartera('fooValue');   // WHERE NOTA_CARTERA = 'fooValue'
     * $query->filterByNotaCartera('%fooValue%'); // WHERE NOTA_CARTERA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $notaCartera The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByNotaCartera($notaCartera = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($notaCartera)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $notaCartera)) {
                $notaCartera = str_replace('*', '%', $notaCartera);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::NOTA_CARTERA, $notaCartera, $comparison);
    }

    /**
     * Filter the query on the BATCH_CARTERA column
     *
     * Example usage:
     * <code>
     * $query->filterByBatchCartera('fooValue');   // WHERE BATCH_CARTERA = 'fooValue'
     * $query->filterByBatchCartera('%fooValue%'); // WHERE BATCH_CARTERA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $batchCartera The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByBatchCartera($batchCartera = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($batchCartera)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $batchCartera)) {
                $batchCartera = str_replace('*', '%', $batchCartera);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::BATCH_CARTERA, $batchCartera, $comparison);
    }

    /**
     * Filter the query on the PAGADOR column
     *
     * Example usage:
     * <code>
     * $query->filterByPagador('fooValue');   // WHERE PAGADOR = 'fooValue'
     * $query->filterByPagador('%fooValue%'); // WHERE PAGADOR LIKE '%fooValue%'
     * </code>
     *
     * @param     string $pagador The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByPagador($pagador = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($pagador)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $pagador)) {
                $pagador = str_replace('*', '%', $pagador);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::PAGADOR, $pagador, $comparison);
    }

    /**
     * Filter the query on the COD_BARRAS_GESTION column
     *
     * Example usage:
     * <code>
     * $query->filterByCodBarrasGestion('fooValue');   // WHERE COD_BARRAS_GESTION = 'fooValue'
     * $query->filterByCodBarrasGestion('%fooValue%'); // WHERE COD_BARRAS_GESTION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codBarrasGestion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByCodBarrasGestion($codBarrasGestion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codBarrasGestion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codBarrasGestion)) {
                $codBarrasGestion = str_replace('*', '%', $codBarrasGestion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::COD_BARRAS_GESTION, $codBarrasGestion, $comparison);
    }

    /**
     * Filter the query on the RUTA column
     *
     * Example usage:
     * <code>
     * $query->filterByRuta('fooValue');   // WHERE RUTA = 'fooValue'
     * $query->filterByRuta('%fooValue%'); // WHERE RUTA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $ruta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByRuta($ruta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($ruta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $ruta)) {
                $ruta = str_replace('*', '%', $ruta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::RUTA, $ruta, $comparison);
    }

    /**
     * Filter the query on the ESTADO column
     *
     * Example usage:
     * <code>
     * $query->filterByEstado('fooValue');   // WHERE ESTADO = 'fooValue'
     * $query->filterByEstado('%fooValue%'); // WHERE ESTADO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByEstado($estado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estado)) {
                $estado = str_replace('*', '%', $estado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::ESTADO, $estado, $comparison);
    }

    /**
     * Filter the query on the ESTADO_DESC column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoDesc('fooValue');   // WHERE ESTADO_DESC = 'fooValue'
     * $query->filterByEstadoDesc('%fooValue%'); // WHERE ESTADO_DESC LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estadoDesc The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByEstadoDesc($estadoDesc = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estadoDesc)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estadoDesc)) {
                $estadoDesc = str_replace('*', '%', $estadoDesc);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::ESTADO_DESC, $estadoDesc, $comparison);
    }

    /**
     * Filter the query on the DESTINO column
     *
     * Example usage:
     * <code>
     * $query->filterByDestino('fooValue');   // WHERE DESTINO = 'fooValue'
     * $query->filterByDestino('%fooValue%'); // WHERE DESTINO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $destino The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByDestino($destino = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($destino)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $destino)) {
                $destino = str_replace('*', '%', $destino);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::DESTINO, $destino, $comparison);
    }

    /**
     * Filter the query on the SOPORTES column
     *
     * Example usage:
     * <code>
     * $query->filterBySoportes('fooValue');   // WHERE SOPORTES = 'fooValue'
     * $query->filterBySoportes('%fooValue%'); // WHERE SOPORTES LIKE '%fooValue%'
     * </code>
     *
     * @param     string $soportes The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterBySoportes($soportes = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($soportes)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $soportes)) {
                $soportes = str_replace('*', '%', $soportes);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::SOPORTES, $soportes, $comparison);
    }

    /**
     * Filter the query on the OBS column
     *
     * Example usage:
     * <code>
     * $query->filterByObs('fooValue');   // WHERE OBS = 'fooValue'
     * $query->filterByObs('%fooValue%'); // WHERE OBS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obs The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByObs($obs = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obs)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obs)) {
                $obs = str_replace('*', '%', $obs);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::OBS, $obs, $comparison);
    }

    /**
     * Filter the query on the ASUNTO column
     *
     * Example usage:
     * <code>
     * $query->filterByAsunto('fooValue');   // WHERE ASUNTO = 'fooValue'
     * $query->filterByAsunto('%fooValue%'); // WHERE ASUNTO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $asunto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByAsunto($asunto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($asunto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $asunto)) {
                $asunto = str_replace('*', '%', $asunto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::ASUNTO, $asunto, $comparison);
    }

    /**
     * Filter the query on the TEMPORAL1 column
     *
     * Example usage:
     * <code>
     * $query->filterByTemporal1('fooValue');   // WHERE TEMPORAL1 = 'fooValue'
     * $query->filterByTemporal1('%fooValue%'); // WHERE TEMPORAL1 LIKE '%fooValue%'
     * </code>
     *
     * @param     string $temporal1 The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByTemporal1($temporal1 = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($temporal1)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $temporal1)) {
                $temporal1 = str_replace('*', '%', $temporal1);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::TEMPORAL1, $temporal1, $comparison);
    }

    /**
     * Filter the query on the EMPORAL2 column
     *
     * Example usage:
     * <code>
     * $query->filterByEmporal2('fooValue');   // WHERE EMPORAL2 = 'fooValue'
     * $query->filterByEmporal2('%fooValue%'); // WHERE EMPORAL2 LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emporal2 The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByEmporal2($emporal2 = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emporal2)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emporal2)) {
                $emporal2 = str_replace('*', '%', $emporal2);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::EMPORAL2, $emporal2, $comparison);
    }

    /**
     * Filter the query on the TEMPORAL3 column
     *
     * Example usage:
     * <code>
     * $query->filterByTemporal3('fooValue');   // WHERE TEMPORAL3 = 'fooValue'
     * $query->filterByTemporal3('%fooValue%'); // WHERE TEMPORAL3 LIKE '%fooValue%'
     * </code>
     *
     * @param     string $temporal3 The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByTemporal3($temporal3 = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($temporal3)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $temporal3)) {
                $temporal3 = str_replace('*', '%', $temporal3);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::TEMPORAL3, $temporal3, $comparison);
    }

    /**
     * Filter the query on the EMPORAL4 column
     *
     * Example usage:
     * <code>
     * $query->filterByEmporal4('fooValue');   // WHERE EMPORAL4 = 'fooValue'
     * $query->filterByEmporal4('%fooValue%'); // WHERE EMPORAL4 LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emporal4 The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByEmporal4($emporal4 = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emporal4)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emporal4)) {
                $emporal4 = str_replace('*', '%', $emporal4);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::EMPORAL4, $emporal4, $comparison);
    }

    /**
     * Filter the query on the TEMPORAL5 column
     *
     * Example usage:
     * <code>
     * $query->filterByTemporal5('fooValue');   // WHERE TEMPORAL5 = 'fooValue'
     * $query->filterByTemporal5('%fooValue%'); // WHERE TEMPORAL5 LIKE '%fooValue%'
     * </code>
     *
     * @param     string $temporal5 The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByTemporal5($temporal5 = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($temporal5)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $temporal5)) {
                $temporal5 = str_replace('*', '%', $temporal5);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::TEMPORAL5, $temporal5, $comparison);
    }

    /**
     * Filter the query on the DEPENDENCIA_NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterByDependenciaNombre('fooValue');   // WHERE DEPENDENCIA_NOMBRE = 'fooValue'
     * $query->filterByDependenciaNombre('%fooValue%'); // WHERE DEPENDENCIA_NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $dependenciaNombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function filterByDependenciaNombre($dependenciaNombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($dependenciaNombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $dependenciaNombre)) {
                $dependenciaNombre = str_replace('*', '%', $dependenciaNombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactHistPeer::DEPENDENCIA_NOMBRE, $dependenciaNombre, $comparison);
    }

    /**
     * Filter the query by a related Regional object
     *
     * @param   Regional|PropelObjectCollection $regional The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 FactHistQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRegional($regional, $comparison = null)
    {
        if ($regional instanceof Regional) {
            return $this
                ->addUsingAlias(FactHistPeer::REGIONAL_ID, $regional->getRegionalId(), $comparison);
        } elseif ($regional instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(FactHistPeer::REGIONAL_ID, $regional->toKeyValue('PrimaryKey', 'RegionalId'), $comparison);
        } else {
            throw new PropelException('filterByRegional() only accepts arguments of type Regional or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Regional relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function joinRegional($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Regional');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Regional');
        }

        return $this;
    }

    /**
     * Use the Regional relation Regional object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RegionalQuery A secondary query class using the current class as primary query
     */
    public function useRegionalQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinRegional($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Regional', 'RegionalQuery');
    }

    /**
     * Filter the query by a related FactHistVitacora object
     *
     * @param   FactHistVitacora|PropelObjectCollection $factHistVitacora  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 FactHistQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByFactHistVitacora($factHistVitacora, $comparison = null)
    {
        if ($factHistVitacora instanceof FactHistVitacora) {
            return $this
                ->addUsingAlias(FactHistPeer::FACTHIST_ID, $factHistVitacora->getFacthistId(), $comparison);
        } elseif ($factHistVitacora instanceof PropelObjectCollection) {
            return $this
                ->useFactHistVitacoraQuery()
                ->filterByPrimaryKeys($factHistVitacora->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByFactHistVitacora() only accepts arguments of type FactHistVitacora or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the FactHistVitacora relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function joinFactHistVitacora($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('FactHistVitacora');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'FactHistVitacora');
        }

        return $this;
    }

    /**
     * Use the FactHistVitacora relation FactHistVitacora object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   FactHistVitacoraQuery A secondary query class using the current class as primary query
     */
    public function useFactHistVitacoraQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinFactHistVitacora($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'FactHistVitacora', 'FactHistVitacoraQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   FactHist $factHist Object to remove from the list of results
     *
     * @return FactHistQuery The current query, for fluid interface
     */
    public function prune($factHist = null)
    {
        if ($factHist) {
            $this->addUsingAlias(FactHistPeer::FACTHIST_ID, $factHist->getFacthistId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a row from the 'ROL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:44
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseRol extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'RolPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        RolPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the rol_id field.
     * @var        int
     */
    protected $rol_id;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * The value for the entidad_id field.
     * @var        int
     */
    protected $entidad_id;

    /**
     * @var        Entidad
     */
    protected $aEntidad;

    /**
     * @var        PropelObjectCollection|RolPorUsuario[] Collection to store aggregation of RolPorUsuario objects.
     */
    protected $collRolPorUsuarios;
    protected $collRolPorUsuariosPartial;

    /**
     * @var        PropelObjectCollection|RolPrivilegio[] Collection to store aggregation of RolPrivilegio objects.
     */
    protected $collRolPrivilegios;
    protected $collRolPrivilegiosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $rolPorUsuariosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $rolPrivilegiosScheduledForDeletion = null;

    /**
     * Get the [rol_id] column value.
     * 
     * @return int
     */
    public function getRolId()
    {

        return $this->rol_id;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Get the [entidad_id] column value.
     * 
     * @return int
     */
    public function getEntidadId()
    {

        return $this->entidad_id;
    }

    /**
     * Set the value of [rol_id] column.
     * 
     * @param  int $v new value
     * @return Rol The current object (for fluent API support)
     */
    public function setRolId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->rol_id !== $v) {
            $this->rol_id = $v;
            $this->modifiedColumns[] = RolPeer::ROL_ID;
        }


        return $this;
    } // setRolId()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return Rol The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = RolPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Set the value of [entidad_id] column.
     * 
     * @param  int $v new value
     * @return Rol The current object (for fluent API support)
     */
    public function setEntidadId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->entidad_id !== $v) {
            $this->entidad_id = $v;
            $this->modifiedColumns[] = RolPeer::ENTIDAD_ID;
        }

        if ($this->aEntidad !== null && $this->aEntidad->getEntidadId() !== $v) {
            $this->aEntidad = null;
        }


        return $this;
    } // setEntidadId()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->rol_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->descripcion = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->entidad_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 3; // 3 = RolPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Rol object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEntidad !== null && $this->entidad_id !== $this->aEntidad->getEntidadId()) {
            $this->aEntidad = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RolPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = RolPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEntidad = null;
            $this->collRolPorUsuarios = null;

            $this->collRolPrivilegios = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RolPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = RolQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseRol:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseRol:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RolPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseRol:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseRol:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                RolPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEntidad !== null) {
                if ($this->aEntidad->isModified() || $this->aEntidad->isNew()) {
                    $affectedRows += $this->aEntidad->save($con);
                }
                $this->setEntidad($this->aEntidad);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->rolPorUsuariosScheduledForDeletion !== null) {
                if (!$this->rolPorUsuariosScheduledForDeletion->isEmpty()) {
                    RolPorUsuarioQuery::create()
                        ->filterByPrimaryKeys($this->rolPorUsuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->rolPorUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collRolPorUsuarios !== null) {
                foreach ($this->collRolPorUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->rolPrivilegiosScheduledForDeletion !== null) {
                if (!$this->rolPrivilegiosScheduledForDeletion->isEmpty()) {
                    RolPrivilegioQuery::create()
                        ->filterByPrimaryKeys($this->rolPrivilegiosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->rolPrivilegiosScheduledForDeletion = null;
                }
            }

            if ($this->collRolPrivilegios !== null) {
                foreach ($this->collRolPrivilegios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = RolPeer::ROL_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(RolPeer::ROL_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . RolPeer::ROL_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(RolPeer::ROL_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setRolId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEntidad !== null) {
                if (!$this->aEntidad->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEntidad->getValidationFailures());
                }
            }


            if (($retval = RolPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collRolPorUsuarios !== null) {
                    foreach ($this->collRolPorUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collRolPrivilegios !== null) {
                    foreach ($this->collRolPrivilegios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = RolPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getRolId();
                break;
            case 1:
                return $this->getDescripcion();
                break;
            case 2:
                return $this->getEntidadId();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Rol'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Rol'][$this->getPrimaryKey()] = true;
        $keys = RolPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getRolId(),
            $keys[1] => $this->getDescripcion(),
            $keys[2] => $this->getEntidadId(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aEntidad) {
                $result['Entidad'] = $this->aEntidad->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collRolPorUsuarios) {
                $result['RolPorUsuarios'] = $this->collRolPorUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collRolPrivilegios) {
                $result['RolPrivilegios'] = $this->collRolPrivilegios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = RolPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setRolId($value);
                break;
            case 1:
                $this->setDescripcion($value);
                break;
            case 2:
                $this->setEntidadId($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = RolPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setRolId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setDescripcion($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setEntidadId($arr[$keys[2]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(RolPeer::DATABASE_NAME);

        if ($this->isColumnModified(RolPeer::ROL_ID)) $criteria->add(RolPeer::ROL_ID, $this->rol_id);
        if ($this->isColumnModified(RolPeer::DESCRIPCION)) $criteria->add(RolPeer::DESCRIPCION, $this->descripcion);
        if ($this->isColumnModified(RolPeer::ENTIDAD_ID)) $criteria->add(RolPeer::ENTIDAD_ID, $this->entidad_id);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(RolPeer::DATABASE_NAME);
        $criteria->add(RolPeer::ROL_ID, $this->rol_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getRolId();
    }

    /**
     * Generic method to set the primary key (rol_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setRolId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getRolId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Rol (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setDescripcion($this->getDescripcion());
        $copyObj->setEntidadId($this->getEntidadId());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getRolPorUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addRolPorUsuario($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getRolPrivilegios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addRolPrivilegio($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setRolId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Rol Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return RolPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new RolPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Entidad object.
     *
     * @param                  Entidad $v
     * @return Rol The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEntidad(Entidad $v = null)
    {
        if ($v === null) {
            $this->setEntidadId(NULL);
        } else {
            $this->setEntidadId($v->getEntidadId());
        }

        $this->aEntidad = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Entidad object, it will not be re-added.
        if ($v !== null) {
            $v->addRol($this);
        }


        return $this;
    }


    /**
     * Get the associated Entidad object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Entidad The associated Entidad object.
     * @throws PropelException
     */
    public function getEntidad(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEntidad === null && ($this->entidad_id !== null) && $doQuery) {
            $this->aEntidad = EntidadQuery::create()->findPk($this->entidad_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEntidad->addRols($this);
             */
        }

        return $this->aEntidad;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('RolPorUsuario' == $relationName) {
            $this->initRolPorUsuarios();
        }
        if ('RolPrivilegio' == $relationName) {
            $this->initRolPrivilegios();
        }
    }

    /**
     * Clears out the collRolPorUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Rol The current object (for fluent API support)
     * @see        addRolPorUsuarios()
     */
    public function clearRolPorUsuarios()
    {
        $this->collRolPorUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collRolPorUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collRolPorUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialRolPorUsuarios($v = true)
    {
        $this->collRolPorUsuariosPartial = $v;
    }

    /**
     * Initializes the collRolPorUsuarios collection.
     *
     * By default this just sets the collRolPorUsuarios collection to an empty array (like clearcollRolPorUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initRolPorUsuarios($overrideExisting = true)
    {
        if (null !== $this->collRolPorUsuarios && !$overrideExisting) {
            return;
        }
        $this->collRolPorUsuarios = new PropelObjectCollection();
        $this->collRolPorUsuarios->setModel('RolPorUsuario');
    }

    /**
     * Gets an array of RolPorUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Rol is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|RolPorUsuario[] List of RolPorUsuario objects
     * @throws PropelException
     */
    public function getRolPorUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collRolPorUsuariosPartial && !$this->isNew();
        if (null === $this->collRolPorUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collRolPorUsuarios) {
                // return empty collection
                $this->initRolPorUsuarios();
            } else {
                $collRolPorUsuarios = RolPorUsuarioQuery::create(null, $criteria)
                    ->filterByRol($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collRolPorUsuariosPartial && count($collRolPorUsuarios)) {
                      $this->initRolPorUsuarios(false);

                      foreach ($collRolPorUsuarios as $obj) {
                        if (false == $this->collRolPorUsuarios->contains($obj)) {
                          $this->collRolPorUsuarios->append($obj);
                        }
                      }

                      $this->collRolPorUsuariosPartial = true;
                    }

                    $collRolPorUsuarios->getInternalIterator()->rewind();

                    return $collRolPorUsuarios;
                }

                if ($partial && $this->collRolPorUsuarios) {
                    foreach ($this->collRolPorUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collRolPorUsuarios[] = $obj;
                        }
                    }
                }

                $this->collRolPorUsuarios = $collRolPorUsuarios;
                $this->collRolPorUsuariosPartial = false;
            }
        }

        return $this->collRolPorUsuarios;
    }

    /**
     * Sets a collection of RolPorUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $rolPorUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Rol The current object (for fluent API support)
     */
    public function setRolPorUsuarios(PropelCollection $rolPorUsuarios, PropelPDO $con = null)
    {
        $rolPorUsuariosToDelete = $this->getRolPorUsuarios(new Criteria(), $con)->diff($rolPorUsuarios);


        $this->rolPorUsuariosScheduledForDeletion = $rolPorUsuariosToDelete;

        foreach ($rolPorUsuariosToDelete as $rolPorUsuarioRemoved) {
            $rolPorUsuarioRemoved->setRol(null);
        }

        $this->collRolPorUsuarios = null;
        foreach ($rolPorUsuarios as $rolPorUsuario) {
            $this->addRolPorUsuario($rolPorUsuario);
        }

        $this->collRolPorUsuarios = $rolPorUsuarios;
        $this->collRolPorUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related RolPorUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related RolPorUsuario objects.
     * @throws PropelException
     */
    public function countRolPorUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collRolPorUsuariosPartial && !$this->isNew();
        if (null === $this->collRolPorUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collRolPorUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getRolPorUsuarios());
            }
            $query = RolPorUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRol($this)
                ->count($con);
        }

        return count($this->collRolPorUsuarios);
    }

    /**
     * Method called to associate a RolPorUsuario object to this object
     * through the RolPorUsuario foreign key attribute.
     *
     * @param    RolPorUsuario $l RolPorUsuario
     * @return Rol The current object (for fluent API support)
     */
    public function addRolPorUsuario(RolPorUsuario $l)
    {
        if ($this->collRolPorUsuarios === null) {
            $this->initRolPorUsuarios();
            $this->collRolPorUsuariosPartial = true;
        }

        if (!in_array($l, $this->collRolPorUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddRolPorUsuario($l);

            if ($this->rolPorUsuariosScheduledForDeletion and $this->rolPorUsuariosScheduledForDeletion->contains($l)) {
                $this->rolPorUsuariosScheduledForDeletion->remove($this->rolPorUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	RolPorUsuario $rolPorUsuario The rolPorUsuario object to add.
     */
    protected function doAddRolPorUsuario($rolPorUsuario)
    {
        $this->collRolPorUsuarios[]= $rolPorUsuario;
        $rolPorUsuario->setRol($this);
    }

    /**
     * @param	RolPorUsuario $rolPorUsuario The rolPorUsuario object to remove.
     * @return Rol The current object (for fluent API support)
     */
    public function removeRolPorUsuario($rolPorUsuario)
    {
        if ($this->getRolPorUsuarios()->contains($rolPorUsuario)) {
            $this->collRolPorUsuarios->remove($this->collRolPorUsuarios->search($rolPorUsuario));
            if (null === $this->rolPorUsuariosScheduledForDeletion) {
                $this->rolPorUsuariosScheduledForDeletion = clone $this->collRolPorUsuarios;
                $this->rolPorUsuariosScheduledForDeletion->clear();
            }
            $this->rolPorUsuariosScheduledForDeletion[]= clone $rolPorUsuario;
            $rolPorUsuario->setRol(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Rol is new, it will return
     * an empty collection; or if this Rol has previously
     * been saved, it will retrieve related RolPorUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Rol.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|RolPorUsuario[] List of RolPorUsuario objects
     */
    public function getRolPorUsuariosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = RolPorUsuarioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getRolPorUsuarios($query, $con);
    }

    /**
     * Clears out the collRolPrivilegios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Rol The current object (for fluent API support)
     * @see        addRolPrivilegios()
     */
    public function clearRolPrivilegios()
    {
        $this->collRolPrivilegios = null; // important to set this to null since that means it is uninitialized
        $this->collRolPrivilegiosPartial = null;

        return $this;
    }

    /**
     * reset is the collRolPrivilegios collection loaded partially
     *
     * @return void
     */
    public function resetPartialRolPrivilegios($v = true)
    {
        $this->collRolPrivilegiosPartial = $v;
    }

    /**
     * Initializes the collRolPrivilegios collection.
     *
     * By default this just sets the collRolPrivilegios collection to an empty array (like clearcollRolPrivilegios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initRolPrivilegios($overrideExisting = true)
    {
        if (null !== $this->collRolPrivilegios && !$overrideExisting) {
            return;
        }
        $this->collRolPrivilegios = new PropelObjectCollection();
        $this->collRolPrivilegios->setModel('RolPrivilegio');
    }

    /**
     * Gets an array of RolPrivilegio objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Rol is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|RolPrivilegio[] List of RolPrivilegio objects
     * @throws PropelException
     */
    public function getRolPrivilegios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collRolPrivilegiosPartial && !$this->isNew();
        if (null === $this->collRolPrivilegios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collRolPrivilegios) {
                // return empty collection
                $this->initRolPrivilegios();
            } else {
                $collRolPrivilegios = RolPrivilegioQuery::create(null, $criteria)
                    ->filterByRol($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collRolPrivilegiosPartial && count($collRolPrivilegios)) {
                      $this->initRolPrivilegios(false);

                      foreach ($collRolPrivilegios as $obj) {
                        if (false == $this->collRolPrivilegios->contains($obj)) {
                          $this->collRolPrivilegios->append($obj);
                        }
                      }

                      $this->collRolPrivilegiosPartial = true;
                    }

                    $collRolPrivilegios->getInternalIterator()->rewind();

                    return $collRolPrivilegios;
                }

                if ($partial && $this->collRolPrivilegios) {
                    foreach ($this->collRolPrivilegios as $obj) {
                        if ($obj->isNew()) {
                            $collRolPrivilegios[] = $obj;
                        }
                    }
                }

                $this->collRolPrivilegios = $collRolPrivilegios;
                $this->collRolPrivilegiosPartial = false;
            }
        }

        return $this->collRolPrivilegios;
    }

    /**
     * Sets a collection of RolPrivilegio objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $rolPrivilegios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Rol The current object (for fluent API support)
     */
    public function setRolPrivilegios(PropelCollection $rolPrivilegios, PropelPDO $con = null)
    {
        $rolPrivilegiosToDelete = $this->getRolPrivilegios(new Criteria(), $con)->diff($rolPrivilegios);


        $this->rolPrivilegiosScheduledForDeletion = $rolPrivilegiosToDelete;

        foreach ($rolPrivilegiosToDelete as $rolPrivilegioRemoved) {
            $rolPrivilegioRemoved->setRol(null);
        }

        $this->collRolPrivilegios = null;
        foreach ($rolPrivilegios as $rolPrivilegio) {
            $this->addRolPrivilegio($rolPrivilegio);
        }

        $this->collRolPrivilegios = $rolPrivilegios;
        $this->collRolPrivilegiosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related RolPrivilegio objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related RolPrivilegio objects.
     * @throws PropelException
     */
    public function countRolPrivilegios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collRolPrivilegiosPartial && !$this->isNew();
        if (null === $this->collRolPrivilegios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collRolPrivilegios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getRolPrivilegios());
            }
            $query = RolPrivilegioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRol($this)
                ->count($con);
        }

        return count($this->collRolPrivilegios);
    }

    /**
     * Method called to associate a RolPrivilegio object to this object
     * through the RolPrivilegio foreign key attribute.
     *
     * @param    RolPrivilegio $l RolPrivilegio
     * @return Rol The current object (for fluent API support)
     */
    public function addRolPrivilegio(RolPrivilegio $l)
    {
        if ($this->collRolPrivilegios === null) {
            $this->initRolPrivilegios();
            $this->collRolPrivilegiosPartial = true;
        }

        if (!in_array($l, $this->collRolPrivilegios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddRolPrivilegio($l);

            if ($this->rolPrivilegiosScheduledForDeletion and $this->rolPrivilegiosScheduledForDeletion->contains($l)) {
                $this->rolPrivilegiosScheduledForDeletion->remove($this->rolPrivilegiosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	RolPrivilegio $rolPrivilegio The rolPrivilegio object to add.
     */
    protected function doAddRolPrivilegio($rolPrivilegio)
    {
        $this->collRolPrivilegios[]= $rolPrivilegio;
        $rolPrivilegio->setRol($this);
    }

    /**
     * @param	RolPrivilegio $rolPrivilegio The rolPrivilegio object to remove.
     * @return Rol The current object (for fluent API support)
     */
    public function removeRolPrivilegio($rolPrivilegio)
    {
        if ($this->getRolPrivilegios()->contains($rolPrivilegio)) {
            $this->collRolPrivilegios->remove($this->collRolPrivilegios->search($rolPrivilegio));
            if (null === $this->rolPrivilegiosScheduledForDeletion) {
                $this->rolPrivilegiosScheduledForDeletion = clone $this->collRolPrivilegios;
                $this->rolPrivilegiosScheduledForDeletion->clear();
            }
            $this->rolPrivilegiosScheduledForDeletion[]= clone $rolPrivilegio;
            $rolPrivilegio->setRol(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Rol is new, it will return
     * an empty collection; or if this Rol has previously
     * been saved, it will retrieve related RolPrivilegios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Rol.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|RolPrivilegio[] List of RolPrivilegio objects
     */
    public function getRolPrivilegiosJoinForma($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = RolPrivilegioQuery::create(null, $criteria);
        $query->joinWith('Forma', $join_behavior);

        return $this->getRolPrivilegios($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->rol_id = null;
        $this->descripcion = null;
        $this->entidad_id = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collRolPorUsuarios) {
                foreach ($this->collRolPorUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collRolPrivilegios) {
                foreach ($this->collRolPrivilegios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aEntidad instanceof Persistent) {
              $this->aEntidad->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collRolPorUsuarios instanceof PropelCollection) {
            $this->collRolPorUsuarios->clearIterator();
        }
        $this->collRolPorUsuarios = null;
        if ($this->collRolPrivilegios instanceof PropelCollection) {
            $this->collRolPrivilegios->clearIterator();
        }
        $this->collRolPrivilegios = null;
        $this->aEntidad = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(RolPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseRol:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

<?php


/**
 * Base class that represents a row from the 'ESPECIALIDAD' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:44
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEspecialidad extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'EspecialidadPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        EspecialidadPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the especialidad_id field.
     * @var        int
     */
    protected $especialidad_id;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * @var        PropelObjectCollection|ManualTecnico[] Collection to store aggregation of ManualTecnico objects.
     */
    protected $collManualTecnicos;
    protected $collManualTecnicosPartial;

    /**
     * @var        PropelObjectCollection|Catalogo[] Collection to store aggregation of Catalogo objects.
     */
    protected $collCatalogos;
    protected $collCatalogosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $manualTecnicosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $catalogosScheduledForDeletion = null;

    /**
     * Get the [especialidad_id] column value.
     * 
     * @return int
     */
    public function getEspecialidadId()
    {

        return $this->especialidad_id;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Set the value of [especialidad_id] column.
     * 
     * @param  int $v new value
     * @return Especialidad The current object (for fluent API support)
     */
    public function setEspecialidadId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->especialidad_id !== $v) {
            $this->especialidad_id = $v;
            $this->modifiedColumns[] = EspecialidadPeer::ESPECIALIDAD_ID;
        }


        return $this;
    } // setEspecialidadId()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return Especialidad The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = EspecialidadPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->especialidad_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->descripcion = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 2; // 2 = EspecialidadPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Especialidad object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EspecialidadPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = EspecialidadPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->collManualTecnicos = null;

            $this->collCatalogos = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EspecialidadPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = EspecialidadQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseEspecialidad:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseEspecialidad:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EspecialidadPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseEspecialidad:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseEspecialidad:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                EspecialidadPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->manualTecnicosScheduledForDeletion !== null) {
                if (!$this->manualTecnicosScheduledForDeletion->isEmpty()) {
                    ManualTecnicoQuery::create()
                        ->filterByPrimaryKeys($this->manualTecnicosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->manualTecnicosScheduledForDeletion = null;
                }
            }

            if ($this->collManualTecnicos !== null) {
                foreach ($this->collManualTecnicos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->catalogosScheduledForDeletion !== null) {
                if (!$this->catalogosScheduledForDeletion->isEmpty()) {
                    CatalogoQuery::create()
                        ->filterByPrimaryKeys($this->catalogosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->catalogosScheduledForDeletion = null;
                }
            }

            if ($this->collCatalogos !== null) {
                foreach ($this->collCatalogos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = EspecialidadPeer::ESPECIALIDAD_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(EspecialidadPeer::ESPECIALIDAD_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . EspecialidadPeer::ESPECIALIDAD_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(EspecialidadPeer::ESPECIALIDAD_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setEspecialidadId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            if (($retval = EspecialidadPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collManualTecnicos !== null) {
                    foreach ($this->collManualTecnicos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collCatalogos !== null) {
                    foreach ($this->collCatalogos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = EspecialidadPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getEspecialidadId();
                break;
            case 1:
                return $this->getDescripcion();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Especialidad'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Especialidad'][$this->getPrimaryKey()] = true;
        $keys = EspecialidadPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getEspecialidadId(),
            $keys[1] => $this->getDescripcion(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->collManualTecnicos) {
                $result['ManualTecnicos'] = $this->collManualTecnicos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collCatalogos) {
                $result['Catalogos'] = $this->collCatalogos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = EspecialidadPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setEspecialidadId($value);
                break;
            case 1:
                $this->setDescripcion($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = EspecialidadPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setEspecialidadId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setDescripcion($arr[$keys[1]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(EspecialidadPeer::DATABASE_NAME);

        if ($this->isColumnModified(EspecialidadPeer::ESPECIALIDAD_ID)) $criteria->add(EspecialidadPeer::ESPECIALIDAD_ID, $this->especialidad_id);
        if ($this->isColumnModified(EspecialidadPeer::DESCRIPCION)) $criteria->add(EspecialidadPeer::DESCRIPCION, $this->descripcion);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(EspecialidadPeer::DATABASE_NAME);
        $criteria->add(EspecialidadPeer::ESPECIALIDAD_ID, $this->especialidad_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getEspecialidadId();
    }

    /**
     * Generic method to set the primary key (especialidad_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setEspecialidadId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getEspecialidadId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Especialidad (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setDescripcion($this->getDescripcion());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getManualTecnicos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addManualTecnico($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getCatalogos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addCatalogo($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setEspecialidadId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Especialidad Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return EspecialidadPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new EspecialidadPeer();
        }

        return self::$peer;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('ManualTecnico' == $relationName) {
            $this->initManualTecnicos();
        }
        if ('Catalogo' == $relationName) {
            $this->initCatalogos();
        }
    }

    /**
     * Clears out the collManualTecnicos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Especialidad The current object (for fluent API support)
     * @see        addManualTecnicos()
     */
    public function clearManualTecnicos()
    {
        $this->collManualTecnicos = null; // important to set this to null since that means it is uninitialized
        $this->collManualTecnicosPartial = null;

        return $this;
    }

    /**
     * reset is the collManualTecnicos collection loaded partially
     *
     * @return void
     */
    public function resetPartialManualTecnicos($v = true)
    {
        $this->collManualTecnicosPartial = $v;
    }

    /**
     * Initializes the collManualTecnicos collection.
     *
     * By default this just sets the collManualTecnicos collection to an empty array (like clearcollManualTecnicos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initManualTecnicos($overrideExisting = true)
    {
        if (null !== $this->collManualTecnicos && !$overrideExisting) {
            return;
        }
        $this->collManualTecnicos = new PropelObjectCollection();
        $this->collManualTecnicos->setModel('ManualTecnico');
    }

    /**
     * Gets an array of ManualTecnico objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Especialidad is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ManualTecnico[] List of ManualTecnico objects
     * @throws PropelException
     */
    public function getManualTecnicos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collManualTecnicosPartial && !$this->isNew();
        if (null === $this->collManualTecnicos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collManualTecnicos) {
                // return empty collection
                $this->initManualTecnicos();
            } else {
                $collManualTecnicos = ManualTecnicoQuery::create(null, $criteria)
                    ->filterByEspecialidad($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collManualTecnicosPartial && count($collManualTecnicos)) {
                      $this->initManualTecnicos(false);

                      foreach ($collManualTecnicos as $obj) {
                        if (false == $this->collManualTecnicos->contains($obj)) {
                          $this->collManualTecnicos->append($obj);
                        }
                      }

                      $this->collManualTecnicosPartial = true;
                    }

                    $collManualTecnicos->getInternalIterator()->rewind();

                    return $collManualTecnicos;
                }

                if ($partial && $this->collManualTecnicos) {
                    foreach ($this->collManualTecnicos as $obj) {
                        if ($obj->isNew()) {
                            $collManualTecnicos[] = $obj;
                        }
                    }
                }

                $this->collManualTecnicos = $collManualTecnicos;
                $this->collManualTecnicosPartial = false;
            }
        }

        return $this->collManualTecnicos;
    }

    /**
     * Sets a collection of ManualTecnico objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $manualTecnicos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Especialidad The current object (for fluent API support)
     */
    public function setManualTecnicos(PropelCollection $manualTecnicos, PropelPDO $con = null)
    {
        $manualTecnicosToDelete = $this->getManualTecnicos(new Criteria(), $con)->diff($manualTecnicos);


        $this->manualTecnicosScheduledForDeletion = $manualTecnicosToDelete;

        foreach ($manualTecnicosToDelete as $manualTecnicoRemoved) {
            $manualTecnicoRemoved->setEspecialidad(null);
        }

        $this->collManualTecnicos = null;
        foreach ($manualTecnicos as $manualTecnico) {
            $this->addManualTecnico($manualTecnico);
        }

        $this->collManualTecnicos = $manualTecnicos;
        $this->collManualTecnicosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ManualTecnico objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ManualTecnico objects.
     * @throws PropelException
     */
    public function countManualTecnicos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collManualTecnicosPartial && !$this->isNew();
        if (null === $this->collManualTecnicos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collManualTecnicos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getManualTecnicos());
            }
            $query = ManualTecnicoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByEspecialidad($this)
                ->count($con);
        }

        return count($this->collManualTecnicos);
    }

    /**
     * Method called to associate a ManualTecnico object to this object
     * through the ManualTecnico foreign key attribute.
     *
     * @param    ManualTecnico $l ManualTecnico
     * @return Especialidad The current object (for fluent API support)
     */
    public function addManualTecnico(ManualTecnico $l)
    {
        if ($this->collManualTecnicos === null) {
            $this->initManualTecnicos();
            $this->collManualTecnicosPartial = true;
        }

        if (!in_array($l, $this->collManualTecnicos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddManualTecnico($l);

            if ($this->manualTecnicosScheduledForDeletion and $this->manualTecnicosScheduledForDeletion->contains($l)) {
                $this->manualTecnicosScheduledForDeletion->remove($this->manualTecnicosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ManualTecnico $manualTecnico The manualTecnico object to add.
     */
    protected function doAddManualTecnico($manualTecnico)
    {
        $this->collManualTecnicos[]= $manualTecnico;
        $manualTecnico->setEspecialidad($this);
    }

    /**
     * @param	ManualTecnico $manualTecnico The manualTecnico object to remove.
     * @return Especialidad The current object (for fluent API support)
     */
    public function removeManualTecnico($manualTecnico)
    {
        if ($this->getManualTecnicos()->contains($manualTecnico)) {
            $this->collManualTecnicos->remove($this->collManualTecnicos->search($manualTecnico));
            if (null === $this->manualTecnicosScheduledForDeletion) {
                $this->manualTecnicosScheduledForDeletion = clone $this->collManualTecnicos;
                $this->manualTecnicosScheduledForDeletion->clear();
            }
            $this->manualTecnicosScheduledForDeletion[]= clone $manualTecnico;
            $manualTecnico->setEspecialidad(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Especialidad is new, it will return
     * an empty collection; or if this Especialidad has previously
     * been saved, it will retrieve related ManualTecnicos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Especialidad.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ManualTecnico[] List of ManualTecnico objects
     */
    public function getManualTecnicosJoinDocumentacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ManualTecnicoQuery::create(null, $criteria);
        $query->joinWith('Documentacion', $join_behavior);

        return $this->getManualTecnicos($query, $con);
    }

    /**
     * Clears out the collCatalogos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Especialidad The current object (for fluent API support)
     * @see        addCatalogos()
     */
    public function clearCatalogos()
    {
        $this->collCatalogos = null; // important to set this to null since that means it is uninitialized
        $this->collCatalogosPartial = null;

        return $this;
    }

    /**
     * reset is the collCatalogos collection loaded partially
     *
     * @return void
     */
    public function resetPartialCatalogos($v = true)
    {
        $this->collCatalogosPartial = $v;
    }

    /**
     * Initializes the collCatalogos collection.
     *
     * By default this just sets the collCatalogos collection to an empty array (like clearcollCatalogos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initCatalogos($overrideExisting = true)
    {
        if (null !== $this->collCatalogos && !$overrideExisting) {
            return;
        }
        $this->collCatalogos = new PropelObjectCollection();
        $this->collCatalogos->setModel('Catalogo');
    }

    /**
     * Gets an array of Catalogo objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Especialidad is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Catalogo[] List of Catalogo objects
     * @throws PropelException
     */
    public function getCatalogos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collCatalogosPartial && !$this->isNew();
        if (null === $this->collCatalogos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collCatalogos) {
                // return empty collection
                $this->initCatalogos();
            } else {
                $collCatalogos = CatalogoQuery::create(null, $criteria)
                    ->filterByEspecialidad($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collCatalogosPartial && count($collCatalogos)) {
                      $this->initCatalogos(false);

                      foreach ($collCatalogos as $obj) {
                        if (false == $this->collCatalogos->contains($obj)) {
                          $this->collCatalogos->append($obj);
                        }
                      }

                      $this->collCatalogosPartial = true;
                    }

                    $collCatalogos->getInternalIterator()->rewind();

                    return $collCatalogos;
                }

                if ($partial && $this->collCatalogos) {
                    foreach ($this->collCatalogos as $obj) {
                        if ($obj->isNew()) {
                            $collCatalogos[] = $obj;
                        }
                    }
                }

                $this->collCatalogos = $collCatalogos;
                $this->collCatalogosPartial = false;
            }
        }

        return $this->collCatalogos;
    }

    /**
     * Sets a collection of Catalogo objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $catalogos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Especialidad The current object (for fluent API support)
     */
    public function setCatalogos(PropelCollection $catalogos, PropelPDO $con = null)
    {
        $catalogosToDelete = $this->getCatalogos(new Criteria(), $con)->diff($catalogos);


        $this->catalogosScheduledForDeletion = $catalogosToDelete;

        foreach ($catalogosToDelete as $catalogoRemoved) {
            $catalogoRemoved->setEspecialidad(null);
        }

        $this->collCatalogos = null;
        foreach ($catalogos as $catalogo) {
            $this->addCatalogo($catalogo);
        }

        $this->collCatalogos = $catalogos;
        $this->collCatalogosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Catalogo objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Catalogo objects.
     * @throws PropelException
     */
    public function countCatalogos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collCatalogosPartial && !$this->isNew();
        if (null === $this->collCatalogos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collCatalogos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getCatalogos());
            }
            $query = CatalogoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByEspecialidad($this)
                ->count($con);
        }

        return count($this->collCatalogos);
    }

    /**
     * Method called to associate a Catalogo object to this object
     * through the Catalogo foreign key attribute.
     *
     * @param    Catalogo $l Catalogo
     * @return Especialidad The current object (for fluent API support)
     */
    public function addCatalogo(Catalogo $l)
    {
        if ($this->collCatalogos === null) {
            $this->initCatalogos();
            $this->collCatalogosPartial = true;
        }

        if (!in_array($l, $this->collCatalogos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddCatalogo($l);

            if ($this->catalogosScheduledForDeletion and $this->catalogosScheduledForDeletion->contains($l)) {
                $this->catalogosScheduledForDeletion->remove($this->catalogosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Catalogo $catalogo The catalogo object to add.
     */
    protected function doAddCatalogo($catalogo)
    {
        $this->collCatalogos[]= $catalogo;
        $catalogo->setEspecialidad($this);
    }

    /**
     * @param	Catalogo $catalogo The catalogo object to remove.
     * @return Especialidad The current object (for fluent API support)
     */
    public function removeCatalogo($catalogo)
    {
        if ($this->getCatalogos()->contains($catalogo)) {
            $this->collCatalogos->remove($this->collCatalogos->search($catalogo));
            if (null === $this->catalogosScheduledForDeletion) {
                $this->catalogosScheduledForDeletion = clone $this->collCatalogos;
                $this->catalogosScheduledForDeletion->clear();
            }
            $this->catalogosScheduledForDeletion[]= clone $catalogo;
            $catalogo->setEspecialidad(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Especialidad is new, it will return
     * an empty collection; or if this Especialidad has previously
     * been saved, it will retrieve related Catalogos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Especialidad.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Catalogo[] List of Catalogo objects
     */
    public function getCatalogosJoinDocumentacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = CatalogoQuery::create(null, $criteria);
        $query->joinWith('Documentacion', $join_behavior);

        return $this->getCatalogos($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->especialidad_id = null;
        $this->descripcion = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collManualTecnicos) {
                foreach ($this->collManualTecnicos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collCatalogos) {
                foreach ($this->collCatalogos as $o) {
                    $o->clearAllReferences($deep);
                }
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collManualTecnicos instanceof PropelCollection) {
            $this->collManualTecnicos->clearIterator();
        }
        $this->collManualTecnicos = null;
        if ($this->collCatalogos instanceof PropelCollection) {
            $this->collCatalogos->clearIterator();
        }
        $this->collCatalogos = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(EspecialidadPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseEspecialidad:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

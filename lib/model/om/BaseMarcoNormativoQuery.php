<?php


/**
 * Base class that represents a query for the 'MARCO_NORMATIVO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:55
 *
 * @method MarcoNormativoQuery orderByMarconormativoId($order = Criteria::ASC) Order by the MARCONORMATIVO_ID column
 * @method MarcoNormativoQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method MarcoNormativoQuery orderByEstaActivo($order = Criteria::ASC) Order by the ESTA_ACTIVO column
 *
 * @method MarcoNormativoQuery groupByMarconormativoId() Group by the MARCONORMATIVO_ID column
 * @method MarcoNormativoQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method MarcoNormativoQuery groupByEstaActivo() Group by the ESTA_ACTIVO column
 *
 * @method MarcoNormativoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method MarcoNormativoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method MarcoNormativoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method MarcoNormativoQuery leftJoinServicio($relationAlias = null) Adds a LEFT JOIN clause to the query using the Servicio relation
 * @method MarcoNormativoQuery rightJoinServicio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Servicio relation
 * @method MarcoNormativoQuery innerJoinServicio($relationAlias = null) Adds a INNER JOIN clause to the query using the Servicio relation
 *
 * @method MarcoNormativo findOne(PropelPDO $con = null) Return the first MarcoNormativo matching the query
 * @method MarcoNormativo findOneOrCreate(PropelPDO $con = null) Return the first MarcoNormativo matching the query, or a new MarcoNormativo object populated from the query conditions when no match is found
 *
 * @method MarcoNormativo findOneByDescripcion(string $DESCRIPCION) Return the first MarcoNormativo filtered by the DESCRIPCION column
 * @method MarcoNormativo findOneByEstaActivo(int $ESTA_ACTIVO) Return the first MarcoNormativo filtered by the ESTA_ACTIVO column
 *
 * @method array findByMarconormativoId(int $MARCONORMATIVO_ID) Return MarcoNormativo objects filtered by the MARCONORMATIVO_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return MarcoNormativo objects filtered by the DESCRIPCION column
 * @method array findByEstaActivo(int $ESTA_ACTIVO) Return MarcoNormativo objects filtered by the ESTA_ACTIVO column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseMarcoNormativoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseMarcoNormativoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'MarcoNormativo';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new MarcoNormativoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   MarcoNormativoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return MarcoNormativoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof MarcoNormativoQuery) {
            return $criteria;
        }
        $query = new MarcoNormativoQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   MarcoNormativo|MarcoNormativo[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = MarcoNormativoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(MarcoNormativoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 MarcoNormativo A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByMarconormativoId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 MarcoNormativo A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [MARCONORMATIVO_ID], [DESCRIPCION], [ESTA_ACTIVO] FROM [MARCO_NORMATIVO] WHERE [MARCONORMATIVO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new MarcoNormativo();
            $obj->hydrate($row);
            MarcoNormativoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return MarcoNormativo|MarcoNormativo[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|MarcoNormativo[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the MARCONORMATIVO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByMarconormativoId(1234); // WHERE MARCONORMATIVO_ID = 1234
     * $query->filterByMarconormativoId(array(12, 34)); // WHERE MARCONORMATIVO_ID IN (12, 34)
     * $query->filterByMarconormativoId(array('min' => 12)); // WHERE MARCONORMATIVO_ID >= 12
     * $query->filterByMarconormativoId(array('max' => 12)); // WHERE MARCONORMATIVO_ID <= 12
     * </code>
     *
     * @param     mixed $marconormativoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function filterByMarconormativoId($marconormativoId = null, $comparison = null)
    {
        if (is_array($marconormativoId)) {
            $useMinMax = false;
            if (isset($marconormativoId['min'])) {
                $this->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $marconormativoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($marconormativoId['max'])) {
                $this->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $marconormativoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $marconormativoId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MarcoNormativoPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ESTA_ACTIVO column
     *
     * Example usage:
     * <code>
     * $query->filterByEstaActivo(1234); // WHERE ESTA_ACTIVO = 1234
     * $query->filterByEstaActivo(array(12, 34)); // WHERE ESTA_ACTIVO IN (12, 34)
     * $query->filterByEstaActivo(array('min' => 12)); // WHERE ESTA_ACTIVO >= 12
     * $query->filterByEstaActivo(array('max' => 12)); // WHERE ESTA_ACTIVO <= 12
     * </code>
     *
     * @param     mixed $estaActivo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function filterByEstaActivo($estaActivo = null, $comparison = null)
    {
        if (is_array($estaActivo)) {
            $useMinMax = false;
            if (isset($estaActivo['min'])) {
                $this->addUsingAlias(MarcoNormativoPeer::ESTA_ACTIVO, $estaActivo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estaActivo['max'])) {
                $this->addUsingAlias(MarcoNormativoPeer::ESTA_ACTIVO, $estaActivo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MarcoNormativoPeer::ESTA_ACTIVO, $estaActivo, $comparison);
    }

    /**
     * Filter the query by a related Servicio object
     *
     * @param   Servicio|PropelObjectCollection $servicio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 MarcoNormativoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByServicio($servicio, $comparison = null)
    {
        if ($servicio instanceof Servicio) {
            return $this
                ->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $servicio->getMarconormativoId(), $comparison);
        } elseif ($servicio instanceof PropelObjectCollection) {
            return $this
                ->useServicioQuery()
                ->filterByPrimaryKeys($servicio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByServicio() only accepts arguments of type Servicio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Servicio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function joinServicio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Servicio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Servicio');
        }

        return $this;
    }

    /**
     * Use the Servicio relation Servicio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ServicioQuery A secondary query class using the current class as primary query
     */
    public function useServicioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinServicio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Servicio', 'ServicioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   MarcoNormativo $marcoNormativo Object to remove from the list of results
     *
     * @return MarcoNormativoQuery The current query, for fluid interface
     */
    public function prune($marcoNormativo = null)
    {
        if ($marcoNormativo) {
            $this->addUsingAlias(MarcoNormativoPeer::MARCONORMATIVO_ID, $marcoNormativo->getMarconormativoId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

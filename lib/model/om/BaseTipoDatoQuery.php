<?php


/**
 * Base class that represents a query for the 'TIPO_DATO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:56
 *
 * @method TipoDatoQuery orderByTipodatoId($order = Criteria::ASC) Order by the TIPODATO_ID column
 * @method TipoDatoQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method TipoDatoQuery orderByRegex($order = Criteria::ASC) Order by the REGEX column
 *
 * @method TipoDatoQuery groupByTipodatoId() Group by the TIPODATO_ID column
 * @method TipoDatoQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method TipoDatoQuery groupByRegex() Group by the REGEX column
 *
 * @method TipoDatoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TipoDatoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TipoDatoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TipoDatoQuery leftJoinMetaDatos($relationAlias = null) Adds a LEFT JOIN clause to the query using the MetaDatos relation
 * @method TipoDatoQuery rightJoinMetaDatos($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MetaDatos relation
 * @method TipoDatoQuery innerJoinMetaDatos($relationAlias = null) Adds a INNER JOIN clause to the query using the MetaDatos relation
 *
 * @method TipoDato findOne(PropelPDO $con = null) Return the first TipoDato matching the query
 * @method TipoDato findOneOrCreate(PropelPDO $con = null) Return the first TipoDato matching the query, or a new TipoDato object populated from the query conditions when no match is found
 *
 * @method TipoDato findOneByDescripcion(string $DESCRIPCION) Return the first TipoDato filtered by the DESCRIPCION column
 * @method TipoDato findOneByRegex(string $REGEX) Return the first TipoDato filtered by the REGEX column
 *
 * @method array findByTipodatoId(int $TIPODATO_ID) Return TipoDato objects filtered by the TIPODATO_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return TipoDato objects filtered by the DESCRIPCION column
 * @method array findByRegex(string $REGEX) Return TipoDato objects filtered by the REGEX column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTipoDatoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTipoDatoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'TipoDato';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TipoDatoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   TipoDatoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TipoDatoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TipoDatoQuery) {
            return $criteria;
        }
        $query = new TipoDatoQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   TipoDato|TipoDato[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TipoDatoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TipoDatoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoDato A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByTipodatoId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoDato A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [TIPODATO_ID], [DESCRIPCION], [REGEX] FROM [TIPO_DATO] WHERE [TIPODATO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new TipoDato();
            $obj->hydrate($row);
            TipoDatoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return TipoDato|TipoDato[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|TipoDato[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the TIPODATO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipodatoId(1234); // WHERE TIPODATO_ID = 1234
     * $query->filterByTipodatoId(array(12, 34)); // WHERE TIPODATO_ID IN (12, 34)
     * $query->filterByTipodatoId(array('min' => 12)); // WHERE TIPODATO_ID >= 12
     * $query->filterByTipodatoId(array('max' => 12)); // WHERE TIPODATO_ID <= 12
     * </code>
     *
     * @param     mixed $tipodatoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function filterByTipodatoId($tipodatoId = null, $comparison = null)
    {
        if (is_array($tipodatoId)) {
            $useMinMax = false;
            if (isset($tipodatoId['min'])) {
                $this->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $tipodatoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipodatoId['max'])) {
                $this->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $tipodatoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $tipodatoId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoDatoPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the REGEX column
     *
     * Example usage:
     * <code>
     * $query->filterByRegex('fooValue');   // WHERE REGEX = 'fooValue'
     * $query->filterByRegex('%fooValue%'); // WHERE REGEX LIKE '%fooValue%'
     * </code>
     *
     * @param     string $regex The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function filterByRegex($regex = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($regex)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $regex)) {
                $regex = str_replace('*', '%', $regex);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoDatoPeer::REGEX, $regex, $comparison);
    }

    /**
     * Filter the query by a related MetaDatos object
     *
     * @param   MetaDatos|PropelObjectCollection $metaDatos  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoDatoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByMetaDatos($metaDatos, $comparison = null)
    {
        if ($metaDatos instanceof MetaDatos) {
            return $this
                ->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $metaDatos->getTipodatoId(), $comparison);
        } elseif ($metaDatos instanceof PropelObjectCollection) {
            return $this
                ->useMetaDatosQuery()
                ->filterByPrimaryKeys($metaDatos->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMetaDatos() only accepts arguments of type MetaDatos or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MetaDatos relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function joinMetaDatos($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MetaDatos');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MetaDatos');
        }

        return $this;
    }

    /**
     * Use the MetaDatos relation MetaDatos object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MetaDatosQuery A secondary query class using the current class as primary query
     */
    public function useMetaDatosQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinMetaDatos($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MetaDatos', 'MetaDatosQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   TipoDato $tipoDato Object to remove from the list of results
     *
     * @return TipoDatoQuery The current query, for fluid interface
     */
    public function prune($tipoDato = null)
    {
        if ($tipoDato) {
            $this->addUsingAlias(TipoDatoPeer::TIPODATO_ID, $tipoDato->getTipodatoId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a row from the 'PROV_LISTA_DOCS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:06
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseProvListaDocs extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'ProvListaDocsPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        ProvListaDocsPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the prov_lista_docs_id field.
     * @var        int
     */
    protected $prov_lista_docs_id;

    /**
     * The value for the nombre field.
     * @var        string
     */
    protected $nombre;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * @var        PropelObjectCollection|ProvCheckListPregunta[] Collection to store aggregation of ProvCheckListPregunta objects.
     */
    protected $collProvCheckListPreguntas;
    protected $collProvCheckListPreguntasPartial;

    /**
     * @var        PropelObjectCollection|ProvDocumento[] Collection to store aggregation of ProvDocumento objects.
     */
    protected $collProvDocumentos;
    protected $collProvDocumentosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $provCheckListPreguntasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $provDocumentosScheduledForDeletion = null;

    /**
     * Get the [prov_lista_docs_id] column value.
     * 
     * @return int
     */
    public function getProvListaDocsId()
    {

        return $this->prov_lista_docs_id;
    }

    /**
     * Get the [nombre] column value.
     * 
     * @return string
     */
    public function getNombre()
    {

        return $this->nombre;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Set the value of [prov_lista_docs_id] column.
     * 
     * @param  int $v new value
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function setProvListaDocsId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->prov_lista_docs_id !== $v) {
            $this->prov_lista_docs_id = $v;
            $this->modifiedColumns[] = ProvListaDocsPeer::PROV_LISTA_DOCS_ID;
        }


        return $this;
    } // setProvListaDocsId()

    /**
     * Set the value of [nombre] column.
     * 
     * @param  string $v new value
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function setNombre($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nombre !== $v) {
            $this->nombre = $v;
            $this->modifiedColumns[] = ProvListaDocsPeer::NOMBRE;
        }


        return $this;
    } // setNombre()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = ProvListaDocsPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->prov_lista_docs_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->nombre = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->descripcion = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 3; // 3 = ProvListaDocsPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating ProvListaDocs object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(ProvListaDocsPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = ProvListaDocsPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->collProvCheckListPreguntas = null;

            $this->collProvDocumentos = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(ProvListaDocsPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = ProvListaDocsQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseProvListaDocs:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseProvListaDocs:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(ProvListaDocsPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseProvListaDocs:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseProvListaDocs:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                ProvListaDocsPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->provCheckListPreguntasScheduledForDeletion !== null) {
                if (!$this->provCheckListPreguntasScheduledForDeletion->isEmpty()) {
                    ProvCheckListPreguntaQuery::create()
                        ->filterByPrimaryKeys($this->provCheckListPreguntasScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->provCheckListPreguntasScheduledForDeletion = null;
                }
            }

            if ($this->collProvCheckListPreguntas !== null) {
                foreach ($this->collProvCheckListPreguntas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->provDocumentosScheduledForDeletion !== null) {
                if (!$this->provDocumentosScheduledForDeletion->isEmpty()) {
                    ProvDocumentoQuery::create()
                        ->filterByPrimaryKeys($this->provDocumentosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->provDocumentosScheduledForDeletion = null;
                }
            }

            if ($this->collProvDocumentos !== null) {
                foreach ($this->collProvDocumentos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = ProvListaDocsPeer::PROV_LISTA_DOCS_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(ProvListaDocsPeer::PROV_LISTA_DOCS_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . ProvListaDocsPeer::PROV_LISTA_DOCS_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(ProvListaDocsPeer::PROV_LISTA_DOCS_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setProvListaDocsId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            if (($retval = ProvListaDocsPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collProvCheckListPreguntas !== null) {
                    foreach ($this->collProvCheckListPreguntas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collProvDocumentos !== null) {
                    foreach ($this->collProvDocumentos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = ProvListaDocsPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getProvListaDocsId();
                break;
            case 1:
                return $this->getNombre();
                break;
            case 2:
                return $this->getDescripcion();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['ProvListaDocs'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['ProvListaDocs'][$this->getPrimaryKey()] = true;
        $keys = ProvListaDocsPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getProvListaDocsId(),
            $keys[1] => $this->getNombre(),
            $keys[2] => $this->getDescripcion(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->collProvCheckListPreguntas) {
                $result['ProvCheckListPreguntas'] = $this->collProvCheckListPreguntas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collProvDocumentos) {
                $result['ProvDocumentos'] = $this->collProvDocumentos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = ProvListaDocsPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setProvListaDocsId($value);
                break;
            case 1:
                $this->setNombre($value);
                break;
            case 2:
                $this->setDescripcion($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = ProvListaDocsPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setProvListaDocsId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setNombre($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setDescripcion($arr[$keys[2]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(ProvListaDocsPeer::DATABASE_NAME);

        if ($this->isColumnModified(ProvListaDocsPeer::PROV_LISTA_DOCS_ID)) $criteria->add(ProvListaDocsPeer::PROV_LISTA_DOCS_ID, $this->prov_lista_docs_id);
        if ($this->isColumnModified(ProvListaDocsPeer::NOMBRE)) $criteria->add(ProvListaDocsPeer::NOMBRE, $this->nombre);
        if ($this->isColumnModified(ProvListaDocsPeer::DESCRIPCION)) $criteria->add(ProvListaDocsPeer::DESCRIPCION, $this->descripcion);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(ProvListaDocsPeer::DATABASE_NAME);
        $criteria->add(ProvListaDocsPeer::PROV_LISTA_DOCS_ID, $this->prov_lista_docs_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getProvListaDocsId();
    }

    /**
     * Generic method to set the primary key (prov_lista_docs_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setProvListaDocsId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getProvListaDocsId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of ProvListaDocs (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setNombre($this->getNombre());
        $copyObj->setDescripcion($this->getDescripcion());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getProvCheckListPreguntas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addProvCheckListPregunta($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getProvDocumentos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addProvDocumento($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setProvListaDocsId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return ProvListaDocs Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return ProvListaDocsPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new ProvListaDocsPeer();
        }

        return self::$peer;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('ProvCheckListPregunta' == $relationName) {
            $this->initProvCheckListPreguntas();
        }
        if ('ProvDocumento' == $relationName) {
            $this->initProvDocumentos();
        }
    }

    /**
     * Clears out the collProvCheckListPreguntas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return ProvListaDocs The current object (for fluent API support)
     * @see        addProvCheckListPreguntas()
     */
    public function clearProvCheckListPreguntas()
    {
        $this->collProvCheckListPreguntas = null; // important to set this to null since that means it is uninitialized
        $this->collProvCheckListPreguntasPartial = null;

        return $this;
    }

    /**
     * reset is the collProvCheckListPreguntas collection loaded partially
     *
     * @return void
     */
    public function resetPartialProvCheckListPreguntas($v = true)
    {
        $this->collProvCheckListPreguntasPartial = $v;
    }

    /**
     * Initializes the collProvCheckListPreguntas collection.
     *
     * By default this just sets the collProvCheckListPreguntas collection to an empty array (like clearcollProvCheckListPreguntas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initProvCheckListPreguntas($overrideExisting = true)
    {
        if (null !== $this->collProvCheckListPreguntas && !$overrideExisting) {
            return;
        }
        $this->collProvCheckListPreguntas = new PropelObjectCollection();
        $this->collProvCheckListPreguntas->setModel('ProvCheckListPregunta');
    }

    /**
     * Gets an array of ProvCheckListPregunta objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this ProvListaDocs is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ProvCheckListPregunta[] List of ProvCheckListPregunta objects
     * @throws PropelException
     */
    public function getProvCheckListPreguntas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collProvCheckListPreguntasPartial && !$this->isNew();
        if (null === $this->collProvCheckListPreguntas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collProvCheckListPreguntas) {
                // return empty collection
                $this->initProvCheckListPreguntas();
            } else {
                $collProvCheckListPreguntas = ProvCheckListPreguntaQuery::create(null, $criteria)
                    ->filterByProvListaDocs($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collProvCheckListPreguntasPartial && count($collProvCheckListPreguntas)) {
                      $this->initProvCheckListPreguntas(false);

                      foreach ($collProvCheckListPreguntas as $obj) {
                        if (false == $this->collProvCheckListPreguntas->contains($obj)) {
                          $this->collProvCheckListPreguntas->append($obj);
                        }
                      }

                      $this->collProvCheckListPreguntasPartial = true;
                    }

                    $collProvCheckListPreguntas->getInternalIterator()->rewind();

                    return $collProvCheckListPreguntas;
                }

                if ($partial && $this->collProvCheckListPreguntas) {
                    foreach ($this->collProvCheckListPreguntas as $obj) {
                        if ($obj->isNew()) {
                            $collProvCheckListPreguntas[] = $obj;
                        }
                    }
                }

                $this->collProvCheckListPreguntas = $collProvCheckListPreguntas;
                $this->collProvCheckListPreguntasPartial = false;
            }
        }

        return $this->collProvCheckListPreguntas;
    }

    /**
     * Sets a collection of ProvCheckListPregunta objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $provCheckListPreguntas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function setProvCheckListPreguntas(PropelCollection $provCheckListPreguntas, PropelPDO $con = null)
    {
        $provCheckListPreguntasToDelete = $this->getProvCheckListPreguntas(new Criteria(), $con)->diff($provCheckListPreguntas);


        $this->provCheckListPreguntasScheduledForDeletion = $provCheckListPreguntasToDelete;

        foreach ($provCheckListPreguntasToDelete as $provCheckListPreguntaRemoved) {
            $provCheckListPreguntaRemoved->setProvListaDocs(null);
        }

        $this->collProvCheckListPreguntas = null;
        foreach ($provCheckListPreguntas as $provCheckListPregunta) {
            $this->addProvCheckListPregunta($provCheckListPregunta);
        }

        $this->collProvCheckListPreguntas = $provCheckListPreguntas;
        $this->collProvCheckListPreguntasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ProvCheckListPregunta objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ProvCheckListPregunta objects.
     * @throws PropelException
     */
    public function countProvCheckListPreguntas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collProvCheckListPreguntasPartial && !$this->isNew();
        if (null === $this->collProvCheckListPreguntas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collProvCheckListPreguntas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getProvCheckListPreguntas());
            }
            $query = ProvCheckListPreguntaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByProvListaDocs($this)
                ->count($con);
        }

        return count($this->collProvCheckListPreguntas);
    }

    /**
     * Method called to associate a ProvCheckListPregunta object to this object
     * through the ProvCheckListPregunta foreign key attribute.
     *
     * @param    ProvCheckListPregunta $l ProvCheckListPregunta
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function addProvCheckListPregunta(ProvCheckListPregunta $l)
    {
        if ($this->collProvCheckListPreguntas === null) {
            $this->initProvCheckListPreguntas();
            $this->collProvCheckListPreguntasPartial = true;
        }

        if (!in_array($l, $this->collProvCheckListPreguntas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddProvCheckListPregunta($l);

            if ($this->provCheckListPreguntasScheduledForDeletion and $this->provCheckListPreguntasScheduledForDeletion->contains($l)) {
                $this->provCheckListPreguntasScheduledForDeletion->remove($this->provCheckListPreguntasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ProvCheckListPregunta $provCheckListPregunta The provCheckListPregunta object to add.
     */
    protected function doAddProvCheckListPregunta($provCheckListPregunta)
    {
        $this->collProvCheckListPreguntas[]= $provCheckListPregunta;
        $provCheckListPregunta->setProvListaDocs($this);
    }

    /**
     * @param	ProvCheckListPregunta $provCheckListPregunta The provCheckListPregunta object to remove.
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function removeProvCheckListPregunta($provCheckListPregunta)
    {
        if ($this->getProvCheckListPreguntas()->contains($provCheckListPregunta)) {
            $this->collProvCheckListPreguntas->remove($this->collProvCheckListPreguntas->search($provCheckListPregunta));
            if (null === $this->provCheckListPreguntasScheduledForDeletion) {
                $this->provCheckListPreguntasScheduledForDeletion = clone $this->collProvCheckListPreguntas;
                $this->provCheckListPreguntasScheduledForDeletion->clear();
            }
            $this->provCheckListPreguntasScheduledForDeletion[]= clone $provCheckListPregunta;
            $provCheckListPregunta->setProvListaDocs(null);
        }

        return $this;
    }

    /**
     * Clears out the collProvDocumentos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return ProvListaDocs The current object (for fluent API support)
     * @see        addProvDocumentos()
     */
    public function clearProvDocumentos()
    {
        $this->collProvDocumentos = null; // important to set this to null since that means it is uninitialized
        $this->collProvDocumentosPartial = null;

        return $this;
    }

    /**
     * reset is the collProvDocumentos collection loaded partially
     *
     * @return void
     */
    public function resetPartialProvDocumentos($v = true)
    {
        $this->collProvDocumentosPartial = $v;
    }

    /**
     * Initializes the collProvDocumentos collection.
     *
     * By default this just sets the collProvDocumentos collection to an empty array (like clearcollProvDocumentos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initProvDocumentos($overrideExisting = true)
    {
        if (null !== $this->collProvDocumentos && !$overrideExisting) {
            return;
        }
        $this->collProvDocumentos = new PropelObjectCollection();
        $this->collProvDocumentos->setModel('ProvDocumento');
    }

    /**
     * Gets an array of ProvDocumento objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this ProvListaDocs is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ProvDocumento[] List of ProvDocumento objects
     * @throws PropelException
     */
    public function getProvDocumentos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collProvDocumentosPartial && !$this->isNew();
        if (null === $this->collProvDocumentos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collProvDocumentos) {
                // return empty collection
                $this->initProvDocumentos();
            } else {
                $collProvDocumentos = ProvDocumentoQuery::create(null, $criteria)
                    ->filterByProvListaDocs($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collProvDocumentosPartial && count($collProvDocumentos)) {
                      $this->initProvDocumentos(false);

                      foreach ($collProvDocumentos as $obj) {
                        if (false == $this->collProvDocumentos->contains($obj)) {
                          $this->collProvDocumentos->append($obj);
                        }
                      }

                      $this->collProvDocumentosPartial = true;
                    }

                    $collProvDocumentos->getInternalIterator()->rewind();

                    return $collProvDocumentos;
                }

                if ($partial && $this->collProvDocumentos) {
                    foreach ($this->collProvDocumentos as $obj) {
                        if ($obj->isNew()) {
                            $collProvDocumentos[] = $obj;
                        }
                    }
                }

                $this->collProvDocumentos = $collProvDocumentos;
                $this->collProvDocumentosPartial = false;
            }
        }

        return $this->collProvDocumentos;
    }

    /**
     * Sets a collection of ProvDocumento objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $provDocumentos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function setProvDocumentos(PropelCollection $provDocumentos, PropelPDO $con = null)
    {
        $provDocumentosToDelete = $this->getProvDocumentos(new Criteria(), $con)->diff($provDocumentos);


        $this->provDocumentosScheduledForDeletion = $provDocumentosToDelete;

        foreach ($provDocumentosToDelete as $provDocumentoRemoved) {
            $provDocumentoRemoved->setProvListaDocs(null);
        }

        $this->collProvDocumentos = null;
        foreach ($provDocumentos as $provDocumento) {
            $this->addProvDocumento($provDocumento);
        }

        $this->collProvDocumentos = $provDocumentos;
        $this->collProvDocumentosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ProvDocumento objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ProvDocumento objects.
     * @throws PropelException
     */
    public function countProvDocumentos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collProvDocumentosPartial && !$this->isNew();
        if (null === $this->collProvDocumentos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collProvDocumentos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getProvDocumentos());
            }
            $query = ProvDocumentoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByProvListaDocs($this)
                ->count($con);
        }

        return count($this->collProvDocumentos);
    }

    /**
     * Method called to associate a ProvDocumento object to this object
     * through the ProvDocumento foreign key attribute.
     *
     * @param    ProvDocumento $l ProvDocumento
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function addProvDocumento(ProvDocumento $l)
    {
        if ($this->collProvDocumentos === null) {
            $this->initProvDocumentos();
            $this->collProvDocumentosPartial = true;
        }

        if (!in_array($l, $this->collProvDocumentos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddProvDocumento($l);

            if ($this->provDocumentosScheduledForDeletion and $this->provDocumentosScheduledForDeletion->contains($l)) {
                $this->provDocumentosScheduledForDeletion->remove($this->provDocumentosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ProvDocumento $provDocumento The provDocumento object to add.
     */
    protected function doAddProvDocumento($provDocumento)
    {
        $this->collProvDocumentos[]= $provDocumento;
        $provDocumento->setProvListaDocs($this);
    }

    /**
     * @param	ProvDocumento $provDocumento The provDocumento object to remove.
     * @return ProvListaDocs The current object (for fluent API support)
     */
    public function removeProvDocumento($provDocumento)
    {
        if ($this->getProvDocumentos()->contains($provDocumento)) {
            $this->collProvDocumentos->remove($this->collProvDocumentos->search($provDocumento));
            if (null === $this->provDocumentosScheduledForDeletion) {
                $this->provDocumentosScheduledForDeletion = clone $this->collProvDocumentos;
                $this->provDocumentosScheduledForDeletion->clear();
            }
            $this->provDocumentosScheduledForDeletion[]= clone $provDocumento;
            $provDocumento->setProvListaDocs(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this ProvListaDocs is new, it will return
     * an empty collection; or if this ProvListaDocs has previously
     * been saved, it will retrieve related ProvDocumentos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in ProvListaDocs.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ProvDocumento[] List of ProvDocumento objects
     */
    public function getProvDocumentosJoinProvPeriodoValidez($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ProvDocumentoQuery::create(null, $criteria);
        $query->joinWith('ProvPeriodoValidez', $join_behavior);

        return $this->getProvDocumentos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this ProvListaDocs is new, it will return
     * an empty collection; or if this ProvListaDocs has previously
     * been saved, it will retrieve related ProvDocumentos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in ProvListaDocs.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ProvDocumento[] List of ProvDocumento objects
     */
    public function getProvDocumentosJoinProvEstadoDoc($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ProvDocumentoQuery::create(null, $criteria);
        $query->joinWith('ProvEstadoDoc', $join_behavior);

        return $this->getProvDocumentos($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->prov_lista_docs_id = null;
        $this->nombre = null;
        $this->descripcion = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collProvCheckListPreguntas) {
                foreach ($this->collProvCheckListPreguntas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collProvDocumentos) {
                foreach ($this->collProvDocumentos as $o) {
                    $o->clearAllReferences($deep);
                }
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collProvCheckListPreguntas instanceof PropelCollection) {
            $this->collProvCheckListPreguntas->clearIterator();
        }
        $this->collProvCheckListPreguntas = null;
        if ($this->collProvDocumentos instanceof PropelCollection) {
            $this->collProvDocumentos->clearIterator();
        }
        $this->collProvDocumentos = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(ProvListaDocsPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseProvListaDocs:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

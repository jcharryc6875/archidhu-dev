<?php


/**
 * Base class that represents a query for the 'TIPO_FIRMA_DIGITAL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:02
 *
 * @method TipoFirmaDigitalQuery orderByTipofirmadigitalId($order = Criteria::ASC) Order by the TIPOFIRMADIGITAL_ID column
 * @method TipoFirmaDigitalQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 *
 * @method TipoFirmaDigitalQuery groupByTipofirmadigitalId() Group by the TIPOFIRMADIGITAL_ID column
 * @method TipoFirmaDigitalQuery groupByDescripcion() Group by the DESCRIPCION column
 *
 * @method TipoFirmaDigitalQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TipoFirmaDigitalQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TipoFirmaDigitalQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TipoFirmaDigitalQuery leftJoinTipoComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoComRecibida relation
 * @method TipoFirmaDigitalQuery rightJoinTipoComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoComRecibida relation
 * @method TipoFirmaDigitalQuery innerJoinTipoComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoComRecibida relation
 *
 * @method TipoFirmaDigitalQuery leftJoinComEnviada($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComEnviada relation
 * @method TipoFirmaDigitalQuery rightJoinComEnviada($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComEnviada relation
 * @method TipoFirmaDigitalQuery innerJoinComEnviada($relationAlias = null) Adds a INNER JOIN clause to the query using the ComEnviada relation
 *
 * @method TipoFirmaDigitalQuery leftJoinComInterna($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComInterna relation
 * @method TipoFirmaDigitalQuery rightJoinComInterna($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComInterna relation
 * @method TipoFirmaDigitalQuery innerJoinComInterna($relationAlias = null) Adds a INNER JOIN clause to the query using the ComInterna relation
 *
 * @method TipoFirmaDigitalQuery leftJoinComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComRecibida relation
 * @method TipoFirmaDigitalQuery rightJoinComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComRecibida relation
 * @method TipoFirmaDigitalQuery innerJoinComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the ComRecibida relation
 *
 * @method TipoFirmaDigitalQuery leftJoinContenidoUnidadDocumental($relationAlias = null) Adds a LEFT JOIN clause to the query using the ContenidoUnidadDocumental relation
 * @method TipoFirmaDigitalQuery rightJoinContenidoUnidadDocumental($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ContenidoUnidadDocumental relation
 * @method TipoFirmaDigitalQuery innerJoinContenidoUnidadDocumental($relationAlias = null) Adds a INNER JOIN clause to the query using the ContenidoUnidadDocumental relation
 *
 * @method TipoFirmaDigital findOne(PropelPDO $con = null) Return the first TipoFirmaDigital matching the query
 * @method TipoFirmaDigital findOneOrCreate(PropelPDO $con = null) Return the first TipoFirmaDigital matching the query, or a new TipoFirmaDigital object populated from the query conditions when no match is found
 *
 * @method TipoFirmaDigital findOneByDescripcion(string $DESCRIPCION) Return the first TipoFirmaDigital filtered by the DESCRIPCION column
 *
 * @method array findByTipofirmadigitalId(int $TIPOFIRMADIGITAL_ID) Return TipoFirmaDigital objects filtered by the TIPOFIRMADIGITAL_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return TipoFirmaDigital objects filtered by the DESCRIPCION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTipoFirmaDigitalQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTipoFirmaDigitalQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'TipoFirmaDigital';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TipoFirmaDigitalQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   TipoFirmaDigitalQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TipoFirmaDigitalQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TipoFirmaDigitalQuery) {
            return $criteria;
        }
        $query = new TipoFirmaDigitalQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   TipoFirmaDigital|TipoFirmaDigital[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TipoFirmaDigitalPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TipoFirmaDigitalPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoFirmaDigital A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByTipofirmadigitalId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoFirmaDigital A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [TIPOFIRMADIGITAL_ID], [DESCRIPCION] FROM [TIPO_FIRMA_DIGITAL] WHERE [TIPOFIRMADIGITAL_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new TipoFirmaDigital();
            $obj->hydrate($row);
            TipoFirmaDigitalPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return TipoFirmaDigital|TipoFirmaDigital[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|TipoFirmaDigital[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the TIPOFIRMADIGITAL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipofirmadigitalId(1234); // WHERE TIPOFIRMADIGITAL_ID = 1234
     * $query->filterByTipofirmadigitalId(array(12, 34)); // WHERE TIPOFIRMADIGITAL_ID IN (12, 34)
     * $query->filterByTipofirmadigitalId(array('min' => 12)); // WHERE TIPOFIRMADIGITAL_ID >= 12
     * $query->filterByTipofirmadigitalId(array('max' => 12)); // WHERE TIPOFIRMADIGITAL_ID <= 12
     * </code>
     *
     * @param     mixed $tipofirmadigitalId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function filterByTipofirmadigitalId($tipofirmadigitalId = null, $comparison = null)
    {
        if (is_array($tipofirmadigitalId)) {
            $useMinMax = false;
            if (isset($tipofirmadigitalId['min'])) {
                $this->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $tipofirmadigitalId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipofirmadigitalId['max'])) {
                $this->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $tipofirmadigitalId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $tipofirmadigitalId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoFirmaDigitalPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query by a related TipoComRecibida object
     *
     * @param   TipoComRecibida|PropelObjectCollection $tipoComRecibida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoFirmaDigitalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoComRecibida($tipoComRecibida, $comparison = null)
    {
        if ($tipoComRecibida instanceof TipoComRecibida) {
            return $this
                ->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $tipoComRecibida->getTipofirmadigitalId(), $comparison);
        } elseif ($tipoComRecibida instanceof PropelObjectCollection) {
            return $this
                ->useTipoComRecibidaQuery()
                ->filterByPrimaryKeys($tipoComRecibida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTipoComRecibida() only accepts arguments of type TipoComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function joinTipoComRecibida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoComRecibida');
        }

        return $this;
    }

    /**
     * Use the TipoComRecibida relation TipoComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useTipoComRecibidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoComRecibida', 'TipoComRecibidaQuery');
    }

    /**
     * Filter the query by a related ComEnviada object
     *
     * @param   ComEnviada|PropelObjectCollection $comEnviada  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoFirmaDigitalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComEnviada($comEnviada, $comparison = null)
    {
        if ($comEnviada instanceof ComEnviada) {
            return $this
                ->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $comEnviada->getTipofirmadigitalId(), $comparison);
        } elseif ($comEnviada instanceof PropelObjectCollection) {
            return $this
                ->useComEnviadaQuery()
                ->filterByPrimaryKeys($comEnviada->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComEnviada() only accepts arguments of type ComEnviada or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComEnviada relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function joinComEnviada($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComEnviada');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComEnviada');
        }

        return $this;
    }

    /**
     * Use the ComEnviada relation ComEnviada object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComEnviadaQuery A secondary query class using the current class as primary query
     */
    public function useComEnviadaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComEnviada($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComEnviada', 'ComEnviadaQuery');
    }

    /**
     * Filter the query by a related ComInterna object
     *
     * @param   ComInterna|PropelObjectCollection $comInterna  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoFirmaDigitalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComInterna($comInterna, $comparison = null)
    {
        if ($comInterna instanceof ComInterna) {
            return $this
                ->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $comInterna->getTipofirmadigitalId(), $comparison);
        } elseif ($comInterna instanceof PropelObjectCollection) {
            return $this
                ->useComInternaQuery()
                ->filterByPrimaryKeys($comInterna->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComInterna() only accepts arguments of type ComInterna or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComInterna relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function joinComInterna($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComInterna');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComInterna');
        }

        return $this;
    }

    /**
     * Use the ComInterna relation ComInterna object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComInternaQuery A secondary query class using the current class as primary query
     */
    public function useComInternaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComInterna($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComInterna', 'ComInternaQuery');
    }

    /**
     * Filter the query by a related ComRecibida object
     *
     * @param   ComRecibida|PropelObjectCollection $comRecibida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoFirmaDigitalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComRecibida($comRecibida, $comparison = null)
    {
        if ($comRecibida instanceof ComRecibida) {
            return $this
                ->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $comRecibida->getTipofirmadigitalId(), $comparison);
        } elseif ($comRecibida instanceof PropelObjectCollection) {
            return $this
                ->useComRecibidaQuery()
                ->filterByPrimaryKeys($comRecibida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComRecibida() only accepts arguments of type ComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function joinComRecibida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComRecibida');
        }

        return $this;
    }

    /**
     * Use the ComRecibida relation ComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useComRecibidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComRecibida', 'ComRecibidaQuery');
    }

    /**
     * Filter the query by a related ContenidoUnidadDocumental object
     *
     * @param   ContenidoUnidadDocumental|PropelObjectCollection $contenidoUnidadDocumental  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoFirmaDigitalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByContenidoUnidadDocumental($contenidoUnidadDocumental, $comparison = null)
    {
        if ($contenidoUnidadDocumental instanceof ContenidoUnidadDocumental) {
            return $this
                ->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $contenidoUnidadDocumental->getTipofirmadigitalId(), $comparison);
        } elseif ($contenidoUnidadDocumental instanceof PropelObjectCollection) {
            return $this
                ->useContenidoUnidadDocumentalQuery()
                ->filterByPrimaryKeys($contenidoUnidadDocumental->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByContenidoUnidadDocumental() only accepts arguments of type ContenidoUnidadDocumental or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ContenidoUnidadDocumental relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function joinContenidoUnidadDocumental($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ContenidoUnidadDocumental');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ContenidoUnidadDocumental');
        }

        return $this;
    }

    /**
     * Use the ContenidoUnidadDocumental relation ContenidoUnidadDocumental object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ContenidoUnidadDocumentalQuery A secondary query class using the current class as primary query
     */
    public function useContenidoUnidadDocumentalQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinContenidoUnidadDocumental($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ContenidoUnidadDocumental', 'ContenidoUnidadDocumentalQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   TipoFirmaDigital $tipoFirmaDigital Object to remove from the list of results
     *
     * @return TipoFirmaDigitalQuery The current query, for fluid interface
     */
    public function prune($tipoFirmaDigital = null)
    {
        if ($tipoFirmaDigital) {
            $this->addUsingAlias(TipoFirmaDigitalPeer::TIPOFIRMADIGITAL_ID, $tipoFirmaDigital->getTipofirmadigitalId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

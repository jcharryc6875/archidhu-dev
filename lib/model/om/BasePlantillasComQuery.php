<?php


/**
 * Base class that represents a query for the 'PLANTILLAS_COM' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * mar 04 nov 2025 20:45:56
 *
 * @method PlantillasComQuery orderByPlantillascomId($order = Criteria::ASC) Order by the PLANTILLASCOM_ID column
 * @method PlantillasComQuery orderByModuloId($order = Criteria::ASC) Order by the MODULO_ID column
 * @method PlantillasComQuery orderByDependenciaId($order = Criteria::ASC) Order by the DEPENDENCIA_ID column
 * @method PlantillasComQuery orderByRegionalId($order = Criteria::ASC) Order by the REGIONAL_ID column
 * @method PlantillasComQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method PlantillasComQuery orderByEsActual($order = Criteria::ASC) Order by the ES_ACTUAL column
 * @method PlantillasComQuery orderByCodigo($order = Criteria::ASC) Order by the CODIGO column
 * @method PlantillasComQuery orderByContents($order = Criteria::ASC) Order by the CONTENTS column
 * @method PlantillasComQuery orderByNombre($order = Criteria::ASC) Order by the NOMBRE column
 * @method PlantillasComQuery orderBySuborigenid($order = Criteria::ASC) Order by the SUBORIGENID column
 * @method PlantillasComQuery orderByUseHeaders($order = Criteria::ASC) Order by the USE_HEADERS column
 * @method PlantillasComQuery orderByImageMembrete($order = Criteria::ASC) Order by the IMAGE_MEMBRETE column
 * @method PlantillasComQuery orderByUseMembrete($order = Criteria::ASC) Order by the USE_MEMBRETE column
 * @method PlantillasComQuery orderByGeneraServicio($order = Criteria::ASC) Order by the GENERA_SERVICIO column
 * @method PlantillasComQuery orderByFirmaDesatendida($order = Criteria::ASC) Order by the FIRMA_DESATENDIDA column
 *
 * @method PlantillasComQuery groupByPlantillascomId() Group by the PLANTILLASCOM_ID column
 * @method PlantillasComQuery groupByModuloId() Group by the MODULO_ID column
 * @method PlantillasComQuery groupByDependenciaId() Group by the DEPENDENCIA_ID column
 * @method PlantillasComQuery groupByRegionalId() Group by the REGIONAL_ID column
 * @method PlantillasComQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method PlantillasComQuery groupByEsActual() Group by the ES_ACTUAL column
 * @method PlantillasComQuery groupByCodigo() Group by the CODIGO column
 * @method PlantillasComQuery groupByContents() Group by the CONTENTS column
 * @method PlantillasComQuery groupByNombre() Group by the NOMBRE column
 * @method PlantillasComQuery groupBySuborigenid() Group by the SUBORIGENID column
 * @method PlantillasComQuery groupByUseHeaders() Group by the USE_HEADERS column
 * @method PlantillasComQuery groupByImageMembrete() Group by the IMAGE_MEMBRETE column
 * @method PlantillasComQuery groupByUseMembrete() Group by the USE_MEMBRETE column
 * @method PlantillasComQuery groupByGeneraServicio() Group by the GENERA_SERVICIO column
 * @method PlantillasComQuery groupByFirmaDesatendida() Group by the FIRMA_DESATENDIDA column
 *
 * @method PlantillasComQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method PlantillasComQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method PlantillasComQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method PlantillasComQuery leftJoinModulo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Modulo relation
 * @method PlantillasComQuery rightJoinModulo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Modulo relation
 * @method PlantillasComQuery innerJoinModulo($relationAlias = null) Adds a INNER JOIN clause to the query using the Modulo relation
 *
 * @method PlantillasComQuery leftJoinDependencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Dependencia relation
 * @method PlantillasComQuery rightJoinDependencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Dependencia relation
 * @method PlantillasComQuery innerJoinDependencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Dependencia relation
 *
 * @method PlantillasComQuery leftJoinRegional($relationAlias = null) Adds a LEFT JOIN clause to the query using the Regional relation
 * @method PlantillasComQuery rightJoinRegional($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Regional relation
 * @method PlantillasComQuery innerJoinRegional($relationAlias = null) Adds a INNER JOIN clause to the query using the Regional relation
 *
 * @method PlantillasComQuery leftJoinComEnviada($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComEnviada relation
 * @method PlantillasComQuery rightJoinComEnviada($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComEnviada relation
 * @method PlantillasComQuery innerJoinComEnviada($relationAlias = null) Adds a INNER JOIN clause to the query using the ComEnviada relation
 *
 * @method PlantillasComQuery leftJoinComenviadaMasivas($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComenviadaMasivas relation
 * @method PlantillasComQuery rightJoinComenviadaMasivas($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComenviadaMasivas relation
 * @method PlantillasComQuery innerJoinComenviadaMasivas($relationAlias = null) Adds a INNER JOIN clause to the query using the ComenviadaMasivas relation
 *
 * @method PlantillasComQuery leftJoinPlantillascomServicio($relationAlias = null) Adds a LEFT JOIN clause to the query using the PlantillascomServicio relation
 * @method PlantillasComQuery rightJoinPlantillascomServicio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PlantillascomServicio relation
 * @method PlantillasComQuery innerJoinPlantillascomServicio($relationAlias = null) Adds a INNER JOIN clause to the query using the PlantillascomServicio relation
 *
 * @method PlantillasComQuery leftJoinTipoComInterna($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoComInterna relation
 * @method PlantillasComQuery rightJoinTipoComInterna($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoComInterna relation
 * @method PlantillasComQuery innerJoinTipoComInterna($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoComInterna relation
 *
 * @method PlantillasCom findOne(PropelPDO $con = null) Return the first PlantillasCom matching the query
 * @method PlantillasCom findOneOrCreate(PropelPDO $con = null) Return the first PlantillasCom matching the query, or a new PlantillasCom object populated from the query conditions when no match is found
 *
 * @method PlantillasCom findOneByModuloId(int $MODULO_ID) Return the first PlantillasCom filtered by the MODULO_ID column
 * @method PlantillasCom findOneByDependenciaId(int $DEPENDENCIA_ID) Return the first PlantillasCom filtered by the DEPENDENCIA_ID column
 * @method PlantillasCom findOneByRegionalId(int $REGIONAL_ID) Return the first PlantillasCom filtered by the REGIONAL_ID column
 * @method PlantillasCom findOneByDescripcion(string $DESCRIPCION) Return the first PlantillasCom filtered by the DESCRIPCION column
 * @method PlantillasCom findOneByEsActual(boolean $ES_ACTUAL) Return the first PlantillasCom filtered by the ES_ACTUAL column
 * @method PlantillasCom findOneByCodigo(string $CODIGO) Return the first PlantillasCom filtered by the CODIGO column
 * @method PlantillasCom findOneByContents(string $CONTENTS) Return the first PlantillasCom filtered by the CONTENTS column
 * @method PlantillasCom findOneByNombre(string $NOMBRE) Return the first PlantillasCom filtered by the NOMBRE column
 * @method PlantillasCom findOneBySuborigenid(int $SUBORIGENID) Return the first PlantillasCom filtered by the SUBORIGENID column
 * @method PlantillasCom findOneByUseHeaders(int $USE_HEADERS) Return the first PlantillasCom filtered by the USE_HEADERS column
 * @method PlantillasCom findOneByImageMembrete(string $IMAGE_MEMBRETE) Return the first PlantillasCom filtered by the IMAGE_MEMBRETE column
 * @method PlantillasCom findOneByUseMembrete(int $USE_MEMBRETE) Return the first PlantillasCom filtered by the USE_MEMBRETE column
 * @method PlantillasCom findOneByGeneraServicio(int $GENERA_SERVICIO) Return the first PlantillasCom filtered by the GENERA_SERVICIO column
 * @method PlantillasCom findOneByFirmaDesatendida(int $FIRMA_DESATENDIDA) Return the first PlantillasCom filtered by the FIRMA_DESATENDIDA column
 *
 * @method array findByPlantillascomId(int $PLANTILLASCOM_ID) Return PlantillasCom objects filtered by the PLANTILLASCOM_ID column
 * @method array findByModuloId(int $MODULO_ID) Return PlantillasCom objects filtered by the MODULO_ID column
 * @method array findByDependenciaId(int $DEPENDENCIA_ID) Return PlantillasCom objects filtered by the DEPENDENCIA_ID column
 * @method array findByRegionalId(int $REGIONAL_ID) Return PlantillasCom objects filtered by the REGIONAL_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return PlantillasCom objects filtered by the DESCRIPCION column
 * @method array findByEsActual(boolean $ES_ACTUAL) Return PlantillasCom objects filtered by the ES_ACTUAL column
 * @method array findByCodigo(string $CODIGO) Return PlantillasCom objects filtered by the CODIGO column
 * @method array findByContents(string $CONTENTS) Return PlantillasCom objects filtered by the CONTENTS column
 * @method array findByNombre(string $NOMBRE) Return PlantillasCom objects filtered by the NOMBRE column
 * @method array findBySuborigenid(int $SUBORIGENID) Return PlantillasCom objects filtered by the SUBORIGENID column
 * @method array findByUseHeaders(int $USE_HEADERS) Return PlantillasCom objects filtered by the USE_HEADERS column
 * @method array findByImageMembrete(string $IMAGE_MEMBRETE) Return PlantillasCom objects filtered by the IMAGE_MEMBRETE column
 * @method array findByUseMembrete(int $USE_MEMBRETE) Return PlantillasCom objects filtered by the USE_MEMBRETE column
 * @method array findByGeneraServicio(int $GENERA_SERVICIO) Return PlantillasCom objects filtered by the GENERA_SERVICIO column
 * @method array findByFirmaDesatendida(int $FIRMA_DESATENDIDA) Return PlantillasCom objects filtered by the FIRMA_DESATENDIDA column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BasePlantillasComQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BasePlantillasComQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'PlantillasCom';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new PlantillasComQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   PlantillasComQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return PlantillasComQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof PlantillasComQuery) {
            return $criteria;
        }
        $query = new PlantillasComQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   PlantillasCom|PlantillasCom[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = PlantillasComPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(PlantillasComPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 PlantillasCom A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByPlantillascomId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 PlantillasCom A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [PLANTILLASCOM_ID], [MODULO_ID], [DEPENDENCIA_ID], [REGIONAL_ID], [DESCRIPCION], [ES_ACTUAL], [CODIGO], [CONTENTS], [NOMBRE], [SUBORIGENID], [USE_HEADERS], [IMAGE_MEMBRETE], [USE_MEMBRETE], [GENERA_SERVICIO], [FIRMA_DESATENDIDA] FROM [PLANTILLAS_COM] WHERE [PLANTILLASCOM_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new PlantillasCom();
            $obj->hydrate($row);
            PlantillasComPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return PlantillasCom|PlantillasCom[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|PlantillasCom[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the PLANTILLASCOM_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByPlantillascomId(1234); // WHERE PLANTILLASCOM_ID = 1234
     * $query->filterByPlantillascomId(array(12, 34)); // WHERE PLANTILLASCOM_ID IN (12, 34)
     * $query->filterByPlantillascomId(array('min' => 12)); // WHERE PLANTILLASCOM_ID >= 12
     * $query->filterByPlantillascomId(array('max' => 12)); // WHERE PLANTILLASCOM_ID <= 12
     * </code>
     *
     * @param     mixed $plantillascomId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByPlantillascomId($plantillascomId = null, $comparison = null)
    {
        if (is_array($plantillascomId)) {
            $useMinMax = false;
            if (isset($plantillascomId['min'])) {
                $this->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $plantillascomId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($plantillascomId['max'])) {
                $this->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $plantillascomId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $plantillascomId, $comparison);
    }

    /**
     * Filter the query on the MODULO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByModuloId(1234); // WHERE MODULO_ID = 1234
     * $query->filterByModuloId(array(12, 34)); // WHERE MODULO_ID IN (12, 34)
     * $query->filterByModuloId(array('min' => 12)); // WHERE MODULO_ID >= 12
     * $query->filterByModuloId(array('max' => 12)); // WHERE MODULO_ID <= 12
     * </code>
     *
     * @see       filterByModulo()
     *
     * @param     mixed $moduloId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByModuloId($moduloId = null, $comparison = null)
    {
        if (is_array($moduloId)) {
            $useMinMax = false;
            if (isset($moduloId['min'])) {
                $this->addUsingAlias(PlantillasComPeer::MODULO_ID, $moduloId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($moduloId['max'])) {
                $this->addUsingAlias(PlantillasComPeer::MODULO_ID, $moduloId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::MODULO_ID, $moduloId, $comparison);
    }

    /**
     * Filter the query on the DEPENDENCIA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDependenciaId(1234); // WHERE DEPENDENCIA_ID = 1234
     * $query->filterByDependenciaId(array(12, 34)); // WHERE DEPENDENCIA_ID IN (12, 34)
     * $query->filterByDependenciaId(array('min' => 12)); // WHERE DEPENDENCIA_ID >= 12
     * $query->filterByDependenciaId(array('max' => 12)); // WHERE DEPENDENCIA_ID <= 12
     * </code>
     *
     * @see       filterByDependencia()
     *
     * @param     mixed $dependenciaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByDependenciaId($dependenciaId = null, $comparison = null)
    {
        if (is_array($dependenciaId)) {
            $useMinMax = false;
            if (isset($dependenciaId['min'])) {
                $this->addUsingAlias(PlantillasComPeer::DEPENDENCIA_ID, $dependenciaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($dependenciaId['max'])) {
                $this->addUsingAlias(PlantillasComPeer::DEPENDENCIA_ID, $dependenciaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::DEPENDENCIA_ID, $dependenciaId, $comparison);
    }

    /**
     * Filter the query on the REGIONAL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRegionalId(1234); // WHERE REGIONAL_ID = 1234
     * $query->filterByRegionalId(array(12, 34)); // WHERE REGIONAL_ID IN (12, 34)
     * $query->filterByRegionalId(array('min' => 12)); // WHERE REGIONAL_ID >= 12
     * $query->filterByRegionalId(array('max' => 12)); // WHERE REGIONAL_ID <= 12
     * </code>
     *
     * @see       filterByRegional()
     *
     * @param     mixed $regionalId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByRegionalId($regionalId = null, $comparison = null)
    {
        if (is_array($regionalId)) {
            $useMinMax = false;
            if (isset($regionalId['min'])) {
                $this->addUsingAlias(PlantillasComPeer::REGIONAL_ID, $regionalId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($regionalId['max'])) {
                $this->addUsingAlias(PlantillasComPeer::REGIONAL_ID, $regionalId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::REGIONAL_ID, $regionalId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ES_ACTUAL column
     *
     * Example usage:
     * <code>
     * $query->filterByEsActual(true); // WHERE ES_ACTUAL = true
     * $query->filterByEsActual('yes'); // WHERE ES_ACTUAL = true
     * </code>
     *
     * @param     boolean|string $esActual The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByEsActual($esActual = null, $comparison = null)
    {
        if (is_string($esActual)) {
            $esActual = in_array(strtolower($esActual), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(PlantillasComPeer::ES_ACTUAL, $esActual, $comparison);
    }

    /**
     * Filter the query on the CODIGO column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE CODIGO = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE CODIGO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the CONTENTS column
     *
     * Example usage:
     * <code>
     * $query->filterByContents('fooValue');   // WHERE CONTENTS = 'fooValue'
     * $query->filterByContents('%fooValue%'); // WHERE CONTENTS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contents The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByContents($contents = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contents)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contents)) {
                $contents = str_replace('*', '%', $contents);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::CONTENTS, $contents, $comparison);
    }

    /**
     * Filter the query on the NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE NOMBRE = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the SUBORIGENID column
     *
     * Example usage:
     * <code>
     * $query->filterBySuborigenid(1234); // WHERE SUBORIGENID = 1234
     * $query->filterBySuborigenid(array(12, 34)); // WHERE SUBORIGENID IN (12, 34)
     * $query->filterBySuborigenid(array('min' => 12)); // WHERE SUBORIGENID >= 12
     * $query->filterBySuborigenid(array('max' => 12)); // WHERE SUBORIGENID <= 12
     * </code>
     *
     * @param     mixed $suborigenid The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterBySuborigenid($suborigenid = null, $comparison = null)
    {
        if (is_array($suborigenid)) {
            $useMinMax = false;
            if (isset($suborigenid['min'])) {
                $this->addUsingAlias(PlantillasComPeer::SUBORIGENID, $suborigenid['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($suborigenid['max'])) {
                $this->addUsingAlias(PlantillasComPeer::SUBORIGENID, $suborigenid['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::SUBORIGENID, $suborigenid, $comparison);
    }

    /**
     * Filter the query on the USE_HEADERS column
     *
     * Example usage:
     * <code>
     * $query->filterByUseHeaders(1234); // WHERE USE_HEADERS = 1234
     * $query->filterByUseHeaders(array(12, 34)); // WHERE USE_HEADERS IN (12, 34)
     * $query->filterByUseHeaders(array('min' => 12)); // WHERE USE_HEADERS >= 12
     * $query->filterByUseHeaders(array('max' => 12)); // WHERE USE_HEADERS <= 12
     * </code>
     *
     * @param     mixed $useHeaders The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByUseHeaders($useHeaders = null, $comparison = null)
    {
        if (is_array($useHeaders)) {
            $useMinMax = false;
            if (isset($useHeaders['min'])) {
                $this->addUsingAlias(PlantillasComPeer::USE_HEADERS, $useHeaders['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($useHeaders['max'])) {
                $this->addUsingAlias(PlantillasComPeer::USE_HEADERS, $useHeaders['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::USE_HEADERS, $useHeaders, $comparison);
    }

    /**
     * Filter the query on the IMAGE_MEMBRETE column
     *
     * Example usage:
     * <code>
     * $query->filterByImageMembrete('fooValue');   // WHERE IMAGE_MEMBRETE = 'fooValue'
     * $query->filterByImageMembrete('%fooValue%'); // WHERE IMAGE_MEMBRETE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $imageMembrete The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByImageMembrete($imageMembrete = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($imageMembrete)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $imageMembrete)) {
                $imageMembrete = str_replace('*', '%', $imageMembrete);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::IMAGE_MEMBRETE, $imageMembrete, $comparison);
    }

    /**
     * Filter the query on the USE_MEMBRETE column
     *
     * Example usage:
     * <code>
     * $query->filterByUseMembrete(1234); // WHERE USE_MEMBRETE = 1234
     * $query->filterByUseMembrete(array(12, 34)); // WHERE USE_MEMBRETE IN (12, 34)
     * $query->filterByUseMembrete(array('min' => 12)); // WHERE USE_MEMBRETE >= 12
     * $query->filterByUseMembrete(array('max' => 12)); // WHERE USE_MEMBRETE <= 12
     * </code>
     *
     * @param     mixed $useMembrete The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByUseMembrete($useMembrete = null, $comparison = null)
    {
        if (is_array($useMembrete)) {
            $useMinMax = false;
            if (isset($useMembrete['min'])) {
                $this->addUsingAlias(PlantillasComPeer::USE_MEMBRETE, $useMembrete['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($useMembrete['max'])) {
                $this->addUsingAlias(PlantillasComPeer::USE_MEMBRETE, $useMembrete['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::USE_MEMBRETE, $useMembrete, $comparison);
    }

    /**
     * Filter the query on the GENERA_SERVICIO column
     *
     * Example usage:
     * <code>
     * $query->filterByGeneraServicio(1234); // WHERE GENERA_SERVICIO = 1234
     * $query->filterByGeneraServicio(array(12, 34)); // WHERE GENERA_SERVICIO IN (12, 34)
     * $query->filterByGeneraServicio(array('min' => 12)); // WHERE GENERA_SERVICIO >= 12
     * $query->filterByGeneraServicio(array('max' => 12)); // WHERE GENERA_SERVICIO <= 12
     * </code>
     *
     * @param     mixed $generaServicio The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByGeneraServicio($generaServicio = null, $comparison = null)
    {
        if (is_array($generaServicio)) {
            $useMinMax = false;
            if (isset($generaServicio['min'])) {
                $this->addUsingAlias(PlantillasComPeer::GENERA_SERVICIO, $generaServicio['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($generaServicio['max'])) {
                $this->addUsingAlias(PlantillasComPeer::GENERA_SERVICIO, $generaServicio['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::GENERA_SERVICIO, $generaServicio, $comparison);
    }

    /**
     * Filter the query on the FIRMA_DESATENDIDA column
     *
     * Example usage:
     * <code>
     * $query->filterByFirmaDesatendida(1234); // WHERE FIRMA_DESATENDIDA = 1234
     * $query->filterByFirmaDesatendida(array(12, 34)); // WHERE FIRMA_DESATENDIDA IN (12, 34)
     * $query->filterByFirmaDesatendida(array('min' => 12)); // WHERE FIRMA_DESATENDIDA >= 12
     * $query->filterByFirmaDesatendida(array('max' => 12)); // WHERE FIRMA_DESATENDIDA <= 12
     * </code>
     *
     * @param     mixed $firmaDesatendida The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function filterByFirmaDesatendida($firmaDesatendida = null, $comparison = null)
    {
        if (is_array($firmaDesatendida)) {
            $useMinMax = false;
            if (isset($firmaDesatendida['min'])) {
                $this->addUsingAlias(PlantillasComPeer::FIRMA_DESATENDIDA, $firmaDesatendida['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($firmaDesatendida['max'])) {
                $this->addUsingAlias(PlantillasComPeer::FIRMA_DESATENDIDA, $firmaDesatendida['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PlantillasComPeer::FIRMA_DESATENDIDA, $firmaDesatendida, $comparison);
    }

    /**
     * Filter the query by a related Modulo object
     *
     * @param   Modulo|PropelObjectCollection $modulo The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByModulo($modulo, $comparison = null)
    {
        if ($modulo instanceof Modulo) {
            return $this
                ->addUsingAlias(PlantillasComPeer::MODULO_ID, $modulo->getModuloId(), $comparison);
        } elseif ($modulo instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(PlantillasComPeer::MODULO_ID, $modulo->toKeyValue('PrimaryKey', 'ModuloId'), $comparison);
        } else {
            throw new PropelException('filterByModulo() only accepts arguments of type Modulo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Modulo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinModulo($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Modulo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Modulo');
        }

        return $this;
    }

    /**
     * Use the Modulo relation Modulo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ModuloQuery A secondary query class using the current class as primary query
     */
    public function useModuloQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinModulo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Modulo', 'ModuloQuery');
    }

    /**
     * Filter the query by a related Dependencia object
     *
     * @param   Dependencia|PropelObjectCollection $dependencia The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDependencia($dependencia, $comparison = null)
    {
        if ($dependencia instanceof Dependencia) {
            return $this
                ->addUsingAlias(PlantillasComPeer::DEPENDENCIA_ID, $dependencia->getDependenciaId(), $comparison);
        } elseif ($dependencia instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(PlantillasComPeer::DEPENDENCIA_ID, $dependencia->toKeyValue('PrimaryKey', 'DependenciaId'), $comparison);
        } else {
            throw new PropelException('filterByDependencia() only accepts arguments of type Dependencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Dependencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinDependencia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Dependencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Dependencia');
        }

        return $this;
    }

    /**
     * Use the Dependencia relation Dependencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DependenciaQuery A secondary query class using the current class as primary query
     */
    public function useDependenciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDependencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Dependencia', 'DependenciaQuery');
    }

    /**
     * Filter the query by a related Regional object
     *
     * @param   Regional|PropelObjectCollection $regional The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRegional($regional, $comparison = null)
    {
        if ($regional instanceof Regional) {
            return $this
                ->addUsingAlias(PlantillasComPeer::REGIONAL_ID, $regional->getRegionalId(), $comparison);
        } elseif ($regional instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(PlantillasComPeer::REGIONAL_ID, $regional->toKeyValue('PrimaryKey', 'RegionalId'), $comparison);
        } else {
            throw new PropelException('filterByRegional() only accepts arguments of type Regional or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Regional relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinRegional($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Regional');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Regional');
        }

        return $this;
    }

    /**
     * Use the Regional relation Regional object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RegionalQuery A secondary query class using the current class as primary query
     */
    public function useRegionalQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRegional($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Regional', 'RegionalQuery');
    }

    /**
     * Filter the query by a related ComEnviada object
     *
     * @param   ComEnviada|PropelObjectCollection $comEnviada  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComEnviada($comEnviada, $comparison = null)
    {
        if ($comEnviada instanceof ComEnviada) {
            return $this
                ->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $comEnviada->getPlantillascomId(), $comparison);
        } elseif ($comEnviada instanceof PropelObjectCollection) {
            return $this
                ->useComEnviadaQuery()
                ->filterByPrimaryKeys($comEnviada->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComEnviada() only accepts arguments of type ComEnviada or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComEnviada relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinComEnviada($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComEnviada');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComEnviada');
        }

        return $this;
    }

    /**
     * Use the ComEnviada relation ComEnviada object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComEnviadaQuery A secondary query class using the current class as primary query
     */
    public function useComEnviadaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComEnviada($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComEnviada', 'ComEnviadaQuery');
    }

    /**
     * Filter the query by a related ComenviadaMasivas object
     *
     * @param   ComenviadaMasivas|PropelObjectCollection $comenviadaMasivas  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComenviadaMasivas($comenviadaMasivas, $comparison = null)
    {
        if ($comenviadaMasivas instanceof ComenviadaMasivas) {
            return $this
                ->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $comenviadaMasivas->getPlantillascomId(), $comparison);
        } elseif ($comenviadaMasivas instanceof PropelObjectCollection) {
            return $this
                ->useComenviadaMasivasQuery()
                ->filterByPrimaryKeys($comenviadaMasivas->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComenviadaMasivas() only accepts arguments of type ComenviadaMasivas or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComenviadaMasivas relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinComenviadaMasivas($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComenviadaMasivas');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComenviadaMasivas');
        }

        return $this;
    }

    /**
     * Use the ComenviadaMasivas relation ComenviadaMasivas object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComenviadaMasivasQuery A secondary query class using the current class as primary query
     */
    public function useComenviadaMasivasQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComenviadaMasivas($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComenviadaMasivas', 'ComenviadaMasivasQuery');
    }

    /**
     * Filter the query by a related PlantillascomServicio object
     *
     * @param   PlantillascomServicio|PropelObjectCollection $plantillascomServicio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByPlantillascomServicio($plantillascomServicio, $comparison = null)
    {
        if ($plantillascomServicio instanceof PlantillascomServicio) {
            return $this
                ->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $plantillascomServicio->getPlantillascomId(), $comparison);
        } elseif ($plantillascomServicio instanceof PropelObjectCollection) {
            return $this
                ->usePlantillascomServicioQuery()
                ->filterByPrimaryKeys($plantillascomServicio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPlantillascomServicio() only accepts arguments of type PlantillascomServicio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PlantillascomServicio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinPlantillascomServicio($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PlantillascomServicio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PlantillascomServicio');
        }

        return $this;
    }

    /**
     * Use the PlantillascomServicio relation PlantillascomServicio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PlantillascomServicioQuery A secondary query class using the current class as primary query
     */
    public function usePlantillascomServicioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinPlantillascomServicio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PlantillascomServicio', 'PlantillascomServicioQuery');
    }

    /**
     * Filter the query by a related TipoComInterna object
     *
     * @param   TipoComInterna|PropelObjectCollection $tipoComInterna  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PlantillasComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoComInterna($tipoComInterna, $comparison = null)
    {
        if ($tipoComInterna instanceof TipoComInterna) {
            return $this
                ->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $tipoComInterna->getPlantillascomId(), $comparison);
        } elseif ($tipoComInterna instanceof PropelObjectCollection) {
            return $this
                ->useTipoComInternaQuery()
                ->filterByPrimaryKeys($tipoComInterna->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTipoComInterna() only accepts arguments of type TipoComInterna or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoComInterna relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function joinTipoComInterna($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoComInterna');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoComInterna');
        }

        return $this;
    }

    /**
     * Use the TipoComInterna relation TipoComInterna object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoComInternaQuery A secondary query class using the current class as primary query
     */
    public function useTipoComInternaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoComInterna($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoComInterna', 'TipoComInternaQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   PlantillasCom $plantillasCom Object to remove from the list of results
     *
     * @return PlantillasComQuery The current query, for fluid interface
     */
    public function prune($plantillasCom = null)
    {
        if ($plantillasCom) {
            $this->addUsingAlias(PlantillasComPeer::PLANTILLASCOM_ID, $plantillasCom->getPlantillascomId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

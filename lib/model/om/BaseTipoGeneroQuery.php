<?php


/**
 * Base class that represents a query for the 'TIPO_GENERO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:09
 *
 * @method TipoGeneroQuery orderByTipogeneroId($order = Criteria::ASC) Order by the TIPOGENERO_ID column
 * @method TipoGeneroQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method TipoGeneroQuery orderByEsVisible($order = Criteria::ASC) Order by the ES_VISIBLE column
 *
 * @method TipoGeneroQuery groupByTipogeneroId() Group by the TIPOGENERO_ID column
 * @method TipoGeneroQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method TipoGeneroQuery groupByEsVisible() Group by the ES_VISIBLE column
 *
 * @method TipoGeneroQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TipoGeneroQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TipoGeneroQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TipoGeneroQuery leftJoinRepresentanteLegal($relationAlias = null) Adds a LEFT JOIN clause to the query using the RepresentanteLegal relation
 * @method TipoGeneroQuery rightJoinRepresentanteLegal($relationAlias = null) Adds a RIGHT JOIN clause to the query using the RepresentanteLegal relation
 * @method TipoGeneroQuery innerJoinRepresentanteLegal($relationAlias = null) Adds a INNER JOIN clause to the query using the RepresentanteLegal relation
 *
 * @method TipoGeneroQuery leftJoinInteresados($relationAlias = null) Adds a LEFT JOIN clause to the query using the Interesados relation
 * @method TipoGeneroQuery rightJoinInteresados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Interesados relation
 * @method TipoGeneroQuery innerJoinInteresados($relationAlias = null) Adds a INNER JOIN clause to the query using the Interesados relation
 *
 * @method TipoGenero findOne(PropelPDO $con = null) Return the first TipoGenero matching the query
 * @method TipoGenero findOneOrCreate(PropelPDO $con = null) Return the first TipoGenero matching the query, or a new TipoGenero object populated from the query conditions when no match is found
 *
 * @method TipoGenero findOneByDescripcion(string $DESCRIPCION) Return the first TipoGenero filtered by the DESCRIPCION column
 * @method TipoGenero findOneByEsVisible(int $ES_VISIBLE) Return the first TipoGenero filtered by the ES_VISIBLE column
 *
 * @method array findByTipogeneroId(int $TIPOGENERO_ID) Return TipoGenero objects filtered by the TIPOGENERO_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return TipoGenero objects filtered by the DESCRIPCION column
 * @method array findByEsVisible(int $ES_VISIBLE) Return TipoGenero objects filtered by the ES_VISIBLE column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTipoGeneroQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTipoGeneroQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'TipoGenero';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TipoGeneroQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   TipoGeneroQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TipoGeneroQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TipoGeneroQuery) {
            return $criteria;
        }
        $query = new TipoGeneroQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   TipoGenero|TipoGenero[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TipoGeneroPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TipoGeneroPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoGenero A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByTipogeneroId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TipoGenero A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [TIPOGENERO_ID], [DESCRIPCION], [ES_VISIBLE] FROM [TIPO_GENERO] WHERE [TIPOGENERO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new TipoGenero();
            $obj->hydrate($row);
            TipoGeneroPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return TipoGenero|TipoGenero[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|TipoGenero[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the TIPOGENERO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipogeneroId(1234); // WHERE TIPOGENERO_ID = 1234
     * $query->filterByTipogeneroId(array(12, 34)); // WHERE TIPOGENERO_ID IN (12, 34)
     * $query->filterByTipogeneroId(array('min' => 12)); // WHERE TIPOGENERO_ID >= 12
     * $query->filterByTipogeneroId(array('max' => 12)); // WHERE TIPOGENERO_ID <= 12
     * </code>
     *
     * @param     mixed $tipogeneroId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function filterByTipogeneroId($tipogeneroId = null, $comparison = null)
    {
        if (is_array($tipogeneroId)) {
            $useMinMax = false;
            if (isset($tipogeneroId['min'])) {
                $this->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $tipogeneroId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipogeneroId['max'])) {
                $this->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $tipogeneroId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $tipogeneroId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoGeneroPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ES_VISIBLE column
     *
     * Example usage:
     * <code>
     * $query->filterByEsVisible(1234); // WHERE ES_VISIBLE = 1234
     * $query->filterByEsVisible(array(12, 34)); // WHERE ES_VISIBLE IN (12, 34)
     * $query->filterByEsVisible(array('min' => 12)); // WHERE ES_VISIBLE >= 12
     * $query->filterByEsVisible(array('max' => 12)); // WHERE ES_VISIBLE <= 12
     * </code>
     *
     * @param     mixed $esVisible The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function filterByEsVisible($esVisible = null, $comparison = null)
    {
        if (is_array($esVisible)) {
            $useMinMax = false;
            if (isset($esVisible['min'])) {
                $this->addUsingAlias(TipoGeneroPeer::ES_VISIBLE, $esVisible['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($esVisible['max'])) {
                $this->addUsingAlias(TipoGeneroPeer::ES_VISIBLE, $esVisible['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoGeneroPeer::ES_VISIBLE, $esVisible, $comparison);
    }

    /**
     * Filter the query by a related RepresentanteLegal object
     *
     * @param   RepresentanteLegal|PropelObjectCollection $representanteLegal  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoGeneroQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRepresentanteLegal($representanteLegal, $comparison = null)
    {
        if ($representanteLegal instanceof RepresentanteLegal) {
            return $this
                ->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $representanteLegal->getTipogeneroId(), $comparison);
        } elseif ($representanteLegal instanceof PropelObjectCollection) {
            return $this
                ->useRepresentanteLegalQuery()
                ->filterByPrimaryKeys($representanteLegal->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRepresentanteLegal() only accepts arguments of type RepresentanteLegal or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the RepresentanteLegal relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function joinRepresentanteLegal($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('RepresentanteLegal');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'RepresentanteLegal');
        }

        return $this;
    }

    /**
     * Use the RepresentanteLegal relation RepresentanteLegal object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RepresentanteLegalQuery A secondary query class using the current class as primary query
     */
    public function useRepresentanteLegalQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRepresentanteLegal($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'RepresentanteLegal', 'RepresentanteLegalQuery');
    }

    /**
     * Filter the query by a related Interesados object
     *
     * @param   Interesados|PropelObjectCollection $interesados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TipoGeneroQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByInteresados($interesados, $comparison = null)
    {
        if ($interesados instanceof Interesados) {
            return $this
                ->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $interesados->getTipogeneroId(), $comparison);
        } elseif ($interesados instanceof PropelObjectCollection) {
            return $this
                ->useInteresadosQuery()
                ->filterByPrimaryKeys($interesados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByInteresados() only accepts arguments of type Interesados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Interesados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function joinInteresados($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Interesados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Interesados');
        }

        return $this;
    }

    /**
     * Use the Interesados relation Interesados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   InteresadosQuery A secondary query class using the current class as primary query
     */
    public function useInteresadosQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinInteresados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Interesados', 'InteresadosQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   TipoGenero $tipoGenero Object to remove from the list of results
     *
     * @return TipoGeneroQuery The current query, for fluid interface
     */
    public function prune($tipoGenero = null)
    {
        if ($tipoGenero) {
            $this->addUsingAlias(TipoGeneroPeer::TIPOGENERO_ID, $tipoGenero->getTipogeneroId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

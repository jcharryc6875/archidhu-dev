<?php


/**
 * Base class that represents a row from the 'FACTURA_VITACORA' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:00
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseFacturaVitacora extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'FacturaVitacoraPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        FacturaVitacoraPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the facturavitacora_id field.
     * @var        int
     */
    protected $facturavitacora_id;

    /**
     * The value for the facturaproceso_id field.
     * @var        int
     */
    protected $facturaproceso_id;

    /**
     * The value for the facturaestado_id field.
     * @var        int
     */
    protected $facturaestado_id;

    /**
     * The value for the factura_id field.
     * @var        int
     */
    protected $factura_id;

    /**
     * The value for the fecha_i field.
     * @var        string
     */
    protected $fecha_i;

    /**
     * The value for the fecha_i_date field.
     * @var        string
     */
    protected $fecha_i_date;

    /**
     * The value for the fecha_f field.
     * @var        string
     */
    protected $fecha_f;

    /**
     * The value for the fecha_f_date field.
     * @var        string
     */
    protected $fecha_f_date;

    /**
     * The value for the observaciones field.
     * @var        string
     */
    protected $observaciones;

    /**
     * The value for the ejecutada field.
     * @var        boolean
     */
    protected $ejecutada;

    /**
     * The value for the facturavitacoraestado_id field.
     * @var        int
     */
    protected $facturavitacoraestado_id;

    /**
     * @var        FacturaProceso
     */
    protected $aFacturaProceso;

    /**
     * @var        FacturaEstado
     */
    protected $aFacturaEstado;

    /**
     * @var        Factura
     */
    protected $aFactura;

    /**
     * @var        FacturaVitacoraEstado
     */
    protected $aFacturaVitacoraEstado;

    /**
     * @var        PropelObjectCollection|FactUsuarioDestino[] Collection to store aggregation of FactUsuarioDestino objects.
     */
    protected $collFactUsuarioDestinos;
    protected $collFactUsuarioDestinosPartial;

    /**
     * @var        PropelObjectCollection|FactUsuarioRequisitor[] Collection to store aggregation of FactUsuarioRequisitor objects.
     */
    protected $collFactUsuarioRequisitors;
    protected $collFactUsuarioRequisitorsPartial;

    /**
     * @var        PropelObjectCollection|FacturaVitacoraUsuario[] Collection to store aggregation of FacturaVitacoraUsuario objects.
     */
    protected $collFacturaVitacoraUsuarios;
    protected $collFacturaVitacoraUsuariosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $factUsuarioDestinosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $factUsuarioRequisitorsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $facturaVitacoraUsuariosScheduledForDeletion = null;

    /**
     * Get the [facturavitacora_id] column value.
     * 
     * @return int
     */
    public function getFacturavitacoraId()
    {

        return $this->facturavitacora_id;
    }

    /**
     * Get the [facturaproceso_id] column value.
     * 
     * @return int
     */
    public function getFacturaprocesoId()
    {

        return $this->facturaproceso_id;
    }

    /**
     * Get the [facturaestado_id] column value.
     * 
     * @return int
     */
    public function getFacturaestadoId()
    {

        return $this->facturaestado_id;
    }

    /**
     * Get the [factura_id] column value.
     * 
     * @return int
     */
    public function getFacturaId()
    {

        return $this->factura_id;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_i] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaI($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_i === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_i);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_i, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [optionally formatted] temporal [fecha_i_date] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaIDate($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_i_date === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_i_date);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_i_date, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [optionally formatted] temporal [fecha_f] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaF($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_f === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_f);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_f, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [optionally formatted] temporal [fecha_f_date] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaFDate($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_f_date === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_f_date);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_f_date, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [observaciones] column value.
     * 
     * @return string
     */
    public function getObservaciones()
    {

        return $this->observaciones;
    }

    /**
     * Get the [ejecutada] column value.
     * 
     * @return boolean
     */
    public function getEjecutada()
    {

        return $this->ejecutada;
    }

    /**
     * Get the [facturavitacoraestado_id] column value.
     * 
     * @return int
     */
    public function getFacturavitacoraestadoId()
    {

        return $this->facturavitacoraestado_id;
    }

    /**
     * Set the value of [facturavitacora_id] column.
     * 
     * @param  int $v new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFacturavitacoraId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->facturavitacora_id !== $v) {
            $this->facturavitacora_id = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::FACTURAVITACORA_ID;
        }


        return $this;
    } // setFacturavitacoraId()

    /**
     * Set the value of [facturaproceso_id] column.
     * 
     * @param  int $v new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFacturaprocesoId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->facturaproceso_id !== $v) {
            $this->facturaproceso_id = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::FACTURAPROCESO_ID;
        }

        if ($this->aFacturaProceso !== null && $this->aFacturaProceso->getFacturaprocesoId() !== $v) {
            $this->aFacturaProceso = null;
        }


        return $this;
    } // setFacturaprocesoId()

    /**
     * Set the value of [facturaestado_id] column.
     * 
     * @param  int $v new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFacturaestadoId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->facturaestado_id !== $v) {
            $this->facturaestado_id = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::FACTURAESTADO_ID;
        }

        if ($this->aFacturaEstado !== null && $this->aFacturaEstado->getFacturaestadoId() !== $v) {
            $this->aFacturaEstado = null;
        }


        return $this;
    } // setFacturaestadoId()

    /**
     * Set the value of [factura_id] column.
     * 
     * @param  int $v new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFacturaId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->factura_id !== $v) {
            $this->factura_id = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::FACTURA_ID;
        }

        if ($this->aFactura !== null && $this->aFactura->getFacturaId() !== $v) {
            $this->aFactura = null;
        }


        return $this;
    } // setFacturaId()

    /**
     * Sets the value of [fecha_i] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFechaI($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_i !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_i !== null && $tmpDt = new DateTime($this->fecha_i)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_i = $newDateAsString;
                $this->modifiedColumns[] = FacturaVitacoraPeer::FECHA_I;
            }
        } // if either are not null


        return $this;
    } // setFechaI()

    /**
     * Sets the value of [fecha_i_date] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFechaIDate($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_i_date !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_i_date !== null && $tmpDt = new DateTime($this->fecha_i_date)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_i_date = $newDateAsString;
                $this->modifiedColumns[] = FacturaVitacoraPeer::FECHA_I_DATE;
            }
        } // if either are not null


        return $this;
    } // setFechaIDate()

    /**
     * Sets the value of [fecha_f] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFechaF($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_f !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_f !== null && $tmpDt = new DateTime($this->fecha_f)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_f = $newDateAsString;
                $this->modifiedColumns[] = FacturaVitacoraPeer::FECHA_F;
            }
        } // if either are not null


        return $this;
    } // setFechaF()

    /**
     * Sets the value of [fecha_f_date] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFechaFDate($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_f_date !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_f_date !== null && $tmpDt = new DateTime($this->fecha_f_date)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_f_date = $newDateAsString;
                $this->modifiedColumns[] = FacturaVitacoraPeer::FECHA_F_DATE;
            }
        } // if either are not null


        return $this;
    } // setFechaFDate()

    /**
     * Set the value of [observaciones] column.
     * 
     * @param  string $v new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setObservaciones($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->observaciones !== $v) {
            $this->observaciones = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::OBSERVACIONES;
        }


        return $this;
    } // setObservaciones()

    /**
     * Sets the value of the [ejecutada] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * 
     * @param boolean|integer|string $v The new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setEjecutada($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->ejecutada !== $v) {
            $this->ejecutada = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::EJECUTADA;
        }


        return $this;
    } // setEjecutada()

    /**
     * Set the value of [facturavitacoraestado_id] column.
     * 
     * @param  int $v new value
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFacturavitacoraestadoId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->facturavitacoraestado_id !== $v) {
            $this->facturavitacoraestado_id = $v;
            $this->modifiedColumns[] = FacturaVitacoraPeer::FACTURAVITACORAESTADO_ID;
        }

        if ($this->aFacturaVitacoraEstado !== null && $this->aFacturaVitacoraEstado->getFacturavitacoraestadoId() !== $v) {
            $this->aFacturaVitacoraEstado = null;
        }


        return $this;
    } // setFacturavitacoraestadoId()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->facturavitacora_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->facturaproceso_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->facturaestado_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->factura_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->fecha_i = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->fecha_i_date = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->fecha_f = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->fecha_f_date = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->observaciones = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->ejecutada = ($row[$startcol + 9] !== null) ? (boolean) $row[$startcol + 9] : null;
            $this->facturavitacoraestado_id = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 11; // 11 = FacturaVitacoraPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating FacturaVitacora object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aFacturaProceso !== null && $this->facturaproceso_id !== $this->aFacturaProceso->getFacturaprocesoId()) {
            $this->aFacturaProceso = null;
        }
        if ($this->aFacturaEstado !== null && $this->facturaestado_id !== $this->aFacturaEstado->getFacturaestadoId()) {
            $this->aFacturaEstado = null;
        }
        if ($this->aFactura !== null && $this->factura_id !== $this->aFactura->getFacturaId()) {
            $this->aFactura = null;
        }
        if ($this->aFacturaVitacoraEstado !== null && $this->facturavitacoraestado_id !== $this->aFacturaVitacoraEstado->getFacturavitacoraestadoId()) {
            $this->aFacturaVitacoraEstado = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaVitacoraPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = FacturaVitacoraPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aFacturaProceso = null;
            $this->aFacturaEstado = null;
            $this->aFactura = null;
            $this->aFacturaVitacoraEstado = null;
            $this->collFactUsuarioDestinos = null;

            $this->collFactUsuarioRequisitors = null;

            $this->collFacturaVitacoraUsuarios = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaVitacoraPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = FacturaVitacoraQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseFacturaVitacora:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseFacturaVitacora:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaVitacoraPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseFacturaVitacora:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseFacturaVitacora:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                FacturaVitacoraPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aFacturaProceso !== null) {
                if ($this->aFacturaProceso->isModified() || $this->aFacturaProceso->isNew()) {
                    $affectedRows += $this->aFacturaProceso->save($con);
                }
                $this->setFacturaProceso($this->aFacturaProceso);
            }

            if ($this->aFacturaEstado !== null) {
                if ($this->aFacturaEstado->isModified() || $this->aFacturaEstado->isNew()) {
                    $affectedRows += $this->aFacturaEstado->save($con);
                }
                $this->setFacturaEstado($this->aFacturaEstado);
            }

            if ($this->aFactura !== null) {
                if ($this->aFactura->isModified() || $this->aFactura->isNew()) {
                    $affectedRows += $this->aFactura->save($con);
                }
                $this->setFactura($this->aFactura);
            }

            if ($this->aFacturaVitacoraEstado !== null) {
                if ($this->aFacturaVitacoraEstado->isModified() || $this->aFacturaVitacoraEstado->isNew()) {
                    $affectedRows += $this->aFacturaVitacoraEstado->save($con);
                }
                $this->setFacturaVitacoraEstado($this->aFacturaVitacoraEstado);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->factUsuarioDestinosScheduledForDeletion !== null) {
                if (!$this->factUsuarioDestinosScheduledForDeletion->isEmpty()) {
                    foreach ($this->factUsuarioDestinosScheduledForDeletion as $factUsuarioDestino) {
                        // need to save related object because we set the relation to null
                        $factUsuarioDestino->save($con);
                    }
                    $this->factUsuarioDestinosScheduledForDeletion = null;
                }
            }

            if ($this->collFactUsuarioDestinos !== null) {
                foreach ($this->collFactUsuarioDestinos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->factUsuarioRequisitorsScheduledForDeletion !== null) {
                if (!$this->factUsuarioRequisitorsScheduledForDeletion->isEmpty()) {
                    foreach ($this->factUsuarioRequisitorsScheduledForDeletion as $factUsuarioRequisitor) {
                        // need to save related object because we set the relation to null
                        $factUsuarioRequisitor->save($con);
                    }
                    $this->factUsuarioRequisitorsScheduledForDeletion = null;
                }
            }

            if ($this->collFactUsuarioRequisitors !== null) {
                foreach ($this->collFactUsuarioRequisitors as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->facturaVitacoraUsuariosScheduledForDeletion !== null) {
                if (!$this->facturaVitacoraUsuariosScheduledForDeletion->isEmpty()) {
                    FacturaVitacoraUsuarioQuery::create()
                        ->filterByPrimaryKeys($this->facturaVitacoraUsuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->facturaVitacoraUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collFacturaVitacoraUsuarios !== null) {
                foreach ($this->collFacturaVitacoraUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = FacturaVitacoraPeer::FACTURAVITACORA_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(FacturaVitacoraPeer::FACTURAVITACORA_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . FacturaVitacoraPeer::FACTURAVITACORA_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(FacturaVitacoraPeer::FACTURAVITACORA_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setFacturavitacoraId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aFacturaProceso !== null) {
                if (!$this->aFacturaProceso->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aFacturaProceso->getValidationFailures());
                }
            }

            if ($this->aFacturaEstado !== null) {
                if (!$this->aFacturaEstado->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aFacturaEstado->getValidationFailures());
                }
            }

            if ($this->aFactura !== null) {
                if (!$this->aFactura->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aFactura->getValidationFailures());
                }
            }

            if ($this->aFacturaVitacoraEstado !== null) {
                if (!$this->aFacturaVitacoraEstado->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aFacturaVitacoraEstado->getValidationFailures());
                }
            }


            if (($retval = FacturaVitacoraPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collFactUsuarioDestinos !== null) {
                    foreach ($this->collFactUsuarioDestinos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFactUsuarioRequisitors !== null) {
                    foreach ($this->collFactUsuarioRequisitors as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFacturaVitacoraUsuarios !== null) {
                    foreach ($this->collFacturaVitacoraUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = FacturaVitacoraPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getFacturavitacoraId();
                break;
            case 1:
                return $this->getFacturaprocesoId();
                break;
            case 2:
                return $this->getFacturaestadoId();
                break;
            case 3:
                return $this->getFacturaId();
                break;
            case 4:
                return $this->getFechaI();
                break;
            case 5:
                return $this->getFechaIDate();
                break;
            case 6:
                return $this->getFechaF();
                break;
            case 7:
                return $this->getFechaFDate();
                break;
            case 8:
                return $this->getObservaciones();
                break;
            case 9:
                return $this->getEjecutada();
                break;
            case 10:
                return $this->getFacturavitacoraestadoId();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['FacturaVitacora'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['FacturaVitacora'][$this->getPrimaryKey()] = true;
        $keys = FacturaVitacoraPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getFacturavitacoraId(),
            $keys[1] => $this->getFacturaprocesoId(),
            $keys[2] => $this->getFacturaestadoId(),
            $keys[3] => $this->getFacturaId(),
            $keys[4] => $this->getFechaI(),
            $keys[5] => $this->getFechaIDate(),
            $keys[6] => $this->getFechaF(),
            $keys[7] => $this->getFechaFDate(),
            $keys[8] => $this->getObservaciones(),
            $keys[9] => $this->getEjecutada(),
            $keys[10] => $this->getFacturavitacoraestadoId(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aFacturaProceso) {
                $result['FacturaProceso'] = $this->aFacturaProceso->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aFacturaEstado) {
                $result['FacturaEstado'] = $this->aFacturaEstado->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aFactura) {
                $result['Factura'] = $this->aFactura->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aFacturaVitacoraEstado) {
                $result['FacturaVitacoraEstado'] = $this->aFacturaVitacoraEstado->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collFactUsuarioDestinos) {
                $result['FactUsuarioDestinos'] = $this->collFactUsuarioDestinos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFactUsuarioRequisitors) {
                $result['FactUsuarioRequisitors'] = $this->collFactUsuarioRequisitors->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFacturaVitacoraUsuarios) {
                $result['FacturaVitacoraUsuarios'] = $this->collFacturaVitacoraUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = FacturaVitacoraPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setFacturavitacoraId($value);
                break;
            case 1:
                $this->setFacturaprocesoId($value);
                break;
            case 2:
                $this->setFacturaestadoId($value);
                break;
            case 3:
                $this->setFacturaId($value);
                break;
            case 4:
                $this->setFechaI($value);
                break;
            case 5:
                $this->setFechaIDate($value);
                break;
            case 6:
                $this->setFechaF($value);
                break;
            case 7:
                $this->setFechaFDate($value);
                break;
            case 8:
                $this->setObservaciones($value);
                break;
            case 9:
                $this->setEjecutada($value);
                break;
            case 10:
                $this->setFacturavitacoraestadoId($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = FacturaVitacoraPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setFacturavitacoraId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setFacturaprocesoId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setFacturaestadoId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setFacturaId($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setFechaI($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setFechaIDate($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setFechaF($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setFechaFDate($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setObservaciones($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setEjecutada($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setFacturavitacoraestadoId($arr[$keys[10]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(FacturaVitacoraPeer::DATABASE_NAME);

        if ($this->isColumnModified(FacturaVitacoraPeer::FACTURAVITACORA_ID)) $criteria->add(FacturaVitacoraPeer::FACTURAVITACORA_ID, $this->facturavitacora_id);
        if ($this->isColumnModified(FacturaVitacoraPeer::FACTURAPROCESO_ID)) $criteria->add(FacturaVitacoraPeer::FACTURAPROCESO_ID, $this->facturaproceso_id);
        if ($this->isColumnModified(FacturaVitacoraPeer::FACTURAESTADO_ID)) $criteria->add(FacturaVitacoraPeer::FACTURAESTADO_ID, $this->facturaestado_id);
        if ($this->isColumnModified(FacturaVitacoraPeer::FACTURA_ID)) $criteria->add(FacturaVitacoraPeer::FACTURA_ID, $this->factura_id);
        if ($this->isColumnModified(FacturaVitacoraPeer::FECHA_I)) $criteria->add(FacturaVitacoraPeer::FECHA_I, $this->fecha_i);
        if ($this->isColumnModified(FacturaVitacoraPeer::FECHA_I_DATE)) $criteria->add(FacturaVitacoraPeer::FECHA_I_DATE, $this->fecha_i_date);
        if ($this->isColumnModified(FacturaVitacoraPeer::FECHA_F)) $criteria->add(FacturaVitacoraPeer::FECHA_F, $this->fecha_f);
        if ($this->isColumnModified(FacturaVitacoraPeer::FECHA_F_DATE)) $criteria->add(FacturaVitacoraPeer::FECHA_F_DATE, $this->fecha_f_date);
        if ($this->isColumnModified(FacturaVitacoraPeer::OBSERVACIONES)) $criteria->add(FacturaVitacoraPeer::OBSERVACIONES, $this->observaciones);
        if ($this->isColumnModified(FacturaVitacoraPeer::EJECUTADA)) $criteria->add(FacturaVitacoraPeer::EJECUTADA, $this->ejecutada);
        if ($this->isColumnModified(FacturaVitacoraPeer::FACTURAVITACORAESTADO_ID)) $criteria->add(FacturaVitacoraPeer::FACTURAVITACORAESTADO_ID, $this->facturavitacoraestado_id);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(FacturaVitacoraPeer::DATABASE_NAME);
        $criteria->add(FacturaVitacoraPeer::FACTURAVITACORA_ID, $this->facturavitacora_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getFacturavitacoraId();
    }

    /**
     * Generic method to set the primary key (facturavitacora_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setFacturavitacoraId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getFacturavitacoraId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of FacturaVitacora (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setFacturaprocesoId($this->getFacturaprocesoId());
        $copyObj->setFacturaestadoId($this->getFacturaestadoId());
        $copyObj->setFacturaId($this->getFacturaId());
        $copyObj->setFechaI($this->getFechaI());
        $copyObj->setFechaIDate($this->getFechaIDate());
        $copyObj->setFechaF($this->getFechaF());
        $copyObj->setFechaFDate($this->getFechaFDate());
        $copyObj->setObservaciones($this->getObservaciones());
        $copyObj->setEjecutada($this->getEjecutada());
        $copyObj->setFacturavitacoraestadoId($this->getFacturavitacoraestadoId());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getFactUsuarioDestinos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactUsuarioDestino($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFactUsuarioRequisitors() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactUsuarioRequisitor($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFacturaVitacoraUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFacturaVitacoraUsuario($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setFacturavitacoraId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return FacturaVitacora Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return FacturaVitacoraPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new FacturaVitacoraPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a FacturaProceso object.
     *
     * @param                  FacturaProceso $v
     * @return FacturaVitacora The current object (for fluent API support)
     * @throws PropelException
     */
    public function setFacturaProceso(FacturaProceso $v = null)
    {
        if ($v === null) {
            $this->setFacturaprocesoId(NULL);
        } else {
            $this->setFacturaprocesoId($v->getFacturaprocesoId());
        }

        $this->aFacturaProceso = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the FacturaProceso object, it will not be re-added.
        if ($v !== null) {
            $v->addFacturaVitacora($this);
        }


        return $this;
    }


    /**
     * Get the associated FacturaProceso object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return FacturaProceso The associated FacturaProceso object.
     * @throws PropelException
     */
    public function getFacturaProceso(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aFacturaProceso === null && ($this->facturaproceso_id !== null) && $doQuery) {
            $this->aFacturaProceso = FacturaProcesoQuery::create()->findPk($this->facturaproceso_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aFacturaProceso->addFacturaVitacoras($this);
             */
        }

        return $this->aFacturaProceso;
    }

    /**
     * Declares an association between this object and a FacturaEstado object.
     *
     * @param                  FacturaEstado $v
     * @return FacturaVitacora The current object (for fluent API support)
     * @throws PropelException
     */
    public function setFacturaEstado(FacturaEstado $v = null)
    {
        if ($v === null) {
            $this->setFacturaestadoId(NULL);
        } else {
            $this->setFacturaestadoId($v->getFacturaestadoId());
        }

        $this->aFacturaEstado = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the FacturaEstado object, it will not be re-added.
        if ($v !== null) {
            $v->addFacturaVitacora($this);
        }


        return $this;
    }


    /**
     * Get the associated FacturaEstado object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return FacturaEstado The associated FacturaEstado object.
     * @throws PropelException
     */
    public function getFacturaEstado(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aFacturaEstado === null && ($this->facturaestado_id !== null) && $doQuery) {
            $this->aFacturaEstado = FacturaEstadoQuery::create()->findPk($this->facturaestado_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aFacturaEstado->addFacturaVitacoras($this);
             */
        }

        return $this->aFacturaEstado;
    }

    /**
     * Declares an association between this object and a Factura object.
     *
     * @param                  Factura $v
     * @return FacturaVitacora The current object (for fluent API support)
     * @throws PropelException
     */
    public function setFactura(Factura $v = null)
    {
        if ($v === null) {
            $this->setFacturaId(NULL);
        } else {
            $this->setFacturaId($v->getFacturaId());
        }

        $this->aFactura = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Factura object, it will not be re-added.
        if ($v !== null) {
            $v->addFacturaVitacora($this);
        }


        return $this;
    }


    /**
     * Get the associated Factura object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Factura The associated Factura object.
     * @throws PropelException
     */
    public function getFactura(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aFactura === null && ($this->factura_id !== null) && $doQuery) {
            $this->aFactura = FacturaQuery::create()->findPk($this->factura_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aFactura->addFacturaVitacoras($this);
             */
        }

        return $this->aFactura;
    }

    /**
     * Declares an association between this object and a FacturaVitacoraEstado object.
     *
     * @param                  FacturaVitacoraEstado $v
     * @return FacturaVitacora The current object (for fluent API support)
     * @throws PropelException
     */
    public function setFacturaVitacoraEstado(FacturaVitacoraEstado $v = null)
    {
        if ($v === null) {
            $this->setFacturavitacoraestadoId(NULL);
        } else {
            $this->setFacturavitacoraestadoId($v->getFacturavitacoraestadoId());
        }

        $this->aFacturaVitacoraEstado = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the FacturaVitacoraEstado object, it will not be re-added.
        if ($v !== null) {
            $v->addFacturaVitacora($this);
        }


        return $this;
    }


    /**
     * Get the associated FacturaVitacoraEstado object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return FacturaVitacoraEstado The associated FacturaVitacoraEstado object.
     * @throws PropelException
     */
    public function getFacturaVitacoraEstado(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aFacturaVitacoraEstado === null && ($this->facturavitacoraestado_id !== null) && $doQuery) {
            $this->aFacturaVitacoraEstado = FacturaVitacoraEstadoQuery::create()->findPk($this->facturavitacoraestado_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aFacturaVitacoraEstado->addFacturaVitacoras($this);
             */
        }

        return $this->aFacturaVitacoraEstado;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('FactUsuarioDestino' == $relationName) {
            $this->initFactUsuarioDestinos();
        }
        if ('FactUsuarioRequisitor' == $relationName) {
            $this->initFactUsuarioRequisitors();
        }
        if ('FacturaVitacoraUsuario' == $relationName) {
            $this->initFacturaVitacoraUsuarios();
        }
    }

    /**
     * Clears out the collFactUsuarioDestinos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaVitacora The current object (for fluent API support)
     * @see        addFactUsuarioDestinos()
     */
    public function clearFactUsuarioDestinos()
    {
        $this->collFactUsuarioDestinos = null; // important to set this to null since that means it is uninitialized
        $this->collFactUsuarioDestinosPartial = null;

        return $this;
    }

    /**
     * reset is the collFactUsuarioDestinos collection loaded partially
     *
     * @return void
     */
    public function resetPartialFactUsuarioDestinos($v = true)
    {
        $this->collFactUsuarioDestinosPartial = $v;
    }

    /**
     * Initializes the collFactUsuarioDestinos collection.
     *
     * By default this just sets the collFactUsuarioDestinos collection to an empty array (like clearcollFactUsuarioDestinos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFactUsuarioDestinos($overrideExisting = true)
    {
        if (null !== $this->collFactUsuarioDestinos && !$overrideExisting) {
            return;
        }
        $this->collFactUsuarioDestinos = new PropelObjectCollection();
        $this->collFactUsuarioDestinos->setModel('FactUsuarioDestino');
    }

    /**
     * Gets an array of FactUsuarioDestino objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaVitacora is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FactUsuarioDestino[] List of FactUsuarioDestino objects
     * @throws PropelException
     */
    public function getFactUsuarioDestinos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFactUsuarioDestinosPartial && !$this->isNew();
        if (null === $this->collFactUsuarioDestinos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFactUsuarioDestinos) {
                // return empty collection
                $this->initFactUsuarioDestinos();
            } else {
                $collFactUsuarioDestinos = FactUsuarioDestinoQuery::create(null, $criteria)
                    ->filterByFacturaVitacora($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFactUsuarioDestinosPartial && count($collFactUsuarioDestinos)) {
                      $this->initFactUsuarioDestinos(false);

                      foreach ($collFactUsuarioDestinos as $obj) {
                        if (false == $this->collFactUsuarioDestinos->contains($obj)) {
                          $this->collFactUsuarioDestinos->append($obj);
                        }
                      }

                      $this->collFactUsuarioDestinosPartial = true;
                    }

                    $collFactUsuarioDestinos->getInternalIterator()->rewind();

                    return $collFactUsuarioDestinos;
                }

                if ($partial && $this->collFactUsuarioDestinos) {
                    foreach ($this->collFactUsuarioDestinos as $obj) {
                        if ($obj->isNew()) {
                            $collFactUsuarioDestinos[] = $obj;
                        }
                    }
                }

                $this->collFactUsuarioDestinos = $collFactUsuarioDestinos;
                $this->collFactUsuarioDestinosPartial = false;
            }
        }

        return $this->collFactUsuarioDestinos;
    }

    /**
     * Sets a collection of FactUsuarioDestino objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $factUsuarioDestinos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFactUsuarioDestinos(PropelCollection $factUsuarioDestinos, PropelPDO $con = null)
    {
        $factUsuarioDestinosToDelete = $this->getFactUsuarioDestinos(new Criteria(), $con)->diff($factUsuarioDestinos);


        $this->factUsuarioDestinosScheduledForDeletion = $factUsuarioDestinosToDelete;

        foreach ($factUsuarioDestinosToDelete as $factUsuarioDestinoRemoved) {
            $factUsuarioDestinoRemoved->setFacturaVitacora(null);
        }

        $this->collFactUsuarioDestinos = null;
        foreach ($factUsuarioDestinos as $factUsuarioDestino) {
            $this->addFactUsuarioDestino($factUsuarioDestino);
        }

        $this->collFactUsuarioDestinos = $factUsuarioDestinos;
        $this->collFactUsuarioDestinosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FactUsuarioDestino objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FactUsuarioDestino objects.
     * @throws PropelException
     */
    public function countFactUsuarioDestinos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFactUsuarioDestinosPartial && !$this->isNew();
        if (null === $this->collFactUsuarioDestinos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFactUsuarioDestinos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFactUsuarioDestinos());
            }
            $query = FactUsuarioDestinoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaVitacora($this)
                ->count($con);
        }

        return count($this->collFactUsuarioDestinos);
    }

    /**
     * Method called to associate a FactUsuarioDestino object to this object
     * through the FactUsuarioDestino foreign key attribute.
     *
     * @param    FactUsuarioDestino $l FactUsuarioDestino
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function addFactUsuarioDestino(FactUsuarioDestino $l)
    {
        if ($this->collFactUsuarioDestinos === null) {
            $this->initFactUsuarioDestinos();
            $this->collFactUsuarioDestinosPartial = true;
        }

        if (!in_array($l, $this->collFactUsuarioDestinos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactUsuarioDestino($l);

            if ($this->factUsuarioDestinosScheduledForDeletion and $this->factUsuarioDestinosScheduledForDeletion->contains($l)) {
                $this->factUsuarioDestinosScheduledForDeletion->remove($this->factUsuarioDestinosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FactUsuarioDestino $factUsuarioDestino The factUsuarioDestino object to add.
     */
    protected function doAddFactUsuarioDestino($factUsuarioDestino)
    {
        $this->collFactUsuarioDestinos[]= $factUsuarioDestino;
        $factUsuarioDestino->setFacturaVitacora($this);
    }

    /**
     * @param	FactUsuarioDestino $factUsuarioDestino The factUsuarioDestino object to remove.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function removeFactUsuarioDestino($factUsuarioDestino)
    {
        if ($this->getFactUsuarioDestinos()->contains($factUsuarioDestino)) {
            $this->collFactUsuarioDestinos->remove($this->collFactUsuarioDestinos->search($factUsuarioDestino));
            if (null === $this->factUsuarioDestinosScheduledForDeletion) {
                $this->factUsuarioDestinosScheduledForDeletion = clone $this->collFactUsuarioDestinos;
                $this->factUsuarioDestinosScheduledForDeletion->clear();
            }
            $this->factUsuarioDestinosScheduledForDeletion[]= $factUsuarioDestino;
            $factUsuarioDestino->setFacturaVitacora(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaVitacora is new, it will return
     * an empty collection; or if this FacturaVitacora has previously
     * been saved, it will retrieve related FactUsuarioDestinos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaVitacora.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactUsuarioDestino[] List of FactUsuarioDestino objects
     */
    public function getFactUsuarioDestinosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactUsuarioDestinoQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getFactUsuarioDestinos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaVitacora is new, it will return
     * an empty collection; or if this FacturaVitacora has previously
     * been saved, it will retrieve related FactUsuarioDestinos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaVitacora.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactUsuarioDestino[] List of FactUsuarioDestino objects
     */
    public function getFactUsuarioDestinosJoinFacturaVitacoraRolUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactUsuarioDestinoQuery::create(null, $criteria);
        $query->joinWith('FacturaVitacoraRolUsuario', $join_behavior);

        return $this->getFactUsuarioDestinos($query, $con);
    }

    /**
     * Clears out the collFactUsuarioRequisitors collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaVitacora The current object (for fluent API support)
     * @see        addFactUsuarioRequisitors()
     */
    public function clearFactUsuarioRequisitors()
    {
        $this->collFactUsuarioRequisitors = null; // important to set this to null since that means it is uninitialized
        $this->collFactUsuarioRequisitorsPartial = null;

        return $this;
    }

    /**
     * reset is the collFactUsuarioRequisitors collection loaded partially
     *
     * @return void
     */
    public function resetPartialFactUsuarioRequisitors($v = true)
    {
        $this->collFactUsuarioRequisitorsPartial = $v;
    }

    /**
     * Initializes the collFactUsuarioRequisitors collection.
     *
     * By default this just sets the collFactUsuarioRequisitors collection to an empty array (like clearcollFactUsuarioRequisitors());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFactUsuarioRequisitors($overrideExisting = true)
    {
        if (null !== $this->collFactUsuarioRequisitors && !$overrideExisting) {
            return;
        }
        $this->collFactUsuarioRequisitors = new PropelObjectCollection();
        $this->collFactUsuarioRequisitors->setModel('FactUsuarioRequisitor');
    }

    /**
     * Gets an array of FactUsuarioRequisitor objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaVitacora is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FactUsuarioRequisitor[] List of FactUsuarioRequisitor objects
     * @throws PropelException
     */
    public function getFactUsuarioRequisitors($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFactUsuarioRequisitorsPartial && !$this->isNew();
        if (null === $this->collFactUsuarioRequisitors || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFactUsuarioRequisitors) {
                // return empty collection
                $this->initFactUsuarioRequisitors();
            } else {
                $collFactUsuarioRequisitors = FactUsuarioRequisitorQuery::create(null, $criteria)
                    ->filterByFacturaVitacora($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFactUsuarioRequisitorsPartial && count($collFactUsuarioRequisitors)) {
                      $this->initFactUsuarioRequisitors(false);

                      foreach ($collFactUsuarioRequisitors as $obj) {
                        if (false == $this->collFactUsuarioRequisitors->contains($obj)) {
                          $this->collFactUsuarioRequisitors->append($obj);
                        }
                      }

                      $this->collFactUsuarioRequisitorsPartial = true;
                    }

                    $collFactUsuarioRequisitors->getInternalIterator()->rewind();

                    return $collFactUsuarioRequisitors;
                }

                if ($partial && $this->collFactUsuarioRequisitors) {
                    foreach ($this->collFactUsuarioRequisitors as $obj) {
                        if ($obj->isNew()) {
                            $collFactUsuarioRequisitors[] = $obj;
                        }
                    }
                }

                $this->collFactUsuarioRequisitors = $collFactUsuarioRequisitors;
                $this->collFactUsuarioRequisitorsPartial = false;
            }
        }

        return $this->collFactUsuarioRequisitors;
    }

    /**
     * Sets a collection of FactUsuarioRequisitor objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $factUsuarioRequisitors A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFactUsuarioRequisitors(PropelCollection $factUsuarioRequisitors, PropelPDO $con = null)
    {
        $factUsuarioRequisitorsToDelete = $this->getFactUsuarioRequisitors(new Criteria(), $con)->diff($factUsuarioRequisitors);


        $this->factUsuarioRequisitorsScheduledForDeletion = $factUsuarioRequisitorsToDelete;

        foreach ($factUsuarioRequisitorsToDelete as $factUsuarioRequisitorRemoved) {
            $factUsuarioRequisitorRemoved->setFacturaVitacora(null);
        }

        $this->collFactUsuarioRequisitors = null;
        foreach ($factUsuarioRequisitors as $factUsuarioRequisitor) {
            $this->addFactUsuarioRequisitor($factUsuarioRequisitor);
        }

        $this->collFactUsuarioRequisitors = $factUsuarioRequisitors;
        $this->collFactUsuarioRequisitorsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FactUsuarioRequisitor objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FactUsuarioRequisitor objects.
     * @throws PropelException
     */
    public function countFactUsuarioRequisitors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFactUsuarioRequisitorsPartial && !$this->isNew();
        if (null === $this->collFactUsuarioRequisitors || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFactUsuarioRequisitors) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFactUsuarioRequisitors());
            }
            $query = FactUsuarioRequisitorQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaVitacora($this)
                ->count($con);
        }

        return count($this->collFactUsuarioRequisitors);
    }

    /**
     * Method called to associate a FactUsuarioRequisitor object to this object
     * through the FactUsuarioRequisitor foreign key attribute.
     *
     * @param    FactUsuarioRequisitor $l FactUsuarioRequisitor
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function addFactUsuarioRequisitor(FactUsuarioRequisitor $l)
    {
        if ($this->collFactUsuarioRequisitors === null) {
            $this->initFactUsuarioRequisitors();
            $this->collFactUsuarioRequisitorsPartial = true;
        }

        if (!in_array($l, $this->collFactUsuarioRequisitors->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactUsuarioRequisitor($l);

            if ($this->factUsuarioRequisitorsScheduledForDeletion and $this->factUsuarioRequisitorsScheduledForDeletion->contains($l)) {
                $this->factUsuarioRequisitorsScheduledForDeletion->remove($this->factUsuarioRequisitorsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FactUsuarioRequisitor $factUsuarioRequisitor The factUsuarioRequisitor object to add.
     */
    protected function doAddFactUsuarioRequisitor($factUsuarioRequisitor)
    {
        $this->collFactUsuarioRequisitors[]= $factUsuarioRequisitor;
        $factUsuarioRequisitor->setFacturaVitacora($this);
    }

    /**
     * @param	FactUsuarioRequisitor $factUsuarioRequisitor The factUsuarioRequisitor object to remove.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function removeFactUsuarioRequisitor($factUsuarioRequisitor)
    {
        if ($this->getFactUsuarioRequisitors()->contains($factUsuarioRequisitor)) {
            $this->collFactUsuarioRequisitors->remove($this->collFactUsuarioRequisitors->search($factUsuarioRequisitor));
            if (null === $this->factUsuarioRequisitorsScheduledForDeletion) {
                $this->factUsuarioRequisitorsScheduledForDeletion = clone $this->collFactUsuarioRequisitors;
                $this->factUsuarioRequisitorsScheduledForDeletion->clear();
            }
            $this->factUsuarioRequisitorsScheduledForDeletion[]= $factUsuarioRequisitor;
            $factUsuarioRequisitor->setFacturaVitacora(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaVitacora is new, it will return
     * an empty collection; or if this FacturaVitacora has previously
     * been saved, it will retrieve related FactUsuarioRequisitors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaVitacora.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactUsuarioRequisitor[] List of FactUsuarioRequisitor objects
     */
    public function getFactUsuarioRequisitorsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactUsuarioRequisitorQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getFactUsuarioRequisitors($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaVitacora is new, it will return
     * an empty collection; or if this FacturaVitacora has previously
     * been saved, it will retrieve related FactUsuarioRequisitors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaVitacora.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactUsuarioRequisitor[] List of FactUsuarioRequisitor objects
     */
    public function getFactUsuarioRequisitorsJoinFacturaVitacoraRolUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactUsuarioRequisitorQuery::create(null, $criteria);
        $query->joinWith('FacturaVitacoraRolUsuario', $join_behavior);

        return $this->getFactUsuarioRequisitors($query, $con);
    }

    /**
     * Clears out the collFacturaVitacoraUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaVitacora The current object (for fluent API support)
     * @see        addFacturaVitacoraUsuarios()
     */
    public function clearFacturaVitacoraUsuarios()
    {
        $this->collFacturaVitacoraUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collFacturaVitacoraUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collFacturaVitacoraUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialFacturaVitacoraUsuarios($v = true)
    {
        $this->collFacturaVitacoraUsuariosPartial = $v;
    }

    /**
     * Initializes the collFacturaVitacoraUsuarios collection.
     *
     * By default this just sets the collFacturaVitacoraUsuarios collection to an empty array (like clearcollFacturaVitacoraUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFacturaVitacoraUsuarios($overrideExisting = true)
    {
        if (null !== $this->collFacturaVitacoraUsuarios && !$overrideExisting) {
            return;
        }
        $this->collFacturaVitacoraUsuarios = new PropelObjectCollection();
        $this->collFacturaVitacoraUsuarios->setModel('FacturaVitacoraUsuario');
    }

    /**
     * Gets an array of FacturaVitacoraUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaVitacora is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FacturaVitacoraUsuario[] List of FacturaVitacoraUsuario objects
     * @throws PropelException
     */
    public function getFacturaVitacoraUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFacturaVitacoraUsuariosPartial && !$this->isNew();
        if (null === $this->collFacturaVitacoraUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFacturaVitacoraUsuarios) {
                // return empty collection
                $this->initFacturaVitacoraUsuarios();
            } else {
                $collFacturaVitacoraUsuarios = FacturaVitacoraUsuarioQuery::create(null, $criteria)
                    ->filterByFacturaVitacora($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFacturaVitacoraUsuariosPartial && count($collFacturaVitacoraUsuarios)) {
                      $this->initFacturaVitacoraUsuarios(false);

                      foreach ($collFacturaVitacoraUsuarios as $obj) {
                        if (false == $this->collFacturaVitacoraUsuarios->contains($obj)) {
                          $this->collFacturaVitacoraUsuarios->append($obj);
                        }
                      }

                      $this->collFacturaVitacoraUsuariosPartial = true;
                    }

                    $collFacturaVitacoraUsuarios->getInternalIterator()->rewind();

                    return $collFacturaVitacoraUsuarios;
                }

                if ($partial && $this->collFacturaVitacoraUsuarios) {
                    foreach ($this->collFacturaVitacoraUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collFacturaVitacoraUsuarios[] = $obj;
                        }
                    }
                }

                $this->collFacturaVitacoraUsuarios = $collFacturaVitacoraUsuarios;
                $this->collFacturaVitacoraUsuariosPartial = false;
            }
        }

        return $this->collFacturaVitacoraUsuarios;
    }

    /**
     * Sets a collection of FacturaVitacoraUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $facturaVitacoraUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function setFacturaVitacoraUsuarios(PropelCollection $facturaVitacoraUsuarios, PropelPDO $con = null)
    {
        $facturaVitacoraUsuariosToDelete = $this->getFacturaVitacoraUsuarios(new Criteria(), $con)->diff($facturaVitacoraUsuarios);


        $this->facturaVitacoraUsuariosScheduledForDeletion = $facturaVitacoraUsuariosToDelete;

        foreach ($facturaVitacoraUsuariosToDelete as $facturaVitacoraUsuarioRemoved) {
            $facturaVitacoraUsuarioRemoved->setFacturaVitacora(null);
        }

        $this->collFacturaVitacoraUsuarios = null;
        foreach ($facturaVitacoraUsuarios as $facturaVitacoraUsuario) {
            $this->addFacturaVitacoraUsuario($facturaVitacoraUsuario);
        }

        $this->collFacturaVitacoraUsuarios = $facturaVitacoraUsuarios;
        $this->collFacturaVitacoraUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FacturaVitacoraUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FacturaVitacoraUsuario objects.
     * @throws PropelException
     */
    public function countFacturaVitacoraUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFacturaVitacoraUsuariosPartial && !$this->isNew();
        if (null === $this->collFacturaVitacoraUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFacturaVitacoraUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFacturaVitacoraUsuarios());
            }
            $query = FacturaVitacoraUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaVitacora($this)
                ->count($con);
        }

        return count($this->collFacturaVitacoraUsuarios);
    }

    /**
     * Method called to associate a FacturaVitacoraUsuario object to this object
     * through the FacturaVitacoraUsuario foreign key attribute.
     *
     * @param    FacturaVitacoraUsuario $l FacturaVitacoraUsuario
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function addFacturaVitacoraUsuario(FacturaVitacoraUsuario $l)
    {
        if ($this->collFacturaVitacoraUsuarios === null) {
            $this->initFacturaVitacoraUsuarios();
            $this->collFacturaVitacoraUsuariosPartial = true;
        }

        if (!in_array($l, $this->collFacturaVitacoraUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFacturaVitacoraUsuario($l);

            if ($this->facturaVitacoraUsuariosScheduledForDeletion and $this->facturaVitacoraUsuariosScheduledForDeletion->contains($l)) {
                $this->facturaVitacoraUsuariosScheduledForDeletion->remove($this->facturaVitacoraUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FacturaVitacoraUsuario $facturaVitacoraUsuario The facturaVitacoraUsuario object to add.
     */
    protected function doAddFacturaVitacoraUsuario($facturaVitacoraUsuario)
    {
        $this->collFacturaVitacoraUsuarios[]= $facturaVitacoraUsuario;
        $facturaVitacoraUsuario->setFacturaVitacora($this);
    }

    /**
     * @param	FacturaVitacoraUsuario $facturaVitacoraUsuario The facturaVitacoraUsuario object to remove.
     * @return FacturaVitacora The current object (for fluent API support)
     */
    public function removeFacturaVitacoraUsuario($facturaVitacoraUsuario)
    {
        if ($this->getFacturaVitacoraUsuarios()->contains($facturaVitacoraUsuario)) {
            $this->collFacturaVitacoraUsuarios->remove($this->collFacturaVitacoraUsuarios->search($facturaVitacoraUsuario));
            if (null === $this->facturaVitacoraUsuariosScheduledForDeletion) {
                $this->facturaVitacoraUsuariosScheduledForDeletion = clone $this->collFacturaVitacoraUsuarios;
                $this->facturaVitacoraUsuariosScheduledForDeletion->clear();
            }
            $this->facturaVitacoraUsuariosScheduledForDeletion[]= clone $facturaVitacoraUsuario;
            $facturaVitacoraUsuario->setFacturaVitacora(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaVitacora is new, it will return
     * an empty collection; or if this FacturaVitacora has previously
     * been saved, it will retrieve related FacturaVitacoraUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaVitacora.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaVitacoraUsuario[] List of FacturaVitacoraUsuario objects
     */
    public function getFacturaVitacoraUsuariosJoinFacturaVitacoraRolUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaVitacoraUsuarioQuery::create(null, $criteria);
        $query->joinWith('FacturaVitacoraRolUsuario', $join_behavior);

        return $this->getFacturaVitacoraUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaVitacora is new, it will return
     * an empty collection; or if this FacturaVitacora has previously
     * been saved, it will retrieve related FacturaVitacoraUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaVitacora.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaVitacoraUsuario[] List of FacturaVitacoraUsuario objects
     */
    public function getFacturaVitacoraUsuariosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaVitacoraUsuarioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getFacturaVitacoraUsuarios($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->facturavitacora_id = null;
        $this->facturaproceso_id = null;
        $this->facturaestado_id = null;
        $this->factura_id = null;
        $this->fecha_i = null;
        $this->fecha_i_date = null;
        $this->fecha_f = null;
        $this->fecha_f_date = null;
        $this->observaciones = null;
        $this->ejecutada = null;
        $this->facturavitacoraestado_id = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collFactUsuarioDestinos) {
                foreach ($this->collFactUsuarioDestinos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFactUsuarioRequisitors) {
                foreach ($this->collFactUsuarioRequisitors as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFacturaVitacoraUsuarios) {
                foreach ($this->collFacturaVitacoraUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aFacturaProceso instanceof Persistent) {
              $this->aFacturaProceso->clearAllReferences($deep);
            }
            if ($this->aFacturaEstado instanceof Persistent) {
              $this->aFacturaEstado->clearAllReferences($deep);
            }
            if ($this->aFactura instanceof Persistent) {
              $this->aFactura->clearAllReferences($deep);
            }
            if ($this->aFacturaVitacoraEstado instanceof Persistent) {
              $this->aFacturaVitacoraEstado->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collFactUsuarioDestinos instanceof PropelCollection) {
            $this->collFactUsuarioDestinos->clearIterator();
        }
        $this->collFactUsuarioDestinos = null;
        if ($this->collFactUsuarioRequisitors instanceof PropelCollection) {
            $this->collFactUsuarioRequisitors->clearIterator();
        }
        $this->collFactUsuarioRequisitors = null;
        if ($this->collFacturaVitacoraUsuarios instanceof PropelCollection) {
            $this->collFacturaVitacoraUsuarios->clearIterator();
        }
        $this->collFacturaVitacoraUsuarios = null;
        $this->aFacturaProceso = null;
        $this->aFacturaEstado = null;
        $this->aFactura = null;
        $this->aFacturaVitacoraEstado = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(FacturaVitacoraPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseFacturaVitacora:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

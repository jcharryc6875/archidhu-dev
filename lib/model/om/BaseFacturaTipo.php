<?php


/**
 * Base class that represents a row from the 'FACTURA_TIPO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:00
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseFacturaTipo extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'FacturaTipoPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        FacturaTipoPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the facturatipo_id field.
     * @var        int
     */
    protected $facturatipo_id;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * The value for the es_visible field.
     * @var        boolean
     */
    protected $es_visible;

    /**
     * The value for the codigo field.
     * @var        string
     */
    protected $codigo;

    /**
     * The value for the partial_name field.
     * @var        string
     */
    protected $partial_name;

    /**
     * The value for the checklist_html field.
     * @var        string
     */
    protected $checklist_html;

    /**
     * @var        PropelObjectCollection|FactProcesoPorTipo[] Collection to store aggregation of FactProcesoPorTipo objects.
     */
    protected $collFactProcesoPorTipos;
    protected $collFactProcesoPorTiposPartial;

    /**
     * @var        PropelObjectCollection|FactVariable[] Collection to store aggregation of FactVariable objects.
     */
    protected $collFactVariables;
    protected $collFactVariablesPartial;

    /**
     * @var        PropelObjectCollection|Factura[] Collection to store aggregation of Factura objects.
     */
    protected $collFacturas;
    protected $collFacturasPartial;

    /**
     * @var        PropelObjectCollection|FacturaDevolucion[] Collection to store aggregation of FacturaDevolucion objects.
     */
    protected $collFacturaDevolucions;
    protected $collFacturaDevolucionsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $factProcesoPorTiposScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $factVariablesScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $facturasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $facturaDevolucionsScheduledForDeletion = null;

    /**
     * Get the [facturatipo_id] column value.
     * 
     * @return int
     */
    public function getFacturatipoId()
    {

        return $this->facturatipo_id;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Get the [es_visible] column value.
     * 
     * @return boolean
     */
    public function getEsVisible()
    {

        return $this->es_visible;
    }

    /**
     * Get the [codigo] column value.
     * 
     * @return string
     */
    public function getCodigo()
    {

        return $this->codigo;
    }

    /**
     * Get the [partial_name] column value.
     * 
     * @return string
     */
    public function getPartialName()
    {

        return $this->partial_name;
    }

    /**
     * Get the [checklist_html] column value.
     * 
     * @return string
     */
    public function getChecklistHtml()
    {

        return $this->checklist_html;
    }

    /**
     * Set the value of [facturatipo_id] column.
     * 
     * @param  int $v new value
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setFacturatipoId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->facturatipo_id !== $v) {
            $this->facturatipo_id = $v;
            $this->modifiedColumns[] = FacturaTipoPeer::FACTURATIPO_ID;
        }


        return $this;
    } // setFacturatipoId()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = FacturaTipoPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Sets the value of the [es_visible] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * 
     * @param boolean|integer|string $v The new value
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setEsVisible($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->es_visible !== $v) {
            $this->es_visible = $v;
            $this->modifiedColumns[] = FacturaTipoPeer::ES_VISIBLE;
        }


        return $this;
    } // setEsVisible()

    /**
     * Set the value of [codigo] column.
     * 
     * @param  string $v new value
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setCodigo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->codigo !== $v) {
            $this->codigo = $v;
            $this->modifiedColumns[] = FacturaTipoPeer::CODIGO;
        }


        return $this;
    } // setCodigo()

    /**
     * Set the value of [partial_name] column.
     * 
     * @param  string $v new value
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setPartialName($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->partial_name !== $v) {
            $this->partial_name = $v;
            $this->modifiedColumns[] = FacturaTipoPeer::PARTIAL_NAME;
        }


        return $this;
    } // setPartialName()

    /**
     * Set the value of [checklist_html] column.
     * 
     * @param  string $v new value
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setChecklistHtml($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->checklist_html !== $v) {
            $this->checklist_html = $v;
            $this->modifiedColumns[] = FacturaTipoPeer::CHECKLIST_HTML;
        }


        return $this;
    } // setChecklistHtml()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->facturatipo_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->descripcion = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->es_visible = ($row[$startcol + 2] !== null) ? (boolean) $row[$startcol + 2] : null;
            $this->codigo = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->partial_name = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->checklist_html = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 6; // 6 = FacturaTipoPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating FacturaTipo object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaTipoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = FacturaTipoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->collFactProcesoPorTipos = null;

            $this->collFactVariables = null;

            $this->collFacturas = null;

            $this->collFacturaDevolucions = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaTipoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = FacturaTipoQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseFacturaTipo:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseFacturaTipo:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaTipoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseFacturaTipo:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseFacturaTipo:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                FacturaTipoPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->factProcesoPorTiposScheduledForDeletion !== null) {
                if (!$this->factProcesoPorTiposScheduledForDeletion->isEmpty()) {
                    foreach ($this->factProcesoPorTiposScheduledForDeletion as $factProcesoPorTipo) {
                        // need to save related object because we set the relation to null
                        $factProcesoPorTipo->save($con);
                    }
                    $this->factProcesoPorTiposScheduledForDeletion = null;
                }
            }

            if ($this->collFactProcesoPorTipos !== null) {
                foreach ($this->collFactProcesoPorTipos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->factVariablesScheduledForDeletion !== null) {
                if (!$this->factVariablesScheduledForDeletion->isEmpty()) {
                    FactVariableQuery::create()
                        ->filterByPrimaryKeys($this->factVariablesScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->factVariablesScheduledForDeletion = null;
                }
            }

            if ($this->collFactVariables !== null) {
                foreach ($this->collFactVariables as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->facturasScheduledForDeletion !== null) {
                if (!$this->facturasScheduledForDeletion->isEmpty()) {
                    foreach ($this->facturasScheduledForDeletion as $factura) {
                        // need to save related object because we set the relation to null
                        $factura->save($con);
                    }
                    $this->facturasScheduledForDeletion = null;
                }
            }

            if ($this->collFacturas !== null) {
                foreach ($this->collFacturas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->facturaDevolucionsScheduledForDeletion !== null) {
                if (!$this->facturaDevolucionsScheduledForDeletion->isEmpty()) {
                    foreach ($this->facturaDevolucionsScheduledForDeletion as $facturaDevolucion) {
                        // need to save related object because we set the relation to null
                        $facturaDevolucion->save($con);
                    }
                    $this->facturaDevolucionsScheduledForDeletion = null;
                }
            }

            if ($this->collFacturaDevolucions !== null) {
                foreach ($this->collFacturaDevolucions as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = FacturaTipoPeer::FACTURATIPO_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(FacturaTipoPeer::FACTURATIPO_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . FacturaTipoPeer::FACTURATIPO_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(FacturaTipoPeer::FACTURATIPO_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setFacturatipoId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            if (($retval = FacturaTipoPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collFactProcesoPorTipos !== null) {
                    foreach ($this->collFactProcesoPorTipos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFactVariables !== null) {
                    foreach ($this->collFactVariables as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFacturas !== null) {
                    foreach ($this->collFacturas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFacturaDevolucions !== null) {
                    foreach ($this->collFacturaDevolucions as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = FacturaTipoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getFacturatipoId();
                break;
            case 1:
                return $this->getDescripcion();
                break;
            case 2:
                return $this->getEsVisible();
                break;
            case 3:
                return $this->getCodigo();
                break;
            case 4:
                return $this->getPartialName();
                break;
            case 5:
                return $this->getChecklistHtml();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['FacturaTipo'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['FacturaTipo'][$this->getPrimaryKey()] = true;
        $keys = FacturaTipoPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getFacturatipoId(),
            $keys[1] => $this->getDescripcion(),
            $keys[2] => $this->getEsVisible(),
            $keys[3] => $this->getCodigo(),
            $keys[4] => $this->getPartialName(),
            $keys[5] => $this->getChecklistHtml(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->collFactProcesoPorTipos) {
                $result['FactProcesoPorTipos'] = $this->collFactProcesoPorTipos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFactVariables) {
                $result['FactVariables'] = $this->collFactVariables->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFacturas) {
                $result['Facturas'] = $this->collFacturas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFacturaDevolucions) {
                $result['FacturaDevolucions'] = $this->collFacturaDevolucions->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = FacturaTipoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setFacturatipoId($value);
                break;
            case 1:
                $this->setDescripcion($value);
                break;
            case 2:
                $this->setEsVisible($value);
                break;
            case 3:
                $this->setCodigo($value);
                break;
            case 4:
                $this->setPartialName($value);
                break;
            case 5:
                $this->setChecklistHtml($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = FacturaTipoPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setFacturatipoId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setDescripcion($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setEsVisible($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setCodigo($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setPartialName($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setChecklistHtml($arr[$keys[5]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(FacturaTipoPeer::DATABASE_NAME);

        if ($this->isColumnModified(FacturaTipoPeer::FACTURATIPO_ID)) $criteria->add(FacturaTipoPeer::FACTURATIPO_ID, $this->facturatipo_id);
        if ($this->isColumnModified(FacturaTipoPeer::DESCRIPCION)) $criteria->add(FacturaTipoPeer::DESCRIPCION, $this->descripcion);
        if ($this->isColumnModified(FacturaTipoPeer::ES_VISIBLE)) $criteria->add(FacturaTipoPeer::ES_VISIBLE, $this->es_visible);
        if ($this->isColumnModified(FacturaTipoPeer::CODIGO)) $criteria->add(FacturaTipoPeer::CODIGO, $this->codigo);
        if ($this->isColumnModified(FacturaTipoPeer::PARTIAL_NAME)) $criteria->add(FacturaTipoPeer::PARTIAL_NAME, $this->partial_name);
        if ($this->isColumnModified(FacturaTipoPeer::CHECKLIST_HTML)) $criteria->add(FacturaTipoPeer::CHECKLIST_HTML, $this->checklist_html);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(FacturaTipoPeer::DATABASE_NAME);
        $criteria->add(FacturaTipoPeer::FACTURATIPO_ID, $this->facturatipo_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getFacturatipoId();
    }

    /**
     * Generic method to set the primary key (facturatipo_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setFacturatipoId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getFacturatipoId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of FacturaTipo (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setDescripcion($this->getDescripcion());
        $copyObj->setEsVisible($this->getEsVisible());
        $copyObj->setCodigo($this->getCodigo());
        $copyObj->setPartialName($this->getPartialName());
        $copyObj->setChecklistHtml($this->getChecklistHtml());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getFactProcesoPorTipos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactProcesoPorTipo($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFactVariables() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactVariable($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFacturas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactura($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFacturaDevolucions() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFacturaDevolucion($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setFacturatipoId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return FacturaTipo Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return FacturaTipoPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new FacturaTipoPeer();
        }

        return self::$peer;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('FactProcesoPorTipo' == $relationName) {
            $this->initFactProcesoPorTipos();
        }
        if ('FactVariable' == $relationName) {
            $this->initFactVariables();
        }
        if ('Factura' == $relationName) {
            $this->initFacturas();
        }
        if ('FacturaDevolucion' == $relationName) {
            $this->initFacturaDevolucions();
        }
    }

    /**
     * Clears out the collFactProcesoPorTipos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaTipo The current object (for fluent API support)
     * @see        addFactProcesoPorTipos()
     */
    public function clearFactProcesoPorTipos()
    {
        $this->collFactProcesoPorTipos = null; // important to set this to null since that means it is uninitialized
        $this->collFactProcesoPorTiposPartial = null;

        return $this;
    }

    /**
     * reset is the collFactProcesoPorTipos collection loaded partially
     *
     * @return void
     */
    public function resetPartialFactProcesoPorTipos($v = true)
    {
        $this->collFactProcesoPorTiposPartial = $v;
    }

    /**
     * Initializes the collFactProcesoPorTipos collection.
     *
     * By default this just sets the collFactProcesoPorTipos collection to an empty array (like clearcollFactProcesoPorTipos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFactProcesoPorTipos($overrideExisting = true)
    {
        if (null !== $this->collFactProcesoPorTipos && !$overrideExisting) {
            return;
        }
        $this->collFactProcesoPorTipos = new PropelObjectCollection();
        $this->collFactProcesoPorTipos->setModel('FactProcesoPorTipo');
    }

    /**
     * Gets an array of FactProcesoPorTipo objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaTipo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FactProcesoPorTipo[] List of FactProcesoPorTipo objects
     * @throws PropelException
     */
    public function getFactProcesoPorTipos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFactProcesoPorTiposPartial && !$this->isNew();
        if (null === $this->collFactProcesoPorTipos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFactProcesoPorTipos) {
                // return empty collection
                $this->initFactProcesoPorTipos();
            } else {
                $collFactProcesoPorTipos = FactProcesoPorTipoQuery::create(null, $criteria)
                    ->filterByFacturaTipo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFactProcesoPorTiposPartial && count($collFactProcesoPorTipos)) {
                      $this->initFactProcesoPorTipos(false);

                      foreach ($collFactProcesoPorTipos as $obj) {
                        if (false == $this->collFactProcesoPorTipos->contains($obj)) {
                          $this->collFactProcesoPorTipos->append($obj);
                        }
                      }

                      $this->collFactProcesoPorTiposPartial = true;
                    }

                    $collFactProcesoPorTipos->getInternalIterator()->rewind();

                    return $collFactProcesoPorTipos;
                }

                if ($partial && $this->collFactProcesoPorTipos) {
                    foreach ($this->collFactProcesoPorTipos as $obj) {
                        if ($obj->isNew()) {
                            $collFactProcesoPorTipos[] = $obj;
                        }
                    }
                }

                $this->collFactProcesoPorTipos = $collFactProcesoPorTipos;
                $this->collFactProcesoPorTiposPartial = false;
            }
        }

        return $this->collFactProcesoPorTipos;
    }

    /**
     * Sets a collection of FactProcesoPorTipo objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $factProcesoPorTipos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setFactProcesoPorTipos(PropelCollection $factProcesoPorTipos, PropelPDO $con = null)
    {
        $factProcesoPorTiposToDelete = $this->getFactProcesoPorTipos(new Criteria(), $con)->diff($factProcesoPorTipos);


        $this->factProcesoPorTiposScheduledForDeletion = $factProcesoPorTiposToDelete;

        foreach ($factProcesoPorTiposToDelete as $factProcesoPorTipoRemoved) {
            $factProcesoPorTipoRemoved->setFacturaTipo(null);
        }

        $this->collFactProcesoPorTipos = null;
        foreach ($factProcesoPorTipos as $factProcesoPorTipo) {
            $this->addFactProcesoPorTipo($factProcesoPorTipo);
        }

        $this->collFactProcesoPorTipos = $factProcesoPorTipos;
        $this->collFactProcesoPorTiposPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FactProcesoPorTipo objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FactProcesoPorTipo objects.
     * @throws PropelException
     */
    public function countFactProcesoPorTipos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFactProcesoPorTiposPartial && !$this->isNew();
        if (null === $this->collFactProcesoPorTipos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFactProcesoPorTipos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFactProcesoPorTipos());
            }
            $query = FactProcesoPorTipoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaTipo($this)
                ->count($con);
        }

        return count($this->collFactProcesoPorTipos);
    }

    /**
     * Method called to associate a FactProcesoPorTipo object to this object
     * through the FactProcesoPorTipo foreign key attribute.
     *
     * @param    FactProcesoPorTipo $l FactProcesoPorTipo
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function addFactProcesoPorTipo(FactProcesoPorTipo $l)
    {
        if ($this->collFactProcesoPorTipos === null) {
            $this->initFactProcesoPorTipos();
            $this->collFactProcesoPorTiposPartial = true;
        }

        if (!in_array($l, $this->collFactProcesoPorTipos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactProcesoPorTipo($l);

            if ($this->factProcesoPorTiposScheduledForDeletion and $this->factProcesoPorTiposScheduledForDeletion->contains($l)) {
                $this->factProcesoPorTiposScheduledForDeletion->remove($this->factProcesoPorTiposScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FactProcesoPorTipo $factProcesoPorTipo The factProcesoPorTipo object to add.
     */
    protected function doAddFactProcesoPorTipo($factProcesoPorTipo)
    {
        $this->collFactProcesoPorTipos[]= $factProcesoPorTipo;
        $factProcesoPorTipo->setFacturaTipo($this);
    }

    /**
     * @param	FactProcesoPorTipo $factProcesoPorTipo The factProcesoPorTipo object to remove.
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function removeFactProcesoPorTipo($factProcesoPorTipo)
    {
        if ($this->getFactProcesoPorTipos()->contains($factProcesoPorTipo)) {
            $this->collFactProcesoPorTipos->remove($this->collFactProcesoPorTipos->search($factProcesoPorTipo));
            if (null === $this->factProcesoPorTiposScheduledForDeletion) {
                $this->factProcesoPorTiposScheduledForDeletion = clone $this->collFactProcesoPorTipos;
                $this->factProcesoPorTiposScheduledForDeletion->clear();
            }
            $this->factProcesoPorTiposScheduledForDeletion[]= $factProcesoPorTipo;
            $factProcesoPorTipo->setFacturaTipo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FactProcesoPorTipos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactProcesoPorTipo[] List of FactProcesoPorTipo objects
     */
    public function getFactProcesoPorTiposJoinFacturaProceso($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactProcesoPorTipoQuery::create(null, $criteria);
        $query->joinWith('FacturaProceso', $join_behavior);

        return $this->getFactProcesoPorTipos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FactProcesoPorTipos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactProcesoPorTipo[] List of FactProcesoPorTipo objects
     */
    public function getFactProcesoPorTiposJoinFacturaEstado($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactProcesoPorTipoQuery::create(null, $criteria);
        $query->joinWith('FacturaEstado', $join_behavior);

        return $this->getFactProcesoPorTipos($query, $con);
    }

    /**
     * Clears out the collFactVariables collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaTipo The current object (for fluent API support)
     * @see        addFactVariables()
     */
    public function clearFactVariables()
    {
        $this->collFactVariables = null; // important to set this to null since that means it is uninitialized
        $this->collFactVariablesPartial = null;

        return $this;
    }

    /**
     * reset is the collFactVariables collection loaded partially
     *
     * @return void
     */
    public function resetPartialFactVariables($v = true)
    {
        $this->collFactVariablesPartial = $v;
    }

    /**
     * Initializes the collFactVariables collection.
     *
     * By default this just sets the collFactVariables collection to an empty array (like clearcollFactVariables());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFactVariables($overrideExisting = true)
    {
        if (null !== $this->collFactVariables && !$overrideExisting) {
            return;
        }
        $this->collFactVariables = new PropelObjectCollection();
        $this->collFactVariables->setModel('FactVariable');
    }

    /**
     * Gets an array of FactVariable objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaTipo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FactVariable[] List of FactVariable objects
     * @throws PropelException
     */
    public function getFactVariables($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFactVariablesPartial && !$this->isNew();
        if (null === $this->collFactVariables || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFactVariables) {
                // return empty collection
                $this->initFactVariables();
            } else {
                $collFactVariables = FactVariableQuery::create(null, $criteria)
                    ->filterByFacturaTipo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFactVariablesPartial && count($collFactVariables)) {
                      $this->initFactVariables(false);

                      foreach ($collFactVariables as $obj) {
                        if (false == $this->collFactVariables->contains($obj)) {
                          $this->collFactVariables->append($obj);
                        }
                      }

                      $this->collFactVariablesPartial = true;
                    }

                    $collFactVariables->getInternalIterator()->rewind();

                    return $collFactVariables;
                }

                if ($partial && $this->collFactVariables) {
                    foreach ($this->collFactVariables as $obj) {
                        if ($obj->isNew()) {
                            $collFactVariables[] = $obj;
                        }
                    }
                }

                $this->collFactVariables = $collFactVariables;
                $this->collFactVariablesPartial = false;
            }
        }

        return $this->collFactVariables;
    }

    /**
     * Sets a collection of FactVariable objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $factVariables A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setFactVariables(PropelCollection $factVariables, PropelPDO $con = null)
    {
        $factVariablesToDelete = $this->getFactVariables(new Criteria(), $con)->diff($factVariables);


        $this->factVariablesScheduledForDeletion = $factVariablesToDelete;

        foreach ($factVariablesToDelete as $factVariableRemoved) {
            $factVariableRemoved->setFacturaTipo(null);
        }

        $this->collFactVariables = null;
        foreach ($factVariables as $factVariable) {
            $this->addFactVariable($factVariable);
        }

        $this->collFactVariables = $factVariables;
        $this->collFactVariablesPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FactVariable objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FactVariable objects.
     * @throws PropelException
     */
    public function countFactVariables(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFactVariablesPartial && !$this->isNew();
        if (null === $this->collFactVariables || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFactVariables) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFactVariables());
            }
            $query = FactVariableQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaTipo($this)
                ->count($con);
        }

        return count($this->collFactVariables);
    }

    /**
     * Method called to associate a FactVariable object to this object
     * through the FactVariable foreign key attribute.
     *
     * @param    FactVariable $l FactVariable
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function addFactVariable(FactVariable $l)
    {
        if ($this->collFactVariables === null) {
            $this->initFactVariables();
            $this->collFactVariablesPartial = true;
        }

        if (!in_array($l, $this->collFactVariables->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactVariable($l);

            if ($this->factVariablesScheduledForDeletion and $this->factVariablesScheduledForDeletion->contains($l)) {
                $this->factVariablesScheduledForDeletion->remove($this->factVariablesScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FactVariable $factVariable The factVariable object to add.
     */
    protected function doAddFactVariable($factVariable)
    {
        $this->collFactVariables[]= $factVariable;
        $factVariable->setFacturaTipo($this);
    }

    /**
     * @param	FactVariable $factVariable The factVariable object to remove.
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function removeFactVariable($factVariable)
    {
        if ($this->getFactVariables()->contains($factVariable)) {
            $this->collFactVariables->remove($this->collFactVariables->search($factVariable));
            if (null === $this->factVariablesScheduledForDeletion) {
                $this->factVariablesScheduledForDeletion = clone $this->collFactVariables;
                $this->factVariablesScheduledForDeletion->clear();
            }
            $this->factVariablesScheduledForDeletion[]= clone $factVariable;
            $factVariable->setFacturaTipo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FactVariables from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FactVariable[] List of FactVariable objects
     */
    public function getFactVariablesJoinFactTipoDato($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FactVariableQuery::create(null, $criteria);
        $query->joinWith('FactTipoDato', $join_behavior);

        return $this->getFactVariables($query, $con);
    }

    /**
     * Clears out the collFacturas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaTipo The current object (for fluent API support)
     * @see        addFacturas()
     */
    public function clearFacturas()
    {
        $this->collFacturas = null; // important to set this to null since that means it is uninitialized
        $this->collFacturasPartial = null;

        return $this;
    }

    /**
     * reset is the collFacturas collection loaded partially
     *
     * @return void
     */
    public function resetPartialFacturas($v = true)
    {
        $this->collFacturasPartial = $v;
    }

    /**
     * Initializes the collFacturas collection.
     *
     * By default this just sets the collFacturas collection to an empty array (like clearcollFacturas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFacturas($overrideExisting = true)
    {
        if (null !== $this->collFacturas && !$overrideExisting) {
            return;
        }
        $this->collFacturas = new PropelObjectCollection();
        $this->collFacturas->setModel('Factura');
    }

    /**
     * Gets an array of Factura objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaTipo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Factura[] List of Factura objects
     * @throws PropelException
     */
    public function getFacturas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFacturasPartial && !$this->isNew();
        if (null === $this->collFacturas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFacturas) {
                // return empty collection
                $this->initFacturas();
            } else {
                $collFacturas = FacturaQuery::create(null, $criteria)
                    ->filterByFacturaTipo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFacturasPartial && count($collFacturas)) {
                      $this->initFacturas(false);

                      foreach ($collFacturas as $obj) {
                        if (false == $this->collFacturas->contains($obj)) {
                          $this->collFacturas->append($obj);
                        }
                      }

                      $this->collFacturasPartial = true;
                    }

                    $collFacturas->getInternalIterator()->rewind();

                    return $collFacturas;
                }

                if ($partial && $this->collFacturas) {
                    foreach ($this->collFacturas as $obj) {
                        if ($obj->isNew()) {
                            $collFacturas[] = $obj;
                        }
                    }
                }

                $this->collFacturas = $collFacturas;
                $this->collFacturasPartial = false;
            }
        }

        return $this->collFacturas;
    }

    /**
     * Sets a collection of Factura objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $facturas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setFacturas(PropelCollection $facturas, PropelPDO $con = null)
    {
        $facturasToDelete = $this->getFacturas(new Criteria(), $con)->diff($facturas);


        $this->facturasScheduledForDeletion = $facturasToDelete;

        foreach ($facturasToDelete as $facturaRemoved) {
            $facturaRemoved->setFacturaTipo(null);
        }

        $this->collFacturas = null;
        foreach ($facturas as $factura) {
            $this->addFactura($factura);
        }

        $this->collFacturas = $facturas;
        $this->collFacturasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Factura objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Factura objects.
     * @throws PropelException
     */
    public function countFacturas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFacturasPartial && !$this->isNew();
        if (null === $this->collFacturas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFacturas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFacturas());
            }
            $query = FacturaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaTipo($this)
                ->count($con);
        }

        return count($this->collFacturas);
    }

    /**
     * Method called to associate a Factura object to this object
     * through the Factura foreign key attribute.
     *
     * @param    Factura $l Factura
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function addFactura(Factura $l)
    {
        if ($this->collFacturas === null) {
            $this->initFacturas();
            $this->collFacturasPartial = true;
        }

        if (!in_array($l, $this->collFacturas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactura($l);

            if ($this->facturasScheduledForDeletion and $this->facturasScheduledForDeletion->contains($l)) {
                $this->facturasScheduledForDeletion->remove($this->facturasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Factura $factura The factura object to add.
     */
    protected function doAddFactura($factura)
    {
        $this->collFacturas[]= $factura;
        $factura->setFacturaTipo($this);
    }

    /**
     * @param	Factura $factura The factura object to remove.
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function removeFactura($factura)
    {
        if ($this->getFacturas()->contains($factura)) {
            $this->collFacturas->remove($this->collFacturas->search($factura));
            if (null === $this->facturasScheduledForDeletion) {
                $this->facturasScheduledForDeletion = clone $this->collFacturas;
                $this->facturasScheduledForDeletion->clear();
            }
            $this->facturasScheduledForDeletion[]= $factura;
            $factura->setFacturaTipo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinProveedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Proveedor', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinRegional($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Regional', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinFacturaEstado($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('FacturaEstado', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinFormaRecepcion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('FormaRecepcion', $join_behavior);

        return $this->getFacturas($query, $con);
    }

    /**
     * Clears out the collFacturaDevolucions collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return FacturaTipo The current object (for fluent API support)
     * @see        addFacturaDevolucions()
     */
    public function clearFacturaDevolucions()
    {
        $this->collFacturaDevolucions = null; // important to set this to null since that means it is uninitialized
        $this->collFacturaDevolucionsPartial = null;

        return $this;
    }

    /**
     * reset is the collFacturaDevolucions collection loaded partially
     *
     * @return void
     */
    public function resetPartialFacturaDevolucions($v = true)
    {
        $this->collFacturaDevolucionsPartial = $v;
    }

    /**
     * Initializes the collFacturaDevolucions collection.
     *
     * By default this just sets the collFacturaDevolucions collection to an empty array (like clearcollFacturaDevolucions());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFacturaDevolucions($overrideExisting = true)
    {
        if (null !== $this->collFacturaDevolucions && !$overrideExisting) {
            return;
        }
        $this->collFacturaDevolucions = new PropelObjectCollection();
        $this->collFacturaDevolucions->setModel('FacturaDevolucion');
    }

    /**
     * Gets an array of FacturaDevolucion objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this FacturaTipo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FacturaDevolucion[] List of FacturaDevolucion objects
     * @throws PropelException
     */
    public function getFacturaDevolucions($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFacturaDevolucionsPartial && !$this->isNew();
        if (null === $this->collFacturaDevolucions || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFacturaDevolucions) {
                // return empty collection
                $this->initFacturaDevolucions();
            } else {
                $collFacturaDevolucions = FacturaDevolucionQuery::create(null, $criteria)
                    ->filterByFacturaTipo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFacturaDevolucionsPartial && count($collFacturaDevolucions)) {
                      $this->initFacturaDevolucions(false);

                      foreach ($collFacturaDevolucions as $obj) {
                        if (false == $this->collFacturaDevolucions->contains($obj)) {
                          $this->collFacturaDevolucions->append($obj);
                        }
                      }

                      $this->collFacturaDevolucionsPartial = true;
                    }

                    $collFacturaDevolucions->getInternalIterator()->rewind();

                    return $collFacturaDevolucions;
                }

                if ($partial && $this->collFacturaDevolucions) {
                    foreach ($this->collFacturaDevolucions as $obj) {
                        if ($obj->isNew()) {
                            $collFacturaDevolucions[] = $obj;
                        }
                    }
                }

                $this->collFacturaDevolucions = $collFacturaDevolucions;
                $this->collFacturaDevolucionsPartial = false;
            }
        }

        return $this->collFacturaDevolucions;
    }

    /**
     * Sets a collection of FacturaDevolucion objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $facturaDevolucions A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function setFacturaDevolucions(PropelCollection $facturaDevolucions, PropelPDO $con = null)
    {
        $facturaDevolucionsToDelete = $this->getFacturaDevolucions(new Criteria(), $con)->diff($facturaDevolucions);


        $this->facturaDevolucionsScheduledForDeletion = $facturaDevolucionsToDelete;

        foreach ($facturaDevolucionsToDelete as $facturaDevolucionRemoved) {
            $facturaDevolucionRemoved->setFacturaTipo(null);
        }

        $this->collFacturaDevolucions = null;
        foreach ($facturaDevolucions as $facturaDevolucion) {
            $this->addFacturaDevolucion($facturaDevolucion);
        }

        $this->collFacturaDevolucions = $facturaDevolucions;
        $this->collFacturaDevolucionsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FacturaDevolucion objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FacturaDevolucion objects.
     * @throws PropelException
     */
    public function countFacturaDevolucions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFacturaDevolucionsPartial && !$this->isNew();
        if (null === $this->collFacturaDevolucions || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFacturaDevolucions) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFacturaDevolucions());
            }
            $query = FacturaDevolucionQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByFacturaTipo($this)
                ->count($con);
        }

        return count($this->collFacturaDevolucions);
    }

    /**
     * Method called to associate a FacturaDevolucion object to this object
     * through the FacturaDevolucion foreign key attribute.
     *
     * @param    FacturaDevolucion $l FacturaDevolucion
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function addFacturaDevolucion(FacturaDevolucion $l)
    {
        if ($this->collFacturaDevolucions === null) {
            $this->initFacturaDevolucions();
            $this->collFacturaDevolucionsPartial = true;
        }

        if (!in_array($l, $this->collFacturaDevolucions->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFacturaDevolucion($l);

            if ($this->facturaDevolucionsScheduledForDeletion and $this->facturaDevolucionsScheduledForDeletion->contains($l)) {
                $this->facturaDevolucionsScheduledForDeletion->remove($this->facturaDevolucionsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FacturaDevolucion $facturaDevolucion The facturaDevolucion object to add.
     */
    protected function doAddFacturaDevolucion($facturaDevolucion)
    {
        $this->collFacturaDevolucions[]= $facturaDevolucion;
        $facturaDevolucion->setFacturaTipo($this);
    }

    /**
     * @param	FacturaDevolucion $facturaDevolucion The facturaDevolucion object to remove.
     * @return FacturaTipo The current object (for fluent API support)
     */
    public function removeFacturaDevolucion($facturaDevolucion)
    {
        if ($this->getFacturaDevolucions()->contains($facturaDevolucion)) {
            $this->collFacturaDevolucions->remove($this->collFacturaDevolucions->search($facturaDevolucion));
            if (null === $this->facturaDevolucionsScheduledForDeletion) {
                $this->facturaDevolucionsScheduledForDeletion = clone $this->collFacturaDevolucions;
                $this->facturaDevolucionsScheduledForDeletion->clear();
            }
            $this->facturaDevolucionsScheduledForDeletion[]= $facturaDevolucion;
            $facturaDevolucion->setFacturaTipo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FacturaDevolucions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaDevolucion[] List of FacturaDevolucion objects
     */
    public function getFacturaDevolucionsJoinFacturaProcesoRelatedByFaprocesoactualId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaDevolucionQuery::create(null, $criteria);
        $query->joinWith('FacturaProcesoRelatedByFaprocesoactualId', $join_behavior);

        return $this->getFacturaDevolucions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FacturaDevolucions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaDevolucion[] List of FacturaDevolucion objects
     */
    public function getFacturaDevolucionsJoinFacturaEstadoRelatedByFaestadoactualId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaDevolucionQuery::create(null, $criteria);
        $query->joinWith('FacturaEstadoRelatedByFaestadoactualId', $join_behavior);

        return $this->getFacturaDevolucions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FacturaDevolucions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaDevolucion[] List of FacturaDevolucion objects
     */
    public function getFacturaDevolucionsJoinFacturaProcesoRelatedByFaprocesodestinoId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaDevolucionQuery::create(null, $criteria);
        $query->joinWith('FacturaProcesoRelatedByFaprocesodestinoId', $join_behavior);

        return $this->getFacturaDevolucions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this FacturaTipo is new, it will return
     * an empty collection; or if this FacturaTipo has previously
     * been saved, it will retrieve related FacturaDevolucions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in FacturaTipo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaDevolucion[] List of FacturaDevolucion objects
     */
    public function getFacturaDevolucionsJoinFacturaEstadoRelatedByFaestadodestinoId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaDevolucionQuery::create(null, $criteria);
        $query->joinWith('FacturaEstadoRelatedByFaestadodestinoId', $join_behavior);

        return $this->getFacturaDevolucions($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->facturatipo_id = null;
        $this->descripcion = null;
        $this->es_visible = null;
        $this->codigo = null;
        $this->partial_name = null;
        $this->checklist_html = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collFactProcesoPorTipos) {
                foreach ($this->collFactProcesoPorTipos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFactVariables) {
                foreach ($this->collFactVariables as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFacturas) {
                foreach ($this->collFacturas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFacturaDevolucions) {
                foreach ($this->collFacturaDevolucions as $o) {
                    $o->clearAllReferences($deep);
                }
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collFactProcesoPorTipos instanceof PropelCollection) {
            $this->collFactProcesoPorTipos->clearIterator();
        }
        $this->collFactProcesoPorTipos = null;
        if ($this->collFactVariables instanceof PropelCollection) {
            $this->collFactVariables->clearIterator();
        }
        $this->collFactVariables = null;
        if ($this->collFacturas instanceof PropelCollection) {
            $this->collFacturas->clearIterator();
        }
        $this->collFacturas = null;
        if ($this->collFacturaDevolucions instanceof PropelCollection) {
            $this->collFacturaDevolucions->clearIterator();
        }
        $this->collFacturaDevolucions = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(FacturaTipoPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseFacturaTipo:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

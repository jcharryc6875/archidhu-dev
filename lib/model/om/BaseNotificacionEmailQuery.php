<?php


/**
 * Base class that represents a query for the 'NOTIFICACION_EMAIL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:54
 *
 * @method NotificacionEmailQuery orderByNotificacionemailId($order = Criteria::ASC) Order by the NOTIFICACIONEMAIL_ID column
 * @method NotificacionEmailQuery orderByDependenciaId($order = Criteria::ASC) Order by the DEPENDENCIA_ID column
 * @method NotificacionEmailQuery orderByHostSmtp($order = Criteria::ASC) Order by the HOST_SMTP column
 * @method NotificacionEmailQuery orderByEmailLogin($order = Criteria::ASC) Order by the EMAIL_LOGIN column
 * @method NotificacionEmailQuery orderByEmailPass($order = Criteria::ASC) Order by the EMAIL_PASS column
 * @method NotificacionEmailQuery orderByPuerto($order = Criteria::ASC) Order by the PUERTO column
 * @method NotificacionEmailQuery orderBySecureSmtp($order = Criteria::ASC) Order by the SECURE_SMTP column
 * @method NotificacionEmailQuery orderByAuthSmtp($order = Criteria::ASC) Order by the AUTH_SMTP column
 * @method NotificacionEmailQuery orderByCertimail($order = Criteria::ASC) Order by the CERTIMAIL column
 * @method NotificacionEmailQuery orderByEmailBody($order = Criteria::ASC) Order by the EMAIL_BODY column
 *
 * @method NotificacionEmailQuery groupByNotificacionemailId() Group by the NOTIFICACIONEMAIL_ID column
 * @method NotificacionEmailQuery groupByDependenciaId() Group by the DEPENDENCIA_ID column
 * @method NotificacionEmailQuery groupByHostSmtp() Group by the HOST_SMTP column
 * @method NotificacionEmailQuery groupByEmailLogin() Group by the EMAIL_LOGIN column
 * @method NotificacionEmailQuery groupByEmailPass() Group by the EMAIL_PASS column
 * @method NotificacionEmailQuery groupByPuerto() Group by the PUERTO column
 * @method NotificacionEmailQuery groupBySecureSmtp() Group by the SECURE_SMTP column
 * @method NotificacionEmailQuery groupByAuthSmtp() Group by the AUTH_SMTP column
 * @method NotificacionEmailQuery groupByCertimail() Group by the CERTIMAIL column
 * @method NotificacionEmailQuery groupByEmailBody() Group by the EMAIL_BODY column
 *
 * @method NotificacionEmailQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method NotificacionEmailQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method NotificacionEmailQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method NotificacionEmailQuery leftJoinDependencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Dependencia relation
 * @method NotificacionEmailQuery rightJoinDependencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Dependencia relation
 * @method NotificacionEmailQuery innerJoinDependencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Dependencia relation
 *
 * @method NotificacionEmail findOne(PropelPDO $con = null) Return the first NotificacionEmail matching the query
 * @method NotificacionEmail findOneOrCreate(PropelPDO $con = null) Return the first NotificacionEmail matching the query, or a new NotificacionEmail object populated from the query conditions when no match is found
 *
 * @method NotificacionEmail findOneByDependenciaId(int $DEPENDENCIA_ID) Return the first NotificacionEmail filtered by the DEPENDENCIA_ID column
 * @method NotificacionEmail findOneByHostSmtp(string $HOST_SMTP) Return the first NotificacionEmail filtered by the HOST_SMTP column
 * @method NotificacionEmail findOneByEmailLogin(string $EMAIL_LOGIN) Return the first NotificacionEmail filtered by the EMAIL_LOGIN column
 * @method NotificacionEmail findOneByEmailPass(string $EMAIL_PASS) Return the first NotificacionEmail filtered by the EMAIL_PASS column
 * @method NotificacionEmail findOneByPuerto(int $PUERTO) Return the first NotificacionEmail filtered by the PUERTO column
 * @method NotificacionEmail findOneBySecureSmtp(string $SECURE_SMTP) Return the first NotificacionEmail filtered by the SECURE_SMTP column
 * @method NotificacionEmail findOneByAuthSmtp(int $AUTH_SMTP) Return the first NotificacionEmail filtered by the AUTH_SMTP column
 * @method NotificacionEmail findOneByCertimail(string $CERTIMAIL) Return the first NotificacionEmail filtered by the CERTIMAIL column
 * @method NotificacionEmail findOneByEmailBody(string $EMAIL_BODY) Return the first NotificacionEmail filtered by the EMAIL_BODY column
 *
 * @method array findByNotificacionemailId(int $NOTIFICACIONEMAIL_ID) Return NotificacionEmail objects filtered by the NOTIFICACIONEMAIL_ID column
 * @method array findByDependenciaId(int $DEPENDENCIA_ID) Return NotificacionEmail objects filtered by the DEPENDENCIA_ID column
 * @method array findByHostSmtp(string $HOST_SMTP) Return NotificacionEmail objects filtered by the HOST_SMTP column
 * @method array findByEmailLogin(string $EMAIL_LOGIN) Return NotificacionEmail objects filtered by the EMAIL_LOGIN column
 * @method array findByEmailPass(string $EMAIL_PASS) Return NotificacionEmail objects filtered by the EMAIL_PASS column
 * @method array findByPuerto(int $PUERTO) Return NotificacionEmail objects filtered by the PUERTO column
 * @method array findBySecureSmtp(string $SECURE_SMTP) Return NotificacionEmail objects filtered by the SECURE_SMTP column
 * @method array findByAuthSmtp(int $AUTH_SMTP) Return NotificacionEmail objects filtered by the AUTH_SMTP column
 * @method array findByCertimail(string $CERTIMAIL) Return NotificacionEmail objects filtered by the CERTIMAIL column
 * @method array findByEmailBody(string $EMAIL_BODY) Return NotificacionEmail objects filtered by the EMAIL_BODY column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseNotificacionEmailQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseNotificacionEmailQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'NotificacionEmail';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new NotificacionEmailQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   NotificacionEmailQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return NotificacionEmailQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof NotificacionEmailQuery) {
            return $criteria;
        }
        $query = new NotificacionEmailQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   NotificacionEmail|NotificacionEmail[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = NotificacionEmailPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(NotificacionEmailPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 NotificacionEmail A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByNotificacionemailId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 NotificacionEmail A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [NOTIFICACIONEMAIL_ID], [DEPENDENCIA_ID], [HOST_SMTP], [EMAIL_LOGIN], [EMAIL_PASS], [PUERTO], [SECURE_SMTP], [AUTH_SMTP], [CERTIMAIL], [EMAIL_BODY] FROM [NOTIFICACION_EMAIL] WHERE [NOTIFICACIONEMAIL_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new NotificacionEmail();
            $obj->hydrate($row);
            NotificacionEmailPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return NotificacionEmail|NotificacionEmail[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|NotificacionEmail[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(NotificacionEmailPeer::NOTIFICACIONEMAIL_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(NotificacionEmailPeer::NOTIFICACIONEMAIL_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the NOTIFICACIONEMAIL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByNotificacionemailId(1234); // WHERE NOTIFICACIONEMAIL_ID = 1234
     * $query->filterByNotificacionemailId(array(12, 34)); // WHERE NOTIFICACIONEMAIL_ID IN (12, 34)
     * $query->filterByNotificacionemailId(array('min' => 12)); // WHERE NOTIFICACIONEMAIL_ID >= 12
     * $query->filterByNotificacionemailId(array('max' => 12)); // WHERE NOTIFICACIONEMAIL_ID <= 12
     * </code>
     *
     * @param     mixed $notificacionemailId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByNotificacionemailId($notificacionemailId = null, $comparison = null)
    {
        if (is_array($notificacionemailId)) {
            $useMinMax = false;
            if (isset($notificacionemailId['min'])) {
                $this->addUsingAlias(NotificacionEmailPeer::NOTIFICACIONEMAIL_ID, $notificacionemailId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($notificacionemailId['max'])) {
                $this->addUsingAlias(NotificacionEmailPeer::NOTIFICACIONEMAIL_ID, $notificacionemailId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::NOTIFICACIONEMAIL_ID, $notificacionemailId, $comparison);
    }

    /**
     * Filter the query on the DEPENDENCIA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDependenciaId(1234); // WHERE DEPENDENCIA_ID = 1234
     * $query->filterByDependenciaId(array(12, 34)); // WHERE DEPENDENCIA_ID IN (12, 34)
     * $query->filterByDependenciaId(array('min' => 12)); // WHERE DEPENDENCIA_ID >= 12
     * $query->filterByDependenciaId(array('max' => 12)); // WHERE DEPENDENCIA_ID <= 12
     * </code>
     *
     * @see       filterByDependencia()
     *
     * @param     mixed $dependenciaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByDependenciaId($dependenciaId = null, $comparison = null)
    {
        if (is_array($dependenciaId)) {
            $useMinMax = false;
            if (isset($dependenciaId['min'])) {
                $this->addUsingAlias(NotificacionEmailPeer::DEPENDENCIA_ID, $dependenciaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($dependenciaId['max'])) {
                $this->addUsingAlias(NotificacionEmailPeer::DEPENDENCIA_ID, $dependenciaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::DEPENDENCIA_ID, $dependenciaId, $comparison);
    }

    /**
     * Filter the query on the HOST_SMTP column
     *
     * Example usage:
     * <code>
     * $query->filterByHostSmtp('fooValue');   // WHERE HOST_SMTP = 'fooValue'
     * $query->filterByHostSmtp('%fooValue%'); // WHERE HOST_SMTP LIKE '%fooValue%'
     * </code>
     *
     * @param     string $hostSmtp The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByHostSmtp($hostSmtp = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($hostSmtp)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $hostSmtp)) {
                $hostSmtp = str_replace('*', '%', $hostSmtp);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::HOST_SMTP, $hostSmtp, $comparison);
    }

    /**
     * Filter the query on the EMAIL_LOGIN column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailLogin('fooValue');   // WHERE EMAIL_LOGIN = 'fooValue'
     * $query->filterByEmailLogin('%fooValue%'); // WHERE EMAIL_LOGIN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailLogin The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByEmailLogin($emailLogin = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailLogin)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailLogin)) {
                $emailLogin = str_replace('*', '%', $emailLogin);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::EMAIL_LOGIN, $emailLogin, $comparison);
    }

    /**
     * Filter the query on the EMAIL_PASS column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailPass('fooValue');   // WHERE EMAIL_PASS = 'fooValue'
     * $query->filterByEmailPass('%fooValue%'); // WHERE EMAIL_PASS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailPass The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByEmailPass($emailPass = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailPass)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailPass)) {
                $emailPass = str_replace('*', '%', $emailPass);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::EMAIL_PASS, $emailPass, $comparison);
    }

    /**
     * Filter the query on the PUERTO column
     *
     * Example usage:
     * <code>
     * $query->filterByPuerto(1234); // WHERE PUERTO = 1234
     * $query->filterByPuerto(array(12, 34)); // WHERE PUERTO IN (12, 34)
     * $query->filterByPuerto(array('min' => 12)); // WHERE PUERTO >= 12
     * $query->filterByPuerto(array('max' => 12)); // WHERE PUERTO <= 12
     * </code>
     *
     * @param     mixed $puerto The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByPuerto($puerto = null, $comparison = null)
    {
        if (is_array($puerto)) {
            $useMinMax = false;
            if (isset($puerto['min'])) {
                $this->addUsingAlias(NotificacionEmailPeer::PUERTO, $puerto['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($puerto['max'])) {
                $this->addUsingAlias(NotificacionEmailPeer::PUERTO, $puerto['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::PUERTO, $puerto, $comparison);
    }

    /**
     * Filter the query on the SECURE_SMTP column
     *
     * Example usage:
     * <code>
     * $query->filterBySecureSmtp('fooValue');   // WHERE SECURE_SMTP = 'fooValue'
     * $query->filterBySecureSmtp('%fooValue%'); // WHERE SECURE_SMTP LIKE '%fooValue%'
     * </code>
     *
     * @param     string $secureSmtp The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterBySecureSmtp($secureSmtp = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($secureSmtp)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $secureSmtp)) {
                $secureSmtp = str_replace('*', '%', $secureSmtp);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::SECURE_SMTP, $secureSmtp, $comparison);
    }

    /**
     * Filter the query on the AUTH_SMTP column
     *
     * Example usage:
     * <code>
     * $query->filterByAuthSmtp(1234); // WHERE AUTH_SMTP = 1234
     * $query->filterByAuthSmtp(array(12, 34)); // WHERE AUTH_SMTP IN (12, 34)
     * $query->filterByAuthSmtp(array('min' => 12)); // WHERE AUTH_SMTP >= 12
     * $query->filterByAuthSmtp(array('max' => 12)); // WHERE AUTH_SMTP <= 12
     * </code>
     *
     * @param     mixed $authSmtp The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByAuthSmtp($authSmtp = null, $comparison = null)
    {
        if (is_array($authSmtp)) {
            $useMinMax = false;
            if (isset($authSmtp['min'])) {
                $this->addUsingAlias(NotificacionEmailPeer::AUTH_SMTP, $authSmtp['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($authSmtp['max'])) {
                $this->addUsingAlias(NotificacionEmailPeer::AUTH_SMTP, $authSmtp['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::AUTH_SMTP, $authSmtp, $comparison);
    }

    /**
     * Filter the query on the CERTIMAIL column
     *
     * Example usage:
     * <code>
     * $query->filterByCertimail('fooValue');   // WHERE CERTIMAIL = 'fooValue'
     * $query->filterByCertimail('%fooValue%'); // WHERE CERTIMAIL LIKE '%fooValue%'
     * </code>
     *
     * @param     string $certimail The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByCertimail($certimail = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($certimail)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $certimail)) {
                $certimail = str_replace('*', '%', $certimail);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::CERTIMAIL, $certimail, $comparison);
    }

    /**
     * Filter the query on the EMAIL_BODY column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailBody('fooValue');   // WHERE EMAIL_BODY = 'fooValue'
     * $query->filterByEmailBody('%fooValue%'); // WHERE EMAIL_BODY LIKE '%fooValue%'
     * </code>
     *
     * @param     string $emailBody The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function filterByEmailBody($emailBody = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($emailBody)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $emailBody)) {
                $emailBody = str_replace('*', '%', $emailBody);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NotificacionEmailPeer::EMAIL_BODY, $emailBody, $comparison);
    }

    /**
     * Filter the query by a related Dependencia object
     *
     * @param   Dependencia|PropelObjectCollection $dependencia The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 NotificacionEmailQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDependencia($dependencia, $comparison = null)
    {
        if ($dependencia instanceof Dependencia) {
            return $this
                ->addUsingAlias(NotificacionEmailPeer::DEPENDENCIA_ID, $dependencia->getDependenciaId(), $comparison);
        } elseif ($dependencia instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(NotificacionEmailPeer::DEPENDENCIA_ID, $dependencia->toKeyValue('PrimaryKey', 'DependenciaId'), $comparison);
        } else {
            throw new PropelException('filterByDependencia() only accepts arguments of type Dependencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Dependencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function joinDependencia($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Dependencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Dependencia');
        }

        return $this;
    }

    /**
     * Use the Dependencia relation Dependencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DependenciaQuery A secondary query class using the current class as primary query
     */
    public function useDependenciaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinDependencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Dependencia', 'DependenciaQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   NotificacionEmail $notificacionEmail Object to remove from the list of results
     *
     * @return NotificacionEmailQuery The current query, for fluid interface
     */
    public function prune($notificacionEmail = null)
    {
        if ($notificacionEmail) {
            $this->addUsingAlias(NotificacionEmailPeer::NOTIFICACIONEMAIL_ID, $notificacionEmail->getNotificacionemailId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

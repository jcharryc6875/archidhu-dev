<?php


/**
 * Base class that represents a row from the 'EMAIL_ACCOUNT' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * sáb 02 ago 2025 12:03:22
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEmailAccount extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'EmailAccountPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        EmailAccountPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the emailaccount_id field.
     * @var        int
     */
    protected $emailaccount_id;

    /**
     * The value for the usuario_id field.
     * @var        int
     */
    protected $usuario_id;

    /**
     * The value for the emailproviderconfig_id field.
     * @var        int
     */
    protected $emailproviderconfig_id;

    /**
     * The value for the account_login field.
     * @var        string
     */
    protected $account_login;

    /**
     * The value for the account_pass field.
     * @var        string
     */
    protected $account_pass;

    /**
     * The value for the auth_provider field.
     * @var        string
     */
    protected $auth_provider;

    /**
     * The value for the fecha_creacion field.
     * @var        string
     */
    protected $fecha_creacion;

    /**
     * The value for the fecha_modificacion field.
     * @var        string
     */
    protected $fecha_modificacion;

    /**
     * The value for the last_sync_data field.
     * @var        string
     */
    protected $last_sync_data;

    /**
     * The value for the es_actual field.
     * Note: this column has a database default value of: 0
     * @var        int
     */
    protected $es_actual;

    /**
     * @var        Usuario
     */
    protected $aUsuario;

    /**
     * @var        EmailProviderConfig
     */
    protected $aEmailProviderConfig;

    /**
     * @var        PropelObjectCollection|EmailAccountUsuario[] Collection to store aggregation of EmailAccountUsuario objects.
     */
    protected $collEmailAccountUsuarios;
    protected $collEmailAccountUsuariosPartial;

    /**
     * @var        PropelObjectCollection|Email[] Collection to store aggregation of Email objects.
     */
    protected $collEmails;
    protected $collEmailsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $emailAccountUsuariosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $emailsScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->es_actual = 0;
    }

    /**
     * Initializes internal state of BaseEmailAccount object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [emailaccount_id] column value.
     * 
     * @return int
     */
    public function getEmailaccountId()
    {

        return $this->emailaccount_id;
    }

    /**
     * Get the [usuario_id] column value.
     * 
     * @return int
     */
    public function getUsuarioId()
    {

        return $this->usuario_id;
    }

    /**
     * Get the [emailproviderconfig_id] column value.
     * 
     * @return int
     */
    public function getEmailproviderconfigId()
    {

        return $this->emailproviderconfig_id;
    }

    /**
     * Get the [account_login] column value.
     * 
     * @return string
     */
    public function getAccountLogin()
    {

        return $this->account_login;
    }

    /**
     * Get the [account_pass] column value.
     * 
     * @return string
     */
    public function getAccountPass()
    {

        return $this->account_pass;
    }

    /**
     * Get the [auth_provider] column value.
     * 
     * @return string
     */
    public function getAuthProvider()
    {

        return $this->auth_provider;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_creacion] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaCreacion($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_creacion === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_creacion);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_creacion, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [optionally formatted] temporal [fecha_modificacion] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaModificacion($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_modificacion === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_modificacion);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_modificacion, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [last_sync_data] column value.
     * 
     * @return string
     */
    public function getLastSyncData()
    {

        return $this->last_sync_data;
    }

    /**
     * Get the [es_actual] column value.
     * 
     * @return int
     */
    public function getEsActual()
    {

        return $this->es_actual;
    }

    /**
     * Set the value of [emailaccount_id] column.
     * 
     * @param  int $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setEmailaccountId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->emailaccount_id !== $v) {
            $this->emailaccount_id = $v;
            $this->modifiedColumns[] = EmailAccountPeer::EMAILACCOUNT_ID;
        }


        return $this;
    } // setEmailaccountId()

    /**
     * Set the value of [usuario_id] column.
     * 
     * @param  int $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setUsuarioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->usuario_id !== $v) {
            $this->usuario_id = $v;
            $this->modifiedColumns[] = EmailAccountPeer::USUARIO_ID;
        }

        if ($this->aUsuario !== null && $this->aUsuario->getUsuarioId() !== $v) {
            $this->aUsuario = null;
        }


        return $this;
    } // setUsuarioId()

    /**
     * Set the value of [emailproviderconfig_id] column.
     * 
     * @param  int $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setEmailproviderconfigId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->emailproviderconfig_id !== $v) {
            $this->emailproviderconfig_id = $v;
            $this->modifiedColumns[] = EmailAccountPeer::EMAILPROVIDERCONFIG_ID;
        }

        if ($this->aEmailProviderConfig !== null && $this->aEmailProviderConfig->getEmailproviderconfigId() !== $v) {
            $this->aEmailProviderConfig = null;
        }


        return $this;
    } // setEmailproviderconfigId()

    /**
     * Set the value of [account_login] column.
     * 
     * @param  string $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setAccountLogin($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->account_login !== $v) {
            $this->account_login = $v;
            $this->modifiedColumns[] = EmailAccountPeer::ACCOUNT_LOGIN;
        }


        return $this;
    } // setAccountLogin()

    /**
     * Set the value of [account_pass] column.
     * 
     * @param  string $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setAccountPass($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->account_pass !== $v) {
            $this->account_pass = $v;
            $this->modifiedColumns[] = EmailAccountPeer::ACCOUNT_PASS;
        }


        return $this;
    } // setAccountPass()

    /**
     * Set the value of [auth_provider] column.
     * 
     * @param  string $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setAuthProvider($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->auth_provider !== $v) {
            $this->auth_provider = $v;
            $this->modifiedColumns[] = EmailAccountPeer::AUTH_PROVIDER;
        }


        return $this;
    } // setAuthProvider()

    /**
     * Sets the value of [fecha_creacion] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setFechaCreacion($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_creacion !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_creacion !== null && $tmpDt = new DateTime($this->fecha_creacion)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_creacion = $newDateAsString;
                $this->modifiedColumns[] = EmailAccountPeer::FECHA_CREACION;
            }
        } // if either are not null


        return $this;
    } // setFechaCreacion()

    /**
     * Sets the value of [fecha_modificacion] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setFechaModificacion($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_modificacion !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_modificacion !== null && $tmpDt = new DateTime($this->fecha_modificacion)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_modificacion = $newDateAsString;
                $this->modifiedColumns[] = EmailAccountPeer::FECHA_MODIFICACION;
            }
        } // if either are not null


        return $this;
    } // setFechaModificacion()

    /**
     * Set the value of [last_sync_data] column.
     * 
     * @param  string $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setLastSyncData($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->last_sync_data !== $v) {
            $this->last_sync_data = $v;
            $this->modifiedColumns[] = EmailAccountPeer::LAST_SYNC_DATA;
        }


        return $this;
    } // setLastSyncData()

    /**
     * Set the value of [es_actual] column.
     * 
     * @param  int $v new value
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setEsActual($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->es_actual !== $v) {
            $this->es_actual = $v;
            $this->modifiedColumns[] = EmailAccountPeer::ES_ACTUAL;
        }


        return $this;
    } // setEsActual()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->es_actual !== 0) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->emailaccount_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->usuario_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->emailproviderconfig_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->account_login = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->account_pass = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->auth_provider = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->fecha_creacion = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->fecha_modificacion = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->last_sync_data = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->es_actual = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 10; // 10 = EmailAccountPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating EmailAccount object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aUsuario !== null && $this->usuario_id !== $this->aUsuario->getUsuarioId()) {
            $this->aUsuario = null;
        }
        if ($this->aEmailProviderConfig !== null && $this->emailproviderconfig_id !== $this->aEmailProviderConfig->getEmailproviderconfigId()) {
            $this->aEmailProviderConfig = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EmailAccountPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = EmailAccountPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aUsuario = null;
            $this->aEmailProviderConfig = null;
            $this->collEmailAccountUsuarios = null;

            $this->collEmails = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EmailAccountPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = EmailAccountQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseEmailAccount:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseEmailAccount:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EmailAccountPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseEmailAccount:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseEmailAccount:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                EmailAccountPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aUsuario !== null) {
                if ($this->aUsuario->isModified() || $this->aUsuario->isNew()) {
                    $affectedRows += $this->aUsuario->save($con);
                }
                $this->setUsuario($this->aUsuario);
            }

            if ($this->aEmailProviderConfig !== null) {
                if ($this->aEmailProviderConfig->isModified() || $this->aEmailProviderConfig->isNew()) {
                    $affectedRows += $this->aEmailProviderConfig->save($con);
                }
                $this->setEmailProviderConfig($this->aEmailProviderConfig);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->emailAccountUsuariosScheduledForDeletion !== null) {
                if (!$this->emailAccountUsuariosScheduledForDeletion->isEmpty()) {
                    EmailAccountUsuarioQuery::create()
                        ->filterByPrimaryKeys($this->emailAccountUsuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->emailAccountUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collEmailAccountUsuarios !== null) {
                foreach ($this->collEmailAccountUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->emailsScheduledForDeletion !== null) {
                if (!$this->emailsScheduledForDeletion->isEmpty()) {
                    EmailQuery::create()
                        ->filterByPrimaryKeys($this->emailsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->emailsScheduledForDeletion = null;
                }
            }

            if ($this->collEmails !== null) {
                foreach ($this->collEmails as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = EmailAccountPeer::EMAILACCOUNT_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(EmailAccountPeer::EMAILACCOUNT_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . EmailAccountPeer::EMAILACCOUNT_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(EmailAccountPeer::EMAILACCOUNT_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setEmailaccountId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aUsuario !== null) {
                if (!$this->aUsuario->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aUsuario->getValidationFailures());
                }
            }

            if ($this->aEmailProviderConfig !== null) {
                if (!$this->aEmailProviderConfig->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEmailProviderConfig->getValidationFailures());
                }
            }


            if (($retval = EmailAccountPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collEmailAccountUsuarios !== null) {
                    foreach ($this->collEmailAccountUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collEmails !== null) {
                    foreach ($this->collEmails as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = EmailAccountPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getEmailaccountId();
                break;
            case 1:
                return $this->getUsuarioId();
                break;
            case 2:
                return $this->getEmailproviderconfigId();
                break;
            case 3:
                return $this->getAccountLogin();
                break;
            case 4:
                return $this->getAccountPass();
                break;
            case 5:
                return $this->getAuthProvider();
                break;
            case 6:
                return $this->getFechaCreacion();
                break;
            case 7:
                return $this->getFechaModificacion();
                break;
            case 8:
                return $this->getLastSyncData();
                break;
            case 9:
                return $this->getEsActual();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['EmailAccount'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['EmailAccount'][$this->getPrimaryKey()] = true;
        $keys = EmailAccountPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getEmailaccountId(),
            $keys[1] => $this->getUsuarioId(),
            $keys[2] => $this->getEmailproviderconfigId(),
            $keys[3] => $this->getAccountLogin(),
            $keys[4] => $this->getAccountPass(),
            $keys[5] => $this->getAuthProvider(),
            $keys[6] => $this->getFechaCreacion(),
            $keys[7] => $this->getFechaModificacion(),
            $keys[8] => $this->getLastSyncData(),
            $keys[9] => $this->getEsActual(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aUsuario) {
                $result['Usuario'] = $this->aUsuario->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aEmailProviderConfig) {
                $result['EmailProviderConfig'] = $this->aEmailProviderConfig->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collEmailAccountUsuarios) {
                $result['EmailAccountUsuarios'] = $this->collEmailAccountUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collEmails) {
                $result['Emails'] = $this->collEmails->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = EmailAccountPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setEmailaccountId($value);
                break;
            case 1:
                $this->setUsuarioId($value);
                break;
            case 2:
                $this->setEmailproviderconfigId($value);
                break;
            case 3:
                $this->setAccountLogin($value);
                break;
            case 4:
                $this->setAccountPass($value);
                break;
            case 5:
                $this->setAuthProvider($value);
                break;
            case 6:
                $this->setFechaCreacion($value);
                break;
            case 7:
                $this->setFechaModificacion($value);
                break;
            case 8:
                $this->setLastSyncData($value);
                break;
            case 9:
                $this->setEsActual($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = EmailAccountPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setEmailaccountId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setUsuarioId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setEmailproviderconfigId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setAccountLogin($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setAccountPass($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setAuthProvider($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setFechaCreacion($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setFechaModificacion($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setLastSyncData($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setEsActual($arr[$keys[9]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(EmailAccountPeer::DATABASE_NAME);

        if ($this->isColumnModified(EmailAccountPeer::EMAILACCOUNT_ID)) $criteria->add(EmailAccountPeer::EMAILACCOUNT_ID, $this->emailaccount_id);
        if ($this->isColumnModified(EmailAccountPeer::USUARIO_ID)) $criteria->add(EmailAccountPeer::USUARIO_ID, $this->usuario_id);
        if ($this->isColumnModified(EmailAccountPeer::EMAILPROVIDERCONFIG_ID)) $criteria->add(EmailAccountPeer::EMAILPROVIDERCONFIG_ID, $this->emailproviderconfig_id);
        if ($this->isColumnModified(EmailAccountPeer::ACCOUNT_LOGIN)) $criteria->add(EmailAccountPeer::ACCOUNT_LOGIN, $this->account_login);
        if ($this->isColumnModified(EmailAccountPeer::ACCOUNT_PASS)) $criteria->add(EmailAccountPeer::ACCOUNT_PASS, $this->account_pass);
        if ($this->isColumnModified(EmailAccountPeer::AUTH_PROVIDER)) $criteria->add(EmailAccountPeer::AUTH_PROVIDER, $this->auth_provider);
        if ($this->isColumnModified(EmailAccountPeer::FECHA_CREACION)) $criteria->add(EmailAccountPeer::FECHA_CREACION, $this->fecha_creacion);
        if ($this->isColumnModified(EmailAccountPeer::FECHA_MODIFICACION)) $criteria->add(EmailAccountPeer::FECHA_MODIFICACION, $this->fecha_modificacion);
        if ($this->isColumnModified(EmailAccountPeer::LAST_SYNC_DATA)) $criteria->add(EmailAccountPeer::LAST_SYNC_DATA, $this->last_sync_data);
        if ($this->isColumnModified(EmailAccountPeer::ES_ACTUAL)) $criteria->add(EmailAccountPeer::ES_ACTUAL, $this->es_actual);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(EmailAccountPeer::DATABASE_NAME);
        $criteria->add(EmailAccountPeer::EMAILACCOUNT_ID, $this->emailaccount_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getEmailaccountId();
    }

    /**
     * Generic method to set the primary key (emailaccount_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setEmailaccountId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getEmailaccountId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of EmailAccount (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setUsuarioId($this->getUsuarioId());
        $copyObj->setEmailproviderconfigId($this->getEmailproviderconfigId());
        $copyObj->setAccountLogin($this->getAccountLogin());
        $copyObj->setAccountPass($this->getAccountPass());
        $copyObj->setAuthProvider($this->getAuthProvider());
        $copyObj->setFechaCreacion($this->getFechaCreacion());
        $copyObj->setFechaModificacion($this->getFechaModificacion());
        $copyObj->setLastSyncData($this->getLastSyncData());
        $copyObj->setEsActual($this->getEsActual());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getEmailAccountUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addEmailAccountUsuario($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getEmails() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addEmail($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setEmailaccountId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return EmailAccount Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return EmailAccountPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new EmailAccountPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Usuario object.
     *
     * @param                  Usuario $v
     * @return EmailAccount The current object (for fluent API support)
     * @throws PropelException
     */
    public function setUsuario(Usuario $v = null)
    {
        if ($v === null) {
            $this->setUsuarioId(NULL);
        } else {
            $this->setUsuarioId($v->getUsuarioId());
        }

        $this->aUsuario = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Usuario object, it will not be re-added.
        if ($v !== null) {
            $v->addEmailAccount($this);
        }


        return $this;
    }


    /**
     * Get the associated Usuario object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Usuario The associated Usuario object.
     * @throws PropelException
     */
    public function getUsuario(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aUsuario === null && ($this->usuario_id !== null) && $doQuery) {
            $this->aUsuario = UsuarioQuery::create()->findPk($this->usuario_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aUsuario->addEmailAccounts($this);
             */
        }

        return $this->aUsuario;
    }

    /**
     * Declares an association between this object and a EmailProviderConfig object.
     *
     * @param                  EmailProviderConfig $v
     * @return EmailAccount The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEmailProviderConfig(EmailProviderConfig $v = null)
    {
        if ($v === null) {
            $this->setEmailproviderconfigId(NULL);
        } else {
            $this->setEmailproviderconfigId($v->getEmailproviderconfigId());
        }

        $this->aEmailProviderConfig = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the EmailProviderConfig object, it will not be re-added.
        if ($v !== null) {
            $v->addEmailAccount($this);
        }


        return $this;
    }


    /**
     * Get the associated EmailProviderConfig object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return EmailProviderConfig The associated EmailProviderConfig object.
     * @throws PropelException
     */
    public function getEmailProviderConfig(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEmailProviderConfig === null && ($this->emailproviderconfig_id !== null) && $doQuery) {
            $this->aEmailProviderConfig = EmailProviderConfigQuery::create()->findPk($this->emailproviderconfig_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEmailProviderConfig->addEmailAccounts($this);
             */
        }

        return $this->aEmailProviderConfig;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('EmailAccountUsuario' == $relationName) {
            $this->initEmailAccountUsuarios();
        }
        if ('Email' == $relationName) {
            $this->initEmails();
        }
    }

    /**
     * Clears out the collEmailAccountUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return EmailAccount The current object (for fluent API support)
     * @see        addEmailAccountUsuarios()
     */
    public function clearEmailAccountUsuarios()
    {
        $this->collEmailAccountUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collEmailAccountUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collEmailAccountUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialEmailAccountUsuarios($v = true)
    {
        $this->collEmailAccountUsuariosPartial = $v;
    }

    /**
     * Initializes the collEmailAccountUsuarios collection.
     *
     * By default this just sets the collEmailAccountUsuarios collection to an empty array (like clearcollEmailAccountUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initEmailAccountUsuarios($overrideExisting = true)
    {
        if (null !== $this->collEmailAccountUsuarios && !$overrideExisting) {
            return;
        }
        $this->collEmailAccountUsuarios = new PropelObjectCollection();
        $this->collEmailAccountUsuarios->setModel('EmailAccountUsuario');
    }

    /**
     * Gets an array of EmailAccountUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this EmailAccount is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|EmailAccountUsuario[] List of EmailAccountUsuario objects
     * @throws PropelException
     */
    public function getEmailAccountUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collEmailAccountUsuariosPartial && !$this->isNew();
        if (null === $this->collEmailAccountUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collEmailAccountUsuarios) {
                // return empty collection
                $this->initEmailAccountUsuarios();
            } else {
                $collEmailAccountUsuarios = EmailAccountUsuarioQuery::create(null, $criteria)
                    ->filterByEmailAccount($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collEmailAccountUsuariosPartial && count($collEmailAccountUsuarios)) {
                      $this->initEmailAccountUsuarios(false);

                      foreach ($collEmailAccountUsuarios as $obj) {
                        if (false == $this->collEmailAccountUsuarios->contains($obj)) {
                          $this->collEmailAccountUsuarios->append($obj);
                        }
                      }

                      $this->collEmailAccountUsuariosPartial = true;
                    }

                    $collEmailAccountUsuarios->getInternalIterator()->rewind();

                    return $collEmailAccountUsuarios;
                }

                if ($partial && $this->collEmailAccountUsuarios) {
                    foreach ($this->collEmailAccountUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collEmailAccountUsuarios[] = $obj;
                        }
                    }
                }

                $this->collEmailAccountUsuarios = $collEmailAccountUsuarios;
                $this->collEmailAccountUsuariosPartial = false;
            }
        }

        return $this->collEmailAccountUsuarios;
    }

    /**
     * Sets a collection of EmailAccountUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $emailAccountUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setEmailAccountUsuarios(PropelCollection $emailAccountUsuarios, PropelPDO $con = null)
    {
        $emailAccountUsuariosToDelete = $this->getEmailAccountUsuarios(new Criteria(), $con)->diff($emailAccountUsuarios);


        $this->emailAccountUsuariosScheduledForDeletion = $emailAccountUsuariosToDelete;

        foreach ($emailAccountUsuariosToDelete as $emailAccountUsuarioRemoved) {
            $emailAccountUsuarioRemoved->setEmailAccount(null);
        }

        $this->collEmailAccountUsuarios = null;
        foreach ($emailAccountUsuarios as $emailAccountUsuario) {
            $this->addEmailAccountUsuario($emailAccountUsuario);
        }

        $this->collEmailAccountUsuarios = $emailAccountUsuarios;
        $this->collEmailAccountUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related EmailAccountUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related EmailAccountUsuario objects.
     * @throws PropelException
     */
    public function countEmailAccountUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collEmailAccountUsuariosPartial && !$this->isNew();
        if (null === $this->collEmailAccountUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collEmailAccountUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getEmailAccountUsuarios());
            }
            $query = EmailAccountUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByEmailAccount($this)
                ->count($con);
        }

        return count($this->collEmailAccountUsuarios);
    }

    /**
     * Method called to associate a EmailAccountUsuario object to this object
     * through the EmailAccountUsuario foreign key attribute.
     *
     * @param    EmailAccountUsuario $l EmailAccountUsuario
     * @return EmailAccount The current object (for fluent API support)
     */
    public function addEmailAccountUsuario(EmailAccountUsuario $l)
    {
        if ($this->collEmailAccountUsuarios === null) {
            $this->initEmailAccountUsuarios();
            $this->collEmailAccountUsuariosPartial = true;
        }

        if (!in_array($l, $this->collEmailAccountUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddEmailAccountUsuario($l);

            if ($this->emailAccountUsuariosScheduledForDeletion and $this->emailAccountUsuariosScheduledForDeletion->contains($l)) {
                $this->emailAccountUsuariosScheduledForDeletion->remove($this->emailAccountUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	EmailAccountUsuario $emailAccountUsuario The emailAccountUsuario object to add.
     */
    protected function doAddEmailAccountUsuario($emailAccountUsuario)
    {
        $this->collEmailAccountUsuarios[]= $emailAccountUsuario;
        $emailAccountUsuario->setEmailAccount($this);
    }

    /**
     * @param	EmailAccountUsuario $emailAccountUsuario The emailAccountUsuario object to remove.
     * @return EmailAccount The current object (for fluent API support)
     */
    public function removeEmailAccountUsuario($emailAccountUsuario)
    {
        if ($this->getEmailAccountUsuarios()->contains($emailAccountUsuario)) {
            $this->collEmailAccountUsuarios->remove($this->collEmailAccountUsuarios->search($emailAccountUsuario));
            if (null === $this->emailAccountUsuariosScheduledForDeletion) {
                $this->emailAccountUsuariosScheduledForDeletion = clone $this->collEmailAccountUsuarios;
                $this->emailAccountUsuariosScheduledForDeletion->clear();
            }
            $this->emailAccountUsuariosScheduledForDeletion[]= clone $emailAccountUsuario;
            $emailAccountUsuario->setEmailAccount(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this EmailAccount is new, it will return
     * an empty collection; or if this EmailAccount has previously
     * been saved, it will retrieve related EmailAccountUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in EmailAccount.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|EmailAccountUsuario[] List of EmailAccountUsuario objects
     */
    public function getEmailAccountUsuariosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = EmailAccountUsuarioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getEmailAccountUsuarios($query, $con);
    }

    /**
     * Clears out the collEmails collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return EmailAccount The current object (for fluent API support)
     * @see        addEmails()
     */
    public function clearEmails()
    {
        $this->collEmails = null; // important to set this to null since that means it is uninitialized
        $this->collEmailsPartial = null;

        return $this;
    }

    /**
     * reset is the collEmails collection loaded partially
     *
     * @return void
     */
    public function resetPartialEmails($v = true)
    {
        $this->collEmailsPartial = $v;
    }

    /**
     * Initializes the collEmails collection.
     *
     * By default this just sets the collEmails collection to an empty array (like clearcollEmails());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initEmails($overrideExisting = true)
    {
        if (null !== $this->collEmails && !$overrideExisting) {
            return;
        }
        $this->collEmails = new PropelObjectCollection();
        $this->collEmails->setModel('Email');
    }

    /**
     * Gets an array of Email objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this EmailAccount is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Email[] List of Email objects
     * @throws PropelException
     */
    public function getEmails($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collEmailsPartial && !$this->isNew();
        if (null === $this->collEmails || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collEmails) {
                // return empty collection
                $this->initEmails();
            } else {
                $collEmails = EmailQuery::create(null, $criteria)
                    ->filterByEmailAccount($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collEmailsPartial && count($collEmails)) {
                      $this->initEmails(false);

                      foreach ($collEmails as $obj) {
                        if (false == $this->collEmails->contains($obj)) {
                          $this->collEmails->append($obj);
                        }
                      }

                      $this->collEmailsPartial = true;
                    }

                    $collEmails->getInternalIterator()->rewind();

                    return $collEmails;
                }

                if ($partial && $this->collEmails) {
                    foreach ($this->collEmails as $obj) {
                        if ($obj->isNew()) {
                            $collEmails[] = $obj;
                        }
                    }
                }

                $this->collEmails = $collEmails;
                $this->collEmailsPartial = false;
            }
        }

        return $this->collEmails;
    }

    /**
     * Sets a collection of Email objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $emails A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return EmailAccount The current object (for fluent API support)
     */
    public function setEmails(PropelCollection $emails, PropelPDO $con = null)
    {
        $emailsToDelete = $this->getEmails(new Criteria(), $con)->diff($emails);


        $this->emailsScheduledForDeletion = $emailsToDelete;

        foreach ($emailsToDelete as $emailRemoved) {
            $emailRemoved->setEmailAccount(null);
        }

        $this->collEmails = null;
        foreach ($emails as $email) {
            $this->addEmail($email);
        }

        $this->collEmails = $emails;
        $this->collEmailsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Email objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Email objects.
     * @throws PropelException
     */
    public function countEmails(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collEmailsPartial && !$this->isNew();
        if (null === $this->collEmails || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collEmails) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getEmails());
            }
            $query = EmailQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByEmailAccount($this)
                ->count($con);
        }

        return count($this->collEmails);
    }

    /**
     * Method called to associate a Email object to this object
     * through the Email foreign key attribute.
     *
     * @param    Email $l Email
     * @return EmailAccount The current object (for fluent API support)
     */
    public function addEmail(Email $l)
    {
        if ($this->collEmails === null) {
            $this->initEmails();
            $this->collEmailsPartial = true;
        }

        if (!in_array($l, $this->collEmails->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddEmail($l);

            if ($this->emailsScheduledForDeletion and $this->emailsScheduledForDeletion->contains($l)) {
                $this->emailsScheduledForDeletion->remove($this->emailsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Email $email The email object to add.
     */
    protected function doAddEmail($email)
    {
        $this->collEmails[]= $email;
        $email->setEmailAccount($this);
    }

    /**
     * @param	Email $email The email object to remove.
     * @return EmailAccount The current object (for fluent API support)
     */
    public function removeEmail($email)
    {
        if ($this->getEmails()->contains($email)) {
            $this->collEmails->remove($this->collEmails->search($email));
            if (null === $this->emailsScheduledForDeletion) {
                $this->emailsScheduledForDeletion = clone $this->collEmails;
                $this->emailsScheduledForDeletion->clear();
            }
            $this->emailsScheduledForDeletion[]= clone $email;
            $email->setEmailAccount(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this EmailAccount is new, it will return
     * an empty collection; or if this EmailAccount has previously
     * been saved, it will retrieve related Emails from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in EmailAccount.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Email[] List of Email objects
     */
    public function getEmailsJoinEmailEstado($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = EmailQuery::create(null, $criteria);
        $query->joinWith('EmailEstado', $join_behavior);

        return $this->getEmails($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->emailaccount_id = null;
        $this->usuario_id = null;
        $this->emailproviderconfig_id = null;
        $this->account_login = null;
        $this->account_pass = null;
        $this->auth_provider = null;
        $this->fecha_creacion = null;
        $this->fecha_modificacion = null;
        $this->last_sync_data = null;
        $this->es_actual = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collEmailAccountUsuarios) {
                foreach ($this->collEmailAccountUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collEmails) {
                foreach ($this->collEmails as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aUsuario instanceof Persistent) {
              $this->aUsuario->clearAllReferences($deep);
            }
            if ($this->aEmailProviderConfig instanceof Persistent) {
              $this->aEmailProviderConfig->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collEmailAccountUsuarios instanceof PropelCollection) {
            $this->collEmailAccountUsuarios->clearIterator();
        }
        $this->collEmailAccountUsuarios = null;
        if ($this->collEmails instanceof PropelCollection) {
            $this->collEmails->clearIterator();
        }
        $this->collEmails = null;
        $this->aUsuario = null;
        $this->aEmailProviderConfig = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(EmailAccountPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseEmailAccount:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

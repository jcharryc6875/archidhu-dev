<?php


/**
 * Base class that represents a query for the 'PRIORIDAD_COM' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:38
 *
 * @method PrioridadComQuery orderByPrioridadcomId($order = Criteria::ASC) Order by the PRIORIDADCOM_ID column
 * @method PrioridadComQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method PrioridadComQuery orderByIconName($order = Criteria::ASC) Order by the ICON_NAME column
 *
 * @method PrioridadComQuery groupByPrioridadcomId() Group by the PRIORIDADCOM_ID column
 * @method PrioridadComQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method PrioridadComQuery groupByIconName() Group by the ICON_NAME column
 *
 * @method PrioridadComQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method PrioridadComQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method PrioridadComQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method PrioridadComQuery leftJoinComEnviada($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComEnviada relation
 * @method PrioridadComQuery rightJoinComEnviada($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComEnviada relation
 * @method PrioridadComQuery innerJoinComEnviada($relationAlias = null) Adds a INNER JOIN clause to the query using the ComEnviada relation
 *
 * @method PrioridadComQuery leftJoinComInterna($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComInterna relation
 * @method PrioridadComQuery rightJoinComInterna($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComInterna relation
 * @method PrioridadComQuery innerJoinComInterna($relationAlias = null) Adds a INNER JOIN clause to the query using the ComInterna relation
 *
 * @method PrioridadComQuery leftJoinComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComRecibida relation
 * @method PrioridadComQuery rightJoinComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComRecibida relation
 * @method PrioridadComQuery innerJoinComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the ComRecibida relation
 *
 * @method PrioridadCom findOne(PropelPDO $con = null) Return the first PrioridadCom matching the query
 * @method PrioridadCom findOneOrCreate(PropelPDO $con = null) Return the first PrioridadCom matching the query, or a new PrioridadCom object populated from the query conditions when no match is found
 *
 * @method PrioridadCom findOneByDescripcion(string $DESCRIPCION) Return the first PrioridadCom filtered by the DESCRIPCION column
 * @method PrioridadCom findOneByIconName(string $ICON_NAME) Return the first PrioridadCom filtered by the ICON_NAME column
 *
 * @method array findByPrioridadcomId(int $PRIORIDADCOM_ID) Return PrioridadCom objects filtered by the PRIORIDADCOM_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return PrioridadCom objects filtered by the DESCRIPCION column
 * @method array findByIconName(string $ICON_NAME) Return PrioridadCom objects filtered by the ICON_NAME column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BasePrioridadComQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BasePrioridadComQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'PrioridadCom';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new PrioridadComQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   PrioridadComQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return PrioridadComQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof PrioridadComQuery) {
            return $criteria;
        }
        $query = new PrioridadComQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   PrioridadCom|PrioridadCom[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = PrioridadComPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(PrioridadComPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 PrioridadCom A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByPrioridadcomId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 PrioridadCom A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [PRIORIDADCOM_ID], [DESCRIPCION], [ICON_NAME] FROM [PRIORIDAD_COM] WHERE [PRIORIDADCOM_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new PrioridadCom();
            $obj->hydrate($row);
            PrioridadComPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return PrioridadCom|PrioridadCom[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|PrioridadCom[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the PRIORIDADCOM_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByPrioridadcomId(1234); // WHERE PRIORIDADCOM_ID = 1234
     * $query->filterByPrioridadcomId(array(12, 34)); // WHERE PRIORIDADCOM_ID IN (12, 34)
     * $query->filterByPrioridadcomId(array('min' => 12)); // WHERE PRIORIDADCOM_ID >= 12
     * $query->filterByPrioridadcomId(array('max' => 12)); // WHERE PRIORIDADCOM_ID <= 12
     * </code>
     *
     * @param     mixed $prioridadcomId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function filterByPrioridadcomId($prioridadcomId = null, $comparison = null)
    {
        if (is_array($prioridadcomId)) {
            $useMinMax = false;
            if (isset($prioridadcomId['min'])) {
                $this->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $prioridadcomId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($prioridadcomId['max'])) {
                $this->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $prioridadcomId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $prioridadcomId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PrioridadComPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ICON_NAME column
     *
     * Example usage:
     * <code>
     * $query->filterByIconName('fooValue');   // WHERE ICON_NAME = 'fooValue'
     * $query->filterByIconName('%fooValue%'); // WHERE ICON_NAME LIKE '%fooValue%'
     * </code>
     *
     * @param     string $iconName The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function filterByIconName($iconName = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($iconName)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $iconName)) {
                $iconName = str_replace('*', '%', $iconName);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(PrioridadComPeer::ICON_NAME, $iconName, $comparison);
    }

    /**
     * Filter the query by a related ComEnviada object
     *
     * @param   ComEnviada|PropelObjectCollection $comEnviada  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PrioridadComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComEnviada($comEnviada, $comparison = null)
    {
        if ($comEnviada instanceof ComEnviada) {
            return $this
                ->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $comEnviada->getPrioridadcomId(), $comparison);
        } elseif ($comEnviada instanceof PropelObjectCollection) {
            return $this
                ->useComEnviadaQuery()
                ->filterByPrimaryKeys($comEnviada->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComEnviada() only accepts arguments of type ComEnviada or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComEnviada relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function joinComEnviada($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComEnviada');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComEnviada');
        }

        return $this;
    }

    /**
     * Use the ComEnviada relation ComEnviada object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComEnviadaQuery A secondary query class using the current class as primary query
     */
    public function useComEnviadaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComEnviada($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComEnviada', 'ComEnviadaQuery');
    }

    /**
     * Filter the query by a related ComInterna object
     *
     * @param   ComInterna|PropelObjectCollection $comInterna  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PrioridadComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComInterna($comInterna, $comparison = null)
    {
        if ($comInterna instanceof ComInterna) {
            return $this
                ->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $comInterna->getPrioridadcomId(), $comparison);
        } elseif ($comInterna instanceof PropelObjectCollection) {
            return $this
                ->useComInternaQuery()
                ->filterByPrimaryKeys($comInterna->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComInterna() only accepts arguments of type ComInterna or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComInterna relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function joinComInterna($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComInterna');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComInterna');
        }

        return $this;
    }

    /**
     * Use the ComInterna relation ComInterna object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComInternaQuery A secondary query class using the current class as primary query
     */
    public function useComInternaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComInterna($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComInterna', 'ComInternaQuery');
    }

    /**
     * Filter the query by a related ComRecibida object
     *
     * @param   ComRecibida|PropelObjectCollection $comRecibida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 PrioridadComQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComRecibida($comRecibida, $comparison = null)
    {
        if ($comRecibida instanceof ComRecibida) {
            return $this
                ->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $comRecibida->getPrioridadcomId(), $comparison);
        } elseif ($comRecibida instanceof PropelObjectCollection) {
            return $this
                ->useComRecibidaQuery()
                ->filterByPrimaryKeys($comRecibida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComRecibida() only accepts arguments of type ComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function joinComRecibida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComRecibida');
        }

        return $this;
    }

    /**
     * Use the ComRecibida relation ComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useComRecibidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComRecibida', 'ComRecibidaQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   PrioridadCom $prioridadCom Object to remove from the list of results
     *
     * @return PrioridadComQuery The current query, for fluid interface
     */
    public function prune($prioridadCom = null)
    {
        if ($prioridadCom) {
            $this->addUsingAlias(PrioridadComPeer::PRIORIDADCOM_ID, $prioridadCom->getPrioridadcomId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

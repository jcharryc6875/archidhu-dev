<?php


/**
 * Base class that represents a row from the 'REGIONAL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:43
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseRegional extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'RegionalPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        RegionalPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the regional_id field.
     * @var        int
     */
    protected $regional_id;

    /**
     * The value for the ciudad_id field.
     * @var        int
     */
    protected $ciudad_id;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * The value for the direccion field.
     * @var        string
     */
    protected $direccion;

    /**
     * The value for the entidad_id field.
     * Note: this column has a database default value of: 0
     * @var        int
     */
    protected $entidad_id;

    /**
     * The value for the es_visible field.
     * @var        boolean
     */
    protected $es_visible;

    /**
     * The value for the directorio_name field.
     * @var        string
     */
    protected $directorio_name;

    /**
     * The value for the image_membrete field.
     * @var        string
     */
    protected $image_membrete;

    /**
     * The value for the codigo field.
     * @var        string
     */
    protected $codigo;

    /**
     * @var        Ciudad
     */
    protected $aCiudad;

    /**
     * @var        Entidad
     */
    protected $aEntidad;

    /**
     * @var        PropelObjectCollection|DocPrestamo[] Collection to store aggregation of DocPrestamo objects.
     */
    protected $collDocPrestamos;
    protected $collDocPrestamosPartial;

    /**
     * @var        PropelObjectCollection|Receptor[] Collection to store aggregation of Receptor objects.
     */
    protected $collReceptors;
    protected $collReceptorsPartial;

    /**
     * @var        PropelObjectCollection|WfFlujo[] Collection to store aggregation of WfFlujo objects.
     */
    protected $collWfFlujos;
    protected $collWfFlujosPartial;

    /**
     * @var        PropelObjectCollection|PlantillasCom[] Collection to store aggregation of PlantillasCom objects.
     */
    protected $collPlantillasComs;
    protected $collPlantillasComsPartial;

    /**
     * @var        PropelObjectCollection|Prestamo[] Collection to store aggregation of Prestamo objects.
     */
    protected $collPrestamos;
    protected $collPrestamosPartial;

    /**
     * @var        PropelObjectCollection|Servicio[] Collection to store aggregation of Servicio objects.
     */
    protected $collServicios;
    protected $collServiciosPartial;

    /**
     * @var        PropelObjectCollection|FactHist[] Collection to store aggregation of FactHist objects.
     */
    protected $collFactHists;
    protected $collFactHistsPartial;

    /**
     * @var        PropelObjectCollection|ClPrestamo[] Collection to store aggregation of ClPrestamo objects.
     */
    protected $collClPrestamos;
    protected $collClPrestamosPartial;

    /**
     * @var        PropelObjectCollection|Factura[] Collection to store aggregation of Factura objects.
     */
    protected $collFacturas;
    protected $collFacturasPartial;

    /**
     * @var        PropelObjectCollection|FacturaReceptor[] Collection to store aggregation of FacturaReceptor objects.
     */
    protected $collFacturaReceptors;
    protected $collFacturaReceptorsPartial;

    /**
     * @var        PropelObjectCollection|ComEnviada[] Collection to store aggregation of ComEnviada objects.
     */
    protected $collComEnviadas;
    protected $collComEnviadasPartial;

    /**
     * @var        PropelObjectCollection|ComInterna[] Collection to store aggregation of ComInterna objects.
     */
    protected $collComInternas;
    protected $collComInternasPartial;

    /**
     * @var        PropelObjectCollection|ComPermisoRegional[] Collection to store aggregation of ComPermisoRegional objects.
     */
    protected $collComPermisoRegionals;
    protected $collComPermisoRegionalsPartial;

    /**
     * @var        PropelObjectCollection|ComRecibida[] Collection to store aggregation of ComRecibida objects.
     */
    protected $collComRecibidas;
    protected $collComRecibidasPartial;

    /**
     * @var        PropelObjectCollection|HistUsuario[] Collection to store aggregation of HistUsuario objects.
     */
    protected $collHistUsuarios;
    protected $collHistUsuariosPartial;

    /**
     * @var        PropelObjectCollection|UnidadDocumental[] Collection to store aggregation of UnidadDocumental objects.
     */
    protected $collUnidadDocumentals;
    protected $collUnidadDocumentalsPartial;

    /**
     * @var        PropelObjectCollection|Usuario[] Collection to store aggregation of Usuario objects.
     */
    protected $collUsuarios;
    protected $collUsuariosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $docPrestamosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $receptorsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $wfFlujosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $plantillasComsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $prestamosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $serviciosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $factHistsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $clPrestamosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $facturasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $facturaReceptorsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $comEnviadasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $comInternasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $comPermisoRegionalsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $comRecibidasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $histUsuariosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $unidadDocumentalsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuariosScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->entidad_id = 0;
    }

    /**
     * Initializes internal state of BaseRegional object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [regional_id] column value.
     * 
     * @return int
     */
    public function getRegionalId()
    {

        return $this->regional_id;
    }

    /**
     * Get the [ciudad_id] column value.
     * 
     * @return int
     */
    public function getCiudadId()
    {

        return $this->ciudad_id;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Get the [direccion] column value.
     * 
     * @return string
     */
    public function getDireccion()
    {

        return $this->direccion;
    }

    /**
     * Get the [entidad_id] column value.
     * 
     * @return int
     */
    public function getEntidadId()
    {

        return $this->entidad_id;
    }

    /**
     * Get the [es_visible] column value.
     * 
     * @return boolean
     */
    public function getEsVisible()
    {

        return $this->es_visible;
    }

    /**
     * Get the [directorio_name] column value.
     * 
     * @return string
     */
    public function getDirectorioName()
    {

        return $this->directorio_name;
    }

    /**
     * Get the [image_membrete] column value.
     * 
     * @return string
     */
    public function getImageMembrete()
    {

        return $this->image_membrete;
    }

    /**
     * Get the [codigo] column value.
     * 
     * @return string
     */
    public function getCodigo()
    {

        return $this->codigo;
    }

    /**
     * Set the value of [regional_id] column.
     * 
     * @param  int $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setRegionalId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->regional_id !== $v) {
            $this->regional_id = $v;
            $this->modifiedColumns[] = RegionalPeer::REGIONAL_ID;
        }


        return $this;
    } // setRegionalId()

    /**
     * Set the value of [ciudad_id] column.
     * 
     * @param  int $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setCiudadId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->ciudad_id !== $v) {
            $this->ciudad_id = $v;
            $this->modifiedColumns[] = RegionalPeer::CIUDAD_ID;
        }

        if ($this->aCiudad !== null && $this->aCiudad->getCiudadId() !== $v) {
            $this->aCiudad = null;
        }


        return $this;
    } // setCiudadId()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = RegionalPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Set the value of [direccion] column.
     * 
     * @param  string $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setDireccion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->direccion !== $v) {
            $this->direccion = $v;
            $this->modifiedColumns[] = RegionalPeer::DIRECCION;
        }


        return $this;
    } // setDireccion()

    /**
     * Set the value of [entidad_id] column.
     * 
     * @param  int $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setEntidadId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->entidad_id !== $v) {
            $this->entidad_id = $v;
            $this->modifiedColumns[] = RegionalPeer::ENTIDAD_ID;
        }

        if ($this->aEntidad !== null && $this->aEntidad->getEntidadId() !== $v) {
            $this->aEntidad = null;
        }


        return $this;
    } // setEntidadId()

    /**
     * Sets the value of the [es_visible] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * 
     * @param boolean|integer|string $v The new value
     * @return Regional The current object (for fluent API support)
     */
    public function setEsVisible($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->es_visible !== $v) {
            $this->es_visible = $v;
            $this->modifiedColumns[] = RegionalPeer::ES_VISIBLE;
        }


        return $this;
    } // setEsVisible()

    /**
     * Set the value of [directorio_name] column.
     * 
     * @param  string $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setDirectorioName($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->directorio_name !== $v) {
            $this->directorio_name = $v;
            $this->modifiedColumns[] = RegionalPeer::DIRECTORIO_NAME;
        }


        return $this;
    } // setDirectorioName()

    /**
     * Set the value of [image_membrete] column.
     * 
     * @param  string $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setImageMembrete($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->image_membrete !== $v) {
            $this->image_membrete = $v;
            $this->modifiedColumns[] = RegionalPeer::IMAGE_MEMBRETE;
        }


        return $this;
    } // setImageMembrete()

    /**
     * Set the value of [codigo] column.
     * 
     * @param  string $v new value
     * @return Regional The current object (for fluent API support)
     */
    public function setCodigo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->codigo !== $v) {
            $this->codigo = $v;
            $this->modifiedColumns[] = RegionalPeer::CODIGO;
        }


        return $this;
    } // setCodigo()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->entidad_id !== 0) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->regional_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->ciudad_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->descripcion = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->direccion = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->entidad_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->es_visible = ($row[$startcol + 5] !== null) ? (boolean) $row[$startcol + 5] : null;
            $this->directorio_name = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->image_membrete = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->codigo = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 9; // 9 = RegionalPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Regional object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aCiudad !== null && $this->ciudad_id !== $this->aCiudad->getCiudadId()) {
            $this->aCiudad = null;
        }
        if ($this->aEntidad !== null && $this->entidad_id !== $this->aEntidad->getEntidadId()) {
            $this->aEntidad = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RegionalPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = RegionalPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aCiudad = null;
            $this->aEntidad = null;
            $this->collDocPrestamos = null;

            $this->collReceptors = null;

            $this->collWfFlujos = null;

            $this->collPlantillasComs = null;

            $this->collPrestamos = null;

            $this->collServicios = null;

            $this->collFactHists = null;

            $this->collClPrestamos = null;

            $this->collFacturas = null;

            $this->collFacturaReceptors = null;

            $this->collComEnviadas = null;

            $this->collComInternas = null;

            $this->collComPermisoRegionals = null;

            $this->collComRecibidas = null;

            $this->collHistUsuarios = null;

            $this->collUnidadDocumentals = null;

            $this->collUsuarios = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RegionalPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = RegionalQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseRegional:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseRegional:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RegionalPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseRegional:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseRegional:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                RegionalPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aCiudad !== null) {
                if ($this->aCiudad->isModified() || $this->aCiudad->isNew()) {
                    $affectedRows += $this->aCiudad->save($con);
                }
                $this->setCiudad($this->aCiudad);
            }

            if ($this->aEntidad !== null) {
                if ($this->aEntidad->isModified() || $this->aEntidad->isNew()) {
                    $affectedRows += $this->aEntidad->save($con);
                }
                $this->setEntidad($this->aEntidad);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->docPrestamosScheduledForDeletion !== null) {
                if (!$this->docPrestamosScheduledForDeletion->isEmpty()) {
                    DocPrestamoQuery::create()
                        ->filterByPrimaryKeys($this->docPrestamosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->docPrestamosScheduledForDeletion = null;
                }
            }

            if ($this->collDocPrestamos !== null) {
                foreach ($this->collDocPrestamos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->receptorsScheduledForDeletion !== null) {
                if (!$this->receptorsScheduledForDeletion->isEmpty()) {
                    foreach ($this->receptorsScheduledForDeletion as $receptor) {
                        // need to save related object because we set the relation to null
                        $receptor->save($con);
                    }
                    $this->receptorsScheduledForDeletion = null;
                }
            }

            if ($this->collReceptors !== null) {
                foreach ($this->collReceptors as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->wfFlujosScheduledForDeletion !== null) {
                if (!$this->wfFlujosScheduledForDeletion->isEmpty()) {
                    foreach ($this->wfFlujosScheduledForDeletion as $wfFlujo) {
                        // need to save related object because we set the relation to null
                        $wfFlujo->save($con);
                    }
                    $this->wfFlujosScheduledForDeletion = null;
                }
            }

            if ($this->collWfFlujos !== null) {
                foreach ($this->collWfFlujos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->plantillasComsScheduledForDeletion !== null) {
                if (!$this->plantillasComsScheduledForDeletion->isEmpty()) {
                    foreach ($this->plantillasComsScheduledForDeletion as $plantillasCom) {
                        // need to save related object because we set the relation to null
                        $plantillasCom->save($con);
                    }
                    $this->plantillasComsScheduledForDeletion = null;
                }
            }

            if ($this->collPlantillasComs !== null) {
                foreach ($this->collPlantillasComs as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->prestamosScheduledForDeletion !== null) {
                if (!$this->prestamosScheduledForDeletion->isEmpty()) {
                    PrestamoQuery::create()
                        ->filterByPrimaryKeys($this->prestamosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->prestamosScheduledForDeletion = null;
                }
            }

            if ($this->collPrestamos !== null) {
                foreach ($this->collPrestamos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->serviciosScheduledForDeletion !== null) {
                if (!$this->serviciosScheduledForDeletion->isEmpty()) {
                    ServicioQuery::create()
                        ->filterByPrimaryKeys($this->serviciosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->serviciosScheduledForDeletion = null;
                }
            }

            if ($this->collServicios !== null) {
                foreach ($this->collServicios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->factHistsScheduledForDeletion !== null) {
                if (!$this->factHistsScheduledForDeletion->isEmpty()) {
                    FactHistQuery::create()
                        ->filterByPrimaryKeys($this->factHistsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->factHistsScheduledForDeletion = null;
                }
            }

            if ($this->collFactHists !== null) {
                foreach ($this->collFactHists as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->clPrestamosScheduledForDeletion !== null) {
                if (!$this->clPrestamosScheduledForDeletion->isEmpty()) {
                    ClPrestamoQuery::create()
                        ->filterByPrimaryKeys($this->clPrestamosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->clPrestamosScheduledForDeletion = null;
                }
            }

            if ($this->collClPrestamos !== null) {
                foreach ($this->collClPrestamos as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->facturasScheduledForDeletion !== null) {
                if (!$this->facturasScheduledForDeletion->isEmpty()) {
                    FacturaQuery::create()
                        ->filterByPrimaryKeys($this->facturasScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->facturasScheduledForDeletion = null;
                }
            }

            if ($this->collFacturas !== null) {
                foreach ($this->collFacturas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->facturaReceptorsScheduledForDeletion !== null) {
                if (!$this->facturaReceptorsScheduledForDeletion->isEmpty()) {
                    FacturaReceptorQuery::create()
                        ->filterByPrimaryKeys($this->facturaReceptorsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->facturaReceptorsScheduledForDeletion = null;
                }
            }

            if ($this->collFacturaReceptors !== null) {
                foreach ($this->collFacturaReceptors as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->comEnviadasScheduledForDeletion !== null) {
                if (!$this->comEnviadasScheduledForDeletion->isEmpty()) {
                    ComEnviadaQuery::create()
                        ->filterByPrimaryKeys($this->comEnviadasScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->comEnviadasScheduledForDeletion = null;
                }
            }

            if ($this->collComEnviadas !== null) {
                foreach ($this->collComEnviadas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->comInternasScheduledForDeletion !== null) {
                if (!$this->comInternasScheduledForDeletion->isEmpty()) {
                    ComInternaQuery::create()
                        ->filterByPrimaryKeys($this->comInternasScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->comInternasScheduledForDeletion = null;
                }
            }

            if ($this->collComInternas !== null) {
                foreach ($this->collComInternas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->comPermisoRegionalsScheduledForDeletion !== null) {
                if (!$this->comPermisoRegionalsScheduledForDeletion->isEmpty()) {
                    ComPermisoRegionalQuery::create()
                        ->filterByPrimaryKeys($this->comPermisoRegionalsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->comPermisoRegionalsScheduledForDeletion = null;
                }
            }

            if ($this->collComPermisoRegionals !== null) {
                foreach ($this->collComPermisoRegionals as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->comRecibidasScheduledForDeletion !== null) {
                if (!$this->comRecibidasScheduledForDeletion->isEmpty()) {
                    ComRecibidaQuery::create()
                        ->filterByPrimaryKeys($this->comRecibidasScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->comRecibidasScheduledForDeletion = null;
                }
            }

            if ($this->collComRecibidas !== null) {
                foreach ($this->collComRecibidas as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->histUsuariosScheduledForDeletion !== null) {
                if (!$this->histUsuariosScheduledForDeletion->isEmpty()) {
                    HistUsuarioQuery::create()
                        ->filterByPrimaryKeys($this->histUsuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->histUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collHistUsuarios !== null) {
                foreach ($this->collHistUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->unidadDocumentalsScheduledForDeletion !== null) {
                if (!$this->unidadDocumentalsScheduledForDeletion->isEmpty()) {
                    UnidadDocumentalQuery::create()
                        ->filterByPrimaryKeys($this->unidadDocumentalsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->unidadDocumentalsScheduledForDeletion = null;
                }
            }

            if ($this->collUnidadDocumentals !== null) {
                foreach ($this->collUnidadDocumentals as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->usuariosScheduledForDeletion !== null) {
                if (!$this->usuariosScheduledForDeletion->isEmpty()) {
                    UsuarioQuery::create()
                        ->filterByPrimaryKeys($this->usuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->usuariosScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarios !== null) {
                foreach ($this->collUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = RegionalPeer::REGIONAL_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(RegionalPeer::REGIONAL_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . RegionalPeer::REGIONAL_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(RegionalPeer::REGIONAL_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setRegionalId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aCiudad !== null) {
                if (!$this->aCiudad->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aCiudad->getValidationFailures());
                }
            }

            if ($this->aEntidad !== null) {
                if (!$this->aEntidad->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEntidad->getValidationFailures());
                }
            }


            if (($retval = RegionalPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collDocPrestamos !== null) {
                    foreach ($this->collDocPrestamos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collReceptors !== null) {
                    foreach ($this->collReceptors as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collWfFlujos !== null) {
                    foreach ($this->collWfFlujos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collPlantillasComs !== null) {
                    foreach ($this->collPlantillasComs as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collPrestamos !== null) {
                    foreach ($this->collPrestamos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collServicios !== null) {
                    foreach ($this->collServicios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFactHists !== null) {
                    foreach ($this->collFactHists as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collClPrestamos !== null) {
                    foreach ($this->collClPrestamos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFacturas !== null) {
                    foreach ($this->collFacturas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collFacturaReceptors !== null) {
                    foreach ($this->collFacturaReceptors as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collComEnviadas !== null) {
                    foreach ($this->collComEnviadas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collComInternas !== null) {
                    foreach ($this->collComInternas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collComPermisoRegionals !== null) {
                    foreach ($this->collComPermisoRegionals as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collComRecibidas !== null) {
                    foreach ($this->collComRecibidas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collHistUsuarios !== null) {
                    foreach ($this->collHistUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collUnidadDocumentals !== null) {
                    foreach ($this->collUnidadDocumentals as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collUsuarios !== null) {
                    foreach ($this->collUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = RegionalPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getRegionalId();
                break;
            case 1:
                return $this->getCiudadId();
                break;
            case 2:
                return $this->getDescripcion();
                break;
            case 3:
                return $this->getDireccion();
                break;
            case 4:
                return $this->getEntidadId();
                break;
            case 5:
                return $this->getEsVisible();
                break;
            case 6:
                return $this->getDirectorioName();
                break;
            case 7:
                return $this->getImageMembrete();
                break;
            case 8:
                return $this->getCodigo();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Regional'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Regional'][$this->getPrimaryKey()] = true;
        $keys = RegionalPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getRegionalId(),
            $keys[1] => $this->getCiudadId(),
            $keys[2] => $this->getDescripcion(),
            $keys[3] => $this->getDireccion(),
            $keys[4] => $this->getEntidadId(),
            $keys[5] => $this->getEsVisible(),
            $keys[6] => $this->getDirectorioName(),
            $keys[7] => $this->getImageMembrete(),
            $keys[8] => $this->getCodigo(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aCiudad) {
                $result['Ciudad'] = $this->aCiudad->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aEntidad) {
                $result['Entidad'] = $this->aEntidad->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collDocPrestamos) {
                $result['DocPrestamos'] = $this->collDocPrestamos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collReceptors) {
                $result['Receptors'] = $this->collReceptors->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collWfFlujos) {
                $result['WfFlujos'] = $this->collWfFlujos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collPlantillasComs) {
                $result['PlantillasComs'] = $this->collPlantillasComs->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collPrestamos) {
                $result['Prestamos'] = $this->collPrestamos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collServicios) {
                $result['Servicios'] = $this->collServicios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFactHists) {
                $result['FactHists'] = $this->collFactHists->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collClPrestamos) {
                $result['ClPrestamos'] = $this->collClPrestamos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFacturas) {
                $result['Facturas'] = $this->collFacturas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collFacturaReceptors) {
                $result['FacturaReceptors'] = $this->collFacturaReceptors->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collComEnviadas) {
                $result['ComEnviadas'] = $this->collComEnviadas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collComInternas) {
                $result['ComInternas'] = $this->collComInternas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collComPermisoRegionals) {
                $result['ComPermisoRegionals'] = $this->collComPermisoRegionals->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collComRecibidas) {
                $result['ComRecibidas'] = $this->collComRecibidas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collHistUsuarios) {
                $result['HistUsuarios'] = $this->collHistUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collUnidadDocumentals) {
                $result['UnidadDocumentals'] = $this->collUnidadDocumentals->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collUsuarios) {
                $result['Usuarios'] = $this->collUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = RegionalPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setRegionalId($value);
                break;
            case 1:
                $this->setCiudadId($value);
                break;
            case 2:
                $this->setDescripcion($value);
                break;
            case 3:
                $this->setDireccion($value);
                break;
            case 4:
                $this->setEntidadId($value);
                break;
            case 5:
                $this->setEsVisible($value);
                break;
            case 6:
                $this->setDirectorioName($value);
                break;
            case 7:
                $this->setImageMembrete($value);
                break;
            case 8:
                $this->setCodigo($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = RegionalPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setRegionalId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setCiudadId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setDescripcion($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setDireccion($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setEntidadId($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setEsVisible($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setDirectorioName($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setImageMembrete($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setCodigo($arr[$keys[8]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(RegionalPeer::DATABASE_NAME);

        if ($this->isColumnModified(RegionalPeer::REGIONAL_ID)) $criteria->add(RegionalPeer::REGIONAL_ID, $this->regional_id);
        if ($this->isColumnModified(RegionalPeer::CIUDAD_ID)) $criteria->add(RegionalPeer::CIUDAD_ID, $this->ciudad_id);
        if ($this->isColumnModified(RegionalPeer::DESCRIPCION)) $criteria->add(RegionalPeer::DESCRIPCION, $this->descripcion);
        if ($this->isColumnModified(RegionalPeer::DIRECCION)) $criteria->add(RegionalPeer::DIRECCION, $this->direccion);
        if ($this->isColumnModified(RegionalPeer::ENTIDAD_ID)) $criteria->add(RegionalPeer::ENTIDAD_ID, $this->entidad_id);
        if ($this->isColumnModified(RegionalPeer::ES_VISIBLE)) $criteria->add(RegionalPeer::ES_VISIBLE, $this->es_visible);
        if ($this->isColumnModified(RegionalPeer::DIRECTORIO_NAME)) $criteria->add(RegionalPeer::DIRECTORIO_NAME, $this->directorio_name);
        if ($this->isColumnModified(RegionalPeer::IMAGE_MEMBRETE)) $criteria->add(RegionalPeer::IMAGE_MEMBRETE, $this->image_membrete);
        if ($this->isColumnModified(RegionalPeer::CODIGO)) $criteria->add(RegionalPeer::CODIGO, $this->codigo);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(RegionalPeer::DATABASE_NAME);
        $criteria->add(RegionalPeer::REGIONAL_ID, $this->regional_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getRegionalId();
    }

    /**
     * Generic method to set the primary key (regional_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setRegionalId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getRegionalId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Regional (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setCiudadId($this->getCiudadId());
        $copyObj->setDescripcion($this->getDescripcion());
        $copyObj->setDireccion($this->getDireccion());
        $copyObj->setEntidadId($this->getEntidadId());
        $copyObj->setEsVisible($this->getEsVisible());
        $copyObj->setDirectorioName($this->getDirectorioName());
        $copyObj->setImageMembrete($this->getImageMembrete());
        $copyObj->setCodigo($this->getCodigo());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getDocPrestamos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addDocPrestamo($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getReceptors() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addReceptor($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getWfFlujos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addWfFlujo($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getPlantillasComs() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addPlantillasCom($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getPrestamos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addPrestamo($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getServicios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addServicio($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFactHists() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactHist($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getClPrestamos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addClPrestamo($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFacturas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFactura($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getFacturaReceptors() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addFacturaReceptor($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getComEnviadas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addComEnviada($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getComInternas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addComInterna($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getComPermisoRegionals() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addComPermisoRegional($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getComRecibidas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addComRecibida($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getHistUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addHistUsuario($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getUnidadDocumentals() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUnidadDocumental($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuario($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setRegionalId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Regional Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return RegionalPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new RegionalPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Ciudad object.
     *
     * @param                  Ciudad $v
     * @return Regional The current object (for fluent API support)
     * @throws PropelException
     */
    public function setCiudad(Ciudad $v = null)
    {
        if ($v === null) {
            $this->setCiudadId(NULL);
        } else {
            $this->setCiudadId($v->getCiudadId());
        }

        $this->aCiudad = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Ciudad object, it will not be re-added.
        if ($v !== null) {
            $v->addRegional($this);
        }


        return $this;
    }


    /**
     * Get the associated Ciudad object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Ciudad The associated Ciudad object.
     * @throws PropelException
     */
    public function getCiudad(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aCiudad === null && ($this->ciudad_id !== null) && $doQuery) {
            $this->aCiudad = CiudadQuery::create()->findPk($this->ciudad_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aCiudad->addRegionals($this);
             */
        }

        return $this->aCiudad;
    }

    /**
     * Declares an association between this object and a Entidad object.
     *
     * @param                  Entidad $v
     * @return Regional The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEntidad(Entidad $v = null)
    {
        if ($v === null) {
            $this->setEntidadId(0);
        } else {
            $this->setEntidadId($v->getEntidadId());
        }

        $this->aEntidad = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Entidad object, it will not be re-added.
        if ($v !== null) {
            $v->addRegional($this);
        }


        return $this;
    }


    /**
     * Get the associated Entidad object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Entidad The associated Entidad object.
     * @throws PropelException
     */
    public function getEntidad(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEntidad === null && ($this->entidad_id !== null) && $doQuery) {
            $this->aEntidad = EntidadQuery::create()->findPk($this->entidad_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEntidad->addRegionals($this);
             */
        }

        return $this->aEntidad;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('DocPrestamo' == $relationName) {
            $this->initDocPrestamos();
        }
        if ('Receptor' == $relationName) {
            $this->initReceptors();
        }
        if ('WfFlujo' == $relationName) {
            $this->initWfFlujos();
        }
        if ('PlantillasCom' == $relationName) {
            $this->initPlantillasComs();
        }
        if ('Prestamo' == $relationName) {
            $this->initPrestamos();
        }
        if ('Servicio' == $relationName) {
            $this->initServicios();
        }
        if ('FactHist' == $relationName) {
            $this->initFactHists();
        }
        if ('ClPrestamo' == $relationName) {
            $this->initClPrestamos();
        }
        if ('Factura' == $relationName) {
            $this->initFacturas();
        }
        if ('FacturaReceptor' == $relationName) {
            $this->initFacturaReceptors();
        }
        if ('ComEnviada' == $relationName) {
            $this->initComEnviadas();
        }
        if ('ComInterna' == $relationName) {
            $this->initComInternas();
        }
        if ('ComPermisoRegional' == $relationName) {
            $this->initComPermisoRegionals();
        }
        if ('ComRecibida' == $relationName) {
            $this->initComRecibidas();
        }
        if ('HistUsuario' == $relationName) {
            $this->initHistUsuarios();
        }
        if ('UnidadDocumental' == $relationName) {
            $this->initUnidadDocumentals();
        }
        if ('Usuario' == $relationName) {
            $this->initUsuarios();
        }
    }

    /**
     * Clears out the collDocPrestamos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addDocPrestamos()
     */
    public function clearDocPrestamos()
    {
        $this->collDocPrestamos = null; // important to set this to null since that means it is uninitialized
        $this->collDocPrestamosPartial = null;

        return $this;
    }

    /**
     * reset is the collDocPrestamos collection loaded partially
     *
     * @return void
     */
    public function resetPartialDocPrestamos($v = true)
    {
        $this->collDocPrestamosPartial = $v;
    }

    /**
     * Initializes the collDocPrestamos collection.
     *
     * By default this just sets the collDocPrestamos collection to an empty array (like clearcollDocPrestamos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initDocPrestamos($overrideExisting = true)
    {
        if (null !== $this->collDocPrestamos && !$overrideExisting) {
            return;
        }
        $this->collDocPrestamos = new PropelObjectCollection();
        $this->collDocPrestamos->setModel('DocPrestamo');
    }

    /**
     * Gets an array of DocPrestamo objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|DocPrestamo[] List of DocPrestamo objects
     * @throws PropelException
     */
    public function getDocPrestamos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collDocPrestamosPartial && !$this->isNew();
        if (null === $this->collDocPrestamos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collDocPrestamos) {
                // return empty collection
                $this->initDocPrestamos();
            } else {
                $collDocPrestamos = DocPrestamoQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collDocPrestamosPartial && count($collDocPrestamos)) {
                      $this->initDocPrestamos(false);

                      foreach ($collDocPrestamos as $obj) {
                        if (false == $this->collDocPrestamos->contains($obj)) {
                          $this->collDocPrestamos->append($obj);
                        }
                      }

                      $this->collDocPrestamosPartial = true;
                    }

                    $collDocPrestamos->getInternalIterator()->rewind();

                    return $collDocPrestamos;
                }

                if ($partial && $this->collDocPrestamos) {
                    foreach ($this->collDocPrestamos as $obj) {
                        if ($obj->isNew()) {
                            $collDocPrestamos[] = $obj;
                        }
                    }
                }

                $this->collDocPrestamos = $collDocPrestamos;
                $this->collDocPrestamosPartial = false;
            }
        }

        return $this->collDocPrestamos;
    }

    /**
     * Sets a collection of DocPrestamo objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $docPrestamos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setDocPrestamos(PropelCollection $docPrestamos, PropelPDO $con = null)
    {
        $docPrestamosToDelete = $this->getDocPrestamos(new Criteria(), $con)->diff($docPrestamos);


        $this->docPrestamosScheduledForDeletion = $docPrestamosToDelete;

        foreach ($docPrestamosToDelete as $docPrestamoRemoved) {
            $docPrestamoRemoved->setRegional(null);
        }

        $this->collDocPrestamos = null;
        foreach ($docPrestamos as $docPrestamo) {
            $this->addDocPrestamo($docPrestamo);
        }

        $this->collDocPrestamos = $docPrestamos;
        $this->collDocPrestamosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related DocPrestamo objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related DocPrestamo objects.
     * @throws PropelException
     */
    public function countDocPrestamos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collDocPrestamosPartial && !$this->isNew();
        if (null === $this->collDocPrestamos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collDocPrestamos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getDocPrestamos());
            }
            $query = DocPrestamoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collDocPrestamos);
    }

    /**
     * Method called to associate a DocPrestamo object to this object
     * through the DocPrestamo foreign key attribute.
     *
     * @param    DocPrestamo $l DocPrestamo
     * @return Regional The current object (for fluent API support)
     */
    public function addDocPrestamo(DocPrestamo $l)
    {
        if ($this->collDocPrestamos === null) {
            $this->initDocPrestamos();
            $this->collDocPrestamosPartial = true;
        }

        if (!in_array($l, $this->collDocPrestamos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddDocPrestamo($l);

            if ($this->docPrestamosScheduledForDeletion and $this->docPrestamosScheduledForDeletion->contains($l)) {
                $this->docPrestamosScheduledForDeletion->remove($this->docPrestamosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	DocPrestamo $docPrestamo The docPrestamo object to add.
     */
    protected function doAddDocPrestamo($docPrestamo)
    {
        $this->collDocPrestamos[]= $docPrestamo;
        $docPrestamo->setRegional($this);
    }

    /**
     * @param	DocPrestamo $docPrestamo The docPrestamo object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeDocPrestamo($docPrestamo)
    {
        if ($this->getDocPrestamos()->contains($docPrestamo)) {
            $this->collDocPrestamos->remove($this->collDocPrestamos->search($docPrestamo));
            if (null === $this->docPrestamosScheduledForDeletion) {
                $this->docPrestamosScheduledForDeletion = clone $this->collDocPrestamos;
                $this->docPrestamosScheduledForDeletion->clear();
            }
            $this->docPrestamosScheduledForDeletion[]= clone $docPrestamo;
            $docPrestamo->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related DocPrestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|DocPrestamo[] List of DocPrestamo objects
     */
    public function getDocPrestamosJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DocPrestamoQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getDocPrestamos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related DocPrestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|DocPrestamo[] List of DocPrestamo objects
     */
    public function getDocPrestamosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DocPrestamoQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getDocPrestamos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related DocPrestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|DocPrestamo[] List of DocPrestamo objects
     */
    public function getDocPrestamosJoinDocEstadoPrestamo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DocPrestamoQuery::create(null, $criteria);
        $query->joinWith('DocEstadoPrestamo', $join_behavior);

        return $this->getDocPrestamos($query, $con);
    }

    /**
     * Clears out the collReceptors collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addReceptors()
     */
    public function clearReceptors()
    {
        $this->collReceptors = null; // important to set this to null since that means it is uninitialized
        $this->collReceptorsPartial = null;

        return $this;
    }

    /**
     * reset is the collReceptors collection loaded partially
     *
     * @return void
     */
    public function resetPartialReceptors($v = true)
    {
        $this->collReceptorsPartial = $v;
    }

    /**
     * Initializes the collReceptors collection.
     *
     * By default this just sets the collReceptors collection to an empty array (like clearcollReceptors());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initReceptors($overrideExisting = true)
    {
        if (null !== $this->collReceptors && !$overrideExisting) {
            return;
        }
        $this->collReceptors = new PropelObjectCollection();
        $this->collReceptors->setModel('Receptor');
    }

    /**
     * Gets an array of Receptor objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Receptor[] List of Receptor objects
     * @throws PropelException
     */
    public function getReceptors($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collReceptorsPartial && !$this->isNew();
        if (null === $this->collReceptors || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collReceptors) {
                // return empty collection
                $this->initReceptors();
            } else {
                $collReceptors = ReceptorQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collReceptorsPartial && count($collReceptors)) {
                      $this->initReceptors(false);

                      foreach ($collReceptors as $obj) {
                        if (false == $this->collReceptors->contains($obj)) {
                          $this->collReceptors->append($obj);
                        }
                      }

                      $this->collReceptorsPartial = true;
                    }

                    $collReceptors->getInternalIterator()->rewind();

                    return $collReceptors;
                }

                if ($partial && $this->collReceptors) {
                    foreach ($this->collReceptors as $obj) {
                        if ($obj->isNew()) {
                            $collReceptors[] = $obj;
                        }
                    }
                }

                $this->collReceptors = $collReceptors;
                $this->collReceptorsPartial = false;
            }
        }

        return $this->collReceptors;
    }

    /**
     * Sets a collection of Receptor objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $receptors A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setReceptors(PropelCollection $receptors, PropelPDO $con = null)
    {
        $receptorsToDelete = $this->getReceptors(new Criteria(), $con)->diff($receptors);


        $this->receptorsScheduledForDeletion = $receptorsToDelete;

        foreach ($receptorsToDelete as $receptorRemoved) {
            $receptorRemoved->setRegional(null);
        }

        $this->collReceptors = null;
        foreach ($receptors as $receptor) {
            $this->addReceptor($receptor);
        }

        $this->collReceptors = $receptors;
        $this->collReceptorsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Receptor objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Receptor objects.
     * @throws PropelException
     */
    public function countReceptors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collReceptorsPartial && !$this->isNew();
        if (null === $this->collReceptors || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collReceptors) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getReceptors());
            }
            $query = ReceptorQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collReceptors);
    }

    /**
     * Method called to associate a Receptor object to this object
     * through the Receptor foreign key attribute.
     *
     * @param    Receptor $l Receptor
     * @return Regional The current object (for fluent API support)
     */
    public function addReceptor(Receptor $l)
    {
        if ($this->collReceptors === null) {
            $this->initReceptors();
            $this->collReceptorsPartial = true;
        }

        if (!in_array($l, $this->collReceptors->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddReceptor($l);

            if ($this->receptorsScheduledForDeletion and $this->receptorsScheduledForDeletion->contains($l)) {
                $this->receptorsScheduledForDeletion->remove($this->receptorsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Receptor $receptor The receptor object to add.
     */
    protected function doAddReceptor($receptor)
    {
        $this->collReceptors[]= $receptor;
        $receptor->setRegional($this);
    }

    /**
     * @param	Receptor $receptor The receptor object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeReceptor($receptor)
    {
        if ($this->getReceptors()->contains($receptor)) {
            $this->collReceptors->remove($this->collReceptors->search($receptor));
            if (null === $this->receptorsScheduledForDeletion) {
                $this->receptorsScheduledForDeletion = clone $this->collReceptors;
                $this->receptorsScheduledForDeletion->clear();
            }
            $this->receptorsScheduledForDeletion[]= $receptor;
            $receptor->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Receptors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Receptor[] List of Receptor objects
     */
    public function getReceptorsJoinModulo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ReceptorQuery::create(null, $criteria);
        $query->joinWith('Modulo', $join_behavior);

        return $this->getReceptors($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Receptors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Receptor[] List of Receptor objects
     */
    public function getReceptorsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ReceptorQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getReceptors($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Receptors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Receptor[] List of Receptor objects
     */
    public function getReceptorsJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ReceptorQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getReceptors($query, $con);
    }

    /**
     * Clears out the collWfFlujos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addWfFlujos()
     */
    public function clearWfFlujos()
    {
        $this->collWfFlujos = null; // important to set this to null since that means it is uninitialized
        $this->collWfFlujosPartial = null;

        return $this;
    }

    /**
     * reset is the collWfFlujos collection loaded partially
     *
     * @return void
     */
    public function resetPartialWfFlujos($v = true)
    {
        $this->collWfFlujosPartial = $v;
    }

    /**
     * Initializes the collWfFlujos collection.
     *
     * By default this just sets the collWfFlujos collection to an empty array (like clearcollWfFlujos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initWfFlujos($overrideExisting = true)
    {
        if (null !== $this->collWfFlujos && !$overrideExisting) {
            return;
        }
        $this->collWfFlujos = new PropelObjectCollection();
        $this->collWfFlujos->setModel('WfFlujo');
    }

    /**
     * Gets an array of WfFlujo objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|WfFlujo[] List of WfFlujo objects
     * @throws PropelException
     */
    public function getWfFlujos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collWfFlujosPartial && !$this->isNew();
        if (null === $this->collWfFlujos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collWfFlujos) {
                // return empty collection
                $this->initWfFlujos();
            } else {
                $collWfFlujos = WfFlujoQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collWfFlujosPartial && count($collWfFlujos)) {
                      $this->initWfFlujos(false);

                      foreach ($collWfFlujos as $obj) {
                        if (false == $this->collWfFlujos->contains($obj)) {
                          $this->collWfFlujos->append($obj);
                        }
                      }

                      $this->collWfFlujosPartial = true;
                    }

                    $collWfFlujos->getInternalIterator()->rewind();

                    return $collWfFlujos;
                }

                if ($partial && $this->collWfFlujos) {
                    foreach ($this->collWfFlujos as $obj) {
                        if ($obj->isNew()) {
                            $collWfFlujos[] = $obj;
                        }
                    }
                }

                $this->collWfFlujos = $collWfFlujos;
                $this->collWfFlujosPartial = false;
            }
        }

        return $this->collWfFlujos;
    }

    /**
     * Sets a collection of WfFlujo objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $wfFlujos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setWfFlujos(PropelCollection $wfFlujos, PropelPDO $con = null)
    {
        $wfFlujosToDelete = $this->getWfFlujos(new Criteria(), $con)->diff($wfFlujos);


        $this->wfFlujosScheduledForDeletion = $wfFlujosToDelete;

        foreach ($wfFlujosToDelete as $wfFlujoRemoved) {
            $wfFlujoRemoved->setRegional(null);
        }

        $this->collWfFlujos = null;
        foreach ($wfFlujos as $wfFlujo) {
            $this->addWfFlujo($wfFlujo);
        }

        $this->collWfFlujos = $wfFlujos;
        $this->collWfFlujosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related WfFlujo objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related WfFlujo objects.
     * @throws PropelException
     */
    public function countWfFlujos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collWfFlujosPartial && !$this->isNew();
        if (null === $this->collWfFlujos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collWfFlujos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getWfFlujos());
            }
            $query = WfFlujoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collWfFlujos);
    }

    /**
     * Method called to associate a WfFlujo object to this object
     * through the WfFlujo foreign key attribute.
     *
     * @param    WfFlujo $l WfFlujo
     * @return Regional The current object (for fluent API support)
     */
    public function addWfFlujo(WfFlujo $l)
    {
        if ($this->collWfFlujos === null) {
            $this->initWfFlujos();
            $this->collWfFlujosPartial = true;
        }

        if (!in_array($l, $this->collWfFlujos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddWfFlujo($l);

            if ($this->wfFlujosScheduledForDeletion and $this->wfFlujosScheduledForDeletion->contains($l)) {
                $this->wfFlujosScheduledForDeletion->remove($this->wfFlujosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	WfFlujo $wfFlujo The wfFlujo object to add.
     */
    protected function doAddWfFlujo($wfFlujo)
    {
        $this->collWfFlujos[]= $wfFlujo;
        $wfFlujo->setRegional($this);
    }

    /**
     * @param	WfFlujo $wfFlujo The wfFlujo object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeWfFlujo($wfFlujo)
    {
        if ($this->getWfFlujos()->contains($wfFlujo)) {
            $this->collWfFlujos->remove($this->collWfFlujos->search($wfFlujo));
            if (null === $this->wfFlujosScheduledForDeletion) {
                $this->wfFlujosScheduledForDeletion = clone $this->collWfFlujos;
                $this->wfFlujosScheduledForDeletion->clear();
            }
            $this->wfFlujosScheduledForDeletion[]= $wfFlujo;
            $wfFlujo->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related WfFlujos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|WfFlujo[] List of WfFlujo objects
     */
    public function getWfFlujosJoinTipoComRecibida($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = WfFlujoQuery::create(null, $criteria);
        $query->joinWith('TipoComRecibida', $join_behavior);

        return $this->getWfFlujos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related WfFlujos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|WfFlujo[] List of WfFlujo objects
     */
    public function getWfFlujosJoinTipoComInterna($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = WfFlujoQuery::create(null, $criteria);
        $query->joinWith('TipoComInterna', $join_behavior);

        return $this->getWfFlujos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related WfFlujos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|WfFlujo[] List of WfFlujo objects
     */
    public function getWfFlujosJoinWfBuzon($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = WfFlujoQuery::create(null, $criteria);
        $query->joinWith('WfBuzon', $join_behavior);

        return $this->getWfFlujos($query, $con);
    }

    /**
     * Clears out the collPlantillasComs collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addPlantillasComs()
     */
    public function clearPlantillasComs()
    {
        $this->collPlantillasComs = null; // important to set this to null since that means it is uninitialized
        $this->collPlantillasComsPartial = null;

        return $this;
    }

    /**
     * reset is the collPlantillasComs collection loaded partially
     *
     * @return void
     */
    public function resetPartialPlantillasComs($v = true)
    {
        $this->collPlantillasComsPartial = $v;
    }

    /**
     * Initializes the collPlantillasComs collection.
     *
     * By default this just sets the collPlantillasComs collection to an empty array (like clearcollPlantillasComs());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initPlantillasComs($overrideExisting = true)
    {
        if (null !== $this->collPlantillasComs && !$overrideExisting) {
            return;
        }
        $this->collPlantillasComs = new PropelObjectCollection();
        $this->collPlantillasComs->setModel('PlantillasCom');
    }

    /**
     * Gets an array of PlantillasCom objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|PlantillasCom[] List of PlantillasCom objects
     * @throws PropelException
     */
    public function getPlantillasComs($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collPlantillasComsPartial && !$this->isNew();
        if (null === $this->collPlantillasComs || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collPlantillasComs) {
                // return empty collection
                $this->initPlantillasComs();
            } else {
                $collPlantillasComs = PlantillasComQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collPlantillasComsPartial && count($collPlantillasComs)) {
                      $this->initPlantillasComs(false);

                      foreach ($collPlantillasComs as $obj) {
                        if (false == $this->collPlantillasComs->contains($obj)) {
                          $this->collPlantillasComs->append($obj);
                        }
                      }

                      $this->collPlantillasComsPartial = true;
                    }

                    $collPlantillasComs->getInternalIterator()->rewind();

                    return $collPlantillasComs;
                }

                if ($partial && $this->collPlantillasComs) {
                    foreach ($this->collPlantillasComs as $obj) {
                        if ($obj->isNew()) {
                            $collPlantillasComs[] = $obj;
                        }
                    }
                }

                $this->collPlantillasComs = $collPlantillasComs;
                $this->collPlantillasComsPartial = false;
            }
        }

        return $this->collPlantillasComs;
    }

    /**
     * Sets a collection of PlantillasCom objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $plantillasComs A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setPlantillasComs(PropelCollection $plantillasComs, PropelPDO $con = null)
    {
        $plantillasComsToDelete = $this->getPlantillasComs(new Criteria(), $con)->diff($plantillasComs);


        $this->plantillasComsScheduledForDeletion = $plantillasComsToDelete;

        foreach ($plantillasComsToDelete as $plantillasComRemoved) {
            $plantillasComRemoved->setRegional(null);
        }

        $this->collPlantillasComs = null;
        foreach ($plantillasComs as $plantillasCom) {
            $this->addPlantillasCom($plantillasCom);
        }

        $this->collPlantillasComs = $plantillasComs;
        $this->collPlantillasComsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related PlantillasCom objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related PlantillasCom objects.
     * @throws PropelException
     */
    public function countPlantillasComs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collPlantillasComsPartial && !$this->isNew();
        if (null === $this->collPlantillasComs || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collPlantillasComs) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getPlantillasComs());
            }
            $query = PlantillasComQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collPlantillasComs);
    }

    /**
     * Method called to associate a PlantillasCom object to this object
     * through the PlantillasCom foreign key attribute.
     *
     * @param    PlantillasCom $l PlantillasCom
     * @return Regional The current object (for fluent API support)
     */
    public function addPlantillasCom(PlantillasCom $l)
    {
        if ($this->collPlantillasComs === null) {
            $this->initPlantillasComs();
            $this->collPlantillasComsPartial = true;
        }

        if (!in_array($l, $this->collPlantillasComs->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddPlantillasCom($l);

            if ($this->plantillasComsScheduledForDeletion and $this->plantillasComsScheduledForDeletion->contains($l)) {
                $this->plantillasComsScheduledForDeletion->remove($this->plantillasComsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	PlantillasCom $plantillasCom The plantillasCom object to add.
     */
    protected function doAddPlantillasCom($plantillasCom)
    {
        $this->collPlantillasComs[]= $plantillasCom;
        $plantillasCom->setRegional($this);
    }

    /**
     * @param	PlantillasCom $plantillasCom The plantillasCom object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removePlantillasCom($plantillasCom)
    {
        if ($this->getPlantillasComs()->contains($plantillasCom)) {
            $this->collPlantillasComs->remove($this->collPlantillasComs->search($plantillasCom));
            if (null === $this->plantillasComsScheduledForDeletion) {
                $this->plantillasComsScheduledForDeletion = clone $this->collPlantillasComs;
                $this->plantillasComsScheduledForDeletion->clear();
            }
            $this->plantillasComsScheduledForDeletion[]= $plantillasCom;
            $plantillasCom->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related PlantillasComs from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|PlantillasCom[] List of PlantillasCom objects
     */
    public function getPlantillasComsJoinModulo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PlantillasComQuery::create(null, $criteria);
        $query->joinWith('Modulo', $join_behavior);

        return $this->getPlantillasComs($query, $con);
    }

    /**
     * Clears out the collPrestamos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addPrestamos()
     */
    public function clearPrestamos()
    {
        $this->collPrestamos = null; // important to set this to null since that means it is uninitialized
        $this->collPrestamosPartial = null;

        return $this;
    }

    /**
     * reset is the collPrestamos collection loaded partially
     *
     * @return void
     */
    public function resetPartialPrestamos($v = true)
    {
        $this->collPrestamosPartial = $v;
    }

    /**
     * Initializes the collPrestamos collection.
     *
     * By default this just sets the collPrestamos collection to an empty array (like clearcollPrestamos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initPrestamos($overrideExisting = true)
    {
        if (null !== $this->collPrestamos && !$overrideExisting) {
            return;
        }
        $this->collPrestamos = new PropelObjectCollection();
        $this->collPrestamos->setModel('Prestamo');
    }

    /**
     * Gets an array of Prestamo objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Prestamo[] List of Prestamo objects
     * @throws PropelException
     */
    public function getPrestamos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collPrestamosPartial && !$this->isNew();
        if (null === $this->collPrestamos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collPrestamos) {
                // return empty collection
                $this->initPrestamos();
            } else {
                $collPrestamos = PrestamoQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collPrestamosPartial && count($collPrestamos)) {
                      $this->initPrestamos(false);

                      foreach ($collPrestamos as $obj) {
                        if (false == $this->collPrestamos->contains($obj)) {
                          $this->collPrestamos->append($obj);
                        }
                      }

                      $this->collPrestamosPartial = true;
                    }

                    $collPrestamos->getInternalIterator()->rewind();

                    return $collPrestamos;
                }

                if ($partial && $this->collPrestamos) {
                    foreach ($this->collPrestamos as $obj) {
                        if ($obj->isNew()) {
                            $collPrestamos[] = $obj;
                        }
                    }
                }

                $this->collPrestamos = $collPrestamos;
                $this->collPrestamosPartial = false;
            }
        }

        return $this->collPrestamos;
    }

    /**
     * Sets a collection of Prestamo objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $prestamos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setPrestamos(PropelCollection $prestamos, PropelPDO $con = null)
    {
        $prestamosToDelete = $this->getPrestamos(new Criteria(), $con)->diff($prestamos);


        $this->prestamosScheduledForDeletion = $prestamosToDelete;

        foreach ($prestamosToDelete as $prestamoRemoved) {
            $prestamoRemoved->setRegional(null);
        }

        $this->collPrestamos = null;
        foreach ($prestamos as $prestamo) {
            $this->addPrestamo($prestamo);
        }

        $this->collPrestamos = $prestamos;
        $this->collPrestamosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Prestamo objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Prestamo objects.
     * @throws PropelException
     */
    public function countPrestamos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collPrestamosPartial && !$this->isNew();
        if (null === $this->collPrestamos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collPrestamos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getPrestamos());
            }
            $query = PrestamoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collPrestamos);
    }

    /**
     * Method called to associate a Prestamo object to this object
     * through the Prestamo foreign key attribute.
     *
     * @param    Prestamo $l Prestamo
     * @return Regional The current object (for fluent API support)
     */
    public function addPrestamo(Prestamo $l)
    {
        if ($this->collPrestamos === null) {
            $this->initPrestamos();
            $this->collPrestamosPartial = true;
        }

        if (!in_array($l, $this->collPrestamos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddPrestamo($l);

            if ($this->prestamosScheduledForDeletion and $this->prestamosScheduledForDeletion->contains($l)) {
                $this->prestamosScheduledForDeletion->remove($this->prestamosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Prestamo $prestamo The prestamo object to add.
     */
    protected function doAddPrestamo($prestamo)
    {
        $this->collPrestamos[]= $prestamo;
        $prestamo->setRegional($this);
    }

    /**
     * @param	Prestamo $prestamo The prestamo object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removePrestamo($prestamo)
    {
        if ($this->getPrestamos()->contains($prestamo)) {
            $this->collPrestamos->remove($this->collPrestamos->search($prestamo));
            if (null === $this->prestamosScheduledForDeletion) {
                $this->prestamosScheduledForDeletion = clone $this->collPrestamos;
                $this->prestamosScheduledForDeletion->clear();
            }
            $this->prestamosScheduledForDeletion[]= clone $prestamo;
            $prestamo->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Prestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Prestamo[] List of Prestamo objects
     */
    public function getPrestamosJoinEstadoPrestamo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PrestamoQuery::create(null, $criteria);
        $query->joinWith('EstadoPrestamo', $join_behavior);

        return $this->getPrestamos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Prestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Prestamo[] List of Prestamo objects
     */
    public function getPrestamosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PrestamoQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getPrestamos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Prestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Prestamo[] List of Prestamo objects
     */
    public function getPrestamosJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PrestamoQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getPrestamos($query, $con);
    }

    /**
     * Clears out the collServicios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addServicios()
     */
    public function clearServicios()
    {
        $this->collServicios = null; // important to set this to null since that means it is uninitialized
        $this->collServiciosPartial = null;

        return $this;
    }

    /**
     * reset is the collServicios collection loaded partially
     *
     * @return void
     */
    public function resetPartialServicios($v = true)
    {
        $this->collServiciosPartial = $v;
    }

    /**
     * Initializes the collServicios collection.
     *
     * By default this just sets the collServicios collection to an empty array (like clearcollServicios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initServicios($overrideExisting = true)
    {
        if (null !== $this->collServicios && !$overrideExisting) {
            return;
        }
        $this->collServicios = new PropelObjectCollection();
        $this->collServicios->setModel('Servicio');
    }

    /**
     * Gets an array of Servicio objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     * @throws PropelException
     */
    public function getServicios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collServiciosPartial && !$this->isNew();
        if (null === $this->collServicios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collServicios) {
                // return empty collection
                $this->initServicios();
            } else {
                $collServicios = ServicioQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collServiciosPartial && count($collServicios)) {
                      $this->initServicios(false);

                      foreach ($collServicios as $obj) {
                        if (false == $this->collServicios->contains($obj)) {
                          $this->collServicios->append($obj);
                        }
                      }

                      $this->collServiciosPartial = true;
                    }

                    $collServicios->getInternalIterator()->rewind();

                    return $collServicios;
                }

                if ($partial && $this->collServicios) {
                    foreach ($this->collServicios as $obj) {
                        if ($obj->isNew()) {
                            $collServicios[] = $obj;
                        }
                    }
                }

                $this->collServicios = $collServicios;
                $this->collServiciosPartial = false;
            }
        }

        return $this->collServicios;
    }

    /**
     * Sets a collection of Servicio objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $servicios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setServicios(PropelCollection $servicios, PropelPDO $con = null)
    {
        $serviciosToDelete = $this->getServicios(new Criteria(), $con)->diff($servicios);


        $this->serviciosScheduledForDeletion = $serviciosToDelete;

        foreach ($serviciosToDelete as $servicioRemoved) {
            $servicioRemoved->setRegional(null);
        }

        $this->collServicios = null;
        foreach ($servicios as $servicio) {
            $this->addServicio($servicio);
        }

        $this->collServicios = $servicios;
        $this->collServiciosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Servicio objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Servicio objects.
     * @throws PropelException
     */
    public function countServicios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collServiciosPartial && !$this->isNew();
        if (null === $this->collServicios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collServicios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getServicios());
            }
            $query = ServicioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collServicios);
    }

    /**
     * Method called to associate a Servicio object to this object
     * through the Servicio foreign key attribute.
     *
     * @param    Servicio $l Servicio
     * @return Regional The current object (for fluent API support)
     */
    public function addServicio(Servicio $l)
    {
        if ($this->collServicios === null) {
            $this->initServicios();
            $this->collServiciosPartial = true;
        }

        if (!in_array($l, $this->collServicios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddServicio($l);

            if ($this->serviciosScheduledForDeletion and $this->serviciosScheduledForDeletion->contains($l)) {
                $this->serviciosScheduledForDeletion->remove($this->serviciosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Servicio $servicio The servicio object to add.
     */
    protected function doAddServicio($servicio)
    {
        $this->collServicios[]= $servicio;
        $servicio->setRegional($this);
    }

    /**
     * @param	Servicio $servicio The servicio object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeServicio($servicio)
    {
        if ($this->getServicios()->contains($servicio)) {
            $this->collServicios->remove($this->collServicios->search($servicio));
            if (null === $this->serviciosScheduledForDeletion) {
                $this->serviciosScheduledForDeletion = clone $this->collServicios;
                $this->serviciosScheduledForDeletion->clear();
            }
            $this->serviciosScheduledForDeletion[]= clone $servicio;
            $servicio->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinEmpresaMensajeria($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('EmpresaMensajeria', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinPrioridadSolicitudServicio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('PrioridadSolicitudServicio', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinDirectorioExterno($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('DirectorioExterno', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinTipoServicio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('TipoServicio', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinServicioEstado($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('ServicioEstado', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinServicioTipoDevolucion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('ServicioTipoDevolucion', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinModulo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('Modulo', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinMarcoNormativo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('MarcoNormativo', $join_behavior);

        return $this->getServicios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Servicios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Servicio[] List of Servicio objects
     */
    public function getServiciosJoinProcesodocOrigen($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ServicioQuery::create(null, $criteria);
        $query->joinWith('ProcesodocOrigen', $join_behavior);

        return $this->getServicios($query, $con);
    }

    /**
     * Clears out the collFactHists collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addFactHists()
     */
    public function clearFactHists()
    {
        $this->collFactHists = null; // important to set this to null since that means it is uninitialized
        $this->collFactHistsPartial = null;

        return $this;
    }

    /**
     * reset is the collFactHists collection loaded partially
     *
     * @return void
     */
    public function resetPartialFactHists($v = true)
    {
        $this->collFactHistsPartial = $v;
    }

    /**
     * Initializes the collFactHists collection.
     *
     * By default this just sets the collFactHists collection to an empty array (like clearcollFactHists());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFactHists($overrideExisting = true)
    {
        if (null !== $this->collFactHists && !$overrideExisting) {
            return;
        }
        $this->collFactHists = new PropelObjectCollection();
        $this->collFactHists->setModel('FactHist');
    }

    /**
     * Gets an array of FactHist objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FactHist[] List of FactHist objects
     * @throws PropelException
     */
    public function getFactHists($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFactHistsPartial && !$this->isNew();
        if (null === $this->collFactHists || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFactHists) {
                // return empty collection
                $this->initFactHists();
            } else {
                $collFactHists = FactHistQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFactHistsPartial && count($collFactHists)) {
                      $this->initFactHists(false);

                      foreach ($collFactHists as $obj) {
                        if (false == $this->collFactHists->contains($obj)) {
                          $this->collFactHists->append($obj);
                        }
                      }

                      $this->collFactHistsPartial = true;
                    }

                    $collFactHists->getInternalIterator()->rewind();

                    return $collFactHists;
                }

                if ($partial && $this->collFactHists) {
                    foreach ($this->collFactHists as $obj) {
                        if ($obj->isNew()) {
                            $collFactHists[] = $obj;
                        }
                    }
                }

                $this->collFactHists = $collFactHists;
                $this->collFactHistsPartial = false;
            }
        }

        return $this->collFactHists;
    }

    /**
     * Sets a collection of FactHist objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $factHists A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setFactHists(PropelCollection $factHists, PropelPDO $con = null)
    {
        $factHistsToDelete = $this->getFactHists(new Criteria(), $con)->diff($factHists);


        $this->factHistsScheduledForDeletion = $factHistsToDelete;

        foreach ($factHistsToDelete as $factHistRemoved) {
            $factHistRemoved->setRegional(null);
        }

        $this->collFactHists = null;
        foreach ($factHists as $factHist) {
            $this->addFactHist($factHist);
        }

        $this->collFactHists = $factHists;
        $this->collFactHistsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FactHist objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FactHist objects.
     * @throws PropelException
     */
    public function countFactHists(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFactHistsPartial && !$this->isNew();
        if (null === $this->collFactHists || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFactHists) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFactHists());
            }
            $query = FactHistQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collFactHists);
    }

    /**
     * Method called to associate a FactHist object to this object
     * through the FactHist foreign key attribute.
     *
     * @param    FactHist $l FactHist
     * @return Regional The current object (for fluent API support)
     */
    public function addFactHist(FactHist $l)
    {
        if ($this->collFactHists === null) {
            $this->initFactHists();
            $this->collFactHistsPartial = true;
        }

        if (!in_array($l, $this->collFactHists->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactHist($l);

            if ($this->factHistsScheduledForDeletion and $this->factHistsScheduledForDeletion->contains($l)) {
                $this->factHistsScheduledForDeletion->remove($this->factHistsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FactHist $factHist The factHist object to add.
     */
    protected function doAddFactHist($factHist)
    {
        $this->collFactHists[]= $factHist;
        $factHist->setRegional($this);
    }

    /**
     * @param	FactHist $factHist The factHist object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeFactHist($factHist)
    {
        if ($this->getFactHists()->contains($factHist)) {
            $this->collFactHists->remove($this->collFactHists->search($factHist));
            if (null === $this->factHistsScheduledForDeletion) {
                $this->factHistsScheduledForDeletion = clone $this->collFactHists;
                $this->factHistsScheduledForDeletion->clear();
            }
            $this->factHistsScheduledForDeletion[]= clone $factHist;
            $factHist->setRegional(null);
        }

        return $this;
    }

    /**
     * Clears out the collClPrestamos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addClPrestamos()
     */
    public function clearClPrestamos()
    {
        $this->collClPrestamos = null; // important to set this to null since that means it is uninitialized
        $this->collClPrestamosPartial = null;

        return $this;
    }

    /**
     * reset is the collClPrestamos collection loaded partially
     *
     * @return void
     */
    public function resetPartialClPrestamos($v = true)
    {
        $this->collClPrestamosPartial = $v;
    }

    /**
     * Initializes the collClPrestamos collection.
     *
     * By default this just sets the collClPrestamos collection to an empty array (like clearcollClPrestamos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initClPrestamos($overrideExisting = true)
    {
        if (null !== $this->collClPrestamos && !$overrideExisting) {
            return;
        }
        $this->collClPrestamos = new PropelObjectCollection();
        $this->collClPrestamos->setModel('ClPrestamo');
    }

    /**
     * Gets an array of ClPrestamo objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ClPrestamo[] List of ClPrestamo objects
     * @throws PropelException
     */
    public function getClPrestamos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collClPrestamosPartial && !$this->isNew();
        if (null === $this->collClPrestamos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collClPrestamos) {
                // return empty collection
                $this->initClPrestamos();
            } else {
                $collClPrestamos = ClPrestamoQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collClPrestamosPartial && count($collClPrestamos)) {
                      $this->initClPrestamos(false);

                      foreach ($collClPrestamos as $obj) {
                        if (false == $this->collClPrestamos->contains($obj)) {
                          $this->collClPrestamos->append($obj);
                        }
                      }

                      $this->collClPrestamosPartial = true;
                    }

                    $collClPrestamos->getInternalIterator()->rewind();

                    return $collClPrestamos;
                }

                if ($partial && $this->collClPrestamos) {
                    foreach ($this->collClPrestamos as $obj) {
                        if ($obj->isNew()) {
                            $collClPrestamos[] = $obj;
                        }
                    }
                }

                $this->collClPrestamos = $collClPrestamos;
                $this->collClPrestamosPartial = false;
            }
        }

        return $this->collClPrestamos;
    }

    /**
     * Sets a collection of ClPrestamo objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $clPrestamos A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setClPrestamos(PropelCollection $clPrestamos, PropelPDO $con = null)
    {
        $clPrestamosToDelete = $this->getClPrestamos(new Criteria(), $con)->diff($clPrestamos);


        $this->clPrestamosScheduledForDeletion = $clPrestamosToDelete;

        foreach ($clPrestamosToDelete as $clPrestamoRemoved) {
            $clPrestamoRemoved->setRegional(null);
        }

        $this->collClPrestamos = null;
        foreach ($clPrestamos as $clPrestamo) {
            $this->addClPrestamo($clPrestamo);
        }

        $this->collClPrestamos = $clPrestamos;
        $this->collClPrestamosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ClPrestamo objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ClPrestamo objects.
     * @throws PropelException
     */
    public function countClPrestamos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collClPrestamosPartial && !$this->isNew();
        if (null === $this->collClPrestamos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collClPrestamos) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getClPrestamos());
            }
            $query = ClPrestamoQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collClPrestamos);
    }

    /**
     * Method called to associate a ClPrestamo object to this object
     * through the ClPrestamo foreign key attribute.
     *
     * @param    ClPrestamo $l ClPrestamo
     * @return Regional The current object (for fluent API support)
     */
    public function addClPrestamo(ClPrestamo $l)
    {
        if ($this->collClPrestamos === null) {
            $this->initClPrestamos();
            $this->collClPrestamosPartial = true;
        }

        if (!in_array($l, $this->collClPrestamos->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddClPrestamo($l);

            if ($this->clPrestamosScheduledForDeletion and $this->clPrestamosScheduledForDeletion->contains($l)) {
                $this->clPrestamosScheduledForDeletion->remove($this->clPrestamosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ClPrestamo $clPrestamo The clPrestamo object to add.
     */
    protected function doAddClPrestamo($clPrestamo)
    {
        $this->collClPrestamos[]= $clPrestamo;
        $clPrestamo->setRegional($this);
    }

    /**
     * @param	ClPrestamo $clPrestamo The clPrestamo object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeClPrestamo($clPrestamo)
    {
        if ($this->getClPrestamos()->contains($clPrestamo)) {
            $this->collClPrestamos->remove($this->collClPrestamos->search($clPrestamo));
            if (null === $this->clPrestamosScheduledForDeletion) {
                $this->clPrestamosScheduledForDeletion = clone $this->collClPrestamos;
                $this->clPrestamosScheduledForDeletion->clear();
            }
            $this->clPrestamosScheduledForDeletion[]= clone $clPrestamo;
            $clPrestamo->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ClPrestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ClPrestamo[] List of ClPrestamo objects
     */
    public function getClPrestamosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ClPrestamoQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getClPrestamos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ClPrestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ClPrestamo[] List of ClPrestamo objects
     */
    public function getClPrestamosJoinClEstadoPrestamo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ClPrestamoQuery::create(null, $criteria);
        $query->joinWith('ClEstadoPrestamo', $join_behavior);

        return $this->getClPrestamos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ClPrestamos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ClPrestamo[] List of ClPrestamo objects
     */
    public function getClPrestamosJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ClPrestamoQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getClPrestamos($query, $con);
    }

    /**
     * Clears out the collFacturas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addFacturas()
     */
    public function clearFacturas()
    {
        $this->collFacturas = null; // important to set this to null since that means it is uninitialized
        $this->collFacturasPartial = null;

        return $this;
    }

    /**
     * reset is the collFacturas collection loaded partially
     *
     * @return void
     */
    public function resetPartialFacturas($v = true)
    {
        $this->collFacturasPartial = $v;
    }

    /**
     * Initializes the collFacturas collection.
     *
     * By default this just sets the collFacturas collection to an empty array (like clearcollFacturas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFacturas($overrideExisting = true)
    {
        if (null !== $this->collFacturas && !$overrideExisting) {
            return;
        }
        $this->collFacturas = new PropelObjectCollection();
        $this->collFacturas->setModel('Factura');
    }

    /**
     * Gets an array of Factura objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Factura[] List of Factura objects
     * @throws PropelException
     */
    public function getFacturas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFacturasPartial && !$this->isNew();
        if (null === $this->collFacturas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFacturas) {
                // return empty collection
                $this->initFacturas();
            } else {
                $collFacturas = FacturaQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFacturasPartial && count($collFacturas)) {
                      $this->initFacturas(false);

                      foreach ($collFacturas as $obj) {
                        if (false == $this->collFacturas->contains($obj)) {
                          $this->collFacturas->append($obj);
                        }
                      }

                      $this->collFacturasPartial = true;
                    }

                    $collFacturas->getInternalIterator()->rewind();

                    return $collFacturas;
                }

                if ($partial && $this->collFacturas) {
                    foreach ($this->collFacturas as $obj) {
                        if ($obj->isNew()) {
                            $collFacturas[] = $obj;
                        }
                    }
                }

                $this->collFacturas = $collFacturas;
                $this->collFacturasPartial = false;
            }
        }

        return $this->collFacturas;
    }

    /**
     * Sets a collection of Factura objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $facturas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setFacturas(PropelCollection $facturas, PropelPDO $con = null)
    {
        $facturasToDelete = $this->getFacturas(new Criteria(), $con)->diff($facturas);


        $this->facturasScheduledForDeletion = $facturasToDelete;

        foreach ($facturasToDelete as $facturaRemoved) {
            $facturaRemoved->setRegional(null);
        }

        $this->collFacturas = null;
        foreach ($facturas as $factura) {
            $this->addFactura($factura);
        }

        $this->collFacturas = $facturas;
        $this->collFacturasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Factura objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Factura objects.
     * @throws PropelException
     */
    public function countFacturas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFacturasPartial && !$this->isNew();
        if (null === $this->collFacturas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFacturas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFacturas());
            }
            $query = FacturaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collFacturas);
    }

    /**
     * Method called to associate a Factura object to this object
     * through the Factura foreign key attribute.
     *
     * @param    Factura $l Factura
     * @return Regional The current object (for fluent API support)
     */
    public function addFactura(Factura $l)
    {
        if ($this->collFacturas === null) {
            $this->initFacturas();
            $this->collFacturasPartial = true;
        }

        if (!in_array($l, $this->collFacturas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFactura($l);

            if ($this->facturasScheduledForDeletion and $this->facturasScheduledForDeletion->contains($l)) {
                $this->facturasScheduledForDeletion->remove($this->facturasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Factura $factura The factura object to add.
     */
    protected function doAddFactura($factura)
    {
        $this->collFacturas[]= $factura;
        $factura->setRegional($this);
    }

    /**
     * @param	Factura $factura The factura object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeFactura($factura)
    {
        if ($this->getFacturas()->contains($factura)) {
            $this->collFacturas->remove($this->collFacturas->search($factura));
            if (null === $this->facturasScheduledForDeletion) {
                $this->facturasScheduledForDeletion = clone $this->collFacturas;
                $this->facturasScheduledForDeletion->clear();
            }
            $this->facturasScheduledForDeletion[]= clone $factura;
            $factura->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinProveedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Proveedor', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinFacturaEstado($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('FacturaEstado', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinFacturaTipo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('FacturaTipo', $join_behavior);

        return $this->getFacturas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Facturas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Factura[] List of Factura objects
     */
    public function getFacturasJoinFormaRecepcion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaQuery::create(null, $criteria);
        $query->joinWith('FormaRecepcion', $join_behavior);

        return $this->getFacturas($query, $con);
    }

    /**
     * Clears out the collFacturaReceptors collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addFacturaReceptors()
     */
    public function clearFacturaReceptors()
    {
        $this->collFacturaReceptors = null; // important to set this to null since that means it is uninitialized
        $this->collFacturaReceptorsPartial = null;

        return $this;
    }

    /**
     * reset is the collFacturaReceptors collection loaded partially
     *
     * @return void
     */
    public function resetPartialFacturaReceptors($v = true)
    {
        $this->collFacturaReceptorsPartial = $v;
    }

    /**
     * Initializes the collFacturaReceptors collection.
     *
     * By default this just sets the collFacturaReceptors collection to an empty array (like clearcollFacturaReceptors());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initFacturaReceptors($overrideExisting = true)
    {
        if (null !== $this->collFacturaReceptors && !$overrideExisting) {
            return;
        }
        $this->collFacturaReceptors = new PropelObjectCollection();
        $this->collFacturaReceptors->setModel('FacturaReceptor');
    }

    /**
     * Gets an array of FacturaReceptor objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|FacturaReceptor[] List of FacturaReceptor objects
     * @throws PropelException
     */
    public function getFacturaReceptors($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collFacturaReceptorsPartial && !$this->isNew();
        if (null === $this->collFacturaReceptors || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collFacturaReceptors) {
                // return empty collection
                $this->initFacturaReceptors();
            } else {
                $collFacturaReceptors = FacturaReceptorQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collFacturaReceptorsPartial && count($collFacturaReceptors)) {
                      $this->initFacturaReceptors(false);

                      foreach ($collFacturaReceptors as $obj) {
                        if (false == $this->collFacturaReceptors->contains($obj)) {
                          $this->collFacturaReceptors->append($obj);
                        }
                      }

                      $this->collFacturaReceptorsPartial = true;
                    }

                    $collFacturaReceptors->getInternalIterator()->rewind();

                    return $collFacturaReceptors;
                }

                if ($partial && $this->collFacturaReceptors) {
                    foreach ($this->collFacturaReceptors as $obj) {
                        if ($obj->isNew()) {
                            $collFacturaReceptors[] = $obj;
                        }
                    }
                }

                $this->collFacturaReceptors = $collFacturaReceptors;
                $this->collFacturaReceptorsPartial = false;
            }
        }

        return $this->collFacturaReceptors;
    }

    /**
     * Sets a collection of FacturaReceptor objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $facturaReceptors A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setFacturaReceptors(PropelCollection $facturaReceptors, PropelPDO $con = null)
    {
        $facturaReceptorsToDelete = $this->getFacturaReceptors(new Criteria(), $con)->diff($facturaReceptors);


        $this->facturaReceptorsScheduledForDeletion = $facturaReceptorsToDelete;

        foreach ($facturaReceptorsToDelete as $facturaReceptorRemoved) {
            $facturaReceptorRemoved->setRegional(null);
        }

        $this->collFacturaReceptors = null;
        foreach ($facturaReceptors as $facturaReceptor) {
            $this->addFacturaReceptor($facturaReceptor);
        }

        $this->collFacturaReceptors = $facturaReceptors;
        $this->collFacturaReceptorsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related FacturaReceptor objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related FacturaReceptor objects.
     * @throws PropelException
     */
    public function countFacturaReceptors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collFacturaReceptorsPartial && !$this->isNew();
        if (null === $this->collFacturaReceptors || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collFacturaReceptors) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getFacturaReceptors());
            }
            $query = FacturaReceptorQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collFacturaReceptors);
    }

    /**
     * Method called to associate a FacturaReceptor object to this object
     * through the FacturaReceptor foreign key attribute.
     *
     * @param    FacturaReceptor $l FacturaReceptor
     * @return Regional The current object (for fluent API support)
     */
    public function addFacturaReceptor(FacturaReceptor $l)
    {
        if ($this->collFacturaReceptors === null) {
            $this->initFacturaReceptors();
            $this->collFacturaReceptorsPartial = true;
        }

        if (!in_array($l, $this->collFacturaReceptors->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddFacturaReceptor($l);

            if ($this->facturaReceptorsScheduledForDeletion and $this->facturaReceptorsScheduledForDeletion->contains($l)) {
                $this->facturaReceptorsScheduledForDeletion->remove($this->facturaReceptorsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	FacturaReceptor $facturaReceptor The facturaReceptor object to add.
     */
    protected function doAddFacturaReceptor($facturaReceptor)
    {
        $this->collFacturaReceptors[]= $facturaReceptor;
        $facturaReceptor->setRegional($this);
    }

    /**
     * @param	FacturaReceptor $facturaReceptor The facturaReceptor object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeFacturaReceptor($facturaReceptor)
    {
        if ($this->getFacturaReceptors()->contains($facturaReceptor)) {
            $this->collFacturaReceptors->remove($this->collFacturaReceptors->search($facturaReceptor));
            if (null === $this->facturaReceptorsScheduledForDeletion) {
                $this->facturaReceptorsScheduledForDeletion = clone $this->collFacturaReceptors;
                $this->facturaReceptorsScheduledForDeletion->clear();
            }
            $this->facturaReceptorsScheduledForDeletion[]= clone $facturaReceptor;
            $facturaReceptor->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related FacturaReceptors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaReceptor[] List of FacturaReceptor objects
     */
    public function getFacturaReceptorsJoinFacturaProceso($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaReceptorQuery::create(null, $criteria);
        $query->joinWith('FacturaProceso', $join_behavior);

        return $this->getFacturaReceptors($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related FacturaReceptors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|FacturaReceptor[] List of FacturaReceptor objects
     */
    public function getFacturaReceptorsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = FacturaReceptorQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getFacturaReceptors($query, $con);
    }

    /**
     * Clears out the collComEnviadas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addComEnviadas()
     */
    public function clearComEnviadas()
    {
        $this->collComEnviadas = null; // important to set this to null since that means it is uninitialized
        $this->collComEnviadasPartial = null;

        return $this;
    }

    /**
     * reset is the collComEnviadas collection loaded partially
     *
     * @return void
     */
    public function resetPartialComEnviadas($v = true)
    {
        $this->collComEnviadasPartial = $v;
    }

    /**
     * Initializes the collComEnviadas collection.
     *
     * By default this just sets the collComEnviadas collection to an empty array (like clearcollComEnviadas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initComEnviadas($overrideExisting = true)
    {
        if (null !== $this->collComEnviadas && !$overrideExisting) {
            return;
        }
        $this->collComEnviadas = new PropelObjectCollection();
        $this->collComEnviadas->setModel('ComEnviada');
    }

    /**
     * Gets an array of ComEnviada objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     * @throws PropelException
     */
    public function getComEnviadas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collComEnviadasPartial && !$this->isNew();
        if (null === $this->collComEnviadas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collComEnviadas) {
                // return empty collection
                $this->initComEnviadas();
            } else {
                $collComEnviadas = ComEnviadaQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collComEnviadasPartial && count($collComEnviadas)) {
                      $this->initComEnviadas(false);

                      foreach ($collComEnviadas as $obj) {
                        if (false == $this->collComEnviadas->contains($obj)) {
                          $this->collComEnviadas->append($obj);
                        }
                      }

                      $this->collComEnviadasPartial = true;
                    }

                    $collComEnviadas->getInternalIterator()->rewind();

                    return $collComEnviadas;
                }

                if ($partial && $this->collComEnviadas) {
                    foreach ($this->collComEnviadas as $obj) {
                        if ($obj->isNew()) {
                            $collComEnviadas[] = $obj;
                        }
                    }
                }

                $this->collComEnviadas = $collComEnviadas;
                $this->collComEnviadasPartial = false;
            }
        }

        return $this->collComEnviadas;
    }

    /**
     * Sets a collection of ComEnviada objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $comEnviadas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setComEnviadas(PropelCollection $comEnviadas, PropelPDO $con = null)
    {
        $comEnviadasToDelete = $this->getComEnviadas(new Criteria(), $con)->diff($comEnviadas);


        $this->comEnviadasScheduledForDeletion = $comEnviadasToDelete;

        foreach ($comEnviadasToDelete as $comEnviadaRemoved) {
            $comEnviadaRemoved->setRegional(null);
        }

        $this->collComEnviadas = null;
        foreach ($comEnviadas as $comEnviada) {
            $this->addComEnviada($comEnviada);
        }

        $this->collComEnviadas = $comEnviadas;
        $this->collComEnviadasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ComEnviada objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ComEnviada objects.
     * @throws PropelException
     */
    public function countComEnviadas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collComEnviadasPartial && !$this->isNew();
        if (null === $this->collComEnviadas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collComEnviadas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getComEnviadas());
            }
            $query = ComEnviadaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collComEnviadas);
    }

    /**
     * Method called to associate a ComEnviada object to this object
     * through the ComEnviada foreign key attribute.
     *
     * @param    ComEnviada $l ComEnviada
     * @return Regional The current object (for fluent API support)
     */
    public function addComEnviada(ComEnviada $l)
    {
        if ($this->collComEnviadas === null) {
            $this->initComEnviadas();
            $this->collComEnviadasPartial = true;
        }

        if (!in_array($l, $this->collComEnviadas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddComEnviada($l);

            if ($this->comEnviadasScheduledForDeletion and $this->comEnviadasScheduledForDeletion->contains($l)) {
                $this->comEnviadasScheduledForDeletion->remove($this->comEnviadasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ComEnviada $comEnviada The comEnviada object to add.
     */
    protected function doAddComEnviada($comEnviada)
    {
        $this->collComEnviadas[]= $comEnviada;
        $comEnviada->setRegional($this);
    }

    /**
     * @param	ComEnviada $comEnviada The comEnviada object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeComEnviada($comEnviada)
    {
        if ($this->getComEnviadas()->contains($comEnviada)) {
            $this->collComEnviadas->remove($this->collComEnviadas->search($comEnviada));
            if (null === $this->comEnviadasScheduledForDeletion) {
                $this->comEnviadasScheduledForDeletion = clone $this->collComEnviadas;
                $this->comEnviadasScheduledForDeletion->clear();
            }
            $this->comEnviadasScheduledForDeletion[]= clone $comEnviada;
            $comEnviada->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinCiudad($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('Ciudad', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinEstadoComEnviada($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('EstadoComEnviada', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinEmpresaMensajeria($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('EmpresaMensajeria', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinEstadoDigitalizacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('EstadoDigitalizacion', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinIdioma($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('Idioma', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinPlantillasCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('PlantillasCom', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinTipoFirmaDigital($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('TipoFirmaDigital', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinTipoProcesoCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('TipoProcesoCom', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinServicio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('Servicio', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComEnviadas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComEnviada[] List of ComEnviada objects
     */
    public function getComEnviadasJoinPrioridadCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComEnviadaQuery::create(null, $criteria);
        $query->joinWith('PrioridadCom', $join_behavior);

        return $this->getComEnviadas($query, $con);
    }

    /**
     * Clears out the collComInternas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addComInternas()
     */
    public function clearComInternas()
    {
        $this->collComInternas = null; // important to set this to null since that means it is uninitialized
        $this->collComInternasPartial = null;

        return $this;
    }

    /**
     * reset is the collComInternas collection loaded partially
     *
     * @return void
     */
    public function resetPartialComInternas($v = true)
    {
        $this->collComInternasPartial = $v;
    }

    /**
     * Initializes the collComInternas collection.
     *
     * By default this just sets the collComInternas collection to an empty array (like clearcollComInternas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initComInternas($overrideExisting = true)
    {
        if (null !== $this->collComInternas && !$overrideExisting) {
            return;
        }
        $this->collComInternas = new PropelObjectCollection();
        $this->collComInternas->setModel('ComInterna');
    }

    /**
     * Gets an array of ComInterna objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     * @throws PropelException
     */
    public function getComInternas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collComInternasPartial && !$this->isNew();
        if (null === $this->collComInternas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collComInternas) {
                // return empty collection
                $this->initComInternas();
            } else {
                $collComInternas = ComInternaQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collComInternasPartial && count($collComInternas)) {
                      $this->initComInternas(false);

                      foreach ($collComInternas as $obj) {
                        if (false == $this->collComInternas->contains($obj)) {
                          $this->collComInternas->append($obj);
                        }
                      }

                      $this->collComInternasPartial = true;
                    }

                    $collComInternas->getInternalIterator()->rewind();

                    return $collComInternas;
                }

                if ($partial && $this->collComInternas) {
                    foreach ($this->collComInternas as $obj) {
                        if ($obj->isNew()) {
                            $collComInternas[] = $obj;
                        }
                    }
                }

                $this->collComInternas = $collComInternas;
                $this->collComInternasPartial = false;
            }
        }

        return $this->collComInternas;
    }

    /**
     * Sets a collection of ComInterna objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $comInternas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setComInternas(PropelCollection $comInternas, PropelPDO $con = null)
    {
        $comInternasToDelete = $this->getComInternas(new Criteria(), $con)->diff($comInternas);


        $this->comInternasScheduledForDeletion = $comInternasToDelete;

        foreach ($comInternasToDelete as $comInternaRemoved) {
            $comInternaRemoved->setRegional(null);
        }

        $this->collComInternas = null;
        foreach ($comInternas as $comInterna) {
            $this->addComInterna($comInterna);
        }

        $this->collComInternas = $comInternas;
        $this->collComInternasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ComInterna objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ComInterna objects.
     * @throws PropelException
     */
    public function countComInternas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collComInternasPartial && !$this->isNew();
        if (null === $this->collComInternas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collComInternas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getComInternas());
            }
            $query = ComInternaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collComInternas);
    }

    /**
     * Method called to associate a ComInterna object to this object
     * through the ComInterna foreign key attribute.
     *
     * @param    ComInterna $l ComInterna
     * @return Regional The current object (for fluent API support)
     */
    public function addComInterna(ComInterna $l)
    {
        if ($this->collComInternas === null) {
            $this->initComInternas();
            $this->collComInternasPartial = true;
        }

        if (!in_array($l, $this->collComInternas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddComInterna($l);

            if ($this->comInternasScheduledForDeletion and $this->comInternasScheduledForDeletion->contains($l)) {
                $this->comInternasScheduledForDeletion->remove($this->comInternasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ComInterna $comInterna The comInterna object to add.
     */
    protected function doAddComInterna($comInterna)
    {
        $this->collComInternas[]= $comInterna;
        $comInterna->setRegional($this);
    }

    /**
     * @param	ComInterna $comInterna The comInterna object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeComInterna($comInterna)
    {
        if ($this->getComInternas()->contains($comInterna)) {
            $this->collComInternas->remove($this->collComInternas->search($comInterna));
            if (null === $this->comInternasScheduledForDeletion) {
                $this->comInternasScheduledForDeletion = clone $this->collComInternas;
                $this->comInternasScheduledForDeletion->clear();
            }
            $this->comInternasScheduledForDeletion[]= clone $comInterna;
            $comInterna->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinEmpresaMensajeria($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('EmpresaMensajeria', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinEstadoComInterna($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('EstadoComInterna', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinTipoComInterna($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('TipoComInterna', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinEstadoDigitalizacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('EstadoDigitalizacion', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinIdioma($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('Idioma', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinTipoFirmaDigital($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('TipoFirmaDigital', $join_behavior);

        return $this->getComInternas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComInternas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComInterna[] List of ComInterna objects
     */
    public function getComInternasJoinPrioridadCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComInternaQuery::create(null, $criteria);
        $query->joinWith('PrioridadCom', $join_behavior);

        return $this->getComInternas($query, $con);
    }

    /**
     * Clears out the collComPermisoRegionals collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addComPermisoRegionals()
     */
    public function clearComPermisoRegionals()
    {
        $this->collComPermisoRegionals = null; // important to set this to null since that means it is uninitialized
        $this->collComPermisoRegionalsPartial = null;

        return $this;
    }

    /**
     * reset is the collComPermisoRegionals collection loaded partially
     *
     * @return void
     */
    public function resetPartialComPermisoRegionals($v = true)
    {
        $this->collComPermisoRegionalsPartial = $v;
    }

    /**
     * Initializes the collComPermisoRegionals collection.
     *
     * By default this just sets the collComPermisoRegionals collection to an empty array (like clearcollComPermisoRegionals());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initComPermisoRegionals($overrideExisting = true)
    {
        if (null !== $this->collComPermisoRegionals && !$overrideExisting) {
            return;
        }
        $this->collComPermisoRegionals = new PropelObjectCollection();
        $this->collComPermisoRegionals->setModel('ComPermisoRegional');
    }

    /**
     * Gets an array of ComPermisoRegional objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ComPermisoRegional[] List of ComPermisoRegional objects
     * @throws PropelException
     */
    public function getComPermisoRegionals($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collComPermisoRegionalsPartial && !$this->isNew();
        if (null === $this->collComPermisoRegionals || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collComPermisoRegionals) {
                // return empty collection
                $this->initComPermisoRegionals();
            } else {
                $collComPermisoRegionals = ComPermisoRegionalQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collComPermisoRegionalsPartial && count($collComPermisoRegionals)) {
                      $this->initComPermisoRegionals(false);

                      foreach ($collComPermisoRegionals as $obj) {
                        if (false == $this->collComPermisoRegionals->contains($obj)) {
                          $this->collComPermisoRegionals->append($obj);
                        }
                      }

                      $this->collComPermisoRegionalsPartial = true;
                    }

                    $collComPermisoRegionals->getInternalIterator()->rewind();

                    return $collComPermisoRegionals;
                }

                if ($partial && $this->collComPermisoRegionals) {
                    foreach ($this->collComPermisoRegionals as $obj) {
                        if ($obj->isNew()) {
                            $collComPermisoRegionals[] = $obj;
                        }
                    }
                }

                $this->collComPermisoRegionals = $collComPermisoRegionals;
                $this->collComPermisoRegionalsPartial = false;
            }
        }

        return $this->collComPermisoRegionals;
    }

    /**
     * Sets a collection of ComPermisoRegional objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $comPermisoRegionals A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setComPermisoRegionals(PropelCollection $comPermisoRegionals, PropelPDO $con = null)
    {
        $comPermisoRegionalsToDelete = $this->getComPermisoRegionals(new Criteria(), $con)->diff($comPermisoRegionals);


        $this->comPermisoRegionalsScheduledForDeletion = $comPermisoRegionalsToDelete;

        foreach ($comPermisoRegionalsToDelete as $comPermisoRegionalRemoved) {
            $comPermisoRegionalRemoved->setRegional(null);
        }

        $this->collComPermisoRegionals = null;
        foreach ($comPermisoRegionals as $comPermisoRegional) {
            $this->addComPermisoRegional($comPermisoRegional);
        }

        $this->collComPermisoRegionals = $comPermisoRegionals;
        $this->collComPermisoRegionalsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ComPermisoRegional objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ComPermisoRegional objects.
     * @throws PropelException
     */
    public function countComPermisoRegionals(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collComPermisoRegionalsPartial && !$this->isNew();
        if (null === $this->collComPermisoRegionals || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collComPermisoRegionals) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getComPermisoRegionals());
            }
            $query = ComPermisoRegionalQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collComPermisoRegionals);
    }

    /**
     * Method called to associate a ComPermisoRegional object to this object
     * through the ComPermisoRegional foreign key attribute.
     *
     * @param    ComPermisoRegional $l ComPermisoRegional
     * @return Regional The current object (for fluent API support)
     */
    public function addComPermisoRegional(ComPermisoRegional $l)
    {
        if ($this->collComPermisoRegionals === null) {
            $this->initComPermisoRegionals();
            $this->collComPermisoRegionalsPartial = true;
        }

        if (!in_array($l, $this->collComPermisoRegionals->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddComPermisoRegional($l);

            if ($this->comPermisoRegionalsScheduledForDeletion and $this->comPermisoRegionalsScheduledForDeletion->contains($l)) {
                $this->comPermisoRegionalsScheduledForDeletion->remove($this->comPermisoRegionalsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ComPermisoRegional $comPermisoRegional The comPermisoRegional object to add.
     */
    protected function doAddComPermisoRegional($comPermisoRegional)
    {
        $this->collComPermisoRegionals[]= $comPermisoRegional;
        $comPermisoRegional->setRegional($this);
    }

    /**
     * @param	ComPermisoRegional $comPermisoRegional The comPermisoRegional object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeComPermisoRegional($comPermisoRegional)
    {
        if ($this->getComPermisoRegionals()->contains($comPermisoRegional)) {
            $this->collComPermisoRegionals->remove($this->collComPermisoRegionals->search($comPermisoRegional));
            if (null === $this->comPermisoRegionalsScheduledForDeletion) {
                $this->comPermisoRegionalsScheduledForDeletion = clone $this->collComPermisoRegionals;
                $this->comPermisoRegionalsScheduledForDeletion->clear();
            }
            $this->comPermisoRegionalsScheduledForDeletion[]= clone $comPermisoRegional;
            $comPermisoRegional->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComPermisoRegionals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComPermisoRegional[] List of ComPermisoRegional objects
     */
    public function getComPermisoRegionalsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComPermisoRegionalQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getComPermisoRegionals($query, $con);
    }

    /**
     * Clears out the collComRecibidas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addComRecibidas()
     */
    public function clearComRecibidas()
    {
        $this->collComRecibidas = null; // important to set this to null since that means it is uninitialized
        $this->collComRecibidasPartial = null;

        return $this;
    }

    /**
     * reset is the collComRecibidas collection loaded partially
     *
     * @return void
     */
    public function resetPartialComRecibidas($v = true)
    {
        $this->collComRecibidasPartial = $v;
    }

    /**
     * Initializes the collComRecibidas collection.
     *
     * By default this just sets the collComRecibidas collection to an empty array (like clearcollComRecibidas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initComRecibidas($overrideExisting = true)
    {
        if (null !== $this->collComRecibidas && !$overrideExisting) {
            return;
        }
        $this->collComRecibidas = new PropelObjectCollection();
        $this->collComRecibidas->setModel('ComRecibida');
    }

    /**
     * Gets an array of ComRecibida objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     * @throws PropelException
     */
    public function getComRecibidas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collComRecibidasPartial && !$this->isNew();
        if (null === $this->collComRecibidas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collComRecibidas) {
                // return empty collection
                $this->initComRecibidas();
            } else {
                $collComRecibidas = ComRecibidaQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collComRecibidasPartial && count($collComRecibidas)) {
                      $this->initComRecibidas(false);

                      foreach ($collComRecibidas as $obj) {
                        if (false == $this->collComRecibidas->contains($obj)) {
                          $this->collComRecibidas->append($obj);
                        }
                      }

                      $this->collComRecibidasPartial = true;
                    }

                    $collComRecibidas->getInternalIterator()->rewind();

                    return $collComRecibidas;
                }

                if ($partial && $this->collComRecibidas) {
                    foreach ($this->collComRecibidas as $obj) {
                        if ($obj->isNew()) {
                            $collComRecibidas[] = $obj;
                        }
                    }
                }

                $this->collComRecibidas = $collComRecibidas;
                $this->collComRecibidasPartial = false;
            }
        }

        return $this->collComRecibidas;
    }

    /**
     * Sets a collection of ComRecibida objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $comRecibidas A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setComRecibidas(PropelCollection $comRecibidas, PropelPDO $con = null)
    {
        $comRecibidasToDelete = $this->getComRecibidas(new Criteria(), $con)->diff($comRecibidas);


        $this->comRecibidasScheduledForDeletion = $comRecibidasToDelete;

        foreach ($comRecibidasToDelete as $comRecibidaRemoved) {
            $comRecibidaRemoved->setRegional(null);
        }

        $this->collComRecibidas = null;
        foreach ($comRecibidas as $comRecibida) {
            $this->addComRecibida($comRecibida);
        }

        $this->collComRecibidas = $comRecibidas;
        $this->collComRecibidasPartial = false;

        return $this;
    }

    /**
     * Returns the number of related ComRecibida objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related ComRecibida objects.
     * @throws PropelException
     */
    public function countComRecibidas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collComRecibidasPartial && !$this->isNew();
        if (null === $this->collComRecibidas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collComRecibidas) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getComRecibidas());
            }
            $query = ComRecibidaQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collComRecibidas);
    }

    /**
     * Method called to associate a ComRecibida object to this object
     * through the ComRecibida foreign key attribute.
     *
     * @param    ComRecibida $l ComRecibida
     * @return Regional The current object (for fluent API support)
     */
    public function addComRecibida(ComRecibida $l)
    {
        if ($this->collComRecibidas === null) {
            $this->initComRecibidas();
            $this->collComRecibidasPartial = true;
        }

        if (!in_array($l, $this->collComRecibidas->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddComRecibida($l);

            if ($this->comRecibidasScheduledForDeletion and $this->comRecibidasScheduledForDeletion->contains($l)) {
                $this->comRecibidasScheduledForDeletion->remove($this->comRecibidasScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	ComRecibida $comRecibida The comRecibida object to add.
     */
    protected function doAddComRecibida($comRecibida)
    {
        $this->collComRecibidas[]= $comRecibida;
        $comRecibida->setRegional($this);
    }

    /**
     * @param	ComRecibida $comRecibida The comRecibida object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeComRecibida($comRecibida)
    {
        if ($this->getComRecibidas()->contains($comRecibida)) {
            $this->collComRecibidas->remove($this->collComRecibidas->search($comRecibida));
            if (null === $this->comRecibidasScheduledForDeletion) {
                $this->comRecibidasScheduledForDeletion = clone $this->collComRecibidas;
                $this->comRecibidasScheduledForDeletion->clear();
            }
            $this->comRecibidasScheduledForDeletion[]= clone $comRecibida;
            $comRecibida->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinComEnviada($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('ComEnviada', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinAsuntoRecibida($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('AsuntoRecibida', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinTipoComRecibida($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('TipoComRecibida', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinPeriodo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('Periodo', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinEstadoDigitalizacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('EstadoDigitalizacion', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinEstadoComRecibida($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('EstadoComRecibida', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinFormaRecepcion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('FormaRecepcion', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinCiudad($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('Ciudad', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinDirectorioExterno($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('DirectorioExterno', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinEmpresaMensajeria($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('EmpresaMensajeria', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinTipoFirmaDigital($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('TipoFirmaDigital', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinMedioRespuesta($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('MedioRespuesta', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinTipoRespuesta($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('TipoRespuesta', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinPrioridadCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('PrioridadCom', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinTipoExpediente($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('TipoExpediente', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related ComRecibidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|ComRecibida[] List of ComRecibida objects
     */
    public function getComRecibidasJoinTipoProcesoCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ComRecibidaQuery::create(null, $criteria);
        $query->joinWith('TipoProcesoCom', $join_behavior);

        return $this->getComRecibidas($query, $con);
    }

    /**
     * Clears out the collHistUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addHistUsuarios()
     */
    public function clearHistUsuarios()
    {
        $this->collHistUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collHistUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collHistUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialHistUsuarios($v = true)
    {
        $this->collHistUsuariosPartial = $v;
    }

    /**
     * Initializes the collHistUsuarios collection.
     *
     * By default this just sets the collHistUsuarios collection to an empty array (like clearcollHistUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initHistUsuarios($overrideExisting = true)
    {
        if (null !== $this->collHistUsuarios && !$overrideExisting) {
            return;
        }
        $this->collHistUsuarios = new PropelObjectCollection();
        $this->collHistUsuarios->setModel('HistUsuario');
    }

    /**
     * Gets an array of HistUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     * @throws PropelException
     */
    public function getHistUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collHistUsuariosPartial && !$this->isNew();
        if (null === $this->collHistUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collHistUsuarios) {
                // return empty collection
                $this->initHistUsuarios();
            } else {
                $collHistUsuarios = HistUsuarioQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collHistUsuariosPartial && count($collHistUsuarios)) {
                      $this->initHistUsuarios(false);

                      foreach ($collHistUsuarios as $obj) {
                        if (false == $this->collHistUsuarios->contains($obj)) {
                          $this->collHistUsuarios->append($obj);
                        }
                      }

                      $this->collHistUsuariosPartial = true;
                    }

                    $collHistUsuarios->getInternalIterator()->rewind();

                    return $collHistUsuarios;
                }

                if ($partial && $this->collHistUsuarios) {
                    foreach ($this->collHistUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collHistUsuarios[] = $obj;
                        }
                    }
                }

                $this->collHistUsuarios = $collHistUsuarios;
                $this->collHistUsuariosPartial = false;
            }
        }

        return $this->collHistUsuarios;
    }

    /**
     * Sets a collection of HistUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $histUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setHistUsuarios(PropelCollection $histUsuarios, PropelPDO $con = null)
    {
        $histUsuariosToDelete = $this->getHistUsuarios(new Criteria(), $con)->diff($histUsuarios);


        $this->histUsuariosScheduledForDeletion = $histUsuariosToDelete;

        foreach ($histUsuariosToDelete as $histUsuarioRemoved) {
            $histUsuarioRemoved->setRegional(null);
        }

        $this->collHistUsuarios = null;
        foreach ($histUsuarios as $histUsuario) {
            $this->addHistUsuario($histUsuario);
        }

        $this->collHistUsuarios = $histUsuarios;
        $this->collHistUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related HistUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related HistUsuario objects.
     * @throws PropelException
     */
    public function countHistUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collHistUsuariosPartial && !$this->isNew();
        if (null === $this->collHistUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collHistUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getHistUsuarios());
            }
            $query = HistUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collHistUsuarios);
    }

    /**
     * Method called to associate a HistUsuario object to this object
     * through the HistUsuario foreign key attribute.
     *
     * @param    HistUsuario $l HistUsuario
     * @return Regional The current object (for fluent API support)
     */
    public function addHistUsuario(HistUsuario $l)
    {
        if ($this->collHistUsuarios === null) {
            $this->initHistUsuarios();
            $this->collHistUsuariosPartial = true;
        }

        if (!in_array($l, $this->collHistUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddHistUsuario($l);

            if ($this->histUsuariosScheduledForDeletion and $this->histUsuariosScheduledForDeletion->contains($l)) {
                $this->histUsuariosScheduledForDeletion->remove($this->histUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	HistUsuario $histUsuario The histUsuario object to add.
     */
    protected function doAddHistUsuario($histUsuario)
    {
        $this->collHistUsuarios[]= $histUsuario;
        $histUsuario->setRegional($this);
    }

    /**
     * @param	HistUsuario $histUsuario The histUsuario object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeHistUsuario($histUsuario)
    {
        if ($this->getHistUsuarios()->contains($histUsuario)) {
            $this->collHistUsuarios->remove($this->collHistUsuarios->search($histUsuario));
            if (null === $this->histUsuariosScheduledForDeletion) {
                $this->histUsuariosScheduledForDeletion = clone $this->collHistUsuarios;
                $this->histUsuariosScheduledForDeletion->clear();
            }
            $this->histUsuariosScheduledForDeletion[]= clone $histUsuario;
            $histUsuario->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinCargo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('Cargo', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinEstadoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('EstadoUsuario', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinTipoProcesoCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoProcesoCom', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinTipoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoUsuario', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }

    /**
     * Clears out the collUnidadDocumentals collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addUnidadDocumentals()
     */
    public function clearUnidadDocumentals()
    {
        $this->collUnidadDocumentals = null; // important to set this to null since that means it is uninitialized
        $this->collUnidadDocumentalsPartial = null;

        return $this;
    }

    /**
     * reset is the collUnidadDocumentals collection loaded partially
     *
     * @return void
     */
    public function resetPartialUnidadDocumentals($v = true)
    {
        $this->collUnidadDocumentalsPartial = $v;
    }

    /**
     * Initializes the collUnidadDocumentals collection.
     *
     * By default this just sets the collUnidadDocumentals collection to an empty array (like clearcollUnidadDocumentals());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUnidadDocumentals($overrideExisting = true)
    {
        if (null !== $this->collUnidadDocumentals && !$overrideExisting) {
            return;
        }
        $this->collUnidadDocumentals = new PropelObjectCollection();
        $this->collUnidadDocumentals->setModel('UnidadDocumental');
    }

    /**
     * Gets an array of UnidadDocumental objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     * @throws PropelException
     */
    public function getUnidadDocumentals($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUnidadDocumentalsPartial && !$this->isNew();
        if (null === $this->collUnidadDocumentals || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUnidadDocumentals) {
                // return empty collection
                $this->initUnidadDocumentals();
            } else {
                $collUnidadDocumentals = UnidadDocumentalQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUnidadDocumentalsPartial && count($collUnidadDocumentals)) {
                      $this->initUnidadDocumentals(false);

                      foreach ($collUnidadDocumentals as $obj) {
                        if (false == $this->collUnidadDocumentals->contains($obj)) {
                          $this->collUnidadDocumentals->append($obj);
                        }
                      }

                      $this->collUnidadDocumentalsPartial = true;
                    }

                    $collUnidadDocumentals->getInternalIterator()->rewind();

                    return $collUnidadDocumentals;
                }

                if ($partial && $this->collUnidadDocumentals) {
                    foreach ($this->collUnidadDocumentals as $obj) {
                        if ($obj->isNew()) {
                            $collUnidadDocumentals[] = $obj;
                        }
                    }
                }

                $this->collUnidadDocumentals = $collUnidadDocumentals;
                $this->collUnidadDocumentalsPartial = false;
            }
        }

        return $this->collUnidadDocumentals;
    }

    /**
     * Sets a collection of UnidadDocumental objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $unidadDocumentals A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setUnidadDocumentals(PropelCollection $unidadDocumentals, PropelPDO $con = null)
    {
        $unidadDocumentalsToDelete = $this->getUnidadDocumentals(new Criteria(), $con)->diff($unidadDocumentals);


        $this->unidadDocumentalsScheduledForDeletion = $unidadDocumentalsToDelete;

        foreach ($unidadDocumentalsToDelete as $unidadDocumentalRemoved) {
            $unidadDocumentalRemoved->setRegional(null);
        }

        $this->collUnidadDocumentals = null;
        foreach ($unidadDocumentals as $unidadDocumental) {
            $this->addUnidadDocumental($unidadDocumental);
        }

        $this->collUnidadDocumentals = $unidadDocumentals;
        $this->collUnidadDocumentalsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related UnidadDocumental objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UnidadDocumental objects.
     * @throws PropelException
     */
    public function countUnidadDocumentals(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUnidadDocumentalsPartial && !$this->isNew();
        if (null === $this->collUnidadDocumentals || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUnidadDocumentals) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getUnidadDocumentals());
            }
            $query = UnidadDocumentalQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collUnidadDocumentals);
    }

    /**
     * Method called to associate a UnidadDocumental object to this object
     * through the UnidadDocumental foreign key attribute.
     *
     * @param    UnidadDocumental $l UnidadDocumental
     * @return Regional The current object (for fluent API support)
     */
    public function addUnidadDocumental(UnidadDocumental $l)
    {
        if ($this->collUnidadDocumentals === null) {
            $this->initUnidadDocumentals();
            $this->collUnidadDocumentalsPartial = true;
        }

        if (!in_array($l, $this->collUnidadDocumentals->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddUnidadDocumental($l);

            if ($this->unidadDocumentalsScheduledForDeletion and $this->unidadDocumentalsScheduledForDeletion->contains($l)) {
                $this->unidadDocumentalsScheduledForDeletion->remove($this->unidadDocumentalsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	UnidadDocumental $unidadDocumental The unidadDocumental object to add.
     */
    protected function doAddUnidadDocumental($unidadDocumental)
    {
        $this->collUnidadDocumentals[]= $unidadDocumental;
        $unidadDocumental->setRegional($this);
    }

    /**
     * @param	UnidadDocumental $unidadDocumental The unidadDocumental object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeUnidadDocumental($unidadDocumental)
    {
        if ($this->getUnidadDocumentals()->contains($unidadDocumental)) {
            $this->collUnidadDocumentals->remove($this->collUnidadDocumentals->search($unidadDocumental));
            if (null === $this->unidadDocumentalsScheduledForDeletion) {
                $this->unidadDocumentalsScheduledForDeletion = clone $this->collUnidadDocumentals;
                $this->unidadDocumentalsScheduledForDeletion->clear();
            }
            $this->unidadDocumentalsScheduledForDeletion[]= clone $unidadDocumental;
            $unidadDocumental->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinLocalizacionUnidadDocumental($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('LocalizacionUnidadDocumental', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinSoporteUnidadDocumental($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('SoporteUnidadDocumental', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinEstadoUnidadDocumental($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('EstadoUnidadDocumental', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinFrecuenciaConsulta($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('FrecuenciaConsulta', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinUnidadConservadora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('UnidadConservadora', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinSubserie($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('Subserie', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related UnidadDocumentals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UnidadDocumental[] List of UnidadDocumental objects
     */
    public function getUnidadDocumentalsJoinTipoCierreExpediente($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UnidadDocumentalQuery::create(null, $criteria);
        $query->joinWith('TipoCierreExpediente', $join_behavior);

        return $this->getUnidadDocumentals($query, $con);
    }

    /**
     * Clears out the collUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Regional The current object (for fluent API support)
     * @see        addUsuarios()
     */
    public function clearUsuarios()
    {
        $this->collUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarios($v = true)
    {
        $this->collUsuariosPartial = $v;
    }

    /**
     * Initializes the collUsuarios collection.
     *
     * By default this just sets the collUsuarios collection to an empty array (like clearcollUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarios($overrideExisting = true)
    {
        if (null !== $this->collUsuarios && !$overrideExisting) {
            return;
        }
        $this->collUsuarios = new PropelObjectCollection();
        $this->collUsuarios->setModel('Usuario');
    }

    /**
     * Gets an array of Usuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Regional is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     * @throws PropelException
     */
    public function getUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuariosPartial && !$this->isNew();
        if (null === $this->collUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarios) {
                // return empty collection
                $this->initUsuarios();
            } else {
                $collUsuarios = UsuarioQuery::create(null, $criteria)
                    ->filterByRegional($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuariosPartial && count($collUsuarios)) {
                      $this->initUsuarios(false);

                      foreach ($collUsuarios as $obj) {
                        if (false == $this->collUsuarios->contains($obj)) {
                          $this->collUsuarios->append($obj);
                        }
                      }

                      $this->collUsuariosPartial = true;
                    }

                    $collUsuarios->getInternalIterator()->rewind();

                    return $collUsuarios;
                }

                if ($partial && $this->collUsuarios) {
                    foreach ($this->collUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collUsuarios[] = $obj;
                        }
                    }
                }

                $this->collUsuarios = $collUsuarios;
                $this->collUsuariosPartial = false;
            }
        }

        return $this->collUsuarios;
    }

    /**
     * Sets a collection of Usuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Regional The current object (for fluent API support)
     */
    public function setUsuarios(PropelCollection $usuarios, PropelPDO $con = null)
    {
        $usuariosToDelete = $this->getUsuarios(new Criteria(), $con)->diff($usuarios);


        $this->usuariosScheduledForDeletion = $usuariosToDelete;

        foreach ($usuariosToDelete as $usuarioRemoved) {
            $usuarioRemoved->setRegional(null);
        }

        $this->collUsuarios = null;
        foreach ($usuarios as $usuario) {
            $this->addUsuario($usuario);
        }

        $this->collUsuarios = $usuarios;
        $this->collUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Usuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Usuario objects.
     * @throws PropelException
     */
    public function countUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuariosPartial && !$this->isNew();
        if (null === $this->collUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getUsuarios());
            }
            $query = UsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRegional($this)
                ->count($con);
        }

        return count($this->collUsuarios);
    }

    /**
     * Method called to associate a Usuario object to this object
     * through the Usuario foreign key attribute.
     *
     * @param    Usuario $l Usuario
     * @return Regional The current object (for fluent API support)
     */
    public function addUsuario(Usuario $l)
    {
        if ($this->collUsuarios === null) {
            $this->initUsuarios();
            $this->collUsuariosPartial = true;
        }

        if (!in_array($l, $this->collUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddUsuario($l);

            if ($this->usuariosScheduledForDeletion and $this->usuariosScheduledForDeletion->contains($l)) {
                $this->usuariosScheduledForDeletion->remove($this->usuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Usuario $usuario The usuario object to add.
     */
    protected function doAddUsuario($usuario)
    {
        $this->collUsuarios[]= $usuario;
        $usuario->setRegional($this);
    }

    /**
     * @param	Usuario $usuario The usuario object to remove.
     * @return Regional The current object (for fluent API support)
     */
    public function removeUsuario($usuario)
    {
        if ($this->getUsuarios()->contains($usuario)) {
            $this->collUsuarios->remove($this->collUsuarios->search($usuario));
            if (null === $this->usuariosScheduledForDeletion) {
                $this->usuariosScheduledForDeletion = clone $this->collUsuarios;
                $this->usuariosScheduledForDeletion->clear();
            }
            $this->usuariosScheduledForDeletion[]= clone $usuario;
            $usuario->setRegional(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinCargo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('Cargo', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinEstadoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('EstadoUsuario', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinTipoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoUsuario', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Regional is new, it will return
     * an empty collection; or if this Regional has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Regional.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinTipoProcesoCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoProcesoCom', $join_behavior);

        return $this->getUsuarios($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->regional_id = null;
        $this->ciudad_id = null;
        $this->descripcion = null;
        $this->direccion = null;
        $this->entidad_id = null;
        $this->es_visible = null;
        $this->directorio_name = null;
        $this->image_membrete = null;
        $this->codigo = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collDocPrestamos) {
                foreach ($this->collDocPrestamos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collReceptors) {
                foreach ($this->collReceptors as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collWfFlujos) {
                foreach ($this->collWfFlujos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collPlantillasComs) {
                foreach ($this->collPlantillasComs as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collPrestamos) {
                foreach ($this->collPrestamos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collServicios) {
                foreach ($this->collServicios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFactHists) {
                foreach ($this->collFactHists as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collClPrestamos) {
                foreach ($this->collClPrestamos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFacturas) {
                foreach ($this->collFacturas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collFacturaReceptors) {
                foreach ($this->collFacturaReceptors as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collComEnviadas) {
                foreach ($this->collComEnviadas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collComInternas) {
                foreach ($this->collComInternas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collComPermisoRegionals) {
                foreach ($this->collComPermisoRegionals as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collComRecibidas) {
                foreach ($this->collComRecibidas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collHistUsuarios) {
                foreach ($this->collHistUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collUnidadDocumentals) {
                foreach ($this->collUnidadDocumentals as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collUsuarios) {
                foreach ($this->collUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aCiudad instanceof Persistent) {
              $this->aCiudad->clearAllReferences($deep);
            }
            if ($this->aEntidad instanceof Persistent) {
              $this->aEntidad->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collDocPrestamos instanceof PropelCollection) {
            $this->collDocPrestamos->clearIterator();
        }
        $this->collDocPrestamos = null;
        if ($this->collReceptors instanceof PropelCollection) {
            $this->collReceptors->clearIterator();
        }
        $this->collReceptors = null;
        if ($this->collWfFlujos instanceof PropelCollection) {
            $this->collWfFlujos->clearIterator();
        }
        $this->collWfFlujos = null;
        if ($this->collPlantillasComs instanceof PropelCollection) {
            $this->collPlantillasComs->clearIterator();
        }
        $this->collPlantillasComs = null;
        if ($this->collPrestamos instanceof PropelCollection) {
            $this->collPrestamos->clearIterator();
        }
        $this->collPrestamos = null;
        if ($this->collServicios instanceof PropelCollection) {
            $this->collServicios->clearIterator();
        }
        $this->collServicios = null;
        if ($this->collFactHists instanceof PropelCollection) {
            $this->collFactHists->clearIterator();
        }
        $this->collFactHists = null;
        if ($this->collClPrestamos instanceof PropelCollection) {
            $this->collClPrestamos->clearIterator();
        }
        $this->collClPrestamos = null;
        if ($this->collFacturas instanceof PropelCollection) {
            $this->collFacturas->clearIterator();
        }
        $this->collFacturas = null;
        if ($this->collFacturaReceptors instanceof PropelCollection) {
            $this->collFacturaReceptors->clearIterator();
        }
        $this->collFacturaReceptors = null;
        if ($this->collComEnviadas instanceof PropelCollection) {
            $this->collComEnviadas->clearIterator();
        }
        $this->collComEnviadas = null;
        if ($this->collComInternas instanceof PropelCollection) {
            $this->collComInternas->clearIterator();
        }
        $this->collComInternas = null;
        if ($this->collComPermisoRegionals instanceof PropelCollection) {
            $this->collComPermisoRegionals->clearIterator();
        }
        $this->collComPermisoRegionals = null;
        if ($this->collComRecibidas instanceof PropelCollection) {
            $this->collComRecibidas->clearIterator();
        }
        $this->collComRecibidas = null;
        if ($this->collHistUsuarios instanceof PropelCollection) {
            $this->collHistUsuarios->clearIterator();
        }
        $this->collHistUsuarios = null;
        if ($this->collUnidadDocumentals instanceof PropelCollection) {
            $this->collUnidadDocumentals->clearIterator();
        }
        $this->collUnidadDocumentals = null;
        if ($this->collUsuarios instanceof PropelCollection) {
            $this->collUsuarios->clearIterator();
        }
        $this->collUsuarios = null;
        $this->aCiudad = null;
        $this->aEntidad = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(RegionalPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseRegional:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

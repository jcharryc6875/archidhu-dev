<?php


/**
 * Base class that represents a query for the 'REPRESENTANTE_LEGAL' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:38
 *
 * @method RepresentanteLegalQuery orderByRepresentantelegalId($order = Criteria::ASC) Order by the REPRESENTANTELEGAL_ID column
 * @method RepresentanteLegalQuery orderByCiudadId($order = Criteria::ASC) Order by the CIUDAD_ID column
 * @method RepresentanteLegalQuery orderByTipoidentificacionId($order = Criteria::ASC) Order by the TIPOIDENTIFICACION_ID column
 * @method RepresentanteLegalQuery orderByTipogeneroId($order = Criteria::ASC) Order by the TIPOGENERO_ID column
 * @method RepresentanteLegalQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method RepresentanteLegalQuery orderByPrimerNombre($order = Criteria::ASC) Order by the PRIMER_NOMBRE column
 * @method RepresentanteLegalQuery orderBySegundoNombre($order = Criteria::ASC) Order by the SEGUNDO_NOMBRE column
 * @method RepresentanteLegalQuery orderByPrimerApellido($order = Criteria::ASC) Order by the PRIMER_APELLIDO column
 * @method RepresentanteLegalQuery orderBySegundoApellido($order = Criteria::ASC) Order by the SEGUNDO_APELLIDO column
 * @method RepresentanteLegalQuery orderByNumeroIdentificacion($order = Criteria::ASC) Order by the NUMERO_IDENTIFICACION column
 * @method RepresentanteLegalQuery orderByDireccion($order = Criteria::ASC) Order by the DIRECCION column
 * @method RepresentanteLegalQuery orderByCodigoPostal($order = Criteria::ASC) Order by the CODIGO_POSTAL column
 * @method RepresentanteLegalQuery orderByTelefono($order = Criteria::ASC) Order by the TELEFONO column
 * @method RepresentanteLegalQuery orderByCelular($order = Criteria::ASC) Order by the CELULAR column
 * @method RepresentanteLegalQuery orderByFax($order = Criteria::ASC) Order by the FAX column
 * @method RepresentanteLegalQuery orderByEmail($order = Criteria::ASC) Order by the EMAIL column
 * @method RepresentanteLegalQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method RepresentanteLegalQuery orderByFechaModificacion($order = Criteria::ASC) Order by the FECHA_MODIFICACION column
 *
 * @method RepresentanteLegalQuery groupByRepresentantelegalId() Group by the REPRESENTANTELEGAL_ID column
 * @method RepresentanteLegalQuery groupByCiudadId() Group by the CIUDAD_ID column
 * @method RepresentanteLegalQuery groupByTipoidentificacionId() Group by the TIPOIDENTIFICACION_ID column
 * @method RepresentanteLegalQuery groupByTipogeneroId() Group by the TIPOGENERO_ID column
 * @method RepresentanteLegalQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method RepresentanteLegalQuery groupByPrimerNombre() Group by the PRIMER_NOMBRE column
 * @method RepresentanteLegalQuery groupBySegundoNombre() Group by the SEGUNDO_NOMBRE column
 * @method RepresentanteLegalQuery groupByPrimerApellido() Group by the PRIMER_APELLIDO column
 * @method RepresentanteLegalQuery groupBySegundoApellido() Group by the SEGUNDO_APELLIDO column
 * @method RepresentanteLegalQuery groupByNumeroIdentificacion() Group by the NUMERO_IDENTIFICACION column
 * @method RepresentanteLegalQuery groupByDireccion() Group by the DIRECCION column
 * @method RepresentanteLegalQuery groupByCodigoPostal() Group by the CODIGO_POSTAL column
 * @method RepresentanteLegalQuery groupByTelefono() Group by the TELEFONO column
 * @method RepresentanteLegalQuery groupByCelular() Group by the CELULAR column
 * @method RepresentanteLegalQuery groupByFax() Group by the FAX column
 * @method RepresentanteLegalQuery groupByEmail() Group by the EMAIL column
 * @method RepresentanteLegalQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method RepresentanteLegalQuery groupByFechaModificacion() Group by the FECHA_MODIFICACION column
 *
 * @method RepresentanteLegalQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method RepresentanteLegalQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method RepresentanteLegalQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method RepresentanteLegalQuery leftJoinCiudad($relationAlias = null) Adds a LEFT JOIN clause to the query using the Ciudad relation
 * @method RepresentanteLegalQuery rightJoinCiudad($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Ciudad relation
 * @method RepresentanteLegalQuery innerJoinCiudad($relationAlias = null) Adds a INNER JOIN clause to the query using the Ciudad relation
 *
 * @method RepresentanteLegalQuery leftJoinTipoIdentificacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoIdentificacion relation
 * @method RepresentanteLegalQuery rightJoinTipoIdentificacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoIdentificacion relation
 * @method RepresentanteLegalQuery innerJoinTipoIdentificacion($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoIdentificacion relation
 *
 * @method RepresentanteLegalQuery leftJoinTipoGenero($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoGenero relation
 * @method RepresentanteLegalQuery rightJoinTipoGenero($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoGenero relation
 * @method RepresentanteLegalQuery innerJoinTipoGenero($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoGenero relation
 *
 * @method RepresentanteLegalQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method RepresentanteLegalQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method RepresentanteLegalQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method RepresentanteLegalQuery leftJoinComrecibidaInteresados($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComrecibidaInteresados relation
 * @method RepresentanteLegalQuery rightJoinComrecibidaInteresados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComrecibidaInteresados relation
 * @method RepresentanteLegalQuery innerJoinComrecibidaInteresados($relationAlias = null) Adds a INNER JOIN clause to the query using the ComrecibidaInteresados relation
 *
 * @method RepresentanteLegalQuery leftJoinUnidaddocumentalInteresados($relationAlias = null) Adds a LEFT JOIN clause to the query using the UnidaddocumentalInteresados relation
 * @method RepresentanteLegalQuery rightJoinUnidaddocumentalInteresados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UnidaddocumentalInteresados relation
 * @method RepresentanteLegalQuery innerJoinUnidaddocumentalInteresados($relationAlias = null) Adds a INNER JOIN clause to the query using the UnidaddocumentalInteresados relation
 *
 * @method RepresentanteLegalQuery leftJoinReprlegalInteresado($relationAlias = null) Adds a LEFT JOIN clause to the query using the ReprlegalInteresado relation
 * @method RepresentanteLegalQuery rightJoinReprlegalInteresado($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ReprlegalInteresado relation
 * @method RepresentanteLegalQuery innerJoinReprlegalInteresado($relationAlias = null) Adds a INNER JOIN clause to the query using the ReprlegalInteresado relation
 *
 * @method RepresentanteLegalQuery leftJoinServicioInteresados($relationAlias = null) Adds a LEFT JOIN clause to the query using the ServicioInteresados relation
 * @method RepresentanteLegalQuery rightJoinServicioInteresados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ServicioInteresados relation
 * @method RepresentanteLegalQuery innerJoinServicioInteresados($relationAlias = null) Adds a INNER JOIN clause to the query using the ServicioInteresados relation
 *
 * @method RepresentanteLegalQuery leftJoinEnviadaInteresados($relationAlias = null) Adds a LEFT JOIN clause to the query using the EnviadaInteresados relation
 * @method RepresentanteLegalQuery rightJoinEnviadaInteresados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EnviadaInteresados relation
 * @method RepresentanteLegalQuery innerJoinEnviadaInteresados($relationAlias = null) Adds a INNER JOIN clause to the query using the EnviadaInteresados relation
 *
 * @method RepresentanteLegal findOne(PropelPDO $con = null) Return the first RepresentanteLegal matching the query
 * @method RepresentanteLegal findOneOrCreate(PropelPDO $con = null) Return the first RepresentanteLegal matching the query, or a new RepresentanteLegal object populated from the query conditions when no match is found
 *
 * @method RepresentanteLegal findOneByCiudadId(int $CIUDAD_ID) Return the first RepresentanteLegal filtered by the CIUDAD_ID column
 * @method RepresentanteLegal findOneByTipoidentificacionId(int $TIPOIDENTIFICACION_ID) Return the first RepresentanteLegal filtered by the TIPOIDENTIFICACION_ID column
 * @method RepresentanteLegal findOneByTipogeneroId(int $TIPOGENERO_ID) Return the first RepresentanteLegal filtered by the TIPOGENERO_ID column
 * @method RepresentanteLegal findOneByUsuarioId(int $USUARIO_ID) Return the first RepresentanteLegal filtered by the USUARIO_ID column
 * @method RepresentanteLegal findOneByPrimerNombre(string $PRIMER_NOMBRE) Return the first RepresentanteLegal filtered by the PRIMER_NOMBRE column
 * @method RepresentanteLegal findOneBySegundoNombre(string $SEGUNDO_NOMBRE) Return the first RepresentanteLegal filtered by the SEGUNDO_NOMBRE column
 * @method RepresentanteLegal findOneByPrimerApellido(string $PRIMER_APELLIDO) Return the first RepresentanteLegal filtered by the PRIMER_APELLIDO column
 * @method RepresentanteLegal findOneBySegundoApellido(string $SEGUNDO_APELLIDO) Return the first RepresentanteLegal filtered by the SEGUNDO_APELLIDO column
 * @method RepresentanteLegal findOneByNumeroIdentificacion(string $NUMERO_IDENTIFICACION) Return the first RepresentanteLegal filtered by the NUMERO_IDENTIFICACION column
 * @method RepresentanteLegal findOneByDireccion(string $DIRECCION) Return the first RepresentanteLegal filtered by the DIRECCION column
 * @method RepresentanteLegal findOneByCodigoPostal(string $CODIGO_POSTAL) Return the first RepresentanteLegal filtered by the CODIGO_POSTAL column
 * @method RepresentanteLegal findOneByTelefono(string $TELEFONO) Return the first RepresentanteLegal filtered by the TELEFONO column
 * @method RepresentanteLegal findOneByCelular(string $CELULAR) Return the first RepresentanteLegal filtered by the CELULAR column
 * @method RepresentanteLegal findOneByFax(string $FAX) Return the first RepresentanteLegal filtered by the FAX column
 * @method RepresentanteLegal findOneByEmail(string $EMAIL) Return the first RepresentanteLegal filtered by the EMAIL column
 * @method RepresentanteLegal findOneByFechaCreacion(string $FECHA_CREACION) Return the first RepresentanteLegal filtered by the FECHA_CREACION column
 * @method RepresentanteLegal findOneByFechaModificacion(string $FECHA_MODIFICACION) Return the first RepresentanteLegal filtered by the FECHA_MODIFICACION column
 *
 * @method array findByRepresentantelegalId(int $REPRESENTANTELEGAL_ID) Return RepresentanteLegal objects filtered by the REPRESENTANTELEGAL_ID column
 * @method array findByCiudadId(int $CIUDAD_ID) Return RepresentanteLegal objects filtered by the CIUDAD_ID column
 * @method array findByTipoidentificacionId(int $TIPOIDENTIFICACION_ID) Return RepresentanteLegal objects filtered by the TIPOIDENTIFICACION_ID column
 * @method array findByTipogeneroId(int $TIPOGENERO_ID) Return RepresentanteLegal objects filtered by the TIPOGENERO_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return RepresentanteLegal objects filtered by the USUARIO_ID column
 * @method array findByPrimerNombre(string $PRIMER_NOMBRE) Return RepresentanteLegal objects filtered by the PRIMER_NOMBRE column
 * @method array findBySegundoNombre(string $SEGUNDO_NOMBRE) Return RepresentanteLegal objects filtered by the SEGUNDO_NOMBRE column
 * @method array findByPrimerApellido(string $PRIMER_APELLIDO) Return RepresentanteLegal objects filtered by the PRIMER_APELLIDO column
 * @method array findBySegundoApellido(string $SEGUNDO_APELLIDO) Return RepresentanteLegal objects filtered by the SEGUNDO_APELLIDO column
 * @method array findByNumeroIdentificacion(string $NUMERO_IDENTIFICACION) Return RepresentanteLegal objects filtered by the NUMERO_IDENTIFICACION column
 * @method array findByDireccion(string $DIRECCION) Return RepresentanteLegal objects filtered by the DIRECCION column
 * @method array findByCodigoPostal(string $CODIGO_POSTAL) Return RepresentanteLegal objects filtered by the CODIGO_POSTAL column
 * @method array findByTelefono(string $TELEFONO) Return RepresentanteLegal objects filtered by the TELEFONO column
 * @method array findByCelular(string $CELULAR) Return RepresentanteLegal objects filtered by the CELULAR column
 * @method array findByFax(string $FAX) Return RepresentanteLegal objects filtered by the FAX column
 * @method array findByEmail(string $EMAIL) Return RepresentanteLegal objects filtered by the EMAIL column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return RepresentanteLegal objects filtered by the FECHA_CREACION column
 * @method array findByFechaModificacion(string $FECHA_MODIFICACION) Return RepresentanteLegal objects filtered by the FECHA_MODIFICACION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseRepresentanteLegalQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseRepresentanteLegalQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'RepresentanteLegal';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new RepresentanteLegalQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   RepresentanteLegalQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return RepresentanteLegalQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof RepresentanteLegalQuery) {
            return $criteria;
        }
        $query = new RepresentanteLegalQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   RepresentanteLegal|RepresentanteLegal[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = RepresentanteLegalPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(RepresentanteLegalPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 RepresentanteLegal A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByRepresentantelegalId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 RepresentanteLegal A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [REPRESENTANTELEGAL_ID], [CIUDAD_ID], [TIPOIDENTIFICACION_ID], [TIPOGENERO_ID], [USUARIO_ID], [PRIMER_NOMBRE], [SEGUNDO_NOMBRE], [PRIMER_APELLIDO], [SEGUNDO_APELLIDO], [NUMERO_IDENTIFICACION], [DIRECCION], [CODIGO_POSTAL], [TELEFONO], [CELULAR], [FAX], [EMAIL], [FECHA_CREACION], [FECHA_MODIFICACION] FROM [REPRESENTANTE_LEGAL] WHERE [REPRESENTANTELEGAL_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new RepresentanteLegal();
            $obj->hydrate($row);
            RepresentanteLegalPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return RepresentanteLegal|RepresentanteLegal[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|RepresentanteLegal[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the REPRESENTANTELEGAL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRepresentantelegalId(1234); // WHERE REPRESENTANTELEGAL_ID = 1234
     * $query->filterByRepresentantelegalId(array(12, 34)); // WHERE REPRESENTANTELEGAL_ID IN (12, 34)
     * $query->filterByRepresentantelegalId(array('min' => 12)); // WHERE REPRESENTANTELEGAL_ID >= 12
     * $query->filterByRepresentantelegalId(array('max' => 12)); // WHERE REPRESENTANTELEGAL_ID <= 12
     * </code>
     *
     * @param     mixed $representantelegalId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByRepresentantelegalId($representantelegalId = null, $comparison = null)
    {
        if (is_array($representantelegalId)) {
            $useMinMax = false;
            if (isset($representantelegalId['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $representantelegalId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($representantelegalId['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $representantelegalId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $representantelegalId, $comparison);
    }

    /**
     * Filter the query on the CIUDAD_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByCiudadId(1234); // WHERE CIUDAD_ID = 1234
     * $query->filterByCiudadId(array(12, 34)); // WHERE CIUDAD_ID IN (12, 34)
     * $query->filterByCiudadId(array('min' => 12)); // WHERE CIUDAD_ID >= 12
     * $query->filterByCiudadId(array('max' => 12)); // WHERE CIUDAD_ID <= 12
     * </code>
     *
     * @see       filterByCiudad()
     *
     * @param     mixed $ciudadId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByCiudadId($ciudadId = null, $comparison = null)
    {
        if (is_array($ciudadId)) {
            $useMinMax = false;
            if (isset($ciudadId['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::CIUDAD_ID, $ciudadId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ciudadId['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::CIUDAD_ID, $ciudadId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::CIUDAD_ID, $ciudadId, $comparison);
    }

    /**
     * Filter the query on the TIPOIDENTIFICACION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoidentificacionId(1234); // WHERE TIPOIDENTIFICACION_ID = 1234
     * $query->filterByTipoidentificacionId(array(12, 34)); // WHERE TIPOIDENTIFICACION_ID IN (12, 34)
     * $query->filterByTipoidentificacionId(array('min' => 12)); // WHERE TIPOIDENTIFICACION_ID >= 12
     * $query->filterByTipoidentificacionId(array('max' => 12)); // WHERE TIPOIDENTIFICACION_ID <= 12
     * </code>
     *
     * @see       filterByTipoIdentificacion()
     *
     * @param     mixed $tipoidentificacionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByTipoidentificacionId($tipoidentificacionId = null, $comparison = null)
    {
        if (is_array($tipoidentificacionId)) {
            $useMinMax = false;
            if (isset($tipoidentificacionId['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::TIPOIDENTIFICACION_ID, $tipoidentificacionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipoidentificacionId['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::TIPOIDENTIFICACION_ID, $tipoidentificacionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::TIPOIDENTIFICACION_ID, $tipoidentificacionId, $comparison);
    }

    /**
     * Filter the query on the TIPOGENERO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipogeneroId(1234); // WHERE TIPOGENERO_ID = 1234
     * $query->filterByTipogeneroId(array(12, 34)); // WHERE TIPOGENERO_ID IN (12, 34)
     * $query->filterByTipogeneroId(array('min' => 12)); // WHERE TIPOGENERO_ID >= 12
     * $query->filterByTipogeneroId(array('max' => 12)); // WHERE TIPOGENERO_ID <= 12
     * </code>
     *
     * @see       filterByTipoGenero()
     *
     * @param     mixed $tipogeneroId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByTipogeneroId($tipogeneroId = null, $comparison = null)
    {
        if (is_array($tipogeneroId)) {
            $useMinMax = false;
            if (isset($tipogeneroId['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::TIPOGENERO_ID, $tipogeneroId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipogeneroId['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::TIPOGENERO_ID, $tipogeneroId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::TIPOGENERO_ID, $tipogeneroId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the PRIMER_NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterByPrimerNombre('fooValue');   // WHERE PRIMER_NOMBRE = 'fooValue'
     * $query->filterByPrimerNombre('%fooValue%'); // WHERE PRIMER_NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $primerNombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByPrimerNombre($primerNombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($primerNombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $primerNombre)) {
                $primerNombre = str_replace('*', '%', $primerNombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::PRIMER_NOMBRE, $primerNombre, $comparison);
    }

    /**
     * Filter the query on the SEGUNDO_NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterBySegundoNombre('fooValue');   // WHERE SEGUNDO_NOMBRE = 'fooValue'
     * $query->filterBySegundoNombre('%fooValue%'); // WHERE SEGUNDO_NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $segundoNombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterBySegundoNombre($segundoNombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($segundoNombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $segundoNombre)) {
                $segundoNombre = str_replace('*', '%', $segundoNombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::SEGUNDO_NOMBRE, $segundoNombre, $comparison);
    }

    /**
     * Filter the query on the PRIMER_APELLIDO column
     *
     * Example usage:
     * <code>
     * $query->filterByPrimerApellido('fooValue');   // WHERE PRIMER_APELLIDO = 'fooValue'
     * $query->filterByPrimerApellido('%fooValue%'); // WHERE PRIMER_APELLIDO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $primerApellido The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByPrimerApellido($primerApellido = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($primerApellido)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $primerApellido)) {
                $primerApellido = str_replace('*', '%', $primerApellido);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::PRIMER_APELLIDO, $primerApellido, $comparison);
    }

    /**
     * Filter the query on the SEGUNDO_APELLIDO column
     *
     * Example usage:
     * <code>
     * $query->filterBySegundoApellido('fooValue');   // WHERE SEGUNDO_APELLIDO = 'fooValue'
     * $query->filterBySegundoApellido('%fooValue%'); // WHERE SEGUNDO_APELLIDO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $segundoApellido The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterBySegundoApellido($segundoApellido = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($segundoApellido)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $segundoApellido)) {
                $segundoApellido = str_replace('*', '%', $segundoApellido);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::SEGUNDO_APELLIDO, $segundoApellido, $comparison);
    }

    /**
     * Filter the query on the NUMERO_IDENTIFICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroIdentificacion('fooValue');   // WHERE NUMERO_IDENTIFICACION = 'fooValue'
     * $query->filterByNumeroIdentificacion('%fooValue%'); // WHERE NUMERO_IDENTIFICACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $numeroIdentificacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByNumeroIdentificacion($numeroIdentificacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($numeroIdentificacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $numeroIdentificacion)) {
                $numeroIdentificacion = str_replace('*', '%', $numeroIdentificacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::NUMERO_IDENTIFICACION, $numeroIdentificacion, $comparison);
    }

    /**
     * Filter the query on the DIRECCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDireccion('fooValue');   // WHERE DIRECCION = 'fooValue'
     * $query->filterByDireccion('%fooValue%'); // WHERE DIRECCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $direccion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByDireccion($direccion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($direccion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $direccion)) {
                $direccion = str_replace('*', '%', $direccion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::DIRECCION, $direccion, $comparison);
    }

    /**
     * Filter the query on the CODIGO_POSTAL column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoPostal('fooValue');   // WHERE CODIGO_POSTAL = 'fooValue'
     * $query->filterByCodigoPostal('%fooValue%'); // WHERE CODIGO_POSTAL LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigoPostal The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByCodigoPostal($codigoPostal = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigoPostal)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigoPostal)) {
                $codigoPostal = str_replace('*', '%', $codigoPostal);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::CODIGO_POSTAL, $codigoPostal, $comparison);
    }

    /**
     * Filter the query on the TELEFONO column
     *
     * Example usage:
     * <code>
     * $query->filterByTelefono('fooValue');   // WHERE TELEFONO = 'fooValue'
     * $query->filterByTelefono('%fooValue%'); // WHERE TELEFONO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $telefono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByTelefono($telefono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($telefono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $telefono)) {
                $telefono = str_replace('*', '%', $telefono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::TELEFONO, $telefono, $comparison);
    }

    /**
     * Filter the query on the CELULAR column
     *
     * Example usage:
     * <code>
     * $query->filterByCelular('fooValue');   // WHERE CELULAR = 'fooValue'
     * $query->filterByCelular('%fooValue%'); // WHERE CELULAR LIKE '%fooValue%'
     * </code>
     *
     * @param     string $celular The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByCelular($celular = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($celular)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $celular)) {
                $celular = str_replace('*', '%', $celular);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::CELULAR, $celular, $comparison);
    }

    /**
     * Filter the query on the FAX column
     *
     * Example usage:
     * <code>
     * $query->filterByFax('fooValue');   // WHERE FAX = 'fooValue'
     * $query->filterByFax('%fooValue%'); // WHERE FAX LIKE '%fooValue%'
     * </code>
     *
     * @param     string $fax The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByFax($fax = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($fax)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $fax)) {
                $fax = str_replace('*', '%', $fax);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::FAX, $fax, $comparison);
    }

    /**
     * Filter the query on the EMAIL column
     *
     * Example usage:
     * <code>
     * $query->filterByEmail('fooValue');   // WHERE EMAIL = 'fooValue'
     * $query->filterByEmail('%fooValue%'); // WHERE EMAIL LIKE '%fooValue%'
     * </code>
     *
     * @param     string $email The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByEmail($email = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($email)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $email)) {
                $email = str_replace('*', '%', $email);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::EMAIL, $email, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_MODIFICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaModificacion('2011-03-14'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion('now'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion(array('max' => 'yesterday')); // WHERE FECHA_MODIFICACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaModificacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function filterByFechaModificacion($fechaModificacion = null, $comparison = null)
    {
        if (is_array($fechaModificacion)) {
            $useMinMax = false;
            if (isset($fechaModificacion['min'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::FECHA_MODIFICACION, $fechaModificacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaModificacion['max'])) {
                $this->addUsingAlias(RepresentanteLegalPeer::FECHA_MODIFICACION, $fechaModificacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RepresentanteLegalPeer::FECHA_MODIFICACION, $fechaModificacion, $comparison);
    }

    /**
     * Filter the query by a related Ciudad object
     *
     * @param   Ciudad|PropelObjectCollection $ciudad The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByCiudad($ciudad, $comparison = null)
    {
        if ($ciudad instanceof Ciudad) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::CIUDAD_ID, $ciudad->getCiudadId(), $comparison);
        } elseif ($ciudad instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RepresentanteLegalPeer::CIUDAD_ID, $ciudad->toKeyValue('PrimaryKey', 'CiudadId'), $comparison);
        } else {
            throw new PropelException('filterByCiudad() only accepts arguments of type Ciudad or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Ciudad relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinCiudad($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Ciudad');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Ciudad');
        }

        return $this;
    }

    /**
     * Use the Ciudad relation Ciudad object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CiudadQuery A secondary query class using the current class as primary query
     */
    public function useCiudadQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinCiudad($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Ciudad', 'CiudadQuery');
    }

    /**
     * Filter the query by a related TipoIdentificacion object
     *
     * @param   TipoIdentificacion|PropelObjectCollection $tipoIdentificacion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoIdentificacion($tipoIdentificacion, $comparison = null)
    {
        if ($tipoIdentificacion instanceof TipoIdentificacion) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::TIPOIDENTIFICACION_ID, $tipoIdentificacion->getTipoidentificacionId(), $comparison);
        } elseif ($tipoIdentificacion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RepresentanteLegalPeer::TIPOIDENTIFICACION_ID, $tipoIdentificacion->toKeyValue('PrimaryKey', 'TipoidentificacionId'), $comparison);
        } else {
            throw new PropelException('filterByTipoIdentificacion() only accepts arguments of type TipoIdentificacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoIdentificacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinTipoIdentificacion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoIdentificacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoIdentificacion');
        }

        return $this;
    }

    /**
     * Use the TipoIdentificacion relation TipoIdentificacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoIdentificacionQuery A secondary query class using the current class as primary query
     */
    public function useTipoIdentificacionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTipoIdentificacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoIdentificacion', 'TipoIdentificacionQuery');
    }

    /**
     * Filter the query by a related TipoGenero object
     *
     * @param   TipoGenero|PropelObjectCollection $tipoGenero The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoGenero($tipoGenero, $comparison = null)
    {
        if ($tipoGenero instanceof TipoGenero) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::TIPOGENERO_ID, $tipoGenero->getTipogeneroId(), $comparison);
        } elseif ($tipoGenero instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RepresentanteLegalPeer::TIPOGENERO_ID, $tipoGenero->toKeyValue('PrimaryKey', 'TipogeneroId'), $comparison);
        } else {
            throw new PropelException('filterByTipoGenero() only accepts arguments of type TipoGenero or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoGenero relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinTipoGenero($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoGenero');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoGenero');
        }

        return $this;
    }

    /**
     * Use the TipoGenero relation TipoGenero object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoGeneroQuery A secondary query class using the current class as primary query
     */
    public function useTipoGeneroQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoGenero($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoGenero', 'TipoGeneroQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RepresentanteLegalPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related ComrecibidaInteresados object
     *
     * @param   ComrecibidaInteresados|PropelObjectCollection $comrecibidaInteresados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComrecibidaInteresados($comrecibidaInteresados, $comparison = null)
    {
        if ($comrecibidaInteresados instanceof ComrecibidaInteresados) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $comrecibidaInteresados->getRepresentantelegalId(), $comparison);
        } elseif ($comrecibidaInteresados instanceof PropelObjectCollection) {
            return $this
                ->useComrecibidaInteresadosQuery()
                ->filterByPrimaryKeys($comrecibidaInteresados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComrecibidaInteresados() only accepts arguments of type ComrecibidaInteresados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComrecibidaInteresados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinComrecibidaInteresados($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComrecibidaInteresados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComrecibidaInteresados');
        }

        return $this;
    }

    /**
     * Use the ComrecibidaInteresados relation ComrecibidaInteresados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComrecibidaInteresadosQuery A secondary query class using the current class as primary query
     */
    public function useComrecibidaInteresadosQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComrecibidaInteresados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComrecibidaInteresados', 'ComrecibidaInteresadosQuery');
    }

    /**
     * Filter the query by a related UnidaddocumentalInteresados object
     *
     * @param   UnidaddocumentalInteresados|PropelObjectCollection $unidaddocumentalInteresados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUnidaddocumentalInteresados($unidaddocumentalInteresados, $comparison = null)
    {
        if ($unidaddocumentalInteresados instanceof UnidaddocumentalInteresados) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $unidaddocumentalInteresados->getRepresentantelegalId(), $comparison);
        } elseif ($unidaddocumentalInteresados instanceof PropelObjectCollection) {
            return $this
                ->useUnidaddocumentalInteresadosQuery()
                ->filterByPrimaryKeys($unidaddocumentalInteresados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUnidaddocumentalInteresados() only accepts arguments of type UnidaddocumentalInteresados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UnidaddocumentalInteresados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinUnidaddocumentalInteresados($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UnidaddocumentalInteresados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UnidaddocumentalInteresados');
        }

        return $this;
    }

    /**
     * Use the UnidaddocumentalInteresados relation UnidaddocumentalInteresados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UnidaddocumentalInteresadosQuery A secondary query class using the current class as primary query
     */
    public function useUnidaddocumentalInteresadosQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUnidaddocumentalInteresados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UnidaddocumentalInteresados', 'UnidaddocumentalInteresadosQuery');
    }

    /**
     * Filter the query by a related ReprlegalInteresado object
     *
     * @param   ReprlegalInteresado|PropelObjectCollection $reprlegalInteresado  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByReprlegalInteresado($reprlegalInteresado, $comparison = null)
    {
        if ($reprlegalInteresado instanceof ReprlegalInteresado) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $reprlegalInteresado->getRepresentantelegalId(), $comparison);
        } elseif ($reprlegalInteresado instanceof PropelObjectCollection) {
            return $this
                ->useReprlegalInteresadoQuery()
                ->filterByPrimaryKeys($reprlegalInteresado->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByReprlegalInteresado() only accepts arguments of type ReprlegalInteresado or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ReprlegalInteresado relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinReprlegalInteresado($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ReprlegalInteresado');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ReprlegalInteresado');
        }

        return $this;
    }

    /**
     * Use the ReprlegalInteresado relation ReprlegalInteresado object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ReprlegalInteresadoQuery A secondary query class using the current class as primary query
     */
    public function useReprlegalInteresadoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinReprlegalInteresado($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ReprlegalInteresado', 'ReprlegalInteresadoQuery');
    }

    /**
     * Filter the query by a related ServicioInteresados object
     *
     * @param   ServicioInteresados|PropelObjectCollection $servicioInteresados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByServicioInteresados($servicioInteresados, $comparison = null)
    {
        if ($servicioInteresados instanceof ServicioInteresados) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $servicioInteresados->getRepresentantelegalId(), $comparison);
        } elseif ($servicioInteresados instanceof PropelObjectCollection) {
            return $this
                ->useServicioInteresadosQuery()
                ->filterByPrimaryKeys($servicioInteresados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByServicioInteresados() only accepts arguments of type ServicioInteresados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ServicioInteresados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinServicioInteresados($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ServicioInteresados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ServicioInteresados');
        }

        return $this;
    }

    /**
     * Use the ServicioInteresados relation ServicioInteresados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ServicioInteresadosQuery A secondary query class using the current class as primary query
     */
    public function useServicioInteresadosQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinServicioInteresados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ServicioInteresados', 'ServicioInteresadosQuery');
    }

    /**
     * Filter the query by a related EnviadaInteresados object
     *
     * @param   EnviadaInteresados|PropelObjectCollection $enviadaInteresados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RepresentanteLegalQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEnviadaInteresados($enviadaInteresados, $comparison = null)
    {
        if ($enviadaInteresados instanceof EnviadaInteresados) {
            return $this
                ->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $enviadaInteresados->getRepresentantelegalId(), $comparison);
        } elseif ($enviadaInteresados instanceof PropelObjectCollection) {
            return $this
                ->useEnviadaInteresadosQuery()
                ->filterByPrimaryKeys($enviadaInteresados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEnviadaInteresados() only accepts arguments of type EnviadaInteresados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EnviadaInteresados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function joinEnviadaInteresados($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EnviadaInteresados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EnviadaInteresados');
        }

        return $this;
    }

    /**
     * Use the EnviadaInteresados relation EnviadaInteresados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EnviadaInteresadosQuery A secondary query class using the current class as primary query
     */
    public function useEnviadaInteresadosQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEnviadaInteresados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EnviadaInteresados', 'EnviadaInteresadosQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   RepresentanteLegal $representanteLegal Object to remove from the list of results
     *
     * @return RepresentanteLegalQuery The current query, for fluid interface
     */
    public function prune($representanteLegal = null)
    {
        if ($representanteLegal) {
            $this->addUsingAlias(RepresentanteLegalPeer::REPRESENTANTELEGAL_ID, $representanteLegal->getRepresentantelegalId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

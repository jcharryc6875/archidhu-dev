<?php


/**
 * Base class that represents a query for the 'COM_RECIBIDA' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * Thu Sep 17 15:28:27 2026
 *
 * @method ComRecibidaQuery orderByComrecibidaId($order = Criteria::ASC) Order by the COMRECIBIDA_ID column
 * @method ComRecibidaQuery orderByRegionalId($order = Criteria::ASC) Order by the REGIONAL_ID column
 * @method ComRecibidaQuery orderByComenviadaId($order = Criteria::ASC) Order by the COMENVIADA_ID column
 * @method ComRecibidaQuery orderByAsuntorecibidaId($order = Criteria::ASC) Order by the ASUNTORECIBIDA_ID column
 * @method ComRecibidaQuery orderByTipocomrecibidaId($order = Criteria::ASC) Order by the TIPOCOMRECIBIDA_ID column
 * @method ComRecibidaQuery orderByPeriodoId($order = Criteria::ASC) Order by the PERIODO_ID column
 * @method ComRecibidaQuery orderByDependenciaId($order = Criteria::ASC) Order by the DEPENDENCIA_ID column
 * @method ComRecibidaQuery orderByEstadodigitalizacionId($order = Criteria::ASC) Order by the ESTADODIGITALIZACION_ID column
 * @method ComRecibidaQuery orderByEstadocomrecibidaId($order = Criteria::ASC) Order by the ESTADOCOMRECIBIDA_ID column
 * @method ComRecibidaQuery orderByFormarecepcionId($order = Criteria::ASC) Order by the FORMARECEPCION_ID column
 * @method ComRecibidaQuery orderByCiudadId($order = Criteria::ASC) Order by the CIUDAD_ID column
 * @method ComRecibidaQuery orderByDirectorioexternoId($order = Criteria::ASC) Order by the DIRECTORIOEXTERNO_ID column
 * @method ComRecibidaQuery orderByEmpresaMensajeriaId($order = Criteria::ASC) Order by the EMPRESA_MENSAJERIA_ID column
 * @method ComRecibidaQuery orderByTipofirmadigitalId($order = Criteria::ASC) Order by the TIPOFIRMADIGITAL_ID column
 * @method ComRecibidaQuery orderByMediorespuestaId($order = Criteria::ASC) Order by the MEDIORESPUESTA_ID column
 * @method ComRecibidaQuery orderByTiporespuestaId($order = Criteria::ASC) Order by the TIPORESPUESTA_ID column
 * @method ComRecibidaQuery orderByPrioridadcomId($order = Criteria::ASC) Order by the PRIORIDADCOM_ID column
 * @method ComRecibidaQuery orderByTipoexpedienteId($order = Criteria::ASC) Order by the TIPOEXPEDIENTE_ID column
 * @method ComRecibidaQuery orderByTipoprocesocomId($order = Criteria::ASC) Order by the TIPOPROCESOCOM_ID column
 * @method ComRecibidaQuery orderByRadicadoOrigen($order = Criteria::ASC) Order by the RADICADO_ORIGEN column
 * @method ComRecibidaQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method ComRecibidaQuery orderByAsunto($order = Criteria::ASC) Order by the ASUNTO column
 * @method ComRecibidaQuery orderByNumeroRadicacion($order = Criteria::ASC) Order by the NUMERO_RADICACION column
 * @method ComRecibidaQuery orderByRadicado($order = Criteria::ASC) Order by the RADICADO column
 * @method ComRecibidaQuery orderByEsCopia($order = Criteria::ASC) Order by the ES_COPIA column
 * @method ComRecibidaQuery orderByGuia($order = Criteria::ASC) Order by the GUIA column
 * @method ComRecibidaQuery orderByFechaEnvioGuia($order = Criteria::ASC) Order by the FECHA_ENVIO_GUIA column
 * @method ComRecibidaQuery orderByValorGuia($order = Criteria::ASC) Order by the VALOR_GUIA column
 * @method ComRecibidaQuery orderByFolios($order = Criteria::ASC) Order by the FOLIOS column
 * @method ComRecibidaQuery orderByAnexos($order = Criteria::ASC) Order by the ANEXOS column
 * @method ComRecibidaQuery orderByRuta($order = Criteria::ASC) Order by the RUTA column
 * @method ComRecibidaQuery orderByCodigoReenResp($order = Criteria::ASC) Order by the CODIGO_REEN_RESP column
 * @method ComRecibidaQuery orderByObsReenResp($order = Criteria::ASC) Order by the OBS_REEN_RESP column
 * @method ComRecibidaQuery orderByEstaentregado($order = Criteria::ASC) Order by the ESTAENTREGADO column
 * @method ComRecibidaQuery orderByFechaMaximaRespuesta($order = Criteria::ASC) Order by the FECHA_MAXIMA_RESPUESTA column
 * @method ComRecibidaQuery orderByFechaDeAnulacion($order = Criteria::ASC) Order by the FECHA_DE_ANULACION column
 * @method ComRecibidaQuery orderByObsAnulacion($order = Criteria::ASC) Order by the OBS_ANULACION column
 * @method ComRecibidaQuery orderByMarca($order = Criteria::ASC) Order by the MARCA column
 * @method ComRecibidaQuery orderByFechaRespuestaReen($order = Criteria::ASC) Order by the FECHA_RESPUESTA_REEN column
 * @method ComRecibidaQuery orderByObservaciones($order = Criteria::ASC) Order by the OBSERVACIONES column
 * @method ComRecibidaQuery orderByInstancia($order = Criteria::ASC) Order by the INSTANCIA column
 * @method ComRecibidaQuery orderByBitacora($order = Criteria::ASC) Order by the BITACORA column
 * @method ComRecibidaQuery orderByObsRemitir($order = Criteria::ASC) Order by the OBS_REMITIR column
 * @method ComRecibidaQuery orderByDestinoComrecibida($order = Criteria::ASC) Order by the DESTINO_COMRECIBIDA column
 * @method ComRecibidaQuery orderByFechaDocumento($order = Criteria::ASC) Order by the FECHA_DOCUMENTO column
 * @method ComRecibidaQuery orderByMarcaVinculacion($order = Criteria::ASC) Order by the MARCA_VINCULACION column
 * @method ComRecibidaQuery orderByNumAnexos($order = Criteria::ASC) Order by the NUM_ANEXOS column
 * @method ComRecibidaQuery orderByNumeroFud($order = Criteria::ASC) Order by the NUMERO_FUD column
 * @method ComRecibidaQuery orderByNumeroProceso($order = Criteria::ASC) Order by the NUMERO_PROCESO column
 * @method ComRecibidaQuery orderByFechaDigit($order = Criteria::ASC) Order by the FECHA_DIGIT column
 * @method ComRecibidaQuery orderByIsLocked($order = Criteria::ASC) Order by the IS_LOCKED column
 * @method ComRecibidaQuery orderByFechaRecibido($order = Criteria::ASC) Order by the FECHA_RECIBIDO column
 * @method ComRecibidaQuery orderByContenidodocId($order = Criteria::ASC) Order by the CONTENIDODOC_ID column
 * @method ComRecibidaQuery orderByResptaIntegracion($order = Criteria::ASC) Order by the RESPTA_INTEGRACION column
 * @method ComRecibidaQuery orderByDirDigit($order = Criteria::ASC) Order by the DIR_DIGIT column
 * @method ComRecibidaQuery orderByTipoLanexo($order = Criteria::ASC) Order by the TIPO_LANEXO column
 * @method ComRecibidaQuery orderByRequiereRespuesta($order = Criteria::ASC) Order by the REQUIERE_RESPUESTA column
 * @method ComRecibidaQuery orderByObsNoRespuesta($order = Criteria::ASC) Order by the OBS_NO_RESPUESTA column
 *
 * @method ComRecibidaQuery groupByComrecibidaId() Group by the COMRECIBIDA_ID column
 * @method ComRecibidaQuery groupByRegionalId() Group by the REGIONAL_ID column
 * @method ComRecibidaQuery groupByComenviadaId() Group by the COMENVIADA_ID column
 * @method ComRecibidaQuery groupByAsuntorecibidaId() Group by the ASUNTORECIBIDA_ID column
 * @method ComRecibidaQuery groupByTipocomrecibidaId() Group by the TIPOCOMRECIBIDA_ID column
 * @method ComRecibidaQuery groupByPeriodoId() Group by the PERIODO_ID column
 * @method ComRecibidaQuery groupByDependenciaId() Group by the DEPENDENCIA_ID column
 * @method ComRecibidaQuery groupByEstadodigitalizacionId() Group by the ESTADODIGITALIZACION_ID column
 * @method ComRecibidaQuery groupByEstadocomrecibidaId() Group by the ESTADOCOMRECIBIDA_ID column
 * @method ComRecibidaQuery groupByFormarecepcionId() Group by the FORMARECEPCION_ID column
 * @method ComRecibidaQuery groupByCiudadId() Group by the CIUDAD_ID column
 * @method ComRecibidaQuery groupByDirectorioexternoId() Group by the DIRECTORIOEXTERNO_ID column
 * @method ComRecibidaQuery groupByEmpresaMensajeriaId() Group by the EMPRESA_MENSAJERIA_ID column
 * @method ComRecibidaQuery groupByTipofirmadigitalId() Group by the TIPOFIRMADIGITAL_ID column
 * @method ComRecibidaQuery groupByMediorespuestaId() Group by the MEDIORESPUESTA_ID column
 * @method ComRecibidaQuery groupByTiporespuestaId() Group by the TIPORESPUESTA_ID column
 * @method ComRecibidaQuery groupByPrioridadcomId() Group by the PRIORIDADCOM_ID column
 * @method ComRecibidaQuery groupByTipoexpedienteId() Group by the TIPOEXPEDIENTE_ID column
 * @method ComRecibidaQuery groupByTipoprocesocomId() Group by the TIPOPROCESOCOM_ID column
 * @method ComRecibidaQuery groupByRadicadoOrigen() Group by the RADICADO_ORIGEN column
 * @method ComRecibidaQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method ComRecibidaQuery groupByAsunto() Group by the ASUNTO column
 * @method ComRecibidaQuery groupByNumeroRadicacion() Group by the NUMERO_RADICACION column
 * @method ComRecibidaQuery groupByRadicado() Group by the RADICADO column
 * @method ComRecibidaQuery groupByEsCopia() Group by the ES_COPIA column
 * @method ComRecibidaQuery groupByGuia() Group by the GUIA column
 * @method ComRecibidaQuery groupByFechaEnvioGuia() Group by the FECHA_ENVIO_GUIA column
 * @method ComRecibidaQuery groupByValorGuia() Group by the VALOR_GUIA column
 * @method ComRecibidaQuery groupByFolios() Group by the FOLIOS column
 * @method ComRecibidaQuery groupByAnexos() Group by the ANEXOS column
 * @method ComRecibidaQuery groupByRuta() Group by the RUTA column
 * @method ComRecibidaQuery groupByCodigoReenResp() Group by the CODIGO_REEN_RESP column
 * @method ComRecibidaQuery groupByObsReenResp() Group by the OBS_REEN_RESP column
 * @method ComRecibidaQuery groupByEstaentregado() Group by the ESTAENTREGADO column
 * @method ComRecibidaQuery groupByFechaMaximaRespuesta() Group by the FECHA_MAXIMA_RESPUESTA column
 * @method ComRecibidaQuery groupByFechaDeAnulacion() Group by the FECHA_DE_ANULACION column
 * @method ComRecibidaQuery groupByObsAnulacion() Group by the OBS_ANULACION column
 * @method ComRecibidaQuery groupByMarca() Group by the MARCA column
 * @method ComRecibidaQuery groupByFechaRespuestaReen() Group by the FECHA_RESPUESTA_REEN column
 * @method ComRecibidaQuery groupByObservaciones() Group by the OBSERVACIONES column
 * @method ComRecibidaQuery groupByInstancia() Group by the INSTANCIA column
 * @method ComRecibidaQuery groupByBitacora() Group by the BITACORA column
 * @method ComRecibidaQuery groupByObsRemitir() Group by the OBS_REMITIR column
 * @method ComRecibidaQuery groupByDestinoComrecibida() Group by the DESTINO_COMRECIBIDA column
 * @method ComRecibidaQuery groupByFechaDocumento() Group by the FECHA_DOCUMENTO column
 * @method ComRecibidaQuery groupByMarcaVinculacion() Group by the MARCA_VINCULACION column
 * @method ComRecibidaQuery groupByNumAnexos() Group by the NUM_ANEXOS column
 * @method ComRecibidaQuery groupByNumeroFud() Group by the NUMERO_FUD column
 * @method ComRecibidaQuery groupByNumeroProceso() Group by the NUMERO_PROCESO column
 * @method ComRecibidaQuery groupByFechaDigit() Group by the FECHA_DIGIT column
 * @method ComRecibidaQuery groupByIsLocked() Group by the IS_LOCKED column
 * @method ComRecibidaQuery groupByFechaRecibido() Group by the FECHA_RECIBIDO column
 * @method ComRecibidaQuery groupByContenidodocId() Group by the CONTENIDODOC_ID column
 * @method ComRecibidaQuery groupByResptaIntegracion() Group by the RESPTA_INTEGRACION column
 * @method ComRecibidaQuery groupByDirDigit() Group by the DIR_DIGIT column
 * @method ComRecibidaQuery groupByTipoLanexo() Group by the TIPO_LANEXO column
 * @method ComRecibidaQuery groupByRequiereRespuesta() Group by the REQUIERE_RESPUESTA column
 * @method ComRecibidaQuery groupByObsNoRespuesta() Group by the OBS_NO_RESPUESTA column
 *
 * @method ComRecibidaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ComRecibidaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ComRecibidaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ComRecibidaQuery leftJoinRegional($relationAlias = null) Adds a LEFT JOIN clause to the query using the Regional relation
 * @method ComRecibidaQuery rightJoinRegional($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Regional relation
 * @method ComRecibidaQuery innerJoinRegional($relationAlias = null) Adds a INNER JOIN clause to the query using the Regional relation
 *
 * @method ComRecibidaQuery leftJoinComEnviada($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComEnviada relation
 * @method ComRecibidaQuery rightJoinComEnviada($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComEnviada relation
 * @method ComRecibidaQuery innerJoinComEnviada($relationAlias = null) Adds a INNER JOIN clause to the query using the ComEnviada relation
 *
 * @method ComRecibidaQuery leftJoinAsuntoRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the AsuntoRecibida relation
 * @method ComRecibidaQuery rightJoinAsuntoRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the AsuntoRecibida relation
 * @method ComRecibidaQuery innerJoinAsuntoRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the AsuntoRecibida relation
 *
 * @method ComRecibidaQuery leftJoinTipoComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoComRecibida relation
 * @method ComRecibidaQuery rightJoinTipoComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoComRecibida relation
 * @method ComRecibidaQuery innerJoinTipoComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoComRecibida relation
 *
 * @method ComRecibidaQuery leftJoinPeriodo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Periodo relation
 * @method ComRecibidaQuery rightJoinPeriodo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Periodo relation
 * @method ComRecibidaQuery innerJoinPeriodo($relationAlias = null) Adds a INNER JOIN clause to the query using the Periodo relation
 *
 * @method ComRecibidaQuery leftJoinDependencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Dependencia relation
 * @method ComRecibidaQuery rightJoinDependencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Dependencia relation
 * @method ComRecibidaQuery innerJoinDependencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Dependencia relation
 *
 * @method ComRecibidaQuery leftJoinEstadoDigitalizacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the EstadoDigitalizacion relation
 * @method ComRecibidaQuery rightJoinEstadoDigitalizacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EstadoDigitalizacion relation
 * @method ComRecibidaQuery innerJoinEstadoDigitalizacion($relationAlias = null) Adds a INNER JOIN clause to the query using the EstadoDigitalizacion relation
 *
 * @method ComRecibidaQuery leftJoinEstadoComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the EstadoComRecibida relation
 * @method ComRecibidaQuery rightJoinEstadoComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EstadoComRecibida relation
 * @method ComRecibidaQuery innerJoinEstadoComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the EstadoComRecibida relation
 *
 * @method ComRecibidaQuery leftJoinFormaRecepcion($relationAlias = null) Adds a LEFT JOIN clause to the query using the FormaRecepcion relation
 * @method ComRecibidaQuery rightJoinFormaRecepcion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the FormaRecepcion relation
 * @method ComRecibidaQuery innerJoinFormaRecepcion($relationAlias = null) Adds a INNER JOIN clause to the query using the FormaRecepcion relation
 *
 * @method ComRecibidaQuery leftJoinCiudad($relationAlias = null) Adds a LEFT JOIN clause to the query using the Ciudad relation
 * @method ComRecibidaQuery rightJoinCiudad($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Ciudad relation
 * @method ComRecibidaQuery innerJoinCiudad($relationAlias = null) Adds a INNER JOIN clause to the query using the Ciudad relation
 *
 * @method ComRecibidaQuery leftJoinDirectorioExterno($relationAlias = null) Adds a LEFT JOIN clause to the query using the DirectorioExterno relation
 * @method ComRecibidaQuery rightJoinDirectorioExterno($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DirectorioExterno relation
 * @method ComRecibidaQuery innerJoinDirectorioExterno($relationAlias = null) Adds a INNER JOIN clause to the query using the DirectorioExterno relation
 *
 * @method ComRecibidaQuery leftJoinEmpresaMensajeria($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmpresaMensajeria relation
 * @method ComRecibidaQuery rightJoinEmpresaMensajeria($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmpresaMensajeria relation
 * @method ComRecibidaQuery innerJoinEmpresaMensajeria($relationAlias = null) Adds a INNER JOIN clause to the query using the EmpresaMensajeria relation
 *
 * @method ComRecibidaQuery leftJoinTipoFirmaDigital($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoFirmaDigital relation
 * @method ComRecibidaQuery rightJoinTipoFirmaDigital($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoFirmaDigital relation
 * @method ComRecibidaQuery innerJoinTipoFirmaDigital($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoFirmaDigital relation
 *
 * @method ComRecibidaQuery leftJoinMedioRespuesta($relationAlias = null) Adds a LEFT JOIN clause to the query using the MedioRespuesta relation
 * @method ComRecibidaQuery rightJoinMedioRespuesta($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MedioRespuesta relation
 * @method ComRecibidaQuery innerJoinMedioRespuesta($relationAlias = null) Adds a INNER JOIN clause to the query using the MedioRespuesta relation
 *
 * @method ComRecibidaQuery leftJoinTipoRespuesta($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoRespuesta relation
 * @method ComRecibidaQuery rightJoinTipoRespuesta($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoRespuesta relation
 * @method ComRecibidaQuery innerJoinTipoRespuesta($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoRespuesta relation
 *
 * @method ComRecibidaQuery leftJoinPrioridadCom($relationAlias = null) Adds a LEFT JOIN clause to the query using the PrioridadCom relation
 * @method ComRecibidaQuery rightJoinPrioridadCom($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PrioridadCom relation
 * @method ComRecibidaQuery innerJoinPrioridadCom($relationAlias = null) Adds a INNER JOIN clause to the query using the PrioridadCom relation
 *
 * @method ComRecibidaQuery leftJoinTipoExpediente($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoExpediente relation
 * @method ComRecibidaQuery rightJoinTipoExpediente($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoExpediente relation
 * @method ComRecibidaQuery innerJoinTipoExpediente($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoExpediente relation
 *
 * @method ComRecibidaQuery leftJoinTipoProcesoCom($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoProcesoCom relation
 * @method ComRecibidaQuery rightJoinTipoProcesoCom($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoProcesoCom relation
 * @method ComRecibidaQuery innerJoinTipoProcesoCom($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoProcesoCom relation
 *
 * @method ComRecibidaQuery leftJoinWfInstancia($relationAlias = null) Adds a LEFT JOIN clause to the query using the WfInstancia relation
 * @method ComRecibidaQuery rightJoinWfInstancia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the WfInstancia relation
 * @method ComRecibidaQuery innerJoinWfInstancia($relationAlias = null) Adds a INNER JOIN clause to the query using the WfInstancia relation
 *
 * @method ComRecibidaQuery leftJoinWfInstanciaBitacora($relationAlias = null) Adds a LEFT JOIN clause to the query using the WfInstanciaBitacora relation
 * @method ComRecibidaQuery rightJoinWfInstanciaBitacora($relationAlias = null) Adds a RIGHT JOIN clause to the query using the WfInstanciaBitacora relation
 * @method ComRecibidaQuery innerJoinWfInstanciaBitacora($relationAlias = null) Adds a INNER JOIN clause to the query using the WfInstanciaBitacora relation
 *
 * @method ComRecibidaQuery leftJoinComrecibidaDuplicados($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComrecibidaDuplicados relation
 * @method ComRecibidaQuery rightJoinComrecibidaDuplicados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComrecibidaDuplicados relation
 * @method ComRecibidaQuery innerJoinComrecibidaDuplicados($relationAlias = null) Adds a INNER JOIN clause to the query using the ComrecibidaDuplicados relation
 *
 * @method ComRecibidaQuery leftJoinComrecibidaInteresados($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComrecibidaInteresados relation
 * @method ComRecibidaQuery rightJoinComrecibidaInteresados($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComrecibidaInteresados relation
 * @method ComRecibidaQuery innerJoinComrecibidaInteresados($relationAlias = null) Adds a INNER JOIN clause to the query using the ComrecibidaInteresados relation
 *
 * @method ComRecibidaQuery leftJoinComrecibidaUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComrecibidaUsuario relation
 * @method ComRecibidaQuery rightJoinComrecibidaUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComrecibidaUsuario relation
 * @method ComRecibidaQuery innerJoinComrecibidaUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the ComrecibidaUsuario relation
 *
 * @method ComRecibidaQuery leftJoinClTransferencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the ClTransferencia relation
 * @method ComRecibidaQuery rightJoinClTransferencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ClTransferencia relation
 * @method ComRecibidaQuery innerJoinClTransferencia($relationAlias = null) Adds a INNER JOIN clause to the query using the ClTransferencia relation
 *
 * @method ComRecibidaQuery leftJoinComenviadaMasivas($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComenviadaMasivas relation
 * @method ComRecibidaQuery rightJoinComenviadaMasivas($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComenviadaMasivas relation
 * @method ComRecibidaQuery innerJoinComenviadaMasivas($relationAlias = null) Adds a INNER JOIN clause to the query using the ComenviadaMasivas relation
 *
 * @method ComRecibidaQuery leftJoinComrecibidaRespuesta($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComrecibidaRespuesta relation
 * @method ComRecibidaQuery rightJoinComrecibidaRespuesta($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComrecibidaRespuesta relation
 * @method ComRecibidaQuery innerJoinComrecibidaRespuesta($relationAlias = null) Adds a INNER JOIN clause to the query using the ComrecibidaRespuesta relation
 *
 * @method ComRecibidaQuery leftJoinTransferencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Transferencia relation
 * @method ComRecibidaQuery rightJoinTransferencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Transferencia relation
 * @method ComRecibidaQuery innerJoinTransferencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Transferencia relation
 *
 * @method ComRecibida findOne(PropelPDO $con = null) Return the first ComRecibida matching the query
 * @method ComRecibida findOneOrCreate(PropelPDO $con = null) Return the first ComRecibida matching the query, or a new ComRecibida object populated from the query conditions when no match is found
 *
 * @method ComRecibida findOneByRegionalId(int $REGIONAL_ID) Return the first ComRecibida filtered by the REGIONAL_ID column
 * @method ComRecibida findOneByComenviadaId(int $COMENVIADA_ID) Return the first ComRecibida filtered by the COMENVIADA_ID column
 * @method ComRecibida findOneByAsuntorecibidaId(int $ASUNTORECIBIDA_ID) Return the first ComRecibida filtered by the ASUNTORECIBIDA_ID column
 * @method ComRecibida findOneByTipocomrecibidaId(int $TIPOCOMRECIBIDA_ID) Return the first ComRecibida filtered by the TIPOCOMRECIBIDA_ID column
 * @method ComRecibida findOneByPeriodoId(int $PERIODO_ID) Return the first ComRecibida filtered by the PERIODO_ID column
 * @method ComRecibida findOneByDependenciaId(int $DEPENDENCIA_ID) Return the first ComRecibida filtered by the DEPENDENCIA_ID column
 * @method ComRecibida findOneByEstadodigitalizacionId(int $ESTADODIGITALIZACION_ID) Return the first ComRecibida filtered by the ESTADODIGITALIZACION_ID column
 * @method ComRecibida findOneByEstadocomrecibidaId(int $ESTADOCOMRECIBIDA_ID) Return the first ComRecibida filtered by the ESTADOCOMRECIBIDA_ID column
 * @method ComRecibida findOneByFormarecepcionId(int $FORMARECEPCION_ID) Return the first ComRecibida filtered by the FORMARECEPCION_ID column
 * @method ComRecibida findOneByCiudadId(int $CIUDAD_ID) Return the first ComRecibida filtered by the CIUDAD_ID column
 * @method ComRecibida findOneByDirectorioexternoId(int $DIRECTORIOEXTERNO_ID) Return the first ComRecibida filtered by the DIRECTORIOEXTERNO_ID column
 * @method ComRecibida findOneByEmpresaMensajeriaId(int $EMPRESA_MENSAJERIA_ID) Return the first ComRecibida filtered by the EMPRESA_MENSAJERIA_ID column
 * @method ComRecibida findOneByTipofirmadigitalId(int $TIPOFIRMADIGITAL_ID) Return the first ComRecibida filtered by the TIPOFIRMADIGITAL_ID column
 * @method ComRecibida findOneByMediorespuestaId(int $MEDIORESPUESTA_ID) Return the first ComRecibida filtered by the MEDIORESPUESTA_ID column
 * @method ComRecibida findOneByTiporespuestaId(int $TIPORESPUESTA_ID) Return the first ComRecibida filtered by the TIPORESPUESTA_ID column
 * @method ComRecibida findOneByPrioridadcomId(int $PRIORIDADCOM_ID) Return the first ComRecibida filtered by the PRIORIDADCOM_ID column
 * @method ComRecibida findOneByTipoexpedienteId(int $TIPOEXPEDIENTE_ID) Return the first ComRecibida filtered by the TIPOEXPEDIENTE_ID column
 * @method ComRecibida findOneByTipoprocesocomId(int $TIPOPROCESOCOM_ID) Return the first ComRecibida filtered by the TIPOPROCESOCOM_ID column
 * @method ComRecibida findOneByRadicadoOrigen(string $RADICADO_ORIGEN) Return the first ComRecibida filtered by the RADICADO_ORIGEN column
 * @method ComRecibida findOneByFechaCreacion(string $FECHA_CREACION) Return the first ComRecibida filtered by the FECHA_CREACION column
 * @method ComRecibida findOneByAsunto(string $ASUNTO) Return the first ComRecibida filtered by the ASUNTO column
 * @method ComRecibida findOneByNumeroRadicacion(int $NUMERO_RADICACION) Return the first ComRecibida filtered by the NUMERO_RADICACION column
 * @method ComRecibida findOneByRadicado(string $RADICADO) Return the first ComRecibida filtered by the RADICADO column
 * @method ComRecibida findOneByEsCopia(boolean $ES_COPIA) Return the first ComRecibida filtered by the ES_COPIA column
 * @method ComRecibida findOneByGuia(string $GUIA) Return the first ComRecibida filtered by the GUIA column
 * @method ComRecibida findOneByFechaEnvioGuia(string $FECHA_ENVIO_GUIA) Return the first ComRecibida filtered by the FECHA_ENVIO_GUIA column
 * @method ComRecibida findOneByValorGuia(double $VALOR_GUIA) Return the first ComRecibida filtered by the VALOR_GUIA column
 * @method ComRecibida findOneByFolios(int $FOLIOS) Return the first ComRecibida filtered by the FOLIOS column
 * @method ComRecibida findOneByAnexos(string $ANEXOS) Return the first ComRecibida filtered by the ANEXOS column
 * @method ComRecibida findOneByRuta(string $RUTA) Return the first ComRecibida filtered by the RUTA column
 * @method ComRecibida findOneByCodigoReenResp(int $CODIGO_REEN_RESP) Return the first ComRecibida filtered by the CODIGO_REEN_RESP column
 * @method ComRecibida findOneByObsReenResp(string $OBS_REEN_RESP) Return the first ComRecibida filtered by the OBS_REEN_RESP column
 * @method ComRecibida findOneByEstaentregado(boolean $ESTAENTREGADO) Return the first ComRecibida filtered by the ESTAENTREGADO column
 * @method ComRecibida findOneByFechaMaximaRespuesta(string $FECHA_MAXIMA_RESPUESTA) Return the first ComRecibida filtered by the FECHA_MAXIMA_RESPUESTA column
 * @method ComRecibida findOneByFechaDeAnulacion(string $FECHA_DE_ANULACION) Return the first ComRecibida filtered by the FECHA_DE_ANULACION column
 * @method ComRecibida findOneByObsAnulacion(string $OBS_ANULACION) Return the first ComRecibida filtered by the OBS_ANULACION column
 * @method ComRecibida findOneByMarca(int $MARCA) Return the first ComRecibida filtered by the MARCA column
 * @method ComRecibida findOneByFechaRespuestaReen(string $FECHA_RESPUESTA_REEN) Return the first ComRecibida filtered by the FECHA_RESPUESTA_REEN column
 * @method ComRecibida findOneByObservaciones(string $OBSERVACIONES) Return the first ComRecibida filtered by the OBSERVACIONES column
 * @method ComRecibida findOneByInstancia(int $INSTANCIA) Return the first ComRecibida filtered by the INSTANCIA column
 * @method ComRecibida findOneByBitacora(int $BITACORA) Return the first ComRecibida filtered by the BITACORA column
 * @method ComRecibida findOneByObsRemitir(string $OBS_REMITIR) Return the first ComRecibida filtered by the OBS_REMITIR column
 * @method ComRecibida findOneByDestinoComrecibida(string $DESTINO_COMRECIBIDA) Return the first ComRecibida filtered by the DESTINO_COMRECIBIDA column
 * @method ComRecibida findOneByFechaDocumento(string $FECHA_DOCUMENTO) Return the first ComRecibida filtered by the FECHA_DOCUMENTO column
 * @method ComRecibida findOneByMarcaVinculacion(boolean $MARCA_VINCULACION) Return the first ComRecibida filtered by the MARCA_VINCULACION column
 * @method ComRecibida findOneByNumAnexos(int $NUM_ANEXOS) Return the first ComRecibida filtered by the NUM_ANEXOS column
 * @method ComRecibida findOneByNumeroFud(string $NUMERO_FUD) Return the first ComRecibida filtered by the NUMERO_FUD column
 * @method ComRecibida findOneByNumeroProceso(string $NUMERO_PROCESO) Return the first ComRecibida filtered by the NUMERO_PROCESO column
 * @method ComRecibida findOneByFechaDigit(string $FECHA_DIGIT) Return the first ComRecibida filtered by the FECHA_DIGIT column
 * @method ComRecibida findOneByIsLocked(int $IS_LOCKED) Return the first ComRecibida filtered by the IS_LOCKED column
 * @method ComRecibida findOneByFechaRecibido(string $FECHA_RECIBIDO) Return the first ComRecibida filtered by the FECHA_RECIBIDO column
 * @method ComRecibida findOneByContenidodocId(int $CONTENIDODOC_ID) Return the first ComRecibida filtered by the CONTENIDODOC_ID column
 * @method ComRecibida findOneByResptaIntegracion(string $RESPTA_INTEGRACION) Return the first ComRecibida filtered by the RESPTA_INTEGRACION column
 * @method ComRecibida findOneByDirDigit(string $DIR_DIGIT) Return the first ComRecibida filtered by the DIR_DIGIT column
 * @method ComRecibida findOneByTipoLanexo(string $TIPO_LANEXO) Return the first ComRecibida filtered by the TIPO_LANEXO column
 * @method ComRecibida findOneByRequiereRespuesta(boolean $REQUIERE_RESPUESTA) Return the first ComRecibida filtered by the REQUIERE_RESPUESTA column
 * @method ComRecibida findOneByObsNoRespuesta(string $OBS_NO_RESPUESTA) Return the first ComRecibida filtered by the OBS_NO_RESPUESTA column
 *
 * @method array findByComrecibidaId(int $COMRECIBIDA_ID) Return ComRecibida objects filtered by the COMRECIBIDA_ID column
 * @method array findByRegionalId(int $REGIONAL_ID) Return ComRecibida objects filtered by the REGIONAL_ID column
 * @method array findByComenviadaId(int $COMENVIADA_ID) Return ComRecibida objects filtered by the COMENVIADA_ID column
 * @method array findByAsuntorecibidaId(int $ASUNTORECIBIDA_ID) Return ComRecibida objects filtered by the ASUNTORECIBIDA_ID column
 * @method array findByTipocomrecibidaId(int $TIPOCOMRECIBIDA_ID) Return ComRecibida objects filtered by the TIPOCOMRECIBIDA_ID column
 * @method array findByPeriodoId(int $PERIODO_ID) Return ComRecibida objects filtered by the PERIODO_ID column
 * @method array findByDependenciaId(int $DEPENDENCIA_ID) Return ComRecibida objects filtered by the DEPENDENCIA_ID column
 * @method array findByEstadodigitalizacionId(int $ESTADODIGITALIZACION_ID) Return ComRecibida objects filtered by the ESTADODIGITALIZACION_ID column
 * @method array findByEstadocomrecibidaId(int $ESTADOCOMRECIBIDA_ID) Return ComRecibida objects filtered by the ESTADOCOMRECIBIDA_ID column
 * @method array findByFormarecepcionId(int $FORMARECEPCION_ID) Return ComRecibida objects filtered by the FORMARECEPCION_ID column
 * @method array findByCiudadId(int $CIUDAD_ID) Return ComRecibida objects filtered by the CIUDAD_ID column
 * @method array findByDirectorioexternoId(int $DIRECTORIOEXTERNO_ID) Return ComRecibida objects filtered by the DIRECTORIOEXTERNO_ID column
 * @method array findByEmpresaMensajeriaId(int $EMPRESA_MENSAJERIA_ID) Return ComRecibida objects filtered by the EMPRESA_MENSAJERIA_ID column
 * @method array findByTipofirmadigitalId(int $TIPOFIRMADIGITAL_ID) Return ComRecibida objects filtered by the TIPOFIRMADIGITAL_ID column
 * @method array findByMediorespuestaId(int $MEDIORESPUESTA_ID) Return ComRecibida objects filtered by the MEDIORESPUESTA_ID column
 * @method array findByTiporespuestaId(int $TIPORESPUESTA_ID) Return ComRecibida objects filtered by the TIPORESPUESTA_ID column
 * @method array findByPrioridadcomId(int $PRIORIDADCOM_ID) Return ComRecibida objects filtered by the PRIORIDADCOM_ID column
 * @method array findByTipoexpedienteId(int $TIPOEXPEDIENTE_ID) Return ComRecibida objects filtered by the TIPOEXPEDIENTE_ID column
 * @method array findByTipoprocesocomId(int $TIPOPROCESOCOM_ID) Return ComRecibida objects filtered by the TIPOPROCESOCOM_ID column
 * @method array findByRadicadoOrigen(string $RADICADO_ORIGEN) Return ComRecibida objects filtered by the RADICADO_ORIGEN column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return ComRecibida objects filtered by the FECHA_CREACION column
 * @method array findByAsunto(string $ASUNTO) Return ComRecibida objects filtered by the ASUNTO column
 * @method array findByNumeroRadicacion(int $NUMERO_RADICACION) Return ComRecibida objects filtered by the NUMERO_RADICACION column
 * @method array findByRadicado(string $RADICADO) Return ComRecibida objects filtered by the RADICADO column
 * @method array findByEsCopia(boolean $ES_COPIA) Return ComRecibida objects filtered by the ES_COPIA column
 * @method array findByGuia(string $GUIA) Return ComRecibida objects filtered by the GUIA column
 * @method array findByFechaEnvioGuia(string $FECHA_ENVIO_GUIA) Return ComRecibida objects filtered by the FECHA_ENVIO_GUIA column
 * @method array findByValorGuia(double $VALOR_GUIA) Return ComRecibida objects filtered by the VALOR_GUIA column
 * @method array findByFolios(int $FOLIOS) Return ComRecibida objects filtered by the FOLIOS column
 * @method array findByAnexos(string $ANEXOS) Return ComRecibida objects filtered by the ANEXOS column
 * @method array findByRuta(string $RUTA) Return ComRecibida objects filtered by the RUTA column
 * @method array findByCodigoReenResp(int $CODIGO_REEN_RESP) Return ComRecibida objects filtered by the CODIGO_REEN_RESP column
 * @method array findByObsReenResp(string $OBS_REEN_RESP) Return ComRecibida objects filtered by the OBS_REEN_RESP column
 * @method array findByEstaentregado(boolean $ESTAENTREGADO) Return ComRecibida objects filtered by the ESTAENTREGADO column
 * @method array findByFechaMaximaRespuesta(string $FECHA_MAXIMA_RESPUESTA) Return ComRecibida objects filtered by the FECHA_MAXIMA_RESPUESTA column
 * @method array findByFechaDeAnulacion(string $FECHA_DE_ANULACION) Return ComRecibida objects filtered by the FECHA_DE_ANULACION column
 * @method array findByObsAnulacion(string $OBS_ANULACION) Return ComRecibida objects filtered by the OBS_ANULACION column
 * @method array findByMarca(int $MARCA) Return ComRecibida objects filtered by the MARCA column
 * @method array findByFechaRespuestaReen(string $FECHA_RESPUESTA_REEN) Return ComRecibida objects filtered by the FECHA_RESPUESTA_REEN column
 * @method array findByObservaciones(string $OBSERVACIONES) Return ComRecibida objects filtered by the OBSERVACIONES column
 * @method array findByInstancia(int $INSTANCIA) Return ComRecibida objects filtered by the INSTANCIA column
 * @method array findByBitacora(int $BITACORA) Return ComRecibida objects filtered by the BITACORA column
 * @method array findByObsRemitir(string $OBS_REMITIR) Return ComRecibida objects filtered by the OBS_REMITIR column
 * @method array findByDestinoComrecibida(string $DESTINO_COMRECIBIDA) Return ComRecibida objects filtered by the DESTINO_COMRECIBIDA column
 * @method array findByFechaDocumento(string $FECHA_DOCUMENTO) Return ComRecibida objects filtered by the FECHA_DOCUMENTO column
 * @method array findByMarcaVinculacion(boolean $MARCA_VINCULACION) Return ComRecibida objects filtered by the MARCA_VINCULACION column
 * @method array findByNumAnexos(int $NUM_ANEXOS) Return ComRecibida objects filtered by the NUM_ANEXOS column
 * @method array findByNumeroFud(string $NUMERO_FUD) Return ComRecibida objects filtered by the NUMERO_FUD column
 * @method array findByNumeroProceso(string $NUMERO_PROCESO) Return ComRecibida objects filtered by the NUMERO_PROCESO column
 * @method array findByFechaDigit(string $FECHA_DIGIT) Return ComRecibida objects filtered by the FECHA_DIGIT column
 * @method array findByIsLocked(int $IS_LOCKED) Return ComRecibida objects filtered by the IS_LOCKED column
 * @method array findByFechaRecibido(string $FECHA_RECIBIDO) Return ComRecibida objects filtered by the FECHA_RECIBIDO column
 * @method array findByContenidodocId(int $CONTENIDODOC_ID) Return ComRecibida objects filtered by the CONTENIDODOC_ID column
 * @method array findByResptaIntegracion(string $RESPTA_INTEGRACION) Return ComRecibida objects filtered by the RESPTA_INTEGRACION column
 * @method array findByDirDigit(string $DIR_DIGIT) Return ComRecibida objects filtered by the DIR_DIGIT column
 * @method array findByTipoLanexo(string $TIPO_LANEXO) Return ComRecibida objects filtered by the TIPO_LANEXO column
 * @method array findByRequiereRespuesta(boolean $REQUIERE_RESPUESTA) Return ComRecibida objects filtered by the REQUIERE_RESPUESTA column
 * @method array findByObsNoRespuesta(string $OBS_NO_RESPUESTA) Return ComRecibida objects filtered by the OBS_NO_RESPUESTA column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseComRecibidaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseComRecibidaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'ComRecibida';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ComRecibidaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   ComRecibidaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ComRecibidaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ComRecibidaQuery) {
            return $criteria;
        }
        $query = new ComRecibidaQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   ComRecibida|ComRecibida[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ComRecibidaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ComRecibidaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ComRecibida A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByComrecibidaId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ComRecibida A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [COMRECIBIDA_ID], [REGIONAL_ID], [COMENVIADA_ID], [ASUNTORECIBIDA_ID], [TIPOCOMRECIBIDA_ID], [PERIODO_ID], [DEPENDENCIA_ID], [ESTADODIGITALIZACION_ID], [ESTADOCOMRECIBIDA_ID], [FORMARECEPCION_ID], [CIUDAD_ID], [DIRECTORIOEXTERNO_ID], [EMPRESA_MENSAJERIA_ID], [TIPOFIRMADIGITAL_ID], [MEDIORESPUESTA_ID], [TIPORESPUESTA_ID], [PRIORIDADCOM_ID], [TIPOEXPEDIENTE_ID], [TIPOPROCESOCOM_ID], [RADICADO_ORIGEN], [FECHA_CREACION], [ASUNTO], [NUMERO_RADICACION], [RADICADO], [ES_COPIA], [GUIA], [FECHA_ENVIO_GUIA], [VALOR_GUIA], [FOLIOS], [ANEXOS], [RUTA], [CODIGO_REEN_RESP], [OBS_REEN_RESP], [ESTAENTREGADO], [FECHA_MAXIMA_RESPUESTA], [FECHA_DE_ANULACION], [OBS_ANULACION], [MARCA], [FECHA_RESPUESTA_REEN], [OBSERVACIONES], [INSTANCIA], [BITACORA], [OBS_REMITIR], [DESTINO_COMRECIBIDA], [FECHA_DOCUMENTO], [MARCA_VINCULACION], [NUM_ANEXOS], [NUMERO_FUD], [NUMERO_PROCESO], [FECHA_DIGIT], [IS_LOCKED], [FECHA_RECIBIDO], [CONTENIDODOC_ID], [RESPTA_INTEGRACION], [DIR_DIGIT], [TIPO_LANEXO], [REQUIERE_RESPUESTA], [OBS_NO_RESPUESTA] FROM [COM_RECIBIDA] WHERE [COMRECIBIDA_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new ComRecibida();
            $obj->hydrate($row);
            ComRecibidaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return ComRecibida|ComRecibida[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|ComRecibida[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the COMRECIBIDA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByComrecibidaId(1234); // WHERE COMRECIBIDA_ID = 1234
     * $query->filterByComrecibidaId(array(12, 34)); // WHERE COMRECIBIDA_ID IN (12, 34)
     * $query->filterByComrecibidaId(array('min' => 12)); // WHERE COMRECIBIDA_ID >= 12
     * $query->filterByComrecibidaId(array('max' => 12)); // WHERE COMRECIBIDA_ID <= 12
     * </code>
     *
     * @param     mixed $comrecibidaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByComrecibidaId($comrecibidaId = null, $comparison = null)
    {
        if (is_array($comrecibidaId)) {
            $useMinMax = false;
            if (isset($comrecibidaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comrecibidaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaId, $comparison);
    }

    /**
     * Filter the query on the REGIONAL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRegionalId(1234); // WHERE REGIONAL_ID = 1234
     * $query->filterByRegionalId(array(12, 34)); // WHERE REGIONAL_ID IN (12, 34)
     * $query->filterByRegionalId(array('min' => 12)); // WHERE REGIONAL_ID >= 12
     * $query->filterByRegionalId(array('max' => 12)); // WHERE REGIONAL_ID <= 12
     * </code>
     *
     * @see       filterByRegional()
     *
     * @param     mixed $regionalId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByRegionalId($regionalId = null, $comparison = null)
    {
        if (is_array($regionalId)) {
            $useMinMax = false;
            if (isset($regionalId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::REGIONAL_ID, $regionalId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($regionalId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::REGIONAL_ID, $regionalId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::REGIONAL_ID, $regionalId, $comparison);
    }

    /**
     * Filter the query on the COMENVIADA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByComenviadaId(1234); // WHERE COMENVIADA_ID = 1234
     * $query->filterByComenviadaId(array(12, 34)); // WHERE COMENVIADA_ID IN (12, 34)
     * $query->filterByComenviadaId(array('min' => 12)); // WHERE COMENVIADA_ID >= 12
     * $query->filterByComenviadaId(array('max' => 12)); // WHERE COMENVIADA_ID <= 12
     * </code>
     *
     * @see       filterByComEnviada()
     *
     * @param     mixed $comenviadaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByComenviadaId($comenviadaId = null, $comparison = null)
    {
        if (is_array($comenviadaId)) {
            $useMinMax = false;
            if (isset($comenviadaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::COMENVIADA_ID, $comenviadaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comenviadaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::COMENVIADA_ID, $comenviadaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::COMENVIADA_ID, $comenviadaId, $comparison);
    }

    /**
     * Filter the query on the ASUNTORECIBIDA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByAsuntorecibidaId(1234); // WHERE ASUNTORECIBIDA_ID = 1234
     * $query->filterByAsuntorecibidaId(array(12, 34)); // WHERE ASUNTORECIBIDA_ID IN (12, 34)
     * $query->filterByAsuntorecibidaId(array('min' => 12)); // WHERE ASUNTORECIBIDA_ID >= 12
     * $query->filterByAsuntorecibidaId(array('max' => 12)); // WHERE ASUNTORECIBIDA_ID <= 12
     * </code>
     *
     * @see       filterByAsuntoRecibida()
     *
     * @param     mixed $asuntorecibidaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByAsuntorecibidaId($asuntorecibidaId = null, $comparison = null)
    {
        if (is_array($asuntorecibidaId)) {
            $useMinMax = false;
            if (isset($asuntorecibidaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::ASUNTORECIBIDA_ID, $asuntorecibidaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($asuntorecibidaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::ASUNTORECIBIDA_ID, $asuntorecibidaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::ASUNTORECIBIDA_ID, $asuntorecibidaId, $comparison);
    }

    /**
     * Filter the query on the TIPOCOMRECIBIDA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipocomrecibidaId(1234); // WHERE TIPOCOMRECIBIDA_ID = 1234
     * $query->filterByTipocomrecibidaId(array(12, 34)); // WHERE TIPOCOMRECIBIDA_ID IN (12, 34)
     * $query->filterByTipocomrecibidaId(array('min' => 12)); // WHERE TIPOCOMRECIBIDA_ID >= 12
     * $query->filterByTipocomrecibidaId(array('max' => 12)); // WHERE TIPOCOMRECIBIDA_ID <= 12
     * </code>
     *
     * @see       filterByTipoComRecibida()
     *
     * @param     mixed $tipocomrecibidaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByTipocomrecibidaId($tipocomrecibidaId = null, $comparison = null)
    {
        if (is_array($tipocomrecibidaId)) {
            $useMinMax = false;
            if (isset($tipocomrecibidaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOCOMRECIBIDA_ID, $tipocomrecibidaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipocomrecibidaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOCOMRECIBIDA_ID, $tipocomrecibidaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::TIPOCOMRECIBIDA_ID, $tipocomrecibidaId, $comparison);
    }

    /**
     * Filter the query on the PERIODO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByPeriodoId(1234); // WHERE PERIODO_ID = 1234
     * $query->filterByPeriodoId(array(12, 34)); // WHERE PERIODO_ID IN (12, 34)
     * $query->filterByPeriodoId(array('min' => 12)); // WHERE PERIODO_ID >= 12
     * $query->filterByPeriodoId(array('max' => 12)); // WHERE PERIODO_ID <= 12
     * </code>
     *
     * @see       filterByPeriodo()
     *
     * @param     mixed $periodoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByPeriodoId($periodoId = null, $comparison = null)
    {
        if (is_array($periodoId)) {
            $useMinMax = false;
            if (isset($periodoId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::PERIODO_ID, $periodoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($periodoId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::PERIODO_ID, $periodoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::PERIODO_ID, $periodoId, $comparison);
    }

    /**
     * Filter the query on the DEPENDENCIA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDependenciaId(1234); // WHERE DEPENDENCIA_ID = 1234
     * $query->filterByDependenciaId(array(12, 34)); // WHERE DEPENDENCIA_ID IN (12, 34)
     * $query->filterByDependenciaId(array('min' => 12)); // WHERE DEPENDENCIA_ID >= 12
     * $query->filterByDependenciaId(array('max' => 12)); // WHERE DEPENDENCIA_ID <= 12
     * </code>
     *
     * @see       filterByDependencia()
     *
     * @param     mixed $dependenciaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByDependenciaId($dependenciaId = null, $comparison = null)
    {
        if (is_array($dependenciaId)) {
            $useMinMax = false;
            if (isset($dependenciaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::DEPENDENCIA_ID, $dependenciaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($dependenciaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::DEPENDENCIA_ID, $dependenciaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::DEPENDENCIA_ID, $dependenciaId, $comparison);
    }

    /**
     * Filter the query on the ESTADODIGITALIZACION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadodigitalizacionId(1234); // WHERE ESTADODIGITALIZACION_ID = 1234
     * $query->filterByEstadodigitalizacionId(array(12, 34)); // WHERE ESTADODIGITALIZACION_ID IN (12, 34)
     * $query->filterByEstadodigitalizacionId(array('min' => 12)); // WHERE ESTADODIGITALIZACION_ID >= 12
     * $query->filterByEstadodigitalizacionId(array('max' => 12)); // WHERE ESTADODIGITALIZACION_ID <= 12
     * </code>
     *
     * @see       filterByEstadoDigitalizacion()
     *
     * @param     mixed $estadodigitalizacionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByEstadodigitalizacionId($estadodigitalizacionId = null, $comparison = null)
    {
        if (is_array($estadodigitalizacionId)) {
            $useMinMax = false;
            if (isset($estadodigitalizacionId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::ESTADODIGITALIZACION_ID, $estadodigitalizacionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadodigitalizacionId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::ESTADODIGITALIZACION_ID, $estadodigitalizacionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::ESTADODIGITALIZACION_ID, $estadodigitalizacionId, $comparison);
    }

    /**
     * Filter the query on the ESTADOCOMRECIBIDA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadocomrecibidaId(1234); // WHERE ESTADOCOMRECIBIDA_ID = 1234
     * $query->filterByEstadocomrecibidaId(array(12, 34)); // WHERE ESTADOCOMRECIBIDA_ID IN (12, 34)
     * $query->filterByEstadocomrecibidaId(array('min' => 12)); // WHERE ESTADOCOMRECIBIDA_ID >= 12
     * $query->filterByEstadocomrecibidaId(array('max' => 12)); // WHERE ESTADOCOMRECIBIDA_ID <= 12
     * </code>
     *
     * @see       filterByEstadoComRecibida()
     *
     * @param     mixed $estadocomrecibidaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByEstadocomrecibidaId($estadocomrecibidaId = null, $comparison = null)
    {
        if (is_array($estadocomrecibidaId)) {
            $useMinMax = false;
            if (isset($estadocomrecibidaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::ESTADOCOMRECIBIDA_ID, $estadocomrecibidaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadocomrecibidaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::ESTADOCOMRECIBIDA_ID, $estadocomrecibidaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::ESTADOCOMRECIBIDA_ID, $estadocomrecibidaId, $comparison);
    }

    /**
     * Filter the query on the FORMARECEPCION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByFormarecepcionId(1234); // WHERE FORMARECEPCION_ID = 1234
     * $query->filterByFormarecepcionId(array(12, 34)); // WHERE FORMARECEPCION_ID IN (12, 34)
     * $query->filterByFormarecepcionId(array('min' => 12)); // WHERE FORMARECEPCION_ID >= 12
     * $query->filterByFormarecepcionId(array('max' => 12)); // WHERE FORMARECEPCION_ID <= 12
     * </code>
     *
     * @see       filterByFormaRecepcion()
     *
     * @param     mixed $formarecepcionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFormarecepcionId($formarecepcionId = null, $comparison = null)
    {
        if (is_array($formarecepcionId)) {
            $useMinMax = false;
            if (isset($formarecepcionId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FORMARECEPCION_ID, $formarecepcionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($formarecepcionId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FORMARECEPCION_ID, $formarecepcionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FORMARECEPCION_ID, $formarecepcionId, $comparison);
    }

    /**
     * Filter the query on the CIUDAD_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByCiudadId(1234); // WHERE CIUDAD_ID = 1234
     * $query->filterByCiudadId(array(12, 34)); // WHERE CIUDAD_ID IN (12, 34)
     * $query->filterByCiudadId(array('min' => 12)); // WHERE CIUDAD_ID >= 12
     * $query->filterByCiudadId(array('max' => 12)); // WHERE CIUDAD_ID <= 12
     * </code>
     *
     * @see       filterByCiudad()
     *
     * @param     mixed $ciudadId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByCiudadId($ciudadId = null, $comparison = null)
    {
        if (is_array($ciudadId)) {
            $useMinMax = false;
            if (isset($ciudadId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::CIUDAD_ID, $ciudadId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ciudadId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::CIUDAD_ID, $ciudadId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::CIUDAD_ID, $ciudadId, $comparison);
    }

    /**
     * Filter the query on the DIRECTORIOEXTERNO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDirectorioexternoId(1234); // WHERE DIRECTORIOEXTERNO_ID = 1234
     * $query->filterByDirectorioexternoId(array(12, 34)); // WHERE DIRECTORIOEXTERNO_ID IN (12, 34)
     * $query->filterByDirectorioexternoId(array('min' => 12)); // WHERE DIRECTORIOEXTERNO_ID >= 12
     * $query->filterByDirectorioexternoId(array('max' => 12)); // WHERE DIRECTORIOEXTERNO_ID <= 12
     * </code>
     *
     * @see       filterByDirectorioExterno()
     *
     * @param     mixed $directorioexternoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByDirectorioexternoId($directorioexternoId = null, $comparison = null)
    {
        if (is_array($directorioexternoId)) {
            $useMinMax = false;
            if (isset($directorioexternoId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::DIRECTORIOEXTERNO_ID, $directorioexternoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($directorioexternoId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::DIRECTORIOEXTERNO_ID, $directorioexternoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::DIRECTORIOEXTERNO_ID, $directorioexternoId, $comparison);
    }

    /**
     * Filter the query on the EMPRESA_MENSAJERIA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaMensajeriaId(1234); // WHERE EMPRESA_MENSAJERIA_ID = 1234
     * $query->filterByEmpresaMensajeriaId(array(12, 34)); // WHERE EMPRESA_MENSAJERIA_ID IN (12, 34)
     * $query->filterByEmpresaMensajeriaId(array('min' => 12)); // WHERE EMPRESA_MENSAJERIA_ID >= 12
     * $query->filterByEmpresaMensajeriaId(array('max' => 12)); // WHERE EMPRESA_MENSAJERIA_ID <= 12
     * </code>
     *
     * @see       filterByEmpresaMensajeria()
     *
     * @param     mixed $empresaMensajeriaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByEmpresaMensajeriaId($empresaMensajeriaId = null, $comparison = null)
    {
        if (is_array($empresaMensajeriaId)) {
            $useMinMax = false;
            if (isset($empresaMensajeriaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::EMPRESA_MENSAJERIA_ID, $empresaMensajeriaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaMensajeriaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::EMPRESA_MENSAJERIA_ID, $empresaMensajeriaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::EMPRESA_MENSAJERIA_ID, $empresaMensajeriaId, $comparison);
    }

    /**
     * Filter the query on the TIPOFIRMADIGITAL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipofirmadigitalId(1234); // WHERE TIPOFIRMADIGITAL_ID = 1234
     * $query->filterByTipofirmadigitalId(array(12, 34)); // WHERE TIPOFIRMADIGITAL_ID IN (12, 34)
     * $query->filterByTipofirmadigitalId(array('min' => 12)); // WHERE TIPOFIRMADIGITAL_ID >= 12
     * $query->filterByTipofirmadigitalId(array('max' => 12)); // WHERE TIPOFIRMADIGITAL_ID <= 12
     * </code>
     *
     * @see       filterByTipoFirmaDigital()
     *
     * @param     mixed $tipofirmadigitalId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByTipofirmadigitalId($tipofirmadigitalId = null, $comparison = null)
    {
        if (is_array($tipofirmadigitalId)) {
            $useMinMax = false;
            if (isset($tipofirmadigitalId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOFIRMADIGITAL_ID, $tipofirmadigitalId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipofirmadigitalId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOFIRMADIGITAL_ID, $tipofirmadigitalId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::TIPOFIRMADIGITAL_ID, $tipofirmadigitalId, $comparison);
    }

    /**
     * Filter the query on the MEDIORESPUESTA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByMediorespuestaId(1234); // WHERE MEDIORESPUESTA_ID = 1234
     * $query->filterByMediorespuestaId(array(12, 34)); // WHERE MEDIORESPUESTA_ID IN (12, 34)
     * $query->filterByMediorespuestaId(array('min' => 12)); // WHERE MEDIORESPUESTA_ID >= 12
     * $query->filterByMediorespuestaId(array('max' => 12)); // WHERE MEDIORESPUESTA_ID <= 12
     * </code>
     *
     * @see       filterByMedioRespuesta()
     *
     * @param     mixed $mediorespuestaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByMediorespuestaId($mediorespuestaId = null, $comparison = null)
    {
        if (is_array($mediorespuestaId)) {
            $useMinMax = false;
            if (isset($mediorespuestaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::MEDIORESPUESTA_ID, $mediorespuestaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($mediorespuestaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::MEDIORESPUESTA_ID, $mediorespuestaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::MEDIORESPUESTA_ID, $mediorespuestaId, $comparison);
    }

    /**
     * Filter the query on the TIPORESPUESTA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTiporespuestaId(1234); // WHERE TIPORESPUESTA_ID = 1234
     * $query->filterByTiporespuestaId(array(12, 34)); // WHERE TIPORESPUESTA_ID IN (12, 34)
     * $query->filterByTiporespuestaId(array('min' => 12)); // WHERE TIPORESPUESTA_ID >= 12
     * $query->filterByTiporespuestaId(array('max' => 12)); // WHERE TIPORESPUESTA_ID <= 12
     * </code>
     *
     * @see       filterByTipoRespuesta()
     *
     * @param     mixed $tiporespuestaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByTiporespuestaId($tiporespuestaId = null, $comparison = null)
    {
        if (is_array($tiporespuestaId)) {
            $useMinMax = false;
            if (isset($tiporespuestaId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPORESPUESTA_ID, $tiporespuestaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiporespuestaId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPORESPUESTA_ID, $tiporespuestaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::TIPORESPUESTA_ID, $tiporespuestaId, $comparison);
    }

    /**
     * Filter the query on the PRIORIDADCOM_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByPrioridadcomId(1234); // WHERE PRIORIDADCOM_ID = 1234
     * $query->filterByPrioridadcomId(array(12, 34)); // WHERE PRIORIDADCOM_ID IN (12, 34)
     * $query->filterByPrioridadcomId(array('min' => 12)); // WHERE PRIORIDADCOM_ID >= 12
     * $query->filterByPrioridadcomId(array('max' => 12)); // WHERE PRIORIDADCOM_ID <= 12
     * </code>
     *
     * @see       filterByPrioridadCom()
     *
     * @param     mixed $prioridadcomId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByPrioridadcomId($prioridadcomId = null, $comparison = null)
    {
        if (is_array($prioridadcomId)) {
            $useMinMax = false;
            if (isset($prioridadcomId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::PRIORIDADCOM_ID, $prioridadcomId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($prioridadcomId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::PRIORIDADCOM_ID, $prioridadcomId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::PRIORIDADCOM_ID, $prioridadcomId, $comparison);
    }

    /**
     * Filter the query on the TIPOEXPEDIENTE_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoexpedienteId(1234); // WHERE TIPOEXPEDIENTE_ID = 1234
     * $query->filterByTipoexpedienteId(array(12, 34)); // WHERE TIPOEXPEDIENTE_ID IN (12, 34)
     * $query->filterByTipoexpedienteId(array('min' => 12)); // WHERE TIPOEXPEDIENTE_ID >= 12
     * $query->filterByTipoexpedienteId(array('max' => 12)); // WHERE TIPOEXPEDIENTE_ID <= 12
     * </code>
     *
     * @see       filterByTipoExpediente()
     *
     * @param     mixed $tipoexpedienteId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByTipoexpedienteId($tipoexpedienteId = null, $comparison = null)
    {
        if (is_array($tipoexpedienteId)) {
            $useMinMax = false;
            if (isset($tipoexpedienteId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOEXPEDIENTE_ID, $tipoexpedienteId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipoexpedienteId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOEXPEDIENTE_ID, $tipoexpedienteId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::TIPOEXPEDIENTE_ID, $tipoexpedienteId, $comparison);
    }

    /**
     * Filter the query on the TIPOPROCESOCOM_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoprocesocomId(1234); // WHERE TIPOPROCESOCOM_ID = 1234
     * $query->filterByTipoprocesocomId(array(12, 34)); // WHERE TIPOPROCESOCOM_ID IN (12, 34)
     * $query->filterByTipoprocesocomId(array('min' => 12)); // WHERE TIPOPROCESOCOM_ID >= 12
     * $query->filterByTipoprocesocomId(array('max' => 12)); // WHERE TIPOPROCESOCOM_ID <= 12
     * </code>
     *
     * @see       filterByTipoProcesoCom()
     *
     * @param     mixed $tipoprocesocomId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByTipoprocesocomId($tipoprocesocomId = null, $comparison = null)
    {
        if (is_array($tipoprocesocomId)) {
            $useMinMax = false;
            if (isset($tipoprocesocomId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOPROCESOCOM_ID, $tipoprocesocomId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipoprocesocomId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::TIPOPROCESOCOM_ID, $tipoprocesocomId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::TIPOPROCESOCOM_ID, $tipoprocesocomId, $comparison);
    }

    /**
     * Filter the query on the RADICADO_ORIGEN column
     *
     * Example usage:
     * <code>
     * $query->filterByRadicadoOrigen('fooValue');   // WHERE RADICADO_ORIGEN = 'fooValue'
     * $query->filterByRadicadoOrigen('%fooValue%'); // WHERE RADICADO_ORIGEN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $radicadoOrigen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByRadicadoOrigen($radicadoOrigen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($radicadoOrigen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $radicadoOrigen)) {
                $radicadoOrigen = str_replace('*', '%', $radicadoOrigen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::RADICADO_ORIGEN, $radicadoOrigen, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the ASUNTO column
     *
     * Example usage:
     * <code>
     * $query->filterByAsunto('fooValue');   // WHERE ASUNTO = 'fooValue'
     * $query->filterByAsunto('%fooValue%'); // WHERE ASUNTO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $asunto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByAsunto($asunto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($asunto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $asunto)) {
                $asunto = str_replace('*', '%', $asunto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::ASUNTO, $asunto, $comparison);
    }

    /**
     * Filter the query on the NUMERO_RADICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroRadicacion(1234); // WHERE NUMERO_RADICACION = 1234
     * $query->filterByNumeroRadicacion(array(12, 34)); // WHERE NUMERO_RADICACION IN (12, 34)
     * $query->filterByNumeroRadicacion(array('min' => 12)); // WHERE NUMERO_RADICACION >= 12
     * $query->filterByNumeroRadicacion(array('max' => 12)); // WHERE NUMERO_RADICACION <= 12
     * </code>
     *
     * @param     mixed $numeroRadicacion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByNumeroRadicacion($numeroRadicacion = null, $comparison = null)
    {
        if (is_array($numeroRadicacion)) {
            $useMinMax = false;
            if (isset($numeroRadicacion['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::NUMERO_RADICACION, $numeroRadicacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($numeroRadicacion['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::NUMERO_RADICACION, $numeroRadicacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::NUMERO_RADICACION, $numeroRadicacion, $comparison);
    }

    /**
     * Filter the query on the RADICADO column
     *
     * Example usage:
     * <code>
     * $query->filterByRadicado('fooValue');   // WHERE RADICADO = 'fooValue'
     * $query->filterByRadicado('%fooValue%'); // WHERE RADICADO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $radicado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByRadicado($radicado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($radicado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $radicado)) {
                $radicado = str_replace('*', '%', $radicado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::RADICADO, $radicado, $comparison);
    }

    /**
     * Filter the query on the ES_COPIA column
     *
     * Example usage:
     * <code>
     * $query->filterByEsCopia(true); // WHERE ES_COPIA = true
     * $query->filterByEsCopia('yes'); // WHERE ES_COPIA = true
     * </code>
     *
     * @param     boolean|string $esCopia The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByEsCopia($esCopia = null, $comparison = null)
    {
        if (is_string($esCopia)) {
            $esCopia = in_array(strtolower($esCopia), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ComRecibidaPeer::ES_COPIA, $esCopia, $comparison);
    }

    /**
     * Filter the query on the GUIA column
     *
     * Example usage:
     * <code>
     * $query->filterByGuia('fooValue');   // WHERE GUIA = 'fooValue'
     * $query->filterByGuia('%fooValue%'); // WHERE GUIA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $guia The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByGuia($guia = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($guia)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $guia)) {
                $guia = str_replace('*', '%', $guia);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::GUIA, $guia, $comparison);
    }

    /**
     * Filter the query on the FECHA_ENVIO_GUIA column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaEnvioGuia('2011-03-14'); // WHERE FECHA_ENVIO_GUIA = '2011-03-14'
     * $query->filterByFechaEnvioGuia('now'); // WHERE FECHA_ENVIO_GUIA = '2011-03-14'
     * $query->filterByFechaEnvioGuia(array('max' => 'yesterday')); // WHERE FECHA_ENVIO_GUIA < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaEnvioGuia The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaEnvioGuia($fechaEnvioGuia = null, $comparison = null)
    {
        if (is_array($fechaEnvioGuia)) {
            $useMinMax = false;
            if (isset($fechaEnvioGuia['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_ENVIO_GUIA, $fechaEnvioGuia['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaEnvioGuia['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_ENVIO_GUIA, $fechaEnvioGuia['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_ENVIO_GUIA, $fechaEnvioGuia, $comparison);
    }

    /**
     * Filter the query on the VALOR_GUIA column
     *
     * Example usage:
     * <code>
     * $query->filterByValorGuia(1234); // WHERE VALOR_GUIA = 1234
     * $query->filterByValorGuia(array(12, 34)); // WHERE VALOR_GUIA IN (12, 34)
     * $query->filterByValorGuia(array('min' => 12)); // WHERE VALOR_GUIA >= 12
     * $query->filterByValorGuia(array('max' => 12)); // WHERE VALOR_GUIA <= 12
     * </code>
     *
     * @param     mixed $valorGuia The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByValorGuia($valorGuia = null, $comparison = null)
    {
        if (is_array($valorGuia)) {
            $useMinMax = false;
            if (isset($valorGuia['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::VALOR_GUIA, $valorGuia['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorGuia['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::VALOR_GUIA, $valorGuia['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::VALOR_GUIA, $valorGuia, $comparison);
    }

    /**
     * Filter the query on the FOLIOS column
     *
     * Example usage:
     * <code>
     * $query->filterByFolios(1234); // WHERE FOLIOS = 1234
     * $query->filterByFolios(array(12, 34)); // WHERE FOLIOS IN (12, 34)
     * $query->filterByFolios(array('min' => 12)); // WHERE FOLIOS >= 12
     * $query->filterByFolios(array('max' => 12)); // WHERE FOLIOS <= 12
     * </code>
     *
     * @param     mixed $folios The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFolios($folios = null, $comparison = null)
    {
        if (is_array($folios)) {
            $useMinMax = false;
            if (isset($folios['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FOLIOS, $folios['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($folios['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FOLIOS, $folios['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FOLIOS, $folios, $comparison);
    }

    /**
     * Filter the query on the ANEXOS column
     *
     * Example usage:
     * <code>
     * $query->filterByAnexos('fooValue');   // WHERE ANEXOS = 'fooValue'
     * $query->filterByAnexos('%fooValue%'); // WHERE ANEXOS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $anexos The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByAnexos($anexos = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($anexos)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $anexos)) {
                $anexos = str_replace('*', '%', $anexos);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::ANEXOS, $anexos, $comparison);
    }

    /**
     * Filter the query on the RUTA column
     *
     * Example usage:
     * <code>
     * $query->filterByRuta('fooValue');   // WHERE RUTA = 'fooValue'
     * $query->filterByRuta('%fooValue%'); // WHERE RUTA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $ruta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByRuta($ruta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($ruta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $ruta)) {
                $ruta = str_replace('*', '%', $ruta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::RUTA, $ruta, $comparison);
    }

    /**
     * Filter the query on the CODIGO_REEN_RESP column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoReenResp(1234); // WHERE CODIGO_REEN_RESP = 1234
     * $query->filterByCodigoReenResp(array(12, 34)); // WHERE CODIGO_REEN_RESP IN (12, 34)
     * $query->filterByCodigoReenResp(array('min' => 12)); // WHERE CODIGO_REEN_RESP >= 12
     * $query->filterByCodigoReenResp(array('max' => 12)); // WHERE CODIGO_REEN_RESP <= 12
     * </code>
     *
     * @param     mixed $codigoReenResp The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByCodigoReenResp($codigoReenResp = null, $comparison = null)
    {
        if (is_array($codigoReenResp)) {
            $useMinMax = false;
            if (isset($codigoReenResp['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::CODIGO_REEN_RESP, $codigoReenResp['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($codigoReenResp['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::CODIGO_REEN_RESP, $codigoReenResp['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::CODIGO_REEN_RESP, $codigoReenResp, $comparison);
    }

    /**
     * Filter the query on the OBS_REEN_RESP column
     *
     * Example usage:
     * <code>
     * $query->filterByObsReenResp('fooValue');   // WHERE OBS_REEN_RESP = 'fooValue'
     * $query->filterByObsReenResp('%fooValue%'); // WHERE OBS_REEN_RESP LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obsReenResp The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByObsReenResp($obsReenResp = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obsReenResp)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obsReenResp)) {
                $obsReenResp = str_replace('*', '%', $obsReenResp);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::OBS_REEN_RESP, $obsReenResp, $comparison);
    }

    /**
     * Filter the query on the ESTAENTREGADO column
     *
     * Example usage:
     * <code>
     * $query->filterByEstaentregado(true); // WHERE ESTAENTREGADO = true
     * $query->filterByEstaentregado('yes'); // WHERE ESTAENTREGADO = true
     * </code>
     *
     * @param     boolean|string $estaentregado The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByEstaentregado($estaentregado = null, $comparison = null)
    {
        if (is_string($estaentregado)) {
            $estaentregado = in_array(strtolower($estaentregado), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ComRecibidaPeer::ESTAENTREGADO, $estaentregado, $comparison);
    }

    /**
     * Filter the query on the FECHA_MAXIMA_RESPUESTA column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaMaximaRespuesta('2011-03-14'); // WHERE FECHA_MAXIMA_RESPUESTA = '2011-03-14'
     * $query->filterByFechaMaximaRespuesta('now'); // WHERE FECHA_MAXIMA_RESPUESTA = '2011-03-14'
     * $query->filterByFechaMaximaRespuesta(array('max' => 'yesterday')); // WHERE FECHA_MAXIMA_RESPUESTA < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaMaximaRespuesta The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaMaximaRespuesta($fechaMaximaRespuesta = null, $comparison = null)
    {
        if (is_array($fechaMaximaRespuesta)) {
            $useMinMax = false;
            if (isset($fechaMaximaRespuesta['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_MAXIMA_RESPUESTA, $fechaMaximaRespuesta['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaMaximaRespuesta['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_MAXIMA_RESPUESTA, $fechaMaximaRespuesta['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_MAXIMA_RESPUESTA, $fechaMaximaRespuesta, $comparison);
    }

    /**
     * Filter the query on the FECHA_DE_ANULACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaDeAnulacion('2011-03-14'); // WHERE FECHA_DE_ANULACION = '2011-03-14'
     * $query->filterByFechaDeAnulacion('now'); // WHERE FECHA_DE_ANULACION = '2011-03-14'
     * $query->filterByFechaDeAnulacion(array('max' => 'yesterday')); // WHERE FECHA_DE_ANULACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaDeAnulacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaDeAnulacion($fechaDeAnulacion = null, $comparison = null)
    {
        if (is_array($fechaDeAnulacion)) {
            $useMinMax = false;
            if (isset($fechaDeAnulacion['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_DE_ANULACION, $fechaDeAnulacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaDeAnulacion['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_DE_ANULACION, $fechaDeAnulacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_DE_ANULACION, $fechaDeAnulacion, $comparison);
    }

    /**
     * Filter the query on the OBS_ANULACION column
     *
     * Example usage:
     * <code>
     * $query->filterByObsAnulacion('fooValue');   // WHERE OBS_ANULACION = 'fooValue'
     * $query->filterByObsAnulacion('%fooValue%'); // WHERE OBS_ANULACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obsAnulacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByObsAnulacion($obsAnulacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obsAnulacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obsAnulacion)) {
                $obsAnulacion = str_replace('*', '%', $obsAnulacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::OBS_ANULACION, $obsAnulacion, $comparison);
    }

    /**
     * Filter the query on the MARCA column
     *
     * Example usage:
     * <code>
     * $query->filterByMarca(1234); // WHERE MARCA = 1234
     * $query->filterByMarca(array(12, 34)); // WHERE MARCA IN (12, 34)
     * $query->filterByMarca(array('min' => 12)); // WHERE MARCA >= 12
     * $query->filterByMarca(array('max' => 12)); // WHERE MARCA <= 12
     * </code>
     *
     * @param     mixed $marca The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByMarca($marca = null, $comparison = null)
    {
        if (is_array($marca)) {
            $useMinMax = false;
            if (isset($marca['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::MARCA, $marca['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($marca['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::MARCA, $marca['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::MARCA, $marca, $comparison);
    }

    /**
     * Filter the query on the FECHA_RESPUESTA_REEN column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaRespuestaReen('2011-03-14'); // WHERE FECHA_RESPUESTA_REEN = '2011-03-14'
     * $query->filterByFechaRespuestaReen('now'); // WHERE FECHA_RESPUESTA_REEN = '2011-03-14'
     * $query->filterByFechaRespuestaReen(array('max' => 'yesterday')); // WHERE FECHA_RESPUESTA_REEN < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaRespuestaReen The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaRespuestaReen($fechaRespuestaReen = null, $comparison = null)
    {
        if (is_array($fechaRespuestaReen)) {
            $useMinMax = false;
            if (isset($fechaRespuestaReen['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_RESPUESTA_REEN, $fechaRespuestaReen['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaRespuestaReen['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_RESPUESTA_REEN, $fechaRespuestaReen['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_RESPUESTA_REEN, $fechaRespuestaReen, $comparison);
    }

    /**
     * Filter the query on the OBSERVACIONES column
     *
     * Example usage:
     * <code>
     * $query->filterByObservaciones('fooValue');   // WHERE OBSERVACIONES = 'fooValue'
     * $query->filterByObservaciones('%fooValue%'); // WHERE OBSERVACIONES LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observaciones The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByObservaciones($observaciones = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observaciones)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observaciones)) {
                $observaciones = str_replace('*', '%', $observaciones);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::OBSERVACIONES, $observaciones, $comparison);
    }

    /**
     * Filter the query on the INSTANCIA column
     *
     * Example usage:
     * <code>
     * $query->filterByInstancia(1234); // WHERE INSTANCIA = 1234
     * $query->filterByInstancia(array(12, 34)); // WHERE INSTANCIA IN (12, 34)
     * $query->filterByInstancia(array('min' => 12)); // WHERE INSTANCIA >= 12
     * $query->filterByInstancia(array('max' => 12)); // WHERE INSTANCIA <= 12
     * </code>
     *
     * @param     mixed $instancia The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByInstancia($instancia = null, $comparison = null)
    {
        if (is_array($instancia)) {
            $useMinMax = false;
            if (isset($instancia['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::INSTANCIA, $instancia['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($instancia['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::INSTANCIA, $instancia['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::INSTANCIA, $instancia, $comparison);
    }

    /**
     * Filter the query on the BITACORA column
     *
     * Example usage:
     * <code>
     * $query->filterByBitacora(1234); // WHERE BITACORA = 1234
     * $query->filterByBitacora(array(12, 34)); // WHERE BITACORA IN (12, 34)
     * $query->filterByBitacora(array('min' => 12)); // WHERE BITACORA >= 12
     * $query->filterByBitacora(array('max' => 12)); // WHERE BITACORA <= 12
     * </code>
     *
     * @param     mixed $bitacora The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByBitacora($bitacora = null, $comparison = null)
    {
        if (is_array($bitacora)) {
            $useMinMax = false;
            if (isset($bitacora['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::BITACORA, $bitacora['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bitacora['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::BITACORA, $bitacora['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::BITACORA, $bitacora, $comparison);
    }

    /**
     * Filter the query on the OBS_REMITIR column
     *
     * Example usage:
     * <code>
     * $query->filterByObsRemitir('fooValue');   // WHERE OBS_REMITIR = 'fooValue'
     * $query->filterByObsRemitir('%fooValue%'); // WHERE OBS_REMITIR LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obsRemitir The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByObsRemitir($obsRemitir = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obsRemitir)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obsRemitir)) {
                $obsRemitir = str_replace('*', '%', $obsRemitir);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::OBS_REMITIR, $obsRemitir, $comparison);
    }

    /**
     * Filter the query on the DESTINO_COMRECIBIDA column
     *
     * Example usage:
     * <code>
     * $query->filterByDestinoComrecibida('fooValue');   // WHERE DESTINO_COMRECIBIDA = 'fooValue'
     * $query->filterByDestinoComrecibida('%fooValue%'); // WHERE DESTINO_COMRECIBIDA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $destinoComrecibida The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByDestinoComrecibida($destinoComrecibida = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($destinoComrecibida)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $destinoComrecibida)) {
                $destinoComrecibida = str_replace('*', '%', $destinoComrecibida);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::DESTINO_COMRECIBIDA, $destinoComrecibida, $comparison);
    }

    /**
     * Filter the query on the FECHA_DOCUMENTO column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaDocumento('2011-03-14'); // WHERE FECHA_DOCUMENTO = '2011-03-14'
     * $query->filterByFechaDocumento('now'); // WHERE FECHA_DOCUMENTO = '2011-03-14'
     * $query->filterByFechaDocumento(array('max' => 'yesterday')); // WHERE FECHA_DOCUMENTO < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaDocumento The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaDocumento($fechaDocumento = null, $comparison = null)
    {
        if (is_array($fechaDocumento)) {
            $useMinMax = false;
            if (isset($fechaDocumento['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_DOCUMENTO, $fechaDocumento['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaDocumento['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_DOCUMENTO, $fechaDocumento['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_DOCUMENTO, $fechaDocumento, $comparison);
    }

    /**
     * Filter the query on the MARCA_VINCULACION column
     *
     * Example usage:
     * <code>
     * $query->filterByMarcaVinculacion(true); // WHERE MARCA_VINCULACION = true
     * $query->filterByMarcaVinculacion('yes'); // WHERE MARCA_VINCULACION = true
     * </code>
     *
     * @param     boolean|string $marcaVinculacion The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByMarcaVinculacion($marcaVinculacion = null, $comparison = null)
    {
        if (is_string($marcaVinculacion)) {
            $marcaVinculacion = in_array(strtolower($marcaVinculacion), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ComRecibidaPeer::MARCA_VINCULACION, $marcaVinculacion, $comparison);
    }

    /**
     * Filter the query on the NUM_ANEXOS column
     *
     * Example usage:
     * <code>
     * $query->filterByNumAnexos(1234); // WHERE NUM_ANEXOS = 1234
     * $query->filterByNumAnexos(array(12, 34)); // WHERE NUM_ANEXOS IN (12, 34)
     * $query->filterByNumAnexos(array('min' => 12)); // WHERE NUM_ANEXOS >= 12
     * $query->filterByNumAnexos(array('max' => 12)); // WHERE NUM_ANEXOS <= 12
     * </code>
     *
     * @param     mixed $numAnexos The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByNumAnexos($numAnexos = null, $comparison = null)
    {
        if (is_array($numAnexos)) {
            $useMinMax = false;
            if (isset($numAnexos['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::NUM_ANEXOS, $numAnexos['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($numAnexos['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::NUM_ANEXOS, $numAnexos['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::NUM_ANEXOS, $numAnexos, $comparison);
    }

    /**
     * Filter the query on the NUMERO_FUD column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroFud('fooValue');   // WHERE NUMERO_FUD = 'fooValue'
     * $query->filterByNumeroFud('%fooValue%'); // WHERE NUMERO_FUD LIKE '%fooValue%'
     * </code>
     *
     * @param     string $numeroFud The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByNumeroFud($numeroFud = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($numeroFud)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $numeroFud)) {
                $numeroFud = str_replace('*', '%', $numeroFud);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::NUMERO_FUD, $numeroFud, $comparison);
    }

    /**
     * Filter the query on the NUMERO_PROCESO column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroProceso('fooValue');   // WHERE NUMERO_PROCESO = 'fooValue'
     * $query->filterByNumeroProceso('%fooValue%'); // WHERE NUMERO_PROCESO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $numeroProceso The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByNumeroProceso($numeroProceso = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($numeroProceso)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $numeroProceso)) {
                $numeroProceso = str_replace('*', '%', $numeroProceso);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::NUMERO_PROCESO, $numeroProceso, $comparison);
    }

    /**
     * Filter the query on the FECHA_DIGIT column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaDigit('2011-03-14'); // WHERE FECHA_DIGIT = '2011-03-14'
     * $query->filterByFechaDigit('now'); // WHERE FECHA_DIGIT = '2011-03-14'
     * $query->filterByFechaDigit(array('max' => 'yesterday')); // WHERE FECHA_DIGIT < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaDigit The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaDigit($fechaDigit = null, $comparison = null)
    {
        if (is_array($fechaDigit)) {
            $useMinMax = false;
            if (isset($fechaDigit['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_DIGIT, $fechaDigit['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaDigit['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::FECHA_DIGIT, $fechaDigit['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_DIGIT, $fechaDigit, $comparison);
    }

    /**
     * Filter the query on the IS_LOCKED column
     *
     * Example usage:
     * <code>
     * $query->filterByIsLocked(1234); // WHERE IS_LOCKED = 1234
     * $query->filterByIsLocked(array(12, 34)); // WHERE IS_LOCKED IN (12, 34)
     * $query->filterByIsLocked(array('min' => 12)); // WHERE IS_LOCKED >= 12
     * $query->filterByIsLocked(array('max' => 12)); // WHERE IS_LOCKED <= 12
     * </code>
     *
     * @param     mixed $isLocked The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByIsLocked($isLocked = null, $comparison = null)
    {
        if (is_array($isLocked)) {
            $useMinMax = false;
            if (isset($isLocked['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::IS_LOCKED, $isLocked['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($isLocked['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::IS_LOCKED, $isLocked['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::IS_LOCKED, $isLocked, $comparison);
    }

    /**
     * Filter the query on the FECHA_RECIBIDO column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaRecibido('fooValue');   // WHERE FECHA_RECIBIDO = 'fooValue'
     * $query->filterByFechaRecibido('%fooValue%'); // WHERE FECHA_RECIBIDO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $fechaRecibido The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByFechaRecibido($fechaRecibido = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($fechaRecibido)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $fechaRecibido)) {
                $fechaRecibido = str_replace('*', '%', $fechaRecibido);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::FECHA_RECIBIDO, $fechaRecibido, $comparison);
    }

    /**
     * Filter the query on the CONTENIDODOC_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByContenidodocId(1234); // WHERE CONTENIDODOC_ID = 1234
     * $query->filterByContenidodocId(array(12, 34)); // WHERE CONTENIDODOC_ID IN (12, 34)
     * $query->filterByContenidodocId(array('min' => 12)); // WHERE CONTENIDODOC_ID >= 12
     * $query->filterByContenidodocId(array('max' => 12)); // WHERE CONTENIDODOC_ID <= 12
     * </code>
     *
     * @param     mixed $contenidodocId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByContenidodocId($contenidodocId = null, $comparison = null)
    {
        if (is_array($contenidodocId)) {
            $useMinMax = false;
            if (isset($contenidodocId['min'])) {
                $this->addUsingAlias(ComRecibidaPeer::CONTENIDODOC_ID, $contenidodocId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($contenidodocId['max'])) {
                $this->addUsingAlias(ComRecibidaPeer::CONTENIDODOC_ID, $contenidodocId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::CONTENIDODOC_ID, $contenidodocId, $comparison);
    }

    /**
     * Filter the query on the RESPTA_INTEGRACION column
     *
     * Example usage:
     * <code>
     * $query->filterByResptaIntegracion('fooValue');   // WHERE RESPTA_INTEGRACION = 'fooValue'
     * $query->filterByResptaIntegracion('%fooValue%'); // WHERE RESPTA_INTEGRACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $resptaIntegracion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByResptaIntegracion($resptaIntegracion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($resptaIntegracion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $resptaIntegracion)) {
                $resptaIntegracion = str_replace('*', '%', $resptaIntegracion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::RESPTA_INTEGRACION, $resptaIntegracion, $comparison);
    }

    /**
     * Filter the query on the DIR_DIGIT column
     *
     * Example usage:
     * <code>
     * $query->filterByDirDigit('fooValue');   // WHERE DIR_DIGIT = 'fooValue'
     * $query->filterByDirDigit('%fooValue%'); // WHERE DIR_DIGIT LIKE '%fooValue%'
     * </code>
     *
     * @param     string $dirDigit The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByDirDigit($dirDigit = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($dirDigit)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $dirDigit)) {
                $dirDigit = str_replace('*', '%', $dirDigit);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::DIR_DIGIT, $dirDigit, $comparison);
    }

    /**
     * Filter the query on the TIPO_LANEXO column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoLanexo('fooValue');   // WHERE TIPO_LANEXO = 'fooValue'
     * $query->filterByTipoLanexo('%fooValue%'); // WHERE TIPO_LANEXO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoLanexo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByTipoLanexo($tipoLanexo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoLanexo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoLanexo)) {
                $tipoLanexo = str_replace('*', '%', $tipoLanexo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::TIPO_LANEXO, $tipoLanexo, $comparison);
    }

    /**
     * Filter the query on the REQUIERE_RESPUESTA column
     *
     * Example usage:
     * <code>
     * $query->filterByRequiereRespuesta(true); // WHERE REQUIERE_RESPUESTA = true
     * $query->filterByRequiereRespuesta('yes'); // WHERE REQUIERE_RESPUESTA = true
     * </code>
     *
     * @param     boolean|string $requiereRespuesta The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByRequiereRespuesta($requiereRespuesta = null, $comparison = null)
    {
        if (is_string($requiereRespuesta)) {
            $requiereRespuesta = in_array(strtolower($requiereRespuesta), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ComRecibidaPeer::REQUIERE_RESPUESTA, $requiereRespuesta, $comparison);
    }

    /**
     * Filter the query on the OBS_NO_RESPUESTA column
     *
     * Example usage:
     * <code>
     * $query->filterByObsNoRespuesta('fooValue');   // WHERE OBS_NO_RESPUESTA = 'fooValue'
     * $query->filterByObsNoRespuesta('%fooValue%'); // WHERE OBS_NO_RESPUESTA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obsNoRespuesta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function filterByObsNoRespuesta($obsNoRespuesta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obsNoRespuesta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obsNoRespuesta)) {
                $obsNoRespuesta = str_replace('*', '%', $obsNoRespuesta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComRecibidaPeer::OBS_NO_RESPUESTA, $obsNoRespuesta, $comparison);
    }

    /**
     * Filter the query by a related Regional object
     *
     * @param   Regional|PropelObjectCollection $regional The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRegional($regional, $comparison = null)
    {
        if ($regional instanceof Regional) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::REGIONAL_ID, $regional->getRegionalId(), $comparison);
        } elseif ($regional instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::REGIONAL_ID, $regional->toKeyValue('PrimaryKey', 'RegionalId'), $comparison);
        } else {
            throw new PropelException('filterByRegional() only accepts arguments of type Regional or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Regional relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinRegional($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Regional');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Regional');
        }

        return $this;
    }

    /**
     * Use the Regional relation Regional object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RegionalQuery A secondary query class using the current class as primary query
     */
    public function useRegionalQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinRegional($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Regional', 'RegionalQuery');
    }

    /**
     * Filter the query by a related ComEnviada object
     *
     * @param   ComEnviada|PropelObjectCollection $comEnviada The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComEnviada($comEnviada, $comparison = null)
    {
        if ($comEnviada instanceof ComEnviada) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMENVIADA_ID, $comEnviada->getComenviadaId(), $comparison);
        } elseif ($comEnviada instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::COMENVIADA_ID, $comEnviada->toKeyValue('PrimaryKey', 'ComenviadaId'), $comparison);
        } else {
            throw new PropelException('filterByComEnviada() only accepts arguments of type ComEnviada or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComEnviada relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinComEnviada($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComEnviada');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComEnviada');
        }

        return $this;
    }

    /**
     * Use the ComEnviada relation ComEnviada object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComEnviadaQuery A secondary query class using the current class as primary query
     */
    public function useComEnviadaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComEnviada($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComEnviada', 'ComEnviadaQuery');
    }

    /**
     * Filter the query by a related AsuntoRecibida object
     *
     * @param   AsuntoRecibida|PropelObjectCollection $asuntoRecibida The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByAsuntoRecibida($asuntoRecibida, $comparison = null)
    {
        if ($asuntoRecibida instanceof AsuntoRecibida) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::ASUNTORECIBIDA_ID, $asuntoRecibida->getAsuntorecibidaId(), $comparison);
        } elseif ($asuntoRecibida instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::ASUNTORECIBIDA_ID, $asuntoRecibida->toKeyValue('PrimaryKey', 'AsuntorecibidaId'), $comparison);
        } else {
            throw new PropelException('filterByAsuntoRecibida() only accepts arguments of type AsuntoRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the AsuntoRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinAsuntoRecibida($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('AsuntoRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'AsuntoRecibida');
        }

        return $this;
    }

    /**
     * Use the AsuntoRecibida relation AsuntoRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   AsuntoRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useAsuntoRecibidaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinAsuntoRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'AsuntoRecibida', 'AsuntoRecibidaQuery');
    }

    /**
     * Filter the query by a related TipoComRecibida object
     *
     * @param   TipoComRecibida|PropelObjectCollection $tipoComRecibida The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoComRecibida($tipoComRecibida, $comparison = null)
    {
        if ($tipoComRecibida instanceof TipoComRecibida) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOCOMRECIBIDA_ID, $tipoComRecibida->getTipocomrecibidaId(), $comparison);
        } elseif ($tipoComRecibida instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOCOMRECIBIDA_ID, $tipoComRecibida->toKeyValue('PrimaryKey', 'TipocomrecibidaId'), $comparison);
        } else {
            throw new PropelException('filterByTipoComRecibida() only accepts arguments of type TipoComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinTipoComRecibida($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoComRecibida');
        }

        return $this;
    }

    /**
     * Use the TipoComRecibida relation TipoComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useTipoComRecibidaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTipoComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoComRecibida', 'TipoComRecibidaQuery');
    }

    /**
     * Filter the query by a related Periodo object
     *
     * @param   Periodo|PropelObjectCollection $periodo The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByPeriodo($periodo, $comparison = null)
    {
        if ($periodo instanceof Periodo) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::PERIODO_ID, $periodo->getPeriodoId(), $comparison);
        } elseif ($periodo instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::PERIODO_ID, $periodo->toKeyValue('PrimaryKey', 'PeriodoId'), $comparison);
        } else {
            throw new PropelException('filterByPeriodo() only accepts arguments of type Periodo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Periodo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinPeriodo($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Periodo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Periodo');
        }

        return $this;
    }

    /**
     * Use the Periodo relation Periodo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PeriodoQuery A secondary query class using the current class as primary query
     */
    public function usePeriodoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinPeriodo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Periodo', 'PeriodoQuery');
    }

    /**
     * Filter the query by a related Dependencia object
     *
     * @param   Dependencia|PropelObjectCollection $dependencia The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDependencia($dependencia, $comparison = null)
    {
        if ($dependencia instanceof Dependencia) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::DEPENDENCIA_ID, $dependencia->getDependenciaId(), $comparison);
        } elseif ($dependencia instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::DEPENDENCIA_ID, $dependencia->toKeyValue('PrimaryKey', 'DependenciaId'), $comparison);
        } else {
            throw new PropelException('filterByDependencia() only accepts arguments of type Dependencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Dependencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinDependencia($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Dependencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Dependencia');
        }

        return $this;
    }

    /**
     * Use the Dependencia relation Dependencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DependenciaQuery A secondary query class using the current class as primary query
     */
    public function useDependenciaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinDependencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Dependencia', 'DependenciaQuery');
    }

    /**
     * Filter the query by a related EstadoDigitalizacion object
     *
     * @param   EstadoDigitalizacion|PropelObjectCollection $estadoDigitalizacion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEstadoDigitalizacion($estadoDigitalizacion, $comparison = null)
    {
        if ($estadoDigitalizacion instanceof EstadoDigitalizacion) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::ESTADODIGITALIZACION_ID, $estadoDigitalizacion->getEstadodigitalizacionId(), $comparison);
        } elseif ($estadoDigitalizacion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::ESTADODIGITALIZACION_ID, $estadoDigitalizacion->toKeyValue('PrimaryKey', 'EstadodigitalizacionId'), $comparison);
        } else {
            throw new PropelException('filterByEstadoDigitalizacion() only accepts arguments of type EstadoDigitalizacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EstadoDigitalizacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinEstadoDigitalizacion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EstadoDigitalizacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EstadoDigitalizacion');
        }

        return $this;
    }

    /**
     * Use the EstadoDigitalizacion relation EstadoDigitalizacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EstadoDigitalizacionQuery A secondary query class using the current class as primary query
     */
    public function useEstadoDigitalizacionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEstadoDigitalizacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EstadoDigitalizacion', 'EstadoDigitalizacionQuery');
    }

    /**
     * Filter the query by a related EstadoComRecibida object
     *
     * @param   EstadoComRecibida|PropelObjectCollection $estadoComRecibida The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEstadoComRecibida($estadoComRecibida, $comparison = null)
    {
        if ($estadoComRecibida instanceof EstadoComRecibida) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::ESTADOCOMRECIBIDA_ID, $estadoComRecibida->getEstadocomrecibidaId(), $comparison);
        } elseif ($estadoComRecibida instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::ESTADOCOMRECIBIDA_ID, $estadoComRecibida->toKeyValue('PrimaryKey', 'EstadocomrecibidaId'), $comparison);
        } else {
            throw new PropelException('filterByEstadoComRecibida() only accepts arguments of type EstadoComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EstadoComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinEstadoComRecibida($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EstadoComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EstadoComRecibida');
        }

        return $this;
    }

    /**
     * Use the EstadoComRecibida relation EstadoComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EstadoComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useEstadoComRecibidaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEstadoComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EstadoComRecibida', 'EstadoComRecibidaQuery');
    }

    /**
     * Filter the query by a related FormaRecepcion object
     *
     * @param   FormaRecepcion|PropelObjectCollection $formaRecepcion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByFormaRecepcion($formaRecepcion, $comparison = null)
    {
        if ($formaRecepcion instanceof FormaRecepcion) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::FORMARECEPCION_ID, $formaRecepcion->getFormarecepcionId(), $comparison);
        } elseif ($formaRecepcion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::FORMARECEPCION_ID, $formaRecepcion->toKeyValue('PrimaryKey', 'FormarecepcionId'), $comparison);
        } else {
            throw new PropelException('filterByFormaRecepcion() only accepts arguments of type FormaRecepcion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the FormaRecepcion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinFormaRecepcion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('FormaRecepcion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'FormaRecepcion');
        }

        return $this;
    }

    /**
     * Use the FormaRecepcion relation FormaRecepcion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   FormaRecepcionQuery A secondary query class using the current class as primary query
     */
    public function useFormaRecepcionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinFormaRecepcion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'FormaRecepcion', 'FormaRecepcionQuery');
    }

    /**
     * Filter the query by a related Ciudad object
     *
     * @param   Ciudad|PropelObjectCollection $ciudad The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByCiudad($ciudad, $comparison = null)
    {
        if ($ciudad instanceof Ciudad) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::CIUDAD_ID, $ciudad->getCiudadId(), $comparison);
        } elseif ($ciudad instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::CIUDAD_ID, $ciudad->toKeyValue('PrimaryKey', 'CiudadId'), $comparison);
        } else {
            throw new PropelException('filterByCiudad() only accepts arguments of type Ciudad or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Ciudad relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinCiudad($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Ciudad');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Ciudad');
        }

        return $this;
    }

    /**
     * Use the Ciudad relation Ciudad object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CiudadQuery A secondary query class using the current class as primary query
     */
    public function useCiudadQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinCiudad($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Ciudad', 'CiudadQuery');
    }

    /**
     * Filter the query by a related DirectorioExterno object
     *
     * @param   DirectorioExterno|PropelObjectCollection $directorioExterno The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDirectorioExterno($directorioExterno, $comparison = null)
    {
        if ($directorioExterno instanceof DirectorioExterno) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::DIRECTORIOEXTERNO_ID, $directorioExterno->getDirectorioexternoId(), $comparison);
        } elseif ($directorioExterno instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::DIRECTORIOEXTERNO_ID, $directorioExterno->toKeyValue('PrimaryKey', 'DirectorioexternoId'), $comparison);
        } else {
            throw new PropelException('filterByDirectorioExterno() only accepts arguments of type DirectorioExterno or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DirectorioExterno relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinDirectorioExterno($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DirectorioExterno');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DirectorioExterno');
        }

        return $this;
    }

    /**
     * Use the DirectorioExterno relation DirectorioExterno object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DirectorioExternoQuery A secondary query class using the current class as primary query
     */
    public function useDirectorioExternoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDirectorioExterno($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DirectorioExterno', 'DirectorioExternoQuery');
    }

    /**
     * Filter the query by a related EmpresaMensajeria object
     *
     * @param   EmpresaMensajeria|PropelObjectCollection $empresaMensajeria The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresaMensajeria($empresaMensajeria, $comparison = null)
    {
        if ($empresaMensajeria instanceof EmpresaMensajeria) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::EMPRESA_MENSAJERIA_ID, $empresaMensajeria->getEmpresaMensajeriaId(), $comparison);
        } elseif ($empresaMensajeria instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::EMPRESA_MENSAJERIA_ID, $empresaMensajeria->toKeyValue('PrimaryKey', 'EmpresaMensajeriaId'), $comparison);
        } else {
            throw new PropelException('filterByEmpresaMensajeria() only accepts arguments of type EmpresaMensajeria or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmpresaMensajeria relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinEmpresaMensajeria($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmpresaMensajeria');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmpresaMensajeria');
        }

        return $this;
    }

    /**
     * Use the EmpresaMensajeria relation EmpresaMensajeria object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaMensajeriaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaMensajeriaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresaMensajeria($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmpresaMensajeria', 'EmpresaMensajeriaQuery');
    }

    /**
     * Filter the query by a related TipoFirmaDigital object
     *
     * @param   TipoFirmaDigital|PropelObjectCollection $tipoFirmaDigital The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoFirmaDigital($tipoFirmaDigital, $comparison = null)
    {
        if ($tipoFirmaDigital instanceof TipoFirmaDigital) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOFIRMADIGITAL_ID, $tipoFirmaDigital->getTipofirmadigitalId(), $comparison);
        } elseif ($tipoFirmaDigital instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOFIRMADIGITAL_ID, $tipoFirmaDigital->toKeyValue('PrimaryKey', 'TipofirmadigitalId'), $comparison);
        } else {
            throw new PropelException('filterByTipoFirmaDigital() only accepts arguments of type TipoFirmaDigital or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoFirmaDigital relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinTipoFirmaDigital($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoFirmaDigital');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoFirmaDigital');
        }

        return $this;
    }

    /**
     * Use the TipoFirmaDigital relation TipoFirmaDigital object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoFirmaDigitalQuery A secondary query class using the current class as primary query
     */
    public function useTipoFirmaDigitalQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoFirmaDigital($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoFirmaDigital', 'TipoFirmaDigitalQuery');
    }

    /**
     * Filter the query by a related MedioRespuesta object
     *
     * @param   MedioRespuesta|PropelObjectCollection $medioRespuesta The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByMedioRespuesta($medioRespuesta, $comparison = null)
    {
        if ($medioRespuesta instanceof MedioRespuesta) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::MEDIORESPUESTA_ID, $medioRespuesta->getMediorespuestaId(), $comparison);
        } elseif ($medioRespuesta instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::MEDIORESPUESTA_ID, $medioRespuesta->toKeyValue('PrimaryKey', 'MediorespuestaId'), $comparison);
        } else {
            throw new PropelException('filterByMedioRespuesta() only accepts arguments of type MedioRespuesta or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MedioRespuesta relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinMedioRespuesta($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MedioRespuesta');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MedioRespuesta');
        }

        return $this;
    }

    /**
     * Use the MedioRespuesta relation MedioRespuesta object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MedioRespuestaQuery A secondary query class using the current class as primary query
     */
    public function useMedioRespuestaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMedioRespuesta($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MedioRespuesta', 'MedioRespuestaQuery');
    }

    /**
     * Filter the query by a related TipoRespuesta object
     *
     * @param   TipoRespuesta|PropelObjectCollection $tipoRespuesta The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoRespuesta($tipoRespuesta, $comparison = null)
    {
        if ($tipoRespuesta instanceof TipoRespuesta) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPORESPUESTA_ID, $tipoRespuesta->getTiporespuestaId(), $comparison);
        } elseif ($tipoRespuesta instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPORESPUESTA_ID, $tipoRespuesta->toKeyValue('PrimaryKey', 'TiporespuestaId'), $comparison);
        } else {
            throw new PropelException('filterByTipoRespuesta() only accepts arguments of type TipoRespuesta or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoRespuesta relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinTipoRespuesta($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoRespuesta');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoRespuesta');
        }

        return $this;
    }

    /**
     * Use the TipoRespuesta relation TipoRespuesta object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoRespuestaQuery A secondary query class using the current class as primary query
     */
    public function useTipoRespuestaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoRespuesta($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoRespuesta', 'TipoRespuestaQuery');
    }

    /**
     * Filter the query by a related PrioridadCom object
     *
     * @param   PrioridadCom|PropelObjectCollection $prioridadCom The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByPrioridadCom($prioridadCom, $comparison = null)
    {
        if ($prioridadCom instanceof PrioridadCom) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::PRIORIDADCOM_ID, $prioridadCom->getPrioridadcomId(), $comparison);
        } elseif ($prioridadCom instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::PRIORIDADCOM_ID, $prioridadCom->toKeyValue('PrimaryKey', 'PrioridadcomId'), $comparison);
        } else {
            throw new PropelException('filterByPrioridadCom() only accepts arguments of type PrioridadCom or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PrioridadCom relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinPrioridadCom($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PrioridadCom');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PrioridadCom');
        }

        return $this;
    }

    /**
     * Use the PrioridadCom relation PrioridadCom object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrioridadComQuery A secondary query class using the current class as primary query
     */
    public function usePrioridadComQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrioridadCom($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PrioridadCom', 'PrioridadComQuery');
    }

    /**
     * Filter the query by a related TipoExpediente object
     *
     * @param   TipoExpediente|PropelObjectCollection $tipoExpediente The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoExpediente($tipoExpediente, $comparison = null)
    {
        if ($tipoExpediente instanceof TipoExpediente) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOEXPEDIENTE_ID, $tipoExpediente->getTipoexpedienteId(), $comparison);
        } elseif ($tipoExpediente instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOEXPEDIENTE_ID, $tipoExpediente->toKeyValue('PrimaryKey', 'TipoexpedienteId'), $comparison);
        } else {
            throw new PropelException('filterByTipoExpediente() only accepts arguments of type TipoExpediente or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoExpediente relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinTipoExpediente($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoExpediente');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoExpediente');
        }

        return $this;
    }

    /**
     * Use the TipoExpediente relation TipoExpediente object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoExpedienteQuery A secondary query class using the current class as primary query
     */
    public function useTipoExpedienteQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoExpediente($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoExpediente', 'TipoExpedienteQuery');
    }

    /**
     * Filter the query by a related TipoProcesoCom object
     *
     * @param   TipoProcesoCom|PropelObjectCollection $tipoProcesoCom The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoProcesoCom($tipoProcesoCom, $comparison = null)
    {
        if ($tipoProcesoCom instanceof TipoProcesoCom) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOPROCESOCOM_ID, $tipoProcesoCom->getTipoprocesocomId(), $comparison);
        } elseif ($tipoProcesoCom instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComRecibidaPeer::TIPOPROCESOCOM_ID, $tipoProcesoCom->toKeyValue('PrimaryKey', 'TipoprocesocomId'), $comparison);
        } else {
            throw new PropelException('filterByTipoProcesoCom() only accepts arguments of type TipoProcesoCom or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoProcesoCom relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinTipoProcesoCom($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoProcesoCom');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoProcesoCom');
        }

        return $this;
    }

    /**
     * Use the TipoProcesoCom relation TipoProcesoCom object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoProcesoComQuery A secondary query class using the current class as primary query
     */
    public function useTipoProcesoComQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTipoProcesoCom($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoProcesoCom', 'TipoProcesoComQuery');
    }

    /**
     * Filter the query by a related WfInstancia object
     *
     * @param   WfInstancia|PropelObjectCollection $wfInstancia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByWfInstancia($wfInstancia, $comparison = null)
    {
        if ($wfInstancia instanceof WfInstancia) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $wfInstancia->getComrecibidaId(), $comparison);
        } elseif ($wfInstancia instanceof PropelObjectCollection) {
            return $this
                ->useWfInstanciaQuery()
                ->filterByPrimaryKeys($wfInstancia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByWfInstancia() only accepts arguments of type WfInstancia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the WfInstancia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinWfInstancia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('WfInstancia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'WfInstancia');
        }

        return $this;
    }

    /**
     * Use the WfInstancia relation WfInstancia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   WfInstanciaQuery A secondary query class using the current class as primary query
     */
    public function useWfInstanciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinWfInstancia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'WfInstancia', 'WfInstanciaQuery');
    }

    /**
     * Filter the query by a related WfInstanciaBitacora object
     *
     * @param   WfInstanciaBitacora|PropelObjectCollection $wfInstanciaBitacora  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByWfInstanciaBitacora($wfInstanciaBitacora, $comparison = null)
    {
        if ($wfInstanciaBitacora instanceof WfInstanciaBitacora) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $wfInstanciaBitacora->getComrecibidaId(), $comparison);
        } elseif ($wfInstanciaBitacora instanceof PropelObjectCollection) {
            return $this
                ->useWfInstanciaBitacoraQuery()
                ->filterByPrimaryKeys($wfInstanciaBitacora->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByWfInstanciaBitacora() only accepts arguments of type WfInstanciaBitacora or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the WfInstanciaBitacora relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinWfInstanciaBitacora($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('WfInstanciaBitacora');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'WfInstanciaBitacora');
        }

        return $this;
    }

    /**
     * Use the WfInstanciaBitacora relation WfInstanciaBitacora object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   WfInstanciaBitacoraQuery A secondary query class using the current class as primary query
     */
    public function useWfInstanciaBitacoraQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinWfInstanciaBitacora($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'WfInstanciaBitacora', 'WfInstanciaBitacoraQuery');
    }

    /**
     * Filter the query by a related ComrecibidaDuplicados object
     *
     * @param   ComrecibidaDuplicados|PropelObjectCollection $comrecibidaDuplicados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComrecibidaDuplicados($comrecibidaDuplicados, $comparison = null)
    {
        if ($comrecibidaDuplicados instanceof ComrecibidaDuplicados) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaDuplicados->getComrecibidaId(), $comparison);
        } elseif ($comrecibidaDuplicados instanceof PropelObjectCollection) {
            return $this
                ->useComrecibidaDuplicadosQuery()
                ->filterByPrimaryKeys($comrecibidaDuplicados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComrecibidaDuplicados() only accepts arguments of type ComrecibidaDuplicados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComrecibidaDuplicados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinComrecibidaDuplicados($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComrecibidaDuplicados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComrecibidaDuplicados');
        }

        return $this;
    }

    /**
     * Use the ComrecibidaDuplicados relation ComrecibidaDuplicados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComrecibidaDuplicadosQuery A secondary query class using the current class as primary query
     */
    public function useComrecibidaDuplicadosQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComrecibidaDuplicados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComrecibidaDuplicados', 'ComrecibidaDuplicadosQuery');
    }

    /**
     * Filter the query by a related ComrecibidaInteresados object
     *
     * @param   ComrecibidaInteresados|PropelObjectCollection $comrecibidaInteresados  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComrecibidaInteresados($comrecibidaInteresados, $comparison = null)
    {
        if ($comrecibidaInteresados instanceof ComrecibidaInteresados) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaInteresados->getComrecibidaId(), $comparison);
        } elseif ($comrecibidaInteresados instanceof PropelObjectCollection) {
            return $this
                ->useComrecibidaInteresadosQuery()
                ->filterByPrimaryKeys($comrecibidaInteresados->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComrecibidaInteresados() only accepts arguments of type ComrecibidaInteresados or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComrecibidaInteresados relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinComrecibidaInteresados($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComrecibidaInteresados');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComrecibidaInteresados');
        }

        return $this;
    }

    /**
     * Use the ComrecibidaInteresados relation ComrecibidaInteresados object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComrecibidaInteresadosQuery A secondary query class using the current class as primary query
     */
    public function useComrecibidaInteresadosQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComrecibidaInteresados($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComrecibidaInteresados', 'ComrecibidaInteresadosQuery');
    }

    /**
     * Filter the query by a related ComrecibidaUsuario object
     *
     * @param   ComrecibidaUsuario|PropelObjectCollection $comrecibidaUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComrecibidaUsuario($comrecibidaUsuario, $comparison = null)
    {
        if ($comrecibidaUsuario instanceof ComrecibidaUsuario) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaUsuario->getComrecibidaId(), $comparison);
        } elseif ($comrecibidaUsuario instanceof PropelObjectCollection) {
            return $this
                ->useComrecibidaUsuarioQuery()
                ->filterByPrimaryKeys($comrecibidaUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComrecibidaUsuario() only accepts arguments of type ComrecibidaUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComrecibidaUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinComrecibidaUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComrecibidaUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComrecibidaUsuario');
        }

        return $this;
    }

    /**
     * Use the ComrecibidaUsuario relation ComrecibidaUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComrecibidaUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useComrecibidaUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComrecibidaUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComrecibidaUsuario', 'ComrecibidaUsuarioQuery');
    }

    /**
     * Filter the query by a related ClTransferencia object
     *
     * @param   ClTransferencia|PropelObjectCollection $clTransferencia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByClTransferencia($clTransferencia, $comparison = null)
    {
        if ($clTransferencia instanceof ClTransferencia) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $clTransferencia->getComrecibidaId(), $comparison);
        } elseif ($clTransferencia instanceof PropelObjectCollection) {
            return $this
                ->useClTransferenciaQuery()
                ->filterByPrimaryKeys($clTransferencia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByClTransferencia() only accepts arguments of type ClTransferencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ClTransferencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinClTransferencia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ClTransferencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ClTransferencia');
        }

        return $this;
    }

    /**
     * Use the ClTransferencia relation ClTransferencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ClTransferenciaQuery A secondary query class using the current class as primary query
     */
    public function useClTransferenciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinClTransferencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ClTransferencia', 'ClTransferenciaQuery');
    }

    /**
     * Filter the query by a related ComenviadaMasivas object
     *
     * @param   ComenviadaMasivas|PropelObjectCollection $comenviadaMasivas  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComenviadaMasivas($comenviadaMasivas, $comparison = null)
    {
        if ($comenviadaMasivas instanceof ComenviadaMasivas) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comenviadaMasivas->getComrecibidaId(), $comparison);
        } elseif ($comenviadaMasivas instanceof PropelObjectCollection) {
            return $this
                ->useComenviadaMasivasQuery()
                ->filterByPrimaryKeys($comenviadaMasivas->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComenviadaMasivas() only accepts arguments of type ComenviadaMasivas or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComenviadaMasivas relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinComenviadaMasivas($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComenviadaMasivas');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComenviadaMasivas');
        }

        return $this;
    }

    /**
     * Use the ComenviadaMasivas relation ComenviadaMasivas object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComenviadaMasivasQuery A secondary query class using the current class as primary query
     */
    public function useComenviadaMasivasQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComenviadaMasivas($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComenviadaMasivas', 'ComenviadaMasivasQuery');
    }

    /**
     * Filter the query by a related ComrecibidaRespuesta object
     *
     * @param   ComrecibidaRespuesta|PropelObjectCollection $comrecibidaRespuesta  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComrecibidaRespuesta($comrecibidaRespuesta, $comparison = null)
    {
        if ($comrecibidaRespuesta instanceof ComrecibidaRespuesta) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comrecibidaRespuesta->getComrecibidaId(), $comparison);
        } elseif ($comrecibidaRespuesta instanceof PropelObjectCollection) {
            return $this
                ->useComrecibidaRespuestaQuery()
                ->filterByPrimaryKeys($comrecibidaRespuesta->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComrecibidaRespuesta() only accepts arguments of type ComrecibidaRespuesta or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComrecibidaRespuesta relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinComrecibidaRespuesta($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComrecibidaRespuesta');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComrecibidaRespuesta');
        }

        return $this;
    }

    /**
     * Use the ComrecibidaRespuesta relation ComrecibidaRespuesta object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComrecibidaRespuestaQuery A secondary query class using the current class as primary query
     */
    public function useComrecibidaRespuestaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComrecibidaRespuesta($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComrecibidaRespuesta', 'ComrecibidaRespuestaQuery');
    }

    /**
     * Filter the query by a related Transferencia object
     *
     * @param   Transferencia|PropelObjectCollection $transferencia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComRecibidaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTransferencia($transferencia, $comparison = null)
    {
        if ($transferencia instanceof Transferencia) {
            return $this
                ->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $transferencia->getComrecibidaId(), $comparison);
        } elseif ($transferencia instanceof PropelObjectCollection) {
            return $this
                ->useTransferenciaQuery()
                ->filterByPrimaryKeys($transferencia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTransferencia() only accepts arguments of type Transferencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Transferencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function joinTransferencia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Transferencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Transferencia');
        }

        return $this;
    }

    /**
     * Use the Transferencia relation Transferencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TransferenciaQuery A secondary query class using the current class as primary query
     */
    public function useTransferenciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTransferencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Transferencia', 'TransferenciaQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   ComRecibida $comRecibida Object to remove from the list of results
     *
     * @return ComRecibidaQuery The current query, for fluid interface
     */
    public function prune($comRecibida = null)
    {
        if ($comRecibida) {
            $this->addUsingAlias(ComRecibidaPeer::COMRECIBIDA_ID, $comRecibida->getComrecibidaId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

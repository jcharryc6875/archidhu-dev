<?php


/**
 * Base class that represents a query for the 'COMENVIADA_MASIVAS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:05
 *
 * @method ComenviadaMasivasQuery orderByComenviadamasivasId($order = Criteria::ASC) Order by the COMENVIADAMASIVAS_ID column
 * @method ComenviadaMasivasQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method ComenviadaMasivasQuery orderByComrecibidaId($order = Criteria::ASC) Order by the COMRECIBIDA_ID column
 * @method ComenviadaMasivasQuery orderByComenviadaId($order = Criteria::ASC) Order by the COMENVIADA_ID column
 * @method ComenviadaMasivasQuery orderByPlantillascomId($order = Criteria::ASC) Order by the PLANTILLASCOM_ID column
 * @method ComenviadaMasivasQuery orderByMarca($order = Criteria::ASC) Order by the MARCA column
 * @method ComenviadaMasivasQuery orderByEstadoProceso($order = Criteria::ASC) Order by the ESTADO_PROCESO column
 * @method ComenviadaMasivasQuery orderByMsgProceso($order = Criteria::ASC) Order by the MSG_PROCESO column
 * @method ComenviadaMasivasQuery orderByUsuariosFirmas($order = Criteria::ASC) Order by the USUARIOS_FIRMAS column
 * @method ComenviadaMasivasQuery orderByCargosFirmas($order = Criteria::ASC) Order by the CARGOS_FIRMAS column
 * @method ComenviadaMasivasQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method ComenviadaMasivasQuery orderByFechaEjecucion($order = Criteria::ASC) Order by the FECHA_EJECUCION column
 * @method ComenviadaMasivasQuery orderByContenidoText($order = Criteria::ASC) Order by the CONTENIDO_TEXT column
 * @method ComenviadaMasivasQuery orderBySendEmail($order = Criteria::ASC) Order by the SEND_EMAIL column
 * @method ComenviadaMasivasQuery orderByGenerateFile($order = Criteria::ASC) Order by the GENERATE_FILE column
 * @method ComenviadaMasivasQuery orderByRadicadoRespuesta($order = Criteria::ASC) Order by the RADICADO_RESPUESTA column
 *
 * @method ComenviadaMasivasQuery groupByComenviadamasivasId() Group by the COMENVIADAMASIVAS_ID column
 * @method ComenviadaMasivasQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method ComenviadaMasivasQuery groupByComrecibidaId() Group by the COMRECIBIDA_ID column
 * @method ComenviadaMasivasQuery groupByComenviadaId() Group by the COMENVIADA_ID column
 * @method ComenviadaMasivasQuery groupByPlantillascomId() Group by the PLANTILLASCOM_ID column
 * @method ComenviadaMasivasQuery groupByMarca() Group by the MARCA column
 * @method ComenviadaMasivasQuery groupByEstadoProceso() Group by the ESTADO_PROCESO column
 * @method ComenviadaMasivasQuery groupByMsgProceso() Group by the MSG_PROCESO column
 * @method ComenviadaMasivasQuery groupByUsuariosFirmas() Group by the USUARIOS_FIRMAS column
 * @method ComenviadaMasivasQuery groupByCargosFirmas() Group by the CARGOS_FIRMAS column
 * @method ComenviadaMasivasQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method ComenviadaMasivasQuery groupByFechaEjecucion() Group by the FECHA_EJECUCION column
 * @method ComenviadaMasivasQuery groupByContenidoText() Group by the CONTENIDO_TEXT column
 * @method ComenviadaMasivasQuery groupBySendEmail() Group by the SEND_EMAIL column
 * @method ComenviadaMasivasQuery groupByGenerateFile() Group by the GENERATE_FILE column
 * @method ComenviadaMasivasQuery groupByRadicadoRespuesta() Group by the RADICADO_RESPUESTA column
 *
 * @method ComenviadaMasivasQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ComenviadaMasivasQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ComenviadaMasivasQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ComenviadaMasivasQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method ComenviadaMasivasQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method ComenviadaMasivasQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method ComenviadaMasivasQuery leftJoinComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComRecibida relation
 * @method ComenviadaMasivasQuery rightJoinComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComRecibida relation
 * @method ComenviadaMasivasQuery innerJoinComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the ComRecibida relation
 *
 * @method ComenviadaMasivasQuery leftJoinComEnviada($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComEnviada relation
 * @method ComenviadaMasivasQuery rightJoinComEnviada($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComEnviada relation
 * @method ComenviadaMasivasQuery innerJoinComEnviada($relationAlias = null) Adds a INNER JOIN clause to the query using the ComEnviada relation
 *
 * @method ComenviadaMasivasQuery leftJoinPlantillasCom($relationAlias = null) Adds a LEFT JOIN clause to the query using the PlantillasCom relation
 * @method ComenviadaMasivasQuery rightJoinPlantillasCom($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PlantillasCom relation
 * @method ComenviadaMasivasQuery innerJoinPlantillasCom($relationAlias = null) Adds a INNER JOIN clause to the query using the PlantillasCom relation
 *
 * @method ComenviadaMasivas findOne(PropelPDO $con = null) Return the first ComenviadaMasivas matching the query
 * @method ComenviadaMasivas findOneOrCreate(PropelPDO $con = null) Return the first ComenviadaMasivas matching the query, or a new ComenviadaMasivas object populated from the query conditions when no match is found
 *
 * @method ComenviadaMasivas findOneByUsuarioId(int $USUARIO_ID) Return the first ComenviadaMasivas filtered by the USUARIO_ID column
 * @method ComenviadaMasivas findOneByComrecibidaId(int $COMRECIBIDA_ID) Return the first ComenviadaMasivas filtered by the COMRECIBIDA_ID column
 * @method ComenviadaMasivas findOneByComenviadaId(int $COMENVIADA_ID) Return the first ComenviadaMasivas filtered by the COMENVIADA_ID column
 * @method ComenviadaMasivas findOneByPlantillascomId(int $PLANTILLASCOM_ID) Return the first ComenviadaMasivas filtered by the PLANTILLASCOM_ID column
 * @method ComenviadaMasivas findOneByMarca(int $MARCA) Return the first ComenviadaMasivas filtered by the MARCA column
 * @method ComenviadaMasivas findOneByEstadoProceso(int $ESTADO_PROCESO) Return the first ComenviadaMasivas filtered by the ESTADO_PROCESO column
 * @method ComenviadaMasivas findOneByMsgProceso(string $MSG_PROCESO) Return the first ComenviadaMasivas filtered by the MSG_PROCESO column
 * @method ComenviadaMasivas findOneByUsuariosFirmas(string $USUARIOS_FIRMAS) Return the first ComenviadaMasivas filtered by the USUARIOS_FIRMAS column
 * @method ComenviadaMasivas findOneByCargosFirmas(string $CARGOS_FIRMAS) Return the first ComenviadaMasivas filtered by the CARGOS_FIRMAS column
 * @method ComenviadaMasivas findOneByFechaCreacion(string $FECHA_CREACION) Return the first ComenviadaMasivas filtered by the FECHA_CREACION column
 * @method ComenviadaMasivas findOneByFechaEjecucion(string $FECHA_EJECUCION) Return the first ComenviadaMasivas filtered by the FECHA_EJECUCION column
 * @method ComenviadaMasivas findOneByContenidoText(string $CONTENIDO_TEXT) Return the first ComenviadaMasivas filtered by the CONTENIDO_TEXT column
 * @method ComenviadaMasivas findOneBySendEmail(int $SEND_EMAIL) Return the first ComenviadaMasivas filtered by the SEND_EMAIL column
 * @method ComenviadaMasivas findOneByGenerateFile(int $GENERATE_FILE) Return the first ComenviadaMasivas filtered by the GENERATE_FILE column
 * @method ComenviadaMasivas findOneByRadicadoRespuesta(string $RADICADO_RESPUESTA) Return the first ComenviadaMasivas filtered by the RADICADO_RESPUESTA column
 *
 * @method array findByComenviadamasivasId(int $COMENVIADAMASIVAS_ID) Return ComenviadaMasivas objects filtered by the COMENVIADAMASIVAS_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return ComenviadaMasivas objects filtered by the USUARIO_ID column
 * @method array findByComrecibidaId(int $COMRECIBIDA_ID) Return ComenviadaMasivas objects filtered by the COMRECIBIDA_ID column
 * @method array findByComenviadaId(int $COMENVIADA_ID) Return ComenviadaMasivas objects filtered by the COMENVIADA_ID column
 * @method array findByPlantillascomId(int $PLANTILLASCOM_ID) Return ComenviadaMasivas objects filtered by the PLANTILLASCOM_ID column
 * @method array findByMarca(int $MARCA) Return ComenviadaMasivas objects filtered by the MARCA column
 * @method array findByEstadoProceso(int $ESTADO_PROCESO) Return ComenviadaMasivas objects filtered by the ESTADO_PROCESO column
 * @method array findByMsgProceso(string $MSG_PROCESO) Return ComenviadaMasivas objects filtered by the MSG_PROCESO column
 * @method array findByUsuariosFirmas(string $USUARIOS_FIRMAS) Return ComenviadaMasivas objects filtered by the USUARIOS_FIRMAS column
 * @method array findByCargosFirmas(string $CARGOS_FIRMAS) Return ComenviadaMasivas objects filtered by the CARGOS_FIRMAS column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return ComenviadaMasivas objects filtered by the FECHA_CREACION column
 * @method array findByFechaEjecucion(string $FECHA_EJECUCION) Return ComenviadaMasivas objects filtered by the FECHA_EJECUCION column
 * @method array findByContenidoText(string $CONTENIDO_TEXT) Return ComenviadaMasivas objects filtered by the CONTENIDO_TEXT column
 * @method array findBySendEmail(int $SEND_EMAIL) Return ComenviadaMasivas objects filtered by the SEND_EMAIL column
 * @method array findByGenerateFile(int $GENERATE_FILE) Return ComenviadaMasivas objects filtered by the GENERATE_FILE column
 * @method array findByRadicadoRespuesta(string $RADICADO_RESPUESTA) Return ComenviadaMasivas objects filtered by the RADICADO_RESPUESTA column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseComenviadaMasivasQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseComenviadaMasivasQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'ComenviadaMasivas';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ComenviadaMasivasQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   ComenviadaMasivasQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ComenviadaMasivasQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ComenviadaMasivasQuery) {
            return $criteria;
        }
        $query = new ComenviadaMasivasQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   ComenviadaMasivas|ComenviadaMasivas[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ComenviadaMasivasPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ComenviadaMasivasPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ComenviadaMasivas A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByComenviadamasivasId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ComenviadaMasivas A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [COMENVIADAMASIVAS_ID], [USUARIO_ID], [COMRECIBIDA_ID], [COMENVIADA_ID], [PLANTILLASCOM_ID], [MARCA], [ESTADO_PROCESO], [MSG_PROCESO], [USUARIOS_FIRMAS], [CARGOS_FIRMAS], [FECHA_CREACION], [FECHA_EJECUCION], [CONTENIDO_TEXT], [SEND_EMAIL], [GENERATE_FILE], [RADICADO_RESPUESTA] FROM [COMENVIADA_MASIVAS] WHERE [COMENVIADAMASIVAS_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new ComenviadaMasivas();
            $obj->hydrate($row);
            ComenviadaMasivasPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return ComenviadaMasivas|ComenviadaMasivas[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|ComenviadaMasivas[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADAMASIVAS_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADAMASIVAS_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the COMENVIADAMASIVAS_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByComenviadamasivasId(1234); // WHERE COMENVIADAMASIVAS_ID = 1234
     * $query->filterByComenviadamasivasId(array(12, 34)); // WHERE COMENVIADAMASIVAS_ID IN (12, 34)
     * $query->filterByComenviadamasivasId(array('min' => 12)); // WHERE COMENVIADAMASIVAS_ID >= 12
     * $query->filterByComenviadamasivasId(array('max' => 12)); // WHERE COMENVIADAMASIVAS_ID <= 12
     * </code>
     *
     * @param     mixed $comenviadamasivasId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByComenviadamasivasId($comenviadamasivasId = null, $comparison = null)
    {
        if (is_array($comenviadamasivasId)) {
            $useMinMax = false;
            if (isset($comenviadamasivasId['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADAMASIVAS_ID, $comenviadamasivasId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comenviadamasivasId['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADAMASIVAS_ID, $comenviadamasivasId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADAMASIVAS_ID, $comenviadamasivasId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the COMRECIBIDA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByComrecibidaId(1234); // WHERE COMRECIBIDA_ID = 1234
     * $query->filterByComrecibidaId(array(12, 34)); // WHERE COMRECIBIDA_ID IN (12, 34)
     * $query->filterByComrecibidaId(array('min' => 12)); // WHERE COMRECIBIDA_ID >= 12
     * $query->filterByComrecibidaId(array('max' => 12)); // WHERE COMRECIBIDA_ID <= 12
     * </code>
     *
     * @see       filterByComRecibida()
     *
     * @param     mixed $comrecibidaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByComrecibidaId($comrecibidaId = null, $comparison = null)
    {
        if (is_array($comrecibidaId)) {
            $useMinMax = false;
            if (isset($comrecibidaId['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::COMRECIBIDA_ID, $comrecibidaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comrecibidaId['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::COMRECIBIDA_ID, $comrecibidaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::COMRECIBIDA_ID, $comrecibidaId, $comparison);
    }

    /**
     * Filter the query on the COMENVIADA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByComenviadaId(1234); // WHERE COMENVIADA_ID = 1234
     * $query->filterByComenviadaId(array(12, 34)); // WHERE COMENVIADA_ID IN (12, 34)
     * $query->filterByComenviadaId(array('min' => 12)); // WHERE COMENVIADA_ID >= 12
     * $query->filterByComenviadaId(array('max' => 12)); // WHERE COMENVIADA_ID <= 12
     * </code>
     *
     * @see       filterByComEnviada()
     *
     * @param     mixed $comenviadaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByComenviadaId($comenviadaId = null, $comparison = null)
    {
        if (is_array($comenviadaId)) {
            $useMinMax = false;
            if (isset($comenviadaId['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADA_ID, $comenviadaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comenviadaId['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADA_ID, $comenviadaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADA_ID, $comenviadaId, $comparison);
    }

    /**
     * Filter the query on the PLANTILLASCOM_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByPlantillascomId(1234); // WHERE PLANTILLASCOM_ID = 1234
     * $query->filterByPlantillascomId(array(12, 34)); // WHERE PLANTILLASCOM_ID IN (12, 34)
     * $query->filterByPlantillascomId(array('min' => 12)); // WHERE PLANTILLASCOM_ID >= 12
     * $query->filterByPlantillascomId(array('max' => 12)); // WHERE PLANTILLASCOM_ID <= 12
     * </code>
     *
     * @see       filterByPlantillasCom()
     *
     * @param     mixed $plantillascomId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByPlantillascomId($plantillascomId = null, $comparison = null)
    {
        if (is_array($plantillascomId)) {
            $useMinMax = false;
            if (isset($plantillascomId['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::PLANTILLASCOM_ID, $plantillascomId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($plantillascomId['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::PLANTILLASCOM_ID, $plantillascomId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::PLANTILLASCOM_ID, $plantillascomId, $comparison);
    }

    /**
     * Filter the query on the MARCA column
     *
     * Example usage:
     * <code>
     * $query->filterByMarca(1234); // WHERE MARCA = 1234
     * $query->filterByMarca(array(12, 34)); // WHERE MARCA IN (12, 34)
     * $query->filterByMarca(array('min' => 12)); // WHERE MARCA >= 12
     * $query->filterByMarca(array('max' => 12)); // WHERE MARCA <= 12
     * </code>
     *
     * @param     mixed $marca The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByMarca($marca = null, $comparison = null)
    {
        if (is_array($marca)) {
            $useMinMax = false;
            if (isset($marca['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::MARCA, $marca['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($marca['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::MARCA, $marca['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::MARCA, $marca, $comparison);
    }

    /**
     * Filter the query on the ESTADO_PROCESO column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoProceso(1234); // WHERE ESTADO_PROCESO = 1234
     * $query->filterByEstadoProceso(array(12, 34)); // WHERE ESTADO_PROCESO IN (12, 34)
     * $query->filterByEstadoProceso(array('min' => 12)); // WHERE ESTADO_PROCESO >= 12
     * $query->filterByEstadoProceso(array('max' => 12)); // WHERE ESTADO_PROCESO <= 12
     * </code>
     *
     * @param     mixed $estadoProceso The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByEstadoProceso($estadoProceso = null, $comparison = null)
    {
        if (is_array($estadoProceso)) {
            $useMinMax = false;
            if (isset($estadoProceso['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::ESTADO_PROCESO, $estadoProceso['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadoProceso['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::ESTADO_PROCESO, $estadoProceso['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::ESTADO_PROCESO, $estadoProceso, $comparison);
    }

    /**
     * Filter the query on the MSG_PROCESO column
     *
     * Example usage:
     * <code>
     * $query->filterByMsgProceso('fooValue');   // WHERE MSG_PROCESO = 'fooValue'
     * $query->filterByMsgProceso('%fooValue%'); // WHERE MSG_PROCESO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $msgProceso The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByMsgProceso($msgProceso = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($msgProceso)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $msgProceso)) {
                $msgProceso = str_replace('*', '%', $msgProceso);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::MSG_PROCESO, $msgProceso, $comparison);
    }

    /**
     * Filter the query on the USUARIOS_FIRMAS column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuariosFirmas('fooValue');   // WHERE USUARIOS_FIRMAS = 'fooValue'
     * $query->filterByUsuariosFirmas('%fooValue%'); // WHERE USUARIOS_FIRMAS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuariosFirmas The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByUsuariosFirmas($usuariosFirmas = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuariosFirmas)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuariosFirmas)) {
                $usuariosFirmas = str_replace('*', '%', $usuariosFirmas);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::USUARIOS_FIRMAS, $usuariosFirmas, $comparison);
    }

    /**
     * Filter the query on the CARGOS_FIRMAS column
     *
     * Example usage:
     * <code>
     * $query->filterByCargosFirmas('fooValue');   // WHERE CARGOS_FIRMAS = 'fooValue'
     * $query->filterByCargosFirmas('%fooValue%'); // WHERE CARGOS_FIRMAS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cargosFirmas The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByCargosFirmas($cargosFirmas = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cargosFirmas)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cargosFirmas)) {
                $cargosFirmas = str_replace('*', '%', $cargosFirmas);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::CARGOS_FIRMAS, $cargosFirmas, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_EJECUCION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaEjecucion('2011-03-14'); // WHERE FECHA_EJECUCION = '2011-03-14'
     * $query->filterByFechaEjecucion('now'); // WHERE FECHA_EJECUCION = '2011-03-14'
     * $query->filterByFechaEjecucion(array('max' => 'yesterday')); // WHERE FECHA_EJECUCION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaEjecucion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByFechaEjecucion($fechaEjecucion = null, $comparison = null)
    {
        if (is_array($fechaEjecucion)) {
            $useMinMax = false;
            if (isset($fechaEjecucion['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::FECHA_EJECUCION, $fechaEjecucion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaEjecucion['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::FECHA_EJECUCION, $fechaEjecucion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::FECHA_EJECUCION, $fechaEjecucion, $comparison);
    }

    /**
     * Filter the query on the CONTENIDO_TEXT column
     *
     * Example usage:
     * <code>
     * $query->filterByContenidoText('fooValue');   // WHERE CONTENIDO_TEXT = 'fooValue'
     * $query->filterByContenidoText('%fooValue%'); // WHERE CONTENIDO_TEXT LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contenidoText The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByContenidoText($contenidoText = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contenidoText)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contenidoText)) {
                $contenidoText = str_replace('*', '%', $contenidoText);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::CONTENIDO_TEXT, $contenidoText, $comparison);
    }

    /**
     * Filter the query on the SEND_EMAIL column
     *
     * Example usage:
     * <code>
     * $query->filterBySendEmail(1234); // WHERE SEND_EMAIL = 1234
     * $query->filterBySendEmail(array(12, 34)); // WHERE SEND_EMAIL IN (12, 34)
     * $query->filterBySendEmail(array('min' => 12)); // WHERE SEND_EMAIL >= 12
     * $query->filterBySendEmail(array('max' => 12)); // WHERE SEND_EMAIL <= 12
     * </code>
     *
     * @param     mixed $sendEmail The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterBySendEmail($sendEmail = null, $comparison = null)
    {
        if (is_array($sendEmail)) {
            $useMinMax = false;
            if (isset($sendEmail['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::SEND_EMAIL, $sendEmail['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($sendEmail['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::SEND_EMAIL, $sendEmail['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::SEND_EMAIL, $sendEmail, $comparison);
    }

    /**
     * Filter the query on the GENERATE_FILE column
     *
     * Example usage:
     * <code>
     * $query->filterByGenerateFile(1234); // WHERE GENERATE_FILE = 1234
     * $query->filterByGenerateFile(array(12, 34)); // WHERE GENERATE_FILE IN (12, 34)
     * $query->filterByGenerateFile(array('min' => 12)); // WHERE GENERATE_FILE >= 12
     * $query->filterByGenerateFile(array('max' => 12)); // WHERE GENERATE_FILE <= 12
     * </code>
     *
     * @param     mixed $generateFile The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByGenerateFile($generateFile = null, $comparison = null)
    {
        if (is_array($generateFile)) {
            $useMinMax = false;
            if (isset($generateFile['min'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::GENERATE_FILE, $generateFile['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($generateFile['max'])) {
                $this->addUsingAlias(ComenviadaMasivasPeer::GENERATE_FILE, $generateFile['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::GENERATE_FILE, $generateFile, $comparison);
    }

    /**
     * Filter the query on the RADICADO_RESPUESTA column
     *
     * Example usage:
     * <code>
     * $query->filterByRadicadoRespuesta('fooValue');   // WHERE RADICADO_RESPUESTA = 'fooValue'
     * $query->filterByRadicadoRespuesta('%fooValue%'); // WHERE RADICADO_RESPUESTA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $radicadoRespuesta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function filterByRadicadoRespuesta($radicadoRespuesta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($radicadoRespuesta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $radicadoRespuesta)) {
                $radicadoRespuesta = str_replace('*', '%', $radicadoRespuesta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ComenviadaMasivasPeer::RADICADO_RESPUESTA, $radicadoRespuesta, $comparison);
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComenviadaMasivasQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related ComRecibida object
     *
     * @param   ComRecibida|PropelObjectCollection $comRecibida The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComenviadaMasivasQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComRecibida($comRecibida, $comparison = null)
    {
        if ($comRecibida instanceof ComRecibida) {
            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::COMRECIBIDA_ID, $comRecibida->getComrecibidaId(), $comparison);
        } elseif ($comRecibida instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::COMRECIBIDA_ID, $comRecibida->toKeyValue('PrimaryKey', 'ComrecibidaId'), $comparison);
        } else {
            throw new PropelException('filterByComRecibida() only accepts arguments of type ComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function joinComRecibida($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComRecibida');
        }

        return $this;
    }

    /**
     * Use the ComRecibida relation ComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useComRecibidaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComRecibida', 'ComRecibidaQuery');
    }

    /**
     * Filter the query by a related ComEnviada object
     *
     * @param   ComEnviada|PropelObjectCollection $comEnviada The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComenviadaMasivasQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComEnviada($comEnviada, $comparison = null)
    {
        if ($comEnviada instanceof ComEnviada) {
            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::COMENVIADA_ID, $comEnviada->getComenviadaId(), $comparison);
        } elseif ($comEnviada instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::COMENVIADA_ID, $comEnviada->toKeyValue('PrimaryKey', 'ComenviadaId'), $comparison);
        } else {
            throw new PropelException('filterByComEnviada() only accepts arguments of type ComEnviada or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComEnviada relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function joinComEnviada($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComEnviada');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComEnviada');
        }

        return $this;
    }

    /**
     * Use the ComEnviada relation ComEnviada object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComEnviadaQuery A secondary query class using the current class as primary query
     */
    public function useComEnviadaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComEnviada($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComEnviada', 'ComEnviadaQuery');
    }

    /**
     * Filter the query by a related PlantillasCom object
     *
     * @param   PlantillasCom|PropelObjectCollection $plantillasCom The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ComenviadaMasivasQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByPlantillasCom($plantillasCom, $comparison = null)
    {
        if ($plantillasCom instanceof PlantillasCom) {
            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::PLANTILLASCOM_ID, $plantillasCom->getPlantillascomId(), $comparison);
        } elseif ($plantillasCom instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ComenviadaMasivasPeer::PLANTILLASCOM_ID, $plantillasCom->toKeyValue('PrimaryKey', 'PlantillascomId'), $comparison);
        } else {
            throw new PropelException('filterByPlantillasCom() only accepts arguments of type PlantillasCom or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PlantillasCom relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function joinPlantillasCom($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PlantillasCom');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PlantillasCom');
        }

        return $this;
    }

    /**
     * Use the PlantillasCom relation PlantillasCom object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PlantillasComQuery A secondary query class using the current class as primary query
     */
    public function usePlantillasComQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinPlantillasCom($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PlantillasCom', 'PlantillasComQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   ComenviadaMasivas $comenviadaMasivas Object to remove from the list of results
     *
     * @return ComenviadaMasivasQuery The current query, for fluid interface
     */
    public function prune($comenviadaMasivas = null)
    {
        if ($comenviadaMasivas) {
            $this->addUsingAlias(ComenviadaMasivasPeer::COMENVIADAMASIVAS_ID, $comenviadaMasivas->getComenviadamasivasId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

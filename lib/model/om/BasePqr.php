<?php


/**
 * Base class that represents a row from the 'PQR' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:50
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BasePqr extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'PqrPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        PqrPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the pqr_id field.
     * @var        int
     */
    protected $pqr_id;

    /**
     * The value for the prioridadpqr_id field.
     * @var        int
     */
    protected $prioridadpqr_id;

    /**
     * The value for the categoriapqr_id field.
     * @var        int
     */
    protected $categoriapqr_id;

    /**
     * The value for the usuario_id field.
     * @var        int
     */
    protected $usuario_id;

    /**
     * The value for the estadopqr_id field.
     * @var        int
     */
    protected $estadopqr_id;

    /**
     * The value for the objeto field.
     * @var        string
     */
    protected $objeto;

    /**
     * The value for the contenido field.
     * @var        string
     */
    protected $contenido;

    /**
     * The value for the ruta field.
     * @var        string
     */
    protected $ruta;

    /**
     * The value for the fecha_creacion field.
     * @var        string
     */
    protected $fecha_creacion;

    /**
     * The value for the nombre_solicitante field.
     * @var        string
     */
    protected $nombre_solicitante;

    /**
     * The value for the email field.
     * @var        string
     */
    protected $email;

    /**
     * @var        PrioridadPqr
     */
    protected $aPrioridadPqr;

    /**
     * @var        CategoriaPqr
     */
    protected $aCategoriaPqr;

    /**
     * @var        Usuario
     */
    protected $aUsuario;

    /**
     * @var        EstadoPqr
     */
    protected $aEstadoPqr;

    /**
     * @var        PropelObjectCollection|NotaPqr[] Collection to store aggregation of NotaPqr objects.
     */
    protected $collNotaPqrs;
    protected $collNotaPqrsPartial;

    /**
     * @var        PropelObjectCollection|AsignacionPqr[] Collection to store aggregation of AsignacionPqr objects.
     */
    protected $collAsignacionPqrs;
    protected $collAsignacionPqrsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $notaPqrsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $asignacionPqrsScheduledForDeletion = null;

    /**
     * Get the [pqr_id] column value.
     * 
     * @return int
     */
    public function getPqrId()
    {

        return $this->pqr_id;
    }

    /**
     * Get the [prioridadpqr_id] column value.
     * 
     * @return int
     */
    public function getPrioridadpqrId()
    {

        return $this->prioridadpqr_id;
    }

    /**
     * Get the [categoriapqr_id] column value.
     * 
     * @return int
     */
    public function getCategoriapqrId()
    {

        return $this->categoriapqr_id;
    }

    /**
     * Get the [usuario_id] column value.
     * 
     * @return int
     */
    public function getUsuarioId()
    {

        return $this->usuario_id;
    }

    /**
     * Get the [estadopqr_id] column value.
     * 
     * @return int
     */
    public function getEstadopqrId()
    {

        return $this->estadopqr_id;
    }

    /**
     * Get the [objeto] column value.
     * 
     * @return string
     */
    public function getObjeto()
    {

        return $this->objeto;
    }

    /**
     * Get the [contenido] column value.
     * 
     * @return string
     */
    public function getContenido()
    {

        return $this->contenido;
    }

    /**
     * Get the [ruta] column value.
     * 
     * @return string
     */
    public function getRuta()
    {

        return $this->ruta;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_creacion] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaCreacion($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_creacion === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_creacion);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_creacion, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [nombre_solicitante] column value.
     * 
     * @return string
     */
    public function getNombreSolicitante()
    {

        return $this->nombre_solicitante;
    }

    /**
     * Get the [email] column value.
     * 
     * @return string
     */
    public function getEmail()
    {

        return $this->email;
    }

    /**
     * Set the value of [pqr_id] column.
     * 
     * @param  int $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setPqrId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->pqr_id !== $v) {
            $this->pqr_id = $v;
            $this->modifiedColumns[] = PqrPeer::PQR_ID;
        }


        return $this;
    } // setPqrId()

    /**
     * Set the value of [prioridadpqr_id] column.
     * 
     * @param  int $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setPrioridadpqrId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->prioridadpqr_id !== $v) {
            $this->prioridadpqr_id = $v;
            $this->modifiedColumns[] = PqrPeer::PRIORIDADPQR_ID;
        }

        if ($this->aPrioridadPqr !== null && $this->aPrioridadPqr->getPrioridadpqrId() !== $v) {
            $this->aPrioridadPqr = null;
        }


        return $this;
    } // setPrioridadpqrId()

    /**
     * Set the value of [categoriapqr_id] column.
     * 
     * @param  int $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setCategoriapqrId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->categoriapqr_id !== $v) {
            $this->categoriapqr_id = $v;
            $this->modifiedColumns[] = PqrPeer::CATEGORIAPQR_ID;
        }

        if ($this->aCategoriaPqr !== null && $this->aCategoriaPqr->getCategoriapqrId() !== $v) {
            $this->aCategoriaPqr = null;
        }


        return $this;
    } // setCategoriapqrId()

    /**
     * Set the value of [usuario_id] column.
     * 
     * @param  int $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setUsuarioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->usuario_id !== $v) {
            $this->usuario_id = $v;
            $this->modifiedColumns[] = PqrPeer::USUARIO_ID;
        }

        if ($this->aUsuario !== null && $this->aUsuario->getUsuarioId() !== $v) {
            $this->aUsuario = null;
        }


        return $this;
    } // setUsuarioId()

    /**
     * Set the value of [estadopqr_id] column.
     * 
     * @param  int $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setEstadopqrId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->estadopqr_id !== $v) {
            $this->estadopqr_id = $v;
            $this->modifiedColumns[] = PqrPeer::ESTADOPQR_ID;
        }

        if ($this->aEstadoPqr !== null && $this->aEstadoPqr->getEstadopqrId() !== $v) {
            $this->aEstadoPqr = null;
        }


        return $this;
    } // setEstadopqrId()

    /**
     * Set the value of [objeto] column.
     * 
     * @param  string $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setObjeto($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->objeto !== $v) {
            $this->objeto = $v;
            $this->modifiedColumns[] = PqrPeer::OBJETO;
        }


        return $this;
    } // setObjeto()

    /**
     * Set the value of [contenido] column.
     * 
     * @param  string $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setContenido($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->contenido !== $v) {
            $this->contenido = $v;
            $this->modifiedColumns[] = PqrPeer::CONTENIDO;
        }


        return $this;
    } // setContenido()

    /**
     * Set the value of [ruta] column.
     * 
     * @param  string $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setRuta($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->ruta !== $v) {
            $this->ruta = $v;
            $this->modifiedColumns[] = PqrPeer::RUTA;
        }


        return $this;
    } // setRuta()

    /**
     * Sets the value of [fecha_creacion] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return Pqr The current object (for fluent API support)
     */
    public function setFechaCreacion($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_creacion !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_creacion !== null && $tmpDt = new DateTime($this->fecha_creacion)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_creacion = $newDateAsString;
                $this->modifiedColumns[] = PqrPeer::FECHA_CREACION;
            }
        } // if either are not null


        return $this;
    } // setFechaCreacion()

    /**
     * Set the value of [nombre_solicitante] column.
     * 
     * @param  string $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setNombreSolicitante($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nombre_solicitante !== $v) {
            $this->nombre_solicitante = $v;
            $this->modifiedColumns[] = PqrPeer::NOMBRE_SOLICITANTE;
        }


        return $this;
    } // setNombreSolicitante()

    /**
     * Set the value of [email] column.
     * 
     * @param  string $v new value
     * @return Pqr The current object (for fluent API support)
     */
    public function setEmail($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->email !== $v) {
            $this->email = $v;
            $this->modifiedColumns[] = PqrPeer::EMAIL;
        }


        return $this;
    } // setEmail()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->pqr_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->prioridadpqr_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->categoriapqr_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->usuario_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->estadopqr_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->objeto = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->contenido = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->ruta = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->fecha_creacion = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->nombre_solicitante = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
            $this->email = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 11; // 11 = PqrPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Pqr object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aPrioridadPqr !== null && $this->prioridadpqr_id !== $this->aPrioridadPqr->getPrioridadpqrId()) {
            $this->aPrioridadPqr = null;
        }
        if ($this->aCategoriaPqr !== null && $this->categoriapqr_id !== $this->aCategoriaPqr->getCategoriapqrId()) {
            $this->aCategoriaPqr = null;
        }
        if ($this->aUsuario !== null && $this->usuario_id !== $this->aUsuario->getUsuarioId()) {
            $this->aUsuario = null;
        }
        if ($this->aEstadoPqr !== null && $this->estadopqr_id !== $this->aEstadoPqr->getEstadopqrId()) {
            $this->aEstadoPqr = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(PqrPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = PqrPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aPrioridadPqr = null;
            $this->aCategoriaPqr = null;
            $this->aUsuario = null;
            $this->aEstadoPqr = null;
            $this->collNotaPqrs = null;

            $this->collAsignacionPqrs = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(PqrPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = PqrQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasePqr:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasePqr:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(PqrPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BasePqr:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BasePqr:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                PqrPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aPrioridadPqr !== null) {
                if ($this->aPrioridadPqr->isModified() || $this->aPrioridadPqr->isNew()) {
                    $affectedRows += $this->aPrioridadPqr->save($con);
                }
                $this->setPrioridadPqr($this->aPrioridadPqr);
            }

            if ($this->aCategoriaPqr !== null) {
                if ($this->aCategoriaPqr->isModified() || $this->aCategoriaPqr->isNew()) {
                    $affectedRows += $this->aCategoriaPqr->save($con);
                }
                $this->setCategoriaPqr($this->aCategoriaPqr);
            }

            if ($this->aUsuario !== null) {
                if ($this->aUsuario->isModified() || $this->aUsuario->isNew()) {
                    $affectedRows += $this->aUsuario->save($con);
                }
                $this->setUsuario($this->aUsuario);
            }

            if ($this->aEstadoPqr !== null) {
                if ($this->aEstadoPqr->isModified() || $this->aEstadoPqr->isNew()) {
                    $affectedRows += $this->aEstadoPqr->save($con);
                }
                $this->setEstadoPqr($this->aEstadoPqr);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->notaPqrsScheduledForDeletion !== null) {
                if (!$this->notaPqrsScheduledForDeletion->isEmpty()) {
                    NotaPqrQuery::create()
                        ->filterByPrimaryKeys($this->notaPqrsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->notaPqrsScheduledForDeletion = null;
                }
            }

            if ($this->collNotaPqrs !== null) {
                foreach ($this->collNotaPqrs as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->asignacionPqrsScheduledForDeletion !== null) {
                if (!$this->asignacionPqrsScheduledForDeletion->isEmpty()) {
                    AsignacionPqrQuery::create()
                        ->filterByPrimaryKeys($this->asignacionPqrsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->asignacionPqrsScheduledForDeletion = null;
                }
            }

            if ($this->collAsignacionPqrs !== null) {
                foreach ($this->collAsignacionPqrs as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = PqrPeer::PQR_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(PqrPeer::PQR_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . PqrPeer::PQR_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(PqrPeer::PQR_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setPqrId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aPrioridadPqr !== null) {
                if (!$this->aPrioridadPqr->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aPrioridadPqr->getValidationFailures());
                }
            }

            if ($this->aCategoriaPqr !== null) {
                if (!$this->aCategoriaPqr->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aCategoriaPqr->getValidationFailures());
                }
            }

            if ($this->aUsuario !== null) {
                if (!$this->aUsuario->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aUsuario->getValidationFailures());
                }
            }

            if ($this->aEstadoPqr !== null) {
                if (!$this->aEstadoPqr->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEstadoPqr->getValidationFailures());
                }
            }


            if (($retval = PqrPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collNotaPqrs !== null) {
                    foreach ($this->collNotaPqrs as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collAsignacionPqrs !== null) {
                    foreach ($this->collAsignacionPqrs as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = PqrPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getPqrId();
                break;
            case 1:
                return $this->getPrioridadpqrId();
                break;
            case 2:
                return $this->getCategoriapqrId();
                break;
            case 3:
                return $this->getUsuarioId();
                break;
            case 4:
                return $this->getEstadopqrId();
                break;
            case 5:
                return $this->getObjeto();
                break;
            case 6:
                return $this->getContenido();
                break;
            case 7:
                return $this->getRuta();
                break;
            case 8:
                return $this->getFechaCreacion();
                break;
            case 9:
                return $this->getNombreSolicitante();
                break;
            case 10:
                return $this->getEmail();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Pqr'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Pqr'][$this->getPrimaryKey()] = true;
        $keys = PqrPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getPqrId(),
            $keys[1] => $this->getPrioridadpqrId(),
            $keys[2] => $this->getCategoriapqrId(),
            $keys[3] => $this->getUsuarioId(),
            $keys[4] => $this->getEstadopqrId(),
            $keys[5] => $this->getObjeto(),
            $keys[6] => $this->getContenido(),
            $keys[7] => $this->getRuta(),
            $keys[8] => $this->getFechaCreacion(),
            $keys[9] => $this->getNombreSolicitante(),
            $keys[10] => $this->getEmail(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aPrioridadPqr) {
                $result['PrioridadPqr'] = $this->aPrioridadPqr->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aCategoriaPqr) {
                $result['CategoriaPqr'] = $this->aCategoriaPqr->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aUsuario) {
                $result['Usuario'] = $this->aUsuario->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aEstadoPqr) {
                $result['EstadoPqr'] = $this->aEstadoPqr->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collNotaPqrs) {
                $result['NotaPqrs'] = $this->collNotaPqrs->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collAsignacionPqrs) {
                $result['AsignacionPqrs'] = $this->collAsignacionPqrs->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = PqrPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setPqrId($value);
                break;
            case 1:
                $this->setPrioridadpqrId($value);
                break;
            case 2:
                $this->setCategoriapqrId($value);
                break;
            case 3:
                $this->setUsuarioId($value);
                break;
            case 4:
                $this->setEstadopqrId($value);
                break;
            case 5:
                $this->setObjeto($value);
                break;
            case 6:
                $this->setContenido($value);
                break;
            case 7:
                $this->setRuta($value);
                break;
            case 8:
                $this->setFechaCreacion($value);
                break;
            case 9:
                $this->setNombreSolicitante($value);
                break;
            case 10:
                $this->setEmail($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = PqrPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setPqrId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setPrioridadpqrId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setCategoriapqrId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setUsuarioId($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setEstadopqrId($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setObjeto($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setContenido($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setRuta($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setFechaCreacion($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setNombreSolicitante($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setEmail($arr[$keys[10]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(PqrPeer::DATABASE_NAME);

        if ($this->isColumnModified(PqrPeer::PQR_ID)) $criteria->add(PqrPeer::PQR_ID, $this->pqr_id);
        if ($this->isColumnModified(PqrPeer::PRIORIDADPQR_ID)) $criteria->add(PqrPeer::PRIORIDADPQR_ID, $this->prioridadpqr_id);
        if ($this->isColumnModified(PqrPeer::CATEGORIAPQR_ID)) $criteria->add(PqrPeer::CATEGORIAPQR_ID, $this->categoriapqr_id);
        if ($this->isColumnModified(PqrPeer::USUARIO_ID)) $criteria->add(PqrPeer::USUARIO_ID, $this->usuario_id);
        if ($this->isColumnModified(PqrPeer::ESTADOPQR_ID)) $criteria->add(PqrPeer::ESTADOPQR_ID, $this->estadopqr_id);
        if ($this->isColumnModified(PqrPeer::OBJETO)) $criteria->add(PqrPeer::OBJETO, $this->objeto);
        if ($this->isColumnModified(PqrPeer::CONTENIDO)) $criteria->add(PqrPeer::CONTENIDO, $this->contenido);
        if ($this->isColumnModified(PqrPeer::RUTA)) $criteria->add(PqrPeer::RUTA, $this->ruta);
        if ($this->isColumnModified(PqrPeer::FECHA_CREACION)) $criteria->add(PqrPeer::FECHA_CREACION, $this->fecha_creacion);
        if ($this->isColumnModified(PqrPeer::NOMBRE_SOLICITANTE)) $criteria->add(PqrPeer::NOMBRE_SOLICITANTE, $this->nombre_solicitante);
        if ($this->isColumnModified(PqrPeer::EMAIL)) $criteria->add(PqrPeer::EMAIL, $this->email);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(PqrPeer::DATABASE_NAME);
        $criteria->add(PqrPeer::PQR_ID, $this->pqr_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getPqrId();
    }

    /**
     * Generic method to set the primary key (pqr_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setPqrId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getPqrId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Pqr (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setPrioridadpqrId($this->getPrioridadpqrId());
        $copyObj->setCategoriapqrId($this->getCategoriapqrId());
        $copyObj->setUsuarioId($this->getUsuarioId());
        $copyObj->setEstadopqrId($this->getEstadopqrId());
        $copyObj->setObjeto($this->getObjeto());
        $copyObj->setContenido($this->getContenido());
        $copyObj->setRuta($this->getRuta());
        $copyObj->setFechaCreacion($this->getFechaCreacion());
        $copyObj->setNombreSolicitante($this->getNombreSolicitante());
        $copyObj->setEmail($this->getEmail());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getNotaPqrs() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addNotaPqr($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getAsignacionPqrs() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addAsignacionPqr($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setPqrId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Pqr Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return PqrPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new PqrPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a PrioridadPqr object.
     *
     * @param                  PrioridadPqr $v
     * @return Pqr The current object (for fluent API support)
     * @throws PropelException
     */
    public function setPrioridadPqr(PrioridadPqr $v = null)
    {
        if ($v === null) {
            $this->setPrioridadpqrId(NULL);
        } else {
            $this->setPrioridadpqrId($v->getPrioridadpqrId());
        }

        $this->aPrioridadPqr = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the PrioridadPqr object, it will not be re-added.
        if ($v !== null) {
            $v->addPqr($this);
        }


        return $this;
    }


    /**
     * Get the associated PrioridadPqr object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return PrioridadPqr The associated PrioridadPqr object.
     * @throws PropelException
     */
    public function getPrioridadPqr(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aPrioridadPqr === null && ($this->prioridadpqr_id !== null) && $doQuery) {
            $this->aPrioridadPqr = PrioridadPqrQuery::create()->findPk($this->prioridadpqr_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aPrioridadPqr->addPqrs($this);
             */
        }

        return $this->aPrioridadPqr;
    }

    /**
     * Declares an association between this object and a CategoriaPqr object.
     *
     * @param                  CategoriaPqr $v
     * @return Pqr The current object (for fluent API support)
     * @throws PropelException
     */
    public function setCategoriaPqr(CategoriaPqr $v = null)
    {
        if ($v === null) {
            $this->setCategoriapqrId(NULL);
        } else {
            $this->setCategoriapqrId($v->getCategoriapqrId());
        }

        $this->aCategoriaPqr = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the CategoriaPqr object, it will not be re-added.
        if ($v !== null) {
            $v->addPqr($this);
        }


        return $this;
    }


    /**
     * Get the associated CategoriaPqr object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return CategoriaPqr The associated CategoriaPqr object.
     * @throws PropelException
     */
    public function getCategoriaPqr(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aCategoriaPqr === null && ($this->categoriapqr_id !== null) && $doQuery) {
            $this->aCategoriaPqr = CategoriaPqrQuery::create()->findPk($this->categoriapqr_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aCategoriaPqr->addPqrs($this);
             */
        }

        return $this->aCategoriaPqr;
    }

    /**
     * Declares an association between this object and a Usuario object.
     *
     * @param                  Usuario $v
     * @return Pqr The current object (for fluent API support)
     * @throws PropelException
     */
    public function setUsuario(Usuario $v = null)
    {
        if ($v === null) {
            $this->setUsuarioId(NULL);
        } else {
            $this->setUsuarioId($v->getUsuarioId());
        }

        $this->aUsuario = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Usuario object, it will not be re-added.
        if ($v !== null) {
            $v->addPqr($this);
        }


        return $this;
    }


    /**
     * Get the associated Usuario object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Usuario The associated Usuario object.
     * @throws PropelException
     */
    public function getUsuario(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aUsuario === null && ($this->usuario_id !== null) && $doQuery) {
            $this->aUsuario = UsuarioQuery::create()->findPk($this->usuario_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aUsuario->addPqrs($this);
             */
        }

        return $this->aUsuario;
    }

    /**
     * Declares an association between this object and a EstadoPqr object.
     *
     * @param                  EstadoPqr $v
     * @return Pqr The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEstadoPqr(EstadoPqr $v = null)
    {
        if ($v === null) {
            $this->setEstadopqrId(NULL);
        } else {
            $this->setEstadopqrId($v->getEstadopqrId());
        }

        $this->aEstadoPqr = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the EstadoPqr object, it will not be re-added.
        if ($v !== null) {
            $v->addPqr($this);
        }


        return $this;
    }


    /**
     * Get the associated EstadoPqr object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return EstadoPqr The associated EstadoPqr object.
     * @throws PropelException
     */
    public function getEstadoPqr(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEstadoPqr === null && ($this->estadopqr_id !== null) && $doQuery) {
            $this->aEstadoPqr = EstadoPqrQuery::create()->findPk($this->estadopqr_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEstadoPqr->addPqrs($this);
             */
        }

        return $this->aEstadoPqr;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('NotaPqr' == $relationName) {
            $this->initNotaPqrs();
        }
        if ('AsignacionPqr' == $relationName) {
            $this->initAsignacionPqrs();
        }
    }

    /**
     * Clears out the collNotaPqrs collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Pqr The current object (for fluent API support)
     * @see        addNotaPqrs()
     */
    public function clearNotaPqrs()
    {
        $this->collNotaPqrs = null; // important to set this to null since that means it is uninitialized
        $this->collNotaPqrsPartial = null;

        return $this;
    }

    /**
     * reset is the collNotaPqrs collection loaded partially
     *
     * @return void
     */
    public function resetPartialNotaPqrs($v = true)
    {
        $this->collNotaPqrsPartial = $v;
    }

    /**
     * Initializes the collNotaPqrs collection.
     *
     * By default this just sets the collNotaPqrs collection to an empty array (like clearcollNotaPqrs());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initNotaPqrs($overrideExisting = true)
    {
        if (null !== $this->collNotaPqrs && !$overrideExisting) {
            return;
        }
        $this->collNotaPqrs = new PropelObjectCollection();
        $this->collNotaPqrs->setModel('NotaPqr');
    }

    /**
     * Gets an array of NotaPqr objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Pqr is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|NotaPqr[] List of NotaPqr objects
     * @throws PropelException
     */
    public function getNotaPqrs($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collNotaPqrsPartial && !$this->isNew();
        if (null === $this->collNotaPqrs || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collNotaPqrs) {
                // return empty collection
                $this->initNotaPqrs();
            } else {
                $collNotaPqrs = NotaPqrQuery::create(null, $criteria)
                    ->filterByPqr($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collNotaPqrsPartial && count($collNotaPqrs)) {
                      $this->initNotaPqrs(false);

                      foreach ($collNotaPqrs as $obj) {
                        if (false == $this->collNotaPqrs->contains($obj)) {
                          $this->collNotaPqrs->append($obj);
                        }
                      }

                      $this->collNotaPqrsPartial = true;
                    }

                    $collNotaPqrs->getInternalIterator()->rewind();

                    return $collNotaPqrs;
                }

                if ($partial && $this->collNotaPqrs) {
                    foreach ($this->collNotaPqrs as $obj) {
                        if ($obj->isNew()) {
                            $collNotaPqrs[] = $obj;
                        }
                    }
                }

                $this->collNotaPqrs = $collNotaPqrs;
                $this->collNotaPqrsPartial = false;
            }
        }

        return $this->collNotaPqrs;
    }

    /**
     * Sets a collection of NotaPqr objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $notaPqrs A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Pqr The current object (for fluent API support)
     */
    public function setNotaPqrs(PropelCollection $notaPqrs, PropelPDO $con = null)
    {
        $notaPqrsToDelete = $this->getNotaPqrs(new Criteria(), $con)->diff($notaPqrs);


        $this->notaPqrsScheduledForDeletion = $notaPqrsToDelete;

        foreach ($notaPqrsToDelete as $notaPqrRemoved) {
            $notaPqrRemoved->setPqr(null);
        }

        $this->collNotaPqrs = null;
        foreach ($notaPqrs as $notaPqr) {
            $this->addNotaPqr($notaPqr);
        }

        $this->collNotaPqrs = $notaPqrs;
        $this->collNotaPqrsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related NotaPqr objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related NotaPqr objects.
     * @throws PropelException
     */
    public function countNotaPqrs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collNotaPqrsPartial && !$this->isNew();
        if (null === $this->collNotaPqrs || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collNotaPqrs) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getNotaPqrs());
            }
            $query = NotaPqrQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByPqr($this)
                ->count($con);
        }

        return count($this->collNotaPqrs);
    }

    /**
     * Method called to associate a NotaPqr object to this object
     * through the NotaPqr foreign key attribute.
     *
     * @param    NotaPqr $l NotaPqr
     * @return Pqr The current object (for fluent API support)
     */
    public function addNotaPqr(NotaPqr $l)
    {
        if ($this->collNotaPqrs === null) {
            $this->initNotaPqrs();
            $this->collNotaPqrsPartial = true;
        }

        if (!in_array($l, $this->collNotaPqrs->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddNotaPqr($l);

            if ($this->notaPqrsScheduledForDeletion and $this->notaPqrsScheduledForDeletion->contains($l)) {
                $this->notaPqrsScheduledForDeletion->remove($this->notaPqrsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	NotaPqr $notaPqr The notaPqr object to add.
     */
    protected function doAddNotaPqr($notaPqr)
    {
        $this->collNotaPqrs[]= $notaPqr;
        $notaPqr->setPqr($this);
    }

    /**
     * @param	NotaPqr $notaPqr The notaPqr object to remove.
     * @return Pqr The current object (for fluent API support)
     */
    public function removeNotaPqr($notaPqr)
    {
        if ($this->getNotaPqrs()->contains($notaPqr)) {
            $this->collNotaPqrs->remove($this->collNotaPqrs->search($notaPqr));
            if (null === $this->notaPqrsScheduledForDeletion) {
                $this->notaPqrsScheduledForDeletion = clone $this->collNotaPqrs;
                $this->notaPqrsScheduledForDeletion->clear();
            }
            $this->notaPqrsScheduledForDeletion[]= clone $notaPqr;
            $notaPqr->setPqr(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Pqr is new, it will return
     * an empty collection; or if this Pqr has previously
     * been saved, it will retrieve related NotaPqrs from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Pqr.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|NotaPqr[] List of NotaPqr objects
     */
    public function getNotaPqrsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = NotaPqrQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getNotaPqrs($query, $con);
    }

    /**
     * Clears out the collAsignacionPqrs collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Pqr The current object (for fluent API support)
     * @see        addAsignacionPqrs()
     */
    public function clearAsignacionPqrs()
    {
        $this->collAsignacionPqrs = null; // important to set this to null since that means it is uninitialized
        $this->collAsignacionPqrsPartial = null;

        return $this;
    }

    /**
     * reset is the collAsignacionPqrs collection loaded partially
     *
     * @return void
     */
    public function resetPartialAsignacionPqrs($v = true)
    {
        $this->collAsignacionPqrsPartial = $v;
    }

    /**
     * Initializes the collAsignacionPqrs collection.
     *
     * By default this just sets the collAsignacionPqrs collection to an empty array (like clearcollAsignacionPqrs());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initAsignacionPqrs($overrideExisting = true)
    {
        if (null !== $this->collAsignacionPqrs && !$overrideExisting) {
            return;
        }
        $this->collAsignacionPqrs = new PropelObjectCollection();
        $this->collAsignacionPqrs->setModel('AsignacionPqr');
    }

    /**
     * Gets an array of AsignacionPqr objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Pqr is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|AsignacionPqr[] List of AsignacionPqr objects
     * @throws PropelException
     */
    public function getAsignacionPqrs($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collAsignacionPqrsPartial && !$this->isNew();
        if (null === $this->collAsignacionPqrs || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collAsignacionPqrs) {
                // return empty collection
                $this->initAsignacionPqrs();
            } else {
                $collAsignacionPqrs = AsignacionPqrQuery::create(null, $criteria)
                    ->filterByPqr($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collAsignacionPqrsPartial && count($collAsignacionPqrs)) {
                      $this->initAsignacionPqrs(false);

                      foreach ($collAsignacionPqrs as $obj) {
                        if (false == $this->collAsignacionPqrs->contains($obj)) {
                          $this->collAsignacionPqrs->append($obj);
                        }
                      }

                      $this->collAsignacionPqrsPartial = true;
                    }

                    $collAsignacionPqrs->getInternalIterator()->rewind();

                    return $collAsignacionPqrs;
                }

                if ($partial && $this->collAsignacionPqrs) {
                    foreach ($this->collAsignacionPqrs as $obj) {
                        if ($obj->isNew()) {
                            $collAsignacionPqrs[] = $obj;
                        }
                    }
                }

                $this->collAsignacionPqrs = $collAsignacionPqrs;
                $this->collAsignacionPqrsPartial = false;
            }
        }

        return $this->collAsignacionPqrs;
    }

    /**
     * Sets a collection of AsignacionPqr objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $asignacionPqrs A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Pqr The current object (for fluent API support)
     */
    public function setAsignacionPqrs(PropelCollection $asignacionPqrs, PropelPDO $con = null)
    {
        $asignacionPqrsToDelete = $this->getAsignacionPqrs(new Criteria(), $con)->diff($asignacionPqrs);


        $this->asignacionPqrsScheduledForDeletion = $asignacionPqrsToDelete;

        foreach ($asignacionPqrsToDelete as $asignacionPqrRemoved) {
            $asignacionPqrRemoved->setPqr(null);
        }

        $this->collAsignacionPqrs = null;
        foreach ($asignacionPqrs as $asignacionPqr) {
            $this->addAsignacionPqr($asignacionPqr);
        }

        $this->collAsignacionPqrs = $asignacionPqrs;
        $this->collAsignacionPqrsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related AsignacionPqr objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related AsignacionPqr objects.
     * @throws PropelException
     */
    public function countAsignacionPqrs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collAsignacionPqrsPartial && !$this->isNew();
        if (null === $this->collAsignacionPqrs || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collAsignacionPqrs) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getAsignacionPqrs());
            }
            $query = AsignacionPqrQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByPqr($this)
                ->count($con);
        }

        return count($this->collAsignacionPqrs);
    }

    /**
     * Method called to associate a AsignacionPqr object to this object
     * through the AsignacionPqr foreign key attribute.
     *
     * @param    AsignacionPqr $l AsignacionPqr
     * @return Pqr The current object (for fluent API support)
     */
    public function addAsignacionPqr(AsignacionPqr $l)
    {
        if ($this->collAsignacionPqrs === null) {
            $this->initAsignacionPqrs();
            $this->collAsignacionPqrsPartial = true;
        }

        if (!in_array($l, $this->collAsignacionPqrs->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddAsignacionPqr($l);

            if ($this->asignacionPqrsScheduledForDeletion and $this->asignacionPqrsScheduledForDeletion->contains($l)) {
                $this->asignacionPqrsScheduledForDeletion->remove($this->asignacionPqrsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	AsignacionPqr $asignacionPqr The asignacionPqr object to add.
     */
    protected function doAddAsignacionPqr($asignacionPqr)
    {
        $this->collAsignacionPqrs[]= $asignacionPqr;
        $asignacionPqr->setPqr($this);
    }

    /**
     * @param	AsignacionPqr $asignacionPqr The asignacionPqr object to remove.
     * @return Pqr The current object (for fluent API support)
     */
    public function removeAsignacionPqr($asignacionPqr)
    {
        if ($this->getAsignacionPqrs()->contains($asignacionPqr)) {
            $this->collAsignacionPqrs->remove($this->collAsignacionPqrs->search($asignacionPqr));
            if (null === $this->asignacionPqrsScheduledForDeletion) {
                $this->asignacionPqrsScheduledForDeletion = clone $this->collAsignacionPqrs;
                $this->asignacionPqrsScheduledForDeletion->clear();
            }
            $this->asignacionPqrsScheduledForDeletion[]= clone $asignacionPqr;
            $asignacionPqr->setPqr(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Pqr is new, it will return
     * an empty collection; or if this Pqr has previously
     * been saved, it will retrieve related AsignacionPqrs from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Pqr.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|AsignacionPqr[] List of AsignacionPqr objects
     */
    public function getAsignacionPqrsJoinEstadoAsignacionPqr($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = AsignacionPqrQuery::create(null, $criteria);
        $query->joinWith('EstadoAsignacionPqr', $join_behavior);

        return $this->getAsignacionPqrs($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Pqr is new, it will return
     * an empty collection; or if this Pqr has previously
     * been saved, it will retrieve related AsignacionPqrs from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Pqr.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|AsignacionPqr[] List of AsignacionPqr objects
     */
    public function getAsignacionPqrsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = AsignacionPqrQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getAsignacionPqrs($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->pqr_id = null;
        $this->prioridadpqr_id = null;
        $this->categoriapqr_id = null;
        $this->usuario_id = null;
        $this->estadopqr_id = null;
        $this->objeto = null;
        $this->contenido = null;
        $this->ruta = null;
        $this->fecha_creacion = null;
        $this->nombre_solicitante = null;
        $this->email = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collNotaPqrs) {
                foreach ($this->collNotaPqrs as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collAsignacionPqrs) {
                foreach ($this->collAsignacionPqrs as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aPrioridadPqr instanceof Persistent) {
              $this->aPrioridadPqr->clearAllReferences($deep);
            }
            if ($this->aCategoriaPqr instanceof Persistent) {
              $this->aCategoriaPqr->clearAllReferences($deep);
            }
            if ($this->aUsuario instanceof Persistent) {
              $this->aUsuario->clearAllReferences($deep);
            }
            if ($this->aEstadoPqr instanceof Persistent) {
              $this->aEstadoPqr->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collNotaPqrs instanceof PropelCollection) {
            $this->collNotaPqrs->clearIterator();
        }
        $this->collNotaPqrs = null;
        if ($this->collAsignacionPqrs instanceof PropelCollection) {
            $this->collAsignacionPqrs->clearIterator();
        }
        $this->collAsignacionPqrs = null;
        $this->aPrioridadPqr = null;
        $this->aCategoriaPqr = null;
        $this->aUsuario = null;
        $this->aEstadoPqr = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(PqrPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BasePqr:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

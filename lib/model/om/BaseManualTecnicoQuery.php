<?php


/**
 * Base class that represents a query for the 'MANUAL_TECNICO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:38
 *
 * @method ManualTecnicoQuery orderByManualtecnicoId($order = Criteria::ASC) Order by the MANUALTECNICO_ID column
 * @method ManualTecnicoQuery orderByEspecialidadId($order = Criteria::ASC) Order by the ESPECIALIDAD_ID column
 * @method ManualTecnicoQuery orderByDocumentacionId($order = Criteria::ASC) Order by the DOCUMENTACION_ID column
 * @method ManualTecnicoQuery orderByMencionEdicion($order = Criteria::ASC) Order by the MENCION_EDICION column
 * @method ManualTecnicoQuery orderByVersion($order = Criteria::ASC) Order by the VERSION column
 * @method ManualTecnicoQuery orderByPaginacion($order = Criteria::ASC) Order by the PAGINACION column
 * @method ManualTecnicoQuery orderByIlustraciones($order = Criteria::ASC) Order by the ILUSTRACIONES column
 * @method ManualTecnicoQuery orderByVolumen($order = Criteria::ASC) Order by the VOLUMEN column
 * @method ManualTecnicoQuery orderByMaterialComplemetario($order = Criteria::ASC) Order by the MATERIAL_COMPLEMETARIO column
 * @method ManualTecnicoQuery orderByIsbn($order = Criteria::ASC) Order by the ISBN column
 * @method ManualTecnicoQuery orderByNotasContenido($order = Criteria::ASC) Order by the NOTAS_CONTENIDO column
 *
 * @method ManualTecnicoQuery groupByManualtecnicoId() Group by the MANUALTECNICO_ID column
 * @method ManualTecnicoQuery groupByEspecialidadId() Group by the ESPECIALIDAD_ID column
 * @method ManualTecnicoQuery groupByDocumentacionId() Group by the DOCUMENTACION_ID column
 * @method ManualTecnicoQuery groupByMencionEdicion() Group by the MENCION_EDICION column
 * @method ManualTecnicoQuery groupByVersion() Group by the VERSION column
 * @method ManualTecnicoQuery groupByPaginacion() Group by the PAGINACION column
 * @method ManualTecnicoQuery groupByIlustraciones() Group by the ILUSTRACIONES column
 * @method ManualTecnicoQuery groupByVolumen() Group by the VOLUMEN column
 * @method ManualTecnicoQuery groupByMaterialComplemetario() Group by the MATERIAL_COMPLEMETARIO column
 * @method ManualTecnicoQuery groupByIsbn() Group by the ISBN column
 * @method ManualTecnicoQuery groupByNotasContenido() Group by the NOTAS_CONTENIDO column
 *
 * @method ManualTecnicoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ManualTecnicoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ManualTecnicoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ManualTecnicoQuery leftJoinEspecialidad($relationAlias = null) Adds a LEFT JOIN clause to the query using the Especialidad relation
 * @method ManualTecnicoQuery rightJoinEspecialidad($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Especialidad relation
 * @method ManualTecnicoQuery innerJoinEspecialidad($relationAlias = null) Adds a INNER JOIN clause to the query using the Especialidad relation
 *
 * @method ManualTecnicoQuery leftJoinDocumentacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Documentacion relation
 * @method ManualTecnicoQuery rightJoinDocumentacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Documentacion relation
 * @method ManualTecnicoQuery innerJoinDocumentacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Documentacion relation
 *
 * @method ManualTecnico findOne(PropelPDO $con = null) Return the first ManualTecnico matching the query
 * @method ManualTecnico findOneOrCreate(PropelPDO $con = null) Return the first ManualTecnico matching the query, or a new ManualTecnico object populated from the query conditions when no match is found
 *
 * @method ManualTecnico findOneByEspecialidadId(int $ESPECIALIDAD_ID) Return the first ManualTecnico filtered by the ESPECIALIDAD_ID column
 * @method ManualTecnico findOneByDocumentacionId(int $DOCUMENTACION_ID) Return the first ManualTecnico filtered by the DOCUMENTACION_ID column
 * @method ManualTecnico findOneByMencionEdicion(string $MENCION_EDICION) Return the first ManualTecnico filtered by the MENCION_EDICION column
 * @method ManualTecnico findOneByVersion(double $VERSION) Return the first ManualTecnico filtered by the VERSION column
 * @method ManualTecnico findOneByPaginacion(int $PAGINACION) Return the first ManualTecnico filtered by the PAGINACION column
 * @method ManualTecnico findOneByIlustraciones(boolean $ILUSTRACIONES) Return the first ManualTecnico filtered by the ILUSTRACIONES column
 * @method ManualTecnico findOneByVolumen(double $VOLUMEN) Return the first ManualTecnico filtered by the VOLUMEN column
 * @method ManualTecnico findOneByMaterialComplemetario(string $MATERIAL_COMPLEMETARIO) Return the first ManualTecnico filtered by the MATERIAL_COMPLEMETARIO column
 * @method ManualTecnico findOneByIsbn(string $ISBN) Return the first ManualTecnico filtered by the ISBN column
 * @method ManualTecnico findOneByNotasContenido(string $NOTAS_CONTENIDO) Return the first ManualTecnico filtered by the NOTAS_CONTENIDO column
 *
 * @method array findByManualtecnicoId(int $MANUALTECNICO_ID) Return ManualTecnico objects filtered by the MANUALTECNICO_ID column
 * @method array findByEspecialidadId(int $ESPECIALIDAD_ID) Return ManualTecnico objects filtered by the ESPECIALIDAD_ID column
 * @method array findByDocumentacionId(int $DOCUMENTACION_ID) Return ManualTecnico objects filtered by the DOCUMENTACION_ID column
 * @method array findByMencionEdicion(string $MENCION_EDICION) Return ManualTecnico objects filtered by the MENCION_EDICION column
 * @method array findByVersion(double $VERSION) Return ManualTecnico objects filtered by the VERSION column
 * @method array findByPaginacion(int $PAGINACION) Return ManualTecnico objects filtered by the PAGINACION column
 * @method array findByIlustraciones(boolean $ILUSTRACIONES) Return ManualTecnico objects filtered by the ILUSTRACIONES column
 * @method array findByVolumen(double $VOLUMEN) Return ManualTecnico objects filtered by the VOLUMEN column
 * @method array findByMaterialComplemetario(string $MATERIAL_COMPLEMETARIO) Return ManualTecnico objects filtered by the MATERIAL_COMPLEMETARIO column
 * @method array findByIsbn(string $ISBN) Return ManualTecnico objects filtered by the ISBN column
 * @method array findByNotasContenido(string $NOTAS_CONTENIDO) Return ManualTecnico objects filtered by the NOTAS_CONTENIDO column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseManualTecnicoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseManualTecnicoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'ManualTecnico';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ManualTecnicoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   ManualTecnicoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ManualTecnicoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ManualTecnicoQuery) {
            return $criteria;
        }
        $query = new ManualTecnicoQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   ManualTecnico|ManualTecnico[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ManualTecnicoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ManualTecnicoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ManualTecnico A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByManualtecnicoId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ManualTecnico A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [MANUALTECNICO_ID], [ESPECIALIDAD_ID], [DOCUMENTACION_ID], [MENCION_EDICION], [VERSION], [PAGINACION], [ILUSTRACIONES], [VOLUMEN], [MATERIAL_COMPLEMETARIO], [ISBN], [NOTAS_CONTENIDO] FROM [MANUAL_TECNICO] WHERE [MANUALTECNICO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new ManualTecnico();
            $obj->hydrate($row);
            ManualTecnicoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return ManualTecnico|ManualTecnico[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|ManualTecnico[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ManualTecnicoPeer::MANUALTECNICO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ManualTecnicoPeer::MANUALTECNICO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the MANUALTECNICO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByManualtecnicoId(1234); // WHERE MANUALTECNICO_ID = 1234
     * $query->filterByManualtecnicoId(array(12, 34)); // WHERE MANUALTECNICO_ID IN (12, 34)
     * $query->filterByManualtecnicoId(array('min' => 12)); // WHERE MANUALTECNICO_ID >= 12
     * $query->filterByManualtecnicoId(array('max' => 12)); // WHERE MANUALTECNICO_ID <= 12
     * </code>
     *
     * @param     mixed $manualtecnicoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByManualtecnicoId($manualtecnicoId = null, $comparison = null)
    {
        if (is_array($manualtecnicoId)) {
            $useMinMax = false;
            if (isset($manualtecnicoId['min'])) {
                $this->addUsingAlias(ManualTecnicoPeer::MANUALTECNICO_ID, $manualtecnicoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($manualtecnicoId['max'])) {
                $this->addUsingAlias(ManualTecnicoPeer::MANUALTECNICO_ID, $manualtecnicoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::MANUALTECNICO_ID, $manualtecnicoId, $comparison);
    }

    /**
     * Filter the query on the ESPECIALIDAD_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEspecialidadId(1234); // WHERE ESPECIALIDAD_ID = 1234
     * $query->filterByEspecialidadId(array(12, 34)); // WHERE ESPECIALIDAD_ID IN (12, 34)
     * $query->filterByEspecialidadId(array('min' => 12)); // WHERE ESPECIALIDAD_ID >= 12
     * $query->filterByEspecialidadId(array('max' => 12)); // WHERE ESPECIALIDAD_ID <= 12
     * </code>
     *
     * @see       filterByEspecialidad()
     *
     * @param     mixed $especialidadId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByEspecialidadId($especialidadId = null, $comparison = null)
    {
        if (is_array($especialidadId)) {
            $useMinMax = false;
            if (isset($especialidadId['min'])) {
                $this->addUsingAlias(ManualTecnicoPeer::ESPECIALIDAD_ID, $especialidadId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($especialidadId['max'])) {
                $this->addUsingAlias(ManualTecnicoPeer::ESPECIALIDAD_ID, $especialidadId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::ESPECIALIDAD_ID, $especialidadId, $comparison);
    }

    /**
     * Filter the query on the DOCUMENTACION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDocumentacionId(1234); // WHERE DOCUMENTACION_ID = 1234
     * $query->filterByDocumentacionId(array(12, 34)); // WHERE DOCUMENTACION_ID IN (12, 34)
     * $query->filterByDocumentacionId(array('min' => 12)); // WHERE DOCUMENTACION_ID >= 12
     * $query->filterByDocumentacionId(array('max' => 12)); // WHERE DOCUMENTACION_ID <= 12
     * </code>
     *
     * @see       filterByDocumentacion()
     *
     * @param     mixed $documentacionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByDocumentacionId($documentacionId = null, $comparison = null)
    {
        if (is_array($documentacionId)) {
            $useMinMax = false;
            if (isset($documentacionId['min'])) {
                $this->addUsingAlias(ManualTecnicoPeer::DOCUMENTACION_ID, $documentacionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($documentacionId['max'])) {
                $this->addUsingAlias(ManualTecnicoPeer::DOCUMENTACION_ID, $documentacionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::DOCUMENTACION_ID, $documentacionId, $comparison);
    }

    /**
     * Filter the query on the MENCION_EDICION column
     *
     * Example usage:
     * <code>
     * $query->filterByMencionEdicion('fooValue');   // WHERE MENCION_EDICION = 'fooValue'
     * $query->filterByMencionEdicion('%fooValue%'); // WHERE MENCION_EDICION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $mencionEdicion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByMencionEdicion($mencionEdicion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($mencionEdicion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $mencionEdicion)) {
                $mencionEdicion = str_replace('*', '%', $mencionEdicion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::MENCION_EDICION, $mencionEdicion, $comparison);
    }

    /**
     * Filter the query on the VERSION column
     *
     * Example usage:
     * <code>
     * $query->filterByVersion(1234); // WHERE VERSION = 1234
     * $query->filterByVersion(array(12, 34)); // WHERE VERSION IN (12, 34)
     * $query->filterByVersion(array('min' => 12)); // WHERE VERSION >= 12
     * $query->filterByVersion(array('max' => 12)); // WHERE VERSION <= 12
     * </code>
     *
     * @param     mixed $version The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByVersion($version = null, $comparison = null)
    {
        if (is_array($version)) {
            $useMinMax = false;
            if (isset($version['min'])) {
                $this->addUsingAlias(ManualTecnicoPeer::VERSION, $version['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($version['max'])) {
                $this->addUsingAlias(ManualTecnicoPeer::VERSION, $version['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::VERSION, $version, $comparison);
    }

    /**
     * Filter the query on the PAGINACION column
     *
     * Example usage:
     * <code>
     * $query->filterByPaginacion(1234); // WHERE PAGINACION = 1234
     * $query->filterByPaginacion(array(12, 34)); // WHERE PAGINACION IN (12, 34)
     * $query->filterByPaginacion(array('min' => 12)); // WHERE PAGINACION >= 12
     * $query->filterByPaginacion(array('max' => 12)); // WHERE PAGINACION <= 12
     * </code>
     *
     * @param     mixed $paginacion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByPaginacion($paginacion = null, $comparison = null)
    {
        if (is_array($paginacion)) {
            $useMinMax = false;
            if (isset($paginacion['min'])) {
                $this->addUsingAlias(ManualTecnicoPeer::PAGINACION, $paginacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($paginacion['max'])) {
                $this->addUsingAlias(ManualTecnicoPeer::PAGINACION, $paginacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::PAGINACION, $paginacion, $comparison);
    }

    /**
     * Filter the query on the ILUSTRACIONES column
     *
     * Example usage:
     * <code>
     * $query->filterByIlustraciones(true); // WHERE ILUSTRACIONES = true
     * $query->filterByIlustraciones('yes'); // WHERE ILUSTRACIONES = true
     * </code>
     *
     * @param     boolean|string $ilustraciones The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByIlustraciones($ilustraciones = null, $comparison = null)
    {
        if (is_string($ilustraciones)) {
            $ilustraciones = in_array(strtolower($ilustraciones), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ManualTecnicoPeer::ILUSTRACIONES, $ilustraciones, $comparison);
    }

    /**
     * Filter the query on the VOLUMEN column
     *
     * Example usage:
     * <code>
     * $query->filterByVolumen(1234); // WHERE VOLUMEN = 1234
     * $query->filterByVolumen(array(12, 34)); // WHERE VOLUMEN IN (12, 34)
     * $query->filterByVolumen(array('min' => 12)); // WHERE VOLUMEN >= 12
     * $query->filterByVolumen(array('max' => 12)); // WHERE VOLUMEN <= 12
     * </code>
     *
     * @param     mixed $volumen The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByVolumen($volumen = null, $comparison = null)
    {
        if (is_array($volumen)) {
            $useMinMax = false;
            if (isset($volumen['min'])) {
                $this->addUsingAlias(ManualTecnicoPeer::VOLUMEN, $volumen['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($volumen['max'])) {
                $this->addUsingAlias(ManualTecnicoPeer::VOLUMEN, $volumen['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::VOLUMEN, $volumen, $comparison);
    }

    /**
     * Filter the query on the MATERIAL_COMPLEMETARIO column
     *
     * Example usage:
     * <code>
     * $query->filterByMaterialComplemetario('fooValue');   // WHERE MATERIAL_COMPLEMETARIO = 'fooValue'
     * $query->filterByMaterialComplemetario('%fooValue%'); // WHERE MATERIAL_COMPLEMETARIO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $materialComplemetario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByMaterialComplemetario($materialComplemetario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($materialComplemetario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $materialComplemetario)) {
                $materialComplemetario = str_replace('*', '%', $materialComplemetario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::MATERIAL_COMPLEMETARIO, $materialComplemetario, $comparison);
    }

    /**
     * Filter the query on the ISBN column
     *
     * Example usage:
     * <code>
     * $query->filterByIsbn('fooValue');   // WHERE ISBN = 'fooValue'
     * $query->filterByIsbn('%fooValue%'); // WHERE ISBN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $isbn The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByIsbn($isbn = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($isbn)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $isbn)) {
                $isbn = str_replace('*', '%', $isbn);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::ISBN, $isbn, $comparison);
    }

    /**
     * Filter the query on the NOTAS_CONTENIDO column
     *
     * Example usage:
     * <code>
     * $query->filterByNotasContenido('fooValue');   // WHERE NOTAS_CONTENIDO = 'fooValue'
     * $query->filterByNotasContenido('%fooValue%'); // WHERE NOTAS_CONTENIDO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $notasContenido The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function filterByNotasContenido($notasContenido = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($notasContenido)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $notasContenido)) {
                $notasContenido = str_replace('*', '%', $notasContenido);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ManualTecnicoPeer::NOTAS_CONTENIDO, $notasContenido, $comparison);
    }

    /**
     * Filter the query by a related Especialidad object
     *
     * @param   Especialidad|PropelObjectCollection $especialidad The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ManualTecnicoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEspecialidad($especialidad, $comparison = null)
    {
        if ($especialidad instanceof Especialidad) {
            return $this
                ->addUsingAlias(ManualTecnicoPeer::ESPECIALIDAD_ID, $especialidad->getEspecialidadId(), $comparison);
        } elseif ($especialidad instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ManualTecnicoPeer::ESPECIALIDAD_ID, $especialidad->toKeyValue('PrimaryKey', 'EspecialidadId'), $comparison);
        } else {
            throw new PropelException('filterByEspecialidad() only accepts arguments of type Especialidad or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Especialidad relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function joinEspecialidad($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Especialidad');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Especialidad');
        }

        return $this;
    }

    /**
     * Use the Especialidad relation Especialidad object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EspecialidadQuery A secondary query class using the current class as primary query
     */
    public function useEspecialidadQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEspecialidad($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Especialidad', 'EspecialidadQuery');
    }

    /**
     * Filter the query by a related Documentacion object
     *
     * @param   Documentacion|PropelObjectCollection $documentacion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ManualTecnicoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDocumentacion($documentacion, $comparison = null)
    {
        if ($documentacion instanceof Documentacion) {
            return $this
                ->addUsingAlias(ManualTecnicoPeer::DOCUMENTACION_ID, $documentacion->getDocumentacionId(), $comparison);
        } elseif ($documentacion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ManualTecnicoPeer::DOCUMENTACION_ID, $documentacion->toKeyValue('PrimaryKey', 'DocumentacionId'), $comparison);
        } else {
            throw new PropelException('filterByDocumentacion() only accepts arguments of type Documentacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Documentacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function joinDocumentacion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Documentacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Documentacion');
        }

        return $this;
    }

    /**
     * Use the Documentacion relation Documentacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DocumentacionQuery A secondary query class using the current class as primary query
     */
    public function useDocumentacionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinDocumentacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Documentacion', 'DocumentacionQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   ManualTecnico $manualTecnico Object to remove from the list of results
     *
     * @return ManualTecnicoQuery The current query, for fluid interface
     */
    public function prune($manualTecnico = null)
    {
        if ($manualTecnico) {
            $this->addUsingAlias(ManualTecnicoPeer::MANUALTECNICO_ID, $manualTecnico->getManualtecnicoId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

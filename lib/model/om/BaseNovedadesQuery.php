<?php


/**
 * Base class that represents a query for the 'NOVEDADES' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:46
 *
 * @method NovedadesQuery orderByNovedadesId($order = Criteria::ASC) Order by the NOVEDADES_ID column
 * @method NovedadesQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method NovedadesQuery orderByModuloId($order = Criteria::ASC) Order by the MODULO_ID column
 * @method NovedadesQuery orderByTipocontrolId($order = Criteria::ASC) Order by the TIPOCONTROL_ID column
 * @method NovedadesQuery orderByCodigoPrincipal($order = Criteria::ASC) Order by the CODIGO_PRINCIPAL column
 * @method NovedadesQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method NovedadesQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method NovedadesQuery orderByRuta($order = Criteria::ASC) Order by the RUTA column
 * @method NovedadesQuery orderByRemitente($order = Criteria::ASC) Order by the REMITENTE column
 * @method NovedadesQuery orderByDestinatario($order = Criteria::ASC) Order by the DESTINATARIO column
 * @method NovedadesQuery orderByValorFactura($order = Criteria::ASC) Order by the VALOR_FACTURA column
 * @method NovedadesQuery orderByTipoDocumento($order = Criteria::ASC) Order by the TIPO_DOCUMENTO column
 * @method NovedadesQuery orderByGuia($order = Criteria::ASC) Order by the GUIA column
 * @method NovedadesQuery orderByNumeroFactura($order = Criteria::ASC) Order by the NUMERO_FACTURA column
 * @method NovedadesQuery orderByEstadoAprobacion($order = Criteria::ASC) Order by the ESTADO_APROBACION column
 * @method NovedadesQuery orderByObsAprobacion($order = Criteria::ASC) Order by the OBS_APROBACION column
 * @method NovedadesQuery orderByUsuarioAprueba($order = Criteria::ASC) Order by the USUARIO_APRUEBA column
 * @method NovedadesQuery orderByUsuarioDestino($order = Criteria::ASC) Order by the USUARIO_DESTINO column
 * @method NovedadesQuery orderByFechaAprobacion($order = Criteria::ASC) Order by the FECHA_APROBACION column
 *
 * @method NovedadesQuery groupByNovedadesId() Group by the NOVEDADES_ID column
 * @method NovedadesQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method NovedadesQuery groupByModuloId() Group by the MODULO_ID column
 * @method NovedadesQuery groupByTipocontrolId() Group by the TIPOCONTROL_ID column
 * @method NovedadesQuery groupByCodigoPrincipal() Group by the CODIGO_PRINCIPAL column
 * @method NovedadesQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method NovedadesQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method NovedadesQuery groupByRuta() Group by the RUTA column
 * @method NovedadesQuery groupByRemitente() Group by the REMITENTE column
 * @method NovedadesQuery groupByDestinatario() Group by the DESTINATARIO column
 * @method NovedadesQuery groupByValorFactura() Group by the VALOR_FACTURA column
 * @method NovedadesQuery groupByTipoDocumento() Group by the TIPO_DOCUMENTO column
 * @method NovedadesQuery groupByGuia() Group by the GUIA column
 * @method NovedadesQuery groupByNumeroFactura() Group by the NUMERO_FACTURA column
 * @method NovedadesQuery groupByEstadoAprobacion() Group by the ESTADO_APROBACION column
 * @method NovedadesQuery groupByObsAprobacion() Group by the OBS_APROBACION column
 * @method NovedadesQuery groupByUsuarioAprueba() Group by the USUARIO_APRUEBA column
 * @method NovedadesQuery groupByUsuarioDestino() Group by the USUARIO_DESTINO column
 * @method NovedadesQuery groupByFechaAprobacion() Group by the FECHA_APROBACION column
 *
 * @method NovedadesQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method NovedadesQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method NovedadesQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method NovedadesQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method NovedadesQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method NovedadesQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method NovedadesQuery leftJoinModulo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Modulo relation
 * @method NovedadesQuery rightJoinModulo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Modulo relation
 * @method NovedadesQuery innerJoinModulo($relationAlias = null) Adds a INNER JOIN clause to the query using the Modulo relation
 *
 * @method NovedadesQuery leftJoinTipoControl($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoControl relation
 * @method NovedadesQuery rightJoinTipoControl($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoControl relation
 * @method NovedadesQuery innerJoinTipoControl($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoControl relation
 *
 * @method NovedadesQuery leftJoinNovedadesUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the NovedadesUsuario relation
 * @method NovedadesQuery rightJoinNovedadesUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the NovedadesUsuario relation
 * @method NovedadesQuery innerJoinNovedadesUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the NovedadesUsuario relation
 *
 * @method Novedades findOne(PropelPDO $con = null) Return the first Novedades matching the query
 * @method Novedades findOneOrCreate(PropelPDO $con = null) Return the first Novedades matching the query, or a new Novedades object populated from the query conditions when no match is found
 *
 * @method Novedades findOneByUsuarioId(int $USUARIO_ID) Return the first Novedades filtered by the USUARIO_ID column
 * @method Novedades findOneByModuloId(int $MODULO_ID) Return the first Novedades filtered by the MODULO_ID column
 * @method Novedades findOneByTipocontrolId(int $TIPOCONTROL_ID) Return the first Novedades filtered by the TIPOCONTROL_ID column
 * @method Novedades findOneByCodigoPrincipal(int $CODIGO_PRINCIPAL) Return the first Novedades filtered by the CODIGO_PRINCIPAL column
 * @method Novedades findOneByDescripcion(string $DESCRIPCION) Return the first Novedades filtered by the DESCRIPCION column
 * @method Novedades findOneByFechaCreacion(string $FECHA_CREACION) Return the first Novedades filtered by the FECHA_CREACION column
 * @method Novedades findOneByRuta(string $RUTA) Return the first Novedades filtered by the RUTA column
 * @method Novedades findOneByRemitente(string $REMITENTE) Return the first Novedades filtered by the REMITENTE column
 * @method Novedades findOneByDestinatario(string $DESTINATARIO) Return the first Novedades filtered by the DESTINATARIO column
 * @method Novedades findOneByValorFactura(string $VALOR_FACTURA) Return the first Novedades filtered by the VALOR_FACTURA column
 * @method Novedades findOneByTipoDocumento(string $TIPO_DOCUMENTO) Return the first Novedades filtered by the TIPO_DOCUMENTO column
 * @method Novedades findOneByGuia(string $GUIA) Return the first Novedades filtered by the GUIA column
 * @method Novedades findOneByNumeroFactura(string $NUMERO_FACTURA) Return the first Novedades filtered by the NUMERO_FACTURA column
 * @method Novedades findOneByEstadoAprobacion(int $ESTADO_APROBACION) Return the first Novedades filtered by the ESTADO_APROBACION column
 * @method Novedades findOneByObsAprobacion(string $OBS_APROBACION) Return the first Novedades filtered by the OBS_APROBACION column
 * @method Novedades findOneByUsuarioAprueba(int $USUARIO_APRUEBA) Return the first Novedades filtered by the USUARIO_APRUEBA column
 * @method Novedades findOneByUsuarioDestino(int $USUARIO_DESTINO) Return the first Novedades filtered by the USUARIO_DESTINO column
 * @method Novedades findOneByFechaAprobacion(string $FECHA_APROBACION) Return the first Novedades filtered by the FECHA_APROBACION column
 *
 * @method array findByNovedadesId(int $NOVEDADES_ID) Return Novedades objects filtered by the NOVEDADES_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return Novedades objects filtered by the USUARIO_ID column
 * @method array findByModuloId(int $MODULO_ID) Return Novedades objects filtered by the MODULO_ID column
 * @method array findByTipocontrolId(int $TIPOCONTROL_ID) Return Novedades objects filtered by the TIPOCONTROL_ID column
 * @method array findByCodigoPrincipal(int $CODIGO_PRINCIPAL) Return Novedades objects filtered by the CODIGO_PRINCIPAL column
 * @method array findByDescripcion(string $DESCRIPCION) Return Novedades objects filtered by the DESCRIPCION column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return Novedades objects filtered by the FECHA_CREACION column
 * @method array findByRuta(string $RUTA) Return Novedades objects filtered by the RUTA column
 * @method array findByRemitente(string $REMITENTE) Return Novedades objects filtered by the REMITENTE column
 * @method array findByDestinatario(string $DESTINATARIO) Return Novedades objects filtered by the DESTINATARIO column
 * @method array findByValorFactura(string $VALOR_FACTURA) Return Novedades objects filtered by the VALOR_FACTURA column
 * @method array findByTipoDocumento(string $TIPO_DOCUMENTO) Return Novedades objects filtered by the TIPO_DOCUMENTO column
 * @method array findByGuia(string $GUIA) Return Novedades objects filtered by the GUIA column
 * @method array findByNumeroFactura(string $NUMERO_FACTURA) Return Novedades objects filtered by the NUMERO_FACTURA column
 * @method array findByEstadoAprobacion(int $ESTADO_APROBACION) Return Novedades objects filtered by the ESTADO_APROBACION column
 * @method array findByObsAprobacion(string $OBS_APROBACION) Return Novedades objects filtered by the OBS_APROBACION column
 * @method array findByUsuarioAprueba(int $USUARIO_APRUEBA) Return Novedades objects filtered by the USUARIO_APRUEBA column
 * @method array findByUsuarioDestino(int $USUARIO_DESTINO) Return Novedades objects filtered by the USUARIO_DESTINO column
 * @method array findByFechaAprobacion(string $FECHA_APROBACION) Return Novedades objects filtered by the FECHA_APROBACION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseNovedadesQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseNovedadesQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Novedades';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new NovedadesQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   NovedadesQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return NovedadesQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof NovedadesQuery) {
            return $criteria;
        }
        $query = new NovedadesQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Novedades|Novedades[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = NovedadesPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(NovedadesPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Novedades A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByNovedadesId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Novedades A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [NOVEDADES_ID], [USUARIO_ID], [MODULO_ID], [TIPOCONTROL_ID], [CODIGO_PRINCIPAL], [DESCRIPCION], [FECHA_CREACION], [RUTA], [REMITENTE], [DESTINATARIO], [VALOR_FACTURA], [TIPO_DOCUMENTO], [GUIA], [NUMERO_FACTURA], [ESTADO_APROBACION], [OBS_APROBACION], [USUARIO_APRUEBA], [USUARIO_DESTINO], [FECHA_APROBACION] FROM [NOVEDADES] WHERE [NOVEDADES_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Novedades();
            $obj->hydrate($row);
            NovedadesPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Novedades|Novedades[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Novedades[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the NOVEDADES_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByNovedadesId(1234); // WHERE NOVEDADES_ID = 1234
     * $query->filterByNovedadesId(array(12, 34)); // WHERE NOVEDADES_ID IN (12, 34)
     * $query->filterByNovedadesId(array('min' => 12)); // WHERE NOVEDADES_ID >= 12
     * $query->filterByNovedadesId(array('max' => 12)); // WHERE NOVEDADES_ID <= 12
     * </code>
     *
     * @param     mixed $novedadesId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByNovedadesId($novedadesId = null, $comparison = null)
    {
        if (is_array($novedadesId)) {
            $useMinMax = false;
            if (isset($novedadesId['min'])) {
                $this->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $novedadesId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($novedadesId['max'])) {
                $this->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $novedadesId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $novedadesId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(NovedadesPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(NovedadesPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the MODULO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByModuloId(1234); // WHERE MODULO_ID = 1234
     * $query->filterByModuloId(array(12, 34)); // WHERE MODULO_ID IN (12, 34)
     * $query->filterByModuloId(array('min' => 12)); // WHERE MODULO_ID >= 12
     * $query->filterByModuloId(array('max' => 12)); // WHERE MODULO_ID <= 12
     * </code>
     *
     * @see       filterByModulo()
     *
     * @param     mixed $moduloId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByModuloId($moduloId = null, $comparison = null)
    {
        if (is_array($moduloId)) {
            $useMinMax = false;
            if (isset($moduloId['min'])) {
                $this->addUsingAlias(NovedadesPeer::MODULO_ID, $moduloId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($moduloId['max'])) {
                $this->addUsingAlias(NovedadesPeer::MODULO_ID, $moduloId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::MODULO_ID, $moduloId, $comparison);
    }

    /**
     * Filter the query on the TIPOCONTROL_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTipocontrolId(1234); // WHERE TIPOCONTROL_ID = 1234
     * $query->filterByTipocontrolId(array(12, 34)); // WHERE TIPOCONTROL_ID IN (12, 34)
     * $query->filterByTipocontrolId(array('min' => 12)); // WHERE TIPOCONTROL_ID >= 12
     * $query->filterByTipocontrolId(array('max' => 12)); // WHERE TIPOCONTROL_ID <= 12
     * </code>
     *
     * @see       filterByTipoControl()
     *
     * @param     mixed $tipocontrolId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByTipocontrolId($tipocontrolId = null, $comparison = null)
    {
        if (is_array($tipocontrolId)) {
            $useMinMax = false;
            if (isset($tipocontrolId['min'])) {
                $this->addUsingAlias(NovedadesPeer::TIPOCONTROL_ID, $tipocontrolId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipocontrolId['max'])) {
                $this->addUsingAlias(NovedadesPeer::TIPOCONTROL_ID, $tipocontrolId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::TIPOCONTROL_ID, $tipocontrolId, $comparison);
    }

    /**
     * Filter the query on the CODIGO_PRINCIPAL column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoPrincipal(1234); // WHERE CODIGO_PRINCIPAL = 1234
     * $query->filterByCodigoPrincipal(array(12, 34)); // WHERE CODIGO_PRINCIPAL IN (12, 34)
     * $query->filterByCodigoPrincipal(array('min' => 12)); // WHERE CODIGO_PRINCIPAL >= 12
     * $query->filterByCodigoPrincipal(array('max' => 12)); // WHERE CODIGO_PRINCIPAL <= 12
     * </code>
     *
     * @param     mixed $codigoPrincipal The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByCodigoPrincipal($codigoPrincipal = null, $comparison = null)
    {
        if (is_array($codigoPrincipal)) {
            $useMinMax = false;
            if (isset($codigoPrincipal['min'])) {
                $this->addUsingAlias(NovedadesPeer::CODIGO_PRINCIPAL, $codigoPrincipal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($codigoPrincipal['max'])) {
                $this->addUsingAlias(NovedadesPeer::CODIGO_PRINCIPAL, $codigoPrincipal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::CODIGO_PRINCIPAL, $codigoPrincipal, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(NovedadesPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(NovedadesPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the RUTA column
     *
     * Example usage:
     * <code>
     * $query->filterByRuta('fooValue');   // WHERE RUTA = 'fooValue'
     * $query->filterByRuta('%fooValue%'); // WHERE RUTA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $ruta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByRuta($ruta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($ruta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $ruta)) {
                $ruta = str_replace('*', '%', $ruta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::RUTA, $ruta, $comparison);
    }

    /**
     * Filter the query on the REMITENTE column
     *
     * Example usage:
     * <code>
     * $query->filterByRemitente('fooValue');   // WHERE REMITENTE = 'fooValue'
     * $query->filterByRemitente('%fooValue%'); // WHERE REMITENTE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $remitente The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByRemitente($remitente = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($remitente)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $remitente)) {
                $remitente = str_replace('*', '%', $remitente);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::REMITENTE, $remitente, $comparison);
    }

    /**
     * Filter the query on the DESTINATARIO column
     *
     * Example usage:
     * <code>
     * $query->filterByDestinatario('fooValue');   // WHERE DESTINATARIO = 'fooValue'
     * $query->filterByDestinatario('%fooValue%'); // WHERE DESTINATARIO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $destinatario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByDestinatario($destinatario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($destinatario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $destinatario)) {
                $destinatario = str_replace('*', '%', $destinatario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::DESTINATARIO, $destinatario, $comparison);
    }

    /**
     * Filter the query on the VALOR_FACTURA column
     *
     * Example usage:
     * <code>
     * $query->filterByValorFactura('fooValue');   // WHERE VALOR_FACTURA = 'fooValue'
     * $query->filterByValorFactura('%fooValue%'); // WHERE VALOR_FACTURA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $valorFactura The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByValorFactura($valorFactura = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($valorFactura)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $valorFactura)) {
                $valorFactura = str_replace('*', '%', $valorFactura);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::VALOR_FACTURA, $valorFactura, $comparison);
    }

    /**
     * Filter the query on the TIPO_DOCUMENTO column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoDocumento('fooValue');   // WHERE TIPO_DOCUMENTO = 'fooValue'
     * $query->filterByTipoDocumento('%fooValue%'); // WHERE TIPO_DOCUMENTO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoDocumento The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByTipoDocumento($tipoDocumento = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoDocumento)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoDocumento)) {
                $tipoDocumento = str_replace('*', '%', $tipoDocumento);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::TIPO_DOCUMENTO, $tipoDocumento, $comparison);
    }

    /**
     * Filter the query on the GUIA column
     *
     * Example usage:
     * <code>
     * $query->filterByGuia(1234); // WHERE GUIA = 1234
     * $query->filterByGuia(array(12, 34)); // WHERE GUIA IN (12, 34)
     * $query->filterByGuia(array('min' => 12)); // WHERE GUIA >= 12
     * $query->filterByGuia(array('max' => 12)); // WHERE GUIA <= 12
     * </code>
     *
     * @param     mixed $guia The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByGuia($guia = null, $comparison = null)
    {
        if (is_array($guia)) {
            $useMinMax = false;
            if (isset($guia['min'])) {
                $this->addUsingAlias(NovedadesPeer::GUIA, $guia['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($guia['max'])) {
                $this->addUsingAlias(NovedadesPeer::GUIA, $guia['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::GUIA, $guia, $comparison);
    }

    /**
     * Filter the query on the NUMERO_FACTURA column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroFactura(1234); // WHERE NUMERO_FACTURA = 1234
     * $query->filterByNumeroFactura(array(12, 34)); // WHERE NUMERO_FACTURA IN (12, 34)
     * $query->filterByNumeroFactura(array('min' => 12)); // WHERE NUMERO_FACTURA >= 12
     * $query->filterByNumeroFactura(array('max' => 12)); // WHERE NUMERO_FACTURA <= 12
     * </code>
     *
     * @param     mixed $numeroFactura The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByNumeroFactura($numeroFactura = null, $comparison = null)
    {
        if (is_array($numeroFactura)) {
            $useMinMax = false;
            if (isset($numeroFactura['min'])) {
                $this->addUsingAlias(NovedadesPeer::NUMERO_FACTURA, $numeroFactura['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($numeroFactura['max'])) {
                $this->addUsingAlias(NovedadesPeer::NUMERO_FACTURA, $numeroFactura['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::NUMERO_FACTURA, $numeroFactura, $comparison);
    }

    /**
     * Filter the query on the ESTADO_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoAprobacion(1234); // WHERE ESTADO_APROBACION = 1234
     * $query->filterByEstadoAprobacion(array(12, 34)); // WHERE ESTADO_APROBACION IN (12, 34)
     * $query->filterByEstadoAprobacion(array('min' => 12)); // WHERE ESTADO_APROBACION >= 12
     * $query->filterByEstadoAprobacion(array('max' => 12)); // WHERE ESTADO_APROBACION <= 12
     * </code>
     *
     * @param     mixed $estadoAprobacion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByEstadoAprobacion($estadoAprobacion = null, $comparison = null)
    {
        if (is_array($estadoAprobacion)) {
            $useMinMax = false;
            if (isset($estadoAprobacion['min'])) {
                $this->addUsingAlias(NovedadesPeer::ESTADO_APROBACION, $estadoAprobacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadoAprobacion['max'])) {
                $this->addUsingAlias(NovedadesPeer::ESTADO_APROBACION, $estadoAprobacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::ESTADO_APROBACION, $estadoAprobacion, $comparison);
    }

    /**
     * Filter the query on the OBS_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByObsAprobacion('fooValue');   // WHERE OBS_APROBACION = 'fooValue'
     * $query->filterByObsAprobacion('%fooValue%'); // WHERE OBS_APROBACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obsAprobacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByObsAprobacion($obsAprobacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obsAprobacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obsAprobacion)) {
                $obsAprobacion = str_replace('*', '%', $obsAprobacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::OBS_APROBACION, $obsAprobacion, $comparison);
    }

    /**
     * Filter the query on the USUARIO_APRUEBA column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioAprueba(1234); // WHERE USUARIO_APRUEBA = 1234
     * $query->filterByUsuarioAprueba(array(12, 34)); // WHERE USUARIO_APRUEBA IN (12, 34)
     * $query->filterByUsuarioAprueba(array('min' => 12)); // WHERE USUARIO_APRUEBA >= 12
     * $query->filterByUsuarioAprueba(array('max' => 12)); // WHERE USUARIO_APRUEBA <= 12
     * </code>
     *
     * @param     mixed $usuarioAprueba The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByUsuarioAprueba($usuarioAprueba = null, $comparison = null)
    {
        if (is_array($usuarioAprueba)) {
            $useMinMax = false;
            if (isset($usuarioAprueba['min'])) {
                $this->addUsingAlias(NovedadesPeer::USUARIO_APRUEBA, $usuarioAprueba['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioAprueba['max'])) {
                $this->addUsingAlias(NovedadesPeer::USUARIO_APRUEBA, $usuarioAprueba['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::USUARIO_APRUEBA, $usuarioAprueba, $comparison);
    }

    /**
     * Filter the query on the USUARIO_DESTINO column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioDestino(1234); // WHERE USUARIO_DESTINO = 1234
     * $query->filterByUsuarioDestino(array(12, 34)); // WHERE USUARIO_DESTINO IN (12, 34)
     * $query->filterByUsuarioDestino(array('min' => 12)); // WHERE USUARIO_DESTINO >= 12
     * $query->filterByUsuarioDestino(array('max' => 12)); // WHERE USUARIO_DESTINO <= 12
     * </code>
     *
     * @param     mixed $usuarioDestino The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByUsuarioDestino($usuarioDestino = null, $comparison = null)
    {
        if (is_array($usuarioDestino)) {
            $useMinMax = false;
            if (isset($usuarioDestino['min'])) {
                $this->addUsingAlias(NovedadesPeer::USUARIO_DESTINO, $usuarioDestino['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioDestino['max'])) {
                $this->addUsingAlias(NovedadesPeer::USUARIO_DESTINO, $usuarioDestino['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::USUARIO_DESTINO, $usuarioDestino, $comparison);
    }

    /**
     * Filter the query on the FECHA_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaAprobacion('2011-03-14'); // WHERE FECHA_APROBACION = '2011-03-14'
     * $query->filterByFechaAprobacion('now'); // WHERE FECHA_APROBACION = '2011-03-14'
     * $query->filterByFechaAprobacion(array('max' => 'yesterday')); // WHERE FECHA_APROBACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaAprobacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function filterByFechaAprobacion($fechaAprobacion = null, $comparison = null)
    {
        if (is_array($fechaAprobacion)) {
            $useMinMax = false;
            if (isset($fechaAprobacion['min'])) {
                $this->addUsingAlias(NovedadesPeer::FECHA_APROBACION, $fechaAprobacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaAprobacion['max'])) {
                $this->addUsingAlias(NovedadesPeer::FECHA_APROBACION, $fechaAprobacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(NovedadesPeer::FECHA_APROBACION, $fechaAprobacion, $comparison);
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 NovedadesQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(NovedadesPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(NovedadesPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related Modulo object
     *
     * @param   Modulo|PropelObjectCollection $modulo The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 NovedadesQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByModulo($modulo, $comparison = null)
    {
        if ($modulo instanceof Modulo) {
            return $this
                ->addUsingAlias(NovedadesPeer::MODULO_ID, $modulo->getModuloId(), $comparison);
        } elseif ($modulo instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(NovedadesPeer::MODULO_ID, $modulo->toKeyValue('PrimaryKey', 'ModuloId'), $comparison);
        } else {
            throw new PropelException('filterByModulo() only accepts arguments of type Modulo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Modulo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function joinModulo($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Modulo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Modulo');
        }

        return $this;
    }

    /**
     * Use the Modulo relation Modulo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ModuloQuery A secondary query class using the current class as primary query
     */
    public function useModuloQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinModulo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Modulo', 'ModuloQuery');
    }

    /**
     * Filter the query by a related TipoControl object
     *
     * @param   TipoControl|PropelObjectCollection $tipoControl The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 NovedadesQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoControl($tipoControl, $comparison = null)
    {
        if ($tipoControl instanceof TipoControl) {
            return $this
                ->addUsingAlias(NovedadesPeer::TIPOCONTROL_ID, $tipoControl->getTipocontrolId(), $comparison);
        } elseif ($tipoControl instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(NovedadesPeer::TIPOCONTROL_ID, $tipoControl->toKeyValue('PrimaryKey', 'TipocontrolId'), $comparison);
        } else {
            throw new PropelException('filterByTipoControl() only accepts arguments of type TipoControl or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoControl relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function joinTipoControl($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoControl');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoControl');
        }

        return $this;
    }

    /**
     * Use the TipoControl relation TipoControl object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoControlQuery A secondary query class using the current class as primary query
     */
    public function useTipoControlQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTipoControl($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoControl', 'TipoControlQuery');
    }

    /**
     * Filter the query by a related NovedadesUsuario object
     *
     * @param   NovedadesUsuario|PropelObjectCollection $novedadesUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 NovedadesQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByNovedadesUsuario($novedadesUsuario, $comparison = null)
    {
        if ($novedadesUsuario instanceof NovedadesUsuario) {
            return $this
                ->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $novedadesUsuario->getNovedadesId(), $comparison);
        } elseif ($novedadesUsuario instanceof PropelObjectCollection) {
            return $this
                ->useNovedadesUsuarioQuery()
                ->filterByPrimaryKeys($novedadesUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByNovedadesUsuario() only accepts arguments of type NovedadesUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the NovedadesUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function joinNovedadesUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('NovedadesUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'NovedadesUsuario');
        }

        return $this;
    }

    /**
     * Use the NovedadesUsuario relation NovedadesUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NovedadesUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useNovedadesUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinNovedadesUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'NovedadesUsuario', 'NovedadesUsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Novedades $novedades Object to remove from the list of results
     *
     * @return NovedadesQuery The current query, for fluid interface
     */
    public function prune($novedades = null)
    {
        if ($novedades) {
            $this->addUsingAlias(NovedadesPeer::NOVEDADES_ID, $novedades->getNovedadesId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

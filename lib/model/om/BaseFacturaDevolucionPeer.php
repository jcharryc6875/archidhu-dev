<?php


/**
 * Base static class for performing query and update operations on the 'FACTURA_DEVOLUCION' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:58
 *
 * @package propel.generator.lib.model.om
 */
abstract class BaseFacturaDevolucionPeer
{

    /** the default database name for this class */
    const DATABASE_NAME = 'propel';

    /** the table name for this class */
    const TABLE_NAME = 'FACTURA_DEVOLUCION';

    /** the related Propel class for this table */
    const OM_CLASS = 'FacturaDevolucion';

    /** the related TableMap class for this table */
    const TM_CLASS = 'FacturaDevolucionTableMap';

    /** The total number of columns. */
    const NUM_COLUMNS = 6;

    /** The number of lazy-loaded columns. */
    const NUM_LAZY_LOAD_COLUMNS = 0;

    /** The number of columns to hydrate (NUM_COLUMNS - NUM_LAZY_LOAD_COLUMNS) */
    const NUM_HYDRATE_COLUMNS = 6;

    /** the column name for the FACTURADEVOLUCION_ID field */
    const FACTURADEVOLUCION_ID = 'FACTURA_DEVOLUCION.FACTURADEVOLUCION_ID';

    /** the column name for the FAPROCESOACTUAL_ID field */
    const FAPROCESOACTUAL_ID = 'FACTURA_DEVOLUCION.FAPROCESOACTUAL_ID';

    /** the column name for the FAESTADOACTUAL_ID field */
    const FAESTADOACTUAL_ID = 'FACTURA_DEVOLUCION.FAESTADOACTUAL_ID';

    /** the column name for the FAPROCESODESTINO_ID field */
    const FAPROCESODESTINO_ID = 'FACTURA_DEVOLUCION.FAPROCESODESTINO_ID';

    /** the column name for the FAESTADODESTINO_ID field */
    const FAESTADODESTINO_ID = 'FACTURA_DEVOLUCION.FAESTADODESTINO_ID';

    /** the column name for the FACTURATIPO_ID field */
    const FACTURATIPO_ID = 'FACTURA_DEVOLUCION.FACTURATIPO_ID';

    /** The default string format for model objects of the related table **/
    const DEFAULT_STRING_FORMAT = 'YAML';

    /**
     * An identity map to hold any loaded instances of FacturaDevolucion objects.
     * This must be public so that other peer classes can access this when hydrating from JOIN
     * queries.
     * @var        array FacturaDevolucion[]
     */
    public static $instances = array();


    /**
     * holds an array of fieldnames
     *
     * first dimension keys are the type constants
     * e.g. FacturaDevolucionPeer::$fieldNames[FacturaDevolucionPeer::TYPE_PHPNAME][0] = 'Id'
     */
    protected static $fieldNames = array (
        BasePeer::TYPE_PHPNAME => array ('FacturadevolucionId', 'FaprocesoactualId', 'FaestadoactualId', 'FaprocesodestinoId', 'FaestadodestinoId', 'FacturatipoId', ),
        BasePeer::TYPE_STUDLYPHPNAME => array ('facturadevolucionId', 'faprocesoactualId', 'faestadoactualId', 'faprocesodestinoId', 'faestadodestinoId', 'facturatipoId', ),
        BasePeer::TYPE_COLNAME => array (FacturaDevolucionPeer::FACTURADEVOLUCION_ID, FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaDevolucionPeer::FACTURATIPO_ID, ),
        BasePeer::TYPE_RAW_COLNAME => array ('FACTURADEVOLUCION_ID', 'FAPROCESOACTUAL_ID', 'FAESTADOACTUAL_ID', 'FAPROCESODESTINO_ID', 'FAESTADODESTINO_ID', 'FACTURATIPO_ID', ),
        BasePeer::TYPE_FIELDNAME => array ('FACTURADEVOLUCION_ID', 'FAPROCESOACTUAL_ID', 'FAESTADOACTUAL_ID', 'FAPROCESODESTINO_ID', 'FAESTADODESTINO_ID', 'FACTURATIPO_ID', ),
        BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, )
    );

    /**
     * holds an array of keys for quick access to the fieldnames array
     *
     * first dimension keys are the type constants
     * e.g. FacturaDevolucionPeer::$fieldNames[BasePeer::TYPE_PHPNAME]['Id'] = 0
     */
    protected static $fieldKeys = array (
        BasePeer::TYPE_PHPNAME => array ('FacturadevolucionId' => 0, 'FaprocesoactualId' => 1, 'FaestadoactualId' => 2, 'FaprocesodestinoId' => 3, 'FaestadodestinoId' => 4, 'FacturatipoId' => 5, ),
        BasePeer::TYPE_STUDLYPHPNAME => array ('facturadevolucionId' => 0, 'faprocesoactualId' => 1, 'faestadoactualId' => 2, 'faprocesodestinoId' => 3, 'faestadodestinoId' => 4, 'facturatipoId' => 5, ),
        BasePeer::TYPE_COLNAME => array (FacturaDevolucionPeer::FACTURADEVOLUCION_ID => 0, FacturaDevolucionPeer::FAPROCESOACTUAL_ID => 1, FacturaDevolucionPeer::FAESTADOACTUAL_ID => 2, FacturaDevolucionPeer::FAPROCESODESTINO_ID => 3, FacturaDevolucionPeer::FAESTADODESTINO_ID => 4, FacturaDevolucionPeer::FACTURATIPO_ID => 5, ),
        BasePeer::TYPE_RAW_COLNAME => array ('FACTURADEVOLUCION_ID' => 0, 'FAPROCESOACTUAL_ID' => 1, 'FAESTADOACTUAL_ID' => 2, 'FAPROCESODESTINO_ID' => 3, 'FAESTADODESTINO_ID' => 4, 'FACTURATIPO_ID' => 5, ),
        BasePeer::TYPE_FIELDNAME => array ('FACTURADEVOLUCION_ID' => 0, 'FAPROCESOACTUAL_ID' => 1, 'FAESTADOACTUAL_ID' => 2, 'FAPROCESODESTINO_ID' => 3, 'FAESTADODESTINO_ID' => 4, 'FACTURATIPO_ID' => 5, ),
        BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, )
    );

    /**
     * Translates a fieldname to another type
     *
     * @param      string $name field name
     * @param      string $fromType One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                         BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
     * @param      string $toType   One of the class type constants
     * @return string          translated name of the field.
     * @throws PropelException - if the specified name could not be found in the fieldname mappings.
     */
    public static function translateFieldName($name, $fromType, $toType)
    {
        $toNames = FacturaDevolucionPeer::getFieldNames($toType);
        $key = isset(FacturaDevolucionPeer::$fieldKeys[$fromType][$name]) ? FacturaDevolucionPeer::$fieldKeys[$fromType][$name] : null;
        if ($key === null) {
            throw new PropelException("'$name' could not be found in the field names of type '$fromType'. These are: " . print_r(FacturaDevolucionPeer::$fieldKeys[$fromType], true));
        }

        return $toNames[$key];
    }

    /**
     * Returns an array of field names.
     *
     * @param      string $type The type of fieldnames to return:
     *                      One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                      BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
     * @return array           A list of field names
     * @throws PropelException - if the type is not valid.
     */
    public static function getFieldNames($type = BasePeer::TYPE_PHPNAME)
    {
        if (!array_key_exists($type, FacturaDevolucionPeer::$fieldNames)) {
            throw new PropelException('Method getFieldNames() expects the parameter $type to be one of the class constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME, BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. ' . $type . ' was given.');
        }

        return FacturaDevolucionPeer::$fieldNames[$type];
    }

    /**
     * Convenience method which changes table.column to alias.column.
     *
     * Using this method you can maintain SQL abstraction while using column aliases.
     * <code>
     *		$c->addAlias("alias1", TablePeer::TABLE_NAME);
     *		$c->addJoin(TablePeer::alias("alias1", TablePeer::PRIMARY_KEY_COLUMN), TablePeer::PRIMARY_KEY_COLUMN);
     * </code>
     * @param      string $alias The alias for the current table.
     * @param      string $column The column name for current table. (i.e. FacturaDevolucionPeer::COLUMN_NAME).
     * @return string
     */
    public static function alias($alias, $column)
    {
        return str_replace(FacturaDevolucionPeer::TABLE_NAME.'.', $alias.'.', $column);
    }

    /**
     * Add all the columns needed to create a new object.
     *
     * Note: any columns that were marked with lazyLoad="true" in the
     * XML schema will not be added to the select list and only loaded
     * on demand.
     *
     * @param      Criteria $criteria object containing the columns to add.
     * @param      string   $alias    optional table alias
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function addSelectColumns(Criteria $criteria, $alias = null)
    {
        if (null === $alias) {
            $criteria->addSelectColumn(FacturaDevolucionPeer::FACTURADEVOLUCION_ID);
            $criteria->addSelectColumn(FacturaDevolucionPeer::FAPROCESOACTUAL_ID);
            $criteria->addSelectColumn(FacturaDevolucionPeer::FAESTADOACTUAL_ID);
            $criteria->addSelectColumn(FacturaDevolucionPeer::FAPROCESODESTINO_ID);
            $criteria->addSelectColumn(FacturaDevolucionPeer::FAESTADODESTINO_ID);
            $criteria->addSelectColumn(FacturaDevolucionPeer::FACTURATIPO_ID);
        } else {
            $criteria->addSelectColumn($alias . '.FACTURADEVOLUCION_ID');
            $criteria->addSelectColumn($alias . '.FAPROCESOACTUAL_ID');
            $criteria->addSelectColumn($alias . '.FAESTADOACTUAL_ID');
            $criteria->addSelectColumn($alias . '.FAPROCESODESTINO_ID');
            $criteria->addSelectColumn($alias . '.FAESTADODESTINO_ID');
            $criteria->addSelectColumn($alias . '.FACTURATIPO_ID');
        }
    }

    /**
     * Returns the number of rows matching criteria.
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @return int Number of matching rows.
     */
    public static function doCount(Criteria $criteria, $distinct = false, PropelPDO $con = null)
    {
        // we may modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME); // Set the correct dbName

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        // BasePeer returns a PDOStatement
        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }
    /**
     * Selects one object from the DB.
     *
     * @param      Criteria $criteria object used to create the SELECT statement.
     * @param      PropelPDO $con
     * @return FacturaDevolucion
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectOne(Criteria $criteria, PropelPDO $con = null)
    {
        $critcopy = clone $criteria;
        $critcopy->setLimit(1);
        $objects = FacturaDevolucionPeer::doSelect($critcopy, $con);
        if ($objects) {
            return $objects[0];
        }

        return null;
    }
    /**
     * Selects several row from the DB.
     *
     * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
     * @param      PropelPDO $con
     * @return array           Array of selected Objects
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelect(Criteria $criteria, PropelPDO $con = null)
    {
        return FacturaDevolucionPeer::populateObjects(FacturaDevolucionPeer::doSelectStmt($criteria, $con));
    }
    /**
     * Prepares the Criteria object and uses the parent doSelect() method to execute a PDOStatement.
     *
     * Use this method directly if you want to work with an executed statement directly (for example
     * to perform your own object hydration).
     *
     * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
     * @param      PropelPDO $con The connection to use
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     * @return PDOStatement The executed PDOStatement object.
     * @see        BasePeer::doSelect()
     */
    public static function doSelectStmt(Criteria $criteria, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        if (!$criteria->hasSelectClause()) {
            $criteria = clone $criteria;
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }


        // BasePeer returns a PDOStatement
        return BasePeer::doSelect($criteria, $con);
    }
    /**
     * Adds an object to the instance pool.
     *
     * Propel keeps cached copies of objects in an instance pool when they are retrieved
     * from the database.  In some cases -- especially when you override doSelect*()
     * methods in your stub classes -- you may need to explicitly add objects
     * to the cache in order to ensure that the same objects are always returned by doSelect*()
     * and retrieveByPK*() calls.
     *
     * @param FacturaDevolucion $obj A FacturaDevolucion object.
     * @param      string $key (optional) key to use for instance map (for performance boost if key was already calculated externally).
     */
    public static function addInstanceToPool($obj, $key = null)
    {
        if (Propel::isInstancePoolingEnabled()) {
            if ($key === null) {
                $key = (string) $obj->getFacturadevolucionId();
            } // if key === null
            FacturaDevolucionPeer::$instances[$key] = $obj;
        }
    }

    /**
     * Removes an object from the instance pool.
     *
     * Propel keeps cached copies of objects in an instance pool when they are retrieved
     * from the database.  In some cases -- especially when you override doDelete
     * methods in your stub classes -- you may need to explicitly remove objects
     * from the cache in order to prevent returning objects that no longer exist.
     *
     * @param      mixed $value A FacturaDevolucion object or a primary key value.
     *
     * @return void
     * @throws PropelException - if the value is invalid.
     */
    public static function removeInstanceFromPool($value)
    {
        if (Propel::isInstancePoolingEnabled() && $value !== null) {
            if (is_object($value) && $value instanceof FacturaDevolucion) {
                $key = (string) $value->getFacturadevolucionId();
            } elseif (is_scalar($value)) {
                // assume we've been passed a primary key
                $key = (string) $value;
            } else {
                $e = new PropelException("Invalid value passed to removeInstanceFromPool().  Expected primary key or FacturaDevolucion object; got " . (is_object($value) ? get_class($value) . ' object.' : var_export($value,true)));
                throw $e;
            }

            unset(FacturaDevolucionPeer::$instances[$key]);
        }
    } // removeInstanceFromPool()

    /**
     * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
     *
     * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
     * a multi-column primary key, a serialize()d version of the primary key will be returned.
     *
     * @param      string $key The key (@see getPrimaryKeyHash()) for this instance.
     * @return FacturaDevolucion Found object or null if 1) no instance exists for specified key or 2) instance pooling has been disabled.
     * @see        getPrimaryKeyHash()
     */
    public static function getInstanceFromPool($key)
    {
        if (Propel::isInstancePoolingEnabled()) {
            if (isset(FacturaDevolucionPeer::$instances[$key])) {
                return FacturaDevolucionPeer::$instances[$key];
            }
        }

        return null; // just to be explicit
    }
    
    /**
     * Clear the instance pool.
     *
     * @return void
     */
    public static function clearInstancePool($and_clear_all_references = false)
    {
      if ($and_clear_all_references) {
        foreach (FacturaDevolucionPeer::$instances as $instance) {
          $instance->clearAllReferences(true);
        }
      }
        FacturaDevolucionPeer::$instances = array();
    }
    
    /**
     * Method to invalidate the instance pool of all tables related to FACTURA_DEVOLUCION
     * by a foreign key with ON DELETE CASCADE
     */
    public static function clearRelatedInstancePool()
    {
    }

    /**
     * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
     *
     * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
     * a multi-column primary key, a serialize()d version of the primary key will be returned.
     *
     * @param      array $row PropelPDO resultset row.
     * @param      int $startcol The 0-based offset for reading from the resultset row.
     * @return string A string version of PK or null if the components of primary key in result array are all null.
     */
    public static function getPrimaryKeyHashFromRow($row, $startcol = 0)
    {
        // If the PK cannot be derived from the row, return null.
        if ($row[$startcol] === null) {
            return null;
        }

        return (string) $row[$startcol];
    }

    /**
     * Retrieves the primary key from the DB resultset row
     * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
     * a multi-column primary key, an array of the primary key columns will be returned.
     *
     * @param      array $row PropelPDO resultset row.
     * @param      int $startcol The 0-based offset for reading from the resultset row.
     * @return mixed The primary key of the row
     */
    public static function getPrimaryKeyFromRow($row, $startcol = 0)
    {

        return (int) $row[$startcol];
    }
    
    /**
     * The returned array will contain objects of the default type or
     * objects that inherit from the default.
     *
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function populateObjects(PDOStatement $stmt)
    {
        $results = array();
    
        // set the class once to avoid overhead in the loop
        $cls = FacturaDevolucionPeer::getOMClass();
        // populate the object(s)
        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj = FacturaDevolucionPeer::getInstanceFromPool($key))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj->hydrate($row, 0, true); // rehydrate
                $results[] = $obj;
            } else {
                $obj = new $cls();
                $obj->hydrate($row);
                $results[] = $obj;
                FacturaDevolucionPeer::addInstanceToPool($obj, $key);
            } // if key exists
        }
        $stmt->closeCursor();

        return $results;
    }
    /**
     * Populates an object of the default type or an object that inherit from the default.
     *
     * @param      array $row PropelPDO resultset row.
     * @param      int $startcol The 0-based offset for reading from the resultset row.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     * @return array (FacturaDevolucion object, last column rank)
     */
    public static function populateObject($row, $startcol = 0)
    {
        $key = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, $startcol);
        if (null !== ($obj = FacturaDevolucionPeer::getInstanceFromPool($key))) {
            // We no longer rehydrate the object, since this can cause data loss.
            // See http://www.propelorm.org/ticket/509
            // $obj->hydrate($row, $startcol, true); // rehydrate
            $col = $startcol + FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;
        } else {
            $cls = FacturaDevolucionPeer::OM_CLASS;
            $obj = new $cls();
            $col = $obj->hydrate($row, $startcol);
            FacturaDevolucionPeer::addInstanceToPool($obj, $key);
        }

        return array($obj, $col);
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaProcesoRelatedByFaprocesoactualId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinFacturaProcesoRelatedByFaprocesoactualId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaEstadoRelatedByFaestadoactualId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinFacturaEstadoRelatedByFaestadoactualId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaProcesoRelatedByFaprocesodestinoId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinFacturaProcesoRelatedByFaprocesodestinoId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaEstadoRelatedByFaestadodestinoId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinFacturaEstadoRelatedByFaestadodestinoId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaTipo table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinFacturaTipo(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with their FacturaProceso objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinFacturaProcesoRelatedByFaprocesoactualId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;
        FacturaProcesoPeer::addSelectColumns($criteria);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = FacturaProcesoPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = FacturaProcesoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    FacturaProcesoPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to $obj2 (FacturaProceso)
                $obj2->addFacturaDevolucionRelatedByFaprocesoactualId($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with their FacturaEstado objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinFacturaEstadoRelatedByFaestadoactualId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;
        FacturaEstadoPeer::addSelectColumns($criteria);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = FacturaEstadoPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = FacturaEstadoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    FacturaEstadoPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to $obj2 (FacturaEstado)
                $obj2->addFacturaDevolucionRelatedByFaestadoactualId($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with their FacturaProceso objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinFacturaProcesoRelatedByFaprocesodestinoId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;
        FacturaProcesoPeer::addSelectColumns($criteria);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = FacturaProcesoPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = FacturaProcesoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    FacturaProcesoPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to $obj2 (FacturaProceso)
                $obj2->addFacturaDevolucionRelatedByFaprocesodestinoId($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with their FacturaEstado objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinFacturaEstadoRelatedByFaestadodestinoId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;
        FacturaEstadoPeer::addSelectColumns($criteria);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = FacturaEstadoPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = FacturaEstadoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    FacturaEstadoPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to $obj2 (FacturaEstado)
                $obj2->addFacturaDevolucionRelatedByFaestadodestinoId($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with their FacturaTipo objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinFacturaTipo(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;
        FacturaTipoPeer::addSelectColumns($criteria);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = FacturaTipoPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = FacturaTipoPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = FacturaTipoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    FacturaTipoPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to $obj2 (FacturaTipo)
                $obj2->addFacturaDevolucion($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Returns the number of rows matching criteria, joining all related tables
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAll(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }

    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with all related objects.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAll(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol2 = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol6 = $startcol5 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaTipoPeer::addSelectColumns($criteria);
        $startcol7 = $startcol6 + FacturaTipoPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

            // Add objects for joined FacturaProceso rows

            $key2 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
            if ($key2 !== null) {
                $obj2 = FacturaProcesoPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = FacturaProcesoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    FacturaProcesoPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj2 (FacturaProceso)
                $obj2->addFacturaDevolucionRelatedByFaprocesoactualId($obj1);
            } // if joined row not null

            // Add objects for joined FacturaEstado rows

            $key3 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
            if ($key3 !== null) {
                $obj3 = FacturaEstadoPeer::getInstanceFromPool($key3);
                if (!$obj3) {

                    $cls = FacturaEstadoPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    FacturaEstadoPeer::addInstanceToPool($obj3, $key3);
                } // if obj3 loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj3 (FacturaEstado)
                $obj3->addFacturaDevolucionRelatedByFaestadoactualId($obj1);
            } // if joined row not null

            // Add objects for joined FacturaProceso rows

            $key4 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
            if ($key4 !== null) {
                $obj4 = FacturaProcesoPeer::getInstanceFromPool($key4);
                if (!$obj4) {

                    $cls = FacturaProcesoPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    FacturaProcesoPeer::addInstanceToPool($obj4, $key4);
                } // if obj4 loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj4 (FacturaProceso)
                $obj4->addFacturaDevolucionRelatedByFaprocesodestinoId($obj1);
            } // if joined row not null

            // Add objects for joined FacturaEstado rows

            $key5 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol5);
            if ($key5 !== null) {
                $obj5 = FacturaEstadoPeer::getInstanceFromPool($key5);
                if (!$obj5) {

                    $cls = FacturaEstadoPeer::getOMClass();

                    $obj5 = new $cls();
                    $obj5->hydrate($row, $startcol5);
                    FacturaEstadoPeer::addInstanceToPool($obj5, $key5);
                } // if obj5 loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj5 (FacturaEstado)
                $obj5->addFacturaDevolucionRelatedByFaestadodestinoId($obj1);
            } // if joined row not null

            // Add objects for joined FacturaTipo rows

            $key6 = FacturaTipoPeer::getPrimaryKeyHashFromRow($row, $startcol6);
            if ($key6 !== null) {
                $obj6 = FacturaTipoPeer::getInstanceFromPool($key6);
                if (!$obj6) {

                    $cls = FacturaTipoPeer::getOMClass();

                    $obj6 = new $cls();
                    $obj6->hydrate($row, $startcol6);
                    FacturaTipoPeer::addInstanceToPool($obj6, $key6);
                } // if obj6 loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj6 (FacturaTipo)
                $obj6->addFacturaDevolucion($obj1);
            } // if joined row not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaProcesoRelatedByFaprocesoactualId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptFacturaProcesoRelatedByFaprocesoactualId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaEstadoRelatedByFaestadoactualId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptFacturaEstadoRelatedByFaestadoactualId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaProcesoRelatedByFaprocesodestinoId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptFacturaProcesoRelatedByFaprocesodestinoId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaEstadoRelatedByFaestadodestinoId table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptFacturaEstadoRelatedByFaestadodestinoId(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related FacturaTipo table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptFacturaTipo(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            FacturaDevolucionPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with all related objects except FacturaProcesoRelatedByFaprocesoactualId.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptFacturaProcesoRelatedByFaprocesoactualId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol2 = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaTipoPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + FacturaTipoPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined FacturaEstado rows

                $key2 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = FacturaEstadoPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = FacturaEstadoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    FacturaEstadoPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj2 (FacturaEstado)
                $obj2->addFacturaDevolucionRelatedByFaestadoactualId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaEstado rows

                $key3 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = FacturaEstadoPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = FacturaEstadoPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    FacturaEstadoPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj3 (FacturaEstado)
                $obj3->addFacturaDevolucionRelatedByFaestadodestinoId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaTipo rows

                $key4 = FacturaTipoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = FacturaTipoPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = FacturaTipoPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    FacturaTipoPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj4 (FacturaTipo)
                $obj4->addFacturaDevolucion($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with all related objects except FacturaEstadoRelatedByFaestadoactualId.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptFacturaEstadoRelatedByFaestadoactualId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol2 = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaTipoPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + FacturaTipoPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined FacturaProceso rows

                $key2 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = FacturaProcesoPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = FacturaProcesoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    FacturaProcesoPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj2 (FacturaProceso)
                $obj2->addFacturaDevolucionRelatedByFaprocesoactualId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaProceso rows

                $key3 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = FacturaProcesoPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = FacturaProcesoPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    FacturaProcesoPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj3 (FacturaProceso)
                $obj3->addFacturaDevolucionRelatedByFaprocesodestinoId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaTipo rows

                $key4 = FacturaTipoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = FacturaTipoPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = FacturaTipoPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    FacturaTipoPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj4 (FacturaTipo)
                $obj4->addFacturaDevolucion($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with all related objects except FacturaProcesoRelatedByFaprocesodestinoId.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptFacturaProcesoRelatedByFaprocesodestinoId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol2 = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaTipoPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + FacturaTipoPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined FacturaEstado rows

                $key2 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = FacturaEstadoPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = FacturaEstadoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    FacturaEstadoPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj2 (FacturaEstado)
                $obj2->addFacturaDevolucionRelatedByFaestadoactualId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaEstado rows

                $key3 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = FacturaEstadoPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = FacturaEstadoPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    FacturaEstadoPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj3 (FacturaEstado)
                $obj3->addFacturaDevolucionRelatedByFaestadodestinoId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaTipo rows

                $key4 = FacturaTipoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = FacturaTipoPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = FacturaTipoPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    FacturaTipoPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj4 (FacturaTipo)
                $obj4->addFacturaDevolucion($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with all related objects except FacturaEstadoRelatedByFaestadodestinoId.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptFacturaEstadoRelatedByFaestadodestinoId(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol2 = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaTipoPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + FacturaTipoPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FACTURATIPO_ID, FacturaTipoPeer::FACTURATIPO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined FacturaProceso rows

                $key2 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = FacturaProcesoPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = FacturaProcesoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    FacturaProcesoPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj2 (FacturaProceso)
                $obj2->addFacturaDevolucionRelatedByFaprocesoactualId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaProceso rows

                $key3 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = FacturaProcesoPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = FacturaProcesoPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    FacturaProcesoPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj3 (FacturaProceso)
                $obj3->addFacturaDevolucionRelatedByFaprocesodestinoId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaTipo rows

                $key4 = FacturaTipoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = FacturaTipoPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = FacturaTipoPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    FacturaTipoPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj4 (FacturaTipo)
                $obj4->addFacturaDevolucion($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of FacturaDevolucion objects pre-filled with all related objects except FacturaTipo.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of FacturaDevolucion objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptFacturaTipo(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);
        }

        FacturaDevolucionPeer::addSelectColumns($criteria);
        $startcol2 = FacturaDevolucionPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        FacturaProcesoPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + FacturaProcesoPeer::NUM_HYDRATE_COLUMNS;

        FacturaEstadoPeer::addSelectColumns($criteria);
        $startcol6 = $startcol5 + FacturaEstadoPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESOACTUAL_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADOACTUAL_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAPROCESODESTINO_ID, FacturaProcesoPeer::FACTURAPROCESO_ID, $join_behavior);

        $criteria->addJoin(FacturaDevolucionPeer::FAESTADODESTINO_ID, FacturaEstadoPeer::FACTURAESTADO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseFacturaDevolucionPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = FacturaDevolucionPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = FacturaDevolucionPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = FacturaDevolucionPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                FacturaDevolucionPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined FacturaProceso rows

                $key2 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = FacturaProcesoPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = FacturaProcesoPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    FacturaProcesoPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj2 (FacturaProceso)
                $obj2->addFacturaDevolucionRelatedByFaprocesoactualId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaEstado rows

                $key3 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = FacturaEstadoPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = FacturaEstadoPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    FacturaEstadoPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj3 (FacturaEstado)
                $obj3->addFacturaDevolucionRelatedByFaestadoactualId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaProceso rows

                $key4 = FacturaProcesoPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = FacturaProcesoPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = FacturaProcesoPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    FacturaProcesoPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj4 (FacturaProceso)
                $obj4->addFacturaDevolucionRelatedByFaprocesodestinoId($obj1);

            } // if joined row is not null

                // Add objects for joined FacturaEstado rows

                $key5 = FacturaEstadoPeer::getPrimaryKeyHashFromRow($row, $startcol5);
                if ($key5 !== null) {
                    $obj5 = FacturaEstadoPeer::getInstanceFromPool($key5);
                    if (!$obj5) {
    
                        $cls = FacturaEstadoPeer::getOMClass();

                    $obj5 = new $cls();
                    $obj5->hydrate($row, $startcol5);
                    FacturaEstadoPeer::addInstanceToPool($obj5, $key5);
                } // if $obj5 already loaded

                // Add the $obj1 (FacturaDevolucion) to the collection in $obj5 (FacturaEstado)
                $obj5->addFacturaDevolucionRelatedByFaestadodestinoId($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }

    /**
     * Returns the TableMap related to this peer.
     * This method is not needed for general use but a specific application could have a need.
     * @return TableMap
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function getTableMap()
    {
        return Propel::getDatabaseMap(FacturaDevolucionPeer::DATABASE_NAME)->getTable(FacturaDevolucionPeer::TABLE_NAME);
    }

    /**
     * Add a TableMap instance to the database for this peer class.
     */
    public static function buildTableMap()
    {
      $dbMap = Propel::getDatabaseMap(BaseFacturaDevolucionPeer::DATABASE_NAME);
      if (!$dbMap->hasTable(BaseFacturaDevolucionPeer::TABLE_NAME)) {
        $dbMap->addTableObject(new \FacturaDevolucionTableMap());
      }
    }

    /**
     * The class that the Peer will make instances of.
     *
     *
     * @return string ClassName
     */
    public static function getOMClass($row = 0, $colnum = 0)
    {
        return FacturaDevolucionPeer::OM_CLASS;
    }

    /**
     * Performs an INSERT on the database, given a FacturaDevolucion or Criteria object.
     *
     * @param      mixed $values Criteria or FacturaDevolucion object containing data that is used to create the INSERT statement.
     * @param      PropelPDO $con the PropelPDO connection to use
     * @return mixed           The new primary key.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doInsert($values, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        if ($values instanceof Criteria) {
            $criteria = clone $values; // rename for clarity
        } else {
            $criteria = $values->buildCriteria(); // build Criteria from FacturaDevolucion object
        }

        if ($criteria->containsKey(FacturaDevolucionPeer::FACTURADEVOLUCION_ID) && $criteria->keyContainsValue(FacturaDevolucionPeer::FACTURADEVOLUCION_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key ('.FacturaDevolucionPeer::FACTURADEVOLUCION_ID.')');
        }

        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(FacturaDevolucionPeer::FACTURADEVOLUCION_ID);


        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        try {
            // use transaction because $criteria could contain info
            // for more than one table (I guess, conceivably)
            $con->beginTransaction();
            $pk = BasePeer::doInsert($criteria, $con);
            $con->commit();
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }

        return $pk;
    }

    /**
     * Performs an UPDATE on the database, given a FacturaDevolucion or Criteria object.
     *
     * @param      mixed $values Criteria or FacturaDevolucion object containing data that is used to create the UPDATE statement.
     * @param      PropelPDO $con The connection to use (specify PropelPDO connection object to exert more control over transactions).
     * @return int             The number of affected rows (if supported by underlying database driver).
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doUpdate($values, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $selectCriteria = new Criteria(FacturaDevolucionPeer::DATABASE_NAME);

        if ($values instanceof Criteria) {
            $criteria = clone $values; // rename for clarity

            $comparison = $criteria->getComparison(FacturaDevolucionPeer::FACTURADEVOLUCION_ID);
            $value = $criteria->remove(FacturaDevolucionPeer::FACTURADEVOLUCION_ID);
            if ($value) {
                $selectCriteria->add(FacturaDevolucionPeer::FACTURADEVOLUCION_ID, $value, $comparison);
            } else {
                $selectCriteria->setPrimaryTableName(FacturaDevolucionPeer::TABLE_NAME);
            }

        } else { // $values is FacturaDevolucion object
            $criteria = $values->buildCriteria(); // gets full criteria
            $selectCriteria = $values->buildPkeyCriteria(); // gets criteria w/ primary key(s)
        }

        // set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        return BasePeer::doUpdate($selectCriteria, $criteria, $con);
    }

    /**
     * Deletes all rows from the FACTURA_DEVOLUCION table.
     *
     * @param      PropelPDO $con the connection to use
     * @return int             The number of affected rows (if supported by underlying database driver).
     * @throws PropelException
     */
    public static function doDeleteAll(PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }
        $affectedRows = 0; // initialize var to track total num of affected rows
        try {
            // use transaction because $criteria could contain info
            // for more than one table or we could emulating ON DELETE CASCADE, etc.
            $con->beginTransaction();
            $affectedRows += BasePeer::doDeleteAll(FacturaDevolucionPeer::TABLE_NAME, $con, FacturaDevolucionPeer::DATABASE_NAME);
            // Because this db requires some delete cascade/set null emulation, we have to
            // clear the cached instance *after* the emulation has happened (since
            // instances get re-added by the select statement contained therein).
            FacturaDevolucionPeer::clearInstancePool();
            FacturaDevolucionPeer::clearRelatedInstancePool();
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs a DELETE on the database, given a FacturaDevolucion or Criteria object OR a primary key value.
     *
     * @param      mixed $values Criteria or FacturaDevolucion object or primary key or array of primary keys
     *              which is used to create the DELETE statement
     * @param      PropelPDO $con the connection to use
     * @return int The number of affected rows (if supported by underlying database driver).  This includes CASCADE-related rows
     *				if supported by native driver or if emulated using Propel.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
     public static function doDelete($values, PropelPDO $con = null)
     {
        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        if ($values instanceof Criteria) {
            // invalidate the cache for all objects of this type, since we have no
            // way of knowing (without running a query) what objects should be invalidated
            // from the cache based on this Criteria.
            FacturaDevolucionPeer::clearInstancePool();
            // rename for clarity
            $criteria = clone $values;
        } elseif ($values instanceof FacturaDevolucion) { // it's a model object
            // invalidate the cache for this single object
            FacturaDevolucionPeer::removeInstanceFromPool($values);
            // create criteria based on pk values
            $criteria = $values->buildPkeyCriteria();
        } else { // it's a primary key, or an array of pks
            $criteria = new Criteria(FacturaDevolucionPeer::DATABASE_NAME);
            $criteria->add(FacturaDevolucionPeer::FACTURADEVOLUCION_ID, (array) $values, Criteria::IN);
            // invalidate the cache for this object(s)
            foreach ((array) $values as $singleval) {
                FacturaDevolucionPeer::removeInstanceFromPool($singleval);
            }
        }

        // Set the correct dbName
        $criteria->setDbName(FacturaDevolucionPeer::DATABASE_NAME);

        $affectedRows = 0; // initialize var to track total num of affected rows

        try {
            // use transaction because $criteria could contain info
            // for more than one table or we could emulating ON DELETE CASCADE, etc.
            $con->beginTransaction();
            
            $affectedRows += BasePeer::doDelete($criteria, $con);
            FacturaDevolucionPeer::clearRelatedInstancePool();
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Validates all modified columns of given FacturaDevolucion object.
     * If parameter $columns is either a single column name or an array of column names
     * than only those columns are validated.
     *
     * NOTICE: This does not apply to primary or foreign keys for now.
     *
     * @param FacturaDevolucion $obj The object to validate.
     * @param      mixed $cols Column name or array of column names.
     *
     * @return mixed TRUE if all columns are valid or the error message of the first invalid column.
     */
    public static function doValidate($obj, $cols = null)
    {
        $columns = array();

        if ($cols) {
            $dbMap = Propel::getDatabaseMap(FacturaDevolucionPeer::DATABASE_NAME);
            $tableMap = $dbMap->getTable(FacturaDevolucionPeer::TABLE_NAME);

            if (! is_array($cols)) {
                $cols = array($cols);
            }

            foreach ($cols as $colName) {
                if ($tableMap->hasColumn($colName)) {
                    $get = 'get' . $tableMap->getColumn($colName)->getPhpName();
                    $columns[$colName] = $obj->$get();
                }
            }
        } else {

        }

        return BasePeer::doValidate(FacturaDevolucionPeer::DATABASE_NAME, FacturaDevolucionPeer::TABLE_NAME, $columns);
    }

    /**
     * Retrieve a single object by pkey.
     *
     * @param int $pk the primary key.
     * @param      PropelPDO $con the connection to use
     * @return FacturaDevolucion
     */
    public static function retrieveByPK($pk, PropelPDO $con = null)
    {

        if (null !== ($obj = FacturaDevolucionPeer::getInstanceFromPool((string) $pk))) {
            return $obj;
        }

        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria = new Criteria(FacturaDevolucionPeer::DATABASE_NAME);
        $criteria->add(FacturaDevolucionPeer::FACTURADEVOLUCION_ID, $pk);

        $v = FacturaDevolucionPeer::doSelect($criteria, $con);

        return !empty($v) > 0 ? $v[0] : null;
    }

    /**
     * Retrieve multiple objects by pkey.
     *
     * @param      array $pks List of primary keys
     * @param      PropelPDO $con the connection to use
     * @return FacturaDevolucion[]
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function retrieveByPKs($pks, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(FacturaDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $objs = null;
        if (empty($pks)) {
            $objs = array();
        } else {
            $criteria = new Criteria(FacturaDevolucionPeer::DATABASE_NAME);
            $criteria->add(FacturaDevolucionPeer::FACTURADEVOLUCION_ID, $pks, Criteria::IN);
            $objs = FacturaDevolucionPeer::doSelect($criteria, $con);
        }

        return $objs;
    }

    // symfony behavior
    
    /**
     * Returns an array of arrays that contain columns in each unique index.
     *
     * @return array
     */
    static public function getUniqueColumnNames()
    {
      return array();
    }

    // symfony_behaviors behavior
    
    /**
     * Returns the name of the hook to call from inside the supplied method.
     *
     * @param string $method The calling method
     *
     * @return string A hook name for {@link sfMixer}
     *
     * @throws LogicException If the method name is not recognized
     */
    static private function getMixerPreSelectHook($method)
    {
      if (preg_match('/^do(Select|Count)(Join(All(Except)?)?|Stmt)?/', $method, $match))
      {
        return sprintf('BaseFacturaDevolucionPeer:%s:%1$s', 'Count' == $match[1] ? 'doCount' : $match[0]);
      }
    
      throw new LogicException(sprintf('Unrecognized function "%s"', $method));
    }

} // BaseFacturaDevolucionPeer

// This is the static code needed to register the TableMap for this table with the main Propel class.
//
BaseFacturaDevolucionPeer::buildTableMap();


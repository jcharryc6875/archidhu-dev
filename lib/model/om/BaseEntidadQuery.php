<?php


/**
 * Base class that represents a query for the 'ENTIDAD' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:43
 *
 * @method EntidadQuery orderByEntidadId($order = Criteria::ASC) Order by the ENTIDAD_ID column
 * @method EntidadQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method EntidadQuery orderByEsActual($order = Criteria::ASC) Order by the ES_ACTUAL column
 * @method EntidadQuery orderByDirectorioName($order = Criteria::ASC) Order by the DIRECTORIO_NAME column
 * @method EntidadQuery orderByCodigo($order = Criteria::ASC) Order by the CODIGO column
 * @method EntidadQuery orderByUsarMembrete($order = Criteria::ASC) Order by the USAR_MEMBRETE column
 * @method EntidadQuery orderByLogoCorporativo($order = Criteria::ASC) Order by the LOGO_CORPORATIVO column
 * @method EntidadQuery orderByLogoHeader($order = Criteria::ASC) Order by the LOGO_HEADER column
 *
 * @method EntidadQuery groupByEntidadId() Group by the ENTIDAD_ID column
 * @method EntidadQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method EntidadQuery groupByEsActual() Group by the ES_ACTUAL column
 * @method EntidadQuery groupByDirectorioName() Group by the DIRECTORIO_NAME column
 * @method EntidadQuery groupByCodigo() Group by the CODIGO column
 * @method EntidadQuery groupByUsarMembrete() Group by the USAR_MEMBRETE column
 * @method EntidadQuery groupByLogoCorporativo() Group by the LOGO_CORPORATIVO column
 * @method EntidadQuery groupByLogoHeader() Group by the LOGO_HEADER column
 *
 * @method EntidadQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method EntidadQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method EntidadQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method EntidadQuery leftJoinDirectorioExterno($relationAlias = null) Adds a LEFT JOIN clause to the query using the DirectorioExterno relation
 * @method EntidadQuery rightJoinDirectorioExterno($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DirectorioExterno relation
 * @method EntidadQuery innerJoinDirectorioExterno($relationAlias = null) Adds a INNER JOIN clause to the query using the DirectorioExterno relation
 *
 * @method EntidadQuery leftJoinDirectorioExternoHist($relationAlias = null) Adds a LEFT JOIN clause to the query using the DirectorioExternoHist relation
 * @method EntidadQuery rightJoinDirectorioExternoHist($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DirectorioExternoHist relation
 * @method EntidadQuery innerJoinDirectorioExternoHist($relationAlias = null) Adds a INNER JOIN clause to the query using the DirectorioExternoHist relation
 *
 * @method EntidadQuery leftJoinProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Proveedor relation
 * @method EntidadQuery rightJoinProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Proveedor relation
 * @method EntidadQuery innerJoinProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the Proveedor relation
 *
 * @method EntidadQuery leftJoinRegional($relationAlias = null) Adds a LEFT JOIN clause to the query using the Regional relation
 * @method EntidadQuery rightJoinRegional($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Regional relation
 * @method EntidadQuery innerJoinRegional($relationAlias = null) Adds a INNER JOIN clause to the query using the Regional relation
 *
 * @method EntidadQuery leftJoinRol($relationAlias = null) Adds a LEFT JOIN clause to the query using the Rol relation
 * @method EntidadQuery rightJoinRol($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Rol relation
 * @method EntidadQuery innerJoinRol($relationAlias = null) Adds a INNER JOIN clause to the query using the Rol relation
 *
 * @method EntidadQuery leftJoinTipoServicio($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoServicio relation
 * @method EntidadQuery rightJoinTipoServicio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoServicio relation
 * @method EntidadQuery innerJoinTipoServicio($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoServicio relation
 *
 * @method EntidadQuery leftJoinDependencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the Dependencia relation
 * @method EntidadQuery rightJoinDependencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Dependencia relation
 * @method EntidadQuery innerJoinDependencia($relationAlias = null) Adds a INNER JOIN clause to the query using the Dependencia relation
 *
 * @method Entidad findOne(PropelPDO $con = null) Return the first Entidad matching the query
 * @method Entidad findOneOrCreate(PropelPDO $con = null) Return the first Entidad matching the query, or a new Entidad object populated from the query conditions when no match is found
 *
 * @method Entidad findOneByDescripcion(string $DESCRIPCION) Return the first Entidad filtered by the DESCRIPCION column
 * @method Entidad findOneByEsActual(boolean $ES_ACTUAL) Return the first Entidad filtered by the ES_ACTUAL column
 * @method Entidad findOneByDirectorioName(string $DIRECTORIO_NAME) Return the first Entidad filtered by the DIRECTORIO_NAME column
 * @method Entidad findOneByCodigo(string $CODIGO) Return the first Entidad filtered by the CODIGO column
 * @method Entidad findOneByUsarMembrete(boolean $USAR_MEMBRETE) Return the first Entidad filtered by the USAR_MEMBRETE column
 * @method Entidad findOneByLogoCorporativo(string $LOGO_CORPORATIVO) Return the first Entidad filtered by the LOGO_CORPORATIVO column
 * @method Entidad findOneByLogoHeader(string $LOGO_HEADER) Return the first Entidad filtered by the LOGO_HEADER column
 *
 * @method array findByEntidadId(int $ENTIDAD_ID) Return Entidad objects filtered by the ENTIDAD_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return Entidad objects filtered by the DESCRIPCION column
 * @method array findByEsActual(boolean $ES_ACTUAL) Return Entidad objects filtered by the ES_ACTUAL column
 * @method array findByDirectorioName(string $DIRECTORIO_NAME) Return Entidad objects filtered by the DIRECTORIO_NAME column
 * @method array findByCodigo(string $CODIGO) Return Entidad objects filtered by the CODIGO column
 * @method array findByUsarMembrete(boolean $USAR_MEMBRETE) Return Entidad objects filtered by the USAR_MEMBRETE column
 * @method array findByLogoCorporativo(string $LOGO_CORPORATIVO) Return Entidad objects filtered by the LOGO_CORPORATIVO column
 * @method array findByLogoHeader(string $LOGO_HEADER) Return Entidad objects filtered by the LOGO_HEADER column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEntidadQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseEntidadQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Entidad';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new EntidadQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   EntidadQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return EntidadQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof EntidadQuery) {
            return $criteria;
        }
        $query = new EntidadQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Entidad|Entidad[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = EntidadPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(EntidadPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Entidad A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByEntidadId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Entidad A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [ENTIDAD_ID], [DESCRIPCION], [ES_ACTUAL], [DIRECTORIO_NAME], [CODIGO], [USAR_MEMBRETE], [LOGO_CORPORATIVO], [LOGO_HEADER] FROM [ENTIDAD] WHERE [ENTIDAD_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Entidad();
            $obj->hydrate($row);
            EntidadPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Entidad|Entidad[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Entidad[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(EntidadPeer::ENTIDAD_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(EntidadPeer::ENTIDAD_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the ENTIDAD_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEntidadId(1234); // WHERE ENTIDAD_ID = 1234
     * $query->filterByEntidadId(array(12, 34)); // WHERE ENTIDAD_ID IN (12, 34)
     * $query->filterByEntidadId(array('min' => 12)); // WHERE ENTIDAD_ID >= 12
     * $query->filterByEntidadId(array('max' => 12)); // WHERE ENTIDAD_ID <= 12
     * </code>
     *
     * @param     mixed $entidadId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByEntidadId($entidadId = null, $comparison = null)
    {
        if (is_array($entidadId)) {
            $useMinMax = false;
            if (isset($entidadId['min'])) {
                $this->addUsingAlias(EntidadPeer::ENTIDAD_ID, $entidadId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($entidadId['max'])) {
                $this->addUsingAlias(EntidadPeer::ENTIDAD_ID, $entidadId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EntidadPeer::ENTIDAD_ID, $entidadId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EntidadPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ES_ACTUAL column
     *
     * Example usage:
     * <code>
     * $query->filterByEsActual(true); // WHERE ES_ACTUAL = true
     * $query->filterByEsActual('yes'); // WHERE ES_ACTUAL = true
     * </code>
     *
     * @param     boolean|string $esActual The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByEsActual($esActual = null, $comparison = null)
    {
        if (is_string($esActual)) {
            $esActual = in_array(strtolower($esActual), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(EntidadPeer::ES_ACTUAL, $esActual, $comparison);
    }

    /**
     * Filter the query on the DIRECTORIO_NAME column
     *
     * Example usage:
     * <code>
     * $query->filterByDirectorioName('fooValue');   // WHERE DIRECTORIO_NAME = 'fooValue'
     * $query->filterByDirectorioName('%fooValue%'); // WHERE DIRECTORIO_NAME LIKE '%fooValue%'
     * </code>
     *
     * @param     string $directorioName The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByDirectorioName($directorioName = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($directorioName)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $directorioName)) {
                $directorioName = str_replace('*', '%', $directorioName);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EntidadPeer::DIRECTORIO_NAME, $directorioName, $comparison);
    }

    /**
     * Filter the query on the CODIGO column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE CODIGO = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE CODIGO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EntidadPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the USAR_MEMBRETE column
     *
     * Example usage:
     * <code>
     * $query->filterByUsarMembrete(true); // WHERE USAR_MEMBRETE = true
     * $query->filterByUsarMembrete('yes'); // WHERE USAR_MEMBRETE = true
     * </code>
     *
     * @param     boolean|string $usarMembrete The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByUsarMembrete($usarMembrete = null, $comparison = null)
    {
        if (is_string($usarMembrete)) {
            $usarMembrete = in_array(strtolower($usarMembrete), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(EntidadPeer::USAR_MEMBRETE, $usarMembrete, $comparison);
    }

    /**
     * Filter the query on the LOGO_CORPORATIVO column
     *
     * Example usage:
     * <code>
     * $query->filterByLogoCorporativo('fooValue');   // WHERE LOGO_CORPORATIVO = 'fooValue'
     * $query->filterByLogoCorporativo('%fooValue%'); // WHERE LOGO_CORPORATIVO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $logoCorporativo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByLogoCorporativo($logoCorporativo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($logoCorporativo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $logoCorporativo)) {
                $logoCorporativo = str_replace('*', '%', $logoCorporativo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EntidadPeer::LOGO_CORPORATIVO, $logoCorporativo, $comparison);
    }

    /**
     * Filter the query on the LOGO_HEADER column
     *
     * Example usage:
     * <code>
     * $query->filterByLogoHeader('fooValue');   // WHERE LOGO_HEADER = 'fooValue'
     * $query->filterByLogoHeader('%fooValue%'); // WHERE LOGO_HEADER LIKE '%fooValue%'
     * </code>
     *
     * @param     string $logoHeader The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function filterByLogoHeader($logoHeader = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($logoHeader)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $logoHeader)) {
                $logoHeader = str_replace('*', '%', $logoHeader);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EntidadPeer::LOGO_HEADER, $logoHeader, $comparison);
    }

    /**
     * Filter the query by a related DirectorioExterno object
     *
     * @param   DirectorioExterno|PropelObjectCollection $directorioExterno  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDirectorioExterno($directorioExterno, $comparison = null)
    {
        if ($directorioExterno instanceof DirectorioExterno) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $directorioExterno->getEntidadId(), $comparison);
        } elseif ($directorioExterno instanceof PropelObjectCollection) {
            return $this
                ->useDirectorioExternoQuery()
                ->filterByPrimaryKeys($directorioExterno->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByDirectorioExterno() only accepts arguments of type DirectorioExterno or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DirectorioExterno relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinDirectorioExterno($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DirectorioExterno');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DirectorioExterno');
        }

        return $this;
    }

    /**
     * Use the DirectorioExterno relation DirectorioExterno object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DirectorioExternoQuery A secondary query class using the current class as primary query
     */
    public function useDirectorioExternoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDirectorioExterno($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DirectorioExterno', 'DirectorioExternoQuery');
    }

    /**
     * Filter the query by a related DirectorioExternoHist object
     *
     * @param   DirectorioExternoHist|PropelObjectCollection $directorioExternoHist  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDirectorioExternoHist($directorioExternoHist, $comparison = null)
    {
        if ($directorioExternoHist instanceof DirectorioExternoHist) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $directorioExternoHist->getEntidadId(), $comparison);
        } elseif ($directorioExternoHist instanceof PropelObjectCollection) {
            return $this
                ->useDirectorioExternoHistQuery()
                ->filterByPrimaryKeys($directorioExternoHist->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByDirectorioExternoHist() only accepts arguments of type DirectorioExternoHist or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DirectorioExternoHist relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinDirectorioExternoHist($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DirectorioExternoHist');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DirectorioExternoHist');
        }

        return $this;
    }

    /**
     * Use the DirectorioExternoHist relation DirectorioExternoHist object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DirectorioExternoHistQuery A secondary query class using the current class as primary query
     */
    public function useDirectorioExternoHistQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDirectorioExternoHist($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DirectorioExternoHist', 'DirectorioExternoHistQuery');
    }

    /**
     * Filter the query by a related Proveedor object
     *
     * @param   Proveedor|PropelObjectCollection $proveedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByProveedor($proveedor, $comparison = null)
    {
        if ($proveedor instanceof Proveedor) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $proveedor->getEntidadId(), $comparison);
        } elseif ($proveedor instanceof PropelObjectCollection) {
            return $this
                ->useProveedorQuery()
                ->filterByPrimaryKeys($proveedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProveedor() only accepts arguments of type Proveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Proveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Proveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Proveedor');
        }

        return $this;
    }

    /**
     * Use the Proveedor relation Proveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProveedorQuery A secondary query class using the current class as primary query
     */
    public function useProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Proveedor', 'ProveedorQuery');
    }

    /**
     * Filter the query by a related Regional object
     *
     * @param   Regional|PropelObjectCollection $regional  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRegional($regional, $comparison = null)
    {
        if ($regional instanceof Regional) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $regional->getEntidadId(), $comparison);
        } elseif ($regional instanceof PropelObjectCollection) {
            return $this
                ->useRegionalQuery()
                ->filterByPrimaryKeys($regional->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRegional() only accepts arguments of type Regional or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Regional relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinRegional($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Regional');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Regional');
        }

        return $this;
    }

    /**
     * Use the Regional relation Regional object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RegionalQuery A secondary query class using the current class as primary query
     */
    public function useRegionalQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinRegional($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Regional', 'RegionalQuery');
    }

    /**
     * Filter the query by a related Rol object
     *
     * @param   Rol|PropelObjectCollection $rol  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRol($rol, $comparison = null)
    {
        if ($rol instanceof Rol) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $rol->getEntidadId(), $comparison);
        } elseif ($rol instanceof PropelObjectCollection) {
            return $this
                ->useRolQuery()
                ->filterByPrimaryKeys($rol->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRol() only accepts arguments of type Rol or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Rol relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinRol($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Rol');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Rol');
        }

        return $this;
    }

    /**
     * Use the Rol relation Rol object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RolQuery A secondary query class using the current class as primary query
     */
    public function useRolQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRol($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Rol', 'RolQuery');
    }

    /**
     * Filter the query by a related TipoServicio object
     *
     * @param   TipoServicio|PropelObjectCollection $tipoServicio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTipoServicio($tipoServicio, $comparison = null)
    {
        if ($tipoServicio instanceof TipoServicio) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $tipoServicio->getEntidadId(), $comparison);
        } elseif ($tipoServicio instanceof PropelObjectCollection) {
            return $this
                ->useTipoServicioQuery()
                ->filterByPrimaryKeys($tipoServicio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTipoServicio() only accepts arguments of type TipoServicio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoServicio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinTipoServicio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoServicio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoServicio');
        }

        return $this;
    }

    /**
     * Use the TipoServicio relation TipoServicio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoServicioQuery A secondary query class using the current class as primary query
     */
    public function useTipoServicioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoServicio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoServicio', 'TipoServicioQuery');
    }

    /**
     * Filter the query by a related Dependencia object
     *
     * @param   Dependencia|PropelObjectCollection $dependencia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EntidadQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByDependencia($dependencia, $comparison = null)
    {
        if ($dependencia instanceof Dependencia) {
            return $this
                ->addUsingAlias(EntidadPeer::ENTIDAD_ID, $dependencia->getEntidadId(), $comparison);
        } elseif ($dependencia instanceof PropelObjectCollection) {
            return $this
                ->useDependenciaQuery()
                ->filterByPrimaryKeys($dependencia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByDependencia() only accepts arguments of type Dependencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Dependencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function joinDependencia($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Dependencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Dependencia');
        }

        return $this;
    }

    /**
     * Use the Dependencia relation Dependencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DependenciaQuery A secondary query class using the current class as primary query
     */
    public function useDependenciaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinDependencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Dependencia', 'DependenciaQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Entidad $entidad Object to remove from the list of results
     *
     * @return EntidadQuery The current query, for fluid interface
     */
    public function prune($entidad = null)
    {
        if ($entidad) {
            $this->addUsingAlias(EntidadPeer::ENTIDAD_ID, $entidad->getEntidadId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

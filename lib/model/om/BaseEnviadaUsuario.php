<?php


/**
 * Base class that represents a row from the 'ENVIADA_USUARIO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:44
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEnviadaUsuario extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'EnviadaUsuarioPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        EnviadaUsuarioPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the enviadausuario_id field.
     * @var        int
     */
    protected $enviadausuario_id;

    /**
     * The value for the estadocomenviada_id field.
     * @var        int
     */
    protected $estadocomenviada_id;

    /**
     * The value for the usuario_id field.
     * @var        int
     */
    protected $usuario_id;

    /**
     * The value for the comenviada_id field.
     * @var        int
     */
    protected $comenviada_id;

    /**
     * The value for the roluscomenviada_id field.
     * @var        int
     */
    protected $roluscomenviada_id;

    /**
     * The value for the cargousuario_id field.
     * @var        int
     */
    protected $cargousuario_id;

    /**
     * The value for the fecha_acceso field.
     * @var        string
     */
    protected $fecha_acceso;

    /**
     * The value for the esta_asignada field.
     * @var        boolean
     */
    protected $esta_asignada;

    /**
     * The value for the fecha_asigna field.
     * @var        string
     */
    protected $fecha_asigna;

    /**
     * The value for the tipoprocesocom_id field.
     * @var        int
     */
    protected $tipoprocesocom_id;

    /**
     * The value for the fecha_aprueba field.
     * @var        string
     */
    protected $fecha_aprueba;

    /**
     * The value for the firma_aprueba field.
     * Note: this column has a database default value of: 0
     * @var        int
     */
    protected $firma_aprueba;

    /**
     * The value for the nota_aprueba field.
     * @var        string
     */
    protected $nota_aprueba;

    /**
     * @var        EstadoComEnviada
     */
    protected $aEstadoComEnviada;

    /**
     * @var        Usuario
     */
    protected $aUsuario;

    /**
     * @var        ComEnviada
     */
    protected $aComEnviada;

    /**
     * @var        RolusComenviada
     */
    protected $aRolusComenviada;

    /**
     * @var        CargoUsuario
     */
    protected $aCargoUsuario;

    /**
     * @var        TipoProcesoCom
     */
    protected $aTipoProcesoCom;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->firma_aprueba = 0;
    }

    /**
     * Initializes internal state of BaseEnviadaUsuario object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [enviadausuario_id] column value.
     * 
     * @return int
     */
    public function getEnviadausuarioId()
    {

        return $this->enviadausuario_id;
    }

    /**
     * Get the [estadocomenviada_id] column value.
     * 
     * @return int
     */
    public function getEstadocomenviadaId()
    {

        return $this->estadocomenviada_id;
    }

    /**
     * Get the [usuario_id] column value.
     * 
     * @return int
     */
    public function getUsuarioId()
    {

        return $this->usuario_id;
    }

    /**
     * Get the [comenviada_id] column value.
     * 
     * @return int
     */
    public function getComenviadaId()
    {

        return $this->comenviada_id;
    }

    /**
     * Get the [roluscomenviada_id] column value.
     * 
     * @return int
     */
    public function getRoluscomenviadaId()
    {

        return $this->roluscomenviada_id;
    }

    /**
     * Get the [cargousuario_id] column value.
     * 
     * @return int
     */
    public function getCargousuarioId()
    {

        return $this->cargousuario_id;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_acceso] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaAcceso($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_acceso === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_acceso);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_acceso, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [esta_asignada] column value.
     * 
     * @return boolean
     */
    public function getEstaAsignada()
    {

        return $this->esta_asignada;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_asigna] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaAsigna($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_asigna === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_asigna);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_asigna, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [tipoprocesocom_id] column value.
     * 
     * @return int
     */
    public function getTipoprocesocomId()
    {

        return $this->tipoprocesocom_id;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_aprueba] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaAprueba($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_aprueba === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_aprueba);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_aprueba, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [firma_aprueba] column value.
     * 
     * @return int
     */
    public function getFirmaAprueba()
    {

        return $this->firma_aprueba;
    }

    /**
     * Get the [nota_aprueba] column value.
     * 
     * @return string
     */
    public function getNotaAprueba()
    {

        return $this->nota_aprueba;
    }

    /**
     * Set the value of [enviadausuario_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setEnviadausuarioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->enviadausuario_id !== $v) {
            $this->enviadausuario_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::ENVIADAUSUARIO_ID;
        }


        return $this;
    } // setEnviadausuarioId()

    /**
     * Set the value of [estadocomenviada_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setEstadocomenviadaId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->estadocomenviada_id !== $v) {
            $this->estadocomenviada_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::ESTADOCOMENVIADA_ID;
        }

        if ($this->aEstadoComEnviada !== null && $this->aEstadoComEnviada->getEstadocomenviadaId() !== $v) {
            $this->aEstadoComEnviada = null;
        }


        return $this;
    } // setEstadocomenviadaId()

    /**
     * Set the value of [usuario_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setUsuarioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->usuario_id !== $v) {
            $this->usuario_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::USUARIO_ID;
        }

        if ($this->aUsuario !== null && $this->aUsuario->getUsuarioId() !== $v) {
            $this->aUsuario = null;
        }


        return $this;
    } // setUsuarioId()

    /**
     * Set the value of [comenviada_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setComenviadaId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->comenviada_id !== $v) {
            $this->comenviada_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::COMENVIADA_ID;
        }

        if ($this->aComEnviada !== null && $this->aComEnviada->getComenviadaId() !== $v) {
            $this->aComEnviada = null;
        }


        return $this;
    } // setComenviadaId()

    /**
     * Set the value of [roluscomenviada_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setRoluscomenviadaId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->roluscomenviada_id !== $v) {
            $this->roluscomenviada_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::ROLUSCOMENVIADA_ID;
        }

        if ($this->aRolusComenviada !== null && $this->aRolusComenviada->getRoluscomenviadaId() !== $v) {
            $this->aRolusComenviada = null;
        }


        return $this;
    } // setRoluscomenviadaId()

    /**
     * Set the value of [cargousuario_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setCargousuarioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->cargousuario_id !== $v) {
            $this->cargousuario_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::CARGOUSUARIO_ID;
        }

        if ($this->aCargoUsuario !== null && $this->aCargoUsuario->getCargousuarioId() !== $v) {
            $this->aCargoUsuario = null;
        }


        return $this;
    } // setCargousuarioId()

    /**
     * Sets the value of [fecha_acceso] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setFechaAcceso($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_acceso !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_acceso !== null && $tmpDt = new DateTime($this->fecha_acceso)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_acceso = $newDateAsString;
                $this->modifiedColumns[] = EnviadaUsuarioPeer::FECHA_ACCESO;
            }
        } // if either are not null


        return $this;
    } // setFechaAcceso()

    /**
     * Sets the value of the [esta_asignada] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * 
     * @param boolean|integer|string $v The new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setEstaAsignada($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->esta_asignada !== $v) {
            $this->esta_asignada = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::ESTA_ASIGNADA;
        }


        return $this;
    } // setEstaAsignada()

    /**
     * Sets the value of [fecha_asigna] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setFechaAsigna($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_asigna !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_asigna !== null && $tmpDt = new DateTime($this->fecha_asigna)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_asigna = $newDateAsString;
                $this->modifiedColumns[] = EnviadaUsuarioPeer::FECHA_ASIGNA;
            }
        } // if either are not null


        return $this;
    } // setFechaAsigna()

    /**
     * Set the value of [tipoprocesocom_id] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setTipoprocesocomId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->tipoprocesocom_id !== $v) {
            $this->tipoprocesocom_id = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::TIPOPROCESOCOM_ID;
        }

        if ($this->aTipoProcesoCom !== null && $this->aTipoProcesoCom->getTipoprocesocomId() !== $v) {
            $this->aTipoProcesoCom = null;
        }


        return $this;
    } // setTipoprocesocomId()

    /**
     * Sets the value of [fecha_aprueba] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setFechaAprueba($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_aprueba !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_aprueba !== null && $tmpDt = new DateTime($this->fecha_aprueba)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_aprueba = $newDateAsString;
                $this->modifiedColumns[] = EnviadaUsuarioPeer::FECHA_APRUEBA;
            }
        } // if either are not null


        return $this;
    } // setFechaAprueba()

    /**
     * Set the value of [firma_aprueba] column.
     * 
     * @param  int $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setFirmaAprueba($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->firma_aprueba !== $v) {
            $this->firma_aprueba = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::FIRMA_APRUEBA;
        }


        return $this;
    } // setFirmaAprueba()

    /**
     * Set the value of [nota_aprueba] column.
     * 
     * @param  string $v new value
     * @return EnviadaUsuario The current object (for fluent API support)
     */
    public function setNotaAprueba($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nota_aprueba !== $v) {
            $this->nota_aprueba = $v;
            $this->modifiedColumns[] = EnviadaUsuarioPeer::NOTA_APRUEBA;
        }


        return $this;
    } // setNotaAprueba()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->firma_aprueba !== 0) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->enviadausuario_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->estadocomenviada_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->usuario_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->comenviada_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->roluscomenviada_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->cargousuario_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
            $this->fecha_acceso = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->esta_asignada = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
            $this->fecha_asigna = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->tipoprocesocom_id = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
            $this->fecha_aprueba = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->firma_aprueba = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
            $this->nota_aprueba = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 13; // 13 = EnviadaUsuarioPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating EnviadaUsuario object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEstadoComEnviada !== null && $this->estadocomenviada_id !== $this->aEstadoComEnviada->getEstadocomenviadaId()) {
            $this->aEstadoComEnviada = null;
        }
        if ($this->aUsuario !== null && $this->usuario_id !== $this->aUsuario->getUsuarioId()) {
            $this->aUsuario = null;
        }
        if ($this->aComEnviada !== null && $this->comenviada_id !== $this->aComEnviada->getComenviadaId()) {
            $this->aComEnviada = null;
        }
        if ($this->aRolusComenviada !== null && $this->roluscomenviada_id !== $this->aRolusComenviada->getRoluscomenviadaId()) {
            $this->aRolusComenviada = null;
        }
        if ($this->aCargoUsuario !== null && $this->cargousuario_id !== $this->aCargoUsuario->getCargousuarioId()) {
            $this->aCargoUsuario = null;
        }
        if ($this->aTipoProcesoCom !== null && $this->tipoprocesocom_id !== $this->aTipoProcesoCom->getTipoprocesocomId()) {
            $this->aTipoProcesoCom = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EnviadaUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = EnviadaUsuarioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEstadoComEnviada = null;
            $this->aUsuario = null;
            $this->aComEnviada = null;
            $this->aRolusComenviada = null;
            $this->aCargoUsuario = null;
            $this->aTipoProcesoCom = null;
        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EnviadaUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = EnviadaUsuarioQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseEnviadaUsuario:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseEnviadaUsuario:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(EnviadaUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseEnviadaUsuario:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseEnviadaUsuario:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                EnviadaUsuarioPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEstadoComEnviada !== null) {
                if ($this->aEstadoComEnviada->isModified() || $this->aEstadoComEnviada->isNew()) {
                    $affectedRows += $this->aEstadoComEnviada->save($con);
                }
                $this->setEstadoComEnviada($this->aEstadoComEnviada);
            }

            if ($this->aUsuario !== null) {
                if ($this->aUsuario->isModified() || $this->aUsuario->isNew()) {
                    $affectedRows += $this->aUsuario->save($con);
                }
                $this->setUsuario($this->aUsuario);
            }

            if ($this->aComEnviada !== null) {
                if ($this->aComEnviada->isModified() || $this->aComEnviada->isNew()) {
                    $affectedRows += $this->aComEnviada->save($con);
                }
                $this->setComEnviada($this->aComEnviada);
            }

            if ($this->aRolusComenviada !== null) {
                if ($this->aRolusComenviada->isModified() || $this->aRolusComenviada->isNew()) {
                    $affectedRows += $this->aRolusComenviada->save($con);
                }
                $this->setRolusComenviada($this->aRolusComenviada);
            }

            if ($this->aCargoUsuario !== null) {
                if ($this->aCargoUsuario->isModified() || $this->aCargoUsuario->isNew()) {
                    $affectedRows += $this->aCargoUsuario->save($con);
                }
                $this->setCargoUsuario($this->aCargoUsuario);
            }

            if ($this->aTipoProcesoCom !== null) {
                if ($this->aTipoProcesoCom->isModified() || $this->aTipoProcesoCom->isNew()) {
                    $affectedRows += $this->aTipoProcesoCom->save($con);
                }
                $this->setTipoProcesoCom($this->aTipoProcesoCom);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = EnviadaUsuarioPeer::ENVIADAUSUARIO_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(EnviadaUsuarioPeer::ENVIADAUSUARIO_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . EnviadaUsuarioPeer::ENVIADAUSUARIO_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(EnviadaUsuarioPeer::ENVIADAUSUARIO_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setEnviadausuarioId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEstadoComEnviada !== null) {
                if (!$this->aEstadoComEnviada->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEstadoComEnviada->getValidationFailures());
                }
            }

            if ($this->aUsuario !== null) {
                if (!$this->aUsuario->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aUsuario->getValidationFailures());
                }
            }

            if ($this->aComEnviada !== null) {
                if (!$this->aComEnviada->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aComEnviada->getValidationFailures());
                }
            }

            if ($this->aRolusComenviada !== null) {
                if (!$this->aRolusComenviada->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aRolusComenviada->getValidationFailures());
                }
            }

            if ($this->aCargoUsuario !== null) {
                if (!$this->aCargoUsuario->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aCargoUsuario->getValidationFailures());
                }
            }

            if ($this->aTipoProcesoCom !== null) {
                if (!$this->aTipoProcesoCom->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aTipoProcesoCom->getValidationFailures());
                }
            }


            if (($retval = EnviadaUsuarioPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }



            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = EnviadaUsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getEnviadausuarioId();
                break;
            case 1:
                return $this->getEstadocomenviadaId();
                break;
            case 2:
                return $this->getUsuarioId();
                break;
            case 3:
                return $this->getComenviadaId();
                break;
            case 4:
                return $this->getRoluscomenviadaId();
                break;
            case 5:
                return $this->getCargousuarioId();
                break;
            case 6:
                return $this->getFechaAcceso();
                break;
            case 7:
                return $this->getEstaAsignada();
                break;
            case 8:
                return $this->getFechaAsigna();
                break;
            case 9:
                return $this->getTipoprocesocomId();
                break;
            case 10:
                return $this->getFechaAprueba();
                break;
            case 11:
                return $this->getFirmaAprueba();
                break;
            case 12:
                return $this->getNotaAprueba();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['EnviadaUsuario'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['EnviadaUsuario'][$this->getPrimaryKey()] = true;
        $keys = EnviadaUsuarioPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getEnviadausuarioId(),
            $keys[1] => $this->getEstadocomenviadaId(),
            $keys[2] => $this->getUsuarioId(),
            $keys[3] => $this->getComenviadaId(),
            $keys[4] => $this->getRoluscomenviadaId(),
            $keys[5] => $this->getCargousuarioId(),
            $keys[6] => $this->getFechaAcceso(),
            $keys[7] => $this->getEstaAsignada(),
            $keys[8] => $this->getFechaAsigna(),
            $keys[9] => $this->getTipoprocesocomId(),
            $keys[10] => $this->getFechaAprueba(),
            $keys[11] => $this->getFirmaAprueba(),
            $keys[12] => $this->getNotaAprueba(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aEstadoComEnviada) {
                $result['EstadoComEnviada'] = $this->aEstadoComEnviada->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aUsuario) {
                $result['Usuario'] = $this->aUsuario->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aComEnviada) {
                $result['ComEnviada'] = $this->aComEnviada->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aRolusComenviada) {
                $result['RolusComenviada'] = $this->aRolusComenviada->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aCargoUsuario) {
                $result['CargoUsuario'] = $this->aCargoUsuario->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aTipoProcesoCom) {
                $result['TipoProcesoCom'] = $this->aTipoProcesoCom->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = EnviadaUsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setEnviadausuarioId($value);
                break;
            case 1:
                $this->setEstadocomenviadaId($value);
                break;
            case 2:
                $this->setUsuarioId($value);
                break;
            case 3:
                $this->setComenviadaId($value);
                break;
            case 4:
                $this->setRoluscomenviadaId($value);
                break;
            case 5:
                $this->setCargousuarioId($value);
                break;
            case 6:
                $this->setFechaAcceso($value);
                break;
            case 7:
                $this->setEstaAsignada($value);
                break;
            case 8:
                $this->setFechaAsigna($value);
                break;
            case 9:
                $this->setTipoprocesocomId($value);
                break;
            case 10:
                $this->setFechaAprueba($value);
                break;
            case 11:
                $this->setFirmaAprueba($value);
                break;
            case 12:
                $this->setNotaAprueba($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = EnviadaUsuarioPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setEnviadausuarioId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setEstadocomenviadaId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setUsuarioId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setComenviadaId($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setRoluscomenviadaId($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setCargousuarioId($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setFechaAcceso($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setEstaAsignada($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setFechaAsigna($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setTipoprocesocomId($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setFechaAprueba($arr[$keys[10]]);
        if (array_key_exists($keys[11], $arr)) $this->setFirmaAprueba($arr[$keys[11]]);
        if (array_key_exists($keys[12], $arr)) $this->setNotaAprueba($arr[$keys[12]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(EnviadaUsuarioPeer::DATABASE_NAME);

        if ($this->isColumnModified(EnviadaUsuarioPeer::ENVIADAUSUARIO_ID)) $criteria->add(EnviadaUsuarioPeer::ENVIADAUSUARIO_ID, $this->enviadausuario_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::ESTADOCOMENVIADA_ID)) $criteria->add(EnviadaUsuarioPeer::ESTADOCOMENVIADA_ID, $this->estadocomenviada_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::USUARIO_ID)) $criteria->add(EnviadaUsuarioPeer::USUARIO_ID, $this->usuario_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::COMENVIADA_ID)) $criteria->add(EnviadaUsuarioPeer::COMENVIADA_ID, $this->comenviada_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::ROLUSCOMENVIADA_ID)) $criteria->add(EnviadaUsuarioPeer::ROLUSCOMENVIADA_ID, $this->roluscomenviada_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::CARGOUSUARIO_ID)) $criteria->add(EnviadaUsuarioPeer::CARGOUSUARIO_ID, $this->cargousuario_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::FECHA_ACCESO)) $criteria->add(EnviadaUsuarioPeer::FECHA_ACCESO, $this->fecha_acceso);
        if ($this->isColumnModified(EnviadaUsuarioPeer::ESTA_ASIGNADA)) $criteria->add(EnviadaUsuarioPeer::ESTA_ASIGNADA, $this->esta_asignada);
        if ($this->isColumnModified(EnviadaUsuarioPeer::FECHA_ASIGNA)) $criteria->add(EnviadaUsuarioPeer::FECHA_ASIGNA, $this->fecha_asigna);
        if ($this->isColumnModified(EnviadaUsuarioPeer::TIPOPROCESOCOM_ID)) $criteria->add(EnviadaUsuarioPeer::TIPOPROCESOCOM_ID, $this->tipoprocesocom_id);
        if ($this->isColumnModified(EnviadaUsuarioPeer::FECHA_APRUEBA)) $criteria->add(EnviadaUsuarioPeer::FECHA_APRUEBA, $this->fecha_aprueba);
        if ($this->isColumnModified(EnviadaUsuarioPeer::FIRMA_APRUEBA)) $criteria->add(EnviadaUsuarioPeer::FIRMA_APRUEBA, $this->firma_aprueba);
        if ($this->isColumnModified(EnviadaUsuarioPeer::NOTA_APRUEBA)) $criteria->add(EnviadaUsuarioPeer::NOTA_APRUEBA, $this->nota_aprueba);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(EnviadaUsuarioPeer::DATABASE_NAME);
        $criteria->add(EnviadaUsuarioPeer::ENVIADAUSUARIO_ID, $this->enviadausuario_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getEnviadausuarioId();
    }

    /**
     * Generic method to set the primary key (enviadausuario_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setEnviadausuarioId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getEnviadausuarioId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of EnviadaUsuario (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setEstadocomenviadaId($this->getEstadocomenviadaId());
        $copyObj->setUsuarioId($this->getUsuarioId());
        $copyObj->setComenviadaId($this->getComenviadaId());
        $copyObj->setRoluscomenviadaId($this->getRoluscomenviadaId());
        $copyObj->setCargousuarioId($this->getCargousuarioId());
        $copyObj->setFechaAcceso($this->getFechaAcceso());
        $copyObj->setEstaAsignada($this->getEstaAsignada());
        $copyObj->setFechaAsigna($this->getFechaAsigna());
        $copyObj->setTipoprocesocomId($this->getTipoprocesocomId());
        $copyObj->setFechaAprueba($this->getFechaAprueba());
        $copyObj->setFirmaAprueba($this->getFirmaAprueba());
        $copyObj->setNotaAprueba($this->getNotaAprueba());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setEnviadausuarioId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return EnviadaUsuario Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return EnviadaUsuarioPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new EnviadaUsuarioPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a EstadoComEnviada object.
     *
     * @param                  EstadoComEnviada $v
     * @return EnviadaUsuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEstadoComEnviada(EstadoComEnviada $v = null)
    {
        if ($v === null) {
            $this->setEstadocomenviadaId(NULL);
        } else {
            $this->setEstadocomenviadaId($v->getEstadocomenviadaId());
        }

        $this->aEstadoComEnviada = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the EstadoComEnviada object, it will not be re-added.
        if ($v !== null) {
            $v->addEnviadaUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated EstadoComEnviada object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return EstadoComEnviada The associated EstadoComEnviada object.
     * @throws PropelException
     */
    public function getEstadoComEnviada(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEstadoComEnviada === null && ($this->estadocomenviada_id !== null) && $doQuery) {
            $this->aEstadoComEnviada = EstadoComEnviadaQuery::create()->findPk($this->estadocomenviada_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEstadoComEnviada->addEnviadaUsuarios($this);
             */
        }

        return $this->aEstadoComEnviada;
    }

    /**
     * Declares an association between this object and a Usuario object.
     *
     * @param                  Usuario $v
     * @return EnviadaUsuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setUsuario(Usuario $v = null)
    {
        if ($v === null) {
            $this->setUsuarioId(NULL);
        } else {
            $this->setUsuarioId($v->getUsuarioId());
        }

        $this->aUsuario = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Usuario object, it will not be re-added.
        if ($v !== null) {
            $v->addEnviadaUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated Usuario object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Usuario The associated Usuario object.
     * @throws PropelException
     */
    public function getUsuario(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aUsuario === null && ($this->usuario_id !== null) && $doQuery) {
            $this->aUsuario = UsuarioQuery::create()->findPk($this->usuario_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aUsuario->addEnviadaUsuarios($this);
             */
        }

        return $this->aUsuario;
    }

    /**
     * Declares an association between this object and a ComEnviada object.
     *
     * @param                  ComEnviada $v
     * @return EnviadaUsuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setComEnviada(ComEnviada $v = null)
    {
        if ($v === null) {
            $this->setComenviadaId(NULL);
        } else {
            $this->setComenviadaId($v->getComenviadaId());
        }

        $this->aComEnviada = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the ComEnviada object, it will not be re-added.
        if ($v !== null) {
            $v->addEnviadaUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated ComEnviada object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return ComEnviada The associated ComEnviada object.
     * @throws PropelException
     */
    public function getComEnviada(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aComEnviada === null && ($this->comenviada_id !== null) && $doQuery) {
            $this->aComEnviada = ComEnviadaQuery::create()->findPk($this->comenviada_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aComEnviada->addEnviadaUsuarios($this);
             */
        }

        return $this->aComEnviada;
    }

    /**
     * Declares an association between this object and a RolusComenviada object.
     *
     * @param                  RolusComenviada $v
     * @return EnviadaUsuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setRolusComenviada(RolusComenviada $v = null)
    {
        if ($v === null) {
            $this->setRoluscomenviadaId(NULL);
        } else {
            $this->setRoluscomenviadaId($v->getRoluscomenviadaId());
        }

        $this->aRolusComenviada = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the RolusComenviada object, it will not be re-added.
        if ($v !== null) {
            $v->addEnviadaUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated RolusComenviada object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return RolusComenviada The associated RolusComenviada object.
     * @throws PropelException
     */
    public function getRolusComenviada(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aRolusComenviada === null && ($this->roluscomenviada_id !== null) && $doQuery) {
            $this->aRolusComenviada = RolusComenviadaQuery::create()->findPk($this->roluscomenviada_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aRolusComenviada->addEnviadaUsuarios($this);
             */
        }

        return $this->aRolusComenviada;
    }

    /**
     * Declares an association between this object and a CargoUsuario object.
     *
     * @param                  CargoUsuario $v
     * @return EnviadaUsuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setCargoUsuario(CargoUsuario $v = null)
    {
        if ($v === null) {
            $this->setCargousuarioId(NULL);
        } else {
            $this->setCargousuarioId($v->getCargousuarioId());
        }

        $this->aCargoUsuario = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the CargoUsuario object, it will not be re-added.
        if ($v !== null) {
            $v->addEnviadaUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated CargoUsuario object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return CargoUsuario The associated CargoUsuario object.
     * @throws PropelException
     */
    public function getCargoUsuario(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aCargoUsuario === null && ($this->cargousuario_id !== null) && $doQuery) {
            $this->aCargoUsuario = CargoUsuarioQuery::create()->findPk($this->cargousuario_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aCargoUsuario->addEnviadaUsuarios($this);
             */
        }

        return $this->aCargoUsuario;
    }

    /**
     * Declares an association between this object and a TipoProcesoCom object.
     *
     * @param                  TipoProcesoCom $v
     * @return EnviadaUsuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setTipoProcesoCom(TipoProcesoCom $v = null)
    {
        if ($v === null) {
            $this->setTipoprocesocomId(NULL);
        } else {
            $this->setTipoprocesocomId($v->getTipoprocesocomId());
        }

        $this->aTipoProcesoCom = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the TipoProcesoCom object, it will not be re-added.
        if ($v !== null) {
            $v->addEnviadaUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated TipoProcesoCom object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return TipoProcesoCom The associated TipoProcesoCom object.
     * @throws PropelException
     */
    public function getTipoProcesoCom(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aTipoProcesoCom === null && ($this->tipoprocesocom_id !== null) && $doQuery) {
            $this->aTipoProcesoCom = TipoProcesoComQuery::create()->findPk($this->tipoprocesocom_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aTipoProcesoCom->addEnviadaUsuarios($this);
             */
        }

        return $this->aTipoProcesoCom;
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->enviadausuario_id = null;
        $this->estadocomenviada_id = null;
        $this->usuario_id = null;
        $this->comenviada_id = null;
        $this->roluscomenviada_id = null;
        $this->cargousuario_id = null;
        $this->fecha_acceso = null;
        $this->esta_asignada = null;
        $this->fecha_asigna = null;
        $this->tipoprocesocom_id = null;
        $this->fecha_aprueba = null;
        $this->firma_aprueba = null;
        $this->nota_aprueba = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->aEstadoComEnviada instanceof Persistent) {
              $this->aEstadoComEnviada->clearAllReferences($deep);
            }
            if ($this->aUsuario instanceof Persistent) {
              $this->aUsuario->clearAllReferences($deep);
            }
            if ($this->aComEnviada instanceof Persistent) {
              $this->aComEnviada->clearAllReferences($deep);
            }
            if ($this->aRolusComenviada instanceof Persistent) {
              $this->aRolusComenviada->clearAllReferences($deep);
            }
            if ($this->aCargoUsuario instanceof Persistent) {
              $this->aCargoUsuario->clearAllReferences($deep);
            }
            if ($this->aTipoProcesoCom instanceof Persistent) {
              $this->aTipoProcesoCom->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        $this->aEstadoComEnviada = null;
        $this->aUsuario = null;
        $this->aComEnviada = null;
        $this->aRolusComenviada = null;
        $this->aCargoUsuario = null;
        $this->aTipoProcesoCom = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(EnviadaUsuarioPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseEnviadaUsuario:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

<?php


/**
 * Base class that represents a row from the 'TIPO_NOTIFICACION' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:54
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTipoNotificacion extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'TipoNotificacionPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        TipoNotificacionPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the tiponotificacion_id field.
     * @var        int
     */
    protected $tiponotificacion_id;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * The value for the esta_activa field.
     * @var        int
     */
    protected $esta_activa;

    /**
     * The value for the mensaje_body field.
     * @var        string
     */
    protected $mensaje_body;

    /**
     * The value for the modulo_id field.
     * @var        int
     */
    protected $modulo_id;

    /**
     * The value for the codigo field.
     * @var        string
     */
    protected $codigo;

    /**
     * @var        Modulo
     */
    protected $aModulo;

    /**
     * @var        PropelObjectCollection|UsuarioNotificacion[] Collection to store aggregation of UsuarioNotificacion objects.
     */
    protected $collUsuarioNotificacions;
    protected $collUsuarioNotificacionsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuarioNotificacionsScheduledForDeletion = null;

    /**
     * Get the [tiponotificacion_id] column value.
     * 
     * @return int
     */
    public function getTiponotificacionId()
    {

        return $this->tiponotificacion_id;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Get the [esta_activa] column value.
     * 
     * @return int
     */
    public function getEstaActiva()
    {

        return $this->esta_activa;
    }

    /**
     * Get the [mensaje_body] column value.
     * 
     * @return string
     */
    public function getMensajeBody()
    {

        return $this->mensaje_body;
    }

    /**
     * Get the [modulo_id] column value.
     * 
     * @return int
     */
    public function getModuloId()
    {

        return $this->modulo_id;
    }

    /**
     * Get the [codigo] column value.
     * 
     * @return string
     */
    public function getCodigo()
    {

        return $this->codigo;
    }

    /**
     * Set the value of [tiponotificacion_id] column.
     * 
     * @param  int $v new value
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setTiponotificacionId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->tiponotificacion_id !== $v) {
            $this->tiponotificacion_id = $v;
            $this->modifiedColumns[] = TipoNotificacionPeer::TIPONOTIFICACION_ID;
        }


        return $this;
    } // setTiponotificacionId()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = TipoNotificacionPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Set the value of [esta_activa] column.
     * 
     * @param  int $v new value
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setEstaActiva($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->esta_activa !== $v) {
            $this->esta_activa = $v;
            $this->modifiedColumns[] = TipoNotificacionPeer::ESTA_ACTIVA;
        }


        return $this;
    } // setEstaActiva()

    /**
     * Set the value of [mensaje_body] column.
     * 
     * @param  string $v new value
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setMensajeBody($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->mensaje_body !== $v) {
            $this->mensaje_body = $v;
            $this->modifiedColumns[] = TipoNotificacionPeer::MENSAJE_BODY;
        }


        return $this;
    } // setMensajeBody()

    /**
     * Set the value of [modulo_id] column.
     * 
     * @param  int $v new value
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setModuloId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->modulo_id !== $v) {
            $this->modulo_id = $v;
            $this->modifiedColumns[] = TipoNotificacionPeer::MODULO_ID;
        }

        if ($this->aModulo !== null && $this->aModulo->getModuloId() !== $v) {
            $this->aModulo = null;
        }


        return $this;
    } // setModuloId()

    /**
     * Set the value of [codigo] column.
     * 
     * @param  string $v new value
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setCodigo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->codigo !== $v) {
            $this->codigo = $v;
            $this->modifiedColumns[] = TipoNotificacionPeer::CODIGO;
        }


        return $this;
    } // setCodigo()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->tiponotificacion_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->descripcion = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->esta_activa = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->mensaje_body = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->modulo_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->codigo = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 6; // 6 = TipoNotificacionPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating TipoNotificacion object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aModulo !== null && $this->modulo_id !== $this->aModulo->getModuloId()) {
            $this->aModulo = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(TipoNotificacionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = TipoNotificacionPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aModulo = null;
            $this->collUsuarioNotificacions = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(TipoNotificacionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = TipoNotificacionQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseTipoNotificacion:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseTipoNotificacion:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(TipoNotificacionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseTipoNotificacion:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseTipoNotificacion:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                TipoNotificacionPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aModulo !== null) {
                if ($this->aModulo->isModified() || $this->aModulo->isNew()) {
                    $affectedRows += $this->aModulo->save($con);
                }
                $this->setModulo($this->aModulo);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->usuarioNotificacionsScheduledForDeletion !== null) {
                if (!$this->usuarioNotificacionsScheduledForDeletion->isEmpty()) {
                    UsuarioNotificacionQuery::create()
                        ->filterByPrimaryKeys($this->usuarioNotificacionsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->usuarioNotificacionsScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarioNotificacions !== null) {
                foreach ($this->collUsuarioNotificacions as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = TipoNotificacionPeer::TIPONOTIFICACION_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(TipoNotificacionPeer::TIPONOTIFICACION_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . TipoNotificacionPeer::TIPONOTIFICACION_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(TipoNotificacionPeer::TIPONOTIFICACION_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setTiponotificacionId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aModulo !== null) {
                if (!$this->aModulo->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aModulo->getValidationFailures());
                }
            }


            if (($retval = TipoNotificacionPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collUsuarioNotificacions !== null) {
                    foreach ($this->collUsuarioNotificacions as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = TipoNotificacionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getTiponotificacionId();
                break;
            case 1:
                return $this->getDescripcion();
                break;
            case 2:
                return $this->getEstaActiva();
                break;
            case 3:
                return $this->getMensajeBody();
                break;
            case 4:
                return $this->getModuloId();
                break;
            case 5:
                return $this->getCodigo();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['TipoNotificacion'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['TipoNotificacion'][$this->getPrimaryKey()] = true;
        $keys = TipoNotificacionPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getTiponotificacionId(),
            $keys[1] => $this->getDescripcion(),
            $keys[2] => $this->getEstaActiva(),
            $keys[3] => $this->getMensajeBody(),
            $keys[4] => $this->getModuloId(),
            $keys[5] => $this->getCodigo(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aModulo) {
                $result['Modulo'] = $this->aModulo->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collUsuarioNotificacions) {
                $result['UsuarioNotificacions'] = $this->collUsuarioNotificacions->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = TipoNotificacionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setTiponotificacionId($value);
                break;
            case 1:
                $this->setDescripcion($value);
                break;
            case 2:
                $this->setEstaActiva($value);
                break;
            case 3:
                $this->setMensajeBody($value);
                break;
            case 4:
                $this->setModuloId($value);
                break;
            case 5:
                $this->setCodigo($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = TipoNotificacionPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setTiponotificacionId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setDescripcion($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setEstaActiva($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setMensajeBody($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setModuloId($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setCodigo($arr[$keys[5]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(TipoNotificacionPeer::DATABASE_NAME);

        if ($this->isColumnModified(TipoNotificacionPeer::TIPONOTIFICACION_ID)) $criteria->add(TipoNotificacionPeer::TIPONOTIFICACION_ID, $this->tiponotificacion_id);
        if ($this->isColumnModified(TipoNotificacionPeer::DESCRIPCION)) $criteria->add(TipoNotificacionPeer::DESCRIPCION, $this->descripcion);
        if ($this->isColumnModified(TipoNotificacionPeer::ESTA_ACTIVA)) $criteria->add(TipoNotificacionPeer::ESTA_ACTIVA, $this->esta_activa);
        if ($this->isColumnModified(TipoNotificacionPeer::MENSAJE_BODY)) $criteria->add(TipoNotificacionPeer::MENSAJE_BODY, $this->mensaje_body);
        if ($this->isColumnModified(TipoNotificacionPeer::MODULO_ID)) $criteria->add(TipoNotificacionPeer::MODULO_ID, $this->modulo_id);
        if ($this->isColumnModified(TipoNotificacionPeer::CODIGO)) $criteria->add(TipoNotificacionPeer::CODIGO, $this->codigo);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(TipoNotificacionPeer::DATABASE_NAME);
        $criteria->add(TipoNotificacionPeer::TIPONOTIFICACION_ID, $this->tiponotificacion_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getTiponotificacionId();
    }

    /**
     * Generic method to set the primary key (tiponotificacion_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setTiponotificacionId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getTiponotificacionId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of TipoNotificacion (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setDescripcion($this->getDescripcion());
        $copyObj->setEstaActiva($this->getEstaActiva());
        $copyObj->setMensajeBody($this->getMensajeBody());
        $copyObj->setModuloId($this->getModuloId());
        $copyObj->setCodigo($this->getCodigo());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getUsuarioNotificacions() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuarioNotificacion($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setTiponotificacionId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return TipoNotificacion Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return TipoNotificacionPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new TipoNotificacionPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Modulo object.
     *
     * @param                  Modulo $v
     * @return TipoNotificacion The current object (for fluent API support)
     * @throws PropelException
     */
    public function setModulo(Modulo $v = null)
    {
        if ($v === null) {
            $this->setModuloId(NULL);
        } else {
            $this->setModuloId($v->getModuloId());
        }

        $this->aModulo = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Modulo object, it will not be re-added.
        if ($v !== null) {
            $v->addTipoNotificacion($this);
        }


        return $this;
    }


    /**
     * Get the associated Modulo object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Modulo The associated Modulo object.
     * @throws PropelException
     */
    public function getModulo(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aModulo === null && ($this->modulo_id !== null) && $doQuery) {
            $this->aModulo = ModuloQuery::create()->findPk($this->modulo_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aModulo->addTipoNotificacions($this);
             */
        }

        return $this->aModulo;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('UsuarioNotificacion' == $relationName) {
            $this->initUsuarioNotificacions();
        }
    }

    /**
     * Clears out the collUsuarioNotificacions collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return TipoNotificacion The current object (for fluent API support)
     * @see        addUsuarioNotificacions()
     */
    public function clearUsuarioNotificacions()
    {
        $this->collUsuarioNotificacions = null; // important to set this to null since that means it is uninitialized
        $this->collUsuarioNotificacionsPartial = null;

        return $this;
    }

    /**
     * reset is the collUsuarioNotificacions collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarioNotificacions($v = true)
    {
        $this->collUsuarioNotificacionsPartial = $v;
    }

    /**
     * Initializes the collUsuarioNotificacions collection.
     *
     * By default this just sets the collUsuarioNotificacions collection to an empty array (like clearcollUsuarioNotificacions());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarioNotificacions($overrideExisting = true)
    {
        if (null !== $this->collUsuarioNotificacions && !$overrideExisting) {
            return;
        }
        $this->collUsuarioNotificacions = new PropelObjectCollection();
        $this->collUsuarioNotificacions->setModel('UsuarioNotificacion');
    }

    /**
     * Gets an array of UsuarioNotificacion objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this TipoNotificacion is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UsuarioNotificacion[] List of UsuarioNotificacion objects
     * @throws PropelException
     */
    public function getUsuarioNotificacions($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioNotificacionsPartial && !$this->isNew();
        if (null === $this->collUsuarioNotificacions || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarioNotificacions) {
                // return empty collection
                $this->initUsuarioNotificacions();
            } else {
                $collUsuarioNotificacions = UsuarioNotificacionQuery::create(null, $criteria)
                    ->filterByTipoNotificacion($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuarioNotificacionsPartial && count($collUsuarioNotificacions)) {
                      $this->initUsuarioNotificacions(false);

                      foreach ($collUsuarioNotificacions as $obj) {
                        if (false == $this->collUsuarioNotificacions->contains($obj)) {
                          $this->collUsuarioNotificacions->append($obj);
                        }
                      }

                      $this->collUsuarioNotificacionsPartial = true;
                    }

                    $collUsuarioNotificacions->getInternalIterator()->rewind();

                    return $collUsuarioNotificacions;
                }

                if ($partial && $this->collUsuarioNotificacions) {
                    foreach ($this->collUsuarioNotificacions as $obj) {
                        if ($obj->isNew()) {
                            $collUsuarioNotificacions[] = $obj;
                        }
                    }
                }

                $this->collUsuarioNotificacions = $collUsuarioNotificacions;
                $this->collUsuarioNotificacionsPartial = false;
            }
        }

        return $this->collUsuarioNotificacions;
    }

    /**
     * Sets a collection of UsuarioNotificacion objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarioNotificacions A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function setUsuarioNotificacions(PropelCollection $usuarioNotificacions, PropelPDO $con = null)
    {
        $usuarioNotificacionsToDelete = $this->getUsuarioNotificacions(new Criteria(), $con)->diff($usuarioNotificacions);


        $this->usuarioNotificacionsScheduledForDeletion = $usuarioNotificacionsToDelete;

        foreach ($usuarioNotificacionsToDelete as $usuarioNotificacionRemoved) {
            $usuarioNotificacionRemoved->setTipoNotificacion(null);
        }

        $this->collUsuarioNotificacions = null;
        foreach ($usuarioNotificacions as $usuarioNotificacion) {
            $this->addUsuarioNotificacion($usuarioNotificacion);
        }

        $this->collUsuarioNotificacions = $usuarioNotificacions;
        $this->collUsuarioNotificacionsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related UsuarioNotificacion objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UsuarioNotificacion objects.
     * @throws PropelException
     */
    public function countUsuarioNotificacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioNotificacionsPartial && !$this->isNew();
        if (null === $this->collUsuarioNotificacions || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarioNotificacions) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getUsuarioNotificacions());
            }
            $query = UsuarioNotificacionQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByTipoNotificacion($this)
                ->count($con);
        }

        return count($this->collUsuarioNotificacions);
    }

    /**
     * Method called to associate a UsuarioNotificacion object to this object
     * through the UsuarioNotificacion foreign key attribute.
     *
     * @param    UsuarioNotificacion $l UsuarioNotificacion
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function addUsuarioNotificacion(UsuarioNotificacion $l)
    {
        if ($this->collUsuarioNotificacions === null) {
            $this->initUsuarioNotificacions();
            $this->collUsuarioNotificacionsPartial = true;
        }

        if (!in_array($l, $this->collUsuarioNotificacions->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddUsuarioNotificacion($l);

            if ($this->usuarioNotificacionsScheduledForDeletion and $this->usuarioNotificacionsScheduledForDeletion->contains($l)) {
                $this->usuarioNotificacionsScheduledForDeletion->remove($this->usuarioNotificacionsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	UsuarioNotificacion $usuarioNotificacion The usuarioNotificacion object to add.
     */
    protected function doAddUsuarioNotificacion($usuarioNotificacion)
    {
        $this->collUsuarioNotificacions[]= $usuarioNotificacion;
        $usuarioNotificacion->setTipoNotificacion($this);
    }

    /**
     * @param	UsuarioNotificacion $usuarioNotificacion The usuarioNotificacion object to remove.
     * @return TipoNotificacion The current object (for fluent API support)
     */
    public function removeUsuarioNotificacion($usuarioNotificacion)
    {
        if ($this->getUsuarioNotificacions()->contains($usuarioNotificacion)) {
            $this->collUsuarioNotificacions->remove($this->collUsuarioNotificacions->search($usuarioNotificacion));
            if (null === $this->usuarioNotificacionsScheduledForDeletion) {
                $this->usuarioNotificacionsScheduledForDeletion = clone $this->collUsuarioNotificacions;
                $this->usuarioNotificacionsScheduledForDeletion->clear();
            }
            $this->usuarioNotificacionsScheduledForDeletion[]= clone $usuarioNotificacion;
            $usuarioNotificacion->setTipoNotificacion(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this TipoNotificacion is new, it will return
     * an empty collection; or if this TipoNotificacion has previously
     * been saved, it will retrieve related UsuarioNotificacions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in TipoNotificacion.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioNotificacion[] List of UsuarioNotificacion objects
     */
    public function getUsuarioNotificacionsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioNotificacionQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getUsuarioNotificacions($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->tiponotificacion_id = null;
        $this->descripcion = null;
        $this->esta_activa = null;
        $this->mensaje_body = null;
        $this->modulo_id = null;
        $this->codigo = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collUsuarioNotificacions) {
                foreach ($this->collUsuarioNotificacions as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aModulo instanceof Persistent) {
              $this->aModulo->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collUsuarioNotificacions instanceof PropelCollection) {
            $this->collUsuarioNotificacions->clearIterator();
        }
        $this->collUsuarioNotificacions = null;
        $this->aModulo = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(TipoNotificacionPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseTipoNotificacion:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

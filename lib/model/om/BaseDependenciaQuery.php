<?php


/**
 * Base class that represents a query for the 'DEPENDENCIA' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:54:09
 *
 * @method DependenciaQuery orderByDependenciaId($order = Criteria::ASC) Order by the DEPENDENCIA_ID column
 * @method DependenciaQuery orderByEntidadId($order = Criteria::ASC) Order by the ENTIDAD_ID column
 * @method DependenciaQuery orderByOficinaproductoraId($order = Criteria::ASC) Order by the OFICINAPRODUCTORA_ID column
 * @method DependenciaQuery orderByNombre($order = Criteria::ASC) Order by the NOMBRE column
 * @method DependenciaQuery orderByCodigo($order = Criteria::ASC) Order by the CODIGO column
 * @method DependenciaQuery orderByTiempoPrestamo($order = Criteria::ASC) Order by the TIEMPO_PRESTAMO column
 * @method DependenciaQuery orderByEsActual($order = Criteria::ASC) Order by the ES_ACTUAL column
 * @method DependenciaQuery orderByTipoTabla($order = Criteria::ASC) Order by the TIPO_TABLA column
 * @method DependenciaQuery orderBySubfondoId($order = Criteria::ASC) Order by the SUBFONDO_ID column
 * @method DependenciaQuery orderBySeccionId($order = Criteria::ASC) Order by the SECCION_ID column
 * @method DependenciaQuery orderByVersionInst($order = Criteria::ASC) Order by the VERSION_INST column
 *
 * @method DependenciaQuery groupByDependenciaId() Group by the DEPENDENCIA_ID column
 * @method DependenciaQuery groupByEntidadId() Group by the ENTIDAD_ID column
 * @method DependenciaQuery groupByOficinaproductoraId() Group by the OFICINAPRODUCTORA_ID column
 * @method DependenciaQuery groupByNombre() Group by the NOMBRE column
 * @method DependenciaQuery groupByCodigo() Group by the CODIGO column
 * @method DependenciaQuery groupByTiempoPrestamo() Group by the TIEMPO_PRESTAMO column
 * @method DependenciaQuery groupByEsActual() Group by the ES_ACTUAL column
 * @method DependenciaQuery groupByTipoTabla() Group by the TIPO_TABLA column
 * @method DependenciaQuery groupBySubfondoId() Group by the SUBFONDO_ID column
 * @method DependenciaQuery groupBySeccionId() Group by the SECCION_ID column
 * @method DependenciaQuery groupByVersionInst() Group by the VERSION_INST column
 *
 * @method DependenciaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method DependenciaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method DependenciaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method DependenciaQuery leftJoinEntidad($relationAlias = null) Adds a LEFT JOIN clause to the query using the Entidad relation
 * @method DependenciaQuery rightJoinEntidad($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Entidad relation
 * @method DependenciaQuery innerJoinEntidad($relationAlias = null) Adds a INNER JOIN clause to the query using the Entidad relation
 *
 * @method DependenciaQuery leftJoinOficinaProductora($relationAlias = null) Adds a LEFT JOIN clause to the query using the OficinaProductora relation
 * @method DependenciaQuery rightJoinOficinaProductora($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OficinaProductora relation
 * @method DependenciaQuery innerJoinOficinaProductora($relationAlias = null) Adds a INNER JOIN clause to the query using the OficinaProductora relation
 *
 * @method DependenciaQuery leftJoinReceptor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Receptor relation
 * @method DependenciaQuery rightJoinReceptor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Receptor relation
 * @method DependenciaQuery innerJoinReceptor($relationAlias = null) Adds a INNER JOIN clause to the query using the Receptor relation
 *
 * @method DependenciaQuery leftJoinReceptorComunicaciones($relationAlias = null) Adds a LEFT JOIN clause to the query using the ReceptorComunicaciones relation
 * @method DependenciaQuery rightJoinReceptorComunicaciones($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ReceptorComunicaciones relation
 * @method DependenciaQuery innerJoinReceptorComunicaciones($relationAlias = null) Adds a INNER JOIN clause to the query using the ReceptorComunicaciones relation
 *
 * @method DependenciaQuery leftJoinCargoUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the CargoUsuario relation
 * @method DependenciaQuery rightJoinCargoUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CargoUsuario relation
 * @method DependenciaQuery innerJoinCargoUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the CargoUsuario relation
 *
 * @method DependenciaQuery leftJoinSerie($relationAlias = null) Adds a LEFT JOIN clause to the query using the Serie relation
 * @method DependenciaQuery rightJoinSerie($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Serie relation
 * @method DependenciaQuery innerJoinSerie($relationAlias = null) Adds a INNER JOIN clause to the query using the Serie relation
 *
 * @method DependenciaQuery leftJoinServicioReceptor($relationAlias = null) Adds a LEFT JOIN clause to the query using the ServicioReceptor relation
 * @method DependenciaQuery rightJoinServicioReceptor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ServicioReceptor relation
 * @method DependenciaQuery innerJoinServicioReceptor($relationAlias = null) Adds a INNER JOIN clause to the query using the ServicioReceptor relation
 *
 * @method DependenciaQuery leftJoinProcedimiento($relationAlias = null) Adds a LEFT JOIN clause to the query using the Procedimiento relation
 * @method DependenciaQuery rightJoinProcedimiento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Procedimiento relation
 * @method DependenciaQuery innerJoinProcedimiento($relationAlias = null) Adds a INNER JOIN clause to the query using the Procedimiento relation
 *
 * @method DependenciaQuery leftJoinNotificacionEmail($relationAlias = null) Adds a LEFT JOIN clause to the query using the NotificacionEmail relation
 * @method DependenciaQuery rightJoinNotificacionEmail($relationAlias = null) Adds a RIGHT JOIN clause to the query using the NotificacionEmail relation
 * @method DependenciaQuery innerJoinNotificacionEmail($relationAlias = null) Adds a INNER JOIN clause to the query using the NotificacionEmail relation
 *
 * @method DependenciaQuery leftJoinFactura($relationAlias = null) Adds a LEFT JOIN clause to the query using the Factura relation
 * @method DependenciaQuery rightJoinFactura($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Factura relation
 * @method DependenciaQuery innerJoinFactura($relationAlias = null) Adds a INNER JOIN clause to the query using the Factura relation
 *
 * @method DependenciaQuery leftJoinComEnviada($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComEnviada relation
 * @method DependenciaQuery rightJoinComEnviada($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComEnviada relation
 * @method DependenciaQuery innerJoinComEnviada($relationAlias = null) Adds a INNER JOIN clause to the query using the ComEnviada relation
 *
 * @method DependenciaQuery leftJoinComInterna($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComInterna relation
 * @method DependenciaQuery rightJoinComInterna($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComInterna relation
 * @method DependenciaQuery innerJoinComInterna($relationAlias = null) Adds a INNER JOIN clause to the query using the ComInterna relation
 *
 * @method DependenciaQuery leftJoinComRecibida($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComRecibida relation
 * @method DependenciaQuery rightJoinComRecibida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComRecibida relation
 * @method DependenciaQuery innerJoinComRecibida($relationAlias = null) Adds a INNER JOIN clause to the query using the ComRecibida relation
 *
 * @method DependenciaQuery leftJoinHistUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the HistUsuario relation
 * @method DependenciaQuery rightJoinHistUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the HistUsuario relation
 * @method DependenciaQuery innerJoinHistUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the HistUsuario relation
 *
 * @method DependenciaQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method DependenciaQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method DependenciaQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method Dependencia findOne(PropelPDO $con = null) Return the first Dependencia matching the query
 * @method Dependencia findOneOrCreate(PropelPDO $con = null) Return the first Dependencia matching the query, or a new Dependencia object populated from the query conditions when no match is found
 *
 * @method Dependencia findOneByEntidadId(int $ENTIDAD_ID) Return the first Dependencia filtered by the ENTIDAD_ID column
 * @method Dependencia findOneByOficinaproductoraId(int $OFICINAPRODUCTORA_ID) Return the first Dependencia filtered by the OFICINAPRODUCTORA_ID column
 * @method Dependencia findOneByNombre(string $NOMBRE) Return the first Dependencia filtered by the NOMBRE column
 * @method Dependencia findOneByCodigo(string $CODIGO) Return the first Dependencia filtered by the CODIGO column
 * @method Dependencia findOneByTiempoPrestamo(int $TIEMPO_PRESTAMO) Return the first Dependencia filtered by the TIEMPO_PRESTAMO column
 * @method Dependencia findOneByEsActual(boolean $ES_ACTUAL) Return the first Dependencia filtered by the ES_ACTUAL column
 * @method Dependencia findOneByTipoTabla(string $TIPO_TABLA) Return the first Dependencia filtered by the TIPO_TABLA column
 * @method Dependencia findOneBySubfondoId(int $SUBFONDO_ID) Return the first Dependencia filtered by the SUBFONDO_ID column
 * @method Dependencia findOneBySeccionId(int $SECCION_ID) Return the first Dependencia filtered by the SECCION_ID column
 * @method Dependencia findOneByVersionInst(string $VERSION_INST) Return the first Dependencia filtered by the VERSION_INST column
 *
 * @method array findByDependenciaId(int $DEPENDENCIA_ID) Return Dependencia objects filtered by the DEPENDENCIA_ID column
 * @method array findByEntidadId(int $ENTIDAD_ID) Return Dependencia objects filtered by the ENTIDAD_ID column
 * @method array findByOficinaproductoraId(int $OFICINAPRODUCTORA_ID) Return Dependencia objects filtered by the OFICINAPRODUCTORA_ID column
 * @method array findByNombre(string $NOMBRE) Return Dependencia objects filtered by the NOMBRE column
 * @method array findByCodigo(string $CODIGO) Return Dependencia objects filtered by the CODIGO column
 * @method array findByTiempoPrestamo(int $TIEMPO_PRESTAMO) Return Dependencia objects filtered by the TIEMPO_PRESTAMO column
 * @method array findByEsActual(boolean $ES_ACTUAL) Return Dependencia objects filtered by the ES_ACTUAL column
 * @method array findByTipoTabla(string $TIPO_TABLA) Return Dependencia objects filtered by the TIPO_TABLA column
 * @method array findBySubfondoId(int $SUBFONDO_ID) Return Dependencia objects filtered by the SUBFONDO_ID column
 * @method array findBySeccionId(int $SECCION_ID) Return Dependencia objects filtered by the SECCION_ID column
 * @method array findByVersionInst(string $VERSION_INST) Return Dependencia objects filtered by the VERSION_INST column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseDependenciaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseDependenciaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Dependencia';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new DependenciaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   DependenciaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return DependenciaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof DependenciaQuery) {
            return $criteria;
        }
        $query = new DependenciaQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Dependencia|Dependencia[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = DependenciaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(DependenciaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Dependencia A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByDependenciaId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Dependencia A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [DEPENDENCIA_ID], [ENTIDAD_ID], [OFICINAPRODUCTORA_ID], [NOMBRE], [CODIGO], [TIEMPO_PRESTAMO], [ES_ACTUAL], [TIPO_TABLA], [SUBFONDO_ID], [SECCION_ID], [VERSION_INST] FROM [DEPENDENCIA] WHERE [DEPENDENCIA_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Dependencia();
            $obj->hydrate($row);
            DependenciaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Dependencia|Dependencia[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Dependencia[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the DEPENDENCIA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByDependenciaId(1234); // WHERE DEPENDENCIA_ID = 1234
     * $query->filterByDependenciaId(array(12, 34)); // WHERE DEPENDENCIA_ID IN (12, 34)
     * $query->filterByDependenciaId(array('min' => 12)); // WHERE DEPENDENCIA_ID >= 12
     * $query->filterByDependenciaId(array('max' => 12)); // WHERE DEPENDENCIA_ID <= 12
     * </code>
     *
     * @param     mixed $dependenciaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByDependenciaId($dependenciaId = null, $comparison = null)
    {
        if (is_array($dependenciaId)) {
            $useMinMax = false;
            if (isset($dependenciaId['min'])) {
                $this->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $dependenciaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($dependenciaId['max'])) {
                $this->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $dependenciaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $dependenciaId, $comparison);
    }

    /**
     * Filter the query on the ENTIDAD_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEntidadId(1234); // WHERE ENTIDAD_ID = 1234
     * $query->filterByEntidadId(array(12, 34)); // WHERE ENTIDAD_ID IN (12, 34)
     * $query->filterByEntidadId(array('min' => 12)); // WHERE ENTIDAD_ID >= 12
     * $query->filterByEntidadId(array('max' => 12)); // WHERE ENTIDAD_ID <= 12
     * </code>
     *
     * @see       filterByEntidad()
     *
     * @param     mixed $entidadId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByEntidadId($entidadId = null, $comparison = null)
    {
        if (is_array($entidadId)) {
            $useMinMax = false;
            if (isset($entidadId['min'])) {
                $this->addUsingAlias(DependenciaPeer::ENTIDAD_ID, $entidadId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($entidadId['max'])) {
                $this->addUsingAlias(DependenciaPeer::ENTIDAD_ID, $entidadId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::ENTIDAD_ID, $entidadId, $comparison);
    }

    /**
     * Filter the query on the OFICINAPRODUCTORA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByOficinaproductoraId(1234); // WHERE OFICINAPRODUCTORA_ID = 1234
     * $query->filterByOficinaproductoraId(array(12, 34)); // WHERE OFICINAPRODUCTORA_ID IN (12, 34)
     * $query->filterByOficinaproductoraId(array('min' => 12)); // WHERE OFICINAPRODUCTORA_ID >= 12
     * $query->filterByOficinaproductoraId(array('max' => 12)); // WHERE OFICINAPRODUCTORA_ID <= 12
     * </code>
     *
     * @see       filterByOficinaProductora()
     *
     * @param     mixed $oficinaproductoraId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByOficinaproductoraId($oficinaproductoraId = null, $comparison = null)
    {
        if (is_array($oficinaproductoraId)) {
            $useMinMax = false;
            if (isset($oficinaproductoraId['min'])) {
                $this->addUsingAlias(DependenciaPeer::OFICINAPRODUCTORA_ID, $oficinaproductoraId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($oficinaproductoraId['max'])) {
                $this->addUsingAlias(DependenciaPeer::OFICINAPRODUCTORA_ID, $oficinaproductoraId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::OFICINAPRODUCTORA_ID, $oficinaproductoraId, $comparison);
    }

    /**
     * Filter the query on the NOMBRE column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE NOMBRE = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE NOMBRE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the CODIGO column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE CODIGO = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE CODIGO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the TIEMPO_PRESTAMO column
     *
     * Example usage:
     * <code>
     * $query->filterByTiempoPrestamo(1234); // WHERE TIEMPO_PRESTAMO = 1234
     * $query->filterByTiempoPrestamo(array(12, 34)); // WHERE TIEMPO_PRESTAMO IN (12, 34)
     * $query->filterByTiempoPrestamo(array('min' => 12)); // WHERE TIEMPO_PRESTAMO >= 12
     * $query->filterByTiempoPrestamo(array('max' => 12)); // WHERE TIEMPO_PRESTAMO <= 12
     * </code>
     *
     * @param     mixed $tiempoPrestamo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByTiempoPrestamo($tiempoPrestamo = null, $comparison = null)
    {
        if (is_array($tiempoPrestamo)) {
            $useMinMax = false;
            if (isset($tiempoPrestamo['min'])) {
                $this->addUsingAlias(DependenciaPeer::TIEMPO_PRESTAMO, $tiempoPrestamo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiempoPrestamo['max'])) {
                $this->addUsingAlias(DependenciaPeer::TIEMPO_PRESTAMO, $tiempoPrestamo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::TIEMPO_PRESTAMO, $tiempoPrestamo, $comparison);
    }

    /**
     * Filter the query on the ES_ACTUAL column
     *
     * Example usage:
     * <code>
     * $query->filterByEsActual(true); // WHERE ES_ACTUAL = true
     * $query->filterByEsActual('yes'); // WHERE ES_ACTUAL = true
     * </code>
     *
     * @param     boolean|string $esActual The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByEsActual($esActual = null, $comparison = null)
    {
        if (is_string($esActual)) {
            $esActual = in_array(strtolower($esActual), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(DependenciaPeer::ES_ACTUAL, $esActual, $comparison);
    }

    /**
     * Filter the query on the TIPO_TABLA column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoTabla('fooValue');   // WHERE TIPO_TABLA = 'fooValue'
     * $query->filterByTipoTabla('%fooValue%'); // WHERE TIPO_TABLA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoTabla The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByTipoTabla($tipoTabla = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoTabla)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoTabla)) {
                $tipoTabla = str_replace('*', '%', $tipoTabla);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::TIPO_TABLA, $tipoTabla, $comparison);
    }

    /**
     * Filter the query on the SUBFONDO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterBySubfondoId(1234); // WHERE SUBFONDO_ID = 1234
     * $query->filterBySubfondoId(array(12, 34)); // WHERE SUBFONDO_ID IN (12, 34)
     * $query->filterBySubfondoId(array('min' => 12)); // WHERE SUBFONDO_ID >= 12
     * $query->filterBySubfondoId(array('max' => 12)); // WHERE SUBFONDO_ID <= 12
     * </code>
     *
     * @param     mixed $subfondoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterBySubfondoId($subfondoId = null, $comparison = null)
    {
        if (is_array($subfondoId)) {
            $useMinMax = false;
            if (isset($subfondoId['min'])) {
                $this->addUsingAlias(DependenciaPeer::SUBFONDO_ID, $subfondoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($subfondoId['max'])) {
                $this->addUsingAlias(DependenciaPeer::SUBFONDO_ID, $subfondoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::SUBFONDO_ID, $subfondoId, $comparison);
    }

    /**
     * Filter the query on the SECCION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterBySeccionId(1234); // WHERE SECCION_ID = 1234
     * $query->filterBySeccionId(array(12, 34)); // WHERE SECCION_ID IN (12, 34)
     * $query->filterBySeccionId(array('min' => 12)); // WHERE SECCION_ID >= 12
     * $query->filterBySeccionId(array('max' => 12)); // WHERE SECCION_ID <= 12
     * </code>
     *
     * @param     mixed $seccionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterBySeccionId($seccionId = null, $comparison = null)
    {
        if (is_array($seccionId)) {
            $useMinMax = false;
            if (isset($seccionId['min'])) {
                $this->addUsingAlias(DependenciaPeer::SECCION_ID, $seccionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($seccionId['max'])) {
                $this->addUsingAlias(DependenciaPeer::SECCION_ID, $seccionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::SECCION_ID, $seccionId, $comparison);
    }

    /**
     * Filter the query on the VERSION_INST column
     *
     * Example usage:
     * <code>
     * $query->filterByVersionInst('fooValue');   // WHERE VERSION_INST = 'fooValue'
     * $query->filterByVersionInst('%fooValue%'); // WHERE VERSION_INST LIKE '%fooValue%'
     * </code>
     *
     * @param     string $versionInst The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function filterByVersionInst($versionInst = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($versionInst)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $versionInst)) {
                $versionInst = str_replace('*', '%', $versionInst);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DependenciaPeer::VERSION_INST, $versionInst, $comparison);
    }

    /**
     * Filter the query by a related Entidad object
     *
     * @param   Entidad|PropelObjectCollection $entidad The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEntidad($entidad, $comparison = null)
    {
        if ($entidad instanceof Entidad) {
            return $this
                ->addUsingAlias(DependenciaPeer::ENTIDAD_ID, $entidad->getEntidadId(), $comparison);
        } elseif ($entidad instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(DependenciaPeer::ENTIDAD_ID, $entidad->toKeyValue('PrimaryKey', 'EntidadId'), $comparison);
        } else {
            throw new PropelException('filterByEntidad() only accepts arguments of type Entidad or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Entidad relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinEntidad($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Entidad');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Entidad');
        }

        return $this;
    }

    /**
     * Use the Entidad relation Entidad object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EntidadQuery A secondary query class using the current class as primary query
     */
    public function useEntidadQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEntidad($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Entidad', 'EntidadQuery');
    }

    /**
     * Filter the query by a related OficinaProductora object
     *
     * @param   OficinaProductora|PropelObjectCollection $oficinaProductora The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByOficinaProductora($oficinaProductora, $comparison = null)
    {
        if ($oficinaProductora instanceof OficinaProductora) {
            return $this
                ->addUsingAlias(DependenciaPeer::OFICINAPRODUCTORA_ID, $oficinaProductora->getOficinaproductoraId(), $comparison);
        } elseif ($oficinaProductora instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(DependenciaPeer::OFICINAPRODUCTORA_ID, $oficinaProductora->toKeyValue('PrimaryKey', 'OficinaproductoraId'), $comparison);
        } else {
            throw new PropelException('filterByOficinaProductora() only accepts arguments of type OficinaProductora or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OficinaProductora relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinOficinaProductora($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OficinaProductora');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OficinaProductora');
        }

        return $this;
    }

    /**
     * Use the OficinaProductora relation OficinaProductora object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OficinaProductoraQuery A secondary query class using the current class as primary query
     */
    public function useOficinaProductoraQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinOficinaProductora($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OficinaProductora', 'OficinaProductoraQuery');
    }

    /**
     * Filter the query by a related Receptor object
     *
     * @param   Receptor|PropelObjectCollection $receptor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByReceptor($receptor, $comparison = null)
    {
        if ($receptor instanceof Receptor) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $receptor->getDependenciaId(), $comparison);
        } elseif ($receptor instanceof PropelObjectCollection) {
            return $this
                ->useReceptorQuery()
                ->filterByPrimaryKeys($receptor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByReceptor() only accepts arguments of type Receptor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Receptor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinReceptor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Receptor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Receptor');
        }

        return $this;
    }

    /**
     * Use the Receptor relation Receptor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ReceptorQuery A secondary query class using the current class as primary query
     */
    public function useReceptorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinReceptor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Receptor', 'ReceptorQuery');
    }

    /**
     * Filter the query by a related ReceptorComunicaciones object
     *
     * @param   ReceptorComunicaciones|PropelObjectCollection $receptorComunicaciones  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByReceptorComunicaciones($receptorComunicaciones, $comparison = null)
    {
        if ($receptorComunicaciones instanceof ReceptorComunicaciones) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $receptorComunicaciones->getDependenciaId(), $comparison);
        } elseif ($receptorComunicaciones instanceof PropelObjectCollection) {
            return $this
                ->useReceptorComunicacionesQuery()
                ->filterByPrimaryKeys($receptorComunicaciones->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByReceptorComunicaciones() only accepts arguments of type ReceptorComunicaciones or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ReceptorComunicaciones relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinReceptorComunicaciones($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ReceptorComunicaciones');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ReceptorComunicaciones');
        }

        return $this;
    }

    /**
     * Use the ReceptorComunicaciones relation ReceptorComunicaciones object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ReceptorComunicacionesQuery A secondary query class using the current class as primary query
     */
    public function useReceptorComunicacionesQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinReceptorComunicaciones($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ReceptorComunicaciones', 'ReceptorComunicacionesQuery');
    }

    /**
     * Filter the query by a related CargoUsuario object
     *
     * @param   CargoUsuario|PropelObjectCollection $cargoUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByCargoUsuario($cargoUsuario, $comparison = null)
    {
        if ($cargoUsuario instanceof CargoUsuario) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $cargoUsuario->getDependenciaId(), $comparison);
        } elseif ($cargoUsuario instanceof PropelObjectCollection) {
            return $this
                ->useCargoUsuarioQuery()
                ->filterByPrimaryKeys($cargoUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCargoUsuario() only accepts arguments of type CargoUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CargoUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinCargoUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CargoUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CargoUsuario');
        }

        return $this;
    }

    /**
     * Use the CargoUsuario relation CargoUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CargoUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useCargoUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCargoUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CargoUsuario', 'CargoUsuarioQuery');
    }

    /**
     * Filter the query by a related Serie object
     *
     * @param   Serie|PropelObjectCollection $serie  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterBySerie($serie, $comparison = null)
    {
        if ($serie instanceof Serie) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $serie->getDependenciaId(), $comparison);
        } elseif ($serie instanceof PropelObjectCollection) {
            return $this
                ->useSerieQuery()
                ->filterByPrimaryKeys($serie->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySerie() only accepts arguments of type Serie or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Serie relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinSerie($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Serie');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Serie');
        }

        return $this;
    }

    /**
     * Use the Serie relation Serie object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SerieQuery A secondary query class using the current class as primary query
     */
    public function useSerieQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinSerie($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Serie', 'SerieQuery');
    }

    /**
     * Filter the query by a related ServicioReceptor object
     *
     * @param   ServicioReceptor|PropelObjectCollection $servicioReceptor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByServicioReceptor($servicioReceptor, $comparison = null)
    {
        if ($servicioReceptor instanceof ServicioReceptor) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $servicioReceptor->getDependenciaId(), $comparison);
        } elseif ($servicioReceptor instanceof PropelObjectCollection) {
            return $this
                ->useServicioReceptorQuery()
                ->filterByPrimaryKeys($servicioReceptor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByServicioReceptor() only accepts arguments of type ServicioReceptor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ServicioReceptor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinServicioReceptor($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ServicioReceptor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ServicioReceptor');
        }

        return $this;
    }

    /**
     * Use the ServicioReceptor relation ServicioReceptor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ServicioReceptorQuery A secondary query class using the current class as primary query
     */
    public function useServicioReceptorQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinServicioReceptor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ServicioReceptor', 'ServicioReceptorQuery');
    }

    /**
     * Filter the query by a related Procedimiento object
     *
     * @param   Procedimiento|PropelObjectCollection $procedimiento  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByProcedimiento($procedimiento, $comparison = null)
    {
        if ($procedimiento instanceof Procedimiento) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $procedimiento->getDependenciaId(), $comparison);
        } elseif ($procedimiento instanceof PropelObjectCollection) {
            return $this
                ->useProcedimientoQuery()
                ->filterByPrimaryKeys($procedimiento->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProcedimiento() only accepts arguments of type Procedimiento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Procedimiento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinProcedimiento($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Procedimiento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Procedimiento');
        }

        return $this;
    }

    /**
     * Use the Procedimiento relation Procedimiento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProcedimientoQuery A secondary query class using the current class as primary query
     */
    public function useProcedimientoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinProcedimiento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Procedimiento', 'ProcedimientoQuery');
    }

    /**
     * Filter the query by a related NotificacionEmail object
     *
     * @param   NotificacionEmail|PropelObjectCollection $notificacionEmail  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByNotificacionEmail($notificacionEmail, $comparison = null)
    {
        if ($notificacionEmail instanceof NotificacionEmail) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $notificacionEmail->getDependenciaId(), $comparison);
        } elseif ($notificacionEmail instanceof PropelObjectCollection) {
            return $this
                ->useNotificacionEmailQuery()
                ->filterByPrimaryKeys($notificacionEmail->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByNotificacionEmail() only accepts arguments of type NotificacionEmail or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the NotificacionEmail relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinNotificacionEmail($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('NotificacionEmail');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'NotificacionEmail');
        }

        return $this;
    }

    /**
     * Use the NotificacionEmail relation NotificacionEmail object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NotificacionEmailQuery A secondary query class using the current class as primary query
     */
    public function useNotificacionEmailQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinNotificacionEmail($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'NotificacionEmail', 'NotificacionEmailQuery');
    }

    /**
     * Filter the query by a related Factura object
     *
     * @param   Factura|PropelObjectCollection $factura  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByFactura($factura, $comparison = null)
    {
        if ($factura instanceof Factura) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $factura->getDependenciaId(), $comparison);
        } elseif ($factura instanceof PropelObjectCollection) {
            return $this
                ->useFacturaQuery()
                ->filterByPrimaryKeys($factura->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByFactura() only accepts arguments of type Factura or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Factura relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinFactura($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Factura');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Factura');
        }

        return $this;
    }

    /**
     * Use the Factura relation Factura object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   FacturaQuery A secondary query class using the current class as primary query
     */
    public function useFacturaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinFactura($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Factura', 'FacturaQuery');
    }

    /**
     * Filter the query by a related ComEnviada object
     *
     * @param   ComEnviada|PropelObjectCollection $comEnviada  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComEnviada($comEnviada, $comparison = null)
    {
        if ($comEnviada instanceof ComEnviada) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $comEnviada->getDependenciaId(), $comparison);
        } elseif ($comEnviada instanceof PropelObjectCollection) {
            return $this
                ->useComEnviadaQuery()
                ->filterByPrimaryKeys($comEnviada->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComEnviada() only accepts arguments of type ComEnviada or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComEnviada relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinComEnviada($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComEnviada');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComEnviada');
        }

        return $this;
    }

    /**
     * Use the ComEnviada relation ComEnviada object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComEnviadaQuery A secondary query class using the current class as primary query
     */
    public function useComEnviadaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComEnviada($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComEnviada', 'ComEnviadaQuery');
    }

    /**
     * Filter the query by a related ComInterna object
     *
     * @param   ComInterna|PropelObjectCollection $comInterna  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComInterna($comInterna, $comparison = null)
    {
        if ($comInterna instanceof ComInterna) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $comInterna->getDependenciaId(), $comparison);
        } elseif ($comInterna instanceof PropelObjectCollection) {
            return $this
                ->useComInternaQuery()
                ->filterByPrimaryKeys($comInterna->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComInterna() only accepts arguments of type ComInterna or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComInterna relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinComInterna($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComInterna');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComInterna');
        }

        return $this;
    }

    /**
     * Use the ComInterna relation ComInterna object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComInternaQuery A secondary query class using the current class as primary query
     */
    public function useComInternaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComInterna($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComInterna', 'ComInternaQuery');
    }

    /**
     * Filter the query by a related ComRecibida object
     *
     * @param   ComRecibida|PropelObjectCollection $comRecibida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByComRecibida($comRecibida, $comparison = null)
    {
        if ($comRecibida instanceof ComRecibida) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $comRecibida->getDependenciaId(), $comparison);
        } elseif ($comRecibida instanceof PropelObjectCollection) {
            return $this
                ->useComRecibidaQuery()
                ->filterByPrimaryKeys($comRecibida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComRecibida() only accepts arguments of type ComRecibida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComRecibida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinComRecibida($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComRecibida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComRecibida');
        }

        return $this;
    }

    /**
     * Use the ComRecibida relation ComRecibida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComRecibidaQuery A secondary query class using the current class as primary query
     */
    public function useComRecibidaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinComRecibida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComRecibida', 'ComRecibidaQuery');
    }

    /**
     * Filter the query by a related HistUsuario object
     *
     * @param   HistUsuario|PropelObjectCollection $histUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByHistUsuario($histUsuario, $comparison = null)
    {
        if ($histUsuario instanceof HistUsuario) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $histUsuario->getDependenciaId(), $comparison);
        } elseif ($histUsuario instanceof PropelObjectCollection) {
            return $this
                ->useHistUsuarioQuery()
                ->filterByPrimaryKeys($histUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByHistUsuario() only accepts arguments of type HistUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the HistUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinHistUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('HistUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'HistUsuario');
        }

        return $this;
    }

    /**
     * Use the HistUsuario relation HistUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   HistUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useHistUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinHistUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'HistUsuario', 'HistUsuarioQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 DependenciaQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $usuario->getDependenciaId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioQuery()
                ->filterByPrimaryKeys($usuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Dependencia $dependencia Object to remove from the list of results
     *
     * @return DependenciaQuery The current query, for fluid interface
     */
    public function prune($dependencia = null)
    {
        if ($dependencia) {
            $this->addUsingAlias(DependenciaPeer::DEPENDENCIA_ID, $dependencia->getDependenciaId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

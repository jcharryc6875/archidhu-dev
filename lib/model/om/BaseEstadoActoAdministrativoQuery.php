<?php


/**
 * Base class that represents a query for the 'ESTADO_ACTO_ADMINISTRATIVO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 05/15/24 13:35:22
 *
 * @method EstadoActoAdministrativoQuery orderByEstadoactoadministrativoId($order = Criteria::ASC) Order by the ESTADOACTOADMINISTRATIVO_ID column
 * @method EstadoActoAdministrativoQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method EstadoActoAdministrativoQuery orderByIcono($order = Criteria::ASC) Order by the ICONO column
 * @method EstadoActoAdministrativoQuery orderByTooltip($order = Criteria::ASC) Order by the TOOLTIP column
 *
 * @method EstadoActoAdministrativoQuery groupByEstadoactoadministrativoId() Group by the ESTADOACTOADMINISTRATIVO_ID column
 * @method EstadoActoAdministrativoQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method EstadoActoAdministrativoQuery groupByIcono() Group by the ICONO column
 * @method EstadoActoAdministrativoQuery groupByTooltip() Group by the TOOLTIP column
 *
 * @method EstadoActoAdministrativoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method EstadoActoAdministrativoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method EstadoActoAdministrativoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method EstadoActoAdministrativoQuery leftJoinActoadministrativoUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the ActoadministrativoUsuario relation
 * @method EstadoActoAdministrativoQuery rightJoinActoadministrativoUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ActoadministrativoUsuario relation
 * @method EstadoActoAdministrativoQuery innerJoinActoadministrativoUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the ActoadministrativoUsuario relation
 *
 * @method EstadoActoAdministrativoQuery leftJoinActoAdministrativo($relationAlias = null) Adds a LEFT JOIN clause to the query using the ActoAdministrativo relation
 * @method EstadoActoAdministrativoQuery rightJoinActoAdministrativo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ActoAdministrativo relation
 * @method EstadoActoAdministrativoQuery innerJoinActoAdministrativo($relationAlias = null) Adds a INNER JOIN clause to the query using the ActoAdministrativo relation
 *
 * @method EstadoActoAdministrativo findOne(PropelPDO $con = null) Return the first EstadoActoAdministrativo matching the query
 * @method EstadoActoAdministrativo findOneOrCreate(PropelPDO $con = null) Return the first EstadoActoAdministrativo matching the query, or a new EstadoActoAdministrativo object populated from the query conditions when no match is found
 *
 * @method EstadoActoAdministrativo findOneByDescripcion(string $DESCRIPCION) Return the first EstadoActoAdministrativo filtered by the DESCRIPCION column
 * @method EstadoActoAdministrativo findOneByIcono(string $ICONO) Return the first EstadoActoAdministrativo filtered by the ICONO column
 * @method EstadoActoAdministrativo findOneByTooltip(string $TOOLTIP) Return the first EstadoActoAdministrativo filtered by the TOOLTIP column
 *
 * @method array findByEstadoactoadministrativoId(int $ESTADOACTOADMINISTRATIVO_ID) Return EstadoActoAdministrativo objects filtered by the ESTADOACTOADMINISTRATIVO_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return EstadoActoAdministrativo objects filtered by the DESCRIPCION column
 * @method array findByIcono(string $ICONO) Return EstadoActoAdministrativo objects filtered by the ICONO column
 * @method array findByTooltip(string $TOOLTIP) Return EstadoActoAdministrativo objects filtered by the TOOLTIP column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEstadoActoAdministrativoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseEstadoActoAdministrativoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'EstadoActoAdministrativo';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new EstadoActoAdministrativoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   EstadoActoAdministrativoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return EstadoActoAdministrativoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof EstadoActoAdministrativoQuery) {
            return $criteria;
        }
        $query = new EstadoActoAdministrativoQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   EstadoActoAdministrativo|EstadoActoAdministrativo[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = EstadoActoAdministrativoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(EstadoActoAdministrativoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 EstadoActoAdministrativo A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByEstadoactoadministrativoId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 EstadoActoAdministrativo A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [ESTADOACTOADMINISTRATIVO_ID], [DESCRIPCION], [ICONO], [TOOLTIP] FROM [ESTADO_ACTO_ADMINISTRATIVO] WHERE [ESTADOACTOADMINISTRATIVO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new EstadoActoAdministrativo();
            $obj->hydrate($row);
            EstadoActoAdministrativoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return EstadoActoAdministrativo|EstadoActoAdministrativo[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|EstadoActoAdministrativo[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the ESTADOACTOADMINISTRATIVO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoactoadministrativoId(1234); // WHERE ESTADOACTOADMINISTRATIVO_ID = 1234
     * $query->filterByEstadoactoadministrativoId(array(12, 34)); // WHERE ESTADOACTOADMINISTRATIVO_ID IN (12, 34)
     * $query->filterByEstadoactoadministrativoId(array('min' => 12)); // WHERE ESTADOACTOADMINISTRATIVO_ID >= 12
     * $query->filterByEstadoactoadministrativoId(array('max' => 12)); // WHERE ESTADOACTOADMINISTRATIVO_ID <= 12
     * </code>
     *
     * @param     mixed $estadoactoadministrativoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function filterByEstadoactoadministrativoId($estadoactoadministrativoId = null, $comparison = null)
    {
        if (is_array($estadoactoadministrativoId)) {
            $useMinMax = false;
            if (isset($estadoactoadministrativoId['min'])) {
                $this->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $estadoactoadministrativoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadoactoadministrativoId['max'])) {
                $this->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $estadoactoadministrativoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $estadoactoadministrativoId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EstadoActoAdministrativoPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the ICONO column
     *
     * Example usage:
     * <code>
     * $query->filterByIcono('fooValue');   // WHERE ICONO = 'fooValue'
     * $query->filterByIcono('%fooValue%'); // WHERE ICONO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $icono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function filterByIcono($icono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($icono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $icono)) {
                $icono = str_replace('*', '%', $icono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EstadoActoAdministrativoPeer::ICONO, $icono, $comparison);
    }

    /**
     * Filter the query on the TOOLTIP column
     *
     * Example usage:
     * <code>
     * $query->filterByTooltip('fooValue');   // WHERE TOOLTIP = 'fooValue'
     * $query->filterByTooltip('%fooValue%'); // WHERE TOOLTIP LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tooltip The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function filterByTooltip($tooltip = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tooltip)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tooltip)) {
                $tooltip = str_replace('*', '%', $tooltip);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EstadoActoAdministrativoPeer::TOOLTIP, $tooltip, $comparison);
    }

    /**
     * Filter the query by a related ActoadministrativoUsuario object
     *
     * @param   ActoadministrativoUsuario|PropelObjectCollection $actoadministrativoUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EstadoActoAdministrativoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByActoadministrativoUsuario($actoadministrativoUsuario, $comparison = null)
    {
        if ($actoadministrativoUsuario instanceof ActoadministrativoUsuario) {
            return $this
                ->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $actoadministrativoUsuario->getEstadoactoadministrativoId(), $comparison);
        } elseif ($actoadministrativoUsuario instanceof PropelObjectCollection) {
            return $this
                ->useActoadministrativoUsuarioQuery()
                ->filterByPrimaryKeys($actoadministrativoUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByActoadministrativoUsuario() only accepts arguments of type ActoadministrativoUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ActoadministrativoUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function joinActoadministrativoUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ActoadministrativoUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ActoadministrativoUsuario');
        }

        return $this;
    }

    /**
     * Use the ActoadministrativoUsuario relation ActoadministrativoUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ActoadministrativoUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useActoadministrativoUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinActoadministrativoUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ActoadministrativoUsuario', 'ActoadministrativoUsuarioQuery');
    }

    /**
     * Filter the query by a related ActoAdministrativo object
     *
     * @param   ActoAdministrativo|PropelObjectCollection $actoAdministrativo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EstadoActoAdministrativoQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByActoAdministrativo($actoAdministrativo, $comparison = null)
    {
        if ($actoAdministrativo instanceof ActoAdministrativo) {
            return $this
                ->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $actoAdministrativo->getEstadoactoadministrativoId(), $comparison);
        } elseif ($actoAdministrativo instanceof PropelObjectCollection) {
            return $this
                ->useActoAdministrativoQuery()
                ->filterByPrimaryKeys($actoAdministrativo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByActoAdministrativo() only accepts arguments of type ActoAdministrativo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ActoAdministrativo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function joinActoAdministrativo($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ActoAdministrativo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ActoAdministrativo');
        }

        return $this;
    }

    /**
     * Use the ActoAdministrativo relation ActoAdministrativo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ActoAdministrativoQuery A secondary query class using the current class as primary query
     */
    public function useActoAdministrativoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinActoAdministrativo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ActoAdministrativo', 'ActoAdministrativoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   EstadoActoAdministrativo $estadoActoAdministrativo Object to remove from the list of results
     *
     * @return EstadoActoAdministrativoQuery The current query, for fluid interface
     */
    public function prune($estadoActoAdministrativo = null)
    {
        if ($estadoActoAdministrativo) {
            $this->addUsingAlias(EstadoActoAdministrativoPeer::ESTADOACTOADMINISTRATIVO_ID, $estadoActoAdministrativo->getEstadoactoadministrativoId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

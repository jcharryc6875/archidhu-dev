<?php


/**
 * Base class that represents a query for the 'PROCEDIMIENTO_APROB' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:53
 *
 * @method ProcedimientoAprobQuery orderByProcedimientoaprobId($order = Criteria::ASC) Order by the PROCEDIMIENTOAPROB_ID column
 * @method ProcedimientoAprobQuery orderByProcedimientoId($order = Criteria::ASC) Order by the PROCEDIMIENTO_ID column
 * @method ProcedimientoAprobQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method ProcedimientoAprobQuery orderByObsAprobacion($order = Criteria::ASC) Order by the OBS_APROBACION column
 * @method ProcedimientoAprobQuery orderByEstadoAprobacion($order = Criteria::ASC) Order by the ESTADO_APROBACION column
 * @method ProcedimientoAprobQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method ProcedimientoAprobQuery orderByFechaAprobacion($order = Criteria::ASC) Order by the FECHA_APROBACION column
 *
 * @method ProcedimientoAprobQuery groupByProcedimientoaprobId() Group by the PROCEDIMIENTOAPROB_ID column
 * @method ProcedimientoAprobQuery groupByProcedimientoId() Group by the PROCEDIMIENTO_ID column
 * @method ProcedimientoAprobQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method ProcedimientoAprobQuery groupByObsAprobacion() Group by the OBS_APROBACION column
 * @method ProcedimientoAprobQuery groupByEstadoAprobacion() Group by the ESTADO_APROBACION column
 * @method ProcedimientoAprobQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method ProcedimientoAprobQuery groupByFechaAprobacion() Group by the FECHA_APROBACION column
 *
 * @method ProcedimientoAprobQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ProcedimientoAprobQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ProcedimientoAprobQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ProcedimientoAprobQuery leftJoinProcedimiento($relationAlias = null) Adds a LEFT JOIN clause to the query using the Procedimiento relation
 * @method ProcedimientoAprobQuery rightJoinProcedimiento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Procedimiento relation
 * @method ProcedimientoAprobQuery innerJoinProcedimiento($relationAlias = null) Adds a INNER JOIN clause to the query using the Procedimiento relation
 *
 * @method ProcedimientoAprobQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method ProcedimientoAprobQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method ProcedimientoAprobQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method ProcedimientoAprob findOne(PropelPDO $con = null) Return the first ProcedimientoAprob matching the query
 * @method ProcedimientoAprob findOneOrCreate(PropelPDO $con = null) Return the first ProcedimientoAprob matching the query, or a new ProcedimientoAprob object populated from the query conditions when no match is found
 *
 * @method ProcedimientoAprob findOneByProcedimientoId(int $PROCEDIMIENTO_ID) Return the first ProcedimientoAprob filtered by the PROCEDIMIENTO_ID column
 * @method ProcedimientoAprob findOneByUsuarioId(int $USUARIO_ID) Return the first ProcedimientoAprob filtered by the USUARIO_ID column
 * @method ProcedimientoAprob findOneByObsAprobacion(string $OBS_APROBACION) Return the first ProcedimientoAprob filtered by the OBS_APROBACION column
 * @method ProcedimientoAprob findOneByEstadoAprobacion(int $ESTADO_APROBACION) Return the first ProcedimientoAprob filtered by the ESTADO_APROBACION column
 * @method ProcedimientoAprob findOneByFechaCreacion(string $FECHA_CREACION) Return the first ProcedimientoAprob filtered by the FECHA_CREACION column
 * @method ProcedimientoAprob findOneByFechaAprobacion(string $FECHA_APROBACION) Return the first ProcedimientoAprob filtered by the FECHA_APROBACION column
 *
 * @method array findByProcedimientoaprobId(int $PROCEDIMIENTOAPROB_ID) Return ProcedimientoAprob objects filtered by the PROCEDIMIENTOAPROB_ID column
 * @method array findByProcedimientoId(int $PROCEDIMIENTO_ID) Return ProcedimientoAprob objects filtered by the PROCEDIMIENTO_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return ProcedimientoAprob objects filtered by the USUARIO_ID column
 * @method array findByObsAprobacion(string $OBS_APROBACION) Return ProcedimientoAprob objects filtered by the OBS_APROBACION column
 * @method array findByEstadoAprobacion(int $ESTADO_APROBACION) Return ProcedimientoAprob objects filtered by the ESTADO_APROBACION column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return ProcedimientoAprob objects filtered by the FECHA_CREACION column
 * @method array findByFechaAprobacion(string $FECHA_APROBACION) Return ProcedimientoAprob objects filtered by the FECHA_APROBACION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseProcedimientoAprobQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseProcedimientoAprobQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'ProcedimientoAprob';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ProcedimientoAprobQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   ProcedimientoAprobQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ProcedimientoAprobQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ProcedimientoAprobQuery) {
            return $criteria;
        }
        $query = new ProcedimientoAprobQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   ProcedimientoAprob|ProcedimientoAprob[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ProcedimientoAprobPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ProcedimientoAprobPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ProcedimientoAprob A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByProcedimientoaprobId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 ProcedimientoAprob A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [PROCEDIMIENTOAPROB_ID], [PROCEDIMIENTO_ID], [USUARIO_ID], [OBS_APROBACION], [ESTADO_APROBACION], [FECHA_CREACION], [FECHA_APROBACION] FROM [PROCEDIMIENTO_APROB] WHERE [PROCEDIMIENTOAPROB_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new ProcedimientoAprob();
            $obj->hydrate($row);
            ProcedimientoAprobPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return ProcedimientoAprob|ProcedimientoAprob[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|ProcedimientoAprob[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTOAPROB_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTOAPROB_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the PROCEDIMIENTOAPROB_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByProcedimientoaprobId(1234); // WHERE PROCEDIMIENTOAPROB_ID = 1234
     * $query->filterByProcedimientoaprobId(array(12, 34)); // WHERE PROCEDIMIENTOAPROB_ID IN (12, 34)
     * $query->filterByProcedimientoaprobId(array('min' => 12)); // WHERE PROCEDIMIENTOAPROB_ID >= 12
     * $query->filterByProcedimientoaprobId(array('max' => 12)); // WHERE PROCEDIMIENTOAPROB_ID <= 12
     * </code>
     *
     * @param     mixed $procedimientoaprobId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByProcedimientoaprobId($procedimientoaprobId = null, $comparison = null)
    {
        if (is_array($procedimientoaprobId)) {
            $useMinMax = false;
            if (isset($procedimientoaprobId['min'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTOAPROB_ID, $procedimientoaprobId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($procedimientoaprobId['max'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTOAPROB_ID, $procedimientoaprobId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTOAPROB_ID, $procedimientoaprobId, $comparison);
    }

    /**
     * Filter the query on the PROCEDIMIENTO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByProcedimientoId(1234); // WHERE PROCEDIMIENTO_ID = 1234
     * $query->filterByProcedimientoId(array(12, 34)); // WHERE PROCEDIMIENTO_ID IN (12, 34)
     * $query->filterByProcedimientoId(array('min' => 12)); // WHERE PROCEDIMIENTO_ID >= 12
     * $query->filterByProcedimientoId(array('max' => 12)); // WHERE PROCEDIMIENTO_ID <= 12
     * </code>
     *
     * @see       filterByProcedimiento()
     *
     * @param     mixed $procedimientoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByProcedimientoId($procedimientoId = null, $comparison = null)
    {
        if (is_array($procedimientoId)) {
            $useMinMax = false;
            if (isset($procedimientoId['min'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTO_ID, $procedimientoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($procedimientoId['max'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTO_ID, $procedimientoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTO_ID, $procedimientoId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the OBS_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByObsAprobacion('fooValue');   // WHERE OBS_APROBACION = 'fooValue'
     * $query->filterByObsAprobacion('%fooValue%'); // WHERE OBS_APROBACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $obsAprobacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByObsAprobacion($obsAprobacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($obsAprobacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $obsAprobacion)) {
                $obsAprobacion = str_replace('*', '%', $obsAprobacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::OBS_APROBACION, $obsAprobacion, $comparison);
    }

    /**
     * Filter the query on the ESTADO_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByEstadoAprobacion(1234); // WHERE ESTADO_APROBACION = 1234
     * $query->filterByEstadoAprobacion(array(12, 34)); // WHERE ESTADO_APROBACION IN (12, 34)
     * $query->filterByEstadoAprobacion(array('min' => 12)); // WHERE ESTADO_APROBACION >= 12
     * $query->filterByEstadoAprobacion(array('max' => 12)); // WHERE ESTADO_APROBACION <= 12
     * </code>
     *
     * @param     mixed $estadoAprobacion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByEstadoAprobacion($estadoAprobacion = null, $comparison = null)
    {
        if (is_array($estadoAprobacion)) {
            $useMinMax = false;
            if (isset($estadoAprobacion['min'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::ESTADO_APROBACION, $estadoAprobacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($estadoAprobacion['max'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::ESTADO_APROBACION, $estadoAprobacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::ESTADO_APROBACION, $estadoAprobacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_APROBACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaAprobacion('2011-03-14'); // WHERE FECHA_APROBACION = '2011-03-14'
     * $query->filterByFechaAprobacion('now'); // WHERE FECHA_APROBACION = '2011-03-14'
     * $query->filterByFechaAprobacion(array('max' => 'yesterday')); // WHERE FECHA_APROBACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaAprobacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function filterByFechaAprobacion($fechaAprobacion = null, $comparison = null)
    {
        if (is_array($fechaAprobacion)) {
            $useMinMax = false;
            if (isset($fechaAprobacion['min'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::FECHA_APROBACION, $fechaAprobacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaAprobacion['max'])) {
                $this->addUsingAlias(ProcedimientoAprobPeer::FECHA_APROBACION, $fechaAprobacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcedimientoAprobPeer::FECHA_APROBACION, $fechaAprobacion, $comparison);
    }

    /**
     * Filter the query by a related Procedimiento object
     *
     * @param   Procedimiento|PropelObjectCollection $procedimiento The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcedimientoAprobQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByProcedimiento($procedimiento, $comparison = null)
    {
        if ($procedimiento instanceof Procedimiento) {
            return $this
                ->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTO_ID, $procedimiento->getProcedimientoId(), $comparison);
        } elseif ($procedimiento instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTO_ID, $procedimiento->toKeyValue('PrimaryKey', 'ProcedimientoId'), $comparison);
        } else {
            throw new PropelException('filterByProcedimiento() only accepts arguments of type Procedimiento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Procedimiento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function joinProcedimiento($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Procedimiento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Procedimiento');
        }

        return $this;
    }

    /**
     * Use the Procedimiento relation Procedimiento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProcedimientoQuery A secondary query class using the current class as primary query
     */
    public function useProcedimientoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinProcedimiento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Procedimiento', 'ProcedimientoQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcedimientoAprobQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(ProcedimientoAprobPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ProcedimientoAprobPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   ProcedimientoAprob $procedimientoAprob Object to remove from the list of results
     *
     * @return ProcedimientoAprobQuery The current query, for fluid interface
     */
    public function prune($procedimientoAprob = null)
    {
        if ($procedimientoAprob) {
            $this->addUsingAlias(ProcedimientoAprobPeer::PROCEDIMIENTOAPROB_ID, $procedimientoAprob->getProcedimientoaprobId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a row from the 'AUTORIZACION_FIRMA' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:49
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseAutorizacionFirma extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'AutorizacionFirmaPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        AutorizacionFirmaPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the autorizacionfirma_id field.
     * @var        int
     */
    protected $autorizacionfirma_id;

    /**
     * The value for the estadofirmaauto_id field.
     * @var        int
     */
    protected $estadofirmaauto_id;

    /**
     * The value for the usuario_id field.
     * @var        int
     */
    protected $usuario_id;

    /**
     * The value for the modulo_id field.
     * @var        int
     */
    protected $modulo_id;

    /**
     * The value for the firma_electronica field.
     * @var        int
     */
    protected $firma_electronica;

    /**
     * The value for the firma_digital field.
     * @var        int
     */
    protected $firma_digital;

    /**
     * The value for the firma_fisica field.
     * @var        int
     */
    protected $firma_fisica;

    /**
     * @var        EstadoFirmaAuto
     */
    protected $aEstadoFirmaAuto;

    /**
     * @var        Usuario
     */
    protected $aUsuario;

    /**
     * @var        Modulo
     */
    protected $aModulo;

    /**
     * @var        PropelObjectCollection|AutFirmaUsuario[] Collection to store aggregation of AutFirmaUsuario objects.
     */
    protected $collAutFirmaUsuarios;
    protected $collAutFirmaUsuariosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $autFirmaUsuariosScheduledForDeletion = null;

    /**
     * Get the [autorizacionfirma_id] column value.
     * 
     * @return int
     */
    public function getAutorizacionfirmaId()
    {

        return $this->autorizacionfirma_id;
    }

    /**
     * Get the [estadofirmaauto_id] column value.
     * 
     * @return int
     */
    public function getEstadofirmaautoId()
    {

        return $this->estadofirmaauto_id;
    }

    /**
     * Get the [usuario_id] column value.
     * 
     * @return int
     */
    public function getUsuarioId()
    {

        return $this->usuario_id;
    }

    /**
     * Get the [modulo_id] column value.
     * 
     * @return int
     */
    public function getModuloId()
    {

        return $this->modulo_id;
    }

    /**
     * Get the [firma_electronica] column value.
     * 
     * @return int
     */
    public function getFirmaElectronica()
    {

        return $this->firma_electronica;
    }

    /**
     * Get the [firma_digital] column value.
     * 
     * @return int
     */
    public function getFirmaDigital()
    {

        return $this->firma_digital;
    }

    /**
     * Get the [firma_fisica] column value.
     * 
     * @return int
     */
    public function getFirmaFisica()
    {

        return $this->firma_fisica;
    }

    /**
     * Set the value of [autorizacionfirma_id] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setAutorizacionfirmaId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->autorizacionfirma_id !== $v) {
            $this->autorizacionfirma_id = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID;
        }


        return $this;
    } // setAutorizacionfirmaId()

    /**
     * Set the value of [estadofirmaauto_id] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setEstadofirmaautoId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->estadofirmaauto_id !== $v) {
            $this->estadofirmaauto_id = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::ESTADOFIRMAAUTO_ID;
        }

        if ($this->aEstadoFirmaAuto !== null && $this->aEstadoFirmaAuto->getEstadofirmaautoId() !== $v) {
            $this->aEstadoFirmaAuto = null;
        }


        return $this;
    } // setEstadofirmaautoId()

    /**
     * Set the value of [usuario_id] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setUsuarioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->usuario_id !== $v) {
            $this->usuario_id = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::USUARIO_ID;
        }

        if ($this->aUsuario !== null && $this->aUsuario->getUsuarioId() !== $v) {
            $this->aUsuario = null;
        }


        return $this;
    } // setUsuarioId()

    /**
     * Set the value of [modulo_id] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setModuloId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->modulo_id !== $v) {
            $this->modulo_id = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::MODULO_ID;
        }

        if ($this->aModulo !== null && $this->aModulo->getModuloId() !== $v) {
            $this->aModulo = null;
        }


        return $this;
    } // setModuloId()

    /**
     * Set the value of [firma_electronica] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setFirmaElectronica($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->firma_electronica !== $v) {
            $this->firma_electronica = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::FIRMA_ELECTRONICA;
        }


        return $this;
    } // setFirmaElectronica()

    /**
     * Set the value of [firma_digital] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setFirmaDigital($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->firma_digital !== $v) {
            $this->firma_digital = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::FIRMA_DIGITAL;
        }


        return $this;
    } // setFirmaDigital()

    /**
     * Set the value of [firma_fisica] column.
     * 
     * @param  int $v new value
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setFirmaFisica($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->firma_fisica !== $v) {
            $this->firma_fisica = $v;
            $this->modifiedColumns[] = AutorizacionFirmaPeer::FIRMA_FISICA;
        }


        return $this;
    } // setFirmaFisica()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->autorizacionfirma_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->estadofirmaauto_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->usuario_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->modulo_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->firma_electronica = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
            $this->firma_digital = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
            $this->firma_fisica = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 7; // 7 = AutorizacionFirmaPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating AutorizacionFirma object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEstadoFirmaAuto !== null && $this->estadofirmaauto_id !== $this->aEstadoFirmaAuto->getEstadofirmaautoId()) {
            $this->aEstadoFirmaAuto = null;
        }
        if ($this->aUsuario !== null && $this->usuario_id !== $this->aUsuario->getUsuarioId()) {
            $this->aUsuario = null;
        }
        if ($this->aModulo !== null && $this->modulo_id !== $this->aModulo->getModuloId()) {
            $this->aModulo = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(AutorizacionFirmaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = AutorizacionFirmaPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEstadoFirmaAuto = null;
            $this->aUsuario = null;
            $this->aModulo = null;
            $this->collAutFirmaUsuarios = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(AutorizacionFirmaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = AutorizacionFirmaQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseAutorizacionFirma:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseAutorizacionFirma:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(AutorizacionFirmaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseAutorizacionFirma:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseAutorizacionFirma:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                AutorizacionFirmaPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEstadoFirmaAuto !== null) {
                if ($this->aEstadoFirmaAuto->isModified() || $this->aEstadoFirmaAuto->isNew()) {
                    $affectedRows += $this->aEstadoFirmaAuto->save($con);
                }
                $this->setEstadoFirmaAuto($this->aEstadoFirmaAuto);
            }

            if ($this->aUsuario !== null) {
                if ($this->aUsuario->isModified() || $this->aUsuario->isNew()) {
                    $affectedRows += $this->aUsuario->save($con);
                }
                $this->setUsuario($this->aUsuario);
            }

            if ($this->aModulo !== null) {
                if ($this->aModulo->isModified() || $this->aModulo->isNew()) {
                    $affectedRows += $this->aModulo->save($con);
                }
                $this->setModulo($this->aModulo);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->autFirmaUsuariosScheduledForDeletion !== null) {
                if (!$this->autFirmaUsuariosScheduledForDeletion->isEmpty()) {
                    foreach ($this->autFirmaUsuariosScheduledForDeletion as $autFirmaUsuario) {
                        // need to save related object because we set the relation to null
                        $autFirmaUsuario->save($con);
                    }
                    $this->autFirmaUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collAutFirmaUsuarios !== null) {
                foreach ($this->collAutFirmaUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setAutorizacionfirmaId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEstadoFirmaAuto !== null) {
                if (!$this->aEstadoFirmaAuto->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEstadoFirmaAuto->getValidationFailures());
                }
            }

            if ($this->aUsuario !== null) {
                if (!$this->aUsuario->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aUsuario->getValidationFailures());
                }
            }

            if ($this->aModulo !== null) {
                if (!$this->aModulo->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aModulo->getValidationFailures());
                }
            }


            if (($retval = AutorizacionFirmaPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collAutFirmaUsuarios !== null) {
                    foreach ($this->collAutFirmaUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = AutorizacionFirmaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getAutorizacionfirmaId();
                break;
            case 1:
                return $this->getEstadofirmaautoId();
                break;
            case 2:
                return $this->getUsuarioId();
                break;
            case 3:
                return $this->getModuloId();
                break;
            case 4:
                return $this->getFirmaElectronica();
                break;
            case 5:
                return $this->getFirmaDigital();
                break;
            case 6:
                return $this->getFirmaFisica();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['AutorizacionFirma'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['AutorizacionFirma'][$this->getPrimaryKey()] = true;
        $keys = AutorizacionFirmaPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getAutorizacionfirmaId(),
            $keys[1] => $this->getEstadofirmaautoId(),
            $keys[2] => $this->getUsuarioId(),
            $keys[3] => $this->getModuloId(),
            $keys[4] => $this->getFirmaElectronica(),
            $keys[5] => $this->getFirmaDigital(),
            $keys[6] => $this->getFirmaFisica(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aEstadoFirmaAuto) {
                $result['EstadoFirmaAuto'] = $this->aEstadoFirmaAuto->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aUsuario) {
                $result['Usuario'] = $this->aUsuario->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aModulo) {
                $result['Modulo'] = $this->aModulo->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collAutFirmaUsuarios) {
                $result['AutFirmaUsuarios'] = $this->collAutFirmaUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = AutorizacionFirmaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setAutorizacionfirmaId($value);
                break;
            case 1:
                $this->setEstadofirmaautoId($value);
                break;
            case 2:
                $this->setUsuarioId($value);
                break;
            case 3:
                $this->setModuloId($value);
                break;
            case 4:
                $this->setFirmaElectronica($value);
                break;
            case 5:
                $this->setFirmaDigital($value);
                break;
            case 6:
                $this->setFirmaFisica($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = AutorizacionFirmaPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setAutorizacionfirmaId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setEstadofirmaautoId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setUsuarioId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setModuloId($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setFirmaElectronica($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setFirmaDigital($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setFirmaFisica($arr[$keys[6]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(AutorizacionFirmaPeer::DATABASE_NAME);

        if ($this->isColumnModified(AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID)) $criteria->add(AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID, $this->autorizacionfirma_id);
        if ($this->isColumnModified(AutorizacionFirmaPeer::ESTADOFIRMAAUTO_ID)) $criteria->add(AutorizacionFirmaPeer::ESTADOFIRMAAUTO_ID, $this->estadofirmaauto_id);
        if ($this->isColumnModified(AutorizacionFirmaPeer::USUARIO_ID)) $criteria->add(AutorizacionFirmaPeer::USUARIO_ID, $this->usuario_id);
        if ($this->isColumnModified(AutorizacionFirmaPeer::MODULO_ID)) $criteria->add(AutorizacionFirmaPeer::MODULO_ID, $this->modulo_id);
        if ($this->isColumnModified(AutorizacionFirmaPeer::FIRMA_ELECTRONICA)) $criteria->add(AutorizacionFirmaPeer::FIRMA_ELECTRONICA, $this->firma_electronica);
        if ($this->isColumnModified(AutorizacionFirmaPeer::FIRMA_DIGITAL)) $criteria->add(AutorizacionFirmaPeer::FIRMA_DIGITAL, $this->firma_digital);
        if ($this->isColumnModified(AutorizacionFirmaPeer::FIRMA_FISICA)) $criteria->add(AutorizacionFirmaPeer::FIRMA_FISICA, $this->firma_fisica);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(AutorizacionFirmaPeer::DATABASE_NAME);
        $criteria->add(AutorizacionFirmaPeer::AUTORIZACIONFIRMA_ID, $this->autorizacionfirma_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getAutorizacionfirmaId();
    }

    /**
     * Generic method to set the primary key (autorizacionfirma_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setAutorizacionfirmaId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getAutorizacionfirmaId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of AutorizacionFirma (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setEstadofirmaautoId($this->getEstadofirmaautoId());
        $copyObj->setUsuarioId($this->getUsuarioId());
        $copyObj->setModuloId($this->getModuloId());
        $copyObj->setFirmaElectronica($this->getFirmaElectronica());
        $copyObj->setFirmaDigital($this->getFirmaDigital());
        $copyObj->setFirmaFisica($this->getFirmaFisica());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getAutFirmaUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addAutFirmaUsuario($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setAutorizacionfirmaId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return AutorizacionFirma Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return AutorizacionFirmaPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new AutorizacionFirmaPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a EstadoFirmaAuto object.
     *
     * @param                  EstadoFirmaAuto $v
     * @return AutorizacionFirma The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEstadoFirmaAuto(EstadoFirmaAuto $v = null)
    {
        if ($v === null) {
            $this->setEstadofirmaautoId(NULL);
        } else {
            $this->setEstadofirmaautoId($v->getEstadofirmaautoId());
        }

        $this->aEstadoFirmaAuto = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the EstadoFirmaAuto object, it will not be re-added.
        if ($v !== null) {
            $v->addAutorizacionFirma($this);
        }


        return $this;
    }


    /**
     * Get the associated EstadoFirmaAuto object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return EstadoFirmaAuto The associated EstadoFirmaAuto object.
     * @throws PropelException
     */
    public function getEstadoFirmaAuto(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEstadoFirmaAuto === null && ($this->estadofirmaauto_id !== null) && $doQuery) {
            $this->aEstadoFirmaAuto = EstadoFirmaAutoQuery::create()->findPk($this->estadofirmaauto_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEstadoFirmaAuto->addAutorizacionFirmas($this);
             */
        }

        return $this->aEstadoFirmaAuto;
    }

    /**
     * Declares an association between this object and a Usuario object.
     *
     * @param                  Usuario $v
     * @return AutorizacionFirma The current object (for fluent API support)
     * @throws PropelException
     */
    public function setUsuario(Usuario $v = null)
    {
        if ($v === null) {
            $this->setUsuarioId(NULL);
        } else {
            $this->setUsuarioId($v->getUsuarioId());
        }

        $this->aUsuario = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Usuario object, it will not be re-added.
        if ($v !== null) {
            $v->addAutorizacionFirma($this);
        }


        return $this;
    }


    /**
     * Get the associated Usuario object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Usuario The associated Usuario object.
     * @throws PropelException
     */
    public function getUsuario(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aUsuario === null && ($this->usuario_id !== null) && $doQuery) {
            $this->aUsuario = UsuarioQuery::create()->findPk($this->usuario_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aUsuario->addAutorizacionFirmas($this);
             */
        }

        return $this->aUsuario;
    }

    /**
     * Declares an association between this object and a Modulo object.
     *
     * @param                  Modulo $v
     * @return AutorizacionFirma The current object (for fluent API support)
     * @throws PropelException
     */
    public function setModulo(Modulo $v = null)
    {
        if ($v === null) {
            $this->setModuloId(NULL);
        } else {
            $this->setModuloId($v->getModuloId());
        }

        $this->aModulo = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Modulo object, it will not be re-added.
        if ($v !== null) {
            $v->addAutorizacionFirma($this);
        }


        return $this;
    }


    /**
     * Get the associated Modulo object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Modulo The associated Modulo object.
     * @throws PropelException
     */
    public function getModulo(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aModulo === null && ($this->modulo_id !== null) && $doQuery) {
            $this->aModulo = ModuloQuery::create()->findPk($this->modulo_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aModulo->addAutorizacionFirmas($this);
             */
        }

        return $this->aModulo;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('AutFirmaUsuario' == $relationName) {
            $this->initAutFirmaUsuarios();
        }
    }

    /**
     * Clears out the collAutFirmaUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return AutorizacionFirma The current object (for fluent API support)
     * @see        addAutFirmaUsuarios()
     */
    public function clearAutFirmaUsuarios()
    {
        $this->collAutFirmaUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collAutFirmaUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collAutFirmaUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialAutFirmaUsuarios($v = true)
    {
        $this->collAutFirmaUsuariosPartial = $v;
    }

    /**
     * Initializes the collAutFirmaUsuarios collection.
     *
     * By default this just sets the collAutFirmaUsuarios collection to an empty array (like clearcollAutFirmaUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initAutFirmaUsuarios($overrideExisting = true)
    {
        if (null !== $this->collAutFirmaUsuarios && !$overrideExisting) {
            return;
        }
        $this->collAutFirmaUsuarios = new PropelObjectCollection();
        $this->collAutFirmaUsuarios->setModel('AutFirmaUsuario');
    }

    /**
     * Gets an array of AutFirmaUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this AutorizacionFirma is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|AutFirmaUsuario[] List of AutFirmaUsuario objects
     * @throws PropelException
     */
    public function getAutFirmaUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collAutFirmaUsuariosPartial && !$this->isNew();
        if (null === $this->collAutFirmaUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collAutFirmaUsuarios) {
                // return empty collection
                $this->initAutFirmaUsuarios();
            } else {
                $collAutFirmaUsuarios = AutFirmaUsuarioQuery::create(null, $criteria)
                    ->filterByAutorizacionFirma($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collAutFirmaUsuariosPartial && count($collAutFirmaUsuarios)) {
                      $this->initAutFirmaUsuarios(false);

                      foreach ($collAutFirmaUsuarios as $obj) {
                        if (false == $this->collAutFirmaUsuarios->contains($obj)) {
                          $this->collAutFirmaUsuarios->append($obj);
                        }
                      }

                      $this->collAutFirmaUsuariosPartial = true;
                    }

                    $collAutFirmaUsuarios->getInternalIterator()->rewind();

                    return $collAutFirmaUsuarios;
                }

                if ($partial && $this->collAutFirmaUsuarios) {
                    foreach ($this->collAutFirmaUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collAutFirmaUsuarios[] = $obj;
                        }
                    }
                }

                $this->collAutFirmaUsuarios = $collAutFirmaUsuarios;
                $this->collAutFirmaUsuariosPartial = false;
            }
        }

        return $this->collAutFirmaUsuarios;
    }

    /**
     * Sets a collection of AutFirmaUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $autFirmaUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function setAutFirmaUsuarios(PropelCollection $autFirmaUsuarios, PropelPDO $con = null)
    {
        $autFirmaUsuariosToDelete = $this->getAutFirmaUsuarios(new Criteria(), $con)->diff($autFirmaUsuarios);


        $this->autFirmaUsuariosScheduledForDeletion = $autFirmaUsuariosToDelete;

        foreach ($autFirmaUsuariosToDelete as $autFirmaUsuarioRemoved) {
            $autFirmaUsuarioRemoved->setAutorizacionFirma(null);
        }

        $this->collAutFirmaUsuarios = null;
        foreach ($autFirmaUsuarios as $autFirmaUsuario) {
            $this->addAutFirmaUsuario($autFirmaUsuario);
        }

        $this->collAutFirmaUsuarios = $autFirmaUsuarios;
        $this->collAutFirmaUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related AutFirmaUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related AutFirmaUsuario objects.
     * @throws PropelException
     */
    public function countAutFirmaUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collAutFirmaUsuariosPartial && !$this->isNew();
        if (null === $this->collAutFirmaUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collAutFirmaUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getAutFirmaUsuarios());
            }
            $query = AutFirmaUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByAutorizacionFirma($this)
                ->count($con);
        }

        return count($this->collAutFirmaUsuarios);
    }

    /**
     * Method called to associate a AutFirmaUsuario object to this object
     * through the AutFirmaUsuario foreign key attribute.
     *
     * @param    AutFirmaUsuario $l AutFirmaUsuario
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function addAutFirmaUsuario(AutFirmaUsuario $l)
    {
        if ($this->collAutFirmaUsuarios === null) {
            $this->initAutFirmaUsuarios();
            $this->collAutFirmaUsuariosPartial = true;
        }

        if (!in_array($l, $this->collAutFirmaUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddAutFirmaUsuario($l);

            if ($this->autFirmaUsuariosScheduledForDeletion and $this->autFirmaUsuariosScheduledForDeletion->contains($l)) {
                $this->autFirmaUsuariosScheduledForDeletion->remove($this->autFirmaUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	AutFirmaUsuario $autFirmaUsuario The autFirmaUsuario object to add.
     */
    protected function doAddAutFirmaUsuario($autFirmaUsuario)
    {
        $this->collAutFirmaUsuarios[]= $autFirmaUsuario;
        $autFirmaUsuario->setAutorizacionFirma($this);
    }

    /**
     * @param	AutFirmaUsuario $autFirmaUsuario The autFirmaUsuario object to remove.
     * @return AutorizacionFirma The current object (for fluent API support)
     */
    public function removeAutFirmaUsuario($autFirmaUsuario)
    {
        if ($this->getAutFirmaUsuarios()->contains($autFirmaUsuario)) {
            $this->collAutFirmaUsuarios->remove($this->collAutFirmaUsuarios->search($autFirmaUsuario));
            if (null === $this->autFirmaUsuariosScheduledForDeletion) {
                $this->autFirmaUsuariosScheduledForDeletion = clone $this->collAutFirmaUsuarios;
                $this->autFirmaUsuariosScheduledForDeletion->clear();
            }
            $this->autFirmaUsuariosScheduledForDeletion[]= $autFirmaUsuario;
            $autFirmaUsuario->setAutorizacionFirma(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AutorizacionFirma is new, it will return
     * an empty collection; or if this AutorizacionFirma has previously
     * been saved, it will retrieve related AutFirmaUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in AutorizacionFirma.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|AutFirmaUsuario[] List of AutFirmaUsuario objects
     */
    public function getAutFirmaUsuariosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = AutFirmaUsuarioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getAutFirmaUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AutorizacionFirma is new, it will return
     * an empty collection; or if this AutorizacionFirma has previously
     * been saved, it will retrieve related AutFirmaUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in AutorizacionFirma.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|AutFirmaUsuario[] List of AutFirmaUsuario objects
     */
    public function getAutFirmaUsuariosJoinRolFirmaUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = AutFirmaUsuarioQuery::create(null, $criteria);
        $query->joinWith('RolFirmaUsuario', $join_behavior);

        return $this->getAutFirmaUsuarios($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->autorizacionfirma_id = null;
        $this->estadofirmaauto_id = null;
        $this->usuario_id = null;
        $this->modulo_id = null;
        $this->firma_electronica = null;
        $this->firma_digital = null;
        $this->firma_fisica = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collAutFirmaUsuarios) {
                foreach ($this->collAutFirmaUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aEstadoFirmaAuto instanceof Persistent) {
              $this->aEstadoFirmaAuto->clearAllReferences($deep);
            }
            if ($this->aUsuario instanceof Persistent) {
              $this->aUsuario->clearAllReferences($deep);
            }
            if ($this->aModulo instanceof Persistent) {
              $this->aModulo->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collAutFirmaUsuarios instanceof PropelCollection) {
            $this->collAutFirmaUsuarios->clearIterator();
        }
        $this->collAutFirmaUsuarios = null;
        $this->aEstadoFirmaAuto = null;
        $this->aUsuario = null;
        $this->aModulo = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(AutorizacionFirmaPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseAutorizacionFirma:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

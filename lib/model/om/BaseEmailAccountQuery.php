<?php


/**
 * Base class that represents a query for the 'EMAIL_ACCOUNT' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * sáb 02 ago 2025 12:03:22
 *
 * @method EmailAccountQuery orderByEmailaccountId($order = Criteria::ASC) Order by the EMAILACCOUNT_ID column
 * @method EmailAccountQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method EmailAccountQuery orderByEmailproviderconfigId($order = Criteria::ASC) Order by the EMAILPROVIDERCONFIG_ID column
 * @method EmailAccountQuery orderByAccountLogin($order = Criteria::ASC) Order by the ACCOUNT_LOGIN column
 * @method EmailAccountQuery orderByAccountPass($order = Criteria::ASC) Order by the ACCOUNT_PASS column
 * @method EmailAccountQuery orderByAuthProvider($order = Criteria::ASC) Order by the AUTH_PROVIDER column
 * @method EmailAccountQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method EmailAccountQuery orderByFechaModificacion($order = Criteria::ASC) Order by the FECHA_MODIFICACION column
 * @method EmailAccountQuery orderByLastSyncData($order = Criteria::ASC) Order by the LAST_SYNC_DATA column
 * @method EmailAccountQuery orderByEsActual($order = Criteria::ASC) Order by the ES_ACTUAL column
 *
 * @method EmailAccountQuery groupByEmailaccountId() Group by the EMAILACCOUNT_ID column
 * @method EmailAccountQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method EmailAccountQuery groupByEmailproviderconfigId() Group by the EMAILPROVIDERCONFIG_ID column
 * @method EmailAccountQuery groupByAccountLogin() Group by the ACCOUNT_LOGIN column
 * @method EmailAccountQuery groupByAccountPass() Group by the ACCOUNT_PASS column
 * @method EmailAccountQuery groupByAuthProvider() Group by the AUTH_PROVIDER column
 * @method EmailAccountQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method EmailAccountQuery groupByFechaModificacion() Group by the FECHA_MODIFICACION column
 * @method EmailAccountQuery groupByLastSyncData() Group by the LAST_SYNC_DATA column
 * @method EmailAccountQuery groupByEsActual() Group by the ES_ACTUAL column
 *
 * @method EmailAccountQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method EmailAccountQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method EmailAccountQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method EmailAccountQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method EmailAccountQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method EmailAccountQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method EmailAccountQuery leftJoinEmailProviderConfig($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailProviderConfig relation
 * @method EmailAccountQuery rightJoinEmailProviderConfig($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailProviderConfig relation
 * @method EmailAccountQuery innerJoinEmailProviderConfig($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailProviderConfig relation
 *
 * @method EmailAccountQuery leftJoinEmailAccountUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the EmailAccountUsuario relation
 * @method EmailAccountQuery rightJoinEmailAccountUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EmailAccountUsuario relation
 * @method EmailAccountQuery innerJoinEmailAccountUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the EmailAccountUsuario relation
 *
 * @method EmailAccountQuery leftJoinEmail($relationAlias = null) Adds a LEFT JOIN clause to the query using the Email relation
 * @method EmailAccountQuery rightJoinEmail($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Email relation
 * @method EmailAccountQuery innerJoinEmail($relationAlias = null) Adds a INNER JOIN clause to the query using the Email relation
 *
 * @method EmailAccount findOne(PropelPDO $con = null) Return the first EmailAccount matching the query
 * @method EmailAccount findOneOrCreate(PropelPDO $con = null) Return the first EmailAccount matching the query, or a new EmailAccount object populated from the query conditions when no match is found
 *
 * @method EmailAccount findOneByUsuarioId(int $USUARIO_ID) Return the first EmailAccount filtered by the USUARIO_ID column
 * @method EmailAccount findOneByEmailproviderconfigId(int $EMAILPROVIDERCONFIG_ID) Return the first EmailAccount filtered by the EMAILPROVIDERCONFIG_ID column
 * @method EmailAccount findOneByAccountLogin(string $ACCOUNT_LOGIN) Return the first EmailAccount filtered by the ACCOUNT_LOGIN column
 * @method EmailAccount findOneByAccountPass(string $ACCOUNT_PASS) Return the first EmailAccount filtered by the ACCOUNT_PASS column
 * @method EmailAccount findOneByAuthProvider(string $AUTH_PROVIDER) Return the first EmailAccount filtered by the AUTH_PROVIDER column
 * @method EmailAccount findOneByFechaCreacion(string $FECHA_CREACION) Return the first EmailAccount filtered by the FECHA_CREACION column
 * @method EmailAccount findOneByFechaModificacion(string $FECHA_MODIFICACION) Return the first EmailAccount filtered by the FECHA_MODIFICACION column
 * @method EmailAccount findOneByLastSyncData(string $LAST_SYNC_DATA) Return the first EmailAccount filtered by the LAST_SYNC_DATA column
 * @method EmailAccount findOneByEsActual(int $ES_ACTUAL) Return the first EmailAccount filtered by the ES_ACTUAL column
 *
 * @method array findByEmailaccountId(int $EMAILACCOUNT_ID) Return EmailAccount objects filtered by the EMAILACCOUNT_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return EmailAccount objects filtered by the USUARIO_ID column
 * @method array findByEmailproviderconfigId(int $EMAILPROVIDERCONFIG_ID) Return EmailAccount objects filtered by the EMAILPROVIDERCONFIG_ID column
 * @method array findByAccountLogin(string $ACCOUNT_LOGIN) Return EmailAccount objects filtered by the ACCOUNT_LOGIN column
 * @method array findByAccountPass(string $ACCOUNT_PASS) Return EmailAccount objects filtered by the ACCOUNT_PASS column
 * @method array findByAuthProvider(string $AUTH_PROVIDER) Return EmailAccount objects filtered by the AUTH_PROVIDER column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return EmailAccount objects filtered by the FECHA_CREACION column
 * @method array findByFechaModificacion(string $FECHA_MODIFICACION) Return EmailAccount objects filtered by the FECHA_MODIFICACION column
 * @method array findByLastSyncData(string $LAST_SYNC_DATA) Return EmailAccount objects filtered by the LAST_SYNC_DATA column
 * @method array findByEsActual(int $ES_ACTUAL) Return EmailAccount objects filtered by the ES_ACTUAL column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEmailAccountQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseEmailAccountQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'EmailAccount';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new EmailAccountQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   EmailAccountQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return EmailAccountQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof EmailAccountQuery) {
            return $criteria;
        }
        $query = new EmailAccountQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   EmailAccount|EmailAccount[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = EmailAccountPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(EmailAccountPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 EmailAccount A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByEmailaccountId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 EmailAccount A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [EMAILACCOUNT_ID], [USUARIO_ID], [EMAILPROVIDERCONFIG_ID], [ACCOUNT_LOGIN], [ACCOUNT_PASS], [AUTH_PROVIDER], [FECHA_CREACION], [FECHA_MODIFICACION], [LAST_SYNC_DATA], [ES_ACTUAL] FROM [EMAIL_ACCOUNT] WHERE [EMAILACCOUNT_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new EmailAccount();
            $obj->hydrate($row);
            EmailAccountPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return EmailAccount|EmailAccount[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|EmailAccount[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the EMAILACCOUNT_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailaccountId(1234); // WHERE EMAILACCOUNT_ID = 1234
     * $query->filterByEmailaccountId(array(12, 34)); // WHERE EMAILACCOUNT_ID IN (12, 34)
     * $query->filterByEmailaccountId(array('min' => 12)); // WHERE EMAILACCOUNT_ID >= 12
     * $query->filterByEmailaccountId(array('max' => 12)); // WHERE EMAILACCOUNT_ID <= 12
     * </code>
     *
     * @param     mixed $emailaccountId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByEmailaccountId($emailaccountId = null, $comparison = null)
    {
        if (is_array($emailaccountId)) {
            $useMinMax = false;
            if (isset($emailaccountId['min'])) {
                $this->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $emailaccountId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($emailaccountId['max'])) {
                $this->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $emailaccountId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $emailaccountId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(EmailAccountPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(EmailAccountPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the EMAILPROVIDERCONFIG_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByEmailproviderconfigId(1234); // WHERE EMAILPROVIDERCONFIG_ID = 1234
     * $query->filterByEmailproviderconfigId(array(12, 34)); // WHERE EMAILPROVIDERCONFIG_ID IN (12, 34)
     * $query->filterByEmailproviderconfigId(array('min' => 12)); // WHERE EMAILPROVIDERCONFIG_ID >= 12
     * $query->filterByEmailproviderconfigId(array('max' => 12)); // WHERE EMAILPROVIDERCONFIG_ID <= 12
     * </code>
     *
     * @see       filterByEmailProviderConfig()
     *
     * @param     mixed $emailproviderconfigId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByEmailproviderconfigId($emailproviderconfigId = null, $comparison = null)
    {
        if (is_array($emailproviderconfigId)) {
            $useMinMax = false;
            if (isset($emailproviderconfigId['min'])) {
                $this->addUsingAlias(EmailAccountPeer::EMAILPROVIDERCONFIG_ID, $emailproviderconfigId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($emailproviderconfigId['max'])) {
                $this->addUsingAlias(EmailAccountPeer::EMAILPROVIDERCONFIG_ID, $emailproviderconfigId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::EMAILPROVIDERCONFIG_ID, $emailproviderconfigId, $comparison);
    }

    /**
     * Filter the query on the ACCOUNT_LOGIN column
     *
     * Example usage:
     * <code>
     * $query->filterByAccountLogin('fooValue');   // WHERE ACCOUNT_LOGIN = 'fooValue'
     * $query->filterByAccountLogin('%fooValue%'); // WHERE ACCOUNT_LOGIN LIKE '%fooValue%'
     * </code>
     *
     * @param     string $accountLogin The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByAccountLogin($accountLogin = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($accountLogin)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $accountLogin)) {
                $accountLogin = str_replace('*', '%', $accountLogin);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::ACCOUNT_LOGIN, $accountLogin, $comparison);
    }

    /**
     * Filter the query on the ACCOUNT_PASS column
     *
     * Example usage:
     * <code>
     * $query->filterByAccountPass('fooValue');   // WHERE ACCOUNT_PASS = 'fooValue'
     * $query->filterByAccountPass('%fooValue%'); // WHERE ACCOUNT_PASS LIKE '%fooValue%'
     * </code>
     *
     * @param     string $accountPass The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByAccountPass($accountPass = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($accountPass)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $accountPass)) {
                $accountPass = str_replace('*', '%', $accountPass);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::ACCOUNT_PASS, $accountPass, $comparison);
    }

    /**
     * Filter the query on the AUTH_PROVIDER column
     *
     * Example usage:
     * <code>
     * $query->filterByAuthProvider('fooValue');   // WHERE AUTH_PROVIDER = 'fooValue'
     * $query->filterByAuthProvider('%fooValue%'); // WHERE AUTH_PROVIDER LIKE '%fooValue%'
     * </code>
     *
     * @param     string $authProvider The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByAuthProvider($authProvider = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($authProvider)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $authProvider)) {
                $authProvider = str_replace('*', '%', $authProvider);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::AUTH_PROVIDER, $authProvider, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('2011-03-14'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion('now'); // WHERE FECHA_CREACION = '2011-03-14'
     * $query->filterByFechaCreacion(array('max' => 'yesterday')); // WHERE FECHA_CREACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (is_array($fechaCreacion)) {
            $useMinMax = false;
            if (isset($fechaCreacion['min'])) {
                $this->addUsingAlias(EmailAccountPeer::FECHA_CREACION, $fechaCreacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreacion['max'])) {
                $this->addUsingAlias(EmailAccountPeer::FECHA_CREACION, $fechaCreacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_MODIFICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaModificacion('2011-03-14'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion('now'); // WHERE FECHA_MODIFICACION = '2011-03-14'
     * $query->filterByFechaModificacion(array('max' => 'yesterday')); // WHERE FECHA_MODIFICACION < '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaModificacion The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByFechaModificacion($fechaModificacion = null, $comparison = null)
    {
        if (is_array($fechaModificacion)) {
            $useMinMax = false;
            if (isset($fechaModificacion['min'])) {
                $this->addUsingAlias(EmailAccountPeer::FECHA_MODIFICACION, $fechaModificacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaModificacion['max'])) {
                $this->addUsingAlias(EmailAccountPeer::FECHA_MODIFICACION, $fechaModificacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::FECHA_MODIFICACION, $fechaModificacion, $comparison);
    }

    /**
     * Filter the query on the LAST_SYNC_DATA column
     *
     * Example usage:
     * <code>
     * $query->filterByLastSyncData('fooValue');   // WHERE LAST_SYNC_DATA = 'fooValue'
     * $query->filterByLastSyncData('%fooValue%'); // WHERE LAST_SYNC_DATA LIKE '%fooValue%'
     * </code>
     *
     * @param     string $lastSyncData The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByLastSyncData($lastSyncData = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($lastSyncData)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $lastSyncData)) {
                $lastSyncData = str_replace('*', '%', $lastSyncData);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::LAST_SYNC_DATA, $lastSyncData, $comparison);
    }

    /**
     * Filter the query on the ES_ACTUAL column
     *
     * Example usage:
     * <code>
     * $query->filterByEsActual(1234); // WHERE ES_ACTUAL = 1234
     * $query->filterByEsActual(array(12, 34)); // WHERE ES_ACTUAL IN (12, 34)
     * $query->filterByEsActual(array('min' => 12)); // WHERE ES_ACTUAL >= 12
     * $query->filterByEsActual(array('max' => 12)); // WHERE ES_ACTUAL <= 12
     * </code>
     *
     * @param     mixed $esActual The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function filterByEsActual($esActual = null, $comparison = null)
    {
        if (is_array($esActual)) {
            $useMinMax = false;
            if (isset($esActual['min'])) {
                $this->addUsingAlias(EmailAccountPeer::ES_ACTUAL, $esActual['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($esActual['max'])) {
                $this->addUsingAlias(EmailAccountPeer::ES_ACTUAL, $esActual['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmailAccountPeer::ES_ACTUAL, $esActual, $comparison);
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailAccountQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(EmailAccountPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmailAccountPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related EmailProviderConfig object
     *
     * @param   EmailProviderConfig|PropelObjectCollection $emailProviderConfig The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailAccountQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailProviderConfig($emailProviderConfig, $comparison = null)
    {
        if ($emailProviderConfig instanceof EmailProviderConfig) {
            return $this
                ->addUsingAlias(EmailAccountPeer::EMAILPROVIDERCONFIG_ID, $emailProviderConfig->getEmailproviderconfigId(), $comparison);
        } elseif ($emailProviderConfig instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmailAccountPeer::EMAILPROVIDERCONFIG_ID, $emailProviderConfig->toKeyValue('PrimaryKey', 'EmailproviderconfigId'), $comparison);
        } else {
            throw new PropelException('filterByEmailProviderConfig() only accepts arguments of type EmailProviderConfig or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailProviderConfig relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function joinEmailProviderConfig($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailProviderConfig');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailProviderConfig');
        }

        return $this;
    }

    /**
     * Use the EmailProviderConfig relation EmailProviderConfig object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailProviderConfigQuery A secondary query class using the current class as primary query
     */
    public function useEmailProviderConfigQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailProviderConfig($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailProviderConfig', 'EmailProviderConfigQuery');
    }

    /**
     * Filter the query by a related EmailAccountUsuario object
     *
     * @param   EmailAccountUsuario|PropelObjectCollection $emailAccountUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailAccountQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmailAccountUsuario($emailAccountUsuario, $comparison = null)
    {
        if ($emailAccountUsuario instanceof EmailAccountUsuario) {
            return $this
                ->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $emailAccountUsuario->getEmailaccountId(), $comparison);
        } elseif ($emailAccountUsuario instanceof PropelObjectCollection) {
            return $this
                ->useEmailAccountUsuarioQuery()
                ->filterByPrimaryKeys($emailAccountUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEmailAccountUsuario() only accepts arguments of type EmailAccountUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EmailAccountUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function joinEmailAccountUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EmailAccountUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EmailAccountUsuario');
        }

        return $this;
    }

    /**
     * Use the EmailAccountUsuario relation EmailAccountUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailAccountUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useEmailAccountUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmailAccountUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EmailAccountUsuario', 'EmailAccountUsuarioQuery');
    }

    /**
     * Filter the query by a related Email object
     *
     * @param   Email|PropelObjectCollection $email  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 EmailAccountQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByEmail($email, $comparison = null)
    {
        if ($email instanceof Email) {
            return $this
                ->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $email->getEmailaccountId(), $comparison);
        } elseif ($email instanceof PropelObjectCollection) {
            return $this
                ->useEmailQuery()
                ->filterByPrimaryKeys($email->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEmail() only accepts arguments of type Email or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Email relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function joinEmail($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Email');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Email');
        }

        return $this;
    }

    /**
     * Use the Email relation Email object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmailQuery A secondary query class using the current class as primary query
     */
    public function useEmailQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinEmail($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Email', 'EmailQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   EmailAccount $emailAccount Object to remove from the list of results
     *
     * @return EmailAccountQuery The current query, for fluid interface
     */
    public function prune($emailAccount = null)
    {
        if ($emailAccount) {
            $this->addUsingAlias(EmailAccountPeer::EMAILACCOUNT_ID, $emailAccount->getEmailaccountId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

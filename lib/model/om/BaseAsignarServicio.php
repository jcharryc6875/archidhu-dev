<?php


/**
 * Base class that represents a row from the 'ASIGNAR_SERVICIO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:47
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseAsignarServicio extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'AsignarServicioPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        AsignarServicioPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the asignarservicio_id field.
     * @var        int
     */
    protected $asignarservicio_id;

    /**
     * The value for the servicio_id field.
     * @var        int
     */
    protected $servicio_id;

    /**
     * The value for the observaciones field.
     * @var        string
     */
    protected $observaciones;

    /**
     * The value for the valor field.
     * @var        int
     */
    protected $valor;

    /**
     * The value for the fecha_asignacion field.
     * @var        string
     */
    protected $fecha_asignacion;

    /**
     * The value for the fecha_ejecucion field.
     * @var        string
     */
    protected $fecha_ejecucion;

    /**
     * @var        Servicio
     */
    protected $aServicio;

    /**
     * @var        PropelObjectCollection|UsuarioAsignadoSolicitud[] Collection to store aggregation of UsuarioAsignadoSolicitud objects.
     */
    protected $collUsuarioAsignadoSolicituds;
    protected $collUsuarioAsignadoSolicitudsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuarioAsignadoSolicitudsScheduledForDeletion = null;

    /**
     * Get the [asignarservicio_id] column value.
     * 
     * @return int
     */
    public function getAsignarservicioId()
    {

        return $this->asignarservicio_id;
    }

    /**
     * Get the [servicio_id] column value.
     * 
     * @return int
     */
    public function getServicioId()
    {

        return $this->servicio_id;
    }

    /**
     * Get the [observaciones] column value.
     * 
     * @return string
     */
    public function getObservaciones()
    {

        return $this->observaciones;
    }

    /**
     * Get the [valor] column value.
     * 
     * @return int
     */
    public function getValor()
    {

        return $this->valor;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_asignacion] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaAsignacion($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_asignacion === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_asignacion);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_asignacion, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [optionally formatted] temporal [fecha_ejecucion] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaEjecucion($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_ejecucion === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_ejecucion);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_ejecucion, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Set the value of [asignarservicio_id] column.
     * 
     * @param  int $v new value
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setAsignarservicioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->asignarservicio_id !== $v) {
            $this->asignarservicio_id = $v;
            $this->modifiedColumns[] = AsignarServicioPeer::ASIGNARSERVICIO_ID;
        }


        return $this;
    } // setAsignarservicioId()

    /**
     * Set the value of [servicio_id] column.
     * 
     * @param  int $v new value
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setServicioId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->servicio_id !== $v) {
            $this->servicio_id = $v;
            $this->modifiedColumns[] = AsignarServicioPeer::SERVICIO_ID;
        }

        if ($this->aServicio !== null && $this->aServicio->getServicioId() !== $v) {
            $this->aServicio = null;
        }


        return $this;
    } // setServicioId()

    /**
     * Set the value of [observaciones] column.
     * 
     * @param  string $v new value
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setObservaciones($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->observaciones !== $v) {
            $this->observaciones = $v;
            $this->modifiedColumns[] = AsignarServicioPeer::OBSERVACIONES;
        }


        return $this;
    } // setObservaciones()

    /**
     * Set the value of [valor] column.
     * 
     * @param  int $v new value
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setValor($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->valor !== $v) {
            $this->valor = $v;
            $this->modifiedColumns[] = AsignarServicioPeer::VALOR;
        }


        return $this;
    } // setValor()

    /**
     * Sets the value of [fecha_asignacion] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setFechaAsignacion($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_asignacion !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_asignacion !== null && $tmpDt = new DateTime($this->fecha_asignacion)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_asignacion = $newDateAsString;
                $this->modifiedColumns[] = AsignarServicioPeer::FECHA_ASIGNACION;
            }
        } // if either are not null


        return $this;
    } // setFechaAsignacion()

    /**
     * Sets the value of [fecha_ejecucion] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setFechaEjecucion($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_ejecucion !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_ejecucion !== null && $tmpDt = new DateTime($this->fecha_ejecucion)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_ejecucion = $newDateAsString;
                $this->modifiedColumns[] = AsignarServicioPeer::FECHA_EJECUCION;
            }
        } // if either are not null


        return $this;
    } // setFechaEjecucion()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->asignarservicio_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->servicio_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->observaciones = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->valor = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
            $this->fecha_asignacion = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->fecha_ejecucion = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 6; // 6 = AsignarServicioPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating AsignarServicio object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aServicio !== null && $this->servicio_id !== $this->aServicio->getServicioId()) {
            $this->aServicio = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(AsignarServicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = AsignarServicioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aServicio = null;
            $this->collUsuarioAsignadoSolicituds = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(AsignarServicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = AsignarServicioQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseAsignarServicio:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseAsignarServicio:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(AsignarServicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseAsignarServicio:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseAsignarServicio:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                AsignarServicioPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aServicio !== null) {
                if ($this->aServicio->isModified() || $this->aServicio->isNew()) {
                    $affectedRows += $this->aServicio->save($con);
                }
                $this->setServicio($this->aServicio);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->usuarioAsignadoSolicitudsScheduledForDeletion !== null) {
                if (!$this->usuarioAsignadoSolicitudsScheduledForDeletion->isEmpty()) {
                    UsuarioAsignadoSolicitudQuery::create()
                        ->filterByPrimaryKeys($this->usuarioAsignadoSolicitudsScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->usuarioAsignadoSolicitudsScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarioAsignadoSolicituds !== null) {
                foreach ($this->collUsuarioAsignadoSolicituds as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = AsignarServicioPeer::ASIGNARSERVICIO_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(AsignarServicioPeer::ASIGNARSERVICIO_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . AsignarServicioPeer::ASIGNARSERVICIO_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(AsignarServicioPeer::ASIGNARSERVICIO_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setAsignarservicioId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aServicio !== null) {
                if (!$this->aServicio->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aServicio->getValidationFailures());
                }
            }


            if (($retval = AsignarServicioPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collUsuarioAsignadoSolicituds !== null) {
                    foreach ($this->collUsuarioAsignadoSolicituds as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = AsignarServicioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getAsignarservicioId();
                break;
            case 1:
                return $this->getServicioId();
                break;
            case 2:
                return $this->getObservaciones();
                break;
            case 3:
                return $this->getValor();
                break;
            case 4:
                return $this->getFechaAsignacion();
                break;
            case 5:
                return $this->getFechaEjecucion();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['AsignarServicio'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['AsignarServicio'][$this->getPrimaryKey()] = true;
        $keys = AsignarServicioPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getAsignarservicioId(),
            $keys[1] => $this->getServicioId(),
            $keys[2] => $this->getObservaciones(),
            $keys[3] => $this->getValor(),
            $keys[4] => $this->getFechaAsignacion(),
            $keys[5] => $this->getFechaEjecucion(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aServicio) {
                $result['Servicio'] = $this->aServicio->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collUsuarioAsignadoSolicituds) {
                $result['UsuarioAsignadoSolicituds'] = $this->collUsuarioAsignadoSolicituds->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = AsignarServicioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setAsignarservicioId($value);
                break;
            case 1:
                $this->setServicioId($value);
                break;
            case 2:
                $this->setObservaciones($value);
                break;
            case 3:
                $this->setValor($value);
                break;
            case 4:
                $this->setFechaAsignacion($value);
                break;
            case 5:
                $this->setFechaEjecucion($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = AsignarServicioPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setAsignarservicioId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setServicioId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setObservaciones($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setValor($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setFechaAsignacion($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setFechaEjecucion($arr[$keys[5]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(AsignarServicioPeer::DATABASE_NAME);

        if ($this->isColumnModified(AsignarServicioPeer::ASIGNARSERVICIO_ID)) $criteria->add(AsignarServicioPeer::ASIGNARSERVICIO_ID, $this->asignarservicio_id);
        if ($this->isColumnModified(AsignarServicioPeer::SERVICIO_ID)) $criteria->add(AsignarServicioPeer::SERVICIO_ID, $this->servicio_id);
        if ($this->isColumnModified(AsignarServicioPeer::OBSERVACIONES)) $criteria->add(AsignarServicioPeer::OBSERVACIONES, $this->observaciones);
        if ($this->isColumnModified(AsignarServicioPeer::VALOR)) $criteria->add(AsignarServicioPeer::VALOR, $this->valor);
        if ($this->isColumnModified(AsignarServicioPeer::FECHA_ASIGNACION)) $criteria->add(AsignarServicioPeer::FECHA_ASIGNACION, $this->fecha_asignacion);
        if ($this->isColumnModified(AsignarServicioPeer::FECHA_EJECUCION)) $criteria->add(AsignarServicioPeer::FECHA_EJECUCION, $this->fecha_ejecucion);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(AsignarServicioPeer::DATABASE_NAME);
        $criteria->add(AsignarServicioPeer::ASIGNARSERVICIO_ID, $this->asignarservicio_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getAsignarservicioId();
    }

    /**
     * Generic method to set the primary key (asignarservicio_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setAsignarservicioId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getAsignarservicioId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of AsignarServicio (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setServicioId($this->getServicioId());
        $copyObj->setObservaciones($this->getObservaciones());
        $copyObj->setValor($this->getValor());
        $copyObj->setFechaAsignacion($this->getFechaAsignacion());
        $copyObj->setFechaEjecucion($this->getFechaEjecucion());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getUsuarioAsignadoSolicituds() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuarioAsignadoSolicitud($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setAsignarservicioId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return AsignarServicio Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return AsignarServicioPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new AsignarServicioPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Servicio object.
     *
     * @param                  Servicio $v
     * @return AsignarServicio The current object (for fluent API support)
     * @throws PropelException
     */
    public function setServicio(Servicio $v = null)
    {
        if ($v === null) {
            $this->setServicioId(NULL);
        } else {
            $this->setServicioId($v->getServicioId());
        }

        $this->aServicio = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Servicio object, it will not be re-added.
        if ($v !== null) {
            $v->addAsignarServicio($this);
        }


        return $this;
    }


    /**
     * Get the associated Servicio object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return Servicio The associated Servicio object.
     * @throws PropelException
     */
    public function getServicio(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aServicio === null && ($this->servicio_id !== null) && $doQuery) {
            $this->aServicio = ServicioQuery::create()->findPk($this->servicio_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aServicio->addAsignarServicios($this);
             */
        }

        return $this->aServicio;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('UsuarioAsignadoSolicitud' == $relationName) {
            $this->initUsuarioAsignadoSolicituds();
        }
    }

    /**
     * Clears out the collUsuarioAsignadoSolicituds collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return AsignarServicio The current object (for fluent API support)
     * @see        addUsuarioAsignadoSolicituds()
     */
    public function clearUsuarioAsignadoSolicituds()
    {
        $this->collUsuarioAsignadoSolicituds = null; // important to set this to null since that means it is uninitialized
        $this->collUsuarioAsignadoSolicitudsPartial = null;

        return $this;
    }

    /**
     * reset is the collUsuarioAsignadoSolicituds collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarioAsignadoSolicituds($v = true)
    {
        $this->collUsuarioAsignadoSolicitudsPartial = $v;
    }

    /**
     * Initializes the collUsuarioAsignadoSolicituds collection.
     *
     * By default this just sets the collUsuarioAsignadoSolicituds collection to an empty array (like clearcollUsuarioAsignadoSolicituds());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarioAsignadoSolicituds($overrideExisting = true)
    {
        if (null !== $this->collUsuarioAsignadoSolicituds && !$overrideExisting) {
            return;
        }
        $this->collUsuarioAsignadoSolicituds = new PropelObjectCollection();
        $this->collUsuarioAsignadoSolicituds->setModel('UsuarioAsignadoSolicitud');
    }

    /**
     * Gets an array of UsuarioAsignadoSolicitud objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this AsignarServicio is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UsuarioAsignadoSolicitud[] List of UsuarioAsignadoSolicitud objects
     * @throws PropelException
     */
    public function getUsuarioAsignadoSolicituds($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioAsignadoSolicitudsPartial && !$this->isNew();
        if (null === $this->collUsuarioAsignadoSolicituds || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarioAsignadoSolicituds) {
                // return empty collection
                $this->initUsuarioAsignadoSolicituds();
            } else {
                $collUsuarioAsignadoSolicituds = UsuarioAsignadoSolicitudQuery::create(null, $criteria)
                    ->filterByAsignarServicio($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuarioAsignadoSolicitudsPartial && count($collUsuarioAsignadoSolicituds)) {
                      $this->initUsuarioAsignadoSolicituds(false);

                      foreach ($collUsuarioAsignadoSolicituds as $obj) {
                        if (false == $this->collUsuarioAsignadoSolicituds->contains($obj)) {
                          $this->collUsuarioAsignadoSolicituds->append($obj);
                        }
                      }

                      $this->collUsuarioAsignadoSolicitudsPartial = true;
                    }

                    $collUsuarioAsignadoSolicituds->getInternalIterator()->rewind();

                    return $collUsuarioAsignadoSolicituds;
                }

                if ($partial && $this->collUsuarioAsignadoSolicituds) {
                    foreach ($this->collUsuarioAsignadoSolicituds as $obj) {
                        if ($obj->isNew()) {
                            $collUsuarioAsignadoSolicituds[] = $obj;
                        }
                    }
                }

                $this->collUsuarioAsignadoSolicituds = $collUsuarioAsignadoSolicituds;
                $this->collUsuarioAsignadoSolicitudsPartial = false;
            }
        }

        return $this->collUsuarioAsignadoSolicituds;
    }

    /**
     * Sets a collection of UsuarioAsignadoSolicitud objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarioAsignadoSolicituds A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function setUsuarioAsignadoSolicituds(PropelCollection $usuarioAsignadoSolicituds, PropelPDO $con = null)
    {
        $usuarioAsignadoSolicitudsToDelete = $this->getUsuarioAsignadoSolicituds(new Criteria(), $con)->diff($usuarioAsignadoSolicituds);


        $this->usuarioAsignadoSolicitudsScheduledForDeletion = $usuarioAsignadoSolicitudsToDelete;

        foreach ($usuarioAsignadoSolicitudsToDelete as $usuarioAsignadoSolicitudRemoved) {
            $usuarioAsignadoSolicitudRemoved->setAsignarServicio(null);
        }

        $this->collUsuarioAsignadoSolicituds = null;
        foreach ($usuarioAsignadoSolicituds as $usuarioAsignadoSolicitud) {
            $this->addUsuarioAsignadoSolicitud($usuarioAsignadoSolicitud);
        }

        $this->collUsuarioAsignadoSolicituds = $usuarioAsignadoSolicituds;
        $this->collUsuarioAsignadoSolicitudsPartial = false;

        return $this;
    }

    /**
     * Returns the number of related UsuarioAsignadoSolicitud objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UsuarioAsignadoSolicitud objects.
     * @throws PropelException
     */
    public function countUsuarioAsignadoSolicituds(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioAsignadoSolicitudsPartial && !$this->isNew();
        if (null === $this->collUsuarioAsignadoSolicituds || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarioAsignadoSolicituds) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getUsuarioAsignadoSolicituds());
            }
            $query = UsuarioAsignadoSolicitudQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByAsignarServicio($this)
                ->count($con);
        }

        return count($this->collUsuarioAsignadoSolicituds);
    }

    /**
     * Method called to associate a UsuarioAsignadoSolicitud object to this object
     * through the UsuarioAsignadoSolicitud foreign key attribute.
     *
     * @param    UsuarioAsignadoSolicitud $l UsuarioAsignadoSolicitud
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function addUsuarioAsignadoSolicitud(UsuarioAsignadoSolicitud $l)
    {
        if ($this->collUsuarioAsignadoSolicituds === null) {
            $this->initUsuarioAsignadoSolicituds();
            $this->collUsuarioAsignadoSolicitudsPartial = true;
        }

        if (!in_array($l, $this->collUsuarioAsignadoSolicituds->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddUsuarioAsignadoSolicitud($l);

            if ($this->usuarioAsignadoSolicitudsScheduledForDeletion and $this->usuarioAsignadoSolicitudsScheduledForDeletion->contains($l)) {
                $this->usuarioAsignadoSolicitudsScheduledForDeletion->remove($this->usuarioAsignadoSolicitudsScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	UsuarioAsignadoSolicitud $usuarioAsignadoSolicitud The usuarioAsignadoSolicitud object to add.
     */
    protected function doAddUsuarioAsignadoSolicitud($usuarioAsignadoSolicitud)
    {
        $this->collUsuarioAsignadoSolicituds[]= $usuarioAsignadoSolicitud;
        $usuarioAsignadoSolicitud->setAsignarServicio($this);
    }

    /**
     * @param	UsuarioAsignadoSolicitud $usuarioAsignadoSolicitud The usuarioAsignadoSolicitud object to remove.
     * @return AsignarServicio The current object (for fluent API support)
     */
    public function removeUsuarioAsignadoSolicitud($usuarioAsignadoSolicitud)
    {
        if ($this->getUsuarioAsignadoSolicituds()->contains($usuarioAsignadoSolicitud)) {
            $this->collUsuarioAsignadoSolicituds->remove($this->collUsuarioAsignadoSolicituds->search($usuarioAsignadoSolicitud));
            if (null === $this->usuarioAsignadoSolicitudsScheduledForDeletion) {
                $this->usuarioAsignadoSolicitudsScheduledForDeletion = clone $this->collUsuarioAsignadoSolicituds;
                $this->usuarioAsignadoSolicitudsScheduledForDeletion->clear();
            }
            $this->usuarioAsignadoSolicitudsScheduledForDeletion[]= clone $usuarioAsignadoSolicitud;
            $usuarioAsignadoSolicitud->setAsignarServicio(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AsignarServicio is new, it will return
     * an empty collection; or if this AsignarServicio has previously
     * been saved, it will retrieve related UsuarioAsignadoSolicituds from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in AsignarServicio.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioAsignadoSolicitud[] List of UsuarioAsignadoSolicitud objects
     */
    public function getUsuarioAsignadoSolicitudsJoinRolUsuarioAsignacionServicio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioAsignadoSolicitudQuery::create(null, $criteria);
        $query->joinWith('RolUsuarioAsignacionServicio', $join_behavior);

        return $this->getUsuarioAsignadoSolicituds($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this AsignarServicio is new, it will return
     * an empty collection; or if this AsignarServicio has previously
     * been saved, it will retrieve related UsuarioAsignadoSolicituds from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in AsignarServicio.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioAsignadoSolicitud[] List of UsuarioAsignadoSolicitud objects
     */
    public function getUsuarioAsignadoSolicitudsJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioAsignadoSolicitudQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getUsuarioAsignadoSolicituds($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->asignarservicio_id = null;
        $this->servicio_id = null;
        $this->observaciones = null;
        $this->valor = null;
        $this->fecha_asignacion = null;
        $this->fecha_ejecucion = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collUsuarioAsignadoSolicituds) {
                foreach ($this->collUsuarioAsignadoSolicituds as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aServicio instanceof Persistent) {
              $this->aServicio->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collUsuarioAsignadoSolicituds instanceof PropelCollection) {
            $this->collUsuarioAsignadoSolicituds->clearIterator();
        }
        $this->collUsuarioAsignadoSolicituds = null;
        $this->aServicio = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(AsignarServicioPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseAsignarServicio:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

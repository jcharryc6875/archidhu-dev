<?php


/**
 * Base class that represents a row from the 'REDIRECCION' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:42
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseRedireccion extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'RedireccionPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        RedireccionPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the redireccion_id field.
     * @var        int
     */
    protected $redireccion_id;

    /**
     * The value for the estadoredireccion_id field.
     * @var        int
     */
    protected $estadoredireccion_id;

    /**
     * The value for the motivo field.
     * @var        string
     */
    protected $motivo;

    /**
     * The value for the fecha_inicial field.
     * @var        string
     */
    protected $fecha_inicial;

    /**
     * The value for the fecha_final field.
     * @var        string
     */
    protected $fecha_final;

    /**
     * @var        EstadoRedireccion
     */
    protected $aEstadoRedireccion;

    /**
     * @var        PropelObjectCollection|RedireccionUsuario[] Collection to store aggregation of RedireccionUsuario objects.
     */
    protected $collRedireccionUsuarios;
    protected $collRedireccionUsuariosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $redireccionUsuariosScheduledForDeletion = null;

    /**
     * Get the [redireccion_id] column value.
     * 
     * @return int
     */
    public function getRedireccionId()
    {

        return $this->redireccion_id;
    }

    /**
     * Get the [estadoredireccion_id] column value.
     * 
     * @return int
     */
    public function getEstadoredireccionId()
    {

        return $this->estadoredireccion_id;
    }

    /**
     * Get the [motivo] column value.
     * 
     * @return string
     */
    public function getMotivo()
    {

        return $this->motivo;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_inicial] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaInicial($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_inicial === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_inicial);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_inicial, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Get the [optionally formatted] temporal [fecha_final] column value.
     * 
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaFinal($format = 'Y-m-d G:i:s')
    {
        if ($this->fecha_final === null) {
            return null;
        }


        try {
            $dt = new DateTime($this->fecha_final);
        } catch (Exception $x) {
            throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_final, true), $x);
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        }

        if (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        }

        return $dt->format($format);
        
    }

    /**
     * Set the value of [redireccion_id] column.
     * 
     * @param  int $v new value
     * @return Redireccion The current object (for fluent API support)
     */
    public function setRedireccionId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->redireccion_id !== $v) {
            $this->redireccion_id = $v;
            $this->modifiedColumns[] = RedireccionPeer::REDIRECCION_ID;
        }


        return $this;
    } // setRedireccionId()

    /**
     * Set the value of [estadoredireccion_id] column.
     * 
     * @param  int $v new value
     * @return Redireccion The current object (for fluent API support)
     */
    public function setEstadoredireccionId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->estadoredireccion_id !== $v) {
            $this->estadoredireccion_id = $v;
            $this->modifiedColumns[] = RedireccionPeer::ESTADOREDIRECCION_ID;
        }

        if ($this->aEstadoRedireccion !== null && $this->aEstadoRedireccion->getEstadoredireccionId() !== $v) {
            $this->aEstadoRedireccion = null;
        }


        return $this;
    } // setEstadoredireccionId()

    /**
     * Set the value of [motivo] column.
     * 
     * @param  string $v new value
     * @return Redireccion The current object (for fluent API support)
     */
    public function setMotivo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->motivo !== $v) {
            $this->motivo = $v;
            $this->modifiedColumns[] = RedireccionPeer::MOTIVO;
        }


        return $this;
    } // setMotivo()

    /**
     * Sets the value of [fecha_inicial] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return Redireccion The current object (for fluent API support)
     */
    public function setFechaInicial($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_inicial !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_inicial !== null && $tmpDt = new DateTime($this->fecha_inicial)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_inicial = $newDateAsString;
                $this->modifiedColumns[] = RedireccionPeer::FECHA_INICIAL;
            }
        } // if either are not null


        return $this;
    } // setFechaInicial()

    /**
     * Sets the value of [fecha_final] column to a normalized version of the date/time value specified.
     * 
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return Redireccion The current object (for fluent API support)
     */
    public function setFechaFinal($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_final !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_final !== null && $tmpDt = new DateTime($this->fecha_final)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_final = $newDateAsString;
                $this->modifiedColumns[] = RedireccionPeer::FECHA_FINAL;
            }
        } // if either are not null


        return $this;
    } // setFechaFinal()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->redireccion_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->estadoredireccion_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->motivo = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->fecha_inicial = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->fecha_final = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 5; // 5 = RedireccionPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Redireccion object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEstadoRedireccion !== null && $this->estadoredireccion_id !== $this->aEstadoRedireccion->getEstadoredireccionId()) {
            $this->aEstadoRedireccion = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RedireccionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = RedireccionPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEstadoRedireccion = null;
            $this->collRedireccionUsuarios = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RedireccionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = RedireccionQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseRedireccion:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseRedireccion:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(RedireccionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseRedireccion:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseRedireccion:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                RedireccionPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEstadoRedireccion !== null) {
                if ($this->aEstadoRedireccion->isModified() || $this->aEstadoRedireccion->isNew()) {
                    $affectedRows += $this->aEstadoRedireccion->save($con);
                }
                $this->setEstadoRedireccion($this->aEstadoRedireccion);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->redireccionUsuariosScheduledForDeletion !== null) {
                if (!$this->redireccionUsuariosScheduledForDeletion->isEmpty()) {
                    RedireccionUsuarioQuery::create()
                        ->filterByPrimaryKeys($this->redireccionUsuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->redireccionUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collRedireccionUsuarios !== null) {
                foreach ($this->collRedireccionUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = RedireccionPeer::REDIRECCION_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(RedireccionPeer::REDIRECCION_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . RedireccionPeer::REDIRECCION_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(RedireccionPeer::REDIRECCION_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setRedireccionId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their corresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEstadoRedireccion !== null) {
                if (!$this->aEstadoRedireccion->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEstadoRedireccion->getValidationFailures());
                }
            }


            if (($retval = RedireccionPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collRedireccionUsuarios !== null) {
                    foreach ($this->collRedireccionUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = RedireccionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getRedireccionId();
                break;
            case 1:
                return $this->getEstadoredireccionId();
                break;
            case 2:
                return $this->getMotivo();
                break;
            case 3:
                return $this->getFechaInicial();
                break;
            case 4:
                return $this->getFechaFinal();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Redireccion'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Redireccion'][$this->getPrimaryKey()] = true;
        $keys = RedireccionPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getRedireccionId(),
            $keys[1] => $this->getEstadoredireccionId(),
            $keys[2] => $this->getMotivo(),
            $keys[3] => $this->getFechaInicial(),
            $keys[4] => $this->getFechaFinal(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->aEstadoRedireccion) {
                $result['EstadoRedireccion'] = $this->aEstadoRedireccion->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collRedireccionUsuarios) {
                $result['RedireccionUsuarios'] = $this->collRedireccionUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = RedireccionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setRedireccionId($value);
                break;
            case 1:
                $this->setEstadoredireccionId($value);
                break;
            case 2:
                $this->setMotivo($value);
                break;
            case 3:
                $this->setFechaInicial($value);
                break;
            case 4:
                $this->setFechaFinal($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = RedireccionPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setRedireccionId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setEstadoredireccionId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setMotivo($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setFechaInicial($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setFechaFinal($arr[$keys[4]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(RedireccionPeer::DATABASE_NAME);

        if ($this->isColumnModified(RedireccionPeer::REDIRECCION_ID)) $criteria->add(RedireccionPeer::REDIRECCION_ID, $this->redireccion_id);
        if ($this->isColumnModified(RedireccionPeer::ESTADOREDIRECCION_ID)) $criteria->add(RedireccionPeer::ESTADOREDIRECCION_ID, $this->estadoredireccion_id);
        if ($this->isColumnModified(RedireccionPeer::MOTIVO)) $criteria->add(RedireccionPeer::MOTIVO, $this->motivo);
        if ($this->isColumnModified(RedireccionPeer::FECHA_INICIAL)) $criteria->add(RedireccionPeer::FECHA_INICIAL, $this->fecha_inicial);
        if ($this->isColumnModified(RedireccionPeer::FECHA_FINAL)) $criteria->add(RedireccionPeer::FECHA_FINAL, $this->fecha_final);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(RedireccionPeer::DATABASE_NAME);
        $criteria->add(RedireccionPeer::REDIRECCION_ID, $this->redireccion_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getRedireccionId();
    }

    /**
     * Generic method to set the primary key (redireccion_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setRedireccionId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getRedireccionId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Redireccion (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setEstadoredireccionId($this->getEstadoredireccionId());
        $copyObj->setMotivo($this->getMotivo());
        $copyObj->setFechaInicial($this->getFechaInicial());
        $copyObj->setFechaFinal($this->getFechaFinal());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getRedireccionUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addRedireccionUsuario($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setRedireccionId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Redireccion Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return RedireccionPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new RedireccionPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a EstadoRedireccion object.
     *
     * @param                  EstadoRedireccion $v
     * @return Redireccion The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEstadoRedireccion(EstadoRedireccion $v = null)
    {
        if ($v === null) {
            $this->setEstadoredireccionId(NULL);
        } else {
            $this->setEstadoredireccionId($v->getEstadoredireccionId());
        }

        $this->aEstadoRedireccion = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the EstadoRedireccion object, it will not be re-added.
        if ($v !== null) {
            $v->addRedireccion($this);
        }


        return $this;
    }


    /**
     * Get the associated EstadoRedireccion object
     *
     * @param PropelPDO $con Optional Connection object.
     * @param $doQuery Executes a query to get the object if required
     * @return EstadoRedireccion The associated EstadoRedireccion object.
     * @throws PropelException
     */
    public function getEstadoRedireccion(PropelPDO $con = null, $doQuery = true)
    {
        if ($this->aEstadoRedireccion === null && ($this->estadoredireccion_id !== null) && $doQuery) {
            $this->aEstadoRedireccion = EstadoRedireccionQuery::create()->findPk($this->estadoredireccion_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEstadoRedireccion->addRedireccions($this);
             */
        }

        return $this->aEstadoRedireccion;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('RedireccionUsuario' == $relationName) {
            $this->initRedireccionUsuarios();
        }
    }

    /**
     * Clears out the collRedireccionUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Redireccion The current object (for fluent API support)
     * @see        addRedireccionUsuarios()
     */
    public function clearRedireccionUsuarios()
    {
        $this->collRedireccionUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collRedireccionUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collRedireccionUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialRedireccionUsuarios($v = true)
    {
        $this->collRedireccionUsuariosPartial = $v;
    }

    /**
     * Initializes the collRedireccionUsuarios collection.
     *
     * By default this just sets the collRedireccionUsuarios collection to an empty array (like clearcollRedireccionUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initRedireccionUsuarios($overrideExisting = true)
    {
        if (null !== $this->collRedireccionUsuarios && !$overrideExisting) {
            return;
        }
        $this->collRedireccionUsuarios = new PropelObjectCollection();
        $this->collRedireccionUsuarios->setModel('RedireccionUsuario');
    }

    /**
     * Gets an array of RedireccionUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Redireccion is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|RedireccionUsuario[] List of RedireccionUsuario objects
     * @throws PropelException
     */
    public function getRedireccionUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collRedireccionUsuariosPartial && !$this->isNew();
        if (null === $this->collRedireccionUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collRedireccionUsuarios) {
                // return empty collection
                $this->initRedireccionUsuarios();
            } else {
                $collRedireccionUsuarios = RedireccionUsuarioQuery::create(null, $criteria)
                    ->filterByRedireccion($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collRedireccionUsuariosPartial && count($collRedireccionUsuarios)) {
                      $this->initRedireccionUsuarios(false);

                      foreach ($collRedireccionUsuarios as $obj) {
                        if (false == $this->collRedireccionUsuarios->contains($obj)) {
                          $this->collRedireccionUsuarios->append($obj);
                        }
                      }

                      $this->collRedireccionUsuariosPartial = true;
                    }

                    $collRedireccionUsuarios->getInternalIterator()->rewind();

                    return $collRedireccionUsuarios;
                }

                if ($partial && $this->collRedireccionUsuarios) {
                    foreach ($this->collRedireccionUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collRedireccionUsuarios[] = $obj;
                        }
                    }
                }

                $this->collRedireccionUsuarios = $collRedireccionUsuarios;
                $this->collRedireccionUsuariosPartial = false;
            }
        }

        return $this->collRedireccionUsuarios;
    }

    /**
     * Sets a collection of RedireccionUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $redireccionUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Redireccion The current object (for fluent API support)
     */
    public function setRedireccionUsuarios(PropelCollection $redireccionUsuarios, PropelPDO $con = null)
    {
        $redireccionUsuariosToDelete = $this->getRedireccionUsuarios(new Criteria(), $con)->diff($redireccionUsuarios);


        $this->redireccionUsuariosScheduledForDeletion = $redireccionUsuariosToDelete;

        foreach ($redireccionUsuariosToDelete as $redireccionUsuarioRemoved) {
            $redireccionUsuarioRemoved->setRedireccion(null);
        }

        $this->collRedireccionUsuarios = null;
        foreach ($redireccionUsuarios as $redireccionUsuario) {
            $this->addRedireccionUsuario($redireccionUsuario);
        }

        $this->collRedireccionUsuarios = $redireccionUsuarios;
        $this->collRedireccionUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related RedireccionUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related RedireccionUsuario objects.
     * @throws PropelException
     */
    public function countRedireccionUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collRedireccionUsuariosPartial && !$this->isNew();
        if (null === $this->collRedireccionUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collRedireccionUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getRedireccionUsuarios());
            }
            $query = RedireccionUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByRedireccion($this)
                ->count($con);
        }

        return count($this->collRedireccionUsuarios);
    }

    /**
     * Method called to associate a RedireccionUsuario object to this object
     * through the RedireccionUsuario foreign key attribute.
     *
     * @param    RedireccionUsuario $l RedireccionUsuario
     * @return Redireccion The current object (for fluent API support)
     */
    public function addRedireccionUsuario(RedireccionUsuario $l)
    {
        if ($this->collRedireccionUsuarios === null) {
            $this->initRedireccionUsuarios();
            $this->collRedireccionUsuariosPartial = true;
        }

        if (!in_array($l, $this->collRedireccionUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddRedireccionUsuario($l);

            if ($this->redireccionUsuariosScheduledForDeletion and $this->redireccionUsuariosScheduledForDeletion->contains($l)) {
                $this->redireccionUsuariosScheduledForDeletion->remove($this->redireccionUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	RedireccionUsuario $redireccionUsuario The redireccionUsuario object to add.
     */
    protected function doAddRedireccionUsuario($redireccionUsuario)
    {
        $this->collRedireccionUsuarios[]= $redireccionUsuario;
        $redireccionUsuario->setRedireccion($this);
    }

    /**
     * @param	RedireccionUsuario $redireccionUsuario The redireccionUsuario object to remove.
     * @return Redireccion The current object (for fluent API support)
     */
    public function removeRedireccionUsuario($redireccionUsuario)
    {
        if ($this->getRedireccionUsuarios()->contains($redireccionUsuario)) {
            $this->collRedireccionUsuarios->remove($this->collRedireccionUsuarios->search($redireccionUsuario));
            if (null === $this->redireccionUsuariosScheduledForDeletion) {
                $this->redireccionUsuariosScheduledForDeletion = clone $this->collRedireccionUsuarios;
                $this->redireccionUsuariosScheduledForDeletion->clear();
            }
            $this->redireccionUsuariosScheduledForDeletion[]= clone $redireccionUsuario;
            $redireccionUsuario->setRedireccion(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Redireccion is new, it will return
     * an empty collection; or if this Redireccion has previously
     * been saved, it will retrieve related RedireccionUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Redireccion.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|RedireccionUsuario[] List of RedireccionUsuario objects
     */
    public function getRedireccionUsuariosJoinRolUsuRedireccion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = RedireccionUsuarioQuery::create(null, $criteria);
        $query->joinWith('RolUsuRedireccion', $join_behavior);

        return $this->getRedireccionUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Redireccion is new, it will return
     * an empty collection; or if this Redireccion has previously
     * been saved, it will retrieve related RedireccionUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Redireccion.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|RedireccionUsuario[] List of RedireccionUsuario objects
     */
    public function getRedireccionUsuariosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = RedireccionUsuarioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getRedireccionUsuarios($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->redireccion_id = null;
        $this->estadoredireccion_id = null;
        $this->motivo = null;
        $this->fecha_inicial = null;
        $this->fecha_final = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collRedireccionUsuarios) {
                foreach ($this->collRedireccionUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->aEstadoRedireccion instanceof Persistent) {
              $this->aEstadoRedireccion->clearAllReferences($deep);
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collRedireccionUsuarios instanceof PropelCollection) {
            $this->collRedireccionUsuarios->clearIterator();
        }
        $this->collRedireccionUsuarios = null;
        $this->aEstadoRedireccion = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(RedireccionPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseRedireccion:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

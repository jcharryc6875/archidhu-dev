<?php


/**
 * Base class that represents a query for the 'FACT_VARIABLE_VALUES' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:56
 *
 * @method FactVariableValuesQuery orderByFactvariablevaluesId($order = Criteria::ASC) Order by the FACTVARIABLEVALUES_ID column
 * @method FactVariableValuesQuery orderByFacturaId($order = Criteria::ASC) Order by the FACTURA_ID column
 * @method FactVariableValuesQuery orderByFactvariableId($order = Criteria::ASC) Order by the FACTVARIABLE_ID column
 * @method FactVariableValuesQuery orderByVariableValue($order = Criteria::ASC) Order by the VARIABLE_VALUE column
 *
 * @method FactVariableValuesQuery groupByFactvariablevaluesId() Group by the FACTVARIABLEVALUES_ID column
 * @method FactVariableValuesQuery groupByFacturaId() Group by the FACTURA_ID column
 * @method FactVariableValuesQuery groupByFactvariableId() Group by the FACTVARIABLE_ID column
 * @method FactVariableValuesQuery groupByVariableValue() Group by the VARIABLE_VALUE column
 *
 * @method FactVariableValuesQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method FactVariableValuesQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method FactVariableValuesQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method FactVariableValuesQuery leftJoinFactura($relationAlias = null) Adds a LEFT JOIN clause to the query using the Factura relation
 * @method FactVariableValuesQuery rightJoinFactura($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Factura relation
 * @method FactVariableValuesQuery innerJoinFactura($relationAlias = null) Adds a INNER JOIN clause to the query using the Factura relation
 *
 * @method FactVariableValuesQuery leftJoinFactVariable($relationAlias = null) Adds a LEFT JOIN clause to the query using the FactVariable relation
 * @method FactVariableValuesQuery rightJoinFactVariable($relationAlias = null) Adds a RIGHT JOIN clause to the query using the FactVariable relation
 * @method FactVariableValuesQuery innerJoinFactVariable($relationAlias = null) Adds a INNER JOIN clause to the query using the FactVariable relation
 *
 * @method FactVariableValues findOne(PropelPDO $con = null) Return the first FactVariableValues matching the query
 * @method FactVariableValues findOneOrCreate(PropelPDO $con = null) Return the first FactVariableValues matching the query, or a new FactVariableValues object populated from the query conditions when no match is found
 *
 * @method FactVariableValues findOneByFacturaId(int $FACTURA_ID) Return the first FactVariableValues filtered by the FACTURA_ID column
 * @method FactVariableValues findOneByFactvariableId(int $FACTVARIABLE_ID) Return the first FactVariableValues filtered by the FACTVARIABLE_ID column
 * @method FactVariableValues findOneByVariableValue(string $VARIABLE_VALUE) Return the first FactVariableValues filtered by the VARIABLE_VALUE column
 *
 * @method array findByFactvariablevaluesId(int $FACTVARIABLEVALUES_ID) Return FactVariableValues objects filtered by the FACTVARIABLEVALUES_ID column
 * @method array findByFacturaId(int $FACTURA_ID) Return FactVariableValues objects filtered by the FACTURA_ID column
 * @method array findByFactvariableId(int $FACTVARIABLE_ID) Return FactVariableValues objects filtered by the FACTVARIABLE_ID column
 * @method array findByVariableValue(string $VARIABLE_VALUE) Return FactVariableValues objects filtered by the VARIABLE_VALUE column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseFactVariableValuesQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseFactVariableValuesQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'FactVariableValues';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new FactVariableValuesQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   FactVariableValuesQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return FactVariableValuesQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof FactVariableValuesQuery) {
            return $criteria;
        }
        $query = new FactVariableValuesQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   FactVariableValues|FactVariableValues[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = FactVariableValuesPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(FactVariableValuesPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 FactVariableValues A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByFactvariablevaluesId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 FactVariableValues A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [FACTVARIABLEVALUES_ID], [FACTURA_ID], [FACTVARIABLE_ID], [VARIABLE_VALUE] FROM [FACT_VARIABLE_VALUES] WHERE [FACTVARIABLEVALUES_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new FactVariableValues();
            $obj->hydrate($row);
            FactVariableValuesPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return FactVariableValues|FactVariableValues[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|FactVariableValues[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLEVALUES_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLEVALUES_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the FACTVARIABLEVALUES_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByFactvariablevaluesId(1234); // WHERE FACTVARIABLEVALUES_ID = 1234
     * $query->filterByFactvariablevaluesId(array(12, 34)); // WHERE FACTVARIABLEVALUES_ID IN (12, 34)
     * $query->filterByFactvariablevaluesId(array('min' => 12)); // WHERE FACTVARIABLEVALUES_ID >= 12
     * $query->filterByFactvariablevaluesId(array('max' => 12)); // WHERE FACTVARIABLEVALUES_ID <= 12
     * </code>
     *
     * @param     mixed $factvariablevaluesId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function filterByFactvariablevaluesId($factvariablevaluesId = null, $comparison = null)
    {
        if (is_array($factvariablevaluesId)) {
            $useMinMax = false;
            if (isset($factvariablevaluesId['min'])) {
                $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLEVALUES_ID, $factvariablevaluesId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($factvariablevaluesId['max'])) {
                $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLEVALUES_ID, $factvariablevaluesId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLEVALUES_ID, $factvariablevaluesId, $comparison);
    }

    /**
     * Filter the query on the FACTURA_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByFacturaId(1234); // WHERE FACTURA_ID = 1234
     * $query->filterByFacturaId(array(12, 34)); // WHERE FACTURA_ID IN (12, 34)
     * $query->filterByFacturaId(array('min' => 12)); // WHERE FACTURA_ID >= 12
     * $query->filterByFacturaId(array('max' => 12)); // WHERE FACTURA_ID <= 12
     * </code>
     *
     * @see       filterByFactura()
     *
     * @param     mixed $facturaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function filterByFacturaId($facturaId = null, $comparison = null)
    {
        if (is_array($facturaId)) {
            $useMinMax = false;
            if (isset($facturaId['min'])) {
                $this->addUsingAlias(FactVariableValuesPeer::FACTURA_ID, $facturaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($facturaId['max'])) {
                $this->addUsingAlias(FactVariableValuesPeer::FACTURA_ID, $facturaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactVariableValuesPeer::FACTURA_ID, $facturaId, $comparison);
    }

    /**
     * Filter the query on the FACTVARIABLE_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByFactvariableId(1234); // WHERE FACTVARIABLE_ID = 1234
     * $query->filterByFactvariableId(array(12, 34)); // WHERE FACTVARIABLE_ID IN (12, 34)
     * $query->filterByFactvariableId(array('min' => 12)); // WHERE FACTVARIABLE_ID >= 12
     * $query->filterByFactvariableId(array('max' => 12)); // WHERE FACTVARIABLE_ID <= 12
     * </code>
     *
     * @see       filterByFactVariable()
     *
     * @param     mixed $factvariableId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function filterByFactvariableId($factvariableId = null, $comparison = null)
    {
        if (is_array($factvariableId)) {
            $useMinMax = false;
            if (isset($factvariableId['min'])) {
                $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLE_ID, $factvariableId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($factvariableId['max'])) {
                $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLE_ID, $factvariableId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLE_ID, $factvariableId, $comparison);
    }

    /**
     * Filter the query on the VARIABLE_VALUE column
     *
     * Example usage:
     * <code>
     * $query->filterByVariableValue('fooValue');   // WHERE VARIABLE_VALUE = 'fooValue'
     * $query->filterByVariableValue('%fooValue%'); // WHERE VARIABLE_VALUE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $variableValue The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function filterByVariableValue($variableValue = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($variableValue)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $variableValue)) {
                $variableValue = str_replace('*', '%', $variableValue);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FactVariableValuesPeer::VARIABLE_VALUE, $variableValue, $comparison);
    }

    /**
     * Filter the query by a related Factura object
     *
     * @param   Factura|PropelObjectCollection $factura The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 FactVariableValuesQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByFactura($factura, $comparison = null)
    {
        if ($factura instanceof Factura) {
            return $this
                ->addUsingAlias(FactVariableValuesPeer::FACTURA_ID, $factura->getFacturaId(), $comparison);
        } elseif ($factura instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(FactVariableValuesPeer::FACTURA_ID, $factura->toKeyValue('PrimaryKey', 'FacturaId'), $comparison);
        } else {
            throw new PropelException('filterByFactura() only accepts arguments of type Factura or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Factura relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function joinFactura($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Factura');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Factura');
        }

        return $this;
    }

    /**
     * Use the Factura relation Factura object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   FacturaQuery A secondary query class using the current class as primary query
     */
    public function useFacturaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinFactura($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Factura', 'FacturaQuery');
    }

    /**
     * Filter the query by a related FactVariable object
     *
     * @param   FactVariable|PropelObjectCollection $factVariable The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 FactVariableValuesQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByFactVariable($factVariable, $comparison = null)
    {
        if ($factVariable instanceof FactVariable) {
            return $this
                ->addUsingAlias(FactVariableValuesPeer::FACTVARIABLE_ID, $factVariable->getFactvariableId(), $comparison);
        } elseif ($factVariable instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(FactVariableValuesPeer::FACTVARIABLE_ID, $factVariable->toKeyValue('PrimaryKey', 'FactvariableId'), $comparison);
        } else {
            throw new PropelException('filterByFactVariable() only accepts arguments of type FactVariable or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the FactVariable relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function joinFactVariable($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('FactVariable');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'FactVariable');
        }

        return $this;
    }

    /**
     * Use the FactVariable relation FactVariable object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   FactVariableQuery A secondary query class using the current class as primary query
     */
    public function useFactVariableQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinFactVariable($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'FactVariable', 'FactVariableQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   FactVariableValues $factVariableValues Object to remove from the list of results
     *
     * @return FactVariableValuesQuery The current query, for fluid interface
     */
    public function prune($factVariableValues = null)
    {
        if ($factVariableValues) {
            $this->addUsingAlias(FactVariableValuesPeer::FACTVARIABLEVALUES_ID, $factVariableValues->getFactvariablevaluesId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

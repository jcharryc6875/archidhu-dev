<?php


/**
 * Base class that represents a query for the 'PROCESOS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:54
 *
 * @method ProcesosQuery orderByProcesosId($order = Criteria::ASC) Order by the PROCESOS_ID column
 * @method ProcesosQuery orderByMacroprocesoId($order = Criteria::ASC) Order by the MACROPROCESO_ID column
 * @method ProcesosQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method ProcesosQuery orderByCodigo($order = Criteria::ASC) Order by the CODIGO column
 *
 * @method ProcesosQuery groupByProcesosId() Group by the PROCESOS_ID column
 * @method ProcesosQuery groupByMacroprocesoId() Group by the MACROPROCESO_ID column
 * @method ProcesosQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method ProcesosQuery groupByCodigo() Group by the CODIGO column
 *
 * @method ProcesosQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ProcesosQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ProcesosQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ProcesosQuery leftJoinMacroProceso($relationAlias = null) Adds a LEFT JOIN clause to the query using the MacroProceso relation
 * @method ProcesosQuery rightJoinMacroProceso($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MacroProceso relation
 * @method ProcesosQuery innerJoinMacroProceso($relationAlias = null) Adds a INNER JOIN clause to the query using the MacroProceso relation
 *
 * @method ProcesosQuery leftJoinOficinaProductora($relationAlias = null) Adds a LEFT JOIN clause to the query using the OficinaProductora relation
 * @method ProcesosQuery rightJoinOficinaProductora($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OficinaProductora relation
 * @method ProcesosQuery innerJoinOficinaProductora($relationAlias = null) Adds a INNER JOIN clause to the query using the OficinaProductora relation
 *
 * @method Procesos findOne(PropelPDO $con = null) Return the first Procesos matching the query
 * @method Procesos findOneOrCreate(PropelPDO $con = null) Return the first Procesos matching the query, or a new Procesos object populated from the query conditions when no match is found
 *
 * @method Procesos findOneByMacroprocesoId(int $MACROPROCESO_ID) Return the first Procesos filtered by the MACROPROCESO_ID column
 * @method Procesos findOneByDescripcion(string $DESCRIPCION) Return the first Procesos filtered by the DESCRIPCION column
 * @method Procesos findOneByCodigo(string $CODIGO) Return the first Procesos filtered by the CODIGO column
 *
 * @method array findByProcesosId(int $PROCESOS_ID) Return Procesos objects filtered by the PROCESOS_ID column
 * @method array findByMacroprocesoId(int $MACROPROCESO_ID) Return Procesos objects filtered by the MACROPROCESO_ID column
 * @method array findByDescripcion(string $DESCRIPCION) Return Procesos objects filtered by the DESCRIPCION column
 * @method array findByCodigo(string $CODIGO) Return Procesos objects filtered by the CODIGO column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseProcesosQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseProcesosQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'Procesos';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ProcesosQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   ProcesosQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ProcesosQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ProcesosQuery) {
            return $criteria;
        }
        $query = new ProcesosQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Procesos|Procesos[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ProcesosPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ProcesosPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Procesos A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByProcesosId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 Procesos A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [PROCESOS_ID], [MACROPROCESO_ID], [DESCRIPCION], [CODIGO] FROM [PROCESOS] WHERE [PROCESOS_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Procesos();
            $obj->hydrate($row);
            ProcesosPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Procesos|Procesos[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Procesos[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ProcesosPeer::PROCESOS_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ProcesosPeer::PROCESOS_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the PROCESOS_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByProcesosId(1234); // WHERE PROCESOS_ID = 1234
     * $query->filterByProcesosId(array(12, 34)); // WHERE PROCESOS_ID IN (12, 34)
     * $query->filterByProcesosId(array('min' => 12)); // WHERE PROCESOS_ID >= 12
     * $query->filterByProcesosId(array('max' => 12)); // WHERE PROCESOS_ID <= 12
     * </code>
     *
     * @param     mixed $procesosId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function filterByProcesosId($procesosId = null, $comparison = null)
    {
        if (is_array($procesosId)) {
            $useMinMax = false;
            if (isset($procesosId['min'])) {
                $this->addUsingAlias(ProcesosPeer::PROCESOS_ID, $procesosId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($procesosId['max'])) {
                $this->addUsingAlias(ProcesosPeer::PROCESOS_ID, $procesosId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcesosPeer::PROCESOS_ID, $procesosId, $comparison);
    }

    /**
     * Filter the query on the MACROPROCESO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByMacroprocesoId(1234); // WHERE MACROPROCESO_ID = 1234
     * $query->filterByMacroprocesoId(array(12, 34)); // WHERE MACROPROCESO_ID IN (12, 34)
     * $query->filterByMacroprocesoId(array('min' => 12)); // WHERE MACROPROCESO_ID >= 12
     * $query->filterByMacroprocesoId(array('max' => 12)); // WHERE MACROPROCESO_ID <= 12
     * </code>
     *
     * @see       filterByMacroProceso()
     *
     * @param     mixed $macroprocesoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function filterByMacroprocesoId($macroprocesoId = null, $comparison = null)
    {
        if (is_array($macroprocesoId)) {
            $useMinMax = false;
            if (isset($macroprocesoId['min'])) {
                $this->addUsingAlias(ProcesosPeer::MACROPROCESO_ID, $macroprocesoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($macroprocesoId['max'])) {
                $this->addUsingAlias(ProcesosPeer::MACROPROCESO_ID, $macroprocesoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ProcesosPeer::MACROPROCESO_ID, $macroprocesoId, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcesosPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the CODIGO column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE CODIGO = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE CODIGO LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ProcesosPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query by a related MacroProceso object
     *
     * @param   MacroProceso|PropelObjectCollection $macroProceso The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcesosQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByMacroProceso($macroProceso, $comparison = null)
    {
        if ($macroProceso instanceof MacroProceso) {
            return $this
                ->addUsingAlias(ProcesosPeer::MACROPROCESO_ID, $macroProceso->getMacroprocesoId(), $comparison);
        } elseif ($macroProceso instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ProcesosPeer::MACROPROCESO_ID, $macroProceso->toKeyValue('PrimaryKey', 'MacroprocesoId'), $comparison);
        } else {
            throw new PropelException('filterByMacroProceso() only accepts arguments of type MacroProceso or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MacroProceso relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function joinMacroProceso($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MacroProceso');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MacroProceso');
        }

        return $this;
    }

    /**
     * Use the MacroProceso relation MacroProceso object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MacroProcesoQuery A secondary query class using the current class as primary query
     */
    public function useMacroProcesoQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinMacroProceso($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MacroProceso', 'MacroProcesoQuery');
    }

    /**
     * Filter the query by a related OficinaProductora object
     *
     * @param   OficinaProductora|PropelObjectCollection $oficinaProductora  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 ProcesosQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByOficinaProductora($oficinaProductora, $comparison = null)
    {
        if ($oficinaProductora instanceof OficinaProductora) {
            return $this
                ->addUsingAlias(ProcesosPeer::PROCESOS_ID, $oficinaProductora->getProcesosId(), $comparison);
        } elseif ($oficinaProductora instanceof PropelObjectCollection) {
            return $this
                ->useOficinaProductoraQuery()
                ->filterByPrimaryKeys($oficinaProductora->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOficinaProductora() only accepts arguments of type OficinaProductora or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OficinaProductora relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function joinOficinaProductora($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OficinaProductora');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OficinaProductora');
        }

        return $this;
    }

    /**
     * Use the OficinaProductora relation OficinaProductora object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OficinaProductoraQuery A secondary query class using the current class as primary query
     */
    public function useOficinaProductoraQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinOficinaProductora($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OficinaProductora', 'OficinaProductoraQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Procesos $procesos Object to remove from the list of results
     *
     * @return ProcesosQuery The current query, for fluid interface
     */
    public function prune($procesos = null)
    {
        if ($procesos) {
            $this->addUsingAlias(ProcesosPeer::PROCESOS_ID, $procesos->getProcesosId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

<?php


/**
 * Base class that represents a row from the 'CARGO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:50
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseCargo extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'CargoPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        CargoPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinite loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the cargo_id field.
     * @var        int
     */
    protected $cargo_id;

    /**
     * The value for the descripcion field.
     * @var        string
     */
    protected $descripcion;

    /**
     * @var        PropelObjectCollection|CargoUsuario[] Collection to store aggregation of CargoUsuario objects.
     */
    protected $collCargoUsuarios;
    protected $collCargoUsuariosPartial;

    /**
     * @var        PropelObjectCollection|HistUsuario[] Collection to store aggregation of HistUsuario objects.
     */
    protected $collHistUsuarios;
    protected $collHistUsuariosPartial;

    /**
     * @var        PropelObjectCollection|Usuario[] Collection to store aggregation of Usuario objects.
     */
    protected $collUsuarios;
    protected $collUsuariosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * Flag to prevent endless clearAllReferences($deep=true) loop, if this object is referenced
     * @var        boolean
     */
    protected $alreadyInClearAllReferencesDeep = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $cargoUsuariosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $histUsuariosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuariosScheduledForDeletion = null;

    /**
     * Get the [cargo_id] column value.
     * 
     * @return int
     */
    public function getCargoId()
    {

        return $this->cargo_id;
    }

    /**
     * Get the [descripcion] column value.
     * 
     * @return string
     */
    public function getDescripcion()
    {

        return $this->descripcion;
    }

    /**
     * Set the value of [cargo_id] column.
     * 
     * @param  int $v new value
     * @return Cargo The current object (for fluent API support)
     */
    public function setCargoId($v)
    {
        if ($v !== null && is_numeric($v)) {
            $v = (int) $v;
        }

        if ($this->cargo_id !== $v) {
            $this->cargo_id = $v;
            $this->modifiedColumns[] = CargoPeer::CARGO_ID;
        }


        return $this;
    } // setCargoId()

    /**
     * Set the value of [descripcion] column.
     * 
     * @param  string $v new value
     * @return Cargo The current object (for fluent API support)
     */
    public function setDescripcion($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->descripcion !== $v) {
            $this->descripcion = $v;
            $this->modifiedColumns[] = CargoPeer::DESCRIPCION;
        }


        return $this;
    } // setDescripcion()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which resultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->cargo_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->descripcion = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }
            $this->postHydrate($row, $startcol, $rehydrate);

            return $startcol + 2; // 2 = CargoPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Cargo object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(CargoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = CargoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->collCargoUsuarios = null;

            $this->collHistUsuarios = null;

            $this->collUsuarios = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(CargoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = CargoQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseCargo:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseCargo:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(CargoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseCargo:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseCargo:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                CargoPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->cargoUsuariosScheduledForDeletion !== null) {
                if (!$this->cargoUsuariosScheduledForDeletion->isEmpty()) {
                    foreach ($this->cargoUsuariosScheduledForDeletion as $cargoUsuario) {
                        // need to save related object because we set the relation to null
                        $cargoUsuario->save($con);
                    }
                    $this->cargoUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collCargoUsuarios !== null) {
                foreach ($this->collCargoUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->histUsuariosScheduledForDeletion !== null) {
                if (!$this->histUsuariosScheduledForDeletion->isEmpty()) {
                    HistUsuarioQuery::create()
                        ->filterByPrimaryKeys($this->histUsuariosScheduledForDeletion->getPrimaryKeys(false))
                        ->delete($con);
                    $this->histUsuariosScheduledForDeletion = null;
                }
            }

            if ($this->collHistUsuarios !== null) {
                foreach ($this->collHistUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->usuariosScheduledForDeletion !== null) {
                if (!$this->usuariosScheduledForDeletion->isEmpty()) {
                    foreach ($this->usuariosScheduledForDeletion as $usuario) {
                        // need to save related object because we set the relation to null
                        $usuario->save($con);
                    }
                    $this->usuariosScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarios !== null) {
                foreach ($this->collUsuarios as $referrerFK) {
                    if (!$referrerFK->isDeleted() && ($referrerFK->isNew() || $referrerFK->isModified())) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $this->modifiedColumns[] = CargoPeer::CARGO_ID;
        $criteria = $this->buildCriteria();
        if ($criteria->keyContainsValue(CargoPeer::CARGO_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . CargoPeer::CARGO_ID . ')');
        }
        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(CargoPeer::CARGO_ID);
        $pk = BasePeer::doInsert($criteria, $con);
        $this->setCargoId($pk);  //[IMV] update autoincrement primary key
        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        }

        $this->validationFailures = $res;

        return false;
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggregated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objects otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            if (($retval = CargoPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collCargoUsuarios !== null) {
                    foreach ($this->collCargoUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collHistUsuarios !== null) {
                    foreach ($this->collHistUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collUsuarios !== null) {
                    foreach ($this->collUsuarios as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = CargoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getCargoId();
                break;
            case 1:
                return $this->getDescripcion();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Cargo'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Cargo'][$this->getPrimaryKey()] = true;
        $keys = CargoPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getCargoId(),
            $keys[1] => $this->getDescripcion(),
        );
        $virtualColumns = $this->virtualColumns;
        foreach ($virtualColumns as $key => $virtualColumn) {
            $result[$key] = $virtualColumn;
        }
        
        if ($includeForeignObjects) {
            if (null !== $this->collCargoUsuarios) {
                $result['CargoUsuarios'] = $this->collCargoUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collHistUsuarios) {
                $result['HistUsuarios'] = $this->collHistUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collUsuarios) {
                $result['Usuarios'] = $this->collUsuarios->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = CargoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setCargoId($value);
                break;
            case 1:
                $this->setDescripcion($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = CargoPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setCargoId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setDescripcion($arr[$keys[1]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(CargoPeer::DATABASE_NAME);

        if ($this->isColumnModified(CargoPeer::CARGO_ID)) $criteria->add(CargoPeer::CARGO_ID, $this->cargo_id);
        if ($this->isColumnModified(CargoPeer::DESCRIPCION)) $criteria->add(CargoPeer::DESCRIPCION, $this->descripcion);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(CargoPeer::DATABASE_NAME);
        $criteria->add(CargoPeer::CARGO_ID, $this->cargo_id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getCargoId();
    }

    /**
     * Generic method to set the primary key (cargo_id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setCargoId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getCargoId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Cargo (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setDescripcion($this->getDescripcion());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getCargoUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addCargoUsuario($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getHistUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addHistUsuario($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getUsuarios() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuario($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setCargoId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Cargo Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return CargoPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new CargoPeer();
        }

        return self::$peer;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('CargoUsuario' == $relationName) {
            $this->initCargoUsuarios();
        }
        if ('HistUsuario' == $relationName) {
            $this->initHistUsuarios();
        }
        if ('Usuario' == $relationName) {
            $this->initUsuarios();
        }
    }

    /**
     * Clears out the collCargoUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Cargo The current object (for fluent API support)
     * @see        addCargoUsuarios()
     */
    public function clearCargoUsuarios()
    {
        $this->collCargoUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collCargoUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collCargoUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialCargoUsuarios($v = true)
    {
        $this->collCargoUsuariosPartial = $v;
    }

    /**
     * Initializes the collCargoUsuarios collection.
     *
     * By default this just sets the collCargoUsuarios collection to an empty array (like clearcollCargoUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initCargoUsuarios($overrideExisting = true)
    {
        if (null !== $this->collCargoUsuarios && !$overrideExisting) {
            return;
        }
        $this->collCargoUsuarios = new PropelObjectCollection();
        $this->collCargoUsuarios->setModel('CargoUsuario');
    }

    /**
     * Gets an array of CargoUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Cargo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|CargoUsuario[] List of CargoUsuario objects
     * @throws PropelException
     */
    public function getCargoUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collCargoUsuariosPartial && !$this->isNew();
        if (null === $this->collCargoUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collCargoUsuarios) {
                // return empty collection
                $this->initCargoUsuarios();
            } else {
                $collCargoUsuarios = CargoUsuarioQuery::create(null, $criteria)
                    ->filterByCargo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collCargoUsuariosPartial && count($collCargoUsuarios)) {
                      $this->initCargoUsuarios(false);

                      foreach ($collCargoUsuarios as $obj) {
                        if (false == $this->collCargoUsuarios->contains($obj)) {
                          $this->collCargoUsuarios->append($obj);
                        }
                      }

                      $this->collCargoUsuariosPartial = true;
                    }

                    $collCargoUsuarios->getInternalIterator()->rewind();

                    return $collCargoUsuarios;
                }

                if ($partial && $this->collCargoUsuarios) {
                    foreach ($this->collCargoUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collCargoUsuarios[] = $obj;
                        }
                    }
                }

                $this->collCargoUsuarios = $collCargoUsuarios;
                $this->collCargoUsuariosPartial = false;
            }
        }

        return $this->collCargoUsuarios;
    }

    /**
     * Sets a collection of CargoUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $cargoUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Cargo The current object (for fluent API support)
     */
    public function setCargoUsuarios(PropelCollection $cargoUsuarios, PropelPDO $con = null)
    {
        $cargoUsuariosToDelete = $this->getCargoUsuarios(new Criteria(), $con)->diff($cargoUsuarios);


        $this->cargoUsuariosScheduledForDeletion = $cargoUsuariosToDelete;

        foreach ($cargoUsuariosToDelete as $cargoUsuarioRemoved) {
            $cargoUsuarioRemoved->setCargo(null);
        }

        $this->collCargoUsuarios = null;
        foreach ($cargoUsuarios as $cargoUsuario) {
            $this->addCargoUsuario($cargoUsuario);
        }

        $this->collCargoUsuarios = $cargoUsuarios;
        $this->collCargoUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related CargoUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related CargoUsuario objects.
     * @throws PropelException
     */
    public function countCargoUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collCargoUsuariosPartial && !$this->isNew();
        if (null === $this->collCargoUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collCargoUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getCargoUsuarios());
            }
            $query = CargoUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByCargo($this)
                ->count($con);
        }

        return count($this->collCargoUsuarios);
    }

    /**
     * Method called to associate a CargoUsuario object to this object
     * through the CargoUsuario foreign key attribute.
     *
     * @param    CargoUsuario $l CargoUsuario
     * @return Cargo The current object (for fluent API support)
     */
    public function addCargoUsuario(CargoUsuario $l)
    {
        if ($this->collCargoUsuarios === null) {
            $this->initCargoUsuarios();
            $this->collCargoUsuariosPartial = true;
        }

        if (!in_array($l, $this->collCargoUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddCargoUsuario($l);

            if ($this->cargoUsuariosScheduledForDeletion and $this->cargoUsuariosScheduledForDeletion->contains($l)) {
                $this->cargoUsuariosScheduledForDeletion->remove($this->cargoUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	CargoUsuario $cargoUsuario The cargoUsuario object to add.
     */
    protected function doAddCargoUsuario($cargoUsuario)
    {
        $this->collCargoUsuarios[]= $cargoUsuario;
        $cargoUsuario->setCargo($this);
    }

    /**
     * @param	CargoUsuario $cargoUsuario The cargoUsuario object to remove.
     * @return Cargo The current object (for fluent API support)
     */
    public function removeCargoUsuario($cargoUsuario)
    {
        if ($this->getCargoUsuarios()->contains($cargoUsuario)) {
            $this->collCargoUsuarios->remove($this->collCargoUsuarios->search($cargoUsuario));
            if (null === $this->cargoUsuariosScheduledForDeletion) {
                $this->cargoUsuariosScheduledForDeletion = clone $this->collCargoUsuarios;
                $this->cargoUsuariosScheduledForDeletion->clear();
            }
            $this->cargoUsuariosScheduledForDeletion[]= $cargoUsuario;
            $cargoUsuario->setCargo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related CargoUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|CargoUsuario[] List of CargoUsuario objects
     */
    public function getCargoUsuariosJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = CargoUsuarioQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getCargoUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related CargoUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|CargoUsuario[] List of CargoUsuario objects
     */
    public function getCargoUsuariosJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = CargoUsuarioQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getCargoUsuarios($query, $con);
    }

    /**
     * Clears out the collHistUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Cargo The current object (for fluent API support)
     * @see        addHistUsuarios()
     */
    public function clearHistUsuarios()
    {
        $this->collHistUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collHistUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collHistUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialHistUsuarios($v = true)
    {
        $this->collHistUsuariosPartial = $v;
    }

    /**
     * Initializes the collHistUsuarios collection.
     *
     * By default this just sets the collHistUsuarios collection to an empty array (like clearcollHistUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initHistUsuarios($overrideExisting = true)
    {
        if (null !== $this->collHistUsuarios && !$overrideExisting) {
            return;
        }
        $this->collHistUsuarios = new PropelObjectCollection();
        $this->collHistUsuarios->setModel('HistUsuario');
    }

    /**
     * Gets an array of HistUsuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Cargo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     * @throws PropelException
     */
    public function getHistUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collHistUsuariosPartial && !$this->isNew();
        if (null === $this->collHistUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collHistUsuarios) {
                // return empty collection
                $this->initHistUsuarios();
            } else {
                $collHistUsuarios = HistUsuarioQuery::create(null, $criteria)
                    ->filterByCargo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collHistUsuariosPartial && count($collHistUsuarios)) {
                      $this->initHistUsuarios(false);

                      foreach ($collHistUsuarios as $obj) {
                        if (false == $this->collHistUsuarios->contains($obj)) {
                          $this->collHistUsuarios->append($obj);
                        }
                      }

                      $this->collHistUsuariosPartial = true;
                    }

                    $collHistUsuarios->getInternalIterator()->rewind();

                    return $collHistUsuarios;
                }

                if ($partial && $this->collHistUsuarios) {
                    foreach ($this->collHistUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collHistUsuarios[] = $obj;
                        }
                    }
                }

                $this->collHistUsuarios = $collHistUsuarios;
                $this->collHistUsuariosPartial = false;
            }
        }

        return $this->collHistUsuarios;
    }

    /**
     * Sets a collection of HistUsuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $histUsuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Cargo The current object (for fluent API support)
     */
    public function setHistUsuarios(PropelCollection $histUsuarios, PropelPDO $con = null)
    {
        $histUsuariosToDelete = $this->getHistUsuarios(new Criteria(), $con)->diff($histUsuarios);


        $this->histUsuariosScheduledForDeletion = $histUsuariosToDelete;

        foreach ($histUsuariosToDelete as $histUsuarioRemoved) {
            $histUsuarioRemoved->setCargo(null);
        }

        $this->collHistUsuarios = null;
        foreach ($histUsuarios as $histUsuario) {
            $this->addHistUsuario($histUsuario);
        }

        $this->collHistUsuarios = $histUsuarios;
        $this->collHistUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related HistUsuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related HistUsuario objects.
     * @throws PropelException
     */
    public function countHistUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collHistUsuariosPartial && !$this->isNew();
        if (null === $this->collHistUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collHistUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getHistUsuarios());
            }
            $query = HistUsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByCargo($this)
                ->count($con);
        }

        return count($this->collHistUsuarios);
    }

    /**
     * Method called to associate a HistUsuario object to this object
     * through the HistUsuario foreign key attribute.
     *
     * @param    HistUsuario $l HistUsuario
     * @return Cargo The current object (for fluent API support)
     */
    public function addHistUsuario(HistUsuario $l)
    {
        if ($this->collHistUsuarios === null) {
            $this->initHistUsuarios();
            $this->collHistUsuariosPartial = true;
        }

        if (!in_array($l, $this->collHistUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddHistUsuario($l);

            if ($this->histUsuariosScheduledForDeletion and $this->histUsuariosScheduledForDeletion->contains($l)) {
                $this->histUsuariosScheduledForDeletion->remove($this->histUsuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	HistUsuario $histUsuario The histUsuario object to add.
     */
    protected function doAddHistUsuario($histUsuario)
    {
        $this->collHistUsuarios[]= $histUsuario;
        $histUsuario->setCargo($this);
    }

    /**
     * @param	HistUsuario $histUsuario The histUsuario object to remove.
     * @return Cargo The current object (for fluent API support)
     */
    public function removeHistUsuario($histUsuario)
    {
        if ($this->getHistUsuarios()->contains($histUsuario)) {
            $this->collHistUsuarios->remove($this->collHistUsuarios->search($histUsuario));
            if (null === $this->histUsuariosScheduledForDeletion) {
                $this->histUsuariosScheduledForDeletion = clone $this->collHistUsuarios;
                $this->histUsuariosScheduledForDeletion->clear();
            }
            $this->histUsuariosScheduledForDeletion[]= clone $histUsuario;
            $histUsuario->setCargo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinRegional($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('Regional', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinEstadoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('EstadoUsuario', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinTipoProcesoCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoProcesoCom', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related HistUsuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|HistUsuario[] List of HistUsuario objects
     */
    public function getHistUsuariosJoinTipoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = HistUsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoUsuario', $join_behavior);

        return $this->getHistUsuarios($query, $con);
    }

    /**
     * Clears out the collUsuarios collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return Cargo The current object (for fluent API support)
     * @see        addUsuarios()
     */
    public function clearUsuarios()
    {
        $this->collUsuarios = null; // important to set this to null since that means it is uninitialized
        $this->collUsuariosPartial = null;

        return $this;
    }

    /**
     * reset is the collUsuarios collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarios($v = true)
    {
        $this->collUsuariosPartial = $v;
    }

    /**
     * Initializes the collUsuarios collection.
     *
     * By default this just sets the collUsuarios collection to an empty array (like clearcollUsuarios());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarios($overrideExisting = true)
    {
        if (null !== $this->collUsuarios && !$overrideExisting) {
            return;
        }
        $this->collUsuarios = new PropelObjectCollection();
        $this->collUsuarios->setModel('Usuario');
    }

    /**
     * Gets an array of Usuario objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Cargo is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     * @throws PropelException
     */
    public function getUsuarios($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuariosPartial && !$this->isNew();
        if (null === $this->collUsuarios || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarios) {
                // return empty collection
                $this->initUsuarios();
            } else {
                $collUsuarios = UsuarioQuery::create(null, $criteria)
                    ->filterByCargo($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuariosPartial && count($collUsuarios)) {
                      $this->initUsuarios(false);

                      foreach ($collUsuarios as $obj) {
                        if (false == $this->collUsuarios->contains($obj)) {
                          $this->collUsuarios->append($obj);
                        }
                      }

                      $this->collUsuariosPartial = true;
                    }

                    $collUsuarios->getInternalIterator()->rewind();

                    return $collUsuarios;
                }

                if ($partial && $this->collUsuarios) {
                    foreach ($this->collUsuarios as $obj) {
                        if ($obj->isNew()) {
                            $collUsuarios[] = $obj;
                        }
                    }
                }

                $this->collUsuarios = $collUsuarios;
                $this->collUsuariosPartial = false;
            }
        }

        return $this->collUsuarios;
    }

    /**
     * Sets a collection of Usuario objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarios A Propel collection.
     * @param PropelPDO $con Optional connection object
     * @return Cargo The current object (for fluent API support)
     */
    public function setUsuarios(PropelCollection $usuarios, PropelPDO $con = null)
    {
        $usuariosToDelete = $this->getUsuarios(new Criteria(), $con)->diff($usuarios);


        $this->usuariosScheduledForDeletion = $usuariosToDelete;

        foreach ($usuariosToDelete as $usuarioRemoved) {
            $usuarioRemoved->setCargo(null);
        }

        $this->collUsuarios = null;
        foreach ($usuarios as $usuario) {
            $this->addUsuario($usuario);
        }

        $this->collUsuarios = $usuarios;
        $this->collUsuariosPartial = false;

        return $this;
    }

    /**
     * Returns the number of related Usuario objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Usuario objects.
     * @throws PropelException
     */
    public function countUsuarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuariosPartial && !$this->isNew();
        if (null === $this->collUsuarios || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarios) {
                return 0;
            }

            if ($partial && !$criteria) {
                return count($this->getUsuarios());
            }
            $query = UsuarioQuery::create(null, $criteria);
            if ($distinct) {
                $query->distinct();
            }

            return $query
                ->filterByCargo($this)
                ->count($con);
        }

        return count($this->collUsuarios);
    }

    /**
     * Method called to associate a Usuario object to this object
     * through the Usuario foreign key attribute.
     *
     * @param    Usuario $l Usuario
     * @return Cargo The current object (for fluent API support)
     */
    public function addUsuario(Usuario $l)
    {
        if ($this->collUsuarios === null) {
            $this->initUsuarios();
            $this->collUsuariosPartial = true;
        }

        if (!in_array($l, $this->collUsuarios->getArrayCopy(), true)) { // only add it if the **same** object is not already associated
            $this->doAddUsuario($l);

            if ($this->usuariosScheduledForDeletion and $this->usuariosScheduledForDeletion->contains($l)) {
                $this->usuariosScheduledForDeletion->remove($this->usuariosScheduledForDeletion->search($l));
            }
        }

        return $this;
    }

    /**
     * @param	Usuario $usuario The usuario object to add.
     */
    protected function doAddUsuario($usuario)
    {
        $this->collUsuarios[]= $usuario;
        $usuario->setCargo($this);
    }

    /**
     * @param	Usuario $usuario The usuario object to remove.
     * @return Cargo The current object (for fluent API support)
     */
    public function removeUsuario($usuario)
    {
        if ($this->getUsuarios()->contains($usuario)) {
            $this->collUsuarios->remove($this->collUsuarios->search($usuario));
            if (null === $this->usuariosScheduledForDeletion) {
                $this->usuariosScheduledForDeletion = clone $this->collUsuarios;
                $this->usuariosScheduledForDeletion->clear();
            }
            $this->usuariosScheduledForDeletion[]= $usuario;
            $usuario->setCargo(null);
        }

        return $this;
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinEstadoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('EstadoUsuario', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinDependencia($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('Dependencia', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinRegional($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('Regional', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinTipoUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoUsuario', $join_behavior);

        return $this->getUsuarios($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Cargo is new, it will return
     * an empty collection; or if this Cargo has previously
     * been saved, it will retrieve related Usuarios from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Cargo.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Usuario[] List of Usuario objects
     */
    public function getUsuariosJoinTipoProcesoCom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioQuery::create(null, $criteria);
        $query->joinWith('TipoProcesoCom', $join_behavior);

        return $this->getUsuarios($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->cargo_id = null;
        $this->descripcion = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->alreadyInClearAllReferencesDeep = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volume/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep && !$this->alreadyInClearAllReferencesDeep) {
            $this->alreadyInClearAllReferencesDeep = true;
            if ($this->collCargoUsuarios) {
                foreach ($this->collCargoUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collHistUsuarios) {
                foreach ($this->collHistUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collUsuarios) {
                foreach ($this->collUsuarios as $o) {
                    $o->clearAllReferences($deep);
                }
            }

            $this->alreadyInClearAllReferencesDeep = false;
        } // if ($deep)

        if ($this->collCargoUsuarios instanceof PropelCollection) {
            $this->collCargoUsuarios->clearIterator();
        }
        $this->collCargoUsuarios = null;
        if ($this->collHistUsuarios instanceof PropelCollection) {
            $this->collHistUsuarios->clearIterator();
        }
        $this->collHistUsuarios = null;
        if ($this->collUsuarios instanceof PropelCollection) {
            $this->collUsuarios->clearIterator();
        }
        $this->collUsuarios = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(CargoPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {
        
        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseCargo:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}

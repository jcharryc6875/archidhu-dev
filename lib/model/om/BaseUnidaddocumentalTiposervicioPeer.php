<?php


/**
 * Base static class for performing query and update operations on the 'UNIDADDOCUMENTAL_TIPOSERVICIO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * vie 12 sep 2025 17:31:36
 *
 * @package propel.generator.lib.model.om
 */
abstract class BaseUnidaddocumentalTiposervicioPeer
{

    /** the default database name for this class */
    const DATABASE_NAME = 'propel';

    /** the table name for this class */
    const TABLE_NAME = 'UNIDADDOCUMENTAL_TIPOSERVICIO';

    /** the related Propel class for this table */
    const OM_CLASS = 'UnidaddocumentalTiposervicio';

    /** the related TableMap class for this table */
    const TM_CLASS = 'UnidaddocumentalTiposervicioTableMap';

    /** The total number of columns. */
    const NUM_COLUMNS = 7;

    /** The number of lazy-loaded columns. */
    const NUM_LAZY_LOAD_COLUMNS = 0;

    /** The number of columns to hydrate (NUM_COLUMNS - NUM_LAZY_LOAD_COLUMNS) */
    const NUM_HYDRATE_COLUMNS = 7;

    /** the column name for the UNIDADDOCUMENTALTIPOSERVICIO_ID field */
    const UNIDADDOCUMENTALTIPOSERVICIO_ID = 'UNIDADDOCUMENTAL_TIPOSERVICIO.UNIDADDOCUMENTALTIPOSERVICIO_ID';

    /** the column name for the USUARIO_ID field */
    const USUARIO_ID = 'UNIDADDOCUMENTAL_TIPOSERVICIO.USUARIO_ID';

    /** the column name for the UNIDADDOCUMENTAL_ID field */
    const UNIDADDOCUMENTAL_ID = 'UNIDADDOCUMENTAL_TIPOSERVICIO.UNIDADDOCUMENTAL_ID';

    /** the column name for the TIPOSERVICIO_ID field */
    const TIPOSERVICIO_ID = 'UNIDADDOCUMENTAL_TIPOSERVICIO.TIPOSERVICIO_ID';

    /** the column name for the TIPODOCUMENTAL_ID field */
    const TIPODOCUMENTAL_ID = 'UNIDADDOCUMENTAL_TIPOSERVICIO.TIPODOCUMENTAL_ID';

    /** the column name for the FECHA_CREACION field */
    const FECHA_CREACION = 'UNIDADDOCUMENTAL_TIPOSERVICIO.FECHA_CREACION';

    /** the column name for the ESTA_ACTIVO field */
    const ESTA_ACTIVO = 'UNIDADDOCUMENTAL_TIPOSERVICIO.ESTA_ACTIVO';

    /** The default string format for model objects of the related table **/
    const DEFAULT_STRING_FORMAT = 'YAML';

    /**
     * An identity map to hold any loaded instances of UnidaddocumentalTiposervicio objects.
     * This must be public so that other peer classes can access this when hydrating from JOIN
     * queries.
     * @var        array UnidaddocumentalTiposervicio[]
     */
    public static $instances = array();


    /**
     * holds an array of fieldnames
     *
     * first dimension keys are the type constants
     * e.g. UnidaddocumentalTiposervicioPeer::$fieldNames[UnidaddocumentalTiposervicioPeer::TYPE_PHPNAME][0] = 'Id'
     */
    protected static $fieldNames = array (
        BasePeer::TYPE_PHPNAME => array ('UnidaddocumentaltiposervicioId', 'UsuarioId', 'UnidaddocumentalId', 'TiposervicioId', 'TipodocumentalId', 'FechaCreacion', 'EstaActivo', ),
        BasePeer::TYPE_STUDLYPHPNAME => array ('unidaddocumentaltiposervicioId', 'usuarioId', 'unidaddocumentalId', 'tiposervicioId', 'tipodocumentalId', 'fechaCreacion', 'estaActivo', ),
        BasePeer::TYPE_COLNAME => array (UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID, UnidaddocumentalTiposervicioPeer::USUARIO_ID, UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, UnidaddocumentalTiposervicioPeer::FECHA_CREACION, UnidaddocumentalTiposervicioPeer::ESTA_ACTIVO, ),
        BasePeer::TYPE_RAW_COLNAME => array ('UNIDADDOCUMENTALTIPOSERVICIO_ID', 'USUARIO_ID', 'UNIDADDOCUMENTAL_ID', 'TIPOSERVICIO_ID', 'TIPODOCUMENTAL_ID', 'FECHA_CREACION', 'ESTA_ACTIVO', ),
        BasePeer::TYPE_FIELDNAME => array ('UNIDADDOCUMENTALTIPOSERVICIO_ID', 'USUARIO_ID', 'UNIDADDOCUMENTAL_ID', 'TIPOSERVICIO_ID', 'TIPODOCUMENTAL_ID', 'FECHA_CREACION', 'ESTA_ACTIVO', ),
        BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, )
    );

    /**
     * holds an array of keys for quick access to the fieldnames array
     *
     * first dimension keys are the type constants
     * e.g. UnidaddocumentalTiposervicioPeer::$fieldNames[BasePeer::TYPE_PHPNAME]['Id'] = 0
     */
    protected static $fieldKeys = array (
        BasePeer::TYPE_PHPNAME => array ('UnidaddocumentaltiposervicioId' => 0, 'UsuarioId' => 1, 'UnidaddocumentalId' => 2, 'TiposervicioId' => 3, 'TipodocumentalId' => 4, 'FechaCreacion' => 5, 'EstaActivo' => 6, ),
        BasePeer::TYPE_STUDLYPHPNAME => array ('unidaddocumentaltiposervicioId' => 0, 'usuarioId' => 1, 'unidaddocumentalId' => 2, 'tiposervicioId' => 3, 'tipodocumentalId' => 4, 'fechaCreacion' => 5, 'estaActivo' => 6, ),
        BasePeer::TYPE_COLNAME => array (UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID => 0, UnidaddocumentalTiposervicioPeer::USUARIO_ID => 1, UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID => 2, UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID => 3, UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID => 4, UnidaddocumentalTiposervicioPeer::FECHA_CREACION => 5, UnidaddocumentalTiposervicioPeer::ESTA_ACTIVO => 6, ),
        BasePeer::TYPE_RAW_COLNAME => array ('UNIDADDOCUMENTALTIPOSERVICIO_ID' => 0, 'USUARIO_ID' => 1, 'UNIDADDOCUMENTAL_ID' => 2, 'TIPOSERVICIO_ID' => 3, 'TIPODOCUMENTAL_ID' => 4, 'FECHA_CREACION' => 5, 'ESTA_ACTIVO' => 6, ),
        BasePeer::TYPE_FIELDNAME => array ('UNIDADDOCUMENTALTIPOSERVICIO_ID' => 0, 'USUARIO_ID' => 1, 'UNIDADDOCUMENTAL_ID' => 2, 'TIPOSERVICIO_ID' => 3, 'TIPODOCUMENTAL_ID' => 4, 'FECHA_CREACION' => 5, 'ESTA_ACTIVO' => 6, ),
        BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, )
    );

    /**
     * Translates a fieldname to another type
     *
     * @param      string $name field name
     * @param      string $fromType One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                         BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
     * @param      string $toType   One of the class type constants
     * @return string          translated name of the field.
     * @throws PropelException - if the specified name could not be found in the fieldname mappings.
     */
    public static function translateFieldName($name, $fromType, $toType)
    {
        $toNames = UnidaddocumentalTiposervicioPeer::getFieldNames($toType);
        $key = isset(UnidaddocumentalTiposervicioPeer::$fieldKeys[$fromType][$name]) ? UnidaddocumentalTiposervicioPeer::$fieldKeys[$fromType][$name] : null;
        if ($key === null) {
            throw new PropelException("'$name' could not be found in the field names of type '$fromType'. These are: " . print_r(UnidaddocumentalTiposervicioPeer::$fieldKeys[$fromType], true));
        }

        return $toNames[$key];
    }

    /**
     * Returns an array of field names.
     *
     * @param      string $type The type of fieldnames to return:
     *                      One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                      BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
     * @return array           A list of field names
     * @throws PropelException - if the type is not valid.
     */
    public static function getFieldNames($type = BasePeer::TYPE_PHPNAME)
    {
        if (!array_key_exists($type, UnidaddocumentalTiposervicioPeer::$fieldNames)) {
            throw new PropelException('Method getFieldNames() expects the parameter $type to be one of the class constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME, BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. ' . $type . ' was given.');
        }

        return UnidaddocumentalTiposervicioPeer::$fieldNames[$type];
    }

    /**
     * Convenience method which changes table.column to alias.column.
     *
     * Using this method you can maintain SQL abstraction while using column aliases.
     * <code>
     *		$c->addAlias("alias1", TablePeer::TABLE_NAME);
     *		$c->addJoin(TablePeer::alias("alias1", TablePeer::PRIMARY_KEY_COLUMN), TablePeer::PRIMARY_KEY_COLUMN);
     * </code>
     * @param      string $alias The alias for the current table.
     * @param      string $column The column name for current table. (i.e. UnidaddocumentalTiposervicioPeer::COLUMN_NAME).
     * @return string
     */
    public static function alias($alias, $column)
    {
        return str_replace(UnidaddocumentalTiposervicioPeer::TABLE_NAME.'.', $alias.'.', $column);
    }

    /**
     * Add all the columns needed to create a new object.
     *
     * Note: any columns that were marked with lazyLoad="true" in the
     * XML schema will not be added to the select list and only loaded
     * on demand.
     *
     * @param      Criteria $criteria object containing the columns to add.
     * @param      string   $alias    optional table alias
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function addSelectColumns(Criteria $criteria, $alias = null)
    {
        if (null === $alias) {
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID);
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::USUARIO_ID);
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID);
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID);
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID);
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::FECHA_CREACION);
            $criteria->addSelectColumn(UnidaddocumentalTiposervicioPeer::ESTA_ACTIVO);
        } else {
            $criteria->addSelectColumn($alias . '.UNIDADDOCUMENTALTIPOSERVICIO_ID');
            $criteria->addSelectColumn($alias . '.USUARIO_ID');
            $criteria->addSelectColumn($alias . '.UNIDADDOCUMENTAL_ID');
            $criteria->addSelectColumn($alias . '.TIPOSERVICIO_ID');
            $criteria->addSelectColumn($alias . '.TIPODOCUMENTAL_ID');
            $criteria->addSelectColumn($alias . '.FECHA_CREACION');
            $criteria->addSelectColumn($alias . '.ESTA_ACTIVO');
        }
    }

    /**
     * Returns the number of rows matching criteria.
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @return int Number of matching rows.
     */
    public static function doCount(Criteria $criteria, $distinct = false, PropelPDO $con = null)
    {
        // we may modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME); // Set the correct dbName

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        // BasePeer returns a PDOStatement
        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }
    /**
     * Selects one object from the DB.
     *
     * @param      Criteria $criteria object used to create the SELECT statement.
     * @param      PropelPDO $con
     * @return UnidaddocumentalTiposervicio
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectOne(Criteria $criteria, PropelPDO $con = null)
    {
        $critcopy = clone $criteria;
        $critcopy->setLimit(1);
        $objects = UnidaddocumentalTiposervicioPeer::doSelect($critcopy, $con);
        if ($objects) {
            return $objects[0];
        }

        return null;
    }
    /**
     * Selects several row from the DB.
     *
     * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
     * @param      PropelPDO $con
     * @return array           Array of selected Objects
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelect(Criteria $criteria, PropelPDO $con = null)
    {
        return UnidaddocumentalTiposervicioPeer::populateObjects(UnidaddocumentalTiposervicioPeer::doSelectStmt($criteria, $con));
    }
    /**
     * Prepares the Criteria object and uses the parent doSelect() method to execute a PDOStatement.
     *
     * Use this method directly if you want to work with an executed statement directly (for example
     * to perform your own object hydration).
     *
     * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
     * @param      PropelPDO $con The connection to use
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     * @return PDOStatement The executed PDOStatement object.
     * @see        BasePeer::doSelect()
     */
    public static function doSelectStmt(Criteria $criteria, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        if (!$criteria->hasSelectClause()) {
            $criteria = clone $criteria;
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }


        // BasePeer returns a PDOStatement
        return BasePeer::doSelect($criteria, $con);
    }
    /**
     * Adds an object to the instance pool.
     *
     * Propel keeps cached copies of objects in an instance pool when they are retrieved
     * from the database.  In some cases -- especially when you override doSelect*()
     * methods in your stub classes -- you may need to explicitly add objects
     * to the cache in order to ensure that the same objects are always returned by doSelect*()
     * and retrieveByPK*() calls.
     *
     * @param UnidaddocumentalTiposervicio $obj A UnidaddocumentalTiposervicio object.
     * @param      string $key (optional) key to use for instance map (for performance boost if key was already calculated externally).
     */
    public static function addInstanceToPool($obj, $key = null)
    {
        if (Propel::isInstancePoolingEnabled()) {
            if ($key === null) {
                $key = (string) $obj->getUnidaddocumentaltiposervicioId();
            } // if key === null
            UnidaddocumentalTiposervicioPeer::$instances[$key] = $obj;
        }
    }

    /**
     * Removes an object from the instance pool.
     *
     * Propel keeps cached copies of objects in an instance pool when they are retrieved
     * from the database.  In some cases -- especially when you override doDelete
     * methods in your stub classes -- you may need to explicitly remove objects
     * from the cache in order to prevent returning objects that no longer exist.
     *
     * @param      mixed $value A UnidaddocumentalTiposervicio object or a primary key value.
     *
     * @return void
     * @throws PropelException - if the value is invalid.
     */
    public static function removeInstanceFromPool($value)
    {
        if (Propel::isInstancePoolingEnabled() && $value !== null) {
            if (is_object($value) && $value instanceof UnidaddocumentalTiposervicio) {
                $key = (string) $value->getUnidaddocumentaltiposervicioId();
            } elseif (is_scalar($value)) {
                // assume we've been passed a primary key
                $key = (string) $value;
            } else {
                $e = new PropelException("Invalid value passed to removeInstanceFromPool().  Expected primary key or UnidaddocumentalTiposervicio object; got " . (is_object($value) ? get_class($value) . ' object.' : var_export($value,true)));
                throw $e;
            }

            unset(UnidaddocumentalTiposervicioPeer::$instances[$key]);
        }
    } // removeInstanceFromPool()

    /**
     * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
     *
     * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
     * a multi-column primary key, a serialize()d version of the primary key will be returned.
     *
     * @param      string $key The key (@see getPrimaryKeyHash()) for this instance.
     * @return UnidaddocumentalTiposervicio Found object or null if 1) no instance exists for specified key or 2) instance pooling has been disabled.
     * @see        getPrimaryKeyHash()
     */
    public static function getInstanceFromPool($key)
    {
        if (Propel::isInstancePoolingEnabled()) {
            if (isset(UnidaddocumentalTiposervicioPeer::$instances[$key])) {
                return UnidaddocumentalTiposervicioPeer::$instances[$key];
            }
        }

        return null; // just to be explicit
    }
    
    /**
     * Clear the instance pool.
     *
     * @return void
     */
    public static function clearInstancePool($and_clear_all_references = false)
    {
      if ($and_clear_all_references) {
        foreach (UnidaddocumentalTiposervicioPeer::$instances as $instance) {
          $instance->clearAllReferences(true);
        }
      }
        UnidaddocumentalTiposervicioPeer::$instances = array();
    }
    
    /**
     * Method to invalidate the instance pool of all tables related to UNIDADDOCUMENTAL_TIPOSERVICIO
     * by a foreign key with ON DELETE CASCADE
     */
    public static function clearRelatedInstancePool()
    {
    }

    /**
     * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
     *
     * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
     * a multi-column primary key, a serialize()d version of the primary key will be returned.
     *
     * @param      array $row PropelPDO resultset row.
     * @param      int $startcol The 0-based offset for reading from the resultset row.
     * @return string A string version of PK or null if the components of primary key in result array are all null.
     */
    public static function getPrimaryKeyHashFromRow($row, $startcol = 0)
    {
        // If the PK cannot be derived from the row, return null.
        if ($row[$startcol] === null) {
            return null;
        }

        return (string) $row[$startcol];
    }

    /**
     * Retrieves the primary key from the DB resultset row
     * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
     * a multi-column primary key, an array of the primary key columns will be returned.
     *
     * @param      array $row PropelPDO resultset row.
     * @param      int $startcol The 0-based offset for reading from the resultset row.
     * @return mixed The primary key of the row
     */
    public static function getPrimaryKeyFromRow($row, $startcol = 0)
    {

        return (int) $row[$startcol];
    }
    
    /**
     * The returned array will contain objects of the default type or
     * objects that inherit from the default.
     *
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function populateObjects(PDOStatement $stmt)
    {
        $results = array();
    
        // set the class once to avoid overhead in the loop
        $cls = UnidaddocumentalTiposervicioPeer::getOMClass();
        // populate the object(s)
        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj->hydrate($row, 0, true); // rehydrate
                $results[] = $obj;
            } else {
                $obj = new $cls();
                $obj->hydrate($row);
                $results[] = $obj;
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj, $key);
            } // if key exists
        }
        $stmt->closeCursor();

        return $results;
    }
    /**
     * Populates an object of the default type or an object that inherit from the default.
     *
     * @param      array $row PropelPDO resultset row.
     * @param      int $startcol The 0-based offset for reading from the resultset row.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     * @return array (UnidaddocumentalTiposervicio object, last column rank)
     */
    public static function populateObject($row, $startcol = 0)
    {
        $key = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, $startcol);
        if (null !== ($obj = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key))) {
            // We no longer rehydrate the object, since this can cause data loss.
            // See http://www.propelorm.org/ticket/509
            // $obj->hydrate($row, $startcol, true); // rehydrate
            $col = $startcol + UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;
        } else {
            $cls = UnidaddocumentalTiposervicioPeer::OM_CLASS;
            $obj = new $cls();
            $col = $obj->hydrate($row, $startcol);
            UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj, $key);
        }

        return array($obj, $col);
    }


    /**
     * Returns the number of rows matching criteria, joining the related Usuario table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinUsuario(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related UnidadDocumental table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinUnidadDocumental(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related TipoServicio table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinTipoServicio(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related TipoDocumental table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinTipoDocumental(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with their Usuario objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinUsuario(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;
        UsuarioPeer::addSelectColumns($criteria);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = UsuarioPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = UsuarioPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = UsuarioPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    UsuarioPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to $obj2 (Usuario)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with their UnidadDocumental objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinUnidadDocumental(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;
        UnidadDocumentalPeer::addSelectColumns($criteria);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = UnidadDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = UnidadDocumentalPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = UnidadDocumentalPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    UnidadDocumentalPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to $obj2 (UnidadDocumental)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with their TipoServicio objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinTipoServicio(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;
        TipoServicioPeer::addSelectColumns($criteria);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = TipoServicioPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = TipoServicioPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = TipoServicioPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    TipoServicioPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to $obj2 (TipoServicio)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with their TipoDocumental objects.
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinTipoDocumental(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;
        TipoDocumentalPeer::addSelectColumns($criteria);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {

                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if $obj1 already loaded

            $key2 = TipoDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol);
            if ($key2 !== null) {
                $obj2 = TipoDocumentalPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = TipoDocumentalPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol);
                    TipoDocumentalPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to $obj2 (TipoDocumental)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row was not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Returns the number of rows matching criteria, joining all related tables
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAll(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }

    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with all related objects.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAll(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol2 = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;

        UsuarioPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + UsuarioPeer::NUM_HYDRATE_COLUMNS;

        UnidadDocumentalPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + UnidadDocumentalPeer::NUM_HYDRATE_COLUMNS;

        TipoServicioPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + TipoServicioPeer::NUM_HYDRATE_COLUMNS;

        TipoDocumentalPeer::addSelectColumns($criteria);
        $startcol6 = $startcol5 + TipoDocumentalPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

            // Add objects for joined Usuario rows

            $key2 = UsuarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
            if ($key2 !== null) {
                $obj2 = UsuarioPeer::getInstanceFromPool($key2);
                if (!$obj2) {

                    $cls = UsuarioPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    UsuarioPeer::addInstanceToPool($obj2, $key2);
                } // if obj2 loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj2 (Usuario)
                $obj2->addUnidaddocumentalTiposervicio($obj1);
            } // if joined row not null

            // Add objects for joined UnidadDocumental rows

            $key3 = UnidadDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol3);
            if ($key3 !== null) {
                $obj3 = UnidadDocumentalPeer::getInstanceFromPool($key3);
                if (!$obj3) {

                    $cls = UnidadDocumentalPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    UnidadDocumentalPeer::addInstanceToPool($obj3, $key3);
                } // if obj3 loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj3 (UnidadDocumental)
                $obj3->addUnidaddocumentalTiposervicio($obj1);
            } // if joined row not null

            // Add objects for joined TipoServicio rows

            $key4 = TipoServicioPeer::getPrimaryKeyHashFromRow($row, $startcol4);
            if ($key4 !== null) {
                $obj4 = TipoServicioPeer::getInstanceFromPool($key4);
                if (!$obj4) {

                    $cls = TipoServicioPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    TipoServicioPeer::addInstanceToPool($obj4, $key4);
                } // if obj4 loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj4 (TipoServicio)
                $obj4->addUnidaddocumentalTiposervicio($obj1);
            } // if joined row not null

            // Add objects for joined TipoDocumental rows

            $key5 = TipoDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol5);
            if ($key5 !== null) {
                $obj5 = TipoDocumentalPeer::getInstanceFromPool($key5);
                if (!$obj5) {

                    $cls = TipoDocumentalPeer::getOMClass();

                    $obj5 = new $cls();
                    $obj5->hydrate($row, $startcol5);
                    TipoDocumentalPeer::addInstanceToPool($obj5, $key5);
                } // if obj5 loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj5 (TipoDocumental)
                $obj5->addUnidaddocumentalTiposervicio($obj1);
            } // if joined row not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Returns the number of rows matching criteria, joining the related Usuario table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptUsuario(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related UnidadDocumental table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptUnidadDocumental(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related TipoServicio table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptTipoServicio(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Returns the number of rows matching criteria, joining the related TipoDocumental table
     *
     * @param      Criteria $criteria
     * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return int Number of matching rows.
     */
    public static function doCountJoinAllExceptTipoDocumental(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        // we're going to modify criteria, so copy it first
        $criteria = clone $criteria;

        // We need to set the primary table name, since in the case that there are no WHERE columns
        // it will be impossible for the BasePeer::createSelectSql() method to determine which
        // tables go into the FROM clause.
        $criteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

        if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
            $criteria->setDistinct();
        }

        if (!$criteria->hasSelectClause()) {
            UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        }

        $criteria->clearOrderByColumns(); // ORDER BY should not affect count

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
    
        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }

        $stmt = BasePeer::doCount($criteria, $con);

        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $count = (int) $row[0];
        } else {
            $count = 0; // no rows returned; we infer that means 0 matches.
        }
        $stmt->closeCursor();

        return $count;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with all related objects except Usuario.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptUsuario(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol2 = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;

        UnidadDocumentalPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + UnidadDocumentalPeer::NUM_HYDRATE_COLUMNS;

        TipoServicioPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + TipoServicioPeer::NUM_HYDRATE_COLUMNS;

        TipoDocumentalPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + TipoDocumentalPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined UnidadDocumental rows

                $key2 = UnidadDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = UnidadDocumentalPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = UnidadDocumentalPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    UnidadDocumentalPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj2 (UnidadDocumental)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined TipoServicio rows

                $key3 = TipoServicioPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = TipoServicioPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = TipoServicioPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    TipoServicioPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj3 (TipoServicio)
                $obj3->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined TipoDocumental rows

                $key4 = TipoDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = TipoDocumentalPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = TipoDocumentalPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    TipoDocumentalPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj4 (TipoDocumental)
                $obj4->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with all related objects except UnidadDocumental.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptUnidadDocumental(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol2 = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;

        UsuarioPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + UsuarioPeer::NUM_HYDRATE_COLUMNS;

        TipoServicioPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + TipoServicioPeer::NUM_HYDRATE_COLUMNS;

        TipoDocumentalPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + TipoDocumentalPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined Usuario rows

                $key2 = UsuarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = UsuarioPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = UsuarioPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    UsuarioPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj2 (Usuario)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined TipoServicio rows

                $key3 = TipoServicioPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = TipoServicioPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = TipoServicioPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    TipoServicioPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj3 (TipoServicio)
                $obj3->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined TipoDocumental rows

                $key4 = TipoDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = TipoDocumentalPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = TipoDocumentalPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    TipoDocumentalPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj4 (TipoDocumental)
                $obj4->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with all related objects except TipoServicio.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptTipoServicio(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol2 = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;

        UsuarioPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + UsuarioPeer::NUM_HYDRATE_COLUMNS;

        UnidadDocumentalPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + UnidadDocumentalPeer::NUM_HYDRATE_COLUMNS;

        TipoDocumentalPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + TipoDocumentalPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPODOCUMENTAL_ID, TipoDocumentalPeer::TIPODOCUMENTAL_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined Usuario rows

                $key2 = UsuarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = UsuarioPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = UsuarioPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    UsuarioPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj2 (Usuario)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined UnidadDocumental rows

                $key3 = UnidadDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = UnidadDocumentalPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = UnidadDocumentalPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    UnidadDocumentalPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj3 (UnidadDocumental)
                $obj3->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined TipoDocumental rows

                $key4 = TipoDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = TipoDocumentalPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = TipoDocumentalPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    TipoDocumentalPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj4 (TipoDocumental)
                $obj4->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }


    /**
     * Selects a collection of UnidaddocumentalTiposervicio objects pre-filled with all related objects except TipoDocumental.
     *
     * @param      Criteria  $criteria
     * @param      PropelPDO $con
     * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
     * @return array           Array of UnidaddocumentalTiposervicio objects.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doSelectJoinAllExceptTipoDocumental(Criteria $criteria, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $criteria = clone $criteria;

        // Set the correct dbName if it has not been overridden
        // $criteria->getDbName() will return the same object if not set to another value
        // so == check is okay and faster
        if ($criteria->getDbName() == Propel::getDefaultDB()) {
            $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        }

        UnidaddocumentalTiposervicioPeer::addSelectColumns($criteria);
        $startcol2 = UnidaddocumentalTiposervicioPeer::NUM_HYDRATE_COLUMNS;

        UsuarioPeer::addSelectColumns($criteria);
        $startcol3 = $startcol2 + UsuarioPeer::NUM_HYDRATE_COLUMNS;

        UnidadDocumentalPeer::addSelectColumns($criteria);
        $startcol4 = $startcol3 + UnidadDocumentalPeer::NUM_HYDRATE_COLUMNS;

        TipoServicioPeer::addSelectColumns($criteria);
        $startcol5 = $startcol4 + TipoServicioPeer::NUM_HYDRATE_COLUMNS;

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::USUARIO_ID, UsuarioPeer::USUARIO_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTAL_ID, UnidadDocumentalPeer::UNIDADDOCUMENTAL_ID, $join_behavior);

        $criteria->addJoin(UnidaddocumentalTiposervicioPeer::TIPOSERVICIO_ID, TipoServicioPeer::TIPOSERVICIO_ID, $join_behavior);

        // symfony_behaviors behavior
        foreach (sfMixer::getCallables(self::getMixerPreSelectHook(__FUNCTION__)) as $sf_hook)
        {
          call_user_func($sf_hook, 'BaseUnidaddocumentalTiposervicioPeer', $criteria, $con);
        }


        $stmt = BasePeer::doSelect($criteria, $con);
        $results = array();

        while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $key1 = UnidaddocumentalTiposervicioPeer::getPrimaryKeyHashFromRow($row, 0);
            if (null !== ($obj1 = UnidaddocumentalTiposervicioPeer::getInstanceFromPool($key1))) {
                // We no longer rehydrate the object, since this can cause data loss.
                // See http://www.propelorm.org/ticket/509
                // $obj1->hydrate($row, 0, true); // rehydrate
            } else {
                $cls = UnidaddocumentalTiposervicioPeer::getOMClass();

                $obj1 = new $cls();
                $obj1->hydrate($row);
                UnidaddocumentalTiposervicioPeer::addInstanceToPool($obj1, $key1);
            } // if obj1 already loaded

                // Add objects for joined Usuario rows

                $key2 = UsuarioPeer::getPrimaryKeyHashFromRow($row, $startcol2);
                if ($key2 !== null) {
                    $obj2 = UsuarioPeer::getInstanceFromPool($key2);
                    if (!$obj2) {
    
                        $cls = UsuarioPeer::getOMClass();

                    $obj2 = new $cls();
                    $obj2->hydrate($row, $startcol2);
                    UsuarioPeer::addInstanceToPool($obj2, $key2);
                } // if $obj2 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj2 (Usuario)
                $obj2->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined UnidadDocumental rows

                $key3 = UnidadDocumentalPeer::getPrimaryKeyHashFromRow($row, $startcol3);
                if ($key3 !== null) {
                    $obj3 = UnidadDocumentalPeer::getInstanceFromPool($key3);
                    if (!$obj3) {
    
                        $cls = UnidadDocumentalPeer::getOMClass();

                    $obj3 = new $cls();
                    $obj3->hydrate($row, $startcol3);
                    UnidadDocumentalPeer::addInstanceToPool($obj3, $key3);
                } // if $obj3 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj3 (UnidadDocumental)
                $obj3->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

                // Add objects for joined TipoServicio rows

                $key4 = TipoServicioPeer::getPrimaryKeyHashFromRow($row, $startcol4);
                if ($key4 !== null) {
                    $obj4 = TipoServicioPeer::getInstanceFromPool($key4);
                    if (!$obj4) {
    
                        $cls = TipoServicioPeer::getOMClass();

                    $obj4 = new $cls();
                    $obj4->hydrate($row, $startcol4);
                    TipoServicioPeer::addInstanceToPool($obj4, $key4);
                } // if $obj4 already loaded

                // Add the $obj1 (UnidaddocumentalTiposervicio) to the collection in $obj4 (TipoServicio)
                $obj4->addUnidaddocumentalTiposervicio($obj1);

            } // if joined row is not null

            $results[] = $obj1;
        }
        $stmt->closeCursor();

        return $results;
    }

    /**
     * Returns the TableMap related to this peer.
     * This method is not needed for general use but a specific application could have a need.
     * @return TableMap
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function getTableMap()
    {
        return Propel::getDatabaseMap(UnidaddocumentalTiposervicioPeer::DATABASE_NAME)->getTable(UnidaddocumentalTiposervicioPeer::TABLE_NAME);
    }

    /**
     * Add a TableMap instance to the database for this peer class.
     */
    public static function buildTableMap()
    {
      $dbMap = Propel::getDatabaseMap(BaseUnidaddocumentalTiposervicioPeer::DATABASE_NAME);
      if (!$dbMap->hasTable(BaseUnidaddocumentalTiposervicioPeer::TABLE_NAME)) {
        $dbMap->addTableObject(new \UnidaddocumentalTiposervicioTableMap());
      }
    }

    /**
     * The class that the Peer will make instances of.
     *
     *
     * @return string ClassName
     */
    public static function getOMClass($row = 0, $colnum = 0)
    {
        return UnidaddocumentalTiposervicioPeer::OM_CLASS;
    }

    /**
     * Performs an INSERT on the database, given a UnidaddocumentalTiposervicio or Criteria object.
     *
     * @param      mixed $values Criteria or UnidaddocumentalTiposervicio object containing data that is used to create the INSERT statement.
     * @param      PropelPDO $con the PropelPDO connection to use
     * @return mixed           The new primary key.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doInsert($values, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        if ($values instanceof Criteria) {
            $criteria = clone $values; // rename for clarity
        } else {
            $criteria = $values->buildCriteria(); // build Criteria from UnidaddocumentalTiposervicio object
        }

        if ($criteria->containsKey(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID) && $criteria->keyContainsValue(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID) ) {
            throw new PropelException('Cannot insert a value for auto-increment primary key ('.UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID.')');
        }

        // remove pkey col since this table uses auto-increment and passing a null value for it is not valid
        $criteria->remove(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID);


        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        try {
            // use transaction because $criteria could contain info
            // for more than one table (I guess, conceivably)
            $con->beginTransaction();
            $pk = BasePeer::doInsert($criteria, $con);
            $con->commit();
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }

        return $pk;
    }

    /**
     * Performs an UPDATE on the database, given a UnidaddocumentalTiposervicio or Criteria object.
     *
     * @param      mixed $values Criteria or UnidaddocumentalTiposervicio object containing data that is used to create the UPDATE statement.
     * @param      PropelPDO $con The connection to use (specify PropelPDO connection object to exert more control over transactions).
     * @return int             The number of affected rows (if supported by underlying database driver).
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function doUpdate($values, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $selectCriteria = new Criteria(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        if ($values instanceof Criteria) {
            $criteria = clone $values; // rename for clarity

            $comparison = $criteria->getComparison(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID);
            $value = $criteria->remove(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID);
            if ($value) {
                $selectCriteria->add(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID, $value, $comparison);
            } else {
                $selectCriteria->setPrimaryTableName(UnidaddocumentalTiposervicioPeer::TABLE_NAME);
            }

        } else { // $values is UnidaddocumentalTiposervicio object
            $criteria = $values->buildCriteria(); // gets full criteria
            $selectCriteria = $values->buildPkeyCriteria(); // gets criteria w/ primary key(s)
        }

        // set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        return BasePeer::doUpdate($selectCriteria, $criteria, $con);
    }

    /**
     * Deletes all rows from the UNIDADDOCUMENTAL_TIPOSERVICIO table.
     *
     * @param      PropelPDO $con the connection to use
     * @return int             The number of affected rows (if supported by underlying database driver).
     * @throws PropelException
     */
    public static function doDeleteAll(PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }
        $affectedRows = 0; // initialize var to track total num of affected rows
        try {
            // use transaction because $criteria could contain info
            // for more than one table or we could emulating ON DELETE CASCADE, etc.
            $con->beginTransaction();
            $affectedRows += BasePeer::doDeleteAll(UnidaddocumentalTiposervicioPeer::TABLE_NAME, $con, UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
            // Because this db requires some delete cascade/set null emulation, we have to
            // clear the cached instance *after* the emulation has happened (since
            // instances get re-added by the select statement contained therein).
            UnidaddocumentalTiposervicioPeer::clearInstancePool();
            UnidaddocumentalTiposervicioPeer::clearRelatedInstancePool();
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs a DELETE on the database, given a UnidaddocumentalTiposervicio or Criteria object OR a primary key value.
     *
     * @param      mixed $values Criteria or UnidaddocumentalTiposervicio object or primary key or array of primary keys
     *              which is used to create the DELETE statement
     * @param      PropelPDO $con the connection to use
     * @return int The number of affected rows (if supported by underlying database driver).  This includes CASCADE-related rows
     *				if supported by native driver or if emulated using Propel.
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
     public static function doDelete($values, PropelPDO $con = null)
     {
        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        if ($values instanceof Criteria) {
            // invalidate the cache for all objects of this type, since we have no
            // way of knowing (without running a query) what objects should be invalidated
            // from the cache based on this Criteria.
            UnidaddocumentalTiposervicioPeer::clearInstancePool();
            // rename for clarity
            $criteria = clone $values;
        } elseif ($values instanceof UnidaddocumentalTiposervicio) { // it's a model object
            // invalidate the cache for this single object
            UnidaddocumentalTiposervicioPeer::removeInstanceFromPool($values);
            // create criteria based on pk values
            $criteria = $values->buildPkeyCriteria();
        } else { // it's a primary key, or an array of pks
            $criteria = new Criteria(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
            $criteria->add(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID, (array) $values, Criteria::IN);
            // invalidate the cache for this object(s)
            foreach ((array) $values as $singleval) {
                UnidaddocumentalTiposervicioPeer::removeInstanceFromPool($singleval);
            }
        }

        // Set the correct dbName
        $criteria->setDbName(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);

        $affectedRows = 0; // initialize var to track total num of affected rows

        try {
            // use transaction because $criteria could contain info
            // for more than one table or we could emulating ON DELETE CASCADE, etc.
            $con->beginTransaction();
            
            $affectedRows += BasePeer::doDelete($criteria, $con);
            UnidaddocumentalTiposervicioPeer::clearRelatedInstancePool();
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Validates all modified columns of given UnidaddocumentalTiposervicio object.
     * If parameter $columns is either a single column name or an array of column names
     * than only those columns are validated.
     *
     * NOTICE: This does not apply to primary or foreign keys for now.
     *
     * @param UnidaddocumentalTiposervicio $obj The object to validate.
     * @param      mixed $cols Column name or array of column names.
     *
     * @return mixed TRUE if all columns are valid or the error message of the first invalid column.
     */
    public static function doValidate($obj, $cols = null)
    {
        $columns = array();

        if ($cols) {
            $dbMap = Propel::getDatabaseMap(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
            $tableMap = $dbMap->getTable(UnidaddocumentalTiposervicioPeer::TABLE_NAME);

            if (! is_array($cols)) {
                $cols = array($cols);
            }

            foreach ($cols as $colName) {
                if ($tableMap->hasColumn($colName)) {
                    $get = 'get' . $tableMap->getColumn($colName)->getPhpName();
                    $columns[$colName] = $obj->$get();
                }
            }
        } else {

        }

        return BasePeer::doValidate(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, UnidaddocumentalTiposervicioPeer::TABLE_NAME, $columns);
    }

    /**
     * Retrieve a single object by pkey.
     *
     * @param int $pk the primary key.
     * @param      PropelPDO $con the connection to use
     * @return UnidaddocumentalTiposervicio
     */
    public static function retrieveByPK($pk, PropelPDO $con = null)
    {

        if (null !== ($obj = UnidaddocumentalTiposervicioPeer::getInstanceFromPool((string) $pk))) {
            return $obj;
        }

        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $criteria = new Criteria(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
        $criteria->add(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID, $pk);

        $v = UnidaddocumentalTiposervicioPeer::doSelect($criteria, $con);

        return !empty($v) > 0 ? $v[0] : null;
    }

    /**
     * Retrieve multiple objects by pkey.
     *
     * @param      array $pks List of primary keys
     * @param      PropelPDO $con the connection to use
     * @return UnidaddocumentalTiposervicio[]
     * @throws PropelException Any exceptions caught during processing will be
     *		 rethrown wrapped into a PropelException.
     */
    public static function retrieveByPKs($pks, PropelPDO $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection(UnidaddocumentalTiposervicioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        $objs = null;
        if (empty($pks)) {
            $objs = array();
        } else {
            $criteria = new Criteria(UnidaddocumentalTiposervicioPeer::DATABASE_NAME);
            $criteria->add(UnidaddocumentalTiposervicioPeer::UNIDADDOCUMENTALTIPOSERVICIO_ID, $pks, Criteria::IN);
            $objs = UnidaddocumentalTiposervicioPeer::doSelect($criteria, $con);
        }

        return $objs;
    }

    // symfony behavior
    
    /**
     * Returns an array of arrays that contain columns in each unique index.
     *
     * @return array
     */
    static public function getUniqueColumnNames()
    {
      return array();
    }

    // symfony_behaviors behavior
    
    /**
     * Returns the name of the hook to call from inside the supplied method.
     *
     * @param string $method The calling method
     *
     * @return string A hook name for {@link sfMixer}
     *
     * @throws LogicException If the method name is not recognized
     */
    static private function getMixerPreSelectHook($method)
    {
      if (preg_match('/^do(Select|Count)(Join(All(Except)?)?|Stmt)?/', $method, $match))
      {
        return sprintf('BaseUnidaddocumentalTiposervicioPeer:%s:%1$s', 'Count' == $match[1] ? 'doCount' : $match[0]);
      }
    
      throw new LogicException(sprintf('Unrecognized function "%s"', $method));
    }

} // BaseUnidaddocumentalTiposervicioPeer

// This is the static code needed to register the TableMap for this table with the main Propel class.
//
BaseUnidaddocumentalTiposervicioPeer::buildTableMap();


<?php


/**
 * Base class that represents a query for the 'REDIRECCION_USUARIO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 09/22/23 12:53:42
 *
 * @method RedireccionUsuarioQuery orderByRedireccionusuarioId($order = Criteria::ASC) Order by the REDIRECCIONUSUARIO_ID column
 * @method RedireccionUsuarioQuery orderByRedireccionId($order = Criteria::ASC) Order by the REDIRECCION_ID column
 * @method RedireccionUsuarioQuery orderByRolusuredireccionId($order = Criteria::ASC) Order by the ROLUSUREDIRECCION_ID column
 * @method RedireccionUsuarioQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 *
 * @method RedireccionUsuarioQuery groupByRedireccionusuarioId() Group by the REDIRECCIONUSUARIO_ID column
 * @method RedireccionUsuarioQuery groupByRedireccionId() Group by the REDIRECCION_ID column
 * @method RedireccionUsuarioQuery groupByRolusuredireccionId() Group by the ROLUSUREDIRECCION_ID column
 * @method RedireccionUsuarioQuery groupByUsuarioId() Group by the USUARIO_ID column
 *
 * @method RedireccionUsuarioQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method RedireccionUsuarioQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method RedireccionUsuarioQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method RedireccionUsuarioQuery leftJoinRedireccion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Redireccion relation
 * @method RedireccionUsuarioQuery rightJoinRedireccion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Redireccion relation
 * @method RedireccionUsuarioQuery innerJoinRedireccion($relationAlias = null) Adds a INNER JOIN clause to the query using the Redireccion relation
 *
 * @method RedireccionUsuarioQuery leftJoinRolUsuRedireccion($relationAlias = null) Adds a LEFT JOIN clause to the query using the RolUsuRedireccion relation
 * @method RedireccionUsuarioQuery rightJoinRolUsuRedireccion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the RolUsuRedireccion relation
 * @method RedireccionUsuarioQuery innerJoinRolUsuRedireccion($relationAlias = null) Adds a INNER JOIN clause to the query using the RolUsuRedireccion relation
 *
 * @method RedireccionUsuarioQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method RedireccionUsuarioQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method RedireccionUsuarioQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method RedireccionUsuario findOne(PropelPDO $con = null) Return the first RedireccionUsuario matching the query
 * @method RedireccionUsuario findOneOrCreate(PropelPDO $con = null) Return the first RedireccionUsuario matching the query, or a new RedireccionUsuario object populated from the query conditions when no match is found
 *
 * @method RedireccionUsuario findOneByRedireccionId(int $REDIRECCION_ID) Return the first RedireccionUsuario filtered by the REDIRECCION_ID column
 * @method RedireccionUsuario findOneByRolusuredireccionId(int $ROLUSUREDIRECCION_ID) Return the first RedireccionUsuario filtered by the ROLUSUREDIRECCION_ID column
 * @method RedireccionUsuario findOneByUsuarioId(int $USUARIO_ID) Return the first RedireccionUsuario filtered by the USUARIO_ID column
 *
 * @method array findByRedireccionusuarioId(int $REDIRECCIONUSUARIO_ID) Return RedireccionUsuario objects filtered by the REDIRECCIONUSUARIO_ID column
 * @method array findByRedireccionId(int $REDIRECCION_ID) Return RedireccionUsuario objects filtered by the REDIRECCION_ID column
 * @method array findByRolusuredireccionId(int $ROLUSUREDIRECCION_ID) Return RedireccionUsuario objects filtered by the ROLUSUREDIRECCION_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return RedireccionUsuario objects filtered by the USUARIO_ID column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseRedireccionUsuarioQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseRedireccionUsuarioQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'RedireccionUsuario';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new RedireccionUsuarioQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   RedireccionUsuarioQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return RedireccionUsuarioQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof RedireccionUsuarioQuery) {
            return $criteria;
        }
        $query = new RedireccionUsuarioQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   RedireccionUsuario|RedireccionUsuario[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = RedireccionUsuarioPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(RedireccionUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 RedireccionUsuario A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByRedireccionusuarioId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 RedireccionUsuario A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [REDIRECCIONUSUARIO_ID], [REDIRECCION_ID], [ROLUSUREDIRECCION_ID], [USUARIO_ID] FROM [REDIRECCION_USUARIO] WHERE [REDIRECCIONUSUARIO_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new RedireccionUsuario();
            $obj->hydrate($row);
            RedireccionUsuarioPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return RedireccionUsuario|RedireccionUsuario[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|RedireccionUsuario[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCIONUSUARIO_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCIONUSUARIO_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the REDIRECCIONUSUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRedireccionusuarioId(1234); // WHERE REDIRECCIONUSUARIO_ID = 1234
     * $query->filterByRedireccionusuarioId(array(12, 34)); // WHERE REDIRECCIONUSUARIO_ID IN (12, 34)
     * $query->filterByRedireccionusuarioId(array('min' => 12)); // WHERE REDIRECCIONUSUARIO_ID >= 12
     * $query->filterByRedireccionusuarioId(array('max' => 12)); // WHERE REDIRECCIONUSUARIO_ID <= 12
     * </code>
     *
     * @param     mixed $redireccionusuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function filterByRedireccionusuarioId($redireccionusuarioId = null, $comparison = null)
    {
        if (is_array($redireccionusuarioId)) {
            $useMinMax = false;
            if (isset($redireccionusuarioId['min'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCIONUSUARIO_ID, $redireccionusuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($redireccionusuarioId['max'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCIONUSUARIO_ID, $redireccionusuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCIONUSUARIO_ID, $redireccionusuarioId, $comparison);
    }

    /**
     * Filter the query on the REDIRECCION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRedireccionId(1234); // WHERE REDIRECCION_ID = 1234
     * $query->filterByRedireccionId(array(12, 34)); // WHERE REDIRECCION_ID IN (12, 34)
     * $query->filterByRedireccionId(array('min' => 12)); // WHERE REDIRECCION_ID >= 12
     * $query->filterByRedireccionId(array('max' => 12)); // WHERE REDIRECCION_ID <= 12
     * </code>
     *
     * @see       filterByRedireccion()
     *
     * @param     mixed $redireccionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function filterByRedireccionId($redireccionId = null, $comparison = null)
    {
        if (is_array($redireccionId)) {
            $useMinMax = false;
            if (isset($redireccionId['min'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCION_ID, $redireccionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($redireccionId['max'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCION_ID, $redireccionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCION_ID, $redireccionId, $comparison);
    }

    /**
     * Filter the query on the ROLUSUREDIRECCION_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByRolusuredireccionId(1234); // WHERE ROLUSUREDIRECCION_ID = 1234
     * $query->filterByRolusuredireccionId(array(12, 34)); // WHERE ROLUSUREDIRECCION_ID IN (12, 34)
     * $query->filterByRolusuredireccionId(array('min' => 12)); // WHERE ROLUSUREDIRECCION_ID >= 12
     * $query->filterByRolusuredireccionId(array('max' => 12)); // WHERE ROLUSUREDIRECCION_ID <= 12
     * </code>
     *
     * @see       filterByRolUsuRedireccion()
     *
     * @param     mixed $rolusuredireccionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function filterByRolusuredireccionId($rolusuredireccionId = null, $comparison = null)
    {
        if (is_array($rolusuredireccionId)) {
            $useMinMax = false;
            if (isset($rolusuredireccionId['min'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::ROLUSUREDIRECCION_ID, $rolusuredireccionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($rolusuredireccionId['max'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::ROLUSUREDIRECCION_ID, $rolusuredireccionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionUsuarioPeer::ROLUSUREDIRECCION_ID, $rolusuredireccionId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(RedireccionUsuarioPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RedireccionUsuarioPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query by a related Redireccion object
     *
     * @param   Redireccion|PropelObjectCollection $redireccion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RedireccionUsuarioQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRedireccion($redireccion, $comparison = null)
    {
        if ($redireccion instanceof Redireccion) {
            return $this
                ->addUsingAlias(RedireccionUsuarioPeer::REDIRECCION_ID, $redireccion->getRedireccionId(), $comparison);
        } elseif ($redireccion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RedireccionUsuarioPeer::REDIRECCION_ID, $redireccion->toKeyValue('PrimaryKey', 'RedireccionId'), $comparison);
        } else {
            throw new PropelException('filterByRedireccion() only accepts arguments of type Redireccion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Redireccion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function joinRedireccion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Redireccion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Redireccion');
        }

        return $this;
    }

    /**
     * Use the Redireccion relation Redireccion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RedireccionQuery A secondary query class using the current class as primary query
     */
    public function useRedireccionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinRedireccion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Redireccion', 'RedireccionQuery');
    }

    /**
     * Filter the query by a related RolUsuRedireccion object
     *
     * @param   RolUsuRedireccion|PropelObjectCollection $rolUsuRedireccion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RedireccionUsuarioQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByRolUsuRedireccion($rolUsuRedireccion, $comparison = null)
    {
        if ($rolUsuRedireccion instanceof RolUsuRedireccion) {
            return $this
                ->addUsingAlias(RedireccionUsuarioPeer::ROLUSUREDIRECCION_ID, $rolUsuRedireccion->getRolusuredireccionId(), $comparison);
        } elseif ($rolUsuRedireccion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RedireccionUsuarioPeer::ROLUSUREDIRECCION_ID, $rolUsuRedireccion->toKeyValue('PrimaryKey', 'RolusuredireccionId'), $comparison);
        } else {
            throw new PropelException('filterByRolUsuRedireccion() only accepts arguments of type RolUsuRedireccion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the RolUsuRedireccion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function joinRolUsuRedireccion($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('RolUsuRedireccion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'RolUsuRedireccion');
        }

        return $this;
    }

    /**
     * Use the RolUsuRedireccion relation RolUsuRedireccion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RolUsuRedireccionQuery A secondary query class using the current class as primary query
     */
    public function useRolUsuRedireccionQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinRolUsuRedireccion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'RolUsuRedireccion', 'RolUsuRedireccionQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 RedireccionUsuarioQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(RedireccionUsuarioPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(RedireccionUsuarioPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   RedireccionUsuario $redireccionUsuario Object to remove from the list of results
     *
     * @return RedireccionUsuarioQuery The current query, for fluid interface
     */
    public function prune($redireccionUsuario = null)
    {
        if ($redireccionUsuario) {
            $this->addUsingAlias(RedireccionUsuarioPeer::REDIRECCIONUSUARIO_ID, $redireccionUsuario->getRedireccionusuarioId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

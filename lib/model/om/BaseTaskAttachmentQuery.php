<?php


/**
 * Base class that represents a query for the 'TASK_ATTACHMENT' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * vie 12 dic 2025 16:15:08
 *
 * @method TaskAttachmentQuery orderByTaskattachmentId($order = Criteria::ASC) Order by the TASKATTACHMENT_ID column
 * @method TaskAttachmentQuery orderByTaskinstanceId($order = Criteria::ASC) Order by the TASKINSTANCE_ID column
 * @method TaskAttachmentQuery orderByWorkflowinstanceId($order = Criteria::ASC) Order by the WORKFLOWINSTANCE_ID column
 * @method TaskAttachmentQuery orderByUsuarioId($order = Criteria::ASC) Order by the USUARIO_ID column
 * @method TaskAttachmentQuery orderByFilename($order = Criteria::ASC) Order by the FILENAME column
 * @method TaskAttachmentQuery orderByOriginalFilename($order = Criteria::ASC) Order by the ORIGINAL_FILENAME column
 * @method TaskAttachmentQuery orderByPathAbsolute($order = Criteria::ASC) Order by the PATH_ABSOLUTE column
 * @method TaskAttachmentQuery orderByPathRelative($order = Criteria::ASC) Order by the PATH_RELATIVE column
 * @method TaskAttachmentQuery orderByFileSize($order = Criteria::ASC) Order by the FILE_SIZE column
 * @method TaskAttachmentQuery orderByMimeType($order = Criteria::ASC) Order by the MIME_TYPE column
 * @method TaskAttachmentQuery orderByDescripcion($order = Criteria::ASC) Order by the DESCRIPCION column
 * @method TaskAttachmentQuery orderByIsPublic($order = Criteria::ASC) Order by the IS_PUBLIC column
 * @method TaskAttachmentQuery orderByFechaCreacion($order = Criteria::ASC) Order by the FECHA_CREACION column
 * @method TaskAttachmentQuery orderByFechaModificacion($order = Criteria::ASC) Order by the FECHA_MODIFICACION column
 *
 * @method TaskAttachmentQuery groupByTaskattachmentId() Group by the TASKATTACHMENT_ID column
 * @method TaskAttachmentQuery groupByTaskinstanceId() Group by the TASKINSTANCE_ID column
 * @method TaskAttachmentQuery groupByWorkflowinstanceId() Group by the WORKFLOWINSTANCE_ID column
 * @method TaskAttachmentQuery groupByUsuarioId() Group by the USUARIO_ID column
 * @method TaskAttachmentQuery groupByFilename() Group by the FILENAME column
 * @method TaskAttachmentQuery groupByOriginalFilename() Group by the ORIGINAL_FILENAME column
 * @method TaskAttachmentQuery groupByPathAbsolute() Group by the PATH_ABSOLUTE column
 * @method TaskAttachmentQuery groupByPathRelative() Group by the PATH_RELATIVE column
 * @method TaskAttachmentQuery groupByFileSize() Group by the FILE_SIZE column
 * @method TaskAttachmentQuery groupByMimeType() Group by the MIME_TYPE column
 * @method TaskAttachmentQuery groupByDescripcion() Group by the DESCRIPCION column
 * @method TaskAttachmentQuery groupByIsPublic() Group by the IS_PUBLIC column
 * @method TaskAttachmentQuery groupByFechaCreacion() Group by the FECHA_CREACION column
 * @method TaskAttachmentQuery groupByFechaModificacion() Group by the FECHA_MODIFICACION column
 *
 * @method TaskAttachmentQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TaskAttachmentQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TaskAttachmentQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TaskAttachmentQuery leftJoinTaskInstance($relationAlias = null) Adds a LEFT JOIN clause to the query using the TaskInstance relation
 * @method TaskAttachmentQuery rightJoinTaskInstance($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TaskInstance relation
 * @method TaskAttachmentQuery innerJoinTaskInstance($relationAlias = null) Adds a INNER JOIN clause to the query using the TaskInstance relation
 *
 * @method TaskAttachmentQuery leftJoinWorkflowInstance($relationAlias = null) Adds a LEFT JOIN clause to the query using the WorkflowInstance relation
 * @method TaskAttachmentQuery rightJoinWorkflowInstance($relationAlias = null) Adds a RIGHT JOIN clause to the query using the WorkflowInstance relation
 * @method TaskAttachmentQuery innerJoinWorkflowInstance($relationAlias = null) Adds a INNER JOIN clause to the query using the WorkflowInstance relation
 *
 * @method TaskAttachmentQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method TaskAttachmentQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method TaskAttachmentQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method TaskAttachment findOne(PropelPDO $con = null) Return the first TaskAttachment matching the query
 * @method TaskAttachment findOneOrCreate(PropelPDO $con = null) Return the first TaskAttachment matching the query, or a new TaskAttachment object populated from the query conditions when no match is found
 *
 * @method TaskAttachment findOneByTaskinstanceId(int $TASKINSTANCE_ID) Return the first TaskAttachment filtered by the TASKINSTANCE_ID column
 * @method TaskAttachment findOneByWorkflowinstanceId(int $WORKFLOWINSTANCE_ID) Return the first TaskAttachment filtered by the WORKFLOWINSTANCE_ID column
 * @method TaskAttachment findOneByUsuarioId(int $USUARIO_ID) Return the first TaskAttachment filtered by the USUARIO_ID column
 * @method TaskAttachment findOneByFilename(string $FILENAME) Return the first TaskAttachment filtered by the FILENAME column
 * @method TaskAttachment findOneByOriginalFilename(string $ORIGINAL_FILENAME) Return the first TaskAttachment filtered by the ORIGINAL_FILENAME column
 * @method TaskAttachment findOneByPathAbsolute(string $PATH_ABSOLUTE) Return the first TaskAttachment filtered by the PATH_ABSOLUTE column
 * @method TaskAttachment findOneByPathRelative(string $PATH_RELATIVE) Return the first TaskAttachment filtered by the PATH_RELATIVE column
 * @method TaskAttachment findOneByFileSize(int $FILE_SIZE) Return the first TaskAttachment filtered by the FILE_SIZE column
 * @method TaskAttachment findOneByMimeType(string $MIME_TYPE) Return the first TaskAttachment filtered by the MIME_TYPE column
 * @method TaskAttachment findOneByDescripcion(string $DESCRIPCION) Return the first TaskAttachment filtered by the DESCRIPCION column
 * @method TaskAttachment findOneByIsPublic(int $IS_PUBLIC) Return the first TaskAttachment filtered by the IS_PUBLIC column
 * @method TaskAttachment findOneByFechaCreacion(string $FECHA_CREACION) Return the first TaskAttachment filtered by the FECHA_CREACION column
 * @method TaskAttachment findOneByFechaModificacion(string $FECHA_MODIFICACION) Return the first TaskAttachment filtered by the FECHA_MODIFICACION column
 *
 * @method array findByTaskattachmentId(int $TASKATTACHMENT_ID) Return TaskAttachment objects filtered by the TASKATTACHMENT_ID column
 * @method array findByTaskinstanceId(int $TASKINSTANCE_ID) Return TaskAttachment objects filtered by the TASKINSTANCE_ID column
 * @method array findByWorkflowinstanceId(int $WORKFLOWINSTANCE_ID) Return TaskAttachment objects filtered by the WORKFLOWINSTANCE_ID column
 * @method array findByUsuarioId(int $USUARIO_ID) Return TaskAttachment objects filtered by the USUARIO_ID column
 * @method array findByFilename(string $FILENAME) Return TaskAttachment objects filtered by the FILENAME column
 * @method array findByOriginalFilename(string $ORIGINAL_FILENAME) Return TaskAttachment objects filtered by the ORIGINAL_FILENAME column
 * @method array findByPathAbsolute(string $PATH_ABSOLUTE) Return TaskAttachment objects filtered by the PATH_ABSOLUTE column
 * @method array findByPathRelative(string $PATH_RELATIVE) Return TaskAttachment objects filtered by the PATH_RELATIVE column
 * @method array findByFileSize(int $FILE_SIZE) Return TaskAttachment objects filtered by the FILE_SIZE column
 * @method array findByMimeType(string $MIME_TYPE) Return TaskAttachment objects filtered by the MIME_TYPE column
 * @method array findByDescripcion(string $DESCRIPCION) Return TaskAttachment objects filtered by the DESCRIPCION column
 * @method array findByIsPublic(int $IS_PUBLIC) Return TaskAttachment objects filtered by the IS_PUBLIC column
 * @method array findByFechaCreacion(string $FECHA_CREACION) Return TaskAttachment objects filtered by the FECHA_CREACION column
 * @method array findByFechaModificacion(string $FECHA_MODIFICACION) Return TaskAttachment objects filtered by the FECHA_MODIFICACION column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTaskAttachmentQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTaskAttachmentQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = null, $modelName = null, $modelAlias = null)
    {
        if (null === $dbName) {
            $dbName = 'propel';
        }
        if (null === $modelName) {
            $modelName = 'TaskAttachment';
        }
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TaskAttachmentQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param   TaskAttachmentQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TaskAttachmentQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TaskAttachmentQuery) {
            return $criteria;
        }
        $query = new TaskAttachmentQuery(null, null, $modelAlias);

        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query 
     * @param     PropelPDO $con an optional connection object
     *
     * @return   TaskAttachment|TaskAttachment[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TaskAttachmentPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is already in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TaskAttachmentPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Alias of findPk to use instance pooling
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TaskAttachment A model object, or null if the key is not found
     * @throws PropelException
     */
     public function findOneByTaskattachmentId($key, $con = null)
     {
        return $this->findPk($key, $con);
     }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return                 TaskAttachment A model object, or null if the key is not found
     * @throws PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT [TASKATTACHMENT_ID], [TASKINSTANCE_ID], [WORKFLOWINSTANCE_ID], [USUARIO_ID], [FILENAME], [ORIGINAL_FILENAME], [PATH_ABSOLUTE], [PATH_RELATIVE], [FILE_SIZE], [MIME_TYPE], [DESCRIPCION], [IS_PUBLIC], [FECHA_CREACION], [FECHA_MODIFICACION] FROM [TASK_ATTACHMENT] WHERE [TASKATTACHMENT_ID] = :p0';
        try {
            $stmt = $con->prepare($sql);			
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new TaskAttachment();
            $obj->hydrate($row);
            TaskAttachmentPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return TaskAttachment|TaskAttachment[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|TaskAttachment[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TaskAttachmentPeer::TASKATTACHMENT_ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TaskAttachmentPeer::TASKATTACHMENT_ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the TASKATTACHMENT_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTaskattachmentId(1234); // WHERE TASKATTACHMENT_ID = 1234
     * $query->filterByTaskattachmentId(array(12, 34)); // WHERE TASKATTACHMENT_ID IN (12, 34)
     * $query->filterByTaskattachmentId(array('min' => 12)); // WHERE TASKATTACHMENT_ID >= 12
     * $query->filterByTaskattachmentId(array('max' => 12)); // WHERE TASKATTACHMENT_ID <= 12
     * </code>
     *
     * @param     mixed $taskattachmentId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByTaskattachmentId($taskattachmentId = null, $comparison = null)
    {
        if (is_array($taskattachmentId)) {
            $useMinMax = false;
            if (isset($taskattachmentId['min'])) {
                $this->addUsingAlias(TaskAttachmentPeer::TASKATTACHMENT_ID, $taskattachmentId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($taskattachmentId['max'])) {
                $this->addUsingAlias(TaskAttachmentPeer::TASKATTACHMENT_ID, $taskattachmentId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::TASKATTACHMENT_ID, $taskattachmentId, $comparison);
    }

    /**
     * Filter the query on the TASKINSTANCE_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByTaskinstanceId(1234); // WHERE TASKINSTANCE_ID = 1234
     * $query->filterByTaskinstanceId(array(12, 34)); // WHERE TASKINSTANCE_ID IN (12, 34)
     * $query->filterByTaskinstanceId(array('min' => 12)); // WHERE TASKINSTANCE_ID >= 12
     * $query->filterByTaskinstanceId(array('max' => 12)); // WHERE TASKINSTANCE_ID <= 12
     * </code>
     *
     * @see       filterByTaskInstance()
     *
     * @param     mixed $taskinstanceId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByTaskinstanceId($taskinstanceId = null, $comparison = null)
    {
        if (is_array($taskinstanceId)) {
            $useMinMax = false;
            if (isset($taskinstanceId['min'])) {
                $this->addUsingAlias(TaskAttachmentPeer::TASKINSTANCE_ID, $taskinstanceId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($taskinstanceId['max'])) {
                $this->addUsingAlias(TaskAttachmentPeer::TASKINSTANCE_ID, $taskinstanceId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::TASKINSTANCE_ID, $taskinstanceId, $comparison);
    }

    /**
     * Filter the query on the WORKFLOWINSTANCE_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByWorkflowinstanceId(1234); // WHERE WORKFLOWINSTANCE_ID = 1234
     * $query->filterByWorkflowinstanceId(array(12, 34)); // WHERE WORKFLOWINSTANCE_ID IN (12, 34)
     * $query->filterByWorkflowinstanceId(array('min' => 12)); // WHERE WORKFLOWINSTANCE_ID >= 12
     * $query->filterByWorkflowinstanceId(array('max' => 12)); // WHERE WORKFLOWINSTANCE_ID <= 12
     * </code>
     *
     * @see       filterByWorkflowInstance()
     *
     * @param     mixed $workflowinstanceId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByWorkflowinstanceId($workflowinstanceId = null, $comparison = null)
    {
        if (is_array($workflowinstanceId)) {
            $useMinMax = false;
            if (isset($workflowinstanceId['min'])) {
                $this->addUsingAlias(TaskAttachmentPeer::WORKFLOWINSTANCE_ID, $workflowinstanceId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($workflowinstanceId['max'])) {
                $this->addUsingAlias(TaskAttachmentPeer::WORKFLOWINSTANCE_ID, $workflowinstanceId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::WORKFLOWINSTANCE_ID, $workflowinstanceId, $comparison);
    }

    /**
     * Filter the query on the USUARIO_ID column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioId(1234); // WHERE USUARIO_ID = 1234
     * $query->filterByUsuarioId(array(12, 34)); // WHERE USUARIO_ID IN (12, 34)
     * $query->filterByUsuarioId(array('min' => 12)); // WHERE USUARIO_ID >= 12
     * $query->filterByUsuarioId(array('max' => 12)); // WHERE USUARIO_ID <= 12
     * </code>
     *
     * @see       filterByUsuario()
     *
     * @param     mixed $usuarioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByUsuarioId($usuarioId = null, $comparison = null)
    {
        if (is_array($usuarioId)) {
            $useMinMax = false;
            if (isset($usuarioId['min'])) {
                $this->addUsingAlias(TaskAttachmentPeer::USUARIO_ID, $usuarioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($usuarioId['max'])) {
                $this->addUsingAlias(TaskAttachmentPeer::USUARIO_ID, $usuarioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::USUARIO_ID, $usuarioId, $comparison);
    }

    /**
     * Filter the query on the FILENAME column
     *
     * Example usage:
     * <code>
     * $query->filterByFilename('fooValue');   // WHERE FILENAME = 'fooValue'
     * $query->filterByFilename('%fooValue%'); // WHERE FILENAME LIKE '%fooValue%'
     * </code>
     *
     * @param     string $filename The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByFilename($filename = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($filename)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $filename)) {
                $filename = str_replace('*', '%', $filename);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::FILENAME, $filename, $comparison);
    }

    /**
     * Filter the query on the ORIGINAL_FILENAME column
     *
     * Example usage:
     * <code>
     * $query->filterByOriginalFilename('fooValue');   // WHERE ORIGINAL_FILENAME = 'fooValue'
     * $query->filterByOriginalFilename('%fooValue%'); // WHERE ORIGINAL_FILENAME LIKE '%fooValue%'
     * </code>
     *
     * @param     string $originalFilename The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByOriginalFilename($originalFilename = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($originalFilename)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $originalFilename)) {
                $originalFilename = str_replace('*', '%', $originalFilename);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::ORIGINAL_FILENAME, $originalFilename, $comparison);
    }

    /**
     * Filter the query on the PATH_ABSOLUTE column
     *
     * Example usage:
     * <code>
     * $query->filterByPathAbsolute('fooValue');   // WHERE PATH_ABSOLUTE = 'fooValue'
     * $query->filterByPathAbsolute('%fooValue%'); // WHERE PATH_ABSOLUTE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $pathAbsolute The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByPathAbsolute($pathAbsolute = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($pathAbsolute)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $pathAbsolute)) {
                $pathAbsolute = str_replace('*', '%', $pathAbsolute);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::PATH_ABSOLUTE, $pathAbsolute, $comparison);
    }

    /**
     * Filter the query on the PATH_RELATIVE column
     *
     * Example usage:
     * <code>
     * $query->filterByPathRelative('fooValue');   // WHERE PATH_RELATIVE = 'fooValue'
     * $query->filterByPathRelative('%fooValue%'); // WHERE PATH_RELATIVE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $pathRelative The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByPathRelative($pathRelative = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($pathRelative)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $pathRelative)) {
                $pathRelative = str_replace('*', '%', $pathRelative);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::PATH_RELATIVE, $pathRelative, $comparison);
    }

    /**
     * Filter the query on the FILE_SIZE column
     *
     * Example usage:
     * <code>
     * $query->filterByFileSize(1234); // WHERE FILE_SIZE = 1234
     * $query->filterByFileSize(array(12, 34)); // WHERE FILE_SIZE IN (12, 34)
     * $query->filterByFileSize(array('min' => 12)); // WHERE FILE_SIZE >= 12
     * $query->filterByFileSize(array('max' => 12)); // WHERE FILE_SIZE <= 12
     * </code>
     *
     * @param     mixed $fileSize The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByFileSize($fileSize = null, $comparison = null)
    {
        if (is_array($fileSize)) {
            $useMinMax = false;
            if (isset($fileSize['min'])) {
                $this->addUsingAlias(TaskAttachmentPeer::FILE_SIZE, $fileSize['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fileSize['max'])) {
                $this->addUsingAlias(TaskAttachmentPeer::FILE_SIZE, $fileSize['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::FILE_SIZE, $fileSize, $comparison);
    }

    /**
     * Filter the query on the MIME_TYPE column
     *
     * Example usage:
     * <code>
     * $query->filterByMimeType('fooValue');   // WHERE MIME_TYPE = 'fooValue'
     * $query->filterByMimeType('%fooValue%'); // WHERE MIME_TYPE LIKE '%fooValue%'
     * </code>
     *
     * @param     string $mimeType The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByMimeType($mimeType = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($mimeType)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $mimeType)) {
                $mimeType = str_replace('*', '%', $mimeType);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::MIME_TYPE, $mimeType, $comparison);
    }

    /**
     * Filter the query on the DESCRIPCION column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE DESCRIPCION = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE DESCRIPCION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the IS_PUBLIC column
     *
     * Example usage:
     * <code>
     * $query->filterByIsPublic(1234); // WHERE IS_PUBLIC = 1234
     * $query->filterByIsPublic(array(12, 34)); // WHERE IS_PUBLIC IN (12, 34)
     * $query->filterByIsPublic(array('min' => 12)); // WHERE IS_PUBLIC >= 12
     * $query->filterByIsPublic(array('max' => 12)); // WHERE IS_PUBLIC <= 12
     * </code>
     *
     * @param     mixed $isPublic The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByIsPublic($isPublic = null, $comparison = null)
    {
        if (is_array($isPublic)) {
            $useMinMax = false;
            if (isset($isPublic['min'])) {
                $this->addUsingAlias(TaskAttachmentPeer::IS_PUBLIC, $isPublic['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($isPublic['max'])) {
                $this->addUsingAlias(TaskAttachmentPeer::IS_PUBLIC, $isPublic['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::IS_PUBLIC, $isPublic, $comparison);
    }

    /**
     * Filter the query on the FECHA_CREACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreacion('fooValue');   // WHERE FECHA_CREACION = 'fooValue'
     * $query->filterByFechaCreacion('%fooValue%'); // WHERE FECHA_CREACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $fechaCreacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByFechaCreacion($fechaCreacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($fechaCreacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $fechaCreacion)) {
                $fechaCreacion = str_replace('*', '%', $fechaCreacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::FECHA_CREACION, $fechaCreacion, $comparison);
    }

    /**
     * Filter the query on the FECHA_MODIFICACION column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaModificacion('fooValue');   // WHERE FECHA_MODIFICACION = 'fooValue'
     * $query->filterByFechaModificacion('%fooValue%'); // WHERE FECHA_MODIFICACION LIKE '%fooValue%'
     * </code>
     *
     * @param     string $fechaModificacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function filterByFechaModificacion($fechaModificacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($fechaModificacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $fechaModificacion)) {
                $fechaModificacion = str_replace('*', '%', $fechaModificacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TaskAttachmentPeer::FECHA_MODIFICACION, $fechaModificacion, $comparison);
    }

    /**
     * Filter the query by a related TaskInstance object
     *
     * @param   TaskInstance|PropelObjectCollection $taskInstance The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TaskAttachmentQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByTaskInstance($taskInstance, $comparison = null)
    {
        if ($taskInstance instanceof TaskInstance) {
            return $this
                ->addUsingAlias(TaskAttachmentPeer::TASKINSTANCE_ID, $taskInstance->getTaskinstanceId(), $comparison);
        } elseif ($taskInstance instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TaskAttachmentPeer::TASKINSTANCE_ID, $taskInstance->toKeyValue('PrimaryKey', 'TaskinstanceId'), $comparison);
        } else {
            throw new PropelException('filterByTaskInstance() only accepts arguments of type TaskInstance or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TaskInstance relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function joinTaskInstance($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TaskInstance');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TaskInstance');
        }

        return $this;
    }

    /**
     * Use the TaskInstance relation TaskInstance object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TaskInstanceQuery A secondary query class using the current class as primary query
     */
    public function useTaskInstanceQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinTaskInstance($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TaskInstance', 'TaskInstanceQuery');
    }

    /**
     * Filter the query by a related WorkflowInstance object
     *
     * @param   WorkflowInstance|PropelObjectCollection $workflowInstance The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TaskAttachmentQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByWorkflowInstance($workflowInstance, $comparison = null)
    {
        if ($workflowInstance instanceof WorkflowInstance) {
            return $this
                ->addUsingAlias(TaskAttachmentPeer::WORKFLOWINSTANCE_ID, $workflowInstance->getWorkflowinstanceId(), $comparison);
        } elseif ($workflowInstance instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TaskAttachmentPeer::WORKFLOWINSTANCE_ID, $workflowInstance->toKeyValue('PrimaryKey', 'WorkflowinstanceId'), $comparison);
        } else {
            throw new PropelException('filterByWorkflowInstance() only accepts arguments of type WorkflowInstance or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the WorkflowInstance relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function joinWorkflowInstance($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('WorkflowInstance');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'WorkflowInstance');
        }

        return $this;
    }

    /**
     * Use the WorkflowInstance relation WorkflowInstance object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   WorkflowInstanceQuery A secondary query class using the current class as primary query
     */
    public function useWorkflowInstanceQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinWorkflowInstance($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'WorkflowInstance', 'WorkflowInstanceQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return                 TaskAttachmentQuery The current query, for fluid interface
     * @throws PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(TaskAttachmentPeer::USUARIO_ID, $usuario->getUsuarioId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TaskAttachmentPeer::USUARIO_ID, $usuario->toKeyValue('PrimaryKey', 'UsuarioId'), $comparison);
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   TaskAttachment $taskAttachment Object to remove from the list of results
     *
     * @return TaskAttachmentQuery The current query, for fluid interface
     */
    public function prune($taskAttachment = null)
    {
        if ($taskAttachment) {
            $this->addUsingAlias(TaskAttachmentPeer::TASKATTACHMENT_ID, $taskAttachment->getTaskattachmentId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}

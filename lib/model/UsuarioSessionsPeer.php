<?php



/**
 * Skeleton subclass for performing query and update operations on the 'USUARIO_SESSIONS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * mié 17 sep 2025 14:57:42
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class UsuarioSessionsPeer extends BaseUsuarioSessionsPeer
{
    /**
     * Verifica si un usuario tiene una sesión activa
     * 
     * @param int $usuario_id
     * @return bool
    */
    public static function hasActiveSession($usuario_id)
    {
        try {
            $c = new Criteria();
            $c->add(UsuarioSessionsPeer::USUARIO_ID, $usuario_id);
            $c->add(UsuarioSessionsPeer::IS_ACTIVE, true);
            $c->add(UsuarioSessionsPeer::EXPIRES_AT, date('Y-m-d G:i:s'), Criteria::GREATER_THAN);
            
            $count = UsuarioSessionsPeer::doCount($c);

            return $count > 0;
        } catch (PropelException $th) {
            return false;
        } catch (\Exception $th) {
            return false;
        } catch (\Throwable $th) {
            return false;
        }
    }

    /**
     * Termina todas las sesiones de un usuario
     * 
     * @param int $usuario_id
    */
    public static function terminateUserSessions($usuario_id)
    {
        try {
            $c = new Criteria();
            $c->add(UsuarioSessionsPeer::USUARIO_ID, $usuario_id);
            $c->add(UsuarioSessionsPeer::IS_ACTIVE, true);
            
            $activeSessions = UsuarioSessionsPeer::doSelect($c);
            
            foreach ($activeSessions as $usession) {
                $usession->setUpdatedAt(date("Y-m-d G:i:s"));
                $usession->setIsActive(0);
                $usession->save();
            }
        } catch (PropelException $th) {
            error_log("Error terminating user sessions: " . $th->getMessage());
        } catch (\Exception $th) {
            error_log("Error terminating user sessions: " . $th->getMessage());
        } catch (\Throwable $th) {
            error_log("Error terminating user sessions: " . $th->getMessage());
        }
    }
}

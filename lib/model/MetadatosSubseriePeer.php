<?php



/**
 * Skeleton subclass for performing query and update operations on the 'metadatos_subserie' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 11/30/21 22:54:51
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class MetadatosSubseriePeer extends BaseMetadatosSubseriePeer
{
    public static function addNewMetadatoSubserie($metadato_id,$subserie_id){
        try {
            if($metadato_id == null || $subserie_id == null){ return false; }
            if(MetadatosSubseriePeer::isExistMetadatoSubserie($metadato_id,$subserie_id)){ return false; }
            //*********************************************************************
            $object = new MetadatosSubserie();
            $object->setMetadatoId($metadato_id);
            $object->setSubserieId($subserie_id);
            $object->save();
            //*********************************************************************
            if($object->getPrimaryKey()){ return true; }else{ return false; }
        } catch (\Throwable $th) {
            $msgerror = $th->getMessage();
            return false;
        }        
    }

    public static function isExistMetadatoSubserie($metadato_id,$subserie_id){
        try {
            $c = new Criteria();
            $c->add(MetadatosSubseriePeer::METADATO_ID,$metadato_id);
            $c->add(MetadatosSubseriePeer::SUBSERIE_ID,$subserie_id);
            return MetadatosSubseriePeer::doCount($c);
        } catch (\Throwable $th) {
            $msgerror = $th->getMessage();
            return 0;
        }
    }

    public static function getListMetadatosBySubserie($subserie_id, $isSearch=false){
        try {
            $c = new Criteria();            
            $c->add(MetadatosSubseriePeer::SUBSERIE_ID,$subserie_id);
            $c->add(MetadatosPeer::ES_ACTIVO,true);
            //**********************************************************************
            if($isSearch){
                $c->add(MetadatosPeer::ES_CONSULTA,true);
            }
            //**********************************************************************
            return MetadatosSubseriePeer::doSelectJoinMetadatos($c,null,Criteria::INNER_JOIN);
        } catch (\Throwable $th) {
            $msgerror = $th->getMessage();
            return null;
        }
    }
}

<?php



/**
 * Skeleton subclass for performing query and update operations on the 'EMAIL_ACCOUNT_USUARIO' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * dom 20 jul 2025 14:28:46
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class EmailAccountUsuarioPeer extends BaseEmailAccountUsuarioPeer
{
    public static function getUserAccessEmailAccount($account_login,$currentuser_id,$esta_activa = 1)
    {
        try {
            $c = new Criteria();
            $c->addJoin(EmailAccountPeer::EMAILACCOUNT_ID,EmailAccountUsuarioPeer::EMAILACCOUNT_ID,Criteria::INNER_JOIN);
            $c->add(EmailAccountPeer::ACCOUNT_LOGIN,$account_login);
            $c->add(EmailAccountUsuarioPeer::USUARIO_ID,$currentuser_id);
            $c->add(EmailAccountUsuarioPeer::ESTA_ACTIVA,$esta_activa);
            //**********************************************************************************************************
            return (EmailAccountPeer::doCount($c) > 0 ? true : false);
        } catch (PropelException $th) {
            return false;
        } catch (\Exception $th) {
            return false;
        } catch (\Throwable $th) {
            return false;
        }
    }

    public static function getEmailAccountByCurrentUser($currentuser_id,$esta_activa = 1)
    {
        $list_email = array();
        //**************************************************************************************************************
        try {
            $listall = sfContext::getInstance()->getUser()->checkPerm('EMAIL_LIST_ALL_ACCOUNT', $currentuser_id);
            //**********************************************************************************************************
            $c = new Criteria();
            $c->addJoin(EmailAccountPeer::EMAILACCOUNT_ID,EmailAccountUsuarioPeer::EMAILACCOUNT_ID,Criteria::INNER_JOIN);
            //**********************************************************************************************************
            if(!$listall){ $c->add(EmailAccountUsuarioPeer::USUARIO_ID,$currentuser_id); }
            //**********************************************************************************************************
            $c->add(EmailAccountUsuarioPeer::ESTA_ACTIVA,$esta_activa);
            $object_list = EmailAccountPeer::doSelect($c);
            //**********************************************************************************************************
            $ilist_account = array();
            foreach ($object_list as $row_object) {
                if(!in_array($row_object->getAccountLogin() , $ilist_account)) {
                    $ilist_account[] = $row_object->getAccountLogin();
                    $list_email[] = $row_object;
                }
            }
            //**********************************************************************************************************
            return $list_email;
        } catch (PropelException $th) {
            return array();
        } catch (\Exception $th) {
            return array();
        } catch (\Throwable $th) {
            return array();
        }
    }

    public static function addAutorizationAccountSync($emailaccount_id,$usuario_id,$esta_activa = 1)
    {
        try {
            $object = new EmailAccountUsuario();
            $object->setEmailaccountId($emailaccount_id);
            $object->setUsuarioId($usuario_id);
            $object->setFechaCreacion(date('Y-m-d G:i:s'));
            $object->setFechaModificacion(date('Y-m-d G:i:s'));
            $object->setEstaActiva($esta_activa);    
            $object->save();            
            return $object->getPrimaryKey() != null ? true : false;
        } catch (PropelException $th) {
            return false;
        } catch (\Exception $th) {
            return false;
        } catch (\Throwable $th) {
            return false;
        }
    }
}

<?php



/**
 * Skeleton subclass for performing query and update operations on the 'metadatos_expediente' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * 12/02/21 21:45:53
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class MetadatosExpedientePeer extends BaseMetadatosExpedientePeer
{
    public static function addNewMetadatoExpediente($info_metadato = array()){
        try {
            if(!isset($info_metadato['usuario_id'])){ return false; }
            if(!isset($info_metadato['metadato_id'])){ return false; }
            if(!isset($info_metadato['unidaddocumental_id'])){ return false; }
            if(!isset($info_metadato['value_metadato'])){ return false; }
            //*********************************************************************
            //if(MetadatosSubseriePeer::isExistMetadatoSubserie($metadato_id,$subserie_id)){ return false; }
            //*********************************************************************
            $object = new MetadatosExpediente();
            $object->setUsuarioId($info_metadato['usuario_id']);
            $object->setMetadatoId($info_metadato['metadato_id']);
            $object->setUnidaddocumentalId($info_metadato['unidaddocumental_id']);
            $object->setValueField($info_metadato['value_metadato']);
            $object->setFechaCreacion($info_metadato['fecha_creacion']);
            $object->save();
            //*********************************************************************
            if($object->getPrimaryKey()){ return true; }else{ return false; }
        } catch (\Throwable $th) {
            $msgerror = $th->getMessage();
            return false;
        }        
    }

    public static function isExistMetadatoSubserie($metadato_id,$subserie_id){
        /*try {
            $c = new Criteria();
            $c->add(MetadatosSubseriePeer::METADATO_ID,$metadato_id);
            $c->add(MetadatosSubseriePeer::SUBSERIE_ID,$subserie_id);
            return MetadatosSubseriePeer::doCount($c);
        } catch (\Throwable $th) {
            $msgerror = $th->getMessage();
            return 0;
        }*/
    }

    public static function getListMetadatosByExpediente($unidaddocumental_id){
        try {
            $c = new Criteria();
            $c->add(MetadatosExpedientePeer::UNIDADDOCUMENTAL_ID,$unidaddocumental_id);            
            return MetadatosExpedientePeer::doSelect($c);
        } catch (\Throwable $th) {
            $msgerror = $th->getMessage();
            return null;
        }
    }
}

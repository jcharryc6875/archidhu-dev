<?php



/**
 * Skeleton subclass for performing query and update operations on the 'EMAIL_TRACKING' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.7.1 on:
 *
 * mié 27 ago 2025 20:28:54
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class EmailTrackingPeer extends BaseEmailTrackingPeer
{
    public static function isMessageProcessed($messageId, $accountEmail, $message_uid = null) 
    {
        try {
            $c = new Criteria();
            $c->addJoin(EmailTrackingPeer::EMAILACCOUNT_ID, EmailAccountPeer::EMAILACCOUNT_ID, Criteria::INNER_JOIN);
            $c->add(EmailAccountPeer::ACCOUNT_LOGIN, $accountEmail);
            $c->add(EmailTrackingPeer::MESSAGE_ID, $messageId);
            //****************************************************************************************
            if(!empty($message_uid)){
                $c->add(EmailTrackingPeer::UID, $message_uid);
            }
            //****************************************************************************************
            $object = EmailTrackingPeer::doCount($c);
            //****************************************************************************************
            return $object > 0 ? true : false;
        } catch (PropelException $th) {
            return false;
        } catch (\Exception $th) {
            return false;
        } catch (\Throwable $th) {
            return false;
        }
    }
    
    public static function addProccesedMessageUid($emailaccount_id = 0, $message_id = 0, $messageUid = 0)
    {
        try {
            $object_sync = new EmailTracking();
            $object_sync->setMessageId($message_id);
            $object_sync->setEmailaccountId($emailaccount_id);
            $object_sync->setUid($messageUid);
            $object_sync->setProcessedAt(date("Y-m-d G:i:s"));
            $object_sync->setStatus(strtoupper(EmailStatus::getEnumName(EmailStatus::Descargado)));
            $object_sync->save();
            //****************************************************************************************
            return true;
        } catch (PropelException $th) {
            return false;
        } catch (\Exception $th) {
            return false;
        } catch (\Throwable $th) {
            return false;
        }
    }
}
